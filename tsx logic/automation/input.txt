```tsx
import React from 'react';
Audio: "Program number one is SNAP for Seniors — the Supplemental Nutrition Assistance Program, formerly food stamps. Many seniors say 'I don't need that, give it to a young family.'"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11_SnapIntro',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_SnapIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Program label entrance
  const labelY = interpolate(sp(5), [0, 1], [-80, 0]);
  const labelOp = interpolate(sp(5), [0, 1], [0, 1]);

  // Big "01" number
  const numScale = sp(15, { damping: 8, stiffness: 90 });
  const numRotate = interpolate(sp(15), [0, 1], [-15, 0]);

  // SNAP word reveal
  const snapScale = sp(35);
  const snapBlur = interpolate(sp(35), [0, 1], [20, 0]);

  // Underline draw
  const underlineW = interpolate(frame, [50, 80], [0, 600], { extrapolateRight: 'clamp' });

  // Subtitle
  const subOp = interpolate(frame, [70, 95], [0, 1], { extrapolateRight: 'clamp' });
  const subY = interpolate(sp(70), [0, 1], [40, 0]);

  // EBT Card animation
  const cardScale = sp(110, { damping: 10, stiffness: 80 });
  const cardRotate = interpolate(frame, [110, 200], [-8, -5]);

  // Quote bubble
  const quoteOp = interpolate(frame, [180, 210], [0, 1], { extrapolateRight: 'clamp' });
  const quoteScale = sp(180);

  // Camera subtle zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 20%, rgba(250,185,91,0.18) 0%, transparent 55%), radial-gradient(circle at 80% 80%, rgba(232,237,242,0.06) 0%, transparent 50%), linear-gradient(135deg, #412D15 0%, #2A1D0E 100%)',
        }}
      />

      {/* Animated shape layers */}
      {[0, 1, 2, 3, 4].map((i) => {
        const seed = seededRandom(i + 1);
        const size = 280 + seed * 320;
        const x = seed * 1700;
        const y = seededRandom(i + 8) * 900;
        const drift = Math.sin(frame * 0.008 + i) * 30;
        const rot = frame * 0.15 + i * 60;
        return (
          <div
            key={`shape-${i}`}
            style={{
              position: 'absolute',
              left: x + drift,
              top: y + drift,
              width: size,
              height: size,
              border: `2px solid #FAB95B`,
              borderRadius: i % 2 === 0 ? '50%' : '12px',
              opacity: 0.05,
              transform: `rotate(${rot}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
            }}
          />
        );
      })}

      {/* Motion texture - diagonal grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="grid11" width="80" height="80" patternUnits="userSpaceOnUse" patternTransform={`translate(${frame * 0.3}, ${frame * 0.3})`}>
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid11)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const s = seededRandom(i + 50);
        const px = s * 1920;
        const py = (seededRandom(i + 70) * 1080 + frame * 0.6) % 1080;
        const size = 2 + s * 4;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: size,
              height: size,
              background: '#FAB95B',
              borderRadius: '50%',
              opacity: 0.35,
              boxShadow: '0 0 8px #FAB95B',
            }}
          />
        );
      })}

      {/* Main content with camera zoom */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        {/* Top program label */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: '50%',
            transform: `translateX(-50%) translateY(${labelY}px)`,
            opacity: labelOp,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div style={{ width: 60, height: 3, background: '#FAB95B' }} />
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            Program · Free Money for Seniors
          </div>
          <div style={{ width: 60, height: 3, background: '#FAB95B' }} />
        </div>

        {/* Giant 01 + SNAP layout */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            left: 120,
            display: 'flex',
            alignItems: 'flex-start',
            gap: 40,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 380,
              fontWeight: 900,
              color: 'transparent',
              WebkitTextStroke: '4px #FAB95B',
              lineHeight: 0.85,
              transform: `scale(${numScale}) rotate(${numRotate}deg)`,
              transformOrigin: 'left top',
            }}
          >
            01
          </div>

          <div style={{ paddingTop: 40 }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 44,
                fontWeight: 600,
                color: '#E8EDF2',
                opacity: 0.7,
                letterSpacing: 3,
                marginBottom: 8,
              }}
            >
              PROGRAM ONE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 238,
                fontWeight: 900,
                color: '#E8EDF2',
                lineHeight: 0.9,
                transform: `scale(${snapScale})`,
                filter: `blur(${snapBlur}px)`,
                transformOrigin: 'left center',
                letterSpacing: -4,
              }}
            >
              SNAP
            </div>
            <div
              style={{
                width: underlineW,
                height: 8,
                background: 'linear-gradient(90deg, #FAB95B 0%, transparent 100%)',
                marginTop: 4,
              }}
            />
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 51,
                fontWeight: 500,
                color: '#FAB95B',
                opacity: subOp,
                transform: `translateY(${subY}px)`,
                marginTop: 24,
                letterSpacing: 1,
              }}
            >
              for Seniors
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 30,
                fontWeight: 400,
                color: '#E8EDF2',
                opacity: subOp * 0.6,
                marginTop: 16,
                letterSpacing: 4,
                textTransform: 'uppercase',
              }}
            >
              Supplemental Nutrition Assistance Program
            </div>
          </div>
        </div>

        {/* EBT Card mockup */}
        <div
          style={{
            position: 'absolute',
            bottom: 180,
            left: 140,
            width: 480,
            height: 290,
            background: 'linear-gradient(135deg, #FAB95B 0%, #d99a45 100%)',
            borderRadius: 24,
            transform: `scale(${cardScale}) rotate(${cardRotate}deg)`,
            transformOrigin: 'center',
            boxShadow: '0 30px 60px rgba(0,0,0,0.5), 0 0 0 1px rgba(232,237,242,0.1)',
            padding: 32,
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'space-between',
          }}
        >
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start' }}>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 23,
                fontWeight: 700,
                color: '#412D15',
                letterSpacing: 2,
              }}
            >
              EBT · BENEFITS CARD
            </div>
            {/* Chip SVG */}
            <svg width="60" height="48" viewBox="0 0 60 48">
              <rect x="0" y="0" width="60" height="48" rx="6" fill="#412D15" opacity="0.4" />
              <rect x="8" y="8" width="44" height="32" rx="3" fill="none" stroke="#412D15" strokeWidth="2" />
              <line x1="8" y1="20" x2="52" y2="20" stroke="#412D15" strokeWidth="1.5" />
              <line x1="8" y1="28" x2="52" y2="28" stroke="#412D15" strokeWidth="1.5" />
              <line x1="20" y1="8" x2="20" y2="40" stroke="#412D15" strokeWidth="1.5" />
              <line x1="40" y1="8" x2="40" y2="40" stroke="#412D15" strokeWidth="1.5" />
            </svg>
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 32,
              fontWeight: 700,
              color: '#412D15',
              letterSpacing: 4,
            }}
          >
            •••• •••• •••• 2026
          </div>
          <div style={{ display: 'flex', justifyContent: 'space-between' }}>
            <div>
              <div style={{ fontFamily: 'Inter', fontSize: 14, color: '#412D15', opacity: 0.7 }}>CARDHOLDER</div>
              <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#412D15' }}>SENIOR BENEFITS</div>
            </div>
            <div>
              <div style={{ fontFamily: 'Inter', fontSize: 14, color: '#412D15', opacity: 0.7 }}>FOOD ASSISTANCE</div>
              <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#412D15' }}>USDA · GOV</div>
            </div>
          </div>
        </div>

        {/* Quote bubble - "I don't need that" */}
        <div
          style={{
            position: 'absolute',
            bottom: 280,
            right: 100,
            opacity: quoteOp,
            transform: `scale(${quoteScale})`,
            transformOrigin: 'right center',
            maxWidth: 620,
          }}
        >
          <div
            style={{
              background: 'rgba(232,237,242,0.06)',
              border: '2px solid #E8EDF2',
              borderRadius: 24,
              padding: '36px 44px',
              position: 'relative',
            }}
          >
            <div
              style={{
                position: 'absolute',
                top: -20,
                left: 60,
                fontFamily: 'Inter',
                fontSize: 120,
                color: '#FAB95B',
                lineHeight: 1,
              }}
            >
              "
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 38,
                fontWeight: 500,
                color: '#E8EDF2',
                fontStyle: 'italic',
                lineHeight: 1.3,
              }}
            >
              I don't need that — give it to a young family.
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 22,
                color: '#FAB95B',
                marginTop: 16,
                letterSpacing: 2,
                textTransform: 'uppercase',
              }}
            >
              — Most Seniors
            </div>
          </div>
          {/* Red X overlay */}
          {frame > 240 && (
            <svg
              style={{
                position: 'absolute',
                top: '50%',
                left: '50%',
                transform: `translate(-50%, -50%) scale(${sp(240)})`,
              }}
              width="180"
              height="180"
              viewBox="0 0 180 180"
            >
              <line x1="30" y1="30" x2="150" y2="150" stroke="#ff0000" strokeWidth="14" strokeLinecap="round" />
              <line x1="150" y1="30" x2="30" y2="150" stroke="#ff0000" strokeWidth="14" strokeLinecap="round" />
            </svg>
          )}
        </div>

        {/* Bottom corner stat */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            right: 100,
            opacity: interpolate(frame, [260, 290], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            textAlign: 'right',
          }}
        >
          <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#E8EDF2', opacity: 0.7, letterSpacing: 3 }}>FORMERLY KNOWN AS</div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 60, fontWeight: 800, color: '#FAB95B' }}>FOOD STAMPS</div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_SnapIntro;
```

```tsx
import React from 'react';
Audio: "Swallow your pride for a second — the rules for seniors are completely different. The government has special, relaxed income limits for anyone over the age of 65."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12_SpecialRules',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_SpecialRules: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Headline reveal word by word
  const w1 = sp(10);
  const w2 = sp(25);
  const w3 = sp(40);

  // Split rule box - regular vs senior
  const leftSlide = interpolate(sp(70), [0, 1], [-400, 0]);
  const rightSlide = interpolate(sp(90), [0, 1], [400, 0]);
  const leftOp = sp(70);
  const rightOp = sp(90);

  // VS badge in middle
  const vsScale = sp(120, { damping: 8 });

  // Age 65+ stamp
  const stampScale = sp(180, { damping: 7, stiffness: 100 });
  const stampRotate = interpolate(sp(180), [0, 1], [-30, -12]);

  const camZoom = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 70% 30%, rgba(250,185,91,0.15) 0%, transparent 50%), linear-gradient(160deg, #412D15 0%, #2D1F0F 100%)',
        }}
      />

      {/* Animated shape layers - triangles */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 20);
        const size = 350 + seed * 250;
        const x = seed * 1700;
        const y = seededRandom(i + 30) * 800;
        const rot = frame * 0.2 + i * 90;
        return (
          <svg
            key={`tri-${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              opacity: 0.04,
              transform: `rotate(${rot}deg)`,
            }}
            width={size}
            height={size}
            viewBox="0 0 100 100"
          >
            <polygon points="50,10 90,90 10,90" fill="none" stroke="#FAB95B" strokeWidth="1.5" />
          </svg>
        );
      })}

      {/* Motion texture - horizontal lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        {Array.from({ length: 20 }).map((_, i) => (
          <line
            key={i}
            x1="0"
            y1={i * 60 + (frame * 0.4) % 60}
            x2="1920"
            y2={i * 60 + (frame * 0.4) % 60}
            stroke="#E8EDF2"
            strokeWidth="1"
          />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 100);
        const px = (s * 1920 + frame * 0.5) % 1920;
        const py = seededRandom(i + 120) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#FAB95B',
              borderRadius: '50%',
              opacity: 0.4,
              boxShadow: '0 0 6px #FAB95B',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        {/* Top headline */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 8,
              textTransform: 'uppercase',
              opacity: sp(0),
              marginBottom: 24,
            }}
          >
            ── Hidden Truth ──
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 110,
              fontWeight: 900,
              lineHeight: 1.05,
              letterSpacing: -2,
            }}
          >
            <span
              style={{
                color: '#E8EDF2',
                display: 'inline-block',
                opacity: w1,
                transform: `translateY(${interpolate(w1, [0, 1], [40, 0])}px)`,
              }}
            >
              THE RULES
            </span>{' '}
            <span
              style={{
                color: '#E8EDF2',
                display: 'inline-block',
                opacity: w2,
                transform: `translateY(${interpolate(w2, [0, 1], [40, 0])}px)`,
              }}
            >
              ARE
            </span>{' '}
            <span
              style={{
                color: '#FAB95B',
                display: 'inline-block',
                opacity: w3,
                transform: `translateY(${interpolate(w3, [0, 1], [40, 0])}px)`,
                textShadow: '0 0 30px rgba(250,185,91,0.5)',
              }}
            >
              DIFFERENT
            </span>
          </div>
        </div>

        {/* Split comparison cards */}
        {/* Left card - Regular Rules */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 140,
            width: 680,
            height: 480,
            background: 'rgba(232,237,242,0.04)',
            border: '2px solid rgba(232,237,242,0.25)',
            borderRadius: 20,
            padding: 50,
            transform: `translateX(${leftSlide}px)`,
            opacity: leftOp,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#E8EDF2',
              opacity: 0.6,
              letterSpacing: 4,
              marginBottom: 16,
            }}
          >
            REGULAR APPLICANT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 70,
              fontWeight: 800,
              color: '#E8EDF2',
              lineHeight: 1,
              marginBottom: 36,
            }}
          >
            Under 65
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
            {['Strict income cap', 'No medical deductions', 'Standard test required'].map((t, i) => (
              <div
                key={i}
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  gap: 18,
                  fontFamily: 'Inter, Poppins',
                  fontSize: 32,
                  fontWeight: 500,
                  color: '#E8EDF2',
                  opacity: interpolate(sp(85 + i * 8), [0, 1], [0, 0.85]),
                }}
              >
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
                  <circle cx="12" cy="12" r="11" stroke="#9E9E9E" strokeWidth="2" />
                  <line x1="7" y1="7" x2="17" y2="17" stroke="#9E9E9E" strokeWidth="2.5" />
                  <line x1="17" y1="7" x2="7" y2="17" stroke="#9E9E9E" strokeWidth="2.5" />
                </svg>
                {t}
              </div>
            ))}
          </div>
        </div>

        {/* Right card - Senior Rules */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            right: 140,
            width: 680,
            height: 480,
            background: 'linear-gradient(135deg, rgba(250,185,91,0.18) 0%, rgba(250,185,91,0.06) 100%)',
            border: '3px solid #FAB95B',
            borderRadius: 20,
            padding: 50,
            transform: `translateX(${rightSlide}px)`,
            opacity: rightOp,
            boxShadow: '0 20px 60px rgba(250,185,91,0.2)',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#FAB95B',
              letterSpacing: 4,
              marginBottom: 16,
            }}
          >
            SENIOR APPLICANT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 70,
              fontWeight: 800,
              color: '#E8EDF2',
              lineHeight: 1,
              marginBottom: 36,
            }}
          >
            Age 65 +
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
            {['Relaxed income limits', 'Medical costs deducted', 'Easier qualification'].map((t, i) => (
              <div
                key={i}
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  gap: 18,
                  fontFamily: 'Inter, Poppins',
                  fontSize: 32,
                  fontWeight: 600,
                  color: '#E8EDF2',
                  opacity: interpolate(sp(105 + i * 8), [0, 1], [0, 1]),
                }}
              >
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
                  <circle cx="12" cy="12" r="11" fill="#FAB95B" />
                  <path d="M7 12 L11 16 L17 8" stroke="#412D15" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
                {t}
              </div>
            ))}
          </div>
        </div>

        {/* VS badge */}
        <div
          style={{
            position: 'absolute',
            top: 550,
            left: '50%',
            transform: `translateX(-50%) scale(${vsScale})`,
            width: 130,
            height: 130,
            borderRadius: '50%',
            background: '#412D15',
            border: '4px solid #FAB95B',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 900,
            color: '#FAB95B',
            boxShadow: '0 0 40px rgba(250,185,91,0.4)',
          }}
        >
          VS
        </div>

        {/* Age 65+ stamp at bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: '50%',
            transform: `translateX(-50%) scale(${stampScale}) rotate(${stampRotate}deg)`,
            transformOrigin: 'center',
            padding: '20px 60px',
            border: '5px solid #ff0000',
            borderRadius: 12,
            fontFamily: 'Inter, Poppins',
            fontSize: 60,
            fontWeight: 900,
            color: '#ff0000',
            letterSpacing: 4,
            background: 'rgba(255,0,0,0.05)',
          }}
        >
          AGE 65+ QUALIFIES
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_SpecialRules;
```

```tsx
import React from 'react';
Audio: "If you're just barely over the regular SNAP limit, you may still qualify for the senior version. Depending on your state, it can add $250 or more every month onto an EBT card for groceries."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13_250PerMonth',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_250PerMonth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter animation from 0 to 250
  const counter = Math.floor(interpolate(frame, [40, 130], [0, 250], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Dollar sign entrance
  const dollarScale = sp(20, { damping: 7 });

  // Subtitle
  const subOp = sp(70);

  // Bar chart bars
  const bar1H = interpolate(sp(150), [0, 1], [0, 180]);
  const bar2H = interpolate(sp(165), [0, 1], [0, 260]);
  const bar3H = interpolate(sp(180), [0, 1], [0, 340]);
  const bar4H = interpolate(sp(195), [0, 1], [0, 420]);

  // Top label
  const labelOp = sp(0);

  // Groceries icons floating in
  const camPan = interpolate(frame, [0, durationInFrames], [0, -20]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(250,185,91,0.2) 0%, transparent 60%), linear-gradient(180deg, #412D15 0%, #2A1D0E 100%)',
        }}
      />

      {/* Animated concentric circles */}
      {[0, 1, 2, 3].map((i) => {
        const size = 600 + i * 200 + Math.sin(frame * 0.02 + i) * 30;
        return (
          <div
            key={`c-${i}`}
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: size,
              height: size,
              transform: `translate(-50%, -50%) rotate(${frame * 0.1 * (i % 2 === 0 ? 1 : -1)}deg)`,
              border: `1.5px dashed #FAB95B`,
              borderRadius: '50%',
              opacity: 0.06,
            }}
          />
        );
      })}

      {/* Motion texture - dot grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="dots13" width="50" height="50" patternUnits="userSpaceOnUse" patternTransform={`translate(0, ${frame * 0.4})`}>
            <circle cx="25" cy="25" r="1.5" fill="#FAB95B" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots13)" />
      </svg>

      {/* Floating dollar particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 200);
        const px = s * 1920;
        const py = (seededRandom(i + 210) * 1080 - frame * 0.8 + 1080) % 1080;
        const rot = Math.sin(frame * 0.03 + i) * 15;
        return (
          <div
            key={`dl-${i}`}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              fontFamily: 'Inter',
              fontSize: 24 + s * 20,
              fontWeight: 800,
              color: '#FAB95B',
              opacity: 0.18,
              transform: `rotate(${rot}deg)`,
            }}
          >
            $
          </div>
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camPan}px)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: labelOp,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              color: '#FAB95B',
              letterSpacing: 12,
              textTransform: 'uppercase',
            }}
          >
            ▸ Senior SNAP · Monthly Benefit ◂
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 700,
              color: '#E8EDF2',
              marginTop: 16,
              opacity: 0.85,
            }}
          >
            On Your EBT Card, Every Month
          </div>
        </div>

        {/* Giant counter */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            left: 0,
            right: 0,
            textAlign: 'center',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'flex-start',
            gap: 12,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 220,
              fontWeight: 900,
              color: '#FAB95B',
              transform: `scale(${dollarScale})`,
              lineHeight: 1,
              textShadow: '0 0 60px rgba(250,185,91,0.5)',
            }}
          >
            $
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 320,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              letterSpacing: -8,
              textShadow: '0 0 80px rgba(232,237,242,0.3)',
            }}
          >
            {counter}
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 600,
              color: '#FAB95B',
              opacity: subOp,
              alignSelf: 'flex-end',
              marginBottom: 60,
              marginLeft: 16,
            }}
          >
            +
          </div>
        </div>

        {/* Per month label */}
        <div
          style={{
            position: 'absolute',
            top: 620,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: subOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            Every Single Month
          </div>
          <div style={{ width: 200, height: 3, background: '#FAB95B', margin: '24px auto' }} />
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              color: '#E8EDF2',
              opacity: 0.7,
              fontStyle: 'italic',
            }}
          >
            * varies by state and household
          </div>
        </div>

        {/* Bottom growth bars - showing escalation */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 140,
            display: 'flex',
            alignItems: 'flex-end',
            gap: 32,
          }}
        >
          {[
            { h: bar1H, label: 'Q1', val: '$60' },
            { h: bar2H, label: 'Q2', val: '$130' },
            { h: bar3H, label: 'Q3', val: '$195' },
            { h: bar4H, label: 'Q4', val: '$250' },
          ].map((b, i) => (
            <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 22,
                  color: '#FAB95B',
                  opacity: b.h > 10 ? 1 : 0,
                  fontWeight: 700,
                }}
              >
                {b.val}
              </div>
              <div
                style={{
                  width: 90,
                  height: b.h,
                  background: `linear-gradient(180deg, #FAB95B 0%, rgba(250,185,91,0.4) 100%)`,
                  borderRadius: '8px 8px 0 0',
                  boxShadow: '0 0 20px rgba(250,185,91,0.3)',
                }}
              />
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 20,
                  color: '#E8EDF2',
                  opacity: 0.6,
                  marginTop: 4,
                }}
              >
                {b.label}
              </div>
            </div>
          ))}
        </div>

        {/* Right side grocery icon stack */}
        <div
          style={{
            position: 'absolute',
            right: 140,
            bottom: 130,
            display: 'flex',
            flexDirection: 'column',
            gap: 20,
            opacity: sp(160),
          }}
        >
          {/* Bread */}
          <svg width="80" height="80" viewBox="0 0 64 64">
            <ellipse cx="32" cy="38" rx="26" ry="18" fill="#FAB95B" opacity="0.9" />
            <path d="M10 38 Q32 18 54 38" stroke="#412D15" strokeWidth="2" fill="none" />
            <line x1="20" y1="32" x2="22" y2="36" stroke="#412D15" strokeWidth="2" />
            <line x1="32" y1="28" x2="34" y2="32" stroke="#412D15" strokeWidth="2" />
            <line x1="44" y1="32" x2="46" y2="36" stroke="#412D15" strokeWidth="2" />
          </svg>
          {/* Apple */}
          <svg width="80" height="80" viewBox="0 0 64 64">
            <circle cx="32" cy="36" r="22" fill="#E8EDF2" />
            <path d="M32 14 Q34 8 40 8" stroke="#FAB95B" strokeWidth="3" fill="none" strokeLinecap="round" />
            <ellipse cx="38" cy="12" rx="6" ry="3" fill="#FAB95B" transform="rotate(-30 38 12)" />
          </svg>
          {/* Milk carton */}
          <svg width="80" height="80" viewBox="0 0 64 64">
            <path d="M18 18 L18 56 L46 56 L46 18 L40 10 L24 10 Z" fill="#E8EDF2" stroke="#412D15" strokeWidth="1.5" />
            <rect x="24" y="28" width="16" height="14" fill="#FAB95B" />
            <text x="32" y="39" fontFamily="Inter" fontSize="9" fontWeight="800" fill="#412D15" textAnchor="middle">MILK</text>
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_250PerMonth;
```

```tsx
import React from 'react';
Audio: "That's $3,000 a year in free food — and it's not just for zero income. If medical expenses are high, the government deducts those costs from your income, so your SNAP benefit goes up."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14_3000Year',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_3000Year: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Multiplication equation: $250 x 12 = $3000
  const eq1 = sp(10);
  const eq2 = sp(40);
  const eq3 = sp(70);
  const equalsScale = sp(100, { damping: 8 });
  const resultScale = sp(115, { damping: 7, stiffness: 100 });

  // Formula box at bottom
  const formulaOp = sp(160);

  // Arrow up indicator
  const arrowOp = interpolate(frame, [220, 250], [0, 1], { extrapolateRight: 'clamp' });
  const arrowY = interpolate(frame, [220, 270], [20, 0], { extrapolateRight: 'clamp' });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Screen shake on impact
  const shakeX = frame >= 115 && frame < 135 ? Math.sin(frame * 3) * 6 * (1 - (frame - 115) / 20) : 0;
  const shakeY = frame >= 115 && frame < 135 ? Math.cos(frame * 3) * 4 * (1 - (frame - 115) / 20) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 20% 80%, rgba(250,185,91,0.16) 0%, transparent 50%), radial-gradient(circle at 80% 20%, rgba(232,237,242,0.05) 0%, transparent 50%), linear-gradient(135deg, #412D15 0%, #2D1F0F 100%)',
        }}
      />

      {/* Animated shape layers - rectangles */}
      {[0, 1, 2, 3, 4].map((i) => {
        const seed = seededRandom(i + 40);
        const w = 200 + seed * 280;
        const h = 100 + seed * 180;
        const x = seed * 1700;
        const y = seededRandom(i + 50) * 900;
        const drift = Math.sin(frame * 0.01 + i) * 25;
        return (
          <div
            key={`r-${i}`}
            style={{
              position: 'absolute',
              left: x + drift,
              top: y - drift,
              width: w,
              height: h,
              border: '2px solid #FAB95B',
              opacity: 0.04,
              borderRadius: 8,
              transform: `rotate(${i * 15 + frame * 0.05}deg)`,
            }}
          />
        );
      })}

      {/* Motion texture - vertical lines moving */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        {Array.from({ length: 16 }).map((_, i) => (
          <line
            key={i}
            x1={i * 120 + (frame * 0.5) % 120}
            y1="0"
            x2={i * 120 + (frame * 0.5) % 120}
            y2="1080"
            stroke="#FAB95B"
            strokeWidth="1"
          />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const s = seededRandom(i + 300);
        const px = s * 1920;
        const py = (seededRandom(i + 310) * 1080 + frame * 0.7) % 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 2 + s * 4,
              height: 2 + s * 4,
              background: '#FAB95B',
              borderRadius: '50%',
              opacity: 0.3,
              boxShadow: '0 0 6px #FAB95B',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom}) translate(${shakeX}px, ${shakeY}px)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(0),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 10,
              textTransform: 'uppercase',
            }}
          >
            ╋ Annual Free Food Value ╋
          </div>
        </div>

        {/* Equation row */}
        <div
          style={{
            position: 'absolute',
            top: 240,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 40,
          }}
        >
          {/* $250 */}
          <div
            style={{
              opacity: eq1,
              transform: `scale(${eq1})`,
              textAlign: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 153,
                fontWeight: 900,
                color: '#E8EDF2',
                lineHeight: 1,
              }}
            >
              $250
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 30,
                color: '#FAB95B',
                letterSpacing: 4,
                marginTop: 8,
                textTransform: 'uppercase',
              }}
            >
              per month
            </div>
          </div>

          {/* × */}
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 800,
              color: '#FAB95B',
              opacity: eq2,
              transform: `rotate(${interpolate(eq2, [0, 1], [180, 0])}deg)`,
            }}
          >
            ×
          </div>

          {/* 12 */}
          <div
            style={{
              opacity: eq3,
              transform: `scale(${eq3})`,
              textAlign: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 153,
                fontWeight: 900,
                color: '#E8EDF2',
                lineHeight: 1,
              }}
            >
              12
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 30,
                color: '#FAB95B',
                letterSpacing: 4,
                marginTop: 8,
                textTransform: 'uppercase',
              }}
            >
              months
            </div>
          </div>

          {/* = */}
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 800,
              color: '#FAB95B',
              transform: `scale(${equalsScale})`,
            }}
          >
            =
          </div>
        </div>

        {/* RESULT - giant $3,000 */}
        <div
          style={{
            position: 'absolute',
            top: 520,
            left: 0,
            right: 0,
            textAlign: 'center',
            transform: `scale(${resultScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 340,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 1,
              letterSpacing: -8,
              textShadow: '0 0 80px rgba(250,185,91,0.6)',
            }}
          >
            $3,000
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: 6,
              textTransform: 'uppercase',
              marginTop: -20,
            }}
          >
            in free food · per year
          </div>
        </div>

        {/* Bottom formula card */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(formulaOp, [0, 1], [40, 0])}px)`,
            opacity: formulaOp,
            background: 'rgba(232,237,242,0.05)',
            border: '2px solid #FAB95B',
            borderRadius: 16,
            padding: '24px 60px',
            display: 'flex',
            alignItems: 'center',
            gap: 40,
          }}
        >
          <div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#FAB95B', letterSpacing: 3, marginBottom: 6 }}>
              HIDDEN FORMULA
            </div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700, color: '#E8EDF2' }}>
              Medical Costs &nbsp;<span style={{ color: '#FAB95B' }}>−</span>&nbsp; Income &nbsp;<span style={{ color: '#FAB95B' }}>=</span>&nbsp; Higher Benefit
            </div>
          </div>

          {/* Arrow up icon */}
          <div
            style={{
              opacity: arrowOp,
              transform: `translateY(${arrowY}px)`,
              display: 'flex',
              alignItems: 'center',
              gap: 8,
            }}
          >
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <path d="M12 4 L12 20" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" />
              <path d="M5 11 L12 4 L19 11" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 800, color: '#FAB95B' }}>UP</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_3000Year;
```

```tsx
import React from 'react';
Audio: "It's a brilliant hidden math formula to keep seniors from going hungry. If you're choosing between vegetables and a bill, apply today. Program number two is the Medicare Extra Help Program."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15_HiddenFormula',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_HiddenFormula: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = sp(5);
  const titleScale = sp(5);

  // Vegetable vs Bill weighing scale
  const leftPlateY = interpolate(frame, [80, 120], [0, 30], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const rightPlateY = interpolate(frame, [80, 120], [0, -30], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const scaleRot = interpolate(frame, [80, 120], [0, -6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Transition to Program 2
  const transOp = interpolate(frame, [240, 280], [0, 1], { extrapolateRight: 'clamp' });
  const transSlide = interpolate(sp(240), [0, 1], [80, 0]);

  // Program 2 label
  const p2Scale = sp(290, { damping: 7, stiffness: 100 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 30%, rgba(250,185,91,0.18) 0%, transparent 55%), linear-gradient(170deg, #412D15 0%, #2A1D0E 100%)',
        }}
      />

      {/* Animated hexagons */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 60);
        const size = 280 + seed * 200;
        const x = seed * 1700;
        const y = seededRandom(i + 70) * 900;
        const rot = frame * 0.15 + i * 60;
        return (
          <svg
            key={`hex-${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              opacity: 0.05,
              transform: `rotate(${rot}deg)`,
            }}
            width={size}
            height={size}
            viewBox="0 0 100 100"
          >
            <polygon points="50,5 90,28 90,72 50,95 10,72 10,28" fill="none" stroke="#FAB95B" strokeWidth="1.5" />
          </svg>
        );
      })}

      {/* Motion texture - cross pattern */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        <defs>
          <pattern id="cross15" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform={`rotate(${frame * 0.2})`}>
            <line x1="0" y1="30" x2="60" y2="30" stroke="#E8EDF2" strokeWidth="1" />
            <line x1="30" y1="0" x2="30" y2="60" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross15)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 400);
        const px = s * 1920;
        const py = (seededRandom(i + 410) * 1080 + frame * 0.6) % 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#FAB95B',
              borderRadius: '50%',
              opacity: 0.35,
              boxShadow: '0 0 6px #FAB95B',
            }}
          />
        );
      })}

      {/* MAIN: First half - choice dilemma */}
      <AbsoluteFill style={{ opacity: interpolate(frame, [0, 30, 220, 250], [0, 1, 1, 0], { extrapolateRight: 'clamp' }) }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `scale(${titleScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              color: '#FAB95B',
              letterSpacing: 10,
              textTransform: 'uppercase',
              marginBottom: 18,
            }}
          >
            The Impossible Choice
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 102,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            Vegetables <span style={{ color: '#FAB95B' }}>OR</span> Bills?
          </div>
        </div>

        {/* Weighing scale visualization */}
        <div
          style={{
            position: 'absolute',
            top: 350,
            left: '50%',
            transform: 'translateX(-50%)',
          }}
        >
          {/* Center pole */}
          <div
            style={{
              position: 'absolute',
              left: '50%',
              top: 60,
              transform: 'translateX(-50%)',
              width: 6,
              height: 320,
              background: '#FAB95B',
              borderRadius: 3,
            }}
          />

          {/* Beam */}
          <svg
            width="900"
            height="160"
            style={{
              transform: `rotate(${scaleRot}deg)`,
              transformOrigin: 'center 80px',
            }}
            viewBox="0 0 900 160"
          >
            <rect x="100" y="70" width="700" height="12" fill="#FAB95B" rx="6" />
            <line x1="180" y1="82" x2="180" y2="100" stroke="#FAB95B" strokeWidth="4" />
            <line x1="720" y1="82" x2="720" y2="100" stroke="#FAB95B" strokeWidth="4" />
          </svg>

          {/* Left plate - Vegetables */}
          <div
            style={{
              position: 'absolute',
              left: 80,
              top: 200 + leftPlateY,
              width: 280,
              textAlign: 'center',
            }}
          >
            <div
              style={{
                width: 280,
                height: 50,
                background: '#FAB95B',
                borderRadius: '0 0 140px 140px',
                marginBottom: 12,
              }}
            />
            <svg width="120" height="120" viewBox="0 0 64 64" style={{ marginBottom: 8 }}>
              {/* Carrot */}
              <path d="M32 12 L24 50 L40 50 Z" fill="#FAB95B" />
              <path d="M28 8 L32 14 L36 8 M30 4 L32 12 L34 4" stroke="#E8EDF2" strokeWidth="2" fill="none" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 42,
                fontWeight: 800,
                color: '#E8EDF2',
              }}
            >
              FOOD
            </div>
          </div>

          {/* Right plate - Bills */}
          <div
            style={{
              position: 'absolute',
              right: 80,
              top: 200 + rightPlateY,
              width: 280,
              textAlign: 'center',
            }}
          >
            <div
              style={{
                width: 280,
                height: 50,
                background: '#ff0000',
                borderRadius: '0 0 140px 140px',
                marginBottom: 12,
                opacity: 0.85,
              }}
            />
            <svg width="120" height="120" viewBox="0 0 64 64" style={{ marginBottom: 8 }}>
              <rect x="8" y="14" width="48" height="32" fill="#E8EDF2" stroke="#ff0000" strokeWidth="2" />
              <text x="32" y="36" fontFamily="Inter" fontSize="18" fontWeight="900" fill="#ff0000" textAnchor="middle">$$$</text>
              <line x1="14" y1="50" x2="50" y2="50" stroke="#ff0000" strokeWidth="3" />
              <line x1="14" y1="56" x2="50" y2="56" stroke="#ff0000" strokeWidth="3" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 42,
                fontWeight: 800,
                color: '#ff0000',
              }}
            >
              BILLS
            </div>
          </div>
        </div>

        {/* Bottom message */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(140),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 48,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            Apply Today · No More Choosing
          </div>
        </div>
      </AbsoluteFill>

      {/* SECOND HALF - Program 2 reveal */}
      <AbsoluteFill style={{ opacity: transOp, transform: `translateY(${transSlide}px)` }}>
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 0,
            right: 0,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 32,
              color: '#FAB95B',
              letterSpacing: 10,
              textTransform: 'uppercase',
              marginBottom: 30,
            }}
          >
            ▸▸ NEXT UP ◂◂
          </div>

          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 700,
              color: '#E8EDF2',
              opacity: 0.7,
              marginBottom: 20,
            }}
          >
            PROGRAM
          </div>

          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 340,
              fontWeight: 900,
              color: 'transparent',
              WebkitTextStroke: '5px #FAB95B',
              lineHeight: 1,
              transform: `scale(${p2Scale})`,
              letterSpacing: -8,
            }}
          >
            02
          </div>

          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 90,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: -1,
              marginTop: -20,
              opacity: sp(310),
            }}
          >
            Medicare Extra Help
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_HiddenFormula;
```

```tsx
import React from 'react';
Audio: "This saves you from the nightmare of the Part D donut hole. With Medicare drug coverage, the premiums are high, deductibles are high, and pharmacy co-pays are shocking."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16_DonutHole',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_DonutHole: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Donut hole visualization - ring drawing
  const ringDraw = interpolate(sp(10), [0, 1], [0, 1]);
  const ringScale = sp(10, { damping: 9 });

  // Center hole text
  const holeTextOp = sp(70);

  // Three pillars: premiums, deductibles, copays
  const pillar1Y = interpolate(sp(110), [0, 1], [100, 0]);
  const pillar2Y = interpolate(sp(130), [0, 1], [100, 0]);
  const pillar3Y = interpolate(sp(150), [0, 1], [100, 0]);
  const pillar1Op = sp(110);
  const pillar2Op = sp(130);
  const pillar3Op = sp(150);

  // Top warning label
  const warnOp = sp(0);

  const camRotate = interpolate(frame, [0, durationInFrames], [-1, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(255,0,0,0.08) 0%, transparent 50%), radial-gradient(circle at 80% 80%, rgba(250,185,91,0.12) 0%, transparent 50%), linear-gradient(180deg, #412D15 0%, #2A1D0E 100%)',
        }}
      />

      {/* Animated rings expanding */}
      {[0, 1, 2].map((i) => {
        const expand = ((frame * 1.5 + i * 80) % 240) / 240;
        const size = 200 + expand * 1200;
        return (
          <div
            key={`exp-${i}`}
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: size,
              height: size,
              transform: 'translate(-50%, -50%)',
              border: '2px solid #ff0000',
              borderRadius: '50%',
              opacity: 0.08 * (1 - expand),
            }}
          />
        );
      })}

      {/* Motion texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="diag16" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform={`rotate(45) translate(${frame * 0.3}, 0)`}>
            <line x1="0" y1="0" x2="0" y2="40" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag16)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 500);
        const px = s * 1920;
        const py = (seededRandom(i + 510) * 1080 + frame * 0.6) % 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#FAB95B',
              borderRadius: '50%',
              opacity: 0.35,
              boxShadow: '0 0 6px #FAB95B',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRotate * 0.5}deg)` }}>
        {/* Top warning */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: warnOp,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 16,
              padding: '12px 36px',
              border: '3px solid #ff0000',
              borderRadius: 12,
              background: 'rgba(255,0,0,0.08)',
            }}
          >
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
              <path d="M12 2 L2 22 L22 22 Z" stroke="#ff0000" strokeWidth="2.5" fill="rgba(255,0,0,0.15)" />
              <line x1="12" y1="9" x2="12" y2="15" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
              <circle cx="12" cy="18" r="1.5" fill="#ff0000" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 36,
                fontWeight: 800,
                color: '#ff0000',
                letterSpacing: 4,
                textTransform: 'uppercase',
              }}
            >
              The Part D Nightmare
            </div>
          </div>
        </div>

        {/* Big DONUT HOLE visualization */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: '50%',
            transform: `translateX(-50%) scale(${ringScale})`,
          }}
        >
          <svg width="500" height="500" viewBox="0 0 200 200">
            <defs>
              <linearGradient id="donutGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stopColor="#ff0000" />
                <stop offset="100%" stopColor="#FAB95B" />
              </linearGradient>
            </defs>
            {/* Outer ring with animated dash */}
            <circle
              cx="100"
              cy="100"
              r="85"
              fill="none"
              stroke="url(#donutGrad)"
              strokeWidth="28"
              strokeDasharray={`${534 * ringDraw} 534`}
              transform="rotate(-90 100 100)"
              strokeLinecap="round"
            />
            {/* Inner hole */}
            <circle cx="100" cy="100" r="55" fill="#412D15" />
            {/* Dashed border inside */}
            <circle cx="100" cy="100" r="55" fill="none" stroke="#ff0000" strokeWidth="1" strokeDasharray="4 3" opacity="0.6" />
          </svg>

          {/* Center text inside the hole */}
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              transform: 'translate(-50%, -50%)',
              textAlign: 'center',
              opacity: holeTextOp,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 47,
                fontWeight: 900,
                color: '#ff0000',
                letterSpacing: 2,
                textTransform: 'uppercase',
                lineHeight: 1,
              }}
            >
              Donut
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 47,
                fontWeight: 900,
                color: '#ff0000',
                letterSpacing: 2,
                textTransform: 'uppercase',
                lineHeight: 1,
              }}
            >
              Hole
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 19,
                color: '#FAB95B',
                marginTop: 10,
                letterSpacing: 3,
              }}
            >
              YOU PAY $$$
            </div>
          </div>
        </div>

        {/* Three pillars at bottom: Premiums | Deductibles | Co-pays */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 48,
          }}
        >
          {[
            { label: 'PREMIUMS', val: 'HIGH', delay: pillar1Op, y: pillar1Y, icon: '$' },
            { label: 'DEDUCTIBLES', val: 'HIGH', delay: pillar2Op, y: pillar2Y, icon: '%' },
            { label: 'CO-PAYS', val: 'SHOCKING', delay: pillar3Op, y: pillar3Y, icon: '!' },
          ].map((p, i) => (
            <div
              key={i}
              style={{
                width: 340,
                padding: 32,
                background: 'rgba(255,0,0,0.08)',
                border: '2px solid #ff0000',
                borderRadius: 16,
                textAlign: 'center',
                opacity: p.delay,
                transform: `translateY(${p.y}px)`,
              }}
            >
              <div
                style={{
                  width: 70,
                  height: 70,
                  margin: '0 auto 16px',
                  borderRadius: '50%',
                  background: '#ff0000',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'Inter',
                  fontSize: 40,
                  fontWeight: 900,
                  color: '#E8EDF2',
                }}
              >
                {p.icon}
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 24,
                  color: '#E8EDF2',
                  opacity: 0.7,
                  letterSpacing: 4,
                  marginBottom: 8,
                }}
              >
                {p.label}
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 51,
                  fontWeight: 900,
                  color: '#FAB95B',
                  letterSpacing: 1,
                }}
              >
                {p.val}
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_DonutHole;
```

```tsx
import React from 'react';
Audio: "If you need expensive brand-name medications, you hit the donut hole — suddenly forced to pay thousands out-of-pocket before coverage kicks back in."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17_ThousandsOutOfPocket',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_ThousandsOutOfPocket: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pill bottle animation
  const bottleY = interpolate(sp(10), [0, 1], [-100, 0]);
  const bottleOp = sp(10);

  // Brand name labels float in
  const brand1Op = sp(40);
  const brand2Op = sp(55);
  const brand3Op = sp(70);

  // Coverage timeline bar
  const timelineDraw = interpolate(sp(120), [0, 1], [0, 1]);

  // Marker on timeline
  const markerX = interpolate(frame, [150, 230], [200, 1100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const markerVisible = frame >= 150 ? 1 : 0;

  // "YOU PAY" callout
  const calloutOp = interpolate(frame, [220, 250], [0, 1], { extrapolateRight: 'clamp' });
  const calloutScale = sp(220, { damping: 7 });

  // Big number ticker $0 → $4,000
  const ticker = Math.floor(interpolate(frame, [240, 330], [0, 4000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 60%, rgba(255,0,0,0.1) 0%, transparent 50%), linear-gradient(135deg, #412D15 0%, #2D1F0F 100%)',
        }}
      />

      {/* Animated shapes - tilted rectangles */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 80);
        return (
          <div
            key={`s-${i}`}
            style={{
              position: 'absolute',
              left: seed * 1700,
              top: seededRandom(i + 90) * 900,
              width: 300 + seed * 200,
              height: 200 + seed * 100,
              border: '2px solid #FAB95B',
              opacity: 0.05,
              transform: `rotate(${(i * 25 + frame * 0.1)}deg)`,
              borderRadius: 12,
            }}
          />
        );
      })}

      {/* Motion texture - vertical lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        {Array.from({ length: 24 }).map((_, i) => (
          <line
            key={i}
            x1={i * 80 + (frame * 0.4) % 80}
            y1="0"
            x2={i * 80 + (frame * 0.4) % 80}
            y2="1080"
            stroke="#E8EDF2"
            strokeWidth="1"
          />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 600);
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: s * 1920,
              top: (seededRandom(i + 610) * 1080 + frame * 0.5) % 1080,
              width: 3,
              height: 3,
              background: '#FAB95B',
              borderRadius: '50%',
              opacity: 0.3,
              boxShadow: '0 0 6px #FAB95B',
            }}
          />
        );
      })}

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: sp(0),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 30,
            color: '#ff0000',
            letterSpacing: 10,
            textTransform: 'uppercase',
            marginBottom: 12,
          }}
        >
          ▾ Brand-Name Drug Trap ▾
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 90,
            fontWeight: 900,
            color: '#E8EDF2',
            lineHeight: 1,
          }}
        >
          Expensive Meds <span style={{ color: '#FAB95B' }}>=</span> Donut Hole
        </div>
      </div>

      {/* Left side - Pill bottle illustration */}
      <div
        style={{
          position: 'absolute',
          top: 320,
          left: 180,
          opacity: bottleOp,
          transform: `translateY(${bottleY}px)`,
        }}
      >
        <svg width="280" height="380" viewBox="0 0 100 140">
          {/* Bottle cap */}
          <rect x="22" y="8" width="56" height="22" fill="#FAB95B" rx="3" />
          <line x1="22" y1="18" x2="78" y2="18" stroke="#412D15" strokeWidth="1" />
          {/* Bottle neck */}
          <rect x="32" y="30" width="36" height="8" fill="#E8EDF2" opacity="0.4" />
          {/* Bottle body */}
          <rect x="20" y="38" width="60" height="92" fill="#E8EDF2" rx="4" stroke="#412D15" strokeWidth="1" />
          {/* Label area */}
          <rect x="26" y="52" width="48" height="64" fill="#412D15" rx="2" />
          <text x="50" y="72" fontFamily="Inter" fontSize="9" fontWeight="900" fill="#FAB95B" textAnchor="middle">
            RX
          </text>
          <line x1="30" y1="80" x2="70" y2="80" stroke="#FAB95B" strokeWidth="1" />
          <line x1="30" y1="86" x2="65" y2="86" stroke="#E8EDF2" strokeWidth="0.5" opacity="0.6" />
          <line x1="30" y1="92" x2="68" y2="92" stroke="#E8EDF2" strokeWidth="0.5" opacity="0.6" />
          <line x1="30" y1="98" x2="60" y2="98" stroke="#E8EDF2" strokeWidth="0.5" opacity="0.6" />
          <text x="50" y="112" fontFamily="JetBrains Mono" fontSize="6" fill="#ff0000" textAnchor="middle">$$$$$</text>
        </svg>

        {/* Floating brand-name tags */}
        <div
          style={{
            position: 'absolute',
            top: -20,
            left: 240,
            display: 'flex',
            flexDirection: 'column',
            gap: 16,
          }}
        >
          {[
            { name: 'BRAND-NAME', op: brand1Op },
            { name: 'NON-GENERIC', op: brand2Op },
            { name: 'SPECIALTY DRUG', op: brand3Op },
          ].map((b, i) => (
            <div
              key={i}
              style={{
                padding: '12px 24px',
                background: 'rgba(255,0,0,0.15)',
                border: '2px solid #ff0000',
                borderRadius: 8,
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#ff0000',
                letterSpacing: 2,
                opacity: b.op,
                transform: `translateX(${interpolate(b.op, [0, 1], [-40, 0])}px)`,
              }}
            >
              {b.name}
            </div>
          ))}
        </div>
      </div>

      {/* Right side - Coverage timeline graph */}
      <div
        style={{
          position: 'absolute',
          top: 360,
          right: 120,
          width: 720,
          opacity: sp(100),
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 30,
            fontWeight: 700,
            color: '#FAB95B',
            letterSpacing: 4,
            textTransform: 'uppercase',
            marginBottom: 20,
          }}
        >
          Your Coverage Year
        </div>

        {/* Timeline bar */}
        <div style={{ position: 'relative', height: 70, marginBottom: 24 }}>
          {/* Phase 1 - Initial coverage (green) */}
          <div
            style={{
              position: 'absolute',
              left: 0,
              top: 0,
              width: 230 * timelineDraw,
              height: 70,
              background: '#FAB95B',
              borderRadius: '8px 0 0 8px',
            }}
          />
          {/* Phase 2 - DONUT HOLE (red) */}
          <div
            style={{
              position: 'absolute',
              left: 230,
              top: 0,
              width: 320 * timelineDraw,
              height: 70,
              background: 'repeating-linear-gradient(45deg, #ff0000 0 12px, #c00000 12px 24px)',
            }}
          />
          {/* Phase 3 - Catastrophic (smaller orange) */}
          <div
            style={{
              position: 'absolute',
              left: 550,
              top: 0,
              width: 170 * timelineDraw,
              height: 70,
              background: '#E8EDF2',
              opacity: 0.6,
              borderRadius: '0 8px 8px 0',
            }}
          />

          {/* Animated marker */}
          {markerVisible > 0 && (
            <div
              style={{
                position: 'absolute',
                left: markerX,
                top: -20,
                transform: 'translateX(-50%)',
              }}
            >
              <svg width="40" height="110" viewBox="0 0 40 110">
                <polygon points="20,0 30,15 30,90 20,110 10,90 10,15" fill="#E8EDF2" stroke="#412D15" strokeWidth="2" />
                <circle cx="20" cy="55" r="6" fill="#412D15" />
              </svg>
            </div>
          )}
        </div>

        {/* Timeline labels */}
        <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 32 }}>
          <div style={{ width: 230, textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#FAB95B', textTransform: 'uppercase' }}>
              Initial
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 17, color: '#E8EDF2', opacity: 0.6 }}>covered</div>
          </div>
          <div style={{ width: 320, textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 900, color: '#ff0000', textTransform: 'uppercase' }}>
              ⚠ Donut Hole ⚠
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 17, color: '#ff0000' }}>YOU PAY ALL</div>
          </div>
          <div style={{ width: 170, textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#E8EDF2', opacity: 0.7, textTransform: 'uppercase' }}>
              Catastrophic
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 17, color: '#E8EDF2', opacity: 0.5 }}>coverage</div>
          </div>
        </div>

        {/* Big out-of-pocket callout */}
        <div
          style={{
            opacity: calloutOp,
            transform: `scale(${calloutScale})`,
            background: 'rgba(255,0,0,0.1)',
            border: '3px solid #ff0000',
            borderRadius: 16,
            padding: '24px 40px',
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#ff0000',
              letterSpacing: 6,
              textTransform: 'uppercase',
              marginBottom: 8,
            }}
          >
            Out-of-Pocket Hit
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 170,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 1,
              letterSpacing: -4,
            }}
          >
            ${ticker.toLocaleString()}+
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              color: '#E8EDF2',
              opacity: 0.8,
              letterSpacing: 3,
              textTransform: 'uppercase',
              marginTop: 4,
            }}
          >
            Before Coverage Returns
          </div>
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_ThousandsOutOfPocket;
```

```tsx
import React from 'react';
Audio: "This is where Extra Help comes in. Officially the Low Income Subsidy, or LIS, it's designed to wipe out your costs. If you qualify, it pays your Part D premium entirely and wipes your deductible."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18_ExtraHelpReveal',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_ExtraHelpReveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // EXTRA HELP big reveal
  const titleScale = sp(10, { damping: 8, stiffness: 90 });
  const titleBlur = interpolate(sp(10), [0, 1], [25, 0]);

  // LIS acronym
  const lisOp = sp(60);
  const lisSlide = interpolate(sp(60), [0, 1], [60, 0]);

  // Two benefit boxes
  const box1Slide = interpolate(sp(120), [0, 1], [-300, 0]);
  const box2Slide = interpolate(sp(150), [0, 1], [300, 0]);
  const box1Op = sp(120);
  const box2Op = sp(150);

  // WIPED stamps animation
  const stamp1Scale = sp(200, { damping: 6, stiffness: 110 });
  const stamp2Scale = sp(230, { damping: 6, stiffness: 110 });
  const stamp1Rot = interpolate(sp(200), [0, 1], [-30, -10]);
  const stamp2Rot = interpolate(sp(230), [0, 1], [30, 12]);

  // Shield icon
  const shieldOp = sp(0);
  const shieldFloat = Math.sin(frame * 0.05) * 8;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 30%, rgba(250,185,91,0.22) 0%, transparent 55%), linear-gradient(180deg, #412D15 0%, #2A1D0E 100%)',
        }}
      />

      {/* Animated radiating shapes from center */}
      {[0, 1, 2, 3, 4].map((i) => {
        const expand = ((frame * 1.2 + i * 60) % 300) / 300;
        const size = 100 + expand * 1400;
        return (
          <div
            key={`ray-${i}`}
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: size,
              height: size,
              transform: 'translate(-50%, -50%)',
              border: '2px solid #FAB95B',
              borderRadius: '50%',
              opacity: 0.08 * (1 - expand),
            }}
          />
        );
      })}

      {/* Motion texture - radial lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        {Array.from({ length: 18 }).map((_, i) => {
          const angle = (i / 18) * Math.PI * 2 + frame * 0.005;
          const x2 = 960 + Math.cos(angle) * 1200;
          const y2 = 540 + Math.sin(angle) * 1200;
          return <line key={i} x1="960" y1="540" x2={x2} y2={y2} stroke="#FAB95B" strokeWidth="1" />;
        })}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const s = seededRandom(i + 700);
        const px = s * 1920;
        const py = (seededRandom(i + 710) * 1080 - frame * 0.7 + 1080) % 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#FAB95B',
              borderRadius: '50%',
              opacity: 0.4,
              boxShadow: '0 0 8px #FAB95B',
            }}
          />
        );
      })}

      {/* Top shield icon */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: '50%',
          transform: `translateX(-50%) translateY(${shieldFloat}px)`,
          opacity: shieldOp,
        }}
      >
        <svg width="100" height="100" viewBox="0 0 64 64">
          <path
            d="M32 4 L8 14 L8 32 Q8 50 32 60 Q56 50 56 32 L56 14 Z"
            fill="rgba(250,185,91,0.2)"
            stroke="#FAB95B"
            strokeWidth="2.5"
          />
          <path d="M22 32 L29 39 L44 24" stroke="#FAB95B" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>

      {/* MAIN TITLE - EXTRA HELP */}
      <div
        style={{
          position: 'absolute',
          top: 190,
          left: 0,
          right: 0,
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 30,
            color: '#FAB95B',
            letterSpacing: 12,
            textTransform: 'uppercase',
            marginBottom: 14,
            opacity: sp(0),
          }}
        >
          ▸ The Lifesaver ◂
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 221,
            fontWeight: 900,
            color: '#E8EDF2',
            lineHeight: 0.95,
            transform: `scale(${titleScale})`,
            filter: `blur(${titleBlur}px)`,
            letterSpacing: -4,
            textShadow: '0 0 60px rgba(250,185,91,0.4)',
          }}
        >
          EXTRA <span style={{ color: '#FAB95B' }}>HELP</span>
        </div>
        <div
          style={{
            opacity: lisOp,
            transform: `translateY(${lisSlide}px)`,
            marginTop: 16,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div style={{ width: 60, height: 2, background: '#FAB95B' }} />
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 36,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 8,
            }}
          >
            LOW INCOME SUBSIDY · LIS
          </div>
          <div style={{ width: 60, height: 2, background: '#FAB95B' }} />
        </div>
      </div>

      {/* Two benefit boxes at bottom */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 40,
          padding: '0 140px',
        }}
      >
        {/* Box 1 - Premium */}
        <div
          style={{
            flex: 1,
            background: 'linear-gradient(135deg, rgba(250,185,91,0.15) 0%, rgba(250,185,91,0.04) 100%)',
            border: '3px solid #FAB95B',
            borderRadius: 20,
            padding: 36,
            opacity: box1Op,
            transform: `translateX(${box1Slide}px)`,
            position: 'relative',
            overflow: 'hidden',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#FAB95B',
              letterSpacing: 4,
              marginBottom: 8,
            }}
          >
            BENEFIT 01
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              marginBottom: 16,
            }}
          >
            Part D Premium
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              color: '#E8EDF2',
              opacity: 0.7,
              marginBottom: 12,
            }}
          >
            Monthly cost · gone
          </div>

          {/* WIPED stamp */}
          <div
            style={{
              position: 'absolute',
              top: 70,
              right: 30,
              transform: `scale(${stamp1Scale}) rotate(${stamp1Rot}deg)`,
              border: '5px solid #FAB95B',
              padding: '12px 28px',
              borderRadius: 10,
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: 3,
              background: 'rgba(250,185,91,0.05)',
            }}
          >
            PAID
          </div>

          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              color: '#FAB95B',
              marginTop: 24,
              fontWeight: 700,
            }}
          >
            → $0/month
          </div>
        </div>

        {/* Box 2 - Deductible */}
        <div
          style={{
            flex: 1,
            background: 'linear-gradient(135deg, rgba(250,185,91,0.15) 0%, rgba(250,185,91,0.04) 100%)',
            border: '3px solid #FAB95B',
            borderRadius: 20,
            padding: 36,
            opacity: box2Op,
            transform: `translateX(${box2Slide}px)`,
            position: 'relative',
            overflow: 'hidden',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#FAB95B',
              letterSpacing: 4,
              marginBottom: 8,
            }}
          >
            BENEFIT 02
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              marginBottom: 16,
            }}
          >
            Annual Deductible
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              color: '#E8EDF2',
              opacity: 0.7,
              marginBottom: 12,
            }}
          >
            Yearly cost · eliminated
          </div>

          {/* WIPED stamp */}
          <div
            style={{
              position: 'absolute',
              top: 70,
              right: 30,
              transform: `scale(${stamp2Scale}) rotate(${stamp2Rot}deg)`,
              border: '5px solid #FAB95B',
              padding: '12px 28px',
              borderRadius: 10,
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: 3,
              background: 'rgba(250,185,91,0.05)',
            }}
          >
            WIPED
          </div>

          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              color: '#FAB95B',
              marginTop: 24,
              fontWeight: 700,
            }}
          >
            → $0/year
          </div>
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_ExtraHelpReveal;
```

```tsx
import React from 'react';
Audio: "It caps your co-pays at just a few dollars per prescription. No more donut hole. No more thousands out-of-pocket. The income limits are much higher than most people think."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19_CoPayCap',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_CoPayCap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Receipt animation - before & after
  const beforeOp = sp(10);
  const beforeSlide = interpolate(sp(10), [0, 1], [-300, 0]);

  const afterOp = sp(80);
  const afterSlide = interpolate(sp(80), [0, 1], [300, 0]);

  // Arrow between
  const arrowScale = sp(140, { damping: 7 });

  // Counter on after receipt
  const counter = Math.floor(interpolate(frame, [110, 180], [0, 4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Bottom income callout
  const incomeOp = sp(220);
  const incomeNumberCount = Math.floor(interpolate(frame, [240, 320], [0, 20000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 20% 70%, rgba(250,185,91,0.16) 0%, transparent 50%), linear-gradient(170deg, #412D15 0%, #2D1F0F 100%)',
        }}
      />

      {/* Animated shapes - rotated squares */}
      {[0, 1, 2, 3].map((i) => {
        const seed = seededRandom(i + 100);
        return (
          <div
            key={`sq-${i}`}
            style={{
              position: 'absolute',
              left: seed * 1700,
              top: seededRandom(i + 110) * 900,
              width: 300 + seed * 200,
              height: 300 + seed * 200,
              border: '2px solid #E8EDF2',
              opacity: 0.04,
              transform: `rotate(${frame * 0.08 + i * 30}deg)`,
              borderRadius: 12,
            }}
          />
        );
      })}

      {/* Motion texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="grid19" width="70" height="70" patternUnits="userSpaceOnUse" patternTransform={`translate(${frame * 0.3}, ${frame * 0.2})`}>
            <circle cx="35" cy="35" r="1.5" fill="#FAB95B" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid19)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 800);
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: s * 1920,
              top: (seededRandom(i + 810) * 1080 + frame * 0.5) % 1080,
              width: 3,
              height: 3,
              background: '#FAB95B',
              borderRadius: '50%',
              opacity: 0.35,
              boxShadow: '0 0 6px #FAB95B',
            }}
          />
        );
      })}

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: sp(0),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            color: '#FAB95B',
            letterSpacing: 10,
            textTransform: 'uppercase',
            marginBottom: 14,
          }}
        >
          ▸ Pharmacy Co-Pay Cap ◂
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 85,
            fontWeight: 900,
            color: '#E8EDF2',
            lineHeight: 1,
            letterSpacing: -2,
          }}
        >
          From <span style={{ color: '#ff0000' }}>SHOCKING</span> to <span style={{ color: '#FAB95B' }}>POCKET CHANGE</span>
        </div>
      </div>

      {/* BEFORE Receipt - left */}
      <div
        style={{
          position: 'absolute',
          top: 290,
          left: 200,
          width: 460,
          opacity: beforeOp,
          transform: `translateX(${beforeSlide}px) rotate(-3deg)`,
        }}
      >
        <div
          style={{
            background: '#E8EDF2',
            padding: 32,
            borderRadius: 8,
            fontFamily: 'JetBrains Mono',
            boxShadow: '0 20px 50px rgba(0,0,0,0.4)',
            position: 'relative',
          }}
        >
          {/* Receipt header */}
          <div style={{ textAlign: 'center', borderBottom: '2px dashed #412D15', paddingBottom: 16, marginBottom: 16 }}>
            <div style={{ fontSize: 22, fontWeight: 800, color: '#412D15' }}>PHARMACY RECEIPT</div>
            <div style={{ fontSize: 16, color: '#412D15', opacity: 0.7, marginTop: 4 }}>WITHOUT EXTRA HELP</div>
          </div>

          {/* Lines */}
          {[
            { item: 'Brand-Name Med', val: '$245.00' },
            { item: 'Blood Pressure', val: '$87.50' },
            { item: 'Diabetes Rx', val: '$162.00' },
            { item: 'Cholesterol', val: '$98.75' },
          ].map((r, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                fontSize: 22,
                color: '#412D15',
                marginBottom: 10,
              }}
            >
              <span>{r.item}</span>
              <span style={{ fontWeight: 700 }}>{r.val}</span>
            </div>
          ))}

          <div style={{ borderTop: '2px solid #412D15', marginTop: 16, paddingTop: 16, display: 'flex', justifyContent: 'space-between' }}>
            <span style={{ fontSize: 28, fontWeight: 900, color: '#ff0000' }}>TOTAL:</span>
            <span style={{ fontSize: 28, fontWeight: 900, color: '#ff0000' }}>$593.25</span>
          </div>
        </div>

        {/* Label below */}
        <div
          style={{
            marginTop: 24,
            textAlign: 'center',
            fontFamily: 'Inter, Poppins',
            fontSize: 36,
            fontWeight: 800,
            color: '#ff0000',
            letterSpacing: 4,
            textTransform: 'uppercase',
          }}
        >
          ✕ Before
        </div>
      </div>

      {/* Arrow in middle */}
      <div
        style={{
          position: 'absolute',
          top: 480,
          left: '50%',
          transform: `translateX(-50%) scale(${arrowScale})`,
        }}
      >
        <svg width="160" height="80" viewBox="0 0 100 50">
          <line x1="5" y1="25" x2="85" y2="25" stroke="#FAB95B" strokeWidth="6" strokeLinecap="round" />
          <polygon points="80,12 95,25 80,38" fill="#FAB95B" />
        </svg>
        <div
          style={{
            textAlign: 'center',
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#FAB95B',
            letterSpacing: 4,
            marginTop: 4,
            fontWeight: 700,
          }}
        >
          EXTRA HELP
        </div>
      </div>

      {/* AFTER Receipt - right */}
      <div
        style={{
          position: 'absolute',
          top: 290,
          right: 200,
          width: 460,
          opacity: afterOp,
          transform: `translateX(${afterSlide}px) rotate(3deg)`,
        }}
      >
        <div
          style={{
            background: '#E8EDF2',
            padding: 32,
            borderRadius: 8,
            fontFamily: 'JetBrains Mono',
            boxShadow: '0 20px 50px rgba(250,185,91,0.3), 0 0 0 4px #FAB95B',
            position: 'relative',
          }}
        >
          {/* Receipt header */}
          <div style={{ textAlign: 'center', borderBottom: '2px dashed #412D15', paddingBottom: 16, marginBottom: 16 }}>
            <div style={{ fontSize: 22, fontWeight: 800, color: '#412D15' }}>PHARMACY RECEIPT</div>
            <div style={{ fontSize: 16, color: '#FAB95B', marginTop: 4, fontWeight: 700 }}>WITH EXTRA HELP</div>
          </div>

          {/* Lines - capped */}
          {[
            { item: 'Brand-Name Med', val: `$${counter}.50` },
            { item: 'Blood Pressure', val: `$${counter}.00` },
            { item: 'Diabetes Rx', val: `$${counter}.25` },
            { item: 'Cholesterol', val: `$${counter}.00` },
          ].map((r, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                fontSize: 22,
                color: '#412D15',
                marginBottom: 10,
              }}
            >
              <span>{r.item}</span>
              <span style={{ fontWeight: 700, color: '#FAB95B' }}>{r.val}</span>
            </div>
          ))}

          <div style={{ borderTop: '2px solid #412D15', marginTop: 16, paddingTop: 16, display: 'flex', justifyContent: 'space-between' }}>
            <span style={{ fontSize: 28, fontWeight: 900, color: '#412D15' }}>TOTAL:</span>
            <span style={{ fontSize: 28, fontWeight: 900, color: '#FAB95B' }}>${(counter * 4 + 1.75).toFixed(2)}</span>
          </div>
        </div>

        {/* Label below */}
        <div
          style={{
            marginTop: 24,
            textAlign: 'center',
            fontFamily: 'Inter, Poppins',
            fontSize: 36,
            fontWeight: 800,
            color: '#FAB95B',
            letterSpacing: 4,
            textTransform: 'uppercase',
          }}
        >
          ✓ After
        </div>
      </div>

      {/* Bottom income limit callout */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(incomeOp, [0, 1], [40, 0])}px)`,
          opacity: incomeOp,
          background: 'rgba(232,237,242,0.04)',
          border: '2px solid #FAB95B',
          borderRadius: 14,
          padding: '20px 50px',
          display: 'flex',
          alignItems: 'center',
          gap: 28,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#E8EDF2', opacity: 0.75 }}>
          Income limit · single senior:
        </div>
        <div style={{ fontFamily: 'Inter, Poppins', fontSize: 60, fontWeight: 900, color: '#FAB95B' }}>
          ${incomeNumberCount.toLocaleString()}+
        </div>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#FAB95B', letterSpacing: 3 }}>YEARLY</div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_CoPayCap;
```

```tsx
import React from 'react';
Audio: "A single senior can make over $20,000 a year and still qualify. If you're paying high pharmacy co-pays, you're leaving massive money on the table. Program number three is the Medicare Savings Programs."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20_MoneyOnTable',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20_MoneyOnTable: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // First half - money on table
  const tableOp = interpolate(frame, [0, 30, 200, 230], [0, 1, 1, 0], { extrapolateRight: 'clamp' });

  // Headline
  const head1 = sp(5);
  const head2 = sp(25);
  const head3 = sp(45);

  // Money bills falling animation
  const fallOp = sp(70);

  // Second half - Program 3 reveal
  const transOp = interpolate(frame, [220, 260], [0, 1], { extrapolateRight: 'clamp' });
  const p3Scale = sp(270, { damping: 7, stiffness: 100 });
  const p3Slide = interpolate(sp(220), [0, 1], [60, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 60% 50%, rgba(250,185,91,0.2) 0%, transparent 55%), linear-gradient(180deg, #412D15 0%, #2A1D0E 100%)',
        }}
      />

      {/* Animated diagonal stripes */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        <defs>
          <pattern id="stripe20" width="100" height="100" patternUnits="userSpaceOnUse" patternTransform={`rotate(30) translate(${frame * 0.5}, 0)`}>
            <rect x="0" y="0" width="50" height="100" fill="#FAB95B" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#stripe20)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 900);
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: s * 1920,
              top: (seededRandom(i + 910) * 1080 + frame * 0.6) % 1080,
              width: 3,
              height: 3,
              background: '#FAB95B',
              borderRadius: '50%',
              opacity: 0.35,
              boxShadow: '0 0 6px #FAB95B',
            }}
          />
        );
      })}

      {/* FIRST HALF: Money on the table */}
      <AbsoluteFill style={{ opacity: tableOp }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(0),
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              color: '#FAB95B',
              letterSpacing: 10,
              textTransform: 'uppercase',
            }}
          >
            ▾ The Cost of Inaction ▾
          </div>
        </div>

        {/* Big stacked headline */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 0,
            right: 0,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              opacity: head1,
              transform: `translateY(${interpolate(head1, [0, 1], [40, 0])}px)`,
              letterSpacing: -2,
            }}
          >
            You're Leaving
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 187,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 1,
              opacity: head2,
              transform: `scale(${head2})`,
              letterSpacing: -4,
              textShadow: '0 0 60px rgba(250,185,91,0.4)',
              marginTop: 10,
            }}
          >
            MASSIVE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              opacity: head3,
              transform: `translateY(${interpolate(head3, [0, 1], [40, 0])}px)`,
              letterSpacing: -2,
              marginTop: 10,
            }}
          >
            Money on the Table
          </div>
        </div>

        {/* Money bills falling */}
        <div style={{ position: 'absolute', inset: 0, opacity: fallOp }}>
          {Array.from({ length: 8 }).map((_, i) => {
            const s = seededRandom(i + 950);
            const x = 200 + s * 1500;
            const startDelay = i * 12;
            const y = interpolate(
              Math.max(0, frame - 100 - startDelay),
              [0, 90],
              [-100, 1100],
              { extrapolateRight: 'clamp' }
            );
            const rot = Math.sin((frame - 100 - startDelay) * 0.08) * 20 + i * 30;
            return (
              <div
                key={`bill-${i}`}
                style={{
                  position: 'absolute',
                  left: x,
                  top: y,
                  transform: `rotate(${rot}deg)`,
                }}
              >
                <svg width="120" height="60" viewBox="0 0 100 50">
                  <rect x="2" y="2" width="96" height="46" fill="#FAB95B" stroke="#412D15" strokeWidth="1.5" rx="4" />
                  <circle cx="50" cy="25" r="14" fill="none" stroke="#412D15" strokeWidth="1.5" />
                  <text x="50" y="30" fontFamily="Inter" fontSize="14" fontWeight="900" fill="#412D15" textAnchor="middle">$</text>
                  <text x="14" y="14" fontFamily="JetBrains Mono" fontSize="7" fontWeight="800" fill="#412D15">100</text>
                  <text x="80" y="44" fontFamily="JetBrains Mono" fontSize="7" fontWeight="800" fill="#412D15">100</text>
                </svg>
              </div>
            );
          })}
        </div>

        {/* Bottom bar - income confirmation */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: '50%',
            transform: 'translateX(-50%)',
            background: 'rgba(232,237,242,0.06)',
            border: '2px solid #FAB95B',
            borderRadius: 14,
            padding: '20px 50px',
            opacity: sp(120),
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <svg width="42" height="42" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="11" fill="#FAB95B" />
            <path d="M7 12 L11 16 L17 8" stroke="#412D15" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700, color: '#E8EDF2' }}>
            Even at <span style={{ color: '#FAB95B' }}>$20,000+/year</span>, you still qualify
          </div>
        </div>
      </AbsoluteFill>

      {/* SECOND HALF: Program 3 reveal */}
      <AbsoluteFill style={{ opacity: transOp, transform: `translateY(${p3Slide}px)` }}>
        <div
          style={{
            position: 'absolute',
            top: 220,
            left: 0,
            right: 0,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 32,
              color: '#FAB95B',
              letterSpacing: 10,
              textTransform: 'uppercase',
              marginBottom: 30,
            }}
          >
            ▸▸ PROGRAM THREE ◂◂
          </div>

          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 360,
              fontWeight: 900,
              color: 'transparent',
              WebkitTextStroke: '5px #FAB95B',
              lineHeight: 1,
              transform: `scale(${p3Scale})`,
              letterSpacing: -10,
            }}
          >
            03
          </div>

          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 102,
              fontWeight: 900,
              color: '#E8EDF2',
              letterSpacing: -2,
              marginTop: -10,
              opacity: sp(290),
              lineHeight: 1,
            }}
          >
            Medicare Savings
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 102,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: -2,
              marginTop: 4,
              opacity: sp(310),
              lineHeight: 1,
            }}
          >
            Programs
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene20_MoneyOnTable;
```

