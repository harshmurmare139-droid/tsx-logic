```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene11BateauFantome',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const Scene11BateauFantome: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Slow camera push-in + horizontal drift
  const camScale = interpolate(frame, [0, durationInFrames], [1.09, 1.0], {
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });
  const camX = Math.sin(frame * 0.008) * 20;

  const boatIn = sp(18, { damping: 14 });
  const waterY = Math.sin(frame * 0.05) * 7;
  const boatTilt = Math.sin(frame * 0.045) * 2.4;

  const counter = Math.round(
    interpolate(frame, [130, 330], [0, 4000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })
  );
  const impact = frame > 330 && frame < 348 ? Math.sin(frame * 2.6) * 7 : 0;

  const marqueeX = -((frame * 2.4) % 960);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 30% 25%, #6A57C4 0%, ${BG} 45%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers — large transparent geometry drifting */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 120 + Math.sin(frame * 0.01) * 40,
            top: -180,
            width: 900,
            height: 900,
            borderRadius: '50%',
            border: `3px solid ${LIGHT}`,
            opacity: 0.06,
            transform: `rotate(${frame * 0.06}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: -220,
            bottom: -240,
            width: 820,
            height: 820,
            backgroundColor: LIGHT,
            opacity: 0.04,
            transform: `rotate(${-frame * 0.05}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: '52%',
            top: 120,
            width: 0,
            height: 0,
            borderLeft: '260px solid transparent',
            borderRight: '260px solid transparent',
            borderBottom: `440px solid ${GOLD}`,
            opacity: 0.05,
            transform: `rotate(${18 + Math.sin(frame * 0.014) * 6}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — halftone dot field */}
      <AbsoluteFill
        style={{
          backgroundImage: `radial-gradient(${LIGHT} 2px, transparent 2px)`,
          backgroundSize: '34px 34px',
          opacity: 0.07,
          transform: `translate(${Math.sin(frame * 0.02) * 16}px, ${Math.cos(frame * 0.02) * 12}px)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 28 }).map((_, i) => {
          const s = seededRandom(i + 3);
          const s2 = seededRandom(i + 71);
          const size = 4 + s * 9;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${s * 100}%`,
                top: `${((s2 * 100 + frame * (0.05 + s * 0.08)) % 110) - 5}%`,
                width: size,
                height: size,
                borderRadius: i % 3 === 0 ? 2 : '50%',
                backgroundColor: i % 4 === 0 ? GOLD : LIGHT,
                opacity: 0.15 + s2 * 0.3,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translate(${camX + impact}px, ${impact * 0.4}px)`,
        }}
      >
        {/* Collage tape strip — headline anchor */}
        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 132,
            padding: '14px 34px',
            backgroundColor: GOLD,
            transform: `rotate(-2.4deg) scale(${sp(6)})`,
            clipPath: 'polygon(0% 6%, 100% 0%, 99% 96%, 1% 100%)',
          }}
        >
          <span
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 30,
              letterSpacing: 6,
              color: INK,
              fontWeight: 700,
            }}
          >
            COÛT N°2 / ENTRETIEN
          </span>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 208,
            width: 900,
            transform: `translateY(${interpolate(sp(16), [0, 1], [60, 0])}px)`,
            opacity: sp(16),
          }}
        >
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 108,
              lineHeight: 0.94,
              color: LIGHT,
              textTransform: 'uppercase',
            }}
          >
            IL NE SORT
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 108,
              lineHeight: 0.94,
              color: GOLD,
              textTransform: 'uppercase',
            }}
          >
            JAMAIS DU PORT
          </div>
          <div
            style={{
              marginTop: 26,
              fontFamily: 'Montserrat, Poppins, sans-serif',
              fontSize: 44,
              fontWeight: 500,
              color: LIGHT,
              opacity: 0.9,
              maxWidth: 760,
            }}
          >
            Et pourtant, la facture tourne toute l’année.
          </div>
        </div>

        {/* Icon graphic — boat on ponton, micro rocking */}
        <div
          style={{
            position: 'absolute',
            right: 150,
            top: 250,
            transform: `translateY(${waterY + interpolate(boatIn, [0, 1], [120, 0])}px) rotate(${boatTilt}deg) scale(${boatIn})`,
            opacity: boatIn,
          }}
        >
          <svg width="520" height="420" viewBox="0 0 520 420">
            <rect x="40" y="20" width="440" height="360" fill={DEEP} opacity="0.35" />
            <line x1="260" y1="60" x2="260" y2="230" stroke={LIGHT} strokeWidth="8" />
            <polygon points="266,70 400,225 266,225" fill={LIGHT} opacity="0.92" />
            <polygon points="254,110 150,225 254,225" fill={GOLD} opacity="0.9" />
            <path d="M90 240 L430 240 L370 320 L150 320 Z" fill={LIGHT} />
            <rect x="150" y="258" width="60" height="26" fill={INK} opacity="0.75" />
            <rect x="230" y="258" width="60" height="26" fill={INK} opacity="0.75" />
            <rect x="310" y="258" width="60" height="26" fill={INK} opacity="0.75" />
            <line x1="60" y1="345" x2="470" y2="345" stroke={LILAC} strokeWidth="6" opacity="0.8" />
            <line
              x1="60"
              y1="368"
              x2="470"
              y2="368"
              stroke={LILAC}
              strokeWidth="4"
              opacity="0.5"
              strokeDasharray="26 18"
              strokeDashoffset={-frame * 1.6}
            />
          </svg>
        </div>

        {/* Data card — annual burn counter */}
        <div
          style={{
            position: 'absolute',
            left: 110,
            bottom: 190,
            padding: '30px 46px',
            backgroundColor: LIGHT,
            transform: `rotate(1.6deg) scale(${sp(110, { damping: 11 })})`,
            boxShadow: `18px 18px 0px ${INK}`,
            clipPath: 'polygon(0% 4%, 100% 0%, 100% 95%, 1% 100%)',
          }}
        >
          <div
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 28,
              letterSpacing: 3,
              color: INK,
              opacity: 0.7,
            }}
          >
            BATEAU 40 000 € — RÈGLE DES 10 %
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif',
              fontSize: 150,
              lineHeight: 1,
              color: INK,
              fontWeight: 700,
            }}
          >
            {counter.toLocaleString('fr-FR')} €
          </div>
          <div
            style={{
              fontFamily: 'Montserrat, Poppins, sans-serif',
              fontSize: 38,
              fontWeight: 600,
              color: INK,
              opacity: 0.75,
            }}
          >
            par an, même à quai
          </div>
        </div>

        {/* Marquee strip — punk collage band */}
        <div
          style={{
            position: 'absolute',
            left: 0,
            bottom: 92,
            width: '100%',
            height: 62,
            backgroundColor: INK,
            transform: 'rotate(-1.1deg)',
            overflow: 'hidden',
            display: 'flex',
            alignItems: 'center',
            opacity: interpolate(frame, [60, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              display: 'flex',
              transform: `translateX(${marqueeX}px)`,
              whiteSpace: 'nowrap',
              fontFamily: 'Space Mono, monospace',
              fontSize: 30,
              letterSpacing: 8,
              color: GOLD,
            }}
          >
            {Array.from({ length: 8 }).map((_, i) => (
              <span key={i} style={{ paddingRight: 60 }}>
                PLACE AU PORT · ASSURANCE · ANTIFOULING · HIVERNAGE ·
              </span>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene11BateauFantome;
```

```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene12EpargneConcentree',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const BARS = [
  { label: 'T2 2025', value: 18.7 },
  { label: 'T3 2025', value: 18.3 },
  { label: 'FIN 2025', value: 17.19 },
];

const Scene12EpargneConcentree: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera — slow tilt/pan
  const camY = interpolate(frame, [0, durationInFrames], [30, -20], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, 120], [1.06, 1.0], {
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const revealSlash = interpolate(frame, [220, 250], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const twoThirds = interpolate(frame, [300, 400], [0, 0.66], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const ringR = 130;
  const ringC = 2 * Math.PI * ringR;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(135deg, ${DEEP} 0%, ${BG} 40%, #6552CB 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: -160,
            top: 200 + Math.sin(frame * 0.012) * 30,
            width: 700,
            height: 700,
            backgroundColor: LIGHT,
            opacity: 0.045,
            transform: `rotate(${frame * 0.04}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 90,
            top: -140,
            width: 640,
            height: 640,
            borderRadius: '50%',
            backgroundColor: GOLD,
            opacity: 0.05,
            transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — vertical scan lines */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(90deg, ${LIGHT} 0px, ${LIGHT} 2px, transparent 2px, transparent 60px)`,
          opacity: 0.06,
          transform: `translateX(${(frame * 0.4) % 60}px)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const s = seededRandom(i + 11);
          const s2 = seededRandom(i + 44);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${s * 100}%`,
                top: `${((s2 * 100 - frame * (0.04 + s * 0.06)) % 110 + 110) % 110}%`,
                width: 6 + s2 * 8,
                height: 6 + s2 * 8,
                backgroundColor: i % 3 === 0 ? GOLD : LILAC,
                opacity: 0.2 + s * 0.25,
                transform: `rotate(${frame * (0.3 + s)}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>
        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 90,
            opacity: sp(4),
            transform: `translateX(${interpolate(sp(4), [0, 1], [-70, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif',
              fontSize: 34,
              letterSpacing: 8,
              color: GOLD,
              fontWeight: 700,
            }}
          >
            TAUX D’ÉPARGNE DES MÉNAGES
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 116,
              color: LIGHT,
              lineHeight: 1,
              marginTop: 6,
            }}
          >
            CHAMPIONS SUR LE PAPIER
          </div>
        </div>

        {/* Bar chart graphic */}
        <div
          style={{
            position: 'absolute',
            left: 130,
            bottom: 210,
            display: 'flex',
            alignItems: 'flex-end',
            gap: 54,
          }}
        >
          {BARS.map((b, i) => {
            const grow = sp(50 + i * 20, { damping: 13 });
            const h = interpolate(grow, [0, 1], [0, b.value * 26]);
            const active = i === 2;
            return (
              <div key={b.label} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
                <div
                  style={{
                    fontFamily: 'Space Mono, monospace',
                    fontSize: 42,
                    color: active ? GOLD : LIGHT,
                    marginBottom: 14,
                    opacity: grow,
                  }}
                >
                  {b.value.toString().replace('.', ',')} %
                </div>
                <div
                  style={{
                    width: 130,
                    height: h,
                    backgroundColor: active ? GOLD : LIGHT,
                    boxShadow: `12px 12px 0px ${INK}`,
                    transform: `translateY(${Math.sin(frame * 0.05 + i) * 3}px)`,
                  }}
                />
                <div
                  style={{
                    fontFamily: 'Montserrat, sans-serif',
                    fontSize: 28,
                    fontWeight: 600,
                    color: LIGHT,
                    opacity: 0.8,
                    marginTop: 16,
                  }}
                >
                  {b.label}
                </div>
              </div>
            );
          })}
        </div>

        {/* Torn-paper slash reveal — the brutal reality */}
        <div
          style={{
            position: 'absolute',
            right: 90,
            top: 300,
            width: 720,
            backgroundColor: LIGHT,
            padding: '40px 46px',
            clipPath: `polygon(0% 3%, 100% 0%, 98% 97%, 2% 100%)`,
            transform: `rotate(-1.8deg) scale(${0.9 + revealSlash * 0.1})`,
            opacity: revealSlash,
            boxShadow: `20px 20px 0px ${INK}`,
          }}
        >
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 74,
              color: INK,
              lineHeight: 1.02,
            }}
          >
            MAIS CETTE ÉPARGNE EST ULTRA CONCENTRÉE
          </div>
        </div>

        {/* Donut graphic — 65 ans et plus */}
        <div
          style={{
            position: 'absolute',
            right: 240,
            bottom: 170,
            opacity: interpolate(frame, [300, 330], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `scale(${0.94 + Math.sin(frame * 0.04) * 0.015})`,
          }}
        >
          <svg width="320" height="320" viewBox="0 0 320 320">
            <circle cx="160" cy="160" r={ringR} stroke={LIGHT} strokeWidth="34" fill="none" opacity="0.25" />
            <circle
              cx="160"
              cy="160"
              r={ringR}
              stroke={GOLD}
              strokeWidth="34"
              fill="none"
              strokeDasharray={`${ringC * twoThirds} ${ringC}`}
              strokeLinecap="butt"
              transform="rotate(-90 160 160)"
            />
            <text
              x="160"
              y="150"
              textAnchor="middle"
              fill={LIGHT}
              fontFamily="Clash Display, Anton, sans-serif"
              fontSize="66"
            >
              2/3
            </text>
            <text
              x="160"
              y="196"
              textAnchor="middle"
              fill={LIGHT}
              fontFamily="Montserrat, sans-serif"
              fontSize="26"
              opacity="0.85"
            >
              65 ANS ET +
            </text>
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 130,
            bottom: 110,
            fontFamily: 'Montserrat, Poppins, sans-serif',
            fontSize: 40,
            fontWeight: 500,
            color: LIGHT,
            opacity: interpolate(frame, [380, 420], [0, 0.9], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          Les jeunes actifs, eux, n’épargnent presque rien.
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene12EpargneConcentree;
```

```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene13PrecariteSilencieuse',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const Scene13PrecariteSilencieuse: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera — slow orbit feel via rotation + scale
  const camRot = Math.sin(frame * 0.006) * 1.1;
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.07], { extrapolateRight: 'clamp' });

  const dripHeight = interpolate(frame, [140, 420], [0, 520], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.quad),
  });

  const stampIn = sp(330, { damping: 9, stiffness: 90 });
  const shake = frame > 330 && frame < 344 ? Math.sin(frame * 3.4) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 75% 80%, #6A57C4 0%, ${BG} 42%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: '30%',
            top: -260,
            width: 760,
            height: 760,
            borderRadius: '50%',
            border: `60px solid ${LIGHT}`,
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.018) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: -120,
            bottom: -200,
            width: 620,
            height: 620,
            backgroundColor: GOLD,
            opacity: 0.05,
            transform: `rotate(${25 + frame * 0.03}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — diagonal grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(45deg, ${LIGHT} 0px, ${LIGHT} 1.5px, transparent 1.5px, transparent 48px)`,
          opacity: 0.07,
          transform: `translate(${(frame * 0.3) % 48}px, ${(frame * 0.3) % 48}px)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 30 }).map((_, i) => {
          const s = seededRandom(i + 21);
          const s2 = seededRandom(i + 90);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${s * 100}%`,
                top: `${((s2 * 100 + frame * (0.03 + s * 0.05)) % 110) - 5}%`,
                width: 3 + s * 5,
                height: 22 + s2 * 30,
                backgroundColor: LIGHT,
                opacity: 0.08 + s2 * 0.14,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) rotate(${camRot}deg) translateX(${shake}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 150,
            width: 1080,
            opacity: sp(8),
            transform: `translateY(${interpolate(sp(8), [0, 1], [70, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif',
              fontSize: 130,
              lineHeight: 0.95,
              color: LIGHT,
              fontWeight: 700,
            }}
          >
            PERSONNE N’A ACHETÉ
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 12,
              padding: '8px 24px',
              backgroundColor: GOLD,
              transform: `rotate(-1.4deg) scale(${sp(34)})`,
            }}
          >
            <span
              style={{
                fontFamily: 'Clash Display, Anton, sans-serif',
                fontSize: 130,
                lineHeight: 1,
                color: INK,
                fontWeight: 700,
              }}
            >
              SA PRÉCARITÉ
            </span>
          </div>
        </div>

        {/* Icon graphic — slow drip into an empty wallet */}
        <div
          style={{
            position: 'absolute',
            right: 200,
            top: 430,
            opacity: sp(90),
            transform: `scale(${sp(90)})`,
          }}
        >
          <svg width="360" height="560" viewBox="0 0 360 560">
            <line x1="180" y1="0" x2="180" y2="520" stroke={LIGHT} strokeWidth="3" opacity="0.28" />
            <rect x="176" y="0" width="8" height={dripHeight} fill={GOLD} opacity="0.9" />
            <circle cx="180" cy={dripHeight} r={9 + Math.sin(frame * 0.3) * 2} fill={GOLD} />
            <rect x="46" y="400" width="268" height="150" rx="16" fill={LIGHT} />
            <rect x="46" y="452" width="268" height="18" fill={INK} opacity="0.85" />
            <circle cx="252" cy="482" r="20" fill={INK} opacity="0.85" />
          </svg>
        </div>

        {/* Micro-touch cards — petites touches */}
        <div
          style={{
            position: 'absolute',
            left: 130,
            bottom: 210,
            display: 'flex',
            gap: 26,
          }}
        >
          {['+ 1 CANAPÉ', '+ 1 VOITURE', '+ 3 ABONNEMENTS'].map((t, i) => {
            const s = sp(170 + i * 26, { damping: 11 });
            return (
              <div
                key={t}
                style={{
                  padding: '22px 34px',
                  backgroundColor: i === 2 ? GOLD : LIGHT,
                  color: INK,
                  fontFamily: 'Space Mono, monospace',
                  fontSize: 32,
                  fontWeight: 700,
                  letterSpacing: 2,
                  transform: `rotate(${i % 2 === 0 ? -2 : 2.4}deg) translateY(${interpolate(s, [0, 1], [60, 0]) + Math.sin(frame * 0.05 + i) * 4}px)`,
                  opacity: s,
                  boxShadow: `12px 12px 0px ${INK}`,
                  clipPath: 'polygon(0% 5%, 100% 0%, 100% 94%, 1% 100%)',
                }}
              >
                {t}
              </div>
            );
          })}
        </div>

        {/* Stamp payoff */}
        <div
          style={{
            position: 'absolute',
            right: 150,
            bottom: 150,
            padding: '18px 40px',
            border: `8px solid ${LIGHT}`,
            transform: `rotate(-8deg) scale(${interpolate(stampIn, [0, 1], [2.2, 1])})`,
            opacity: stampIn,
          }}
        >
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 72,
              color: LIGHT,
              letterSpacing: 4,
            }}
          >
            + 800 € / MOIS
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 40%, rgba(0,0,0,0.6) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene13PrecariteSilencieuse;
```

```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene14LifestyleCreep',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const Scene14LifestyleCreep: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera — lateral pan across the staircase
  const camX = interpolate(frame, [0, durationInFrames], [90, -90], {
    extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.quad),
  });
  const camScale = interpolate(frame, [0, 90], [1.1, 1.02], { extrapolateRight: 'clamp' });

  const steps = [
    { label: 'CANAPÉ', cost: '+ 90 €' },
    { label: 'VOITURE', cost: '+ 260 €' },
    { label: 'ABOS', cost: '+ 210 €' },
    { label: 'SORTIES', cost: '+ 240 €' },
  ];

  const totalIn = sp(300, { damping: 10 });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(200deg, #6E5AD4 0%, ${BG} 46%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 200 + Math.sin(frame * 0.011) * 50,
            top: 60,
            width: 560,
            height: 560,
            backgroundColor: LIGHT,
            opacity: 0.05,
            transform: `rotate(${frame * 0.05}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 120,
            bottom: 80,
            width: 520,
            height: 520,
            borderRadius: '50%',
            border: `4px dashed ${GOLD}`,
            opacity: 0.08,
            transform: `rotate(${-frame * 0.12}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — horizontal rulers */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(0deg, ${LIGHT} 0px, ${LIGHT} 2px, transparent 2px, transparent 90px)`,
          opacity: 0.05,
          transform: `translateY(${(frame * 0.5) % 90}px)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const s = seededRandom(i + 55);
          const s2 = seededRandom(i + 130);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${s * 100}%`,
                top: `${((s2 * 100 - frame * 0.06) % 110 + 110) % 110}%`,
                width: 8,
                height: 8,
                borderRadius: '50%',
                backgroundColor: GOLD,
                opacity: 0.12 + s * 0.3,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)` }}>
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 100,
            opacity: sp(6),
            transform: `translateY(${interpolate(sp(6), [0, 1], [-60, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 112,
              color: LIGHT,
              lineHeight: 0.98,
            }}
          >
            LA DILATATION INVISIBLE
          </div>
          <div
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 34,
              letterSpacing: 8,
              color: GOLD,
              marginTop: 10,
            }}
          >
            LIFESTYLE CREEP · MOIS APRÈS MOIS
          </div>
        </div>

        {/* Staircase of costs — animated cards */}
        <div
          style={{
            position: 'absolute',
            left: 180,
            bottom: 190,
            display: 'flex',
            alignItems: 'flex-end',
            gap: 30,
          }}
        >
          {steps.map((st, i) => {
            const s = sp(70 + i * 30, { damping: 13, stiffness: 110 });
            const h = 140 + i * 96;
            return (
              <div
                key={st.label}
                style={{
                  width: 250,
                  height: interpolate(s, [0, 1], [0, h]),
                  backgroundColor: i === 3 ? GOLD : LIGHT,
                  boxShadow: `14px 14px 0px ${INK}`,
                  display: 'flex',
                  flexDirection: 'column',
                  justifyContent: 'flex-start',
                  padding: 22,
                  transform: `translateY(${Math.sin(frame * 0.05 + i * 1.2) * 4}px) rotate(${i % 2 === 0 ? -0.8 : 0.9}deg)`,
                  overflow: 'hidden',
                }}
              >
                <div
                  style={{
                    fontFamily: 'Anton, sans-serif',
                    fontSize: 40,
                    color: INK,
                    letterSpacing: 1,
                  }}
                >
                  {st.label}
                </div>
                <div
                  style={{
                    fontFamily: 'Space Mono, monospace',
                    fontSize: 34,
                    color: INK,
                    opacity: 0.72,
                    marginTop: 6,
                  }}
                >
                  {st.cost}
                </div>
                <svg width="120" height="120" viewBox="0 0 120 120" style={{ marginTop: 'auto' }}>
                  {i === 0 && (
                    <g fill={INK} opacity="0.85">
                      <rect x="10" y="52" width="100" height="34" rx="8" />
                      <rect x="4" y="44" width="26" height="34" rx="8" />
                      <rect x="90" y="44" width="26" height="34" rx="8" />
                    </g>
                  )}
                  {i === 1 && (
                    <g fill={INK} opacity="0.85">
                      <rect x="8" y="52" width="104" height="30" rx="10" />
                      <path d="M26 52 L38 32 L84 32 L96 52 Z" />
                      <circle cx="34" cy="88" r="12" />
                      <circle cx="88" cy="88" r="12" />
                    </g>
                  )}
                  {i === 2 && (
                    <g fill={INK} opacity="0.85">
                      <rect x="12" y="26" width="96" height="26" rx="6" />
                      <rect x="12" y="60" width="96" height="26" rx="6" />
                      <rect
                        x="12"
                        y="94"
                        width={40 + Math.sin(frame * 0.1) * 20}
                        height="16"
                        rx="6"
                      />
                    </g>
                  )}
                  {i === 3 && (
                    <g fill={INK} opacity="0.85">
                      <circle cx="60" cy="60" r="40" />
                      <rect x="54" y="30" width="12" height="34" fill={GOLD} />
                      <rect x="54" y="58" width="30" height="12" fill={GOLD} />
                    </g>
                  )}
                </svg>
              </div>
            );
          })}
        </div>

        {/* Payoff total card */}
        <div
          style={{
            position: 'absolute',
            right: 130,
            top: 380,
            padding: '34px 50px',
            backgroundColor: INK,
            transform: `rotate(2.2deg) scale(${totalIn})`,
            opacity: totalIn,
            border: `6px solid ${LIGHT}`,
          }}
        >
          <div
            style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 32,
              fontWeight: 600,
              color: LILAC,
              letterSpacing: 4,
            }}
          >
            EN 3 ANS, LE BUDGET A GONFLÉ DE
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif',
              fontSize: 160,
              lineHeight: 1,
              color: GOLD,
              fontWeight: 700,
            }}
          >
            800 €
          </div>
          <div
            style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 34,
              color: LIGHT,
              opacity: 0.85,
            }}
          >
            sans qu’on s’en rende compte
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 55%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.56) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene14LifestyleCreep;
```

```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene15CoutParUsage',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const Scene15CoutParUsage: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera — zoom out from the formula
  const camScale = interpolate(frame, [0, 200], [1.22, 1.0], {
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const camY = Math.sin(frame * 0.01) * 14;

  const numIn = sp(20);
  const divIn = sp(60);
  const denIn = sp(95);
  const resIn = sp(160, { damping: 10 });
  const blink = Math.sin(frame * 0.25) > 0 ? 1 : 0.35;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% 40%, #7160D8 0%, ${BG} 48%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: 1200,
            height: 1200,
            marginLeft: -600,
            marginTop: -600,
            borderRadius: '50%',
            border: `2px solid ${LIGHT}`,
            opacity: 0.05,
            transform: `scale(${0.7 + Math.sin(frame * 0.02) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 80,
            bottom: 60,
            width: 460,
            height: 460,
            backgroundColor: GOLD,
            opacity: 0.05,
            transform: `rotate(${frame * 0.07}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — engineering grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `linear-gradient(${LIGHT} 1px, transparent 1px), linear-gradient(90deg, ${LIGHT} 1px, transparent 1px)`,
          backgroundSize: '72px 72px',
          opacity: 0.06,
          transform: `translate(${(frame * 0.25) % 72}px, ${(frame * 0.25) % 72}px)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 26 }).map((_, i) => {
          const s = seededRandom(i + 200);
          const s2 = seededRandom(i + 320);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${s * 100}%`,
                top: `${((s2 * 100 + frame * 0.05) % 110) - 5}%`,
                fontFamily: 'Space Mono, monospace',
                fontSize: 20 + s * 16,
                color: LIGHT,
                opacity: 0.1 + s2 * 0.16,
                transform: `rotate(${s * 40 - 20}deg)`,
              }}
            >
              {i % 2 === 0 ? '€' : '÷'}
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            width: '100%',
            textAlign: 'center',
            opacity: sp(2),
          }}
        >
          <span
            style={{
              display: 'inline-block',
              padding: '12px 40px',
              backgroundColor: LIGHT,
              color: INK,
              fontFamily: 'Space Mono, monospace',
              fontSize: 32,
              letterSpacing: 8,
              fontWeight: 700,
              transform: 'rotate(-1.2deg)',
            }}
          >
            PRINCIPE N°01
          </span>
        </div>

        {/* Formula row */}
        <div
          style={{
            position: 'absolute',
            top: 300,
            left: 0,
            width: '100%',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 40,
          }}
        >
          <div
            style={{
              padding: '30px 44px',
              backgroundColor: LIGHT,
              boxShadow: `14px 14px 0px ${INK}`,
              transform: `translateY(${interpolate(numIn, [0, 1], [80, 0])}px) rotate(-1.6deg)`,
              opacity: numIn,
              textAlign: 'center',
            }}
          >
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 28, color: INK, opacity: 0.65 }}>
              PRIX PAYÉ
            </div>
            <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 110, color: INK, fontWeight: 700 }}>
              799 €
            </div>
          </div>

          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 120,
              color: GOLD,
              transform: `scale(${divIn}) rotate(${Math.sin(frame * 0.08) * 4}deg)`,
              opacity: divIn,
            }}
          >
            ÷
          </div>

          <div
            style={{
              padding: '30px 44px',
              backgroundColor: GOLD,
              boxShadow: `14px 14px 0px ${INK}`,
              transform: `translateY(${interpolate(denIn, [0, 1], [80, 0])}px) rotate(1.8deg)`,
              opacity: denIn,
              textAlign: 'center',
            }}
          >
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 28, color: INK, opacity: 0.7 }}>
              USAGES RÉELS / AN
            </div>
            <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 110, color: INK, fontWeight: 700 }}>
              24
            </div>
          </div>
        </div>

        {/* Result — robot cuiseur */}
        <div
          style={{
            position: 'absolute',
            top: 610,
            left: 0,
            width: '100%',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 44,
            opacity: resIn,
            transform: `scale(${interpolate(resIn, [0, 1], [0.72, 1])})`,
          }}
        >
          <svg width="180" height="180" viewBox="0 0 180 180">
            <rect x="30" y="60" width="120" height="96" rx="14" fill={LIGHT} />
            <rect x="46" y="20" width="88" height="42" rx="10" fill={LIGHT} opacity="0.85" />
            <circle cx="90" cy="108" r={26 + Math.sin(frame * 0.2) * 2} fill={INK} opacity="0.8" />
            <rect x="120" y="88" width="18" height="18" rx="4" fill={GOLD} opacity={blink} />
          </svg>
          <div>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 150,
                color: LIGHT,
                lineHeight: 1,
              }}
            >
              33 € / USAGE
            </div>
            <div
              style={{
                fontFamily: 'Montserrat, Poppins, sans-serif',
                fontSize: 42,
                fontWeight: 500,
                color: LILAC,
              }}
            >
              soit trois fois le menu du restaurant du coin
            </div>
          </div>
        </div>

        {/* Collage tape corner */}
        <div
          style={{
            position: 'absolute',
            left: 70,
            top: 250,
            width: 300,
            height: 54,
            backgroundColor: LIGHT,
            opacity: 0.25,
            transform: 'rotate(-42deg)',
          }}
        />
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 40%, rgba(0,0,0,0.6) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene15CoutParUsage;
```

```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene16DuelDesVestes',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const Jacket: React.FC<{ fill: string; accent: string }> = ({ fill, accent }) => (
  <svg width="230" height="250" viewBox="0 0 230 250">
    <path d="M60 30 L115 60 L170 30 L215 70 L190 110 L180 100 L180 230 L50 230 L50 100 L40 110 L15 70 Z" fill={fill} />
    <path d="M115 60 L115 230" stroke={accent} strokeWidth="6" />
    <circle cx="115" cy="120" r="7" fill={accent} />
    <circle cx="115" cy="160" r="7" fill={accent} />
    <circle cx="115" cy="200" r="7" fill={accent} />
  </svg>
);

const Scene16DuelDesVestes: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera — subtle tilt then settle
  const camRot = interpolate(frame, [0, 120], [-1.6, 0], {
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const camScale = interpolate(frame, [0, durationInFrames], [1.03, 1.09], { extrapolateRight: 'clamp' });

  const leftIn = sp(16, { damping: 13 });
  const rightIn = sp(46, { damping: 13 });
  const verdictIn = sp(230, { damping: 9 });
  const impact = frame > 230 && frame < 246 ? Math.sin(frame * 3.1) * 6 : 0;

  const barLeft = interpolate(sp(120), [0, 1], [0, 40]);
  const barRight = interpolate(sp(150), [0, 1], [0, 420]);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(90deg, ${DEEP} 0%, ${BG} 50%, #6E5AD4 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: -100,
            width: 6,
            height: 1300,
            marginLeft: -3,
            backgroundColor: LIGHT,
            opacity: 0.12,
            transform: `rotate(${1.5 + Math.sin(frame * 0.02) * 1}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 60,
            top: 120,
            width: 520,
            height: 520,
            borderRadius: '50%',
            backgroundColor: LIGHT,
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 60,
            bottom: 80,
            width: 480,
            height: 480,
            backgroundColor: GOLD,
            opacity: 0.05,
            transform: `rotate(${-frame * 0.05}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — concentric circles */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-radial-gradient(circle at 50% 50%, ${LIGHT} 0px, ${LIGHT} 1.5px, transparent 1.5px, transparent 70px)`,
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.03})`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const s = seededRandom(i + 400);
          const s2 = seededRandom(i + 500);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${s * 100}%`,
                top: `${((s2 * 100 - frame * 0.05) % 110 + 110) % 110}%`,
                width: 5 + s * 7,
                height: 5 + s * 7,
                backgroundColor: i % 2 === 0 ? LILAC : LIGHT,
                borderRadius: '50%',
                opacity: 0.15 + s2 * 0.25,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill
        style={{ transform: `scale(${camScale}) rotate(${camRot}deg) translateX(${impact}px)` }}
      >
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            width: '100%',
            textAlign: 'center',
            fontFamily: 'Anton, sans-serif',
            fontSize: 96,
            color: LIGHT,
            opacity: sp(4),
          }}
        >
          LAQUELLE EST LA PLUS CHÈRE ?
        </div>

        {/* Left card — 400 € */}
        <div
          style={{
            position: 'absolute',
            left: 190,
            top: 250,
            width: 620,
            padding: 36,
            backgroundColor: LIGHT,
            boxShadow: `18px 18px 0px ${INK}`,
            transform: `translateX(${interpolate(leftIn, [0, 1], [-220, 0])}px) rotate(-1.5deg)`,
            opacity: leftIn,
            clipPath: 'polygon(0% 2%, 100% 0%, 99% 98%, 1% 100%)',
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 26 }}>
            <div style={{ transform: `translateY(${Math.sin(frame * 0.05) * 5}px)` }}>
              <Jacket fill={INK} accent={GOLD} />
            </div>
            <div>
              <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 84, color: INK, lineHeight: 1 }}>400 €</div>
              <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 30, color: INK, opacity: 0.7 }}>
                200 PORTS
              </div>
            </div>
          </div>
          <div style={{ marginTop: 20, height: 34, backgroundColor: '#E3DDF7' }}>
            <div style={{ width: barLeft, height: 34, backgroundColor: INK }} />
          </div>
          <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 96, color: INK, fontWeight: 700 }}>
            2 € / PORT
          </div>
        </div>

        {/* Right card — 150 € */}
        <div
          style={{
            position: 'absolute',
            right: 190,
            top: 250,
            width: 620,
            padding: 36,
            backgroundColor: GOLD,
            boxShadow: `18px 18px 0px ${INK}`,
            transform: `translateX(${interpolate(rightIn, [0, 1], [220, 0])}px) rotate(1.7deg)`,
            opacity: rightIn,
            clipPath: 'polygon(0% 0%, 100% 2%, 99% 100%, 1% 98%)',
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 26 }}>
            <div style={{ transform: `translateY(${Math.sin(frame * 0.05 + 1.5) * 5}px)` }}>
              <Jacket fill={INK} accent={LIGHT} />
            </div>
            <div>
              <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 84, color: INK, lineHeight: 1 }}>150 €</div>
              <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 30, color: INK, opacity: 0.75 }}>
                3 PORTS
              </div>
            </div>
          </div>
          <div style={{ marginTop: 20, height: 34, backgroundColor: '#F0DFA0' }}>
            <div style={{ width: barRight, height: 34, backgroundColor: INK }} />
          </div>
          <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 96, color: INK, fontWeight: 700 }}>
            50 € / PORT
          </div>
        </div>

        {/* Verdict band */}
        <div
          style={{
            position: 'absolute',
            left: 0,
            bottom: 150,
            width: '100%',
            display: 'flex',
            justifyContent: 'center',
            opacity: verdictIn,
            transform: `scale(${interpolate(verdictIn, [0, 1], [0.8, 1])})`,
          }}
        >
          <div
            style={{
              padding: '24px 60px',
              backgroundColor: INK,
              border: `6px solid ${LIGHT}`,
              transform: 'rotate(-1.2deg)',
              fontFamily: 'Anton, sans-serif',
              fontSize: 78,
              color: LIGHT,
              letterSpacing: 2,
            }}
          >
            LA MOINS CHÈRE COÛTE 25 FOIS PLUS
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene16DuelDesVestes;
```

```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene17FiltreHonnetete',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const Scene17FiltreHonnetete: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera — slow orbit
  const camScale = interpolate(frame, [0, durationInFrames], [1.12, 1.0], { extrapolateRight: 'clamp' });
  const camX = Math.cos(frame * 0.009) * 26;
  const camY = Math.sin(frame * 0.009) * 16;

  const pct = interpolate(frame, [120, 300], [0, 70], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const ringR = 190;
  const ringC = 2 * Math.PI * ringR;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 68% 50%, #7563DD 0%, ${BG} 44%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: -140,
            top: 60,
            width: 700,
            height: 700,
            border: `70px solid ${LIGHT}`,
            opacity: 0.04,
            transform: `rotate(${frame * 0.04}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 300,
            bottom: -180,
            width: 0,
            height: 0,
            borderLeft: '300px solid transparent',
            borderRight: '300px solid transparent',
            borderBottom: `500px solid ${GOLD}`,
            opacity: 0.05,
            transform: `rotate(${190 + Math.sin(frame * 0.015) * 8}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — chevron lines */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(-45deg, ${LIGHT} 0px, ${LIGHT} 2px, transparent 2px, transparent 56px)`,
          opacity: 0.05,
          transform: `translateX(${-(frame * 0.6) % 56}px)`,
        }}
      />

      {/* Floating particles — falling impulse tags */}
      <AbsoluteFill>
        {Array.from({ length: 18 }).map((_, i) => {
          const s = seededRandom(i + 610);
          const s2 = seededRandom(i + 720);
          const fall = ((s2 * 120 + frame * (0.22 + s * 0.2)) % 130) - 15;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${6 + s * 40}%`,
                top: `${fall}%`,
                padding: '8px 16px',
                backgroundColor: LIGHT,
                opacity: 0.22,
                fontFamily: 'Space Mono, monospace',
                fontSize: 24,
                color: INK,
                transform: `rotate(${s * 60 - 30}deg)`,
              }}
            >
              ACHAT IMPULSIF
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${camX}px, ${camY}px)` }}>
        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 120,
            width: 760,
            opacity: sp(8),
            transform: `translateX(${interpolate(sp(8), [0, 1], [-90, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif',
              fontSize: 36,
              letterSpacing: 6,
              color: GOLD,
              fontWeight: 700,
            }}
          >
            LE FILTRE
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif',
              fontSize: 118,
              lineHeight: 0.96,
              color: LIGHT,
              fontWeight: 700,
              marginTop: 8,
            }}
          >
            UNE RÈGLE QUI T’OBLIGE À ÊTRE HONNÊTE
          </div>
        </div>

        {/* Funnel graphic */}
        <div
          style={{
            position: 'absolute',
            left: 190,
            bottom: 170,
            opacity: sp(70),
            transform: `scale(${sp(70)})`,
          }}
        >
          <svg width="460" height="420" viewBox="0 0 460 420">
            <path d="M20 20 L440 20 L280 210 L280 380 L180 340 L180 210 Z" fill={LIGHT} opacity="0.9" />
            <path
              d="M20 20 L440 20 L280 210 L280 380 L180 340 L180 210 Z"
              fill="none"
              stroke={INK}
              strokeWidth="8"
            />
            <circle cx="230" cy={120 + Math.sin(frame * 0.12) * 26} r="18" fill={GOLD} />
            <circle cx="230" cy={300 + Math.sin(frame * 0.12 + 2) * 12} r="10" fill={INK} />
          </svg>
        </div>

        {/* Percentage ring */}
        <div
          style={{
            position: 'absolute',
            right: 170,
            top: 280,
            opacity: interpolate(frame, [110, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `scale(${1 + Math.sin(frame * 0.05) * 0.012})`,
          }}
        >
          <svg width="440" height="440" viewBox="0 0 440 440">
            <circle cx="220" cy="220" r={ringR} stroke={LIGHT} strokeWidth="40" fill="none" opacity="0.2" />
            <circle
              cx="220"
              cy="220"
              r={ringR}
              stroke={GOLD}
              strokeWidth="40"
              fill="none"
              strokeDasharray={`${(ringC * pct) / 100} ${ringC}`}
              transform="rotate(-90 220 220)"
            />
            <text
              x="220"
              y="238"
              textAnchor="middle"
              fill={LIGHT}
              fontFamily="Clash Display, Anton, sans-serif"
              fontSize="130"
              fontWeight="700"
            >
              {Math.round(pct)}%
            </text>
          </svg>
          <div
            style={{
              textAlign: 'center',
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 38,
              fontWeight: 600,
              color: LIGHT,
              opacity: 0.9,
              marginTop: 4,
            }}
          >
            d’achats impulsifs éliminés
          </div>
        </div>

        {/* Collage note */}
        <div
          style={{
            position: 'absolute',
            left: 700,
            bottom: 120,
            padding: '20px 32px',
            backgroundColor: LIGHT,
            transform: `rotate(-3deg) scale(${sp(260)})`,
            opacity: sp(260),
            boxShadow: `10px 10px 0px ${INK}`,
          }}
        >
          <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 30, color: INK, fontWeight: 700 }}>
            &gt; 50 € = TU CALCULES AVANT
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene17FiltreHonnetete;
```

```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene18TestDuRemplacement',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const Scene18TestDuRemplacement: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera — push in on the empty shelf
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.11], { extrapolateRight: 'clamp' });
  const camY = interpolate(frame, [0, durationInFrames], [-16, 16], { extrapolateRight: 'clamp' });

  // Objects vanish one by one
  const vanish = (i: number) =>
    interpolate(frame, [150 + i * 34, 186 + i * 34], [1, 0], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
      easing: Easing.in(Easing.quad),
    });

  const questionIn = sp(300, { damping: 10 });

  const items = ['SORBETIÈRE', 'FRITEUSE', 'RACLETTE ×12', 'VESTE 2019'];

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(160deg, ${DEEP} 0%, ${BG} 55%, #5F4CB8 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            right: 120,
            top: 60 + Math.sin(frame * 0.014) * 26,
            width: 420,
            height: 420,
            borderRadius: '50%',
            backgroundColor: LIGHT,
            opacity: 0.05,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 40,
            bottom: -120,
            width: 700,
            height: 700,
            border: `3px solid ${GOLD}`,
            opacity: 0.07,
            transform: `rotate(${frame * 0.05}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — night stars grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `radial-gradient(${LIGHT} 1.6px, transparent 1.6px)`,
          backgroundSize: '58px 58px',
          opacity: 0.08 + Math.sin(frame * 0.05) * 0.02,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 26 }).map((_, i) => {
          const s = seededRandom(i + 810);
          const s2 = seededRandom(i + 910);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${s * 100}%`,
                top: `${((s2 * 100 - frame * 0.045) % 110 + 110) % 110}%`,
                width: 4 + s2 * 6,
                height: 4 + s2 * 6,
                borderRadius: '50%',
                backgroundColor: LILAC,
                opacity: 0.2 + s * 0.3,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 110,
            opacity: sp(4),
            transform: `translateY(${interpolate(sp(4), [0, 1], [-50, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 32,
              letterSpacing: 8,
              color: GOLD,
            }}
          >
            PRINCIPE N°02 — LE TEST DU REMPLACEMENT
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 104,
              color: LIGHT,
              lineHeight: 1,
              marginTop: 10,
            }}
          >
            SI ÇA DISPARAISSAIT CETTE NUIT
          </div>
        </div>

        {/* Shelf with vanishing objects */}
        <div
          style={{
            position: 'absolute',
            left: 130,
            top: 340,
            width: 1080,
          }}
        >
          <div style={{ display: 'flex', gap: 26 }}>
            {items.map((it, i) => {
              const enter = sp(30 + i * 18, { damping: 13 });
              const alive = vanish(i);
              return (
                <div
                  key={it}
                  style={{
                    width: 250,
                    height: 250,
                    backgroundColor: LIGHT,
                    boxShadow: `12px 12px 0px ${INK}`,
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'center',
                    justifyContent: 'center',
                    gap: 12,
                    opacity: enter * alive,
                    transform: `scale(${enter * (0.85 + alive * 0.15)}) rotate(${(i % 2 === 0 ? -2 : 2) * alive}deg) translateY(${(1 - alive) * -60}px)`,
                    filter: `blur(${(1 - alive) * 6}px)`,
                  }}
                >
                  <svg width="110" height="110" viewBox="0 0 110 110">
                    {i === 0 && (
                      <g fill={INK}>
                        <rect x="20" y="30" width="70" height="60" rx="10" />
                        <rect x="34" y="14" width="42" height="16" rx="6" />
                      </g>
                    )}
                    {i === 1 && (
                      <g fill={INK}>
                        <rect x="18" y="34" width="74" height="56" rx="12" />
                        <circle cx="55" cy="62" r="14" fill={GOLD} />
                      </g>
                    )}
                    {i === 2 && (
                      <g fill={INK}>
                        <rect x="14" y="46" width="82" height="30" rx="8" />
                        <rect x="26" y="26" width="58" height="18" rx="6" />
                        <rect x="20" y="82" width="70" height="10" rx="4" />
                      </g>
                    )}
                    {i === 3 && (
                      <g fill={INK}>
                        <path d="M30 20 L55 34 L80 20 L96 40 L84 52 L84 96 L26 96 L26 52 L14 40 Z" />
                      </g>
                    )}
                  </svg>
                  <span
                    style={{
                      fontFamily: 'Space Mono, monospace',
                      fontSize: 26,
                      color: INK,
                      fontWeight: 700,
                      letterSpacing: 1,
                    }}
                  >
                    {it}
                  </span>
                </div>
              );
            })}
          </div>
          <div
            style={{
              width: '100%',
              height: 16,
              backgroundColor: LIGHT,
              opacity: 0.5,
              marginTop: 18,
            }}
          />
        </div>

        {/* Question payoff */}
        <div
          style={{
            position: 'absolute',
            left: 0,
            bottom: 140,
            width: '100%',
            display: 'flex',
            justifyContent: 'center',
            opacity: questionIn,
            transform: `scale(${interpolate(questionIn, [0, 1], [0.82, 1])})`,
          }}
        >
          <div
            style={{
              padding: '28px 56px',
              backgroundColor: GOLD,
              transform: `rotate(${Math.sin(frame * 0.04) * 0.8 - 1.4}deg)`,
              boxShadow: `16px 16px 0px ${INK}`,
              fontFamily: 'Anton, sans-serif',
              fontSize: 82,
              color: INK,
              textAlign: 'center',
            }}
          >
            EST-CE QUE TU LE RACHÈTERAIS DEMAIN ?
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 45%, rgba(0,0,0,0) 40%, rgba(0,0,0,0.62) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene18TestDuRemplacement;
```

```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene19InertieDesObjets',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const Scene19InertieDesObjets: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera — slow diagonal drift
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.12], { extrapolateRight: 'clamp' });
  const camX = interpolate(frame, [0, durationInFrames], [-40, 40], { extrapolateRight: 'clamp' });

  const stampIn = sp(200, { damping: 8, stiffness: 100 });
  const impact = frame > 200 && frame < 218 ? Math.sin(frame * 3.2) * 8 : 0;

  const labels = [
    'MACHINE À PAIN',
    'KIT DE MACRAMÉ',
    'PATINS À GLACE',
    'CLAVIER SYNTHÉ',
    'VÉLO ELLIPTIQUE',
    'CARTONS FERMÉS',
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 20% 80%, #6957CE 0%, ${BG} 46%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 500,
            top: -200,
            width: 900,
            height: 900,
            borderRadius: '50%',
            backgroundColor: LIGHT,
            opacity: 0.035,
            transform: `scale(${1 + Math.sin(frame * 0.017) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 60,
            top: 400,
            width: 480,
            height: 480,
            backgroundColor: GOLD,
            opacity: 0.05,
            transform: `rotate(${-frame * 0.06}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — drifting dust lines */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(115deg, ${LIGHT} 0px, ${LIGHT} 1.4px, transparent 1.4px, transparent 40px)`,
          opacity: 0.05,
          transform: `translateY(${(frame * 0.35) % 40}px)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 34 }).map((_, i) => {
          const s = seededRandom(i + 1010);
          const s2 = seededRandom(i + 1120);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${s * 100}%`,
                top: `${((s2 * 100 + frame * 0.03) % 110) - 5}%`,
                width: 3 + s * 4,
                height: 3 + s * 4,
                borderRadius: '50%',
                backgroundColor: LIGHT,
                opacity: 0.12 + s2 * 0.2,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX + impact}px)` }}>
        {/* Drifting object chips — inertia field */}
        <AbsoluteFill>
          {labels.map((l, i) => {
            const s = seededRandom(i + 5);
            const enter = sp(20 + i * 16, { damping: 14 });
            const driftX = Math.sin(frame * 0.012 + i) * 26;
            const driftY = Math.cos(frame * 0.011 + i * 1.7) * 20;
            const left = [90, 1380, 210, 1500, 120, 1330][i];
            const top = [230, 190, 640, 560, 850, 840][i];
            return (
              <div
                key={l}
                style={{
                  position: 'absolute',
                  left,
                  top,
                  padding: '18px 28px',
                  backgroundColor: i % 3 === 0 ? GOLD : LIGHT,
                  color: INK,
                  fontFamily: 'Space Mono, monospace',
                  fontSize: 28,
                  fontWeight: 700,
                  letterSpacing: 1,
                  boxShadow: `10px 10px 0px ${INK}`,
                  opacity: enter * 0.95,
                  transform: `translate(${driftX}px, ${driftY}px) rotate(${(s * 10 - 5).toFixed(2)}deg) scale(${enter})`,
                  clipPath: 'polygon(0% 6%, 100% 0%, 99% 94%, 1% 100%)',
                }}
              >
                {l}
              </div>
            );
          })}
        </AbsoluteFill>

        {/* Central typographic block */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: 900,
            marginLeft: -450,
            marginTop: -230,
            textAlign: 'center',
            opacity: sp(90),
            transform: `scale(${interpolate(sp(90), [0, 1], [0.86, 1])})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif',
              fontSize: 128,
              lineHeight: 0.94,
              color: LIGHT,
              fontWeight: 700,
            }}
          >
            ILS EXISTENT PAR
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 14,
              padding: '10px 34px',
              backgroundColor: LIGHT,
              transform: `rotate(-1.6deg) scale(${sp(130)})`,
            }}
          >
            <span
              style={{
                fontFamily: 'Clash Display, Anton, sans-serif',
                fontSize: 150,
                color: INK,
                fontWeight: 700,
              }}
            >
              INERTIE
            </span>
          </div>
          <div
            style={{
              marginTop: 26,
              fontFamily: 'Montserrat, Poppins, sans-serif',
              fontSize: 46,
              fontWeight: 500,
              color: LILAC,
              opacity: sp(160),
            }}
          >
            pas par choix — et tu payes chaque mois pour les stocker
          </div>
        </div>

        {/* Stamp — destination */}
        <div
          style={{
            position: 'absolute',
            right: 210,
            bottom: 120,
            padding: '16px 36px',
            border: `7px solid ${GOLD}`,
            transform: `rotate(-7deg) scale(${interpolate(stampIn, [0, 1], [1.9, 1])})`,
            opacity: stampIn,
          }}
        >
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 64,
              color: GOLD,
              letterSpacing: 3,
            }}
          >
            LE BON COIN · VINTED · EMMAÜS
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 38%, rgba(0,0,0,0.62) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene19InertieDesObjets;
```

```tsx
import React from 'react';

import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene20CaFonctionnePoint',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#504099';
const LIGHT = '#F5F5F5';
const INK = '#000000';
const LILAC = '#C7B9FF';
const DEEP = '#2C2160';
const GOLD = '#FFC107';

const Scene20CaFonctionnePoint: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera — locked push then micro tilt
  const camScale = interpolate(frame, [0, 160], [1.14, 1.0], {
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const camRot = Math.sin(frame * 0.007) * 0.8;

  const phoneIn = sp(24, { damping: 14 });
  const cycleShift = -((frame * 1.9) % 380);

  const counter = Math.round(
    interpolate(frame, [230, 400], [0, 25000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })
  );
  const pointIn = sp(420, { damping: 9 });
  const impact = frame > 420 && frame < 438 ? Math.sin(frame * 3) * 7 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(20deg, ${DEEP} 0%, ${BG} 50%, #7261DA 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: 980,
            height: 980,
            marginLeft: -490,
            marginTop: -490,
            border: `100px solid ${LIGHT}`,
            borderRadius: '50%',
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 100,
            top: 120,
            width: 420,
            height: 420,
            backgroundColor: GOLD,
            opacity: 0.06,
            transform: `rotate(${25 + frame * 0.05}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — ticker grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `linear-gradient(90deg, ${LIGHT} 1px, transparent 1px)`,
          backgroundSize: '110px 100%',
          opacity: 0.06,
          transform: `translateX(${(frame * 0.7) % 110}px)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const s = seededRandom(i + 1300);
          const s2 = seededRandom(i + 1400);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${s * 100}%`,
                top: `${((s2 * 100 + frame * 0.06) % 110) - 5}%`,
                width: 6,
                height: 18,
                backgroundColor: i % 3 === 0 ? GOLD : LIGHT,
                opacity: 0.12 + s * 0.26,
                transform: `rotate(${s * 30}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill
        style={{ transform: `scale(${camScale}) rotate(${camRot}deg) translateX(${impact}px)` }}
      >
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            width: '100%',
            textAlign: 'center',
            opacity: sp(4),
          }}
        >
          <span
            style={{
              display: 'inline-block',
              padding: '12px 38px',
              backgroundColor: GOLD,
              color: INK,
              fontFamily: 'Space Mono, monospace',
              fontSize: 32,
              letterSpacing: 8,
              fontWeight: 700,
              transform: 'rotate(1.2deg)',
            }}
          >
            PRINCIPE N°03
          </span>
        </div>

        {/* Phone icon — still perfectly working */}
        <div
          style={{
            position: 'absolute',
            left: 230,
            top: 240,
            transform: `translateY(${interpolate(phoneIn, [0, 1], [140, 0]) + Math.sin(frame * 0.05) * 5}px) rotate(${Math.sin(frame * 0.04) * 1.6}deg) scale(${phoneIn})`,
            opacity: phoneIn,
          }}
        >
          <svg width="330" height="560" viewBox="0 0 330 560">
            <rect x="30" y="20" width="270" height="520" rx="42" fill={LIGHT} />
            <rect x="52" y="60" width="226" height="430" rx="18" fill={INK} />
            <rect x="132" y="34" width="66" height="12" rx="6" fill={INK} opacity="0.6" />
            <circle cx="165" cy="514" r="18" fill={INK} opacity="0.35" />
            <rect
              x="80"
              y={110 + Math.sin(frame * 0.06) * 6}
              width="170"
              height="18"
              rx="9"
              fill={GOLD}
              opacity="0.9"
            />
            <rect x="80" y="160" width="120" height="14" rx="7" fill={LIGHT} opacity="0.45" />
            <rect x="80" y="196" width="150" height="14" rx="7" fill={LIGHT} opacity="0.3" />
            <circle cx="165" cy="330" r={40 + Math.sin(frame * 0.12) * 3} fill={LIGHT} opacity="0.16" />
          </svg>
        </div>

        {/* Typographic verdict */}
        <div
          style={{
            position: 'absolute',
            right: 140,
            top: 250,
            width: 900,
            opacity: sp(70),
            transform: `translateX(${interpolate(sp(70), [0, 1], [120, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 118,
              lineHeight: 0.96,
              color: LIGHT,
            }}
          >
            IL FONCTIONNE.
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 10,
              padding: '6px 30px',
              backgroundColor: LIGHT,
              transform: `rotate(-2deg) scale(${pointIn === 0 ? 1 : interpolate(pointIn, [0, 1], [1.6, 1])})`,
              opacity: pointIn,
            }}
          >
            <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 118, color: INK }}>POINT.</span>
          </div>

          <div
            style={{
              marginTop: 40,
              padding: '26px 34px',
              backgroundColor: INK,
              border: `5px solid ${LIGHT}`,
              transform: 'rotate(0.8deg)',
              opacity: interpolate(frame, [220, 250], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            <div
              style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 30,
                fontWeight: 600,
                letterSpacing: 3,
                color: LILAC,
              }}
            >
              CHANGER DE SMARTPHONE TOUS LES 2,5 ANS
            </div>
            <div
              style={{
                fontFamily: 'Clash Display, Anton, sans-serif',
                fontSize: 130,
                lineHeight: 1,
                color: GOLD,
                fontWeight: 700,
              }}
            >
              {counter.toLocaleString('fr-FR')} €
            </div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 32, color: LIGHT, opacity: 0.85 }}>
              sur 30 ans de vie active
            </div>
          </div>
        </div>

        {/* Marketing loop ticker — punk band */}
        <div
          style={{
            position: 'absolute',
            left: 0,
            bottom: 96,
            width: '100%',
            height: 66,
            backgroundColor: GOLD,
            transform: 'rotate(1.2deg)',
            overflow: 'hidden',
            display: 'flex',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              display: 'flex',
              transform: `translateX(${cycleShift}px)`,
              whiteSpace: 'nowrap',
              fontFamily: 'Space Mono, monospace',
              fontSize: 30,
              letterSpacing: 6,
              color: INK,
              fontWeight: 700,
            }}
          >
            {Array.from({ length: 10 }).map((_, i) => (
              <span key={i} style={{ paddingRight: 50 }}>
                NOUVEAU MODÈLE · COQUE · CHARGEUR · LA BOUCLE INFINIE ·
              </span>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene20CaFonctionnePoint;
```

