```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11BarCrash',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11BarCrash: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // bars data: 2023 high -> 2026 low
  const bars = [
    { label: '2023', val: 5.0, h: 480 },
    { label: '2024', val: 3.5, h: 336 },
    { label: '2025', val: 2.6, h: 250 },
    { label: '2026', val: 2.1, h: 200 },
  ];

  const camZoom = interpolate(frame, [0, durationInFrames], [1.04, 1.1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 20%, #EAEAEA 0%, #DDDDDD 55%, #CFCFCF 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', top: -180, left: -140, width: 700, height: 700, borderRadius: '50%', border: '3px solid #F62440', opacity: 0.05, transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})` }} />
      <div style={{ position: 'absolute', bottom: -220, right: -120, width: 560, height: 560, background: '#000000', opacity: 0.03, transform: `rotate(${-frame * 0.15}deg)` }} />

      {/* Motion texture - vertical grid lines */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: `${i * 7.4}%`, top: 0, width: 1, height: '100%', background: '#000', opacity: 0.04 }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const drift = Math.sin(frame * 0.02 + i) * 14;
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 30) * 100}%`, width: 6, height: 6, borderRadius: '50%', background: i % 3 === 0 ? '#F62440' : '#000', opacity: 0.18, transform: `translateY(${drift}px)` }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        {/* headline */}
        <div style={{ position: 'absolute', top: 80, left: 100, transform: `translateX(${interpolate(sp(0), [0, 1], [-80, 0])}px)`, opacity: sp(0) }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 100, color: '#000', lineHeight: 0.95, letterSpacing: -2 }}>LA CHUTE</div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 100, color: '#F62440', lineHeight: 0.95, letterSpacing: -2 }}>DES HAUSSES</div>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 32, fontWeight: 600, color: '#000', marginTop: 14, opacity: 0.7 }}>Étude Alexio — BFM Business</div>
        </div>

        {/* chart base line */}
        <div style={{ position: 'absolute', bottom: 180, left: 120, width: 1640, height: 4, background: '#000', opacity: 0.85, transform: `scaleX(${sp(15)})`, transformOrigin: 'left' }} />

        {/* bars */}
        {bars.map((b, i) => {
          const grow = sp(30 + i * 14, { stiffness: 90 });
          const isLast = i === bars.length - 1;
          const x = 360 + i * 360;
          return (
            <div key={b.label} style={{ position: 'absolute', bottom: 184, left: x }}>
              <div style={{ width: 200, height: b.h * grow, background: isLast ? '#F62440' : '#000', boxShadow: isLast ? '0 0 40px rgba(246,36,64,0.45)' : 'none' }} />
              <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 56, fontWeight: 700, color: isLast ? '#F62440' : '#000', position: 'absolute', top: -78, width: 200, textAlign: 'center', opacity: grow }}>{b.val}%</div>
              <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 34, fontWeight: 700, color: '#000', position: 'absolute', top: b.h * grow + 16, width: 200, textAlign: 'center', opacity: grow }}>{b.label}</div>
            </div>
          );
        })}

        {/* downward arrow */}
        <svg style={{ position: 'absolute', top: 360, right: 160, opacity: sp(70) }} width="220" height="220" viewBox="0 0 100 100">
          <path d="M20 20 L80 80" stroke="#F62440" strokeWidth="8" strokeLinecap="round" />
          <path d="M80 80 L80 45 M80 80 L45 80" stroke="#F62440" strokeWidth="8" strokeLinecap="round" fill="none" />
        </svg>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene11BarCrash;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12Diverge',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12Diverge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // two diverging line paths drawn via dashoffset
  const lineProg = interpolate(frame, [20, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const dash = 1200;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #E6E6E6 0%, #DDDDDD 50%, #D2D2D2 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', top: '30%', left: '60%', width: 520, height: 520, border: '4px solid #000', opacity: 0.04, transform: `rotate(${frame * 0.25}deg)` }} />
      <div style={{ position: 'absolute', bottom: -160, left: -100, width: 420, height: 420, borderRadius: '50%', background: '#F62440', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})` }} />

      {/* Motion texture - dotted horizontal lines */}
      {Array.from({ length: 9 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', top: `${i * 11}%`, left: 0, width: '100%', height: 1, borderTop: '2px dashed #000', opacity: 0.05 }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 5);
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 12) * 100}%`, width: 5, height: 5, borderRadius: '50%', background: '#000', opacity: 0.15, transform: `translateY(${Math.sin(frame * 0.025 + i) * 12}px)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${interpolate(frame, [0, durationInFrames], [1.02, 1.08])})` }}>
        <div style={{ position: 'absolute', top: 70, width: '100%', textAlign: 'center', opacity: sp(0), transform: `translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)` }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 110, color: '#000', letterSpacing: -2 }}>LA MÂCHOIRE DU PIÈGE</div>
        </div>

        {/* SVG diverging lines */}
        <svg style={{ position: 'absolute', top: 240, left: 160 }} width="1600" height="640" viewBox="0 0 1600 640">
          {/* prices line up */}
          <path d="M0 480 C400 460 800 280 1600 80" fill="none" stroke="#F62440" strokeWidth="12" strokeLinecap="round" strokeDasharray={dash} strokeDashoffset={dash * (1 - lineProg)} />
          {/* salaries line flat-ish */}
          <path d="M0 480 C400 478 800 470 1600 440" fill="none" stroke="#000" strokeWidth="12" strokeLinecap="round" strokeDasharray={dash} strokeDashoffset={dash * (1 - lineProg)} />
        </svg>

        {/* labels */}
        <div style={{ position: 'absolute', top: 280, right: 130, opacity: sp(95), fontFamily: 'Montserrat, sans-serif', fontWeight: 800, fontSize: 46, color: '#F62440' }}>PRIX +12%</div>
        <div style={{ position: 'absolute', top: 640, right: 130, opacity: sp(95), fontFamily: 'Montserrat, sans-serif', fontWeight: 800, fontSize: 46, color: '#000' }}>SALAIRES +2%</div>

        {/* icons row */}
        {['🛒'].length && (
          <div style={{ position: 'absolute', bottom: 110, left: 0, width: '100%', display: 'flex', justifyContent: 'center', gap: 60, opacity: sp(70) }}>
            {['COURSES', 'ÉNERGIE', 'ESSENCE', 'LOYER'].map((t, i) => (
              <div key={t} style={{ fontFamily: 'Space Mono, monospace', fontWeight: 700, fontSize: 30, color: '#000', border: '3px solid #F62440', padding: '12px 22px', transform: `translateY(${Math.sin(frame * 0.06 + i) * 5}px)` }}>{t}</div>
            ))}
          </div>
        )}
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene12Diverge;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13Inflation',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13Inflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // big counter 0 -> 14
  const counter = Math.floor(interpolate(frame, [25, 100], [0, 14], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 80%, #E8E8E8 0%, #DDDDDD 50%, #CCCCCC 100%)' }} />

      {/* Animated shape layers - expanding triangles */}
      <svg style={{ position: 'absolute', top: '20%', left: '10%', opacity: 0.05, transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.02) * 0.06})` }} width="400" height="400" viewBox="0 0 100 100">
        <polygon points="50,5 95,95 5,95" fill="none" stroke="#F62440" strokeWidth="3" />
      </svg>
      <div style={{ position: 'absolute', bottom: -200, right: -120, width: 600, height: 600, borderRadius: '50%', background: '#000', opacity: 0.03 }} />

      {/* Motion texture - concentric circles */}
      {Array.from({ length: 5 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', top: '50%', left: '50%', width: 300 + i * 200, height: 300 + i * 200, marginLeft: -(150 + i * 100), marginTop: -(150 + i * 100), borderRadius: '50%', border: '2px solid #F62440', opacity: 0.04 }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 9);
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 20) * 100}%`, width: 7, height: 7, borderRadius: '50%', background: i % 2 ? '#F62440' : '#000', opacity: 0.16, transform: `translateY(${Math.sin(frame * 0.03 + i) * 16}px)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `scale(${interpolate(frame, [0, durationInFrames], [1.05, 1.12])})` }}>
        <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 44, color: '#000', opacity: sp(0), letterSpacing: 4 }}>INFLATION CUMULÉE 2022—2026</div>
        <div style={{ display: 'flex', alignItems: 'baseline', marginTop: -10, transform: `scale(${interpolate(sp(20), [0, 1], [0.5, 1])})`, opacity: sp(20) }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 360, color: '#F62440', lineHeight: 1, textShadow: '0 0 50px rgba(246,36,64,0.4)' }}>{counter}</div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 160, color: '#000' }}>%</div>
        </div>
        <div style={{ fontFamily: 'Space Mono, monospace', fontWeight: 700, fontSize: 32, color: '#000', opacity: sp(80) * 0.8, marginTop: -20 }}>Banque de France · OFCE</div>
      </AbsoluteFill>

      {/* impact pulse ring */}
      <div style={{ position: 'absolute', top: '50%', left: '50%', width: 200, height: 200, marginLeft: -100, marginTop: -100, borderRadius: '50%', border: '6px solid #F62440', opacity: interpolate(frame, [95, 130], [0.5, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }), transform: `scale(${interpolate(frame, [95, 130], [1, 4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})` }} />

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene13Inflation;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14Squeeze',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14Squeeze: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // two plates squeezing in
  const squeeze = interpolate(frame, [30, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const topY = interpolate(squeeze, [0, 1], [-300, 0]);
  const botY = interpolate(squeeze, [0, 1], [300, 0]);
  const shake = squeeze > 0.85 ? Math.sin(frame * 0.9) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #E2E2E2 0%, #DDDDDD 50%, #D0D0D0 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', top: '40%', left: -160, width: 500, height: 500, borderRadius: '50%', border: '4px solid #F62440', opacity: 0.05, transform: `rotate(${frame * 0.2}deg)` }} />
      <div style={{ position: 'absolute', top: '10%', right: -120, width: 380, height: 380, background: '#000', opacity: 0.03, transform: `rotate(${frame * 0.18}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})` }} />

      {/* Motion texture - diagonal lines */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', top: 0, left: `${i * 9 - 20}%`, width: 2, height: '140%', background: '#000', opacity: 0.04, transform: 'rotate(18deg)' }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 3);
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 18) * 100}%`, width: 6, height: 6, background: '#F62440', opacity: 0.14, transform: `translateY(${Math.sin(frame * 0.02 + i) * 10}px)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${shake}px)` }}>
        {/* top plate */}
        <div style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '38%', background: '#F62440', transform: `translateY(${topY}px)`, display: 'flex', alignItems: 'flex-end', justifyContent: 'center', paddingBottom: 30 }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 70, color: '#DDDDDD', letterSpacing: 2 }}>PRIX EN HAUSSE ↑</div>
        </div>

        {/* bottom plate */}
        <div style={{ position: 'absolute', bottom: 0, left: 0, width: '100%', height: '38%', background: '#000', transform: `translateY(${botY}px)`, display: 'flex', alignItems: 'flex-start', justifyContent: 'center', paddingTop: 30 }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 70, color: '#DDDDDD', letterSpacing: 2 }}>SALAIRES GELÉS ↓</div>
        </div>

        {/* middle text - you */}
        <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ transform: `scale(${interpolate(sp(60), [0, 1], [0.6, 1])})`, opacity: sp(60), background: '#DDDDDD', padding: '18px 50px', boxShadow: '0 0 50px rgba(0,0,0,0.2)' }}>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 90, color: '#000', textAlign: 'center', letterSpacing: -1 }}>LA CLASSE MOYENNE</div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 36, color: '#F62440', textAlign: 'center' }}>VOUS · MOI · COINCÉS AU MILIEU</div>
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.3)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene14Squeeze;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15Backto2019',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15Backto2019: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // U-shaped curve drawn
  const curveProg = interpolate(frame, [20, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const dash = 1500;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 30%, #EBEBEB 0%, #DDDDDD 55%, #CECECE 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', top: -140, right: '20%', width: 460, height: 460, border: '4px solid #000', opacity: 0.04, borderRadius: '50%', transform: `rotate(${-frame * 0.2}deg)` }} />
      <svg style={{ position: 'absolute', bottom: -100, left: -80, opacity: 0.05, transform: `rotate(${frame * 0.15}deg)` }} width="420" height="420" viewBox="0 0 100 100">
        <rect x="10" y="10" width="80" height="80" fill="none" stroke="#F62440" strokeWidth="3" />
      </svg>

      {/* Motion texture - grid dots */}
      {Array.from({ length: 40 }).map((_, i) => {
        const col = i % 8; const row = Math.floor(i / 8);
        return <div key={i} style={{ position: 'absolute', left: `${10 + col * 11}%`, top: `${20 + row * 16}%`, width: 4, height: 4, borderRadius: '50%', background: '#000', opacity: 0.06 }} />;
      })}

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const r = seededRandom(i + 7);
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 22) * 100}%`, width: 6, height: 6, borderRadius: '50%', background: '#F62440', opacity: 0.15, transform: `translateY(${Math.sin(frame * 0.025 + i) * 12}px)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${interpolate(frame, [0, durationInFrames], [1.03, 1.09])})` }}>
        <div style={{ position: 'absolute', top: 80, width: '100%', textAlign: 'center', opacity: sp(0) }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 96, color: '#000', letterSpacing: -2 }}>RETOUR À LA CASE</div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 140, color: '#F62440', letterSpacing: -3, lineHeight: 0.9 }}>2019</div>
        </div>

        {/* U curve */}
        <svg style={{ position: 'absolute', bottom: 120, left: 200 }} width="1520" height="500" viewBox="0 0 1520 500">
          <path d="M40 60 C300 480 1220 480 1480 60" fill="none" stroke="#000" strokeWidth="12" strokeLinecap="round" strokeDasharray={dash} strokeDashoffset={dash * (1 - curveProg)} />
          <circle cx={40} cy={60} r="16" fill="#000" opacity={curveProg > 0.05 ? 1 : 0} />
          <circle cx={1480} cy={60} r="18" fill="#F62440" opacity={curveProg > 0.95 ? 1 : 0} />
        </svg>
        <div style={{ position: 'absolute', bottom: 540, left: 230, fontFamily: 'Space Mono, monospace', fontWeight: 700, fontSize: 30, color: '#000', opacity: curveProg > 0.05 ? 1 : 0 }}>2019</div>
        <div style={{ position: 'absolute', bottom: 540, right: 200, fontFamily: 'Space Mono, monospace', fontWeight: 700, fontSize: 30, color: '#F62440', opacity: curveProg > 0.9 ? 1 : 0 }}>2026</div>

        {/* bottom banner */}
        <div style={{ position: 'absolute', bottom: 110, width: '100%', textAlign: 'center', opacity: sp(95), transform: `translateY(${interpolate(sp(95), [0, 1], [30, 0])}px)` }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 800, fontSize: 42, color: '#000', display: 'inline-block', borderBottom: '5px solid #F62440', paddingBottom: 8 }}>STRATÉGIE INDIVIDUELLE &gt; REVALORISATIONS COLLECTIVES</div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene15Backto2019;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16Door',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16Door: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // door opening angle (skew to simulate)
  const open = interpolate(frame, [40, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const glow = interpolate(open, [0.4, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, #E5E5E5 0%, #DDDDDD 60%, #CCCCCC 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', top: '20%', left: '8%', width: 420, height: 420, border: '4px solid #F62440', opacity: 0.05, transform: `rotate(${frame * 0.22}deg)` }} />
      <div style={{ position: 'absolute', bottom: '10%', right: '10%', width: 360, height: 360, borderRadius: '50%', background: '#000', opacity: 0.03, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})` }} />

      {/* Motion texture - radial lines */}
      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', top: '50%', left: '50%', width: 700, height: 2, background: '#000', opacity: 0.03, transformOrigin: 'left center', transform: `rotate(${i * 22.5 + frame * 0.1}deg)` }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 11);
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 25) * 100}%`, width: 6, height: 6, borderRadius: '50%', background: '#F62440', opacity: 0.15, transform: `translateY(${Math.sin(frame * 0.03 + i) * 14}px)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `scale(${interpolate(frame, [0, durationInFrames], [1.02, 1.08])})` }}>
        {/* door frame */}
        <div style={{ position: 'relative', width: 420, height: 640, perspective: 1200 }}>
          {/* glowing light behind */}
          <div style={{ position: 'absolute', inset: 0, background: '#F62440', opacity: glow, boxShadow: `0 0 ${glow * 120}px rgba(246,36,64,0.8)` }} />
          {/* door panel */}
          <div style={{ position: 'absolute', inset: 0, background: '#000', transformOrigin: 'left center', transform: `rotateY(${-open * 75}deg)`, borderRight: '6px solid #F62440' }}>
            <div style={{ position: 'absolute', right: 30, top: '50%', width: 18, height: 18, borderRadius: '50%', background: '#F62440' }} />
          </div>
          {/* frame border */}
          <div style={{ position: 'absolute', inset: -10, border: '10px solid #000', pointerEvents: 'none' }} />
        </div>

        {/* text */}
        <div style={{ position: 'absolute', bottom: 90, width: '100%', textAlign: 'center', opacity: sp(90), transform: `translateY(${interpolate(sp(90), [0, 1], [30, 0])}px)` }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 100, color: '#000', letterSpacing: -2 }}>UNE PORTE DE SORTIE</div>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 800, fontSize: 40, color: '#F62440' }}>ELLE DÉPEND DE VOUS</div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.3)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene16Door;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17Buffett',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17Buffett: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // the swap animation of two boxes
  const swap = interpolate(frame, [60, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const xA = interpolate(swap, [0, 1], [0, 540]);
  const xB = interpolate(swap, [0, 1], [0, -540]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #E4E4E4 0%, #DDDDDD 50%, #D1D1D1 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', top: -120, left: '40%', width: 480, height: 480, borderRadius: '50%', border: '4px solid #000', opacity: 0.04, transform: `rotate(${frame * 0.2}deg)` }} />
      <svg style={{ position: 'absolute', bottom: -120, right: 40, opacity: 0.05, transform: `rotate(${-frame * 0.18}deg)` }} width="400" height="400" viewBox="0 0 100 100">
        <polygon points="50,10 90,90 10,90" fill="none" stroke="#F62440" strokeWidth="3" />
      </svg>

      {/* Motion texture - vertical bars */}
      {Array.from({ length: 20 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: `${i * 5}%`, bottom: 0, width: 1, height: '100%', background: '#000', opacity: 0.03 }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 13);
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 28) * 100}%`, width: 5, height: 5, borderRadius: '50%', background: '#F62440', opacity: 0.14, transform: `translateY(${Math.sin(frame * 0.02 + i) * 12}px)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${interpolate(frame, [0, durationInFrames], [1.02, 1.07])})` }}>
        {/* quote mark */}
        <div style={{ position: 'absolute', top: 50, left: 120, fontFamily: 'Bricolage Grotesque, serif', fontSize: 200, color: '#F62440', opacity: sp(0) * 0.6, lineHeight: 0.8 }}>“</div>
        <div style={{ position: 'absolute', top: 120, left: 120, right: 120, opacity: sp(10) }}>
          <div style={{ fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700, fontSize: 56, color: '#000', lineHeight: 1.15 }}>Ne pas épargner ce qui reste après avoir dépensé, mais dépenser ce qui reste après avoir épargné.</div>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 800, fontSize: 34, color: '#F62440', marginTop: 18 }}>— WARREN BUFFETT</div>
        </div>

        {/* swap boxes */}
        <div style={{ position: 'absolute', bottom: 150, left: '50%', marginLeft: -560, display: 'flex', gap: 80 }}>
          <div style={{ width: 440, height: 140, background: '#000', display: 'flex', alignItems: 'center', justifyContent: 'center', transform: `translateX(${xA}px)`, opacity: sp(50) }}>
            <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 56, color: '#DDDDDD' }}>ÉPARGNER</span>
          </div>
          <div style={{ width: 440, height: 140, background: '#F62440', display: 'flex', alignItems: 'center', justifyContent: 'center', transform: `translateX(${xB}px)`, opacity: sp(50) }}>
            <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 56, color: '#DDDDDD' }}>DÉPENSER</span>
          </div>
        </div>
        <div style={{ position: 'absolute', bottom: 90, width: '100%', textAlign: 'center', opacity: sp(115), fontFamily: 'Space Mono, monospace', fontWeight: 700, fontSize: 32, color: '#000' }}>INVERSEZ L'ORDRE</div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene17Buffett;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18LivretA',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18LivretA: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 25% 75%, #EAEAEA 0%, #DDDDDD 55%, #CFCFCF 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', top: '15%', right: -120, width: 500, height: 500, border: '4px solid #F62440', opacity: 0.05, borderRadius: 40, transform: `rotate(${frame * 0.2}deg)` }} />
      <div style={{ position: 'absolute', bottom: -160, left: '30%', width: 420, height: 420, borderRadius: '50%', background: '#000', opacity: 0.03, transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})` }} />

      {/* Motion texture - horizontal lines */}
      {Array.from({ length: 11 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', top: `${i * 9}%`, left: 0, width: '100%', height: 1, background: '#000', opacity: 0.04 }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 17);
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 31) * 100}%`, width: 6, height: 6, borderRadius: '50%', background: '#F62440', opacity: 0.15, transform: `translateY(${Math.sin(frame * 0.03 + i) * 14}px)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${interpolate(frame, [0, durationInFrames], [1.02, 1.08])})` }}>
        {/* number tag */}
        <div style={{ position: 'absolute', top: 80, left: 110, opacity: sp(0), transform: `translateX(${interpolate(sp(0), [0, 1], [-60, 0])}px)` }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 60, color: '#F62440', letterSpacing: 4 }}>LEVIER 01</div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 150, color: '#000', lineHeight: 0.9, letterSpacing: -3 }}>LE LIVRET A</div>
        </div>

        {/* savings booklet card */}
        <div style={{ position: 'absolute', top: 380, left: 110, width: 760, opacity: sp(40), transform: `translateY(${interpolate(sp(40), [0, 1], [60, 0])}px) rotate(-2deg)` }}>
          <div style={{ background: '#F62440', padding: '40px 50px', boxShadow: '0 30px 60px rgba(0,0,0,0.25)' }}>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 30, color: '#DDDDDD', opacity: 0.85 }}>OUTIL TACTIQUE</div>
            <div style={{ fontFamily: 'Space Mono, monospace', fontWeight: 700, fontSize: 92, color: '#DDDDDD' }}>0 % IMPÔT</div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 600, fontSize: 28, color: '#DDDDDD' }}>Disponible à tout instant</div>
          </div>
        </div>

        {/* right column - benefit chips */}
        <div style={{ position: 'absolute', top: 400, right: 130, display: 'flex', flexDirection: 'column', gap: 26 }}>
          {['MATELAS DE SÉCURITÉ', 'TOTALEMENT DÉFISCALISÉ', 'ZÉRO RISQUE'].map((t, i) => (
            <div key={t} style={{ opacity: sp(70 + i * 16), transform: `translateX(${interpolate(sp(70 + i * 16), [0, 1], [80, 0])}px)`, border: '4px solid #000', padding: '20px 30px', display: 'flex', alignItems: 'center', gap: 18 }}>
              <svg width="34" height="34" viewBox="0 0 24 24"><path d="M20 6L9 17l-5-5" fill="none" stroke="#F62440" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round" /></svg>
              <span style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 800, fontSize: 32, color: '#000' }}>{t}</span>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene18LivretA;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19Plafond',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19Plafond: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // counter to 22950
  const raw = interpolate(frame, [25, 95], [0, 22950], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const counter = Math.floor(raw).toLocaleString('fr-FR');

  // fill bar to ceiling
  const fill = interpolate(frame, [25, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #E6E6E6 0%, #DDDDDD 55%, #D0D0D0 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', top: '10%', left: -140, width: 460, height: 460, borderRadius: '50%', border: '4px solid #000', opacity: 0.04, transform: `rotate(${frame * 0.2}deg)` }} />
      <div style={{ position: 'absolute', bottom: -120, right: '15%', width: 380, height: 380, background: '#F62440', opacity: 0.05, transform: `rotate(${frame * 0.16}deg) scale(${1 + Math.sin(frame * 0.03) * 0.04})` }} />

      {/* Motion texture - grid */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={`v${i}`} style={{ position: 'absolute', left: `${i * 8.5}%`, top: 0, width: 1, height: '100%', background: '#000', opacity: 0.03 }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 19);
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 33) * 100}%`, width: 6, height: 6, borderRadius: '50%', background: '#F62440', opacity: 0.15, transform: `translateY(${Math.sin(frame * 0.025 + i) * 12}px)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `scale(${interpolate(frame, [0, durationInFrames], [1.02, 1.07])})` }}>
        <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 40, color: '#000', letterSpacing: 4, opacity: sp(0) }}>PLAFOND DU LIVRET A</div>
        <div style={{ display: 'flex', alignItems: 'baseline', opacity: sp(20), transform: `scale(${interpolate(sp(20), [0, 1], [0.6, 1])})` }}>
          <div style={{ fontFamily: 'Space Mono, monospace', fontWeight: 700, fontSize: 220, color: '#F62440', lineHeight: 1, textShadow: '0 0 50px rgba(246,36,64,0.35)' }}>{counter}</div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 110, color: '#000', marginLeft: 16 }}>€</div>
        </div>

        {/* vertical fill tube */}
        <div style={{ position: 'absolute', right: 180, top: '20%', width: 120, height: 600, border: '6px solid #000', overflow: 'hidden' }}>
          <div style={{ position: 'absolute', bottom: 0, left: 0, width: '100%', height: `${fill * 100}%`, background: '#F62440' }} />
        </div>
        <div style={{ position: 'absolute', right: 130, top: 'calc(20% - 50px)', fontFamily: 'Montserrat, sans-serif', fontWeight: 800, fontSize: 30, color: '#000', opacity: sp(90) }}>PLAFOND</div>

        {/* bottom tag */}
        <div style={{ position: 'absolute', bottom: 120, opacity: sp(95), border: '4px solid #F62440', padding: '18px 40px' }}>
          <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 48, color: '#000' }}>AUCUN IMPÔT · AUCUNE PONCTION SOCIALE</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene19Plafond;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20Trap',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20Trap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // sleeping money shrinking due to inflation
  const shrink = interpolate(frame, [40, 110], [1, 0.74], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const warnPulse = 0.5 + Math.sin(frame * 0.18) * 0.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DDDDDD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 40%, #E8E8E8 0%, #DDDDDD 55%, #CDCDCD 100%)' }} />

      {/* Animated shape layers */}
      <svg style={{ position: 'absolute', top: '12%', left: '6%', opacity: 0.05, transform: `rotate(${frame * 0.25}deg)` }} width="420" height="420" viewBox="0 0 100 100">
        <polygon points="50,8 92,90 8,90" fill="none" stroke="#F62440" strokeWidth="3" />
      </svg>
      <div style={{ position: 'absolute', bottom: -150, right: -100, width: 440, height: 440, borderRadius: '50%', background: '#000', opacity: 0.03 }} />

      {/* Motion texture - diagonal hatch */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', top: 0, left: `${i * 8 - 10}%`, width: 2, height: '150%', background: '#F62440', opacity: 0.04, transform: 'rotate(-20deg)' }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 23);
        return <div key={`p${i}`} style={{ position: 'absolute', left: `${r * 100}%`, top: `${seededRandom(i + 35) * 100}%`, width: 5, height: 5, borderRadius: '50%', background: '#000', opacity: 0.15, transform: `translateY(${Math.sin(frame * 0.02 + i) * 12}px)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${interpolate(frame, [0, durationInFrames], [1.02, 1.07])})` }}>
        {/* warning header */}
        <div style={{ position: 'absolute', top: 70, width: '100%', display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 24, opacity: sp(0) }}>
          <svg width="80" height="80" viewBox="0 0 24 24" style={{ opacity: warnPulse }}>
            <path d="M12 2L1 21h22L12 2z" fill="none" stroke="#F62440" strokeWidth="2" strokeLinejoin="round" />
            <line x1="12" y1="9" x2="12" y2="14" stroke="#F62440" strokeWidth="2.5" strokeLinecap="round" />
            <circle cx="12" cy="17.5" r="1.4" fill="#F62440" />
          </svg>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 100, color: '#F62440', letterSpacing: -2 }}>LE VRAI PIÈGE</div>
        </div>

        {/* shrinking money stack */}
        <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ transform: `scale(${shrink})`, transformOrigin: 'center', opacity: sp(35) }}>
            <div style={{ background: '#000', padding: '30px 60px', textAlign: 'center' }}>
              <div style={{ fontFamily: 'Space Mono, monospace', fontWeight: 700, fontSize: 110, color: '#DDDDDD' }}>22 000 €</div>
              <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 600, fontSize: 32, color: '#F62440' }}>QUI DORMENT 10 ANS</div>
            </div>
          </div>
          <div style={{ marginTop: 30, fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 36, color: '#000', opacity: sp(90) }}>↓ PERD DE LA VALEUR FACE À L'INFLATION</div>
        </AbsoluteFill>

        {/* bottom rule */}
        <div style={{ position: 'absolute', bottom: 110, width: '100%', textAlign: 'center', opacity: sp(110) }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 52, color: '#000', display: 'inline-block', background: '#F62440', color: '#DDDDDD', padding: '14px 36px' }}>COUSSIN D'URGENCE ≠ PATRIMOINE</div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>Matures Finance</div>
    </AbsoluteFill>
  );
};

export default Scene20Trap;
```

