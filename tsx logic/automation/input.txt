```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_1_SarkarKaJhatka',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_1_SarkarKaJhatka: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Hammer slam — massive impact
  const hammerY = interpolate(frame, [5, 18], [-300, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.in(Easing.cubic),
  });
  const hammerOp = interpolate(frame, [5, 14], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Screen shake on impact
  const shakeX = frame >= 17 && frame <= 24
    ? Math.sin(frame * 22) * 8
    : 0;
  const shakeY = frame >= 17 && frame <= 24
    ? Math.sin(frame * 28) * 6
    : 0;

  // Impact rings
  const ringScale1 = sp(18, { damping: 4, stiffness: 80 });
  const ringOp1 = interpolate(frame, [18, 42], [0.6, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const ringScale2 = sp(22, { damping: 4, stiffness: 60 });
  const ringOp2 = interpolate(frame, [22, 48], [0.4, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // "एक झटके में" text
  const jhatkaOp = interpolate(frame, [20, 32], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const jhatkaScale = sp(20, { damping: 6, stiffness: 200 });

  // Hypothetical question box
  const qboxOp = interpolate(frame, [35, 48], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const qboxY = interpolate(frame, [35, 48], [50, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // ₹10L limit tag
  const limitOp = interpolate(frame, [52, 64], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const limitScale = sp(52, { damping: 8, stiffness: 180 });

  // Lock icon on bank
  const lockOp = interpolate(frame, [58, 70], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const lockScale = sp(58);

  // Particles — shockwave debris
  const particles = Array.from({ length: 28 }, (_, i) => {
    const angle = (i / 28) * Math.PI * 2;
    const dist = frame > 18 ? Math.min((frame - 18) * (4 + seededRandom(i * 3) * 6), 400) : 0;
    return {
      x: 960 + Math.cos(angle) * dist,
      y: 380 + Math.sin(angle) * dist * 0.5,
      r: 2 + seededRandom(i * 5) * 4,
      op: Math.max(0, 0.5 - dist / 800),
      color: seededRandom(i * 7) > 0.5 ? '#ff0000' : '#FFC107',
    };
  });

  // Stick figure — blown back
  const figureScale = sp(25);
  const breathY = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;
  const blownBack = interpolate(frame, [18, 35], [0, 30], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const blownTilt = interpolate(frame, [18, 35], [0, -18], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const shape1Rot = frame * 0.15;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translate(${shakeX}px, ${shakeY}px)`,
      }}
    >
      {/* === GRADIENT BG — authoritarian red-black === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 70% 60% at 50% 40%, #200000 0%, #1C1C1C 55%, #080808 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 40% 35% at 50% 38%, rgba(255,0,0,0.1) 0%, transparent 70%)',
        }}
      />

      {/* === SHAPE LAYER 1 — govt seal rings === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {[180, 280, 400].map((r, i) => (
            <circle
              key={i}
              cx="960"
              cy="380"
              r={r}
              fill="none"
              stroke="#ff0000"
              strokeWidth="1.5"
              strokeDasharray={i % 2 === 0 ? '12 8' : 'none'}
              transform={`rotate(${shape1Rot * (i % 2 === 0 ? 1 : -1)}, 960, 380)`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER 2 — top authority bar === */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <rect x="0" y="0" width="1920" height="8" fill="#ff0000" />
          <rect x="0" y="16" width="1920" height="3" fill="#ff0000" />
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — radial warning lines === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 16 }, (_, i) => {
            const angle = (i / 16) * Math.PI * 2;
            return (
              <line
                key={i}
                x1="960"
                y1="380"
                x2={960 + Math.cos(angle) * 700}
                y2={380 + Math.sin(angle) * 500}
                stroke="#ff0000"
                strokeWidth="0.8"
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === IMPACT RINGS === */}
      <div
        style={{
          position: 'absolute',
          top: 380 - 200,
          left: 960 - 200,
          width: 400,
          height: 400,
          borderRadius: '50%',
          border: '4px solid #ff0000',
          opacity: ringOp1,
          transform: `scale(${ringScale1})`,
          transformOrigin: 'center center',
          pointerEvents: 'none',
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: 380 - 200,
          left: 960 - 200,
          width: 400,
          height: 400,
          borderRadius: '50%',
          border: '2px solid #FFC107',
          opacity: ringOp2,
          transform: `scale(${ringScale2})`,
          transformOrigin: 'center center',
          pointerEvents: 'none',
        }}
      />

      {/* === PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill={p.color} opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === HAMMER SVG — center slam === */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) translateY(${hammerY}px)`,
          opacity: hammerOp,
        }}
      >
        <svg width="200" height="260" viewBox="0 0 200 260">
          {/* Handle */}
          <rect x="90" y="100" width="20" height="160" rx="6" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
          {/* Head */}
          <rect x="30" y="30" width="140" height="80" rx="12" fill="#ff0000" />
          <rect x="30" y="30" width="140" height="80" rx="12" fill="none" stroke="rgba(255,100,100,0.4)" strokeWidth="3" />
          {/* Sheen */}
          <rect x="38" y="38" width="120" height="24" rx="6" fill="rgba(255,255,255,0.08)" />
          {/* GOVT text */}
          <text x="100" y="80" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#F5F5F5" letterSpacing="4">GOV</text>
        </svg>
      </div>

      {/* === "एक झटके में" === */}
      <div
        style={{
          position: 'absolute',
          top: 130,
          left: 90,
          opacity: jhatkaOp,
          transform: `scale(${jhatkaScale})`,
          transformOrigin: 'top left',
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 130,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 1,
            textShadow: '0 0 60px rgba(255,0,0,0.5)',
          }}
        >
          एक झटके में
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 72,
            fontWeight: 700,
            color: '#F5F5F5',
            lineHeight: 1.2,
          }}
        >
          खेल पलट दिया
        </div>
      </div>

      {/* === HYPOTHETICAL QUESTION === */}
      <div
        style={{
          position: 'absolute',
          bottom: 220,
          left: 90,
          right: 90,
          opacity: qboxOp,
          transform: `translateY(${qboxY}px)`,
        }}
      >
        <div
          style={{
            background: '#2D2D2D',
            border: '2px solid rgba(255,193,7,0.5)',
            borderRadius: 20,
            padding: '24px 44px',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 20,
              color: '#FFC107',
              letterSpacing: 5,
              marginBottom: 10,
              fontWeight: 700,
            }}
          >
            ◈ HYPOTHETICAL SCENARIO ◈
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 52,
              fontWeight: 800,
              color: '#F5F5F5',
              lineHeight: 1.3,
            }}
          >
            अगर कल सरकार कहे... बैंक से सिर्फ{' '}
            <span style={{ color: '#ff0000' }}>₹10 लाख</span> निकाल सकते हो?
          </div>
        </div>
      </div>

      {/* === LOCK + LIMIT TAG === */}
      <div
        style={{
          position: 'absolute',
          right: 120,
          top: 280,
          opacity: limitOp,
          transform: `scale(${limitScale})`,
          transformOrigin: 'right top',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: 20,
        }}
      >
        {/* Lock icon */}
        <div style={{ opacity: lockOp, transform: `scale(${lockScale})`, transformOrigin: 'center' }}>
          <svg width="100" height="120" viewBox="0 0 100 120">
            <rect x="15" y="50" width="70" height="60" rx="10" fill="#ff0000" />
            <path d="M28 50 L28 32 Q28 12 50 12 Q72 12 72 32 L72 50" fill="none" stroke="#ff0000" strokeWidth="10" strokeLinecap="round" />
            <circle cx="50" cy="78" r="10" fill="#1C1C1C" />
            <rect x="46" y="78" width="8" height="16" rx="4" fill="#1C1C1C" />
          </svg>
        </div>
        <div
          style={{
            background: '#ff0000',
            borderRadius: 14,
            padding: '16px 32px',
            textAlign: 'center',
          }}
        >
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: 'rgba(255,255,255,0.7)', letterSpacing: 4 }}>MAX LIMIT</div>
          <div style={{ fontFamily: 'Inter', fontSize: 72, fontWeight: 900, color: '#F5F5F5', lineHeight: 1 }}>₹10L</div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 26, color: 'rgba(255,255,255,0.8)' }}>बाकि — LOCKED</div>
        </div>
      </div>

      {/* === STICK FIGURE — blown back === */}
      <div
        style={{
          position: 'absolute',
          left: 90,
          bottom: 80,
          transform: `scale(${figureScale}) translateX(${blownBack}px) translateY(${breathY}px) rotate(${blownTilt}deg)`,
          transformOrigin: 'bottom left',
        }}
      >
        <svg width="100" height="180" viewBox="0 0 100 180">
          <ellipse cx="50" cy="172" rx="38" ry="10" fill="rgba(255,0,0,0.08)" />
          <line x1="50" y1="70" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2="15" y2="68" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2="85" y2="68" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="28" y2="168" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="72" y2="172" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="50" cy="44" r="25" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" />
          <ellipse cx="38" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="62" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="47" y1="44" x2="53" y2="44" stroke="#e9d700" strokeWidth="1.5" />
          <ellipse cx="50" cy="58" rx="5" ry="4" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 85% 80% at 50% 50%, transparent 35%, rgba(0,0,0,0.82) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_1_SarkarKaJhatka;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_2_CyprusKaSach',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_2_CyprusKaSach: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Map pin drop — Cyprus on map
  const mapOp = interpolate(frame, [5, 18], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const pinDrop = interpolate(frame, [8, 20], [-80, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.back(1.5)),
  });

  // "2013" year stamp
  const yearOp = interpolate(frame, [18, 30], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const yearScale = sp(18);

  // Tax slash animation
  const taxOp = interpolate(frame, [30, 42], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const taxScale = sp(30, { damping: 6, stiffness: 200 });

  // Savings drain bar
  const drainWidth = interpolate(frame, [42, 68], [800, 200], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Legal stamp
  const legalOp = interpolate(frame, [58, 70], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const legalScale = sp(58, { damping: 7, stiffness: 160 });

  // Particles — euro coins scattering
  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: 300 + seededRandom(i * 11) * 1200,
    y: ((seededRandom(i * 7) * 900 + frame * (0.8 + seededRandom(i * 3) * 0.6)) % 900),
    r: 2 + seededRandom(i * 5) * 3.5,
    op: 0.08 + seededRandom(i * 9) * 0.15,
  }));

  // Stick figure — pointing at map, explaining
  const figureScale = sp(15);
  const breathY = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;
  const pointY = Math.sin(frame * 0.06) * 4;

  const shapeScale = 1 + Math.sin(frame * 0.022) * 0.04;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === GRADIENT BG — deep Mediterranean blue-black === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 75% 65% at 35% 45%, #080C1A 0%, #1C1C1C 55%, #0A0A0A 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 35% 35% at 30% 40%, rgba(0,80,180,0.08) 0%, transparent 70%)',
        }}
      />

      {/* === SHAPE LAYER 1 — globe meridian lines === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 8 }, (_, i) => (
            <ellipse
              key={i}
              cx="420"
              cy="400"
              rx={80 + i * 55}
              ry={280}
              fill="none"
              stroke="#4080FF"
              strokeWidth="0.8"
            />
          ))}
          <circle cx="420" cy="400" r="280" fill="none" stroke="#4080FF" strokeWidth="1.5" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER 2 — EU flag stars === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 12 }, (_, i) => {
            const angle = (i / 12) * Math.PI * 2;
            return (
              <text
                key={i}
                x={1700 + Math.cos(angle) * 120}
                y={200 + Math.sin(angle) * 120}
                textAnchor="middle"
                fontSize="28"
                fill="#FFC107"
                fontFamily="Inter"
              >
                ★
              </text>
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — horizontal data lines === */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 28 }, (_, i) => (
            <line
              key={i}
              x1="700"
              y1={30 + i * 38}
              x2="1900"
              y2={30 + i * 38}
              stroke="#9E9E9E"
              strokeWidth="0.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#FFC107" opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === STYLIZED MAP — left panel === */}
      <div
        style={{
          position: 'absolute',
          top: 120,
          left: 80,
          opacity: mapOp,
        }}
      >
        <svg width="560" height="580" viewBox="0 0 560 580">
          {/* Globe circle */}
          <circle cx="280" cy="290" r="250" fill="rgba(0,40,100,0.3)" stroke="rgba(64,128,255,0.3)" strokeWidth="2" />
          {/* Continent blobs */}
          <ellipse cx="200" cy="220" rx="100" ry="80" fill="rgba(64,128,255,0.15)" />
          <ellipse cx="320" cy="280" rx="60" ry="50" fill="rgba(64,128,255,0.12)" />
          {/* Cyprus highlight */}
          <circle cx="340" cy="260" r="22" fill="#ff0000" opacity="0.8" />
          <circle cx="340" cy="260" r="30" fill="none" stroke="#ff0000" strokeWidth="2" opacity="0.5" />
          {/* Pin */}
          <line
            x1="340"
            y1={260 + pinDrop}
            x2="340"
            y2={220 + pinDrop}
            stroke="#ff0000"
            strokeWidth="3"
            strokeLinecap="round"
          />
          <circle cx="340" cy={215 + pinDrop} r="12" fill="#ff0000" />
          {/* Label */}
          <text x="370" y="265" fontFamily="Inter" fontSize="20" fontWeight="700" fill="#F5F5F5">Cyprus</text>
          {/* Lat/Long grid */}
          {Array.from({ length: 5 }, (_, i) => (
            <line key={`h${i}`} x1="30" y1={90 + i * 100} x2="530" y2={90 + i * 100} stroke="rgba(255,255,255,0.06)" strokeWidth="1" />
          ))}
          {Array.from({ length: 5 }, (_, i) => (
            <line key={`v${i}`} x1={80 + i * 100} y1="40" x2={80 + i * 100} y2="540" stroke="rgba(255,255,255,0.06)" strokeWidth="1" />
          ))}
        </svg>
      </div>

      {/* === YEAR STAMP — 2013 === */}
      <div
        style={{
          position: 'absolute',
          top: 120,
          left: '55%',
          opacity: yearOp,
          transform: `scale(${yearScale})`,
          transformOrigin: 'top left',
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#9E9E9E',
            letterSpacing: 8,
            fontWeight: 700,
            marginBottom: 6,
          }}
        >
          REAL EVENT
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 160,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
          }}
        >
          2013
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 44,
            color: '#ff0000',
            fontWeight: 700,
          }}
        >
          साइप्रस — बैंकिंग संकट
        </div>
      </div>

      {/* === TAX SLASH === */}
      <div
        style={{
          position: 'absolute',
          top: 380,
          left: '55%',
          opacity: taxOp,
          transform: `scale(${taxScale})`,
          transformOrigin: 'top left',
        }}
      >
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div
            style={{
              background: '#ff0000',
              borderRadius: 12,
              padding: '14px 28px',
            }}
          >
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: 'rgba(255,255,255,0.7)', letterSpacing: 3 }}>TAX LEVIED</div>
            <div style={{ fontFamily: 'Inter', fontSize: 80, fontWeight: 900, color: '#F5F5F5', lineHeight: 1 }}>€1L+</div>
          </div>
          <div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 40, color: '#F5F5F5', fontWeight: 700, lineHeight: 1.3 }}>
              एक लाख यूरो से<br />ज़्यादा पर टैक्स
            </div>
          </div>
        </div>
      </div>

      {/* === SAVINGS DRAIN BAR === */}
      <div
        style={{
          position: 'absolute',
          bottom: 220,
          left: '55%',
          right: 80,
        }}
      >
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#9E9E9E', marginBottom: 10 }}>
          लोगों की बचत
        </div>
        <div style={{ width: 800, height: 40, background: '#2D2D2D', borderRadius: 8, overflow: 'hidden', position: 'relative' }}>
          <div
            style={{
              width: drainWidth,
              height: '100%',
              background: 'linear-gradient(90deg, #e9d700, #FFC107)',
              borderRadius: 8,
              transition: 'width 0.1s',
              boxShadow: '0 0 20px rgba(233,215,0,0.3)',
            }}
          />
          <div
            style={{
              position: 'absolute',
              right: 16,
              top: '50%',
              transform: 'translateY(-50%)',
              fontFamily: 'JetBrains Mono',
              fontSize: 18,
              color: '#ff0000',
              fontWeight: 700,
            }}
          >
            DRAINING...
          </div>
        </div>
        <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 8, width: 800 }}>
          <span style={{ fontFamily: 'Inter', fontSize: 20, color: '#9E9E9E' }}>₀</span>
          <span style={{ fontFamily: 'Inter', fontSize: 20, color: '#e9d700' }}>पूरी बचत</span>
        </div>
      </div>

      {/* === LEGAL STAMP === */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: '55%',
          opacity: legalOp,
          transform: `scale(${legalScale}) rotate(-4deg)`,
          transformOrigin: 'left bottom',
        }}
      >
        <div
          style={{
            border: '5px solid #9E9E9E',
            borderRadius: 10,
            padding: '10px 28px',
            display: 'inline-block',
          }}
        >
          <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#9E9E9E', letterSpacing: 6, opacity: 0.7 }}>
            LEGAL ✓ COURT — कुछ नहीं कर सका
          </div>
        </div>
      </div>

      {/* === STICK FIGURE — teacher pose === */}
      <div
        style={{
          position: 'absolute',
          left: 90,
          bottom: 60,
          transform: `scale(${figureScale}) translateY(${breathY}px)`,
          transformOrigin: 'bottom left',
        }}
      >
        <svg width="100" height="180" viewBox="0 0 100 180">
          <ellipse cx="50" cy="172" rx="38" ry="10" fill="rgba(64,128,255,0.1)" />
          <line x1="50" y1="70" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2="18" y2={108 + pointY} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2="85" y2={80 - pointY} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="32" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="68" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="50" cy="44" r="25" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${headNod}, 50, 44)`} />
          <ellipse cx="38" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="62" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="47" y1="44" x2="53" y2="44" stroke="#e9d700" strokeWidth="1.5" />
          <path d="M 42 57 Q 50 63 58 57" fill="none" stroke="#F5F5F5" strokeWidth="1.8" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 88% 82% at 50% 50%, transparent 38%, rgba(0,0,0,0.78) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_2_CyprusKaSach;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_3_RaatRaat',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_3_RaatRaat: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Clock — midnight transition
  const clockRotate = interpolate(frame, [0, durationInFrames], [0, 360], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const nightOp = interpolate(frame, [10, 30], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Money disappearing
  const moneyOp = interpolate(frame, [8, 20], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.in(Easing.cubic),
  });

  // Govt hand grabbing
  const handY = interpolate(frame, [12, 28], [-200, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const handOp = interpolate(frame, [12, 24], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // "रात-रात" dramatic text
  const raatOp = interpolate(frame, [25, 36], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const raatScale = sp(25, { damping: 6, stiffness: 200 });

  // Court crossed out
  const courtOp = interpolate(frame, [42, 54], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const courtScale = sp(42);
  const crossOp = interpolate(frame, [50, 60], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // "राष्ट्रीय संकट" badge
  const crisisOp = interpolate(frame, [58, 70], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const crisisScale = sp(58);

  // Night particles — stars going dark
  const particles = Array.from({ length: 35 }, (_, i) => ({
    x: seededRandom(i * 13) * 1920,
    y: seededRandom(i * 7) * 540,
    r: 0.8 + seededRandom(i * 5) * 2,
    op: (0.2 + seededRandom(i * 9) * 0.4) * nightOp,
  }));

  // Stick figure — sleeping then shocked
  const figureScale = sp(30);
  const breathY = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;
  const wakeUp = interpolate(frame, [28, 40], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const shapeRot = frame * 0.08;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === GRADIENT BG — midnight dark blue === */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(180deg, #040812 0%, #0A0A18 40%, #1C1C1C 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background:
            `radial-gradient(ellipse 60% 40% at 50% 0%, rgba(20,20,80,${nightOp * 0.3}) 0%, transparent 70%)`,
        }}
      />

      {/* === SHAPE LAYER 1 — moon glow === */}
      <AbsoluteFill style={{ opacity: 0.08 * nightOp }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <circle cx="1700" cy="120" r="80" fill="#F5F5F5" opacity="0.6" />
          <circle cx="1755" cy="100" r="70" fill="#040812" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER 2 — rotating govt seal === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <circle
            cx="960"
            cy="540"
            r="350"
            fill="none"
            stroke="#ff0000"
            strokeWidth="1"
            strokeDasharray="20 12"
            transform={`rotate(${shapeRot}, 960, 540)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === STARS/PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#F5F5F5" opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === CLOCK — center top === */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: '50%',
          transform: 'translateX(-50%)',
        }}
      >
        <svg width="200" height="200" viewBox="0 0 200 200">
          <circle cx="100" cy="100" r="90" fill="#0A0A18" stroke="#2D2D2D" strokeWidth="3" />
          {Array.from({ length: 12 }, (_, i) => {
            const a = (i / 12) * Math.PI * 2 - Math.PI / 2;
            return (
              <line
                key={i}
                x1={100 + Math.cos(a) * 74}
                y1={100 + Math.sin(a) * 74}
                x2={100 + Math.cos(a) * 82}
                y2={100 + Math.sin(a) * 82}
                stroke="#9E9E9E"
                strokeWidth="2"
              />
            );
          })}
          {/* Hour hand — pointing to 12 */}
          <line
            x1="100"
            y1="100"
            x2={100 + Math.cos(-Math.PI / 2 + clockRotate * Math.PI / 180 * 0.083) * 55}
            y2={100 + Math.sin(-Math.PI / 2 + clockRotate * Math.PI / 180 * 0.083) * 55}
            stroke="#F5F5F5"
            strokeWidth="4"
            strokeLinecap="round"
          />
          {/* Minute hand */}
          <line
            x1="100"
            y1="100"
            x2={100 + Math.cos(-Math.PI / 2 + clockRotate * Math.PI / 180) * 72}
            y2={100 + Math.sin(-Math.PI / 2 + clockRotate * Math.PI / 180) * 72}
            stroke="#ff0000"
            strokeWidth="3"
            strokeLinecap="round"
          />
          <circle cx="100" cy="100" r="6" fill="#ff0000" />
          <text x="100" y="155" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="14" fill="#9E9E9E">MIDNIGHT</text>
        </svg>
      </div>

      {/* === MONEY STACK — vanishing === */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 120,
          opacity: moneyOp,
        }}
      >
        <svg width="280" height="220" viewBox="0 0 280 220">
          {[4, 3, 2, 1, 0].map(i => (
            <g key={i} transform={`translate(${i * 6}, ${i * -8})`}>
              <rect x="10" y={60 + i * 10} width="260" height="80" rx="8" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="1" />
              <text x="140" y={108 + i * 10} textAnchor="middle" fontFamily="JetBrains Mono" fontSize="28" fontWeight="700" fill="#e9d700">€€€</text>
            </g>
          ))}
          <text x="140" y="200" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="22" fill="#9E9E9E">जीवन भर की कमाई</text>
        </svg>
      </div>

      {/* === GOVT HAND GRABBING === */}
      <div
        style={{
          position: 'absolute',
          top: handY,
          left: 80,
          opacity: handOp,
        }}
      >
        <svg width="320" height="280" viewBox="0 0 320 280">
          {/* Sleeve */}
          <rect x="60" y="160" width="200" height="120" rx="20" fill="#ff0000" />
          {/* Hand */}
          <ellipse cx="160" cy="170" rx="80" ry="50" fill="#ff0000" />
          {/* Fingers curling */}
          {[0, 1, 2, 3].map(i => (
            <rect key={i} x={80 + i * 40} y="100" width="28" height="80" rx="14" fill="#ff0000" />
          ))}
          {/* GOVT text */}
          <text x="160" y="220" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="900" fill="#F5F5F5">GOVT</text>
        </svg>
      </div>

      {/* === "रात-रात" TEXT === */}
      <div
        style={{
          position: 'absolute',
          top: 260,
          left: '50%',
          transform: `translateX(-50%) scale(${raatScale})`,
          transformOrigin: 'top center',
          opacity: raatOp,
          textAlign: 'center',
          whiteSpace: 'nowrap',
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 120,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
          }}
        >
          रात-<span style={{ color: '#ff0000' }}>रात</span>
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 52,
            color: '#9E9E9E',
            fontWeight: 600,
          }}
        >
          सरकार ने ले लिया
        </div>
      </div>

      {/* === COURT CROSSED OUT === */}
      <div
        style={{
          position: 'absolute',
          bottom: 180,
          right: 120,
          opacity: courtOp,
          transform: `scale(${courtScale})`,
          transformOrigin: 'right bottom',
        }}
      >
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <svg width="220" height="180" viewBox="0 0 220 180">
            {/* Courthouse */}
            <rect x="30" y="70" width="160" height="100" rx="4" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
            <polygon points="110,20 200,70 20,70" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
            <rect x="90" y="100" width="40" height="70" rx="4" fill="#1C1C1C" />
            {[50, 90, 130, 170].map(x => (
              <rect key={x} x={x} y="80" width="20" height="40" rx="3" fill="#1C1C1C" />
            ))}
            <text x="110" y="160" textAnchor="middle" fontFamily="Inter" fontSize="16" fill="#9E9E9E">COURT</text>
            {/* Cross */}
            {crossOp > 0.1 && (
              <>
                <line x1="20" y1="20" x2="200" y2="170" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" opacity={crossOp} />
                <line x1="200" y1="20" x2="20" y2="170" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" opacity={crossOp} />
              </>
            )}
          </svg>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 26,
              color: '#ff0000',
              textAlign: 'center',
              marginTop: 8,
              opacity: crossOp,
              fontWeight: 700,
            }}
          >
            कुछ नहीं कर सकी
          </div>
        </div>
      </div>

      {/* === NATIONAL CRISIS BADGE === */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${crisisScale})`,
          transformOrigin: 'center bottom',
          opacity: crisisOp,
          whiteSpace: 'nowrap',
        }}
      >
        <div
          style={{
            background: '#ff0000',
            borderRadius: 16,
            padding: '16px 48px',
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <svg width="36" height="36" viewBox="0 0 36 36">
            <polygon points="18,3 33,30 3,30" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="18" y1="12" x2="18" y2="22" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
            <circle cx="18" cy="26" r="2" fill="#F5F5F5" />
          </svg>
          <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 900, color: '#F5F5F5' }}>
            राष्ट्रीय संकट — कानून भी मजबूर
          </span>
        </div>
      </div>

      {/* === STICK FIGURE — waking up shocked === */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          bottom: 80,
          transform: `scale(${figureScale}) translateY(${breathY}px)`,
          transformOrigin: 'bottom right',
        }}
      >
        <svg width="100" height="180" viewBox="0 0 100 180">
          <ellipse cx="50" cy="172" rx="38" ry="10" fill="rgba(255,0,0,0.08)" />
          <line x1="50" y1="70" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms up — shocked */}
          <line x1="50" y1="88" x2={22 - wakeUp * 10} y2={68 - wakeUp * 15} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2={78 + wakeUp * 10} y2={68 - wakeUp * 15} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="32" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="68" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="50" cy="44" r="25" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${headNod}, 50, 44)`} />
          <ellipse cx="38" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="62" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="47" y1="44" x2="53" y2="44" stroke="#e9d700" strokeWidth="1.5" />
          <ellipse cx="50" cy="58" rx="5" ry={3 + wakeUp * 2} fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 85% 80% at 50% 50%, transparent 35%, rgba(0,0,0,0.85) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_3_RaatRaat;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_4_NoteBandi',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_4_NoteBandi: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // TV announcement — screen flicker
  const tvFlicker = frame < 20
    ? 0.85 + Math.sin(frame * 12) * 0.15
    : 1;

  // "8 PM" clock reveal
  const timeOp = interpolate(frame, [5, 16], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const timeScale = sp(5);

  // Note X-cross animation
  const crossProgress = interpolate(frame, [18, 35], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Screen shake on announcement
  const shakeX = frame >= 18 && frame <= 26
    ? Math.sin(frame * 20) * 7
    : 0;
  const shakeY = frame >= 18 && frame <= 26
    ? Math.sin(frame * 26) * 5
    : 0;

  // "DEMONETISED" stamp slam
  const demoOp = interpolate(frame, [28, 38], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const demoY = interpolate(frame, [28, 36], [-100, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.back(1.6)),
  });
  const demoRot = interpolate(frame, [28, 38], [-5, -2], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Bank queue — people lining up
  const queueOp = interpolate(frame, [42, 54], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const queueScale = sp(42);

  // "2016" label
  const yearOp = interpolate(frame, [0, 12], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles — scattered notes
  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: 400 + seededRandom(i * 13) * 1100,
    y: ((seededRandom(i * 9) * 800 + frame * (1 + seededRandom(i * 3) * 0.8)) % 800) + 100,
    w: 40 + seededRandom(i * 5) * 30,
    h: 20 + seededRandom(i * 7) * 10,
    rot: seededRandom(i * 11) * 360 + frame * seededRandom(i * 2) * 2,
    op: 0.08 + seededRandom(i * 17) * 0.12,
  }));

  // Stick figure — frantic
  const figureScale = sp(44);
  const breathY = Math.sin(frame * 0.08) * 3;
  const headNod = Math.sin(frame * 0.09) * 5;
  const franticArm = Math.sin(frame * 0.14) * 12;

  const shapeRot = frame * 0.1;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translate(${shakeX}px, ${shakeY}px)`,
      }}
    >
      {/* === GRADIENT BG — urgent broadcast red === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 65% 55% at 50% 45%, #1A0800 0%, #1C1C1C 55%, #0A0A0A 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 40% 35% at 50% 45%, rgba(255,80,0,0.07) 0%, transparent 70%)',
        }}
      />

      {/* === SHAPE LAYER 1 — broadcast signal rings === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {[100, 200, 320].map((r, i) => (
            <circle
              key={i}
              cx="960"
              cy="360"
              r={r}
              fill="none"
              stroke="#ff0000"
              strokeWidth="1.5"
              opacity={0.8 - i * 0.2}
              transform={`scale(${1 + Math.sin(frame * 0.04 + i) * 0.06})`}
              style={{ transformOrigin: '960px 360px' }}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER 2 — national flag tricolor hint === */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <rect x="0" y="0" width="1920" height="6" fill="#FF9933" />
          <rect x="0" y="8" width="1920" height="6" fill="#F5F5F5" />
          <rect x="0" y="16" width="1920" height="6" fill="#138808" />
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — TV scanlines === */}
      <AbsoluteFill style={{ opacity: 0.03 * tvFlicker }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 108 }, (_, i) => (
            <line key={i} x1="0" y1={i * 10} x2="1920" y2={i * 10} stroke="#F5F5F5" strokeWidth="0.4" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SCATTERED NOTE PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <g key={i} transform={`translate(${p.x}, ${p.y}) rotate(${p.rot})`}>
              <rect x={-p.w / 2} y={-p.h / 2} width={p.w} height={p.h} rx="3" fill="#e9d700" opacity={p.op} />
              <line x1={-p.w / 2 + 4} y1="0" x2={p.w / 2 - 4} y2="0" stroke="#1C1C1C" strokeWidth="0.8" opacity={p.op} />
            </g>
          ))}
        </svg>
      </AbsoluteFill>

      {/* === 8 PM TIME BADGE === */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 90,
          opacity: timeOp,
          transform: `scale(${timeScale})`,
          transformOrigin: 'top left',
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#ff0000',
            letterSpacing: 6,
            fontWeight: 700,
            marginBottom: 6,
          }}
        >
          ◈ 8 NOV 2016 — 8:00 PM ◈
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 90,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
          }}
        >
          नोटबंदी
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 36,
            color: '#9E9E9E',
            fontWeight: 600,
          }}
        >
          DEMONETISATION
        </div>
      </div>

      {/* === TWO NOTES — CENTER WITH X === */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          transform: 'translate(-50%, -50%)',
          display: 'flex',
          gap: 60,
        }}
      >
        {['₹500', '₹1000'].map((val, ni) => (
          <div key={ni} style={{ position: 'relative' }}>
            <svg width="280" height="140" viewBox="0 0 280 140">
              <rect x="5" y="5" width="270" height="130" rx="10" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
              <rect x="5" y="5" width="270" height="130" rx="10" fill="none" stroke="#e9d700" strokeWidth="1" opacity="0.3" />
              <text x="140" y="70" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="48" fontWeight="900" fill="#e9d700">{val}</text>
              <text x="140" y="110" textAnchor="middle" fontFamily="Inter" fontSize="18" fill="#9E9E9E">INDIA</text>
              {/* Diagonal X cross */}
              {crossProgress > 0 && (
                <>
                  <line x1="20" y1="20" x2={20 + crossProgress * 240} y2={20 + crossProgress * 100} stroke="#ff0000" strokeWidth="10" strokeLinecap="round" opacity="0.85" />
                  <line x1="260" y1="20" x2={260 - crossProgress * 240} y2={20 + crossProgress * 100} stroke="#ff0000" strokeWidth="10" strokeLinecap="round" opacity="0.85" />
                </>
              )}
            </svg>
          </div>
        ))}
      </div>

      {/* === DEMONETISED STAMP === */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          transform: `translate(-50%, calc(-50% + ${demoY}px)) rotate(${demoRot}deg)`,
          opacity: demoOp,
        }}
      >
        <div
          style={{
            border: '8px solid #ff0000',
            borderRadius: 14,
            padding: '14px 44px',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 64,
              fontWeight: 900,
              color: '#ff0000',
              letterSpacing: 8,
              opacity: 0.9,
            }}
          >
            BANNED
          </div>
        </div>
      </div>

      {/* === BANK QUEUE === */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: '50%',
          transform: `translateX(-50%) scale(${queueScale})`,
          transformOrigin: 'center bottom',
          opacity: queueOp,
          display: 'flex',
          alignItems: 'flex-end',
          gap: 16,
        }}
      >
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, color: '#9E9E9E', marginRight: 16 }}>बैंक लाइन →</div>
        {Array.from({ length: 9 }, (_, i) => (
          <svg key={i} width="36" height="72" viewBox="0 0 36 72">
            <circle cx="18" cy="12" r="9" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="1.5" />
            <line x1="18" y1="21" x2="18" y2="48" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
            <line x1="18" y1="30" x2="6" y2="40" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
            <line x1="18" y1="30" x2="30" y2="40" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
            <line x1="18" y1="48" x2="10" y2="68" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
            <line x1="18" y1="48" x2="26" y2="68" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
          </svg>
        ))}
        {/* Bank building */}
        <svg width="80" height="100" viewBox="0 0 80 100">
          <rect x="5" y="35" width="70" height="60" rx="3" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="1.5" />
          <polygon points="40,5 75,35 5,35" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="1.5" />
          <rect x="30" y="60" width="20" height="35" rx="3" fill="#ff0000" opacity="0.4" />
          <text x="40" y="52" textAnchor="middle" fontFamily="Inter" fontSize="10" fill="#9E9E9E">BANK</text>
        </svg>
      </div>

      {/* === STICK FIGURE — frantic === */}
      <div
        style={{
          position: 'absolute',
          left: 90,
          bottom: 80,
          transform: `scale(${figureScale}) translateY(${breathY}px)`,
          transformOrigin: 'bottom left',
        }}
      >
        <svg width="100" height="180" viewBox="0 0 100 180">
          <ellipse cx="50" cy="172" rx="38" ry="10" fill="rgba(255,0,0,0.08)" />
          <line x1="50" y1="70" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2={18 + franticArm} y2={72 - franticArm * 0.5} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2={82 - franticArm} y2={72 + franticArm * 0.5} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="32" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="68" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="50" cy="44" r="25" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${headNod}, 50, 44)`} />
          <ellipse cx="38" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="62" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="47" y1="44" x2="53" y2="44" stroke="#e9d700" strokeWidth="1.5" />
          <ellipse cx="50" cy="57" rx="6" ry="5" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 88% 82% at 50% 50%, transparent 38%, rgba(0,0,0,0.8) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_4_NoteBandi;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_5_NiyamBadle',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_5_NiyamBadle: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow zoom in — confrontational
  const cameraScale = interpolate(frame, [0, durationInFrames], [1, 1.06], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Rule book flip animation
  const bookOp = interpolate(frame, [5, 18], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const bookScale = sp(5);

  // Rules changing — strike through old, new appears
  const rule1StrikeW = interpolate(frame, [20, 32], [0, 340], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const rule2StrikeW = interpolate(frame, [28, 40], [0, 340], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const rule3StrikeW = interpolate(frame, [36, 48], [0, 280], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // New rules appear
  const newRule1Op = interpolate(frame, [30, 42], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const newRule1Scale = sp(30);
  const newRule2Op = interpolate(frame, [38, 50], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const newRule2Scale = sp(38);

  // Big warning statement
  const warnOp = interpolate(frame, [54, 66], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const warnScale = sp(54, { damping: 7, stiffness: 160 });

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 13) * 1920,
    y: ((seededRandom(i * 9) * 1080 + frame * (0.4 + seededRandom(i * 3) * 0.3)) % 1080),
    r: 1.5 + seededRandom(i * 5) * 2.5,
    op: 0.06 + seededRandom(i * 7) * 0.1,
  }));

  // Stick figure — arms crossed, assertive
  const figureScale = sp(40);
  const breathY = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 2;

  const shapeRot = frame * 0.08;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `scale(${cameraScale})`,
        transformOrigin: 'center center',
      }}
    >
      {/* === GRADIENT BG — split authority === */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(135deg, #0A0A0A 0%, #1C1C1C 50%, #150A00 100%)',
        }}
      />

      {/* === SHAPE LAYER 1 — law document lines === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 18 }, (_, i) => (
            <line
              key={i}
              x1="80"
              y1={80 + i * 55}
              x2="680"
              y2={80 + i * 55}
              stroke="#9E9E9E"
              strokeWidth="0.8"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER 2 — authority circles === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <circle
            cx="1600"
            cy="300"
            r="280"
            fill="none"
            stroke="#ff0000"
            strokeWidth="1.5"
            transform={`rotate(${shapeRot}, 1600, 300)`}
          />
          <circle
            cx="1600"
            cy="300"
            r="180"
            fill="none"
            stroke="#ff0000"
            strokeWidth="1"
            transform={`rotate(${-shapeRot * 1.5}, 1600, 300)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — erasing lines === */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 8 }, (_, i) => (
            <line
              key={i}
              x1="0"
              y1={200 + i * 110}
              x2="1920"
              y2={200 + i * 110}
              stroke="#ff0000"
              strokeWidth="0.6"
              strokeDasharray="60 40"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#ff0000" opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === RULEBOOK — LEFT === */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 90,
          opacity: bookOp,
          transform: `scale(${bookScale})`,
          transformOrigin: 'top left',
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 20,
            color: '#9E9E9E',
            letterSpacing: 6,
            marginBottom: 16,
            fontWeight: 700,
          }}
        >
          ◈ RULE BOOK ◈
        </div>
        <div
          style={{
            background: '#2D2D2D',
            borderRadius: 16,
            padding: '32px 40px',
            width: 700,
            border: '1px solid rgba(158,158,158,0.2)',
          }}
        >
          {/* Old Rule 1 */}
          <div style={{ position: 'relative', marginBottom: 28 }}>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, color: '#9E9E9E', fontWeight: 600 }}>
              पुराना नियम: सोना रख सकते हो
            </div>
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                height: 4,
                width: rule1StrikeW,
                background: '#ff0000',
                borderRadius: 2,
              }}
            />
          </div>
          {/* Old Rule 2 */}
          <div style={{ position: 'relative', marginBottom: 28 }}>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, color: '#9E9E9E', fontWeight: 600 }}>
              पुराना नियम: कितनी भी नगदी रखो
            </div>
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                height: 4,
                width: rule2StrikeW,
                background: '#ff0000',
                borderRadius: 2,
              }}
            />
          </div>
          {/* Old Rule 3 */}
          <div style={{ position: 'relative' }}>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, color: '#9E9E9E', fontWeight: 600 }}>
              पुराना नियम: पैसा कब भी निकालो
            </div>
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                height: 4,
                width: rule3StrikeW,
                background: '#ff0000',
                borderRadius: 2,
              }}
            />
          </div>
        </div>
      </div>

      {/* === NEW RULES — RIGHT === */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          right: 90,
          width: 680,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 20,
            color: '#ff0000',
            letterSpacing: 6,
            marginBottom: 16,
            fontWeight: 700,
            opacity: newRule1Op,
          }}
        >
          ◈ NEW RULES ◈
        </div>
        <div
          style={{
            opacity: newRule1Op,
            transform: `scale(${newRule1Scale})`,
            transformOrigin: 'top right',
            background: 'rgba(255,0,0,0.08)',
            border: '2px solid rgba(255,0,0,0.4)',
            borderRadius: 14,
            padding: '20px 28px',
            marginBottom: 20,
          }}
        >
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 36, color: '#F5F5F5', fontWeight: 700 }}>
            नया: सोने पर 50% टैक्स
          </div>
        </div>
        <div
          style={{
            opacity: newRule2Op,
            transform: `scale(${newRule2Scale})`,
            transformOrigin: 'top right',
            background: 'rgba(255,0,0,0.08)',
            border: '2px solid rgba(255,0,0,0.4)',
            borderRadius: 14,
            padding: '20px 28px',
          }}
        >
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 36, color: '#F5F5F5', fontWeight: 700 }}>
            नया: सिर्फ ₹10L निकाल सकते हो
          </div>
        </div>
      </div>

      {/* === BIG WARNING STATEMENT === */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: '50%',
          transform: `translateX(-50%) scale(${warnScale})`,
          transformOrigin: 'center bottom',
          opacity: warnOp,
          textAlign: 'center',
          whiteSpace: 'nowrap',
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 66,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.2,
          }}
        >
          सरकार जब चाहे तब{' '}
          <span style={{ color: '#ff0000' }}>नियम बदल सकती है</span>
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 44,
            color: '#9E9E9E',
            marginTop: 8,
          }}
        >
          — तुम्हारी सुरक्षित संपत्ति रातोंरात असुरक्षित
        </div>
      </div>

      {/* === STICK FIGURE — assertive, arms folded === */}
      <div
        style={{
          position: 'absolute',
          left: 90,
          bottom: 80,
          transform: `scale(${figureScale}) translateY(${breathY}px)`,
          transformOrigin: 'bottom left',
        }}
      >
        <svg width="100" height="180" viewBox="0 0 100 180">
          <ellipse cx="50" cy="172" rx="38" ry="10" fill="rgba(255,0,0,0.08)" />
          <line x1="50" y1="70" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms crossed */}
          <line x1="50" y1="90" x2="22" y2="105" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="22" y1="105" x2="55" y2="98" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="90" x2="78" y2="105" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="78" y1="105" x2="45" y2="98" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="32" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="68" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="50" cy="44" r="25" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${headNod}, 50, 44)`} />
          <ellipse cx="38" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="62" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="47" y1="44" x2="53" y2="44" stroke="#e9d700" strokeWidth="1.5" />
          <path d="M 42 57 Q 50 62 58 57" fill="none" stroke="#F5F5F5" strokeWidth="1.8" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 88% 82% at 50% 50%, transparent 38%, rgba(0,0,0,0.78) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_5_NiyamBadle;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_6_ArgentinaDollar',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_6_ArgentinaDollar: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera pan right — Latin America
  const cameraPan = interpolate(frame, [0, durationInFrames], [20, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Argentina label
  const arOp = interpolate(frame, [5, 18], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const arScale = sp(5);

  // Dollar stack rising (false security)
  const dollarRise = interpolate(frame, [15, 40], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // "SAFE" checkmark — green initially
  const safeOp = interpolate(frame, [38, 48], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const safeScale = sp(38);

  // Peso collapse arrow
  const pesoOp = interpolate(frame, [28, 40], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Foreshadowing crack
  const crackOp = interpolate(frame, [55, 68], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles — dollar signs drifting
  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 13) * 1920,
    y: ((seededRandom(i * 9) * 900 + frame * (0.6 + seededRandom(i * 3) * 0.4)) % 900) + 100,
    op: 0.05 + seededRandom(i * 7) * 0.1,
    scale: 0.6 + seededRandom(i * 5) * 0.8,
  }));

  // Stick figure — dollar thought bubble
  const figureScale = sp(42);
  const breathY = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const shapeRot = frame * 0.06;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translateX(${cameraPan}px)`,
      }}
    >
      {/* === GRADIENT BG — warm Latin America ochre === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 70% 60% at 40% 50%, #1A1000 0%, #1C1C1C 55%, #0A0A0A 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 35% 35% at 35% 45%, rgba(74,144,226,0.07) 0%, transparent 70%)',
        }}
      />

      {/* === SHAPE LAYER 1 — South America outline === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <path
            d="M 200 100 Q 280 80 320 160 Q 380 280 360 400 Q 340 520 300 620 Q 260 720 220 780 Q 180 840 200 900 Q 220 960 260 980 Q 200 960 160 880 Q 120 800 140 700 Q 160 580 180 460 Q 160 360 180 260 Q 200 160 200 100 Z"
            fill="none"
            stroke="#FFC107"
            strokeWidth="2"
            opacity="0.6"
          />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER 2 — flag-inspired stripes === */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <rect x="0" y="0" width="1920" height="5" fill="#74B2DD" />
          <rect x="0" y="9" width="1920" height="12" fill="#F5F5F5" />
          <rect x="0" y="25" width="1920" height="5" fill="#74B2DD" />
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — currency grid === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 10 }, (_, i) =>
            Array.from({ length: 6 }, (_, j) => (
              <text
                key={`${i}-${j}`}
                x={100 + i * 190}
                y={120 + j * 170}
                fontFamily="JetBrains Mono"
                fontSize="36"
                fill="#74B2DD"
                textAnchor="middle"
                opacity="0.5"
              >
                $
              </text>
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* === DOLLAR PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <text
              key={i}
              x={p.x}
              y={p.y}
              fontFamily="JetBrains Mono"
              fontSize={28 * p.scale}
              fill="#74B2DD"
              opacity={p.op}
              textAnchor="middle"
            >
              $
            </text>
          ))}
        </svg>
      </AbsoluteFill>

      {/* === ARGENTINA 2001 HEADER === */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 90,
          opacity: arOp,
          transform: `scale(${arScale})`,
          transformOrigin: 'top left',
        }}
      >
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 20, color: '#74B2DD', letterSpacing: 8, fontWeight: 700, marginBottom: 8 }}>
          CASE STUDY: ARGENTINA
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 140, fontWeight: 900, color: '#F5F5F5', lineHeight: 1 }}>
          2001
        </div>
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#FFC107', fontWeight: 700 }}>
          का संकट
        </div>
      </div>

      {/* === DOLLAR STACK — center right === */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          right: 200,
          transform: 'translateY(-50%)',
        }}
      >
        <svg width="300" height={300 * dollarRise + 20} viewBox={`0 0 300 ${300 * dollarRise + 20}`}>
          {Array.from({ length: Math.ceil(dollarRise * 8) }, (_, i) => (
            <g key={i} transform={`translate(0, ${(7 - i) * 35})`}>
              <rect x="10" y="0" width="280" height="52" rx="8" fill="#0A3D00" stroke="#74B2DD" strokeWidth="1.5" />
              <text x="150" y="33" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="26" fontWeight="900" fill="#74B2DD">
                $ DOLLAR
              </text>
            </g>
          ))}
        </svg>
        <div style={{ textAlign: 'center', marginTop: 12 }}>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#9E9E9E' }}>डॉलर में बचत</div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 20, color: '#9E9E9E', marginTop: 4 }}>"SAFE HAVEN"</div>
        </div>
      </div>

      {/* === PESO WEAK ARROW === */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '55%',
          transform: 'translateY(-50%)',
          opacity: pesoOp,
        }}
      >
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, color: '#ff0000', fontWeight: 900 }}>PESO</div>
          <svg width="60" height="100" viewBox="0 0 60 100">
            <line x1="30" y1="0" x2="30" y2="80" stroke="#ff0000" strokeWidth="4" strokeLinecap="round" />
            <polygon points="30,100 5,70 55,70" fill="#ff0000" />
          </svg>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#ff0000', fontWeight: 700 }}>कमजोर ↓</div>
        </div>
      </div>

      {/* === SAFE CHECKMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 220,
          right: 180,
          opacity: safeOp,
          transform: `scale(${safeScale})`,
          transformOrigin: 'center bottom',
        }}
      >
        <div
          style={{
            background: 'rgba(0,100,0,0.3)',
            border: '3px solid #00AA00',
            borderRadius: 16,
            padding: '16px 32px',
            display: 'flex',
            alignItems: 'center',
            gap: 16,
          }}
        >
          <svg width="36" height="36" viewBox="0 0 36 36">
            <circle cx="18" cy="18" r="16" fill="none" stroke="#00AA00" strokeWidth="2.5" />
            <path d="M10 18 L16 24 L26 12" stroke="#00AA00" strokeWidth="3" fill="none" strokeLinecap="round" />
          </svg>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 36, color: '#F5F5F5', fontWeight: 700 }}>
            "डॉलर में सुरक्षित हैं"
          </div>
        </div>
      </div>

      {/* === FORESHADOWING — crack in safety === */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: crackOp,
          whiteSpace: 'nowrap',
          textAlign: 'center',
        }}
      >
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#9E9E9E', fontWeight: 600 }}>
          लेकिन... सरकार के पास एक जवाब था —{' '}
          <span style={{ color: '#ff0000' }}>Corralito</span>
        </div>
      </div>

      {/* === STICK FIGURE — thinking/calculating === */}
      <div
        style={{
          position: 'absolute',
          left: 90,
          bottom: 80,
          transform: `scale(${figureScale}) translateY(${breathY}px)`,
          transformOrigin: 'bottom left',
        }}
      >
        <svg width="100" height="200" viewBox="0 0 100 200">
          {/* Thought bubble */}
          <circle cx="80" cy="20" r="6" fill="#74B2DD" opacity="0.5" />
          <circle cx="90" cy="10" r="4" fill="#74B2DD" opacity="0.4" />
          <circle cx="96" cy="3" r="10" fill="#2D2D2D" stroke="#74B2DD" strokeWidth="1.5" />
          <text x="96" y="7" textAnchor="middle" fontSize="10" fontFamily="JetBrains Mono" fill="#74B2DD">$</text>
          <ellipse cx="50" cy="192" rx="38" ry="10" fill="rgba(116,178,221,0.08)" />
          <line x1="50" y1="90" x2="50" y2="150" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="108" x2="22" y2="125" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="108" x2="78" y2="118" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="150" x2="32" y2="190" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="150" x2="68" y2="190" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="50" cy="64" r="25" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${headNod}, 50, 64)`} />
          <ellipse cx="38" cy="64" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="62" cy="64" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="47" y1="64" x2="53" y2="64" stroke="#e9d700" strokeWidth="1.5" />
          <path d="M 42 77 Q 50 83 58 77" fill="none" stroke="#F5F5F5" strokeWidth="1.8" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 88% 82% at 50% 50%, transparent 38%, rgba(0,0,0,0.76) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_6_ArgentinaDollar;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_7_Corralito',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_7_Corralito: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Fence/cage bars closing in
  const fenceProgress = interpolate(frame, [8, 35], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // "CORRALITO" word entrance
  const wordOp = interpolate(frame, [5, 18], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const wordScale = sp(5, { damping: 6, stiffness: 180 });

  // "छोटा बाड़ा" translation
  const transOp = interpolate(frame, [20, 30], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // $250 limit — tiny window
  const limitOp = interpolate(frame, [35, 46], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const limitScale = sp(35);

  // FROZEN stamp
  const frozenOp = interpolate(frame, [50, 60], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const frozenScale = sp(50, { damping: 5, stiffness: 200 });
  const frozenRot = interpolate(frame, [50, 60], [-8, -4], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Ice particles
  const particles = Array.from({ length: 25 }, (_, i) => ({
    x: seededRandom(i * 13) * 1920,
    y: ((seededRandom(i * 9) * 1080 + frame * (0.5 + seededRandom(i * 3) * 0.3)) % 1080),
    r: 1.5 + seededRandom(i * 5) * 3,
    op: 0.06 + seededRandom(i * 7) * 0.12,
  }));

  // Stick figure — caged, hands on bars
  const figureScale = sp(38);
  const breathY = Math.sin(frame * 0.05) * 1.5;
  const headNod = Math.sin(frame * 0.04) * 3;
  const graspBars = Math.min(1, fenceProgress * 1.5);

  const shapeRot = frame * 0.06;

  const numBars = 8;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === GRADIENT BG — ice blue-black === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 70% 60% at 50% 50%, #050D18 0%, #1C1C1C 55%, #0A0A0A 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 40% 40% at 50% 50%, rgba(30,100,200,0.08) 0%, transparent 70%)',
        }}
      />

      {/* === SHAPE LAYER 1 — rotating cage hexagon === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <polygon
            points="960,180 1200,310 1200,570 960,700 720,570 720,310"
            fill="none"
            stroke="#4080CC"
            strokeWidth="1.5"
            transform={`rotate(${shapeRot}, 960, 440)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER 2 — frost crystals === */}
      <AbsoluteFill style={{ opacity: 0.035 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {[
            [200, 200], [1700, 180], [300, 880], [1650, 860], [960, 100],
          ].map(([cx, cy], i) => (
            <g key={i}>
              <line x1={cx - 40} y1={cy} x2={cx + 40} y2={cy} stroke="#74B2DD" strokeWidth="1.5" />
              <line x1={cx} y1={cy - 40} x2={cx} y2={cy + 40} stroke="#74B2DD" strokeWidth="1.5" />
              <line x1={cx - 28} y1={cy - 28} x2={cx + 28} y2={cy + 28} stroke="#74B2DD" strokeWidth="1" />
              <line x1={cx + 28} y1={cy - 28} x2={cx - 28} y2={cy + 28} stroke="#74B2DD" strokeWidth="1" />
            </g>
          ))}
        </svg>
      </AbsoluteFill>

      {/* === ICE PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <polygon
              key={i}
              points={`${p.x},${p.y - p.r} ${p.x + p.r},${p.y} ${p.x},${p.y + p.r} ${p.x - p.r},${p.y}`}
              fill="#74B2DD"
              opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === "CORRALITO" WORD === */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${wordScale})`,
          transformOrigin: 'top center',
          opacity: wordOp,
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 160,
            fontWeight: 900,
            color: '#74B2DD',
            lineHeight: 1,
            letterSpacing: -4,
          }}
        >
          CORRALITO
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 48,
            color: '#9E9E9E',
            fontWeight: 500,
            marginTop: 4,
            opacity: transOp,
          }}
        >
          मतलब: <span style={{ color: '#F5F5F5', fontWeight: 700 }}>छोटा बाड़ा</span>
        </div>
      </div>

      {/* === ANIMATED FENCE BARS — center === */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          transform: 'translate(-50%, -40%)',
        }}
      >
        <svg width="600" height="300" viewBox="0 0 600 300">
          {/* Top bar */}
          <rect x={0} y="0" width={600 * fenceProgress} height="16" rx="8" fill="#74B2DD" />
          {/* Bottom bar */}
          <rect x={600 - 600 * fenceProgress} y="284" width={600 * fenceProgress} height="16" rx="8" fill="#74B2DD" />
          {/* Vertical bars */}
          {Array.from({ length: numBars }, (_, i) => {
            const barX = (i / (numBars - 1)) * 560 + 20;
            const barH = fenceProgress * 300;
            return (
              <rect
                key={i}
                x={barX - 8}
                y={(300 - barH) / 2}
                width="16"
                height={barH}
                rx="8"
                fill="#4080CC"
                opacity={0.7 + i * 0.04}
              />
            );
          })}
          {/* $250 tiny window */}
          <rect x="260" y="80" width="80" height="140" rx="4" fill="rgba(116,178,221,0.1)" stroke="#74B2DD" strokeWidth="2" strokeDasharray="8 4" />
          <text x="300" y="145" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="16" fill="#74B2DD" fontWeight="700">$250</text>
          <text x="300" y="165" textAnchor="middle" fontFamily="Inter" fontSize="12" fill="#9E9E9E">per week</text>
        </svg>
      </div>

      {/* === $250 LIMIT INFO === */}
      <div
        style={{
          position: 'absolute',
          bottom: 240,
          left: 90,
          opacity: limitOp,
          transform: `scale(${limitScale})`,
          transformOrigin: 'left bottom',
        }}
      >
        <div
          style={{
            background: '#2D2D2D',
            border: '2px solid #74B2DD',
            borderRadius: 16,
            padding: '20px 36px',
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: '#74B2DD', letterSpacing: 4, fontWeight: 700 }}>WEEKLY LIMIT</div>
            <div style={{ fontFamily: 'Inter', fontSize: 90, fontWeight: 900, color: '#74B2DD', lineHeight: 1 }}>$250</div>
          </div>
          <div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, color: '#F5F5F5', fontWeight: 700, lineHeight: 1.3 }}>
              हफ्ते में बस<br />ढाई सौ डॉलर
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#9E9E9E', marginTop: 8 }}>
              बाकि सब — FROZEN
            </div>
          </div>
        </div>
      </div>

      {/* === FROZEN STAMP === */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          right: 120,
          opacity: frozenOp,
          transform: `scale(${frozenScale}) rotate(${frozenRot}deg)`,
          transformOrigin: 'right bottom',
        }}
      >
        <div
          style={{
            border: '7px solid #74B2DD',
            borderRadius: 12,
            padding: '12px 36px',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 72,
              fontWeight: 900,
              color: '#74B2DD',
              letterSpacing: 8,
              opacity: 0.9,
            }}
          >
            FROZEN
          </div>
        </div>
      </div>

      {/* === STICK FIGURE — caged === */}
      <div
        style={{
          position: 'absolute',
          right: 120,
          top: 260,
          transform: `scale(${figureScale}) translateY(${breathY}px)`,
          transformOrigin: 'top right',
          opacity: Math.min(1, fenceProgress * 2),
        }}
      >
        <svg width="100" height="180" viewBox="0 0 100 180">
          <ellipse cx="50" cy="172" rx="38" ry="10" fill="rgba(116,178,221,0.08)" />
          <line x1="50" y1="70" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms reaching through bars */}
          <line x1="50" y1="88" x2={20 + graspBars * 8} y2={85 + graspBars * 5} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2={80 - graspBars * 8} y2={85 + graspBars * 5} stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="32" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="68" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="50" cy="44" r="25" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${headNod}, 50, 44)`} />
          <ellipse cx="38" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="62" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="47" y1="44" x2="53" y2="44" stroke="#e9d700" strokeWidth="1.5" />
          {/* Distressed mouth */}
          <path d="M 40 58 Q 50 53 60 58" fill="none" stroke="#F5F5F5" strokeWidth="1.5" strokeLinecap="round" />
          {/* Cage bars over figure */}
          {graspBars > 0.3 && Array.from({ length: 3 }, (_, i) => (
            <line
              key={i}
              x1={22 + i * 28}
              y1="20"
              x2={22 + i * 28}
              y2="175"
              stroke="#74B2DD"
              strokeWidth="4"
              strokeLinecap="round"
              opacity={graspBars * 0.5}
            />
          ))}
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 88% 82% at 50% 50%, transparent 38%, rgba(0,0,0,0.8) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_7_Corralito;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_8_DollarToPeso',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_8_DollarToPeso: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Conversion animation — dollar morphing to peso
  const convProgress = interpolate(frame, [10, 45], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  // Dollar opacity fades, peso fades in
  const dollarOp = interpolate(frame, [10, 45], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const pesoOp = interpolate(frame, [25, 50], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Value destruction bar
  const valueBar = interpolate(frame, [45, 68], [1, 0.42], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // "-58%" impact
  const lossOp = interpolate(frame, [55, 66], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const lossScale = sp(55, { damping: 5, stiffness: 220 });

  // Screen shake on conversion
  const shakeX = frame >= 43 && frame <= 50
    ? Math.sin(frame * 22) * 6
    : 0;
  const shakeY = frame >= 43 && frame <= 50
    ? Math.sin(frame * 28) * 4
    : 0;

  // Particles — currency symbols scattering
  const particles = Array.from({ length: 26 }, (_, i) => ({
    x: 600 + seededRandom(i * 13) * 700,
    y: ((seededRandom(i * 9) * 700 + frame * (1.2 + seededRandom(i * 3))) % 700) + 100,
    op: 0.06 + seededRandom(i * 7) * 0.12,
    isDollar: seededRandom(i * 11) > convProgress,
  }));

  // Stick figure — watching value evaporate
  const figureScale = sp(38);
  const breathY = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;
  const slump = interpolate(frame, [45, 65], [0, 10], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const shapeRot = frame * 0.07;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translate(${shakeX}px, ${shakeY}px)`,
      }}
    >
      {/* === GRADIENT BG — alchemy gone wrong === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 65% 55% at 50% 45%, #0A0800 0%, #1C1C1C 55%, #0A0A0A 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background: `radial-gradient(ellipse 40% 35% at 50% 45%, rgba(${
            Math.round(116 * (1 - convProgress))},${Math.round(178 * (1 - convProgress))},${Math.round(221 * (1 - convProgress))},0.06) 0%, transparent 70%)`,
        }}
      />

      {/* === SHAPE LAYER — alchemy circle === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <circle
            cx="960"
            cy="440"
            r="300"
            fill="none"
            stroke="#FFC107"
            strokeWidth="1.5"
            strokeDasharray="30 20"
            transform={`rotate(${shapeRot}, 960, 440)`}
          />
          <circle
            cx="960"
            cy="440"
            r="200"
            fill="none"
            stroke="#ff0000"
            strokeWidth="1"
            transform={`rotate(${-shapeRot * 1.4}, 960, 440)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — flowing conversion lines === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 6 }, (_, i) => (
            <path
              key={i}
              d={`M ${300 + i * 40} 200 Q 960 ${400 + Math.sin(i) * 100} ${1620 - i * 40} 680`}
              fill="none"
              stroke="#ff0000"
              strokeWidth="0.8"
              opacity={convProgress}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === CURRENCY PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <text
              key={i}
              x={p.x}
              y={p.y}
              fontFamily="JetBrains Mono"
              fontSize="28"
              fill={p.isDollar ? '#74B2DD' : '#ff0000'}
              opacity={p.op}
              textAnchor="middle"
            >
              {p.isDollar ? '$' : '₱'}
            </text>
          ))}
        </svg>
      </AbsoluteFill>

      {/* === CONVERSION VISUAL — CENTER === */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          transform: 'translate(-50%, -55%)',
          display: 'flex',
          alignItems: 'center',
          gap: 60,
        }}
      >
        {/* Dollar block */}
        <div style={{ opacity: dollarOp, textAlign: 'center' }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 48, color: '#74B2DD', fontWeight: 900, marginBottom: 8 }}>DOLLAR</div>
          <div
            style={{
              width: 200,
              height: 200,
              background: 'rgba(116,178,221,0.15)',
              border: '4px solid #74B2DD',
              borderRadius: 20,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <span style={{ fontFamily: 'Inter', fontSize: 100, fontWeight: 900, color: '#74B2DD' }}>$</span>
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, color: '#74B2DD', marginTop: 8 }}>$1.00</div>
        </div>

        {/* Arrow → */}
        <div>
          <svg width="160" height="80" viewBox="0 0 160 80">
            <defs>
              <linearGradient id="arrowConv" x1="0" y1="0" x2="1" y2="0">
                <stop offset="0%" stopColor="#74B2DD" />
                <stop offset="100%" stopColor="#ff0000" />
              </linearGradient>
            </defs>
            <line x1="10" y1="40" x2="130" y2="40" stroke="url(#arrowConv)" strokeWidth="5" strokeLinecap="round" />
            <polygon points="150,40 120,20 120,60" fill="#ff0000" />
            <text x="80" y="24" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="18" fill="#9E9E9E">सरकारी रेट</text>
          </svg>
        </div>

        {/* Peso block */}
        <div style={{ opacity: pesoOp, textAlign: 'center' }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 48, color: '#ff0000', fontWeight: 900, marginBottom: 8 }}>PESO</div>
          <div
            style={{
              width: 200,
              height: 200,
              background: 'rgba(255,0,0,0.1)',
              border: '4px solid #ff0000',
              borderRadius: 20,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <span style={{ fontFamily: 'Inter', fontSize: 100, fontWeight: 900, color: '#ff0000' }}>₱</span>
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, color: '#ff0000', marginTop: 8 }}>$0.42</div>
        </div>
      </div>

      {/* === VALUE BAR === */}
      <div
        style={{
          position: 'absolute',
          bottom: 240,
          left: '50%',
          transform: 'translateX(-50%)',
          width: 900,
          textAlign: 'center',
        }}
      >
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#9E9E9E', marginBottom: 10 }}>संपत्ति की वास्तविक कीमत</div>
        <div style={{ width: 900, height: 50, background: '#2D2D2D', borderRadius: 10, overflow: 'hidden', position: 'relative' }}>
          <div
            style={{
              width: `${valueBar * 100}%`,
              height: '100%',
              background: valueBar > 0.6
                ? 'linear-gradient(90deg, #e9d700, #FFC107)'
                : 'linear-gradient(90deg, #ff0000, #ff4444)',
              transition: 'width 0.1s',
              borderRadius: 10,
              boxShadow: `0 0 20px rgba(${valueBar > 0.6 ? '233,215,0' : '255,0,0'},0.3)`,
            }}
          />
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              transform: 'translate(-50%, -50%)',
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#F5F5F5',
              fontWeight: 700,
            }}
          >
            {Math.round(valueBar * 100)}%
          </div>
        </div>
      </div>

      {/* === "-58%" LOSS === */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: '50%',
          transform: `translateX(-50%) scale(${lossScale})`,
          transformOrigin: 'center bottom',
          opacity: lossOp,
          textAlign: 'center',
          whiteSpace: 'nowrap',
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 120,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 1,
            textShadow: '0 0 50px rgba(255,0,0,0.5)',
          }}
        >
          -58%
        </div>
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#F5F5F5', fontWeight: 700 }}>
          एक झटके में — आधी से भी कम
        </div>
      </div>

      {/* === STICK FIGURE — slumped === */}
      <div
        style={{
          position: 'absolute',
          left: 90,
          bottom: 80,
          transform: `scale(${figureScale}) translateY(${breathY + slump}px)`,
          transformOrigin: 'bottom left',
        }}
      >
        <svg width="100" height="180" viewBox="0 0 100 180">
          <ellipse cx="50" cy="172" rx="38" ry="10" fill="rgba(255,0,0,0.08)" />
          <line x1="50" y1="70" x2="50" y2="135" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2="22" y2="118" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2="78" y2="118" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="135" x2="32" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="135" x2="68" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="50" cy="44" r="25" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${headNod + slump * 0.5}, 50, 44)`} />
          <ellipse cx="38" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="62" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="47" y1="44" x2="53" y2="44" stroke="#e9d700" strokeWidth="1.5" />
          <path d="M 40 58 Q 50 53 60 58" fill="none" stroke="#F5F5F5" strokeWidth="1.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 88% 82% at 50% 50%, transparent 38%, rgba(0,0,0,0.8) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_8_DollarToPeso;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_9_BharatKaScenario',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_9_BharatKaScenario: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow tilt — building tension
  const cameraTilt = interpolate(frame, [0, durationInFrames], [0, 0.8], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Three warning gauges
  const gauge1Fill = interpolate(frame, [10, 35], [0, 0.85], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const gauge2Fill = interpolate(frame, [18, 42], [0, 0.78], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const gauge3Fill = interpolate(frame, [26, 50], [0, 0.92], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const gaugeOp1 = interpolate(frame, [8, 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const gaugeOp2 = interpolate(frame, [16, 28], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const gaugeOp3 = interpolate(frame, [24, 36], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // "Capital Controls" warning
  const warningOp = interpolate(frame, [54, 66], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const warningScale = sp(54, { damping: 7, stiffness: 160 });

  // Alert blink
  const alertBlink = Math.floor(frame / 8) % 2 === 0 ? 1 : 0.3;

  // Particles — danger signals
  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 13) * 1920,
    y: ((seededRandom(i * 9) * 1080 + frame * (0.4 + seededRandom(i * 3) * 0.3)) % 1080),
    r: 1.5 + seededRandom(i * 5) * 2.5,
    op: 0.06 + seededRandom(i * 7) * 0.1,
  }));

  // Stick figure — watching gauges, nervous
  const figureScale = sp(40);
  const breathY = Math.sin(frame * 0.06) * 2.5;
  const headNod = Math.sin(frame * 0.05) * 4;
  const nervousShift = Math.sin(frame * 0.08) * 3;

  const gauges = [
    { label: 'विदेशी मुद्रा भंडार', sub: 'FOREX RESERVES', fill: gauge1Fill, op: gaugeOp1, danger: gauge1Fill > 0.7 },
    { label: 'रुपये की गिरावट', sub: 'RUPEE FALL', fill: gauge2Fill, op: gaugeOp2, danger: gauge2Fill > 0.7 },
    { label: 'विदेशी कर्ज', sub: 'FOREIGN DEBT', fill: gauge3Fill, op: gaugeOp3, danger: gauge3Fill > 0.7 },
  ];

  const shapeRot = frame * 0.06;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `rotate(${cameraTilt}deg)`,
        transformOrigin: 'center center',
      }}
    >
      {/* === GRADIENT BG — India in crisis tones === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 70% 60% at 50% 45%, #120800 0%, #1C1C1C 55%, #080808 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 40% 35% at 50% 45%, rgba(255,60,0,0.07) 0%, transparent 70%)',
        }}
      />

      {/* === SHAPE LAYER 1 — danger concentric arcs === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {[200, 340, 480].map((r, i) => (
            <circle
              key={i}
              cx="960"
              cy="500"
              r={r}
              fill="none"
              stroke="#ff0000"
              strokeWidth="1.2"
              strokeDasharray={i % 2 === 0 ? '15 10' : 'none'}
              opacity={0.7 - i * 0.15}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER 2 — india map outline silhouette === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <polygon
            points="960,100 1100,200 1150,350 1080,500 1020,580 960,700 900,580 840,500 770,350 820,200"
            fill="none"
            stroke="#FF9933"
            strokeWidth="2"
            transform={`rotate(${shapeRot * 0.3}, 960, 400)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — alert scanlines === */}
      <AbsoluteFill style={{ opacity: 0.025 * alertBlink }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 54 }, (_, i) => (
            <line key={i} x1="0" y1={i * 20} x2="1920" y2={i * 20} stroke="#ff0000" strokeWidth="0.4" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <polygon
              key={i}
              points={`${p.x},${p.y - p.r * 1.5} ${p.x + p.r},${p.y + p.r} ${p.x - p.r},${p.y + p.r}`}
              fill="#ff0000"
              opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === HEADLINE === */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 90,
        }}
      >
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 20, color: '#ff0000', letterSpacing: 6, fontWeight: 700, marginBottom: 8, opacity: alertBlink }}>
          ◈ INDIA RISK SCENARIO ◈
        </div>
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 80, fontWeight: 900, color: '#F5F5F5', lineHeight: 1.1 }}>
          अगर परिस्थितियां<br /><span style={{ color: '#ff0000' }}>बिगड़ें...</span>
        </div>
      </div>

      {/* === THREE GAUGES — center === */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          transform: 'translate(-50%, -40%)',
          display: 'flex',
          gap: 60,
        }}
      >
        {gauges.map((g, i) => (
          <div
            key={i}
            style={{
              opacity: g.op,
              textAlign: 'center',
              width: 280,
            }}
          >
            {/* Semicircle gauge */}
            <svg width="280" height="160" viewBox="0 0 280 160">
              <path d="M 30 140 A 110 110 0 0 1 250 140" fill="none" stroke="#2D2D2D" strokeWidth="20" strokeLinecap="round" />
              <path
                d={`M 30 140 A 110 110 0 0 1 ${30 + Math.cos(Math.PI - g.fill * Math.PI) * -110 + 110} ${140 - Math.sin(g.fill * Math.PI) * 110}`}
                fill="none"
                stroke={g.danger ? '#ff0000' : '#FFC107'}
                strokeWidth="20"
                strokeLinecap="round"
              />
              {/* Needle */}
              <line
                x1="140"
                y1="140"
                x2={140 + Math.cos(Math.PI - g.fill * Math.PI) * 90}
                y2={140 - Math.sin(g.fill * Math.PI) * 90}
                stroke="#F5F5F5"
                strokeWidth="3"
                strokeLinecap="round"
              />
              <circle cx="140" cy="140" r="8" fill="#F5F5F5" />
              {/* Danger zone text */}
              <text x="140" y="100" textAnchor="middle" fontFamily="JetBrains Mono" fontSize={g.danger ? '22' : '18'} fill={g.danger ? '#ff0000' : '#9E9E9E'} fontWeight="700">
                {Math.round(g.fill * 100)}%
              </text>
            </svg>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: g.danger ? '#ff0000' : '#F5F5F5', fontWeight: 700, lineHeight: 1.2 }}>{g.label}</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 16, color: '#9E9E9E', letterSpacing: 2 }}>{g.sub}</div>
            {g.danger && (
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: '#ff0000', fontWeight: 700, marginTop: 6, opacity: alertBlink }}>
                ⚠ DANGER
              </div>
            )}
          </div>
        ))}
      </div>

      {/* === CAPITAL CONTROLS WARNING === */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: '50%',
          transform: `translateX(-50%) scale(${warningScale})`,
          transformOrigin: 'center bottom',
          opacity: warningOp,
          whiteSpace: 'nowrap',
        }}
      >
        <div
          style={{
            background: 'rgba(255,0,0,0.12)',
            border: '2px solid #ff0000',
            borderRadius: 16,
            padding: '18px 48px',
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <svg width="40" height="40" viewBox="0 0 40 40">
            <polygon points="20,4 36,32 4,32" fill="none" stroke="#ff0000" strokeWidth="2.5" />
            <line x1="20" y1="14" x2="20" y2="24" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
            <circle cx="20" cy="29" r="2" fill="#ff0000" />
          </svg>
          <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 900, color: '#F5F5F5' }}>
            Capital Controls · विदेश पैसा रोक · निकासी पाबंदी
          </span>
        </div>
      </div>

      {/* === STICK FIGURE — nervous, watching === */}
      <div
        style={{
          position: 'absolute',
          left: 90,
          bottom: 80,
          transform: `scale(${figureScale}) translateX(${nervousShift}px) translateY(${breathY}px)`,
          transformOrigin: 'bottom left',
        }}
      >
        <svg width="100" height="180" viewBox="0 0 100 180">
          <ellipse cx="50" cy="172" rx="38" ry="10" fill="rgba(255,0,0,0.08)" />
          <line x1="50" y1="70" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2="20" y2="110" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="88" x2="80" y2="100" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="32" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="68" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="50" cy="44" r="25" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${headNod}, 50, 44)`} />
          <ellipse cx="38" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="62" cy="44" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="47" y1="44" x2="53" y2="44" stroke="#e9d700" strokeWidth="1.5" />
          <path d="M 40 58 Q 50 54 60 58" fill="none" stroke="#F5F5F5" strokeWidth="1.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 88% 82% at 50% 50%, transparent 38%, rgba(0,0,0,0.82) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_9_BharatKaScenario;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_10_HistoryRepeats',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_10_HistoryRepeats: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera pulls back dramatically
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.1, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Timeline events appearing
  const ev1Op = interpolate(frame, [5, 16], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const ev1Scale = sp(5);
  const ev2Op = interpolate(frame, [15, 26], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const ev2Scale = sp(15);
  const ev3Op = interpolate(frame, [25, 36], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const ev3Scale = sp(25);
  const ev4Op = interpolate(frame, [35, 46], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const ev4Scale = sp(35);

  // Timeline line draw
  const lineW = interpolate(frame, [5, 55], [0, 1700], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // "NOT FICTION" big statement
  const notFictionOp = interpolate(frame, [50, 62], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const notFictionScale = sp(50, { damping: 7, stiffness: 150 });

  // Chapter end pulse
  const chapterPulse = 0.6 + Math.sin(frame * 0.1) * 0.3;

  // Particles — history dots
  const particles = Array.from({ length: 25 }, (_, i) => ({
    x: seededRandom(i * 13) * 1920,
    y: ((seededRandom(i * 9) * 1080 + frame * (0.3 + seededRandom(i * 3) * 0.25)) % 1080),
    r: 1.5 + seededRandom(i * 5) * 2.5,
    op: 0.06 + seededRandom(i * 7) * 0.1,
    color: ['#e9d700', '#ff0000', '#9E9E9E', '#74B2DD'][Math.floor(seededRandom(i * 11) * 4)],
  }));

  // Stick figure — big, authoritative finale
  const figureScale = sp(42);
  const breathY = Math.sin(frame * 0.04) * 2.5;
  const headNod = Math.sin(frame * 0.035) * 2;
  const armGesture = Math.sin(frame * 0.06) * 5;

  const events = [
    { year: '1933', label: 'USA\nसोना जब्त', color: '#e9d700', op: ev1Op, scale: ev1Scale, x: 110 },
    { year: '1980', label: 'Gold\nपीक', color: '#FFC107', op: ev2Op, scale: ev2Scale, x: 510 },
    { year: '2001', label: 'Argentina\nCorralito', color: '#ff0000', op: ev3Op, scale: ev3Scale, x: 960 },
    { year: '2013', label: 'Cyprus\nBailout', color: '#74B2DD', op: ev4Op, scale: ev4Scale, x: 1400 },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `scale(${cameraScale})`,
        transformOrigin: 'center center',
      }}
    >
      {/* === GRADIENT BG — historical panorama === */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(160deg, #0A0500 0%, #1C1C1C 40%, #050A10 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 50% 40% at 50% 50%, rgba(233,215,0,0.05) 0%, transparent 70%)',
        }}
      />

      {/* === SHAPE LAYER 1 — world map grid === */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 9 }, (_, i) => (
            <line key={`h${i}`} x1="0" y1={i * 135} x2="1920" y2={i * 135} stroke="#9E9E9E" strokeWidth="0.5" />
          ))}
          {Array.from({ length: 13 }, (_, i) => (
            <line key={`v${i}`} x1={i * 160} y1="0" x2={i * 160} y2="1080" stroke="#9E9E9E" strokeWidth="0.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYER 2 — chapter end glow === */}
      <AbsoluteFill style={{ opacity: 0.04 * chapterPulse }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          <circle cx="960" cy="540" r="500" fill="none" stroke="#e9d700" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — diagonal history lines === */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 12 }, (_, i) => (
            <line key={i} x1={i * 180} y1="0" x2={i * 180 + 400} y2="1080" stroke="#9E9E9E" strokeWidth="0.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill={p.color} opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === TIMELINE === */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 110,
          right: 110,
        }}
      >
        {/* Timeline line */}
        <svg width={lineW} height="4" style={{ display: 'block' }}>
          <defs>
            <linearGradient id="timelineGrad" x1="0" y1="0" x2="1" y2="0">
              <stop offset="0%" stopColor="#e9d700" />
              <stop offset="50%" stopColor="#ff0000" />
              <stop offset="100%" stopColor="#74B2DD" />
            </linearGradient>
          </defs>
          <rect x="0" y="0" width={lineW} height="4" rx="2" fill="url(#timelineGrad)" />
        </svg>

        {/* Events */}
        <div style={{ position: 'relative', height: 0 }}>
          {events.map((ev, i) => (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: ev.x - 80,
                top: -180,
                width: 200,
                opacity: ev.op,
                transform: `scale(${ev.scale})`,
                transformOrigin: 'bottom center',
                textAlign: 'center',
              }}
            >
              {/* Dot on timeline */}
              <div
                style={{
                  width: 20,
                  height: 20,
                  borderRadius: '50%',
                  background: ev.color,
                  margin: '0 auto 12px',
                  boxShadow: `0 0 20px ${ev.color}66`,
                  position: 'absolute',
                  bottom: -180,
                  left: '50%',
                  transform: 'translateX(-50%)',
                }}
              />
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 900, color: ev.color, marginBottom: 8 }}>{ev.year}</div>
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 22,
                  color: '#F5F5F5',
                  fontWeight: 600,
                  lineHeight: 1.3,
                  whiteSpace: 'pre-line',
                }}
              >
                {ev.label}
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* === "NOT FICTION" STATEMENT === */}
      <div
        style={{
          position: 'absolute',
          bottom: 140,
          left: '50%',
          transform: `translateX(-50%) scale(${notFictionScale})`,
          transformOrigin: 'center bottom',
          opacity: notFictionOp,
          textAlign: 'center',
          whiteSpace: 'nowrap',
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 48,
            fontWeight: 900,
            color: '#9E9E9E',
            letterSpacing: 12,
            marginBottom: 8,
          }}
        >
          THIS IS NOT FICTION
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 72,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
          }}
        >
          ये कल्पना नहीं —{' '}
          <span style={{ color: '#e9d700' }}>इतिहास है</span>
        </div>
      </div>

      {/* === CHAPTER END MARKER === */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          right: 90,
          opacity: notFictionOp * chapterPulse,
        }}
      >
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: '#9E9E9E', letterSpacing: 4, fontWeight: 600 }}>
          CHAPTER 2 END ◈
        </div>
      </div>

      {/* === STICK FIGURE — final authoritative pose === */}
      <div
        style={{
          position: 'absolute',
          left: 90,
          bottom: 60,
          transform: `scale(${figureScale}) translateY(${breathY}px)`,
          transformOrigin: 'bottom left',
        }}
      >
        <svg width="110" height="190" viewBox="0 0 110 190">
          <ellipse cx="55" cy="182" rx="44" ry="11" fill="rgba(233,215,0,0.12)" />
          <circle cx="55" cy="182" r="0" fill="rgba(233,215,0,0.05)" />
          <line x1="55" y1="72" x2="55" y2="138" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="55" y1="92" x2={18 - armGesture} y2={75 - armGesture} stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="55" y1="92" x2={92 + armGesture} y2={88 + armGesture * 0.5} stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="55" y1="138" x2="35" y2="180" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="55" y1="138" x2="75" y2="180" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <circle cx="55" cy="46" r="26" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${headNod}, 55, 46)`} />
          <ellipse cx="42" cy="47" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="68" cy="47" rx="9" ry="6" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="51" y1="47" x2="59" y2="47" stroke="#e9d700" strokeWidth="1.5" />
          <path d="M 44 58 Q 55 65 66 58" fill="none" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse 88% 82% at 50% 50%, transparent 38%, rgba(0,0,0,0.82) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_10_HistoryRepeats;
```

