```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01RukJaiye',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const Scene01RukJaiye: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: slow push-in */
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07], { extrapolateRight: 'clamp' });

  /* Phase A: रुकिए + chips (0-185) | Phase B: थाली question (190+) */
  const aOut = interpolate(frame, [165, 192], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const bIn = interpolate(frame, [195, 218], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const heroS = sp(8, { damping: 9, stiffness: 140 });
  const chip1 = sp(60);
  const chip2 = sp(80);

  const plateL = sp(270, { damping: 10 });
  const plateR = sp(305, { damping: 10 });
  /* Micro-animation: right thali shrinks — the punch moment */
  const shrink = interpolate(frame, [380, 450], [1, 0.58], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });
  const qIn = sp(470, { damping: 11 });
  const pulse = 1 + Math.sin(frame * 0.12) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at ${50 + Math.sin(frame * 0.01) * 12}% ${40 + Math.cos(frame * 0.008) * 10}%, ${C.bg} 0%, ${C.bgDeep} 78%, #8AA80B 100%)`,
      }} />

      {/* Layer 2: Animated transparent shape layers */}
      <div style={{
        position: 'absolute', width: 720, height: 720, borderRadius: '50%',
        border: `3px solid ${C.ink}`, opacity: 0.05,
        left: -180 + Math.sin(frame * 0.006) * 30, top: -220 + Math.cos(frame * 0.005) * 24,
        transform: `scale(${1 + Math.sin(frame * 0.01) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 520, height: 520, borderRadius: '50%',
        backgroundColor: C.lav, opacity: 0.06,
        right: -140 + Math.cos(frame * 0.007) * 34, bottom: -160,
        transform: `scale(${1 + Math.cos(frame * 0.012) * 0.05})`,
      }} />

      {/* Layer 3: Motion texture — drifting grid */}
      <AbsoluteFill style={{
        backgroundImage: `linear-gradient(${C.ink} 1px, transparent 1px), linear-gradient(90deg, ${C.ink} 1px, transparent 1px)`,
        backgroundSize: '110px 110px',
        backgroundPosition: `${frame * 0.22}px ${frame * 0.12}px`,
        opacity: 0.045,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const sx = seededRandom(i + 1) * 1920;
        const sy = seededRandom(i + 40) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: sx + Math.sin(frame * 0.02 + i) * 26,
            top: sy + Math.cos(frame * 0.016 + i * 2) * 20,
            width: 5 + seededRandom(i + 9) * 6, height: 5 + seededRandom(i + 9) * 6,
            borderRadius: '50%', backgroundColor: i % 2 === 0 ? C.ink : C.cream,
            opacity: 0.12 + seededRandom(i + 3) * 0.1,
          }} />
        );
      })}

      {/* Layer 5: Main content with camera */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* ---- PHASE A: रुकिए ---- */}
        <div style={{ position: 'absolute', opacity: aOut, textAlign: 'center', width: '100%' }}>
          <div style={{
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 195, color: C.ink, lineHeight: 1,
            transform: `scale(${heroS}) rotate(${interpolate(heroS, [0, 1], [-6, 0])}deg)`,
            textShadow: `10px 10px 0 rgba(31,35,18,0.12)`,
          }}>
            रुकिए.
          </div>
          <div style={{ display: 'flex', gap: 30, justifyContent: 'center', marginTop: 48 }}>
            <div style={{
              backgroundColor: C.ink, color: C.cream, borderRadius: 16,
              padding: '20px 44px', fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 48, fontWeight: 700,
              transform: `translateY(${interpolate(chip1, [0, 1], [80, 0])}px)`, opacity: chip1,
            }}>टीवी बंद</div>
            <div style={{
              backgroundColor: C.lavDeep, color: C.cream, borderRadius: 16,
              padding: '20px 44px', fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 48, fontWeight: 700,
              transform: `translateY(${interpolate(chip2, [0, 1], [80, 0])}px)`, opacity: chip2,
            }}>फोन नीचे</div>
          </div>
        </div>

        {/* ---- PHASE B: थाली comparison ---- */}
        <div style={{ position: 'absolute', opacity: bIn, width: '100%', textAlign: 'center' }}>
          <div style={{
            display: 'inline-block', backgroundColor: C.ink, color: C.cream,
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800,
            fontSize: 62, padding: '18px 56px', borderRadius: 20, marginBottom: 60,
          }}>
            पिछले <span style={{ color: C.lav }}>6 महीने</span> में आपकी थाली…
          </div>

          <div style={{ display: 'flex', justifyContent: 'center', gap: 220, alignItems: 'flex-end' }}>
            {/* पहले की थाली */}
            <div style={{ transform: `scale(${plateL})`, opacity: plateL }}>
              <svg width="360" height="360" viewBox="0 0 360 360">
                <circle cx="180" cy="180" r="160" fill={C.cream} />
                <circle cx="180" cy="180" r="118" fill="none" stroke={C.ink} strokeWidth="4" strokeDasharray="14 10" />
                <circle cx="140" cy="150" r="34" fill={C.lav} />
                <circle cx="225" cy="160" r="28" fill={C.lavDeep} />
                <circle cx="180" cy="225" r="30" fill={C.bgDeep} />
              </svg>
              <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 800, color: C.ink, marginTop: 14 }}>पहले</div>
            </div>
            {/* अब की थाली — shrinking */}
            <div style={{ transform: `scale(${plateR * shrink})`, opacity: plateR, transformOrigin: 'center bottom' }}>
              <svg width="360" height="360" viewBox="0 0 360 360">
                <circle cx="180" cy="180" r="160" fill={C.cream} />
                <circle cx="180" cy="180" r="118" fill="none" stroke={C.ink} strokeWidth="4" strokeDasharray="14 10" />
                <circle cx="160" cy="170" r="24" fill={C.lav} />
                <circle cx="215" cy="200" r="18" fill={C.bgDeep} />
              </svg>
              <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 800, color: C.ink, marginTop: 14 }}>अब</div>
            </div>
          </div>

          <div style={{
            marginTop: 54, fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 100, color: C.ink,
            transform: `scale(${qIn * pulse})`, opacity: qIn,
          }}>
            छोटी हुई <span style={{ color: C.lavDeep }}>या</span> बड़ी?
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(31,35,18,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01RukJaiye;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02PetrolMath',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const Scene02PetrolMath: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: gentle pan left→right */
  const camX = interpolate(frame, [0, durationInFrames], [30, -30]);
  const camScale = 1.04;

  const titleIn = sp(6);
  const noteIn = sp(50, { damping: 9 });
  const noteFloat = Math.sin(frame * 0.05) * 8;

  /* Bars grow */
  const barA = sp(140, { damping: 13 });
  const barB = sp(200, { damping: 13 });
  /* Counters */
  const litA = interpolate(frame, [140, 220], [0, 10.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const litB = interpolate(frame, [200, 300], [0, 9.2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  /* Drop highlight */
  const dropIn = sp(340, { damping: 8 });
  const qIn = sp(430);
  const blink = 0.75 + Math.sin(frame * 0.18) * 0.25;

  const maxH = 460;

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${125 + Math.sin(frame * 0.008) * 8}deg, ${C.bg} 0%, ${C.bgDeep} 60%, #8FAE0C 100%)`,
      }} />

      {/* Layer 2: Animated shape layers — rectangles */}
      <div style={{
        position: 'absolute', width: 560, height: 560, borderRadius: 60,
        border: `3px solid ${C.ink}`, opacity: 0.05,
        left: -160, top: 120 + Math.sin(frame * 0.007) * 26,
        transform: `rotate(${12 + Math.sin(frame * 0.006) * 4}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 420, height: 420, borderRadius: 48,
        backgroundColor: C.lav, opacity: 0.06,
        right: -120, top: -140 + Math.cos(frame * 0.008) * 22,
        transform: `rotate(${-18 + Math.cos(frame * 0.007) * 5}deg)`,
      }} />

      {/* Layer 3: Motion texture — diagonal lines */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(45deg, ${C.ink} 0px, ${C.ink} 2px, transparent 2px, transparent 90px)`,
        backgroundPosition: `${frame * 0.35}px 0px`,
        opacity: 0.04,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 9 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 11) * 1920 + Math.sin(frame * 0.018 + i) * 24,
          top: seededRandom(i + 55) * 1080 + Math.cos(frame * 0.014 + i * 1.7) * 22,
          width: 6, height: 6, borderRadius: '50%',
          backgroundColor: i % 3 === 0 ? C.lavDeep : C.cream,
          opacity: 0.15,
        }} />
      ))}

      {/* Layer 5: Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px) scale(${camScale})` }}>

        {/* Headline */}
        <div style={{
          position: 'absolute', top: 80, width: '100%', textAlign: 'center',
          opacity: titleIn, transform: `translateY(${interpolate(titleIn, [0, 1], [-60, 0])}px)`,
        }}>
          <span style={{
            backgroundColor: C.ink, color: C.cream, borderRadius: 18,
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 76, padding: '14px 60px',
          }}>
            पेट्रोल पंप का <span style={{ color: C.lav }}>हिसाब</span>
          </span>
        </div>

        {/* ₹1000 note card — floating */}
        <div style={{
          position: 'absolute', left: 180, top: 380 + noteFloat,
          transform: `scale(${noteIn}) rotate(${interpolate(noteIn, [0, 1], [-14, -6])}deg)`,
          opacity: noteIn,
          width: 420, height: 200, borderRadius: 20,
          backgroundColor: C.ink, border: `4px solid ${C.lav}`,
          display: 'flex', justifyContent: 'center', alignItems: 'center',
          boxShadow: '14px 14px 0 rgba(31,35,18,0.15)',
        }}>
          <span style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 92, fontWeight: 800, color: C.cream }}>₹1000</span>
        </div>
        <div style={{
          position: 'absolute', left: 235, top: 610,
          fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 700,
          color: C.ink, opacity: noteIn,
        }}>वही एक नोट…</div>

        {/* Bars: पहले vs अब */}
        <div style={{ position: 'absolute', right: 260, top: 250, display: 'flex', gap: 150, alignItems: 'flex-end', height: maxH + 120 }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 64, fontWeight: 800, color: C.ink, marginBottom: 12, opacity: barA }}>
              {litA.toFixed(1)}L
            </div>
            <div style={{
              width: 190, height: maxH * barA, backgroundColor: C.cream,
              borderRadius: '18px 18px 0 0', border: `4px solid ${C.ink}`, borderBottom: 'none',
              transformOrigin: 'bottom',
            }} />
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 800, color: C.ink, marginTop: 14 }}>पहले</div>
          </div>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 64, fontWeight: 800, color: C.lavDeep, marginBottom: 12, opacity: barB }}>
              {litB.toFixed(1)}L
            </div>
            <div style={{
              width: 190, height: maxH * 0.78 * barB, backgroundColor: C.lav,
              borderRadius: '18px 18px 0 0', border: `4px solid ${C.ink}`, borderBottom: 'none',
              transformOrigin: 'bottom',
            }} />
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 800, color: C.ink, marginTop: 14 }}>अब</div>
          </div>
        </div>

        {/* Drop badge */}
        <div style={{
          position: 'absolute', right: 160, top: 300,
          transform: `scale(${dropIn}) rotate(8deg)`, opacity: dropIn,
          backgroundColor: C.lavDeep, color: C.cream, borderRadius: 999,
          fontFamily: "'JetBrains Mono', monospace", fontSize: 46, fontWeight: 800,
          padding: '16px 34px',
        }}>▼ कम लीटर</div>

        {/* Bottom question */}
        <div style={{
          position: 'absolute', bottom: 90, width: '100%', textAlign: 'center',
          opacity: qIn, transform: `translateY(${interpolate(qIn, [0, 1], [70, 0])}px)`,
        }}>
          <span style={{
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 84, color: C.ink, opacity: blink,
          }}>
            उतने ही पैसे… <span style={{ color: C.lavDeep }}>कितने आते हैं?</span>
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(31,35,18,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02PetrolMath;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03KharchaStack',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const ROWS = [
  { label: 'घर की EMI', amt: '₹32,000', w: 640 },
  { label: 'स्कूल फीस', amt: '₹18,000', w: 460 },
  { label: 'सब्जी-राशन', amt: '₹12,000', w: 360 },
  { label: 'दवाइयों का परचा', amt: '₹6,000', w: 250 },
];

const Scene03KharchaStack: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: slow tilt-zoom */
  const camScale = interpolate(frame, [0, durationInFrames], [1.08, 1]);
  const camRot = interpolate(frame, [0, durationInFrames], [0.6, -0.4]);

  const titleIn = sp(6);
  /* Wallet drain counter */
  const drain = interpolate(frame, [330, 480], [25000, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.ease),
  });
  const qIn = sp(490, { damping: 9 });
  /* Screen shake on ₹0 impact */
  const shakeT = frame - 485;
  const shake = shakeT > 0 && shakeT < 18 ? Math.sin(shakeT * 1.6) * (18 - shakeT) * 0.7 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${20 + Math.sin(frame * 0.009) * 8}% 80%, #A9C90F 0%, ${C.bg} 45%, ${C.bgDeep} 100%)`,
      }} />

      {/* Layer 2: Animated shape layers — triangle + circle */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <polygon
          points="1560,80 1860,620 1260,620"
          fill="none" stroke={C.ink} strokeWidth="3"
          transform={`rotate(${Math.sin(frame * 0.007) * 6} 1560 350)`}
        />
        <circle cx={220 + Math.sin(frame * 0.006) * 30} cy="880" r="260" fill={C.lav} opacity="0.9" />
      </svg>

      {/* Layer 3: Motion texture — dots */}
      <AbsoluteFill style={{
        backgroundImage: `radial-gradient(${C.ink} 2px, transparent 2px)`,
        backgroundSize: '70px 70px',
        backgroundPosition: `${frame * 0.18}px ${-frame * 0.14}px`,
        opacity: 0.05,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 21) * 1920 + Math.sin(frame * 0.02 + i * 2) * 20,
          top: seededRandom(i + 66) * 1080 + Math.cos(frame * 0.017 + i) * 24,
          width: 7, height: 7, borderRadius: 2,
          backgroundColor: i % 2 === 0 ? C.cream : C.lavDeep,
          opacity: 0.14, transform: `rotate(${frame * 0.6 + i * 30}deg)`,
        }} />
      ))}

      {/* Layer 5: Main content */}
      <AbsoluteFill style={{ transform: `translateX(${shake}px) scale(${camScale}) rotate(${camRot}deg)` }}>

        <div style={{
          position: 'absolute', top: 70, width: '100%', textAlign: 'center',
          opacity: titleIn, transform: `translateY(${interpolate(titleIn, [0, 1], [-50, 0])}px)`,
        }}>
          <span style={{
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 80, color: C.ink,
          }}>
            महीने का <span style={{ backgroundColor: C.ink, color: C.lav, padding: '0 28px', borderRadius: 14 }}>बोझ</span>
          </span>
        </div>

        {/* Expense rows — staggered slide-in */}
        <div style={{ position: 'absolute', left: 200, top: 230, display: 'flex', flexDirection: 'column', gap: 26 }}>
          {ROWS.map((r, i) => {
            const s = sp(60 + i * 22, { damping: 11 });
            return (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 30,
                transform: `translateX(${interpolate(s, [0, 1], [-500, 0])}px)`, opacity: s,
              }}>
                <div style={{
                  width: 430, backgroundColor: C.ink, color: C.cream, borderRadius: 16,
                  fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 800,
                  padding: '18px 32px',
                }}>{r.label}</div>
                <div style={{
                  width: r.w * s, height: 74, backgroundColor: i % 2 === 0 ? C.lav : C.cream,
                  borderRadius: 14, border: `4px solid ${C.ink}`,
                  display: 'flex', alignItems: 'center', justifyContent: 'flex-end', paddingRight: 24,
                  fontFamily: "'JetBrains Mono', monospace", fontSize: 40, fontWeight: 800, color: C.ink,
                  overflow: 'hidden', whiteSpace: 'nowrap',
                }}>{r.amt}</div>
              </div>
            );
          })}
        </div>

        {/* Wallet drain */}
        <div style={{
          position: 'absolute', right: 170, top: 300, width: 460, textAlign: 'center',
          opacity: interpolate(frame, [300, 325], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            backgroundColor: C.cream, border: `5px solid ${C.ink}`, borderRadius: 26,
            padding: '36px 20px', boxShadow: '12px 12px 0 rgba(31,35,18,0.15)',
          }}>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 800, color: C.ink }}>
              महीने के आखिर में
            </div>
            <div style={{
              fontFamily: "'JetBrains Mono', monospace", fontSize: 110, fontWeight: 800,
              color: drain < 2000 ? C.lavDeep : C.ink, marginTop: 10,
              transform: `scale(${drain < 100 ? 1 + Math.sin(frame * 0.3) * 0.05 : 1})`,
            }}>
              ₹{Math.floor(drain).toLocaleString('en-IN')}
            </div>
          </div>
        </div>

        {/* Bottom question */}
        <div style={{
          position: 'absolute', bottom: 80, width: '100%', textAlign: 'center',
          transform: `scale(${qIn})`, opacity: qIn,
        }}>
          <span style={{
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 88, color: C.ink,
          }}>
            जेब में कुछ <span style={{ color: C.lavDeep, textDecoration: 'underline', textDecorationThickness: 8 }}>बचता भी है?</span>
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(31,35,18,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03KharchaStack;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04PehliTareekh',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const Scene04PehliTareekh: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: slow orbit-like drift */
  const camX = Math.sin(frame * 0.01) * 18;
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  const calIn = sp(15, { damping: 9 });
  /* Pulsing fear ring around "1" */
  const ringPulse = (frame % 45) / 45;
  const fearIn = sp(150);
  /* उधार stamp slams in */
  const stampIn = sp(300, { damping: 7, stiffness: 200 });
  const shakeT = frame - 300;
  const shake = shakeT > 0 && shakeT < 16 ? Math.sin(shakeT * 1.8) * (16 - shakeT) * 0.9 : 0;
  const qIn = sp(420);
  const calTilt = Math.sin(frame * 0.04) * 1.5;

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${180 + Math.sin(frame * 0.006) * 10}deg, ${C.bg} 0%, #A6C60F 50%, #8CAA0B 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 640, height: 640, borderRadius: '50%',
        border: `40px solid ${C.cream}`, opacity: 0.06,
        right: -200 + Math.sin(frame * 0.005) * 20, top: -220,
      }} />
      <div style={{
        position: 'absolute', width: 380, height: 380, borderRadius: 40,
        backgroundColor: C.ink, opacity: 0.04,
        left: -100, bottom: -120, transform: `rotate(${20 + Math.cos(frame * 0.008) * 6}deg)`,
      }} />

      {/* Layer 3: Motion texture — horizontal lines */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(0deg, ${C.ink} 0px, ${C.ink} 1.5px, transparent 1.5px, transparent 80px)`,
        backgroundPosition: `0px ${frame * 0.3}px`,
        opacity: 0.045,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 9 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 31) * 1920 + Math.sin(frame * 0.015 + i) * 28,
          top: seededRandom(i + 77) * 1080 + Math.cos(frame * 0.019 + i * 1.4) * 18,
          width: 5 + seededRandom(i) * 5, height: 5 + seededRandom(i) * 5,
          borderRadius: '50%', backgroundColor: i % 2 === 0 ? C.lavDeep : C.cream,
          opacity: 0.14,
        }} />
      ))}

      {/* Layer 5: Main content */}
      <AbsoluteFill style={{
        transform: `translateX(${camX + shake}px) scale(${camScale})`,
        justifyContent: 'center', alignItems: 'center',
      }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 130 }}>

          {/* Calendar card */}
          <div style={{
            position: 'relative',
            transform: `scale(${calIn}) rotate(${calTilt}deg)`, opacity: calIn,
          }}>
            {/* Pulsing rings */}
            <div style={{
              position: 'absolute', left: '50%', top: '50%',
              width: 560 + ringPulse * 220, height: 560 + ringPulse * 220,
              transform: 'translate(-50%, -50%)', borderRadius: 44,
              border: `4px solid ${C.lavDeep}`, opacity: (1 - ringPulse) * 0.5,
            }} />
            <div style={{
              width: 460, borderRadius: 36, overflow: 'hidden',
              border: `6px solid ${C.ink}`, boxShadow: '16px 16px 0 rgba(31,35,18,0.15)',
            }}>
              <div style={{
                backgroundColor: C.lavDeep, color: C.cream, textAlign: 'center',
                fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 800,
                padding: '18px 0',
              }}>पहली तारीख</div>
              <div style={{
                backgroundColor: C.cream, textAlign: 'center', padding: '30px 0 40px 0',
              }}>
                <span style={{
                  fontFamily: "'JetBrains Mono', monospace", fontSize: 230, fontWeight: 800,
                  color: C.ink, lineHeight: 1,
                  display: 'inline-block',
                  transform: `scale(${1 + Math.sin(frame * 0.15) * 0.03})`,
                }}>1</span>
              </div>
            </div>
            {/* उधार stamp */}
            <div style={{
              position: 'absolute', right: -70, bottom: -30,
              transform: `scale(${stampIn}) rotate(-14deg)`, opacity: stampIn,
              border: `7px solid ${C.ink}`, color: C.ink, borderRadius: 18,
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 74, padding: '8px 34px', backgroundColor: C.lav,
            }}>उधार?</div>
          </div>

          {/* Right typographic block */}
          <div style={{ width: 760 }}>
            <div style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 96, color: C.ink, lineHeight: 1.15,
              opacity: fearIn, transform: `translateY(${interpolate(fearIn, [0, 1], [60, 0])}px)`,
            }}>
              वही <span style={{ color: C.lavDeep }}>डर</span>,<br />हर महीने<br />दुबारा?
            </div>
            <div style={{
              marginTop: 40, display: 'inline-block',
              backgroundColor: C.ink, color: C.cream, borderRadius: 16,
              fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 48, fontWeight: 700,
              padding: '18px 40px',
              opacity: qIn, transform: `translateX(${interpolate(qIn, [0, 1], [90, 0])}px)`,
            }}>
              "इस बार भी <span style={{ color: C.lav }}>उधार लेना पड़ेगा?</span>"
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(31,35,18,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04PehliTareekh;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05DhyanSe',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const LINE1 = ['अगर', 'एक', 'भी', 'सवाल'];
const LINE2 = ['अंदर', 'कहीं', 'चुभा', 'है…'];

const Scene05DhyanSe: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: dramatic slow zoom-in for intimacy */
  const camScale = interpolate(frame, [0, durationInFrames], [1.12, 1], { easing: Easing.out(Easing.ease) });

  /* Phase A: word-by-word sting (0-280) | Phase B: focus ring (280+) */
  const aOut = interpolate(frame, [255, 285], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const bIn = interpolate(frame, [290, 315], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  /* Needle prick flash on "चुभा" */
  const prickT = frame - 150;
  const prick = prickT > 0 && prickT < 12 ? (12 - prickT) / 12 : 0;

  /* Focus arc progress */
  const arcLen = 2 * Math.PI * 210;
  const arcProg = interpolate(frame, [320, 560], [0, 0.85], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const sitIn = sp(340, { damping: 10 });

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% ${52 + Math.sin(frame * 0.01) * 6}%, ${C.bg} 0%, ${C.bgDeep} 62%, #86A30A 100%)`,
      }} />
      {/* Prick flash */}
      <AbsoluteFill style={{ backgroundColor: C.lav, opacity: prick * 0.35 }} />

      {/* Layer 2: Animated shape layers — concentric circles */}
      {[900, 660, 430].map((d, i) => (
        <div key={i} style={{
          position: 'absolute', left: '50%', top: '50%',
          width: d + Math.sin(frame * 0.012 + i) * 16, height: d + Math.sin(frame * 0.012 + i) * 16,
          transform: 'translate(-50%, -50%)', borderRadius: '50%',
          border: `2.5px solid ${C.ink}`, opacity: 0.05,
        }} />
      ))}

      {/* Layer 3: Motion texture — fine cross grid */}
      <AbsoluteFill style={{
        backgroundImage: `linear-gradient(${C.ink} 1px, transparent 1px), linear-gradient(90deg, ${C.ink} 1px, transparent 1px)`,
        backgroundSize: '160px 160px',
        backgroundPosition: `${-frame * 0.15}px ${frame * 0.1}px`,
        opacity: 0.035,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 41) * 1920 + Math.sin(frame * 0.017 + i * 2) * 22,
          top: seededRandom(i + 88) * 1080 + Math.cos(frame * 0.013 + i) * 26,
          width: 6, height: 6, borderRadius: '50%',
          backgroundColor: C.cream, opacity: 0.16,
        }} />
      ))}

      {/* Layer 5: Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* ---- PHASE A: word-stagger sting ---- */}
        <div style={{ position: 'absolute', textAlign: 'center', opacity: aOut }}>
          <div>
            {LINE1.map((w, i) => {
              const s = sp(20 + i * 18, { damping: 10 });
              return (
                <span key={i} style={{
                  display: 'inline-block', margin: '0 18px',
                  fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                  fontSize: 110, color: C.ink,
                  opacity: s, transform: `translateY(${interpolate(s, [0, 1], [70, 0])}px)`,
                }}>{w}</span>
              );
            })}
          </div>
          <div style={{ marginTop: 20 }}>
            {LINE2.map((w, i) => {
              const s = sp(110 + i * 18, { damping: 10 });
              const isChubha = w === 'चुभा';
              return (
                <span key={i} style={{
                  display: 'inline-block', margin: '0 18px',
                  fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                  fontSize: 110,
                  color: isChubha ? C.lavDeep : C.ink,
                  backgroundColor: isChubha ? C.ink : 'transparent',
                  padding: isChubha ? '0 30px' : 0, borderRadius: isChubha ? 18 : 0,
                  opacity: s, transform: `translateY(${interpolate(s, [0, 1], [70, 0])}px) scale(${isChubha ? 1 + prick * 0.15 : 1})`,
                }}>{isChubha ? <span style={{ color: C.lav }}>{w}</span> : w}</span>
              );
            })}
          </div>
        </div>

        {/* ---- PHASE B: focus ring commitment ---- */}
        <div style={{ position: 'absolute', opacity: bIn, textAlign: 'center' }}>
          <svg width="520" height="520" viewBox="0 0 520 520" style={{ display: 'block', margin: '0 auto' }}>
            <circle cx="260" cy="260" r="210" fill="none" stroke={C.cream} strokeWidth="10" opacity="0.4" />
            <circle
              cx="260" cy="260" r="210" fill="none" stroke={C.ink} strokeWidth="14"
              strokeLinecap="round"
              strokeDasharray={arcLen}
              strokeDashoffset={arcLen * (1 - arcProg)}
              transform="rotate(-90 260 260)"
            />
            <text x="260" y="235" textAnchor="middle"
              fontFamily="'Noto Sans Devanagari', sans-serif" fontSize="52" fontWeight="800" fill={C.ink}>
              अगले कुछ
            </text>
            <text x="260" y="310" textAnchor="middle"
              fontFamily="'Yantra One', 'Noto Sans Devanagari', sans-serif" fontSize="76" fill={C.lavDeep}>
              मिनट
            </text>
          </svg>
          <div style={{
            marginTop: 36, display: 'inline-block',
            backgroundColor: C.ink, color: C.cream, borderRadius: 18,
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 54, fontWeight: 800,
            padding: '20px 52px',
            transform: `scale(${sitIn})`, opacity: sitIn,
          }}>
            पूरा ध्यान — <span style={{ color: C.lav }}>मेरे साथ बैठिए</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(31,35,18,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05DhyanSe;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06ToofanBehindScreen',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const Scene06ToofanBehindScreen: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: slow pan up */
  const camY = interpolate(frame, [0, durationInFrames], [20, -20]);
  const camScale = 1.05;

  const screenIn = sp(20, { damping: 10 });
  /* Chart line draw */
  const lineLen = 1400;
  const lineDraw = interpolate(frame, [60, 220], [lineLen, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  /* Screen slides left to reveal storm behind */
  const slide = interpolate(frame, [280, 360], [0, -420], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });
  const tilt = interpolate(frame, [280, 360], [0, -8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const stormIn = sp(340, { damping: 9 });
  const tagIn = sp(450);
  const glint = interpolate((frame % 90), [0, 90], [-300, 900]);

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${200 + Math.sin(frame * 0.007) * 10}deg, ${C.bg} 0%, ${C.bgDeep} 55%, #82A009 100%)`,
      }} />

      {/* Layer 2: Animated shape layers — storm swirls */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <circle cx={1500 + Math.sin(frame * 0.008) * 26} cy="260" r="300" fill="none" stroke={C.ink} strokeWidth="3" strokeDasharray="40 26"
          transform={`rotate(${frame * 0.25} 1500 260)`} />
        <circle cx="330" cy={820 + Math.cos(frame * 0.009) * 22} r="220" fill={C.lav} />
      </svg>

      {/* Layer 3: Motion texture — grid */}
      <AbsoluteFill style={{
        backgroundImage: `linear-gradient(${C.ink} 1px, transparent 1px), linear-gradient(90deg, ${C.ink} 1px, transparent 1px)`,
        backgroundSize: '95px 95px',
        backgroundPosition: `${-frame * 0.2}px ${frame * 0.16}px`,
        opacity: 0.04,
      }} />

      {/* Layer 4: Floating particles — rain-hint diagonal */}
      {Array.from({ length: 10 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 51) * 1920 + Math.sin(frame * 0.02 + i) * 16,
          top: ((seededRandom(i + 99) * 1080) + frame * (1.2 + seededRandom(i) * 1.5)) % 1080,
          width: 3, height: 16, borderRadius: 3,
          backgroundColor: C.ink, opacity: 0.1, transform: 'rotate(18deg)',
        }} />
      ))}

      {/* Layer 5: Main content */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px) scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Storm text BEHIND screen */}
        <div style={{
          position: 'absolute', right: 130, textAlign: 'right',
          opacity: stormIn, transform: `translateX(${interpolate(stormIn, [0, 1], [120, 0])}px)`,
        }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 54, fontWeight: 800, color: C.lavDeep, letterSpacing: 8 }}>
            2026
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 150, color: C.ink, lineHeight: 1 }}>
            तूफान
          </div>
          <div style={{
            display: 'inline-block', marginTop: 24,
            backgroundColor: C.ink, color: C.cream, borderRadius: 14,
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 42, fontWeight: 700,
            padding: '14px 30px',
          }}>
            स्क्रीन के <span style={{ color: C.lav }}>पीछे छिपा</span>
          </div>
        </div>

        {/* Shiny market screen card — slides away */}
        <div style={{
          position: 'absolute',
          transform: `translateX(${slide}px) rotate(${tilt}deg) scale(${screenIn})`,
          opacity: screenIn,
          width: 1060, height: 620, borderRadius: 34,
          backgroundColor: C.ink, border: `6px solid ${C.cream}`,
          boxShadow: '20px 20px 0 rgba(31,35,18,0.18)', overflow: 'hidden',
        }}>
          {/* Glossy glint sweep */}
          <div style={{
            position: 'absolute', top: 0, bottom: 0, width: 180,
            left: glint, transform: 'skewX(-20deg)',
            background: 'linear-gradient(90deg, transparent, rgba(235,237,227,0.14), transparent)',
          }} />
          <div style={{
            padding: '28px 40px', fontFamily: "'JetBrains Mono', monospace",
            fontSize: 34, fontWeight: 700, color: C.lav, display: 'flex', justifyContent: 'space-between',
          }}>
            <span>SHARE BAZAAR • LIVE</span>
            <span style={{ color: C.cream }}>▲ {(interpolate(frame, [60, 240], [72400, 74980], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })).toFixed(0)}</span>
          </div>
          <svg width="1060" height="480" viewBox="0 0 1060 480">
            {/* Grid on screen */}
            {[1, 2, 3].map((g) => (
              <line key={g} x1="40" x2="1020" y1={g * 120} y2={g * 120} stroke={C.cream} strokeWidth="1" opacity="0.12" />
            ))}
            {/* Rising line */}
            <polyline
              points="60,400 200,360 320,380 460,300 580,320 700,230 820,250 940,140 1010,120"
              fill="none" stroke={C.lav} strokeWidth="10" strokeLinecap="round" strokeLinejoin="round"
              strokeDasharray={lineLen} strokeDashoffset={lineDraw}
            />
            <circle
              cx="1010" cy="120" r={10 + Math.sin(frame * 0.2) * 4}
              fill={C.lav} opacity={frame > 210 ? 1 : 0}
            />
          </svg>
        </div>

        {/* Bottom tag */}
        <div style={{
          position: 'absolute', bottom: 70, width: '100%', textAlign: 'center',
          opacity: tagIn, transform: `translateY(${interpolate(tagIn, [0, 1], [60, 0])}px)`,
        }}>
          <span style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 50, fontWeight: 800,
            color: C.ink, backgroundColor: C.cream, padding: '16px 44px', borderRadius: 16,
            border: `4px solid ${C.ink}`,
          }}>
            चमकती स्क्रीन ≠ पूरी सच्चाई
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(31,35,18,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06ToofanBehindScreen;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07KunalIntro',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const PILLARS = [
  { label: 'नंबर', icon: 'bars' },
  { label: 'नीति', icon: 'doc' },
  { label: 'नजरिया', icon: 'eye' },
];

const Scene07KunalIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: gentle zoom-out reveal */
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1], { easing: Easing.out(Easing.ease) });

  const nameIn = sp(10, { damping: 9 });
  const brandIn = sp(70, { damping: 10 });
  const tableIn = sp(280, { damping: 12 });
  const truthIn = sp(430, { damping: 9 });
  const underline = interpolate(frame, [90, 150], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const Icon: React.FC<{ type: string }> = ({ type }) => {
    if (type === 'bars') {
      return (
        <svg width="80" height="80" viewBox="0 0 80 80">
          <rect x="10" y={44 - Math.sin(frame * 0.08) * 4} width="14" height={30 + Math.sin(frame * 0.08) * 4} rx="4" fill={C.lav} />
          <rect x="33" y={30 - Math.cos(frame * 0.07) * 4} width="14" height={44 + Math.cos(frame * 0.07) * 4} rx="4" fill={C.cream} />
          <rect x="56" y={16 - Math.sin(frame * 0.09) * 4} width="14" height={58 + Math.sin(frame * 0.09) * 4} rx="4" fill={C.lavDeep} />
        </svg>
      );
    }
    if (type === 'doc') {
      return (
        <svg width="80" height="80" viewBox="0 0 80 80">
          <rect x="16" y="8" width="48" height="64" rx="8" fill={C.cream} />
          <rect x="26" y="24" width={28 + Math.sin(frame * 0.1) * 3} height="6" rx="3" fill={C.lavDeep} />
          <rect x="26" y="38" width="28" height="6" rx="3" fill={C.ink} opacity="0.5" />
          <rect x="26" y="52" width="20" height="6" rx="3" fill={C.ink} opacity="0.5" />
        </svg>
      );
    }
    return (
      <svg width="80" height="80" viewBox="0 0 80 80">
        <ellipse cx="40" cy="40" rx="32" ry="20" fill="none" stroke={C.cream} strokeWidth="5" />
        <circle cx={40 + Math.sin(frame * 0.06) * 6} cy="40" r="10" fill={C.lav} />
      </svg>
    );
  };

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${28 + Math.sin(frame * 0.008) * 10}% 30%, #C4E518 0%, ${C.bg} 40%, ${C.bgDeep} 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        border: `2.5px solid ${C.ink}`, opacity: 0.05,
        left: '50%', top: '46%',
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.01) * 0.05}) rotate(${frame * 0.1}deg)`,
        borderStyle: 'dashed',
      }} />
      <div style={{
        position: 'absolute', width: 460, height: 460, borderRadius: 60,
        backgroundColor: C.lav, opacity: 0.05,
        right: -140, bottom: -160, transform: `rotate(${25 + Math.sin(frame * 0.009) * 5}deg)`,
      }} />

      {/* Layer 3: Motion texture — vertical bars */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(90deg, ${C.ink} 0px, ${C.ink} 2px, transparent 2px, transparent 130px)`,
        backgroundPosition: `${frame * 0.25}px 0px`,
        opacity: 0.035,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 61) * 1920 + Math.sin(frame * 0.016 + i * 1.3) * 26,
          top: seededRandom(i + 12) * 1080 + Math.cos(frame * 0.02 + i) * 20,
          width: 6, height: 6, borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? C.lavDeep : C.cream, opacity: 0.15,
        }} />
      ))}

      {/* Layer 5: Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        <div style={{ textAlign: 'center', width: '100%' }}>

          {/* Name */}
          <div style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 60, fontWeight: 800,
            color: C.ink, opacity: nameIn, transform: `translateY(${interpolate(nameIn, [0, 1], [-50, 0])}px)`,
          }}>
            मैं <span style={{ color: C.lavDeep }}>कुनाल</span> हूँ
          </div>

          {/* Brand logotype */}
          <div style={{
            marginTop: 8, fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900,
            fontSize: 130, color: C.ink, letterSpacing: -2,
            transform: `scale(${brandIn})`, opacity: brandIn,
          }}>
            ProBuffet <span style={{ color: C.cream, textShadow: `5px 5px 0 ${C.ink}` }}>HQ</span>
          </div>
          <div style={{ height: 10, width: `${underline * 6}px`, maxWidth: 620, backgroundColor: C.lavDeep, borderRadius: 6, margin: '6px auto 0 auto' }} />

          {/* मेज (table) with 3 pillars */}
          <div style={{
            marginTop: 60, display: 'flex', justifyContent: 'center', gap: 40, alignItems: 'flex-end',
          }}>
            {PILLARS.map((p, i) => {
              const s = sp(150 + i * 25, { damping: 10 });
              return (
                <div key={i} style={{
                  width: 300, backgroundColor: C.ink, borderRadius: 24,
                  padding: '30px 0 24px 0', textAlign: 'center',
                  transform: `translateY(${interpolate(s, [0, 1], [140, 0])}px) rotate(${(i - 1) * 1.5}deg)`,
                  opacity: s, boxShadow: '10px 12px 0 rgba(31,35,18,0.18)',
                }}>
                  <div style={{ display: 'flex', justifyContent: 'center' }}><Icon type={p.icon} /></div>
                  <div style={{
                    fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 50, fontWeight: 800,
                    color: C.cream, marginTop: 10,
                  }}>{p.label}</div>
                </div>
              );
            })}
          </div>
          {/* Table plank */}
          <div style={{
            margin: '0 auto', width: 1060 * tableIn, height: 26, backgroundColor: C.cream,
            border: `4px solid ${C.ink}`, borderRadius: 13, opacity: tableIn,
          }} />

          {/* Truth line */}
          <div style={{
            marginTop: 40, display: 'inline-block',
            transform: `scale(${truthIn})`, opacity: truthIn,
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 66, color: C.ink,
          }}>
            एक ही मेज पर — <span style={{ backgroundColor: C.lavDeep, color: C.cream, padding: '0 26px', borderRadius: 14 }}>सच वाली बात</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(31,35,18,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07KunalIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08EconomicBomb',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const Scene08EconomicBomb: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: tension zoom */
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.1], { easing: Easing.in(Easing.ease) });

  const whisperIn = sp(15);
  const bombIn = sp(90, { damping: 8, stiffness: 160 });
  /* Ticking pulse — heartbeat of danger */
  const tick = 1 + Math.max(0, Math.sin(frame * 0.42)) * 0.03;
  /* Fuse burn: 90 → 480 */
  const fuseP = interpolate(frame, [120, 500], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const fuseLen = 420;
  /* Spark position along fuse curve */
  const sparkX = interpolate(fuseP, [0, 1], [1310, 1035]);
  const sparkY = 470 - Math.sin(fuseP * Math.PI) * 130 - (1 - fuseP) * 120;
  const titleIn = sp(220, { damping: 9 });
  const shakeT = frame - 220;
  const shake = shakeT > 0 && shakeT < 15 ? Math.sin(shakeT * 1.7) * (15 - shakeT) * 0.8 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 60% ${44 + Math.sin(frame * 0.01) * 5}%, ${C.bg} 0%, ${C.bgDeep} 55%, #7E9B08 100%)`,
      }} />

      {/* Layer 2: Animated shape layers — hazard rotation */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <g transform={`rotate(${frame * 0.15} 960 540)`}>
          <circle cx="960" cy="540" r="470" fill="none" stroke={C.ink} strokeWidth="3" strokeDasharray="70 40" />
        </g>
        <rect x="-80" y={720 + Math.sin(frame * 0.007) * 20} width="500" height="500" rx="70" fill={C.lav} opacity="0.9" />
      </svg>

      {/* Layer 3: Motion texture — radial dashes via repeating gradient */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(135deg, ${C.ink} 0px, ${C.ink} 2px, transparent 2px, transparent 70px)`,
        backgroundPosition: `${-frame * 0.3}px 0px`,
        opacity: 0.04,
      }} />

      {/* Layer 4: Floating particles — ember sparks */}
      {Array.from({ length: 10 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 71) * 1920 + Math.sin(frame * 0.03 + i) * 30,
          top: (seededRandom(i + 23) * 1080 - frame * (0.8 + seededRandom(i) * 1.4) + 1080 * 3) % 1080,
          width: 5, height: 5, borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? C.lavDeep : C.cream,
          opacity: 0.2,
        }} />
      ))}

      {/* Layer 5: Main content */}
      <AbsoluteFill style={{ transform: `translateX(${shake}px) scale(${camScale})` }}>

        {/* Whisper chip */}
        <div style={{
          position: 'absolute', top: 110, width: '100%', textAlign: 'center',
          opacity: whisperIn, transform: `translateY(${interpolate(whisperIn, [0, 1], [-40, 0])}px)`,
        }}>
          <span style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 700,
            color: C.cream, backgroundColor: C.ink, padding: '14px 40px', borderRadius: 999,
            letterSpacing: 2,
          }}>
            अर्थशास्त्री… <span style={{ color: C.lav, fontStyle: 'italic' }}>दबी जुबान में</span> कहते हैं
          </span>
        </div>

        {/* Bomb assembly */}
        <div style={{
          position: 'absolute', left: 620, top: 330,
          transform: `scale(${bombIn * tick})`, opacity: bombIn, transformOrigin: '340px 340px',
        }}>
          <svg width="720" height="640" viewBox="0 0 720 640">
            {/* Fuse — burns away */}
            <path
              d="M 460 200 C 560 90, 660 130, 690 40"
              fill="none" stroke={C.ink} strokeWidth="12" strokeLinecap="round"
              strokeDasharray={fuseLen}
              strokeDashoffset={-fuseLen * fuseP}
            />
            {/* Spark glow */}
            <circle
              cx={interpolate(fuseP, [0, 1], [690, 460])}
              cy={interpolate(fuseP, [0, 0.5, 1], [40, 120, 200])}
              r={14 + Math.sin(frame * 0.5) * 6}
              fill={C.lav} opacity={fuseP > 0.02 && fuseP < 0.99 ? 1 : 0}
            />
            <circle
              cx={interpolate(fuseP, [0, 1], [690, 460])}
              cy={interpolate(fuseP, [0, 0.5, 1], [40, 120, 200])}
              r={26 + Math.sin(frame * 0.5) * 8}
              fill={C.lav} opacity={fuseP > 0.02 && fuseP < 0.99 ? 0.3 : 0}
            />
            {/* Bomb cap */}
            <rect x="410" y="170" width="100" height="70" rx="18" fill={C.lavDeep} />
            {/* Bomb body */}
            <circle cx="360" cy="420" r="220" fill={C.ink} />
            {/* Body highlight */}
            <circle cx="290" cy="350" r="60" fill={C.cream} opacity="0.12" />
            {/* ₹ symbol on bomb */}
            <text x="360" y="465" textAnchor="middle"
              fontFamily="'JetBrains Mono', monospace" fontSize="150" fontWeight="800" fill={C.cream}>₹</text>
          </svg>
        </div>

        {/* ECONOMIC BOMB title */}
        <div style={{
          position: 'absolute', bottom: 140, width: '100%', textAlign: 'center',
          transform: `scale(${titleIn})`, opacity: titleIn,
        }}>
          <div style={{
            fontFamily: "'JetBrains Mono', monospace", fontWeight: 800,
            fontSize: 108, letterSpacing: 14, color: C.ink, lineHeight: 1,
          }}>
            ECONOMIC <span style={{ color: C.cream, textShadow: `6px 6px 0 ${C.ink}` }}>BOMB</span>
          </div>
          <div style={{
            marginTop: 20, fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontSize: 44, fontWeight: 700, color: C.ink, opacity: 0.85,
          }}>
            आज की बात — उसी खतरनाक कॉम्बिनेशन की
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(31,35,18,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08EconomicBomb;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09Stagflation',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const Scene09Stagflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: slow rise (tilt-up feel) */
  const camY = interpolate(frame, [0, durationInFrames], [36, -24]);
  const camScale = 1.04;

  /* Arrow draw */
  const arrowLen = 700;
  const drawL = interpolate(frame, [50, 190], [arrowLen, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const drawR = interpolate(frame, [90, 230], [arrowLen, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const labL = sp(150);
  const labR = sp(190);
  const collideIn = sp(280, { damping: 7, stiffness: 180 });
  const shakeT = frame - 280;
  const shake = shakeT > 0 && shakeT < 20 ? Math.sin(shakeT * 1.5) * (20 - shakeT) * 1 : 0;
  const windIn = sp(400);
  /* Wind streaks drift */
  const windX = (frame * 3) % 2100;

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${160 + Math.sin(frame * 0.008) * 12}deg, #C6E71A 0%, ${C.bg} 45%, ${C.bgDeep} 100%)`,
      }} />

      {/* Layer 2: Animated shape layers — wind curves */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <path d={`M ${-400 + windX} 260 q 120 -40 240 0 t 240 0 t 240 0`} fill="none" stroke={C.cream} strokeWidth="5" strokeLinecap="round" />
        <path d={`M ${-700 + windX} 560 q 150 -50 300 0 t 300 0`} fill="none" stroke={C.ink} strokeWidth="4" strokeLinecap="round" />
        <path d={`M ${-550 + windX} 860 q 130 -45 260 0 t 260 0`} fill="none" stroke={C.lav} strokeWidth="5" strokeLinecap="round" />
      </svg>

      {/* Layer 3: Motion texture — diagonal cross grid */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(60deg, ${C.ink} 0px, ${C.ink} 1.5px, transparent 1.5px, transparent 100px)`,
        backgroundPosition: `${frame * 0.4}px 0px`,
        opacity: 0.04,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 9 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 81) * 1920 + frame * (1 + seededRandom(i))) % 1920,
          top: seededRandom(i + 33) * 1080 + Math.sin(frame * 0.02 + i) * 14,
          width: 14, height: 3, borderRadius: 3,
          backgroundColor: C.ink, opacity: 0.12,
        }} />
      ))}

      {/* Layer 5: Main content */}
      <AbsoluteFill style={{ transform: `translate(${shake}px, ${camY}px) scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Twin rising arrows */}
        <svg width="1920" height="1080" viewBox="0 0 1920 1080" style={{ position: 'absolute' }}>
          {/* महंगाई arrow */}
          <path
            d="M 420 880 L 620 700 L 700 760 L 900 480"
            fill="none" stroke={C.lavDeep} strokeWidth="22" strokeLinecap="round" strokeLinejoin="round"
            strokeDasharray={arrowLen} strokeDashoffset={drawL}
          />
          <polygon points="900,480 830,505 880,555" fill={C.lavDeep} opacity={frame > 175 ? 1 : 0} />
          {/* बेरोजगारी arrow */}
          <path
            d="M 1500 880 L 1300 690 L 1220 750 L 1020 480"
            fill="none" stroke={C.cream} strokeWidth="22" strokeLinecap="round" strokeLinejoin="round"
            strokeDasharray={arrowLen} strokeDashoffset={drawR}
          />
          <polygon points="1020,480 1090,505 1042,557" fill={C.cream} opacity={frame > 215 ? 1 : 0} />
        </svg>

        {/* Labels */}
        <div style={{
          position: 'absolute', left: 300, bottom: 130,
          backgroundColor: C.lavDeep, color: C.cream, borderRadius: 16,
          fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 52, fontWeight: 800,
          padding: '16px 40px',
          opacity: labL, transform: `translateY(${interpolate(labL, [0, 1], [60, 0])}px) rotate(-2deg)`,
        }}>महंगाई ▲</div>
        <div style={{
          position: 'absolute', right: 300, bottom: 130,
          backgroundColor: C.ink, color: C.cream, borderRadius: 16,
          fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 52, fontWeight: 800,
          padding: '16px 40px',
          opacity: labR, transform: `translateY(${interpolate(labR, [0, 1], [60, 0])}px) rotate(2deg)`,
        }}>बेरोजगारी ▲</div>

        {/* Collision title */}
        <div style={{
          position: 'absolute', top: 200, width: '100%', textAlign: 'center',
          transform: `scale(${collideIn})`, opacity: collideIn,
        }}>
          <div style={{
            fontFamily: "'JetBrains Mono', monospace", fontWeight: 800,
            fontSize: 118, letterSpacing: 10, color: C.ink, lineHeight: 1,
            textShadow: '8px 8px 0 rgba(235,237,227,0.55)',
          }}>
            STAGFLATION
          </div>
          <div style={{
            marginTop: 16, fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontSize: 46, fontWeight: 700, color: C.ink,
          }}>
            दोनों का <span style={{ backgroundColor: C.ink, color: C.lav, padding: '2px 22px', borderRadius: 12 }}>एक साथ</span> ऊपर जाना
          </div>
        </div>

        {/* Wind warning */}
        <div style={{
          position: 'absolute', bottom: 40, width: '100%', textAlign: 'center',
          opacity: windIn, transform: `translateY(${interpolate(windIn, [0, 1], [50, 0])}px)`,
        }}>
          <span style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 42, fontWeight: 700,
            color: C.cream, backgroundColor: C.ink, padding: '14px 38px', borderRadius: 999,
          }}>
            घर में घुस जाए… तो निकलने में <span style={{ color: C.lav }}>सालों</span> लगते हैं
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(31,35,18,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09Stagflation;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10Dehleez',
  durationInSeconds: 24,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C = {
  bg: '#BBDC12',
  bgDeep: '#9DBA0E',
  ink: '#1F2312',
  cream: '#EBEDE3',
  lav: '#B5B9F0',
  lavDeep: '#6B72D6',
};

const STATS = [
  { v: '7%', l: 'बेरोजगारी' },
  { v: '4.5%', l: 'महंगाई' },
  { v: '15.2%', l: 'युवा बेरोजगारी' },
];

const Scene10Dehleez: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  /* Camera: dolly through the door in final act */
  const camScale = interpolate(frame, [0, 430, 700], [1, 1.06, 1.45], {
    extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  const doorIn = sp(15, { damping: 11 });
  /* Door light beam grows */
  const beam = interpolate(frame, [70, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const bharatIn = sp(120, { damping: 9 });
  const line1 = sp(240);
  const line2 = sp(400, { damping: 9 });
  /* Stat chips tease at the end */
  const breathe = 1 + Math.sin(frame * 0.06) * 0.015;

  return (
    <AbsoluteFill style={{ backgroundColor: C.bg, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% ${58 - beam * 12}%, #C9EA1C 0%, ${C.bg} 42%, ${C.bgDeep} 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: `3px solid ${C.ink}`, opacity: 0.045,
        left: '50%', top: '50%',
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.008) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 340, height: 340, borderRadius: 46,
        backgroundColor: C.lav, opacity: 0.06,
        left: -110 + Math.sin(frame * 0.006) * 20, top: 120,
        transform: `rotate(${-14 + Math.cos(frame * 0.008) * 5}deg)`,
      }} />

      {/* Layer 3: Motion texture — dots drift */}
      <AbsoluteFill style={{
        backgroundImage: `radial-gradient(${C.ink} 1.8px, transparent 1.8px)`,
        backgroundSize: '85px 85px',
        backgroundPosition: `${frame * 0.14}px ${frame * 0.1}px`,
        opacity: 0.05,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 10 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 91) * 1920 + Math.sin(frame * 0.014 + i * 1.6) * 24,
          top: seededRandom(i + 44) * 1080 + Math.cos(frame * 0.018 + i) * 22,
          width: 5 + seededRandom(i + 5) * 5, height: 5 + seededRandom(i + 5) * 5,
          borderRadius: '50%', backgroundColor: i % 3 === 0 ? C.lavDeep : C.cream,
          opacity: 0.14,
        }} />
      ))}

      {/* Layer 5: Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Door frame — the दहलीज */}
        <div style={{
          position: 'absolute', transform: `scale(${doorIn * breathe})`, opacity: doorIn,
        }}>
          <svg width="640" height="820" viewBox="0 0 640 820">
            {/* Light beam behind door */}
            <rect x={320 - 220 * beam} y="60" width={440 * beam} height="700" fill={C.lav} opacity={0.35 * beam} />
            {/* Frame */}
            <rect x="90" y="60" width="460" height="700" rx="16" fill="none" stroke={C.ink} strokeWidth="20" />
            {/* Threshold step */}
            <rect x="40" y="760" width="560" height="34" rx="17" fill={C.ink} />
            {/* Door slightly open */}
            <polygon points="550,60 550,760 430,700 430,120" fill={C.ink} opacity="0.85" />
          </svg>
        </div>

        {/* भारत standing at threshold */}
        <div style={{
          position: 'absolute', top: 300, width: '100%', textAlign: 'center',
          transform: `scale(${bharatIn})`, opacity: bharatIn,
        }}>
          <div style={{
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 170, color: C.ink, lineHeight: 1,
            textShadow: `0 0 60px rgba(181,185,240,0.8)`,
          }}>भारत</div>
          <div style={{
            marginTop: 8, fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontSize: 54, fontWeight: 800, color: C.cream,
            backgroundColor: C.ink, display: 'inline-block',
            padding: '12px 40px', borderRadius: 14,
            opacity: line1, transform: `translateY(${interpolate(line1, [0, 1], [40, 0])}px)`,
          }}>
            ठीक उसी <span style={{ color: C.lav }}>दहलीज</span> पर खड़ा है
          </div>
        </div>

        {/* Final beat: भावना से पहले सच्चाई + stat tease */}
        <div style={{
          position: 'absolute', bottom: 70, width: '100%', textAlign: 'center',
          opacity: line2, transform: `translateY(${interpolate(line2, [0, 1], [70, 0])}px)`,
        }}>
          <div style={{
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 72, color: C.ink, marginBottom: 26,
          }}>
            भावना से पहले — <span style={{ backgroundColor: C.lavDeep, color: C.cream, padding: '0 26px', borderRadius: 14 }}>सच्चाई</span>
          </div>
          <div style={{ display: 'flex', justifyContent: 'center', gap: 30 }}>
            {STATS.map((s, i) => {
              const cs = sp(440 + i * 25, { damping: 10 });
              return (
                <div key={i} style={{
                  backgroundColor: C.cream, border: `4px solid ${C.ink}`, borderRadius: 18,
                  padding: '14px 34px', textAlign: 'center',
                  transform: `translateY(${interpolate(cs, [0, 1], [80, 0])}px) rotate(${(i - 1) * 2}deg)`,
                  opacity: cs,
                }}>
                  <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 56, fontWeight: 800, color: C.lavDeep }}>{s.v}</div>
                  <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 30, fontWeight: 700, color: C.ink }}>{s.l}</div>
                </div>
              );
            })}
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 52%, rgba(31,35,18,0.38) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600,
        color: C.ink, opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10Dehleez;
```

