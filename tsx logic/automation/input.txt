```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01UnChiffre',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#8E0B1C';

const ArrowGlyph: React.FC<{ dir: 'up' | 'down'; color: string; size: number }> = ({ dir, color, size }) => (
  <svg width={size} height={size} viewBox="0 0 48 48" fill="none">
    <path
      d={dir === 'up' ? 'M24 42V8M24 8L11 21M24 8L37 21' : 'M24 6V40M24 40L11 27M24 40L37 27'}
      stroke={color}
      strokeWidth={6}
      strokeLinecap="round"
      strokeLinejoin="round"
    />
  </svg>
);

const Scene01UnChiffre: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow push-in with a punk hand-held drift
  const camScale = interpolate(frame, [0, 900], [1.06, 1.14], { extrapolateRight: 'clamp' });
  const camX = Math.sin(frame * 0.012) * 14;
  const camY = Math.cos(frame * 0.009) * 10;

  // Beat windows
  const b1 = win(-1, 22, 195, 225);
  const b2 = win(210, 240, 455, 485);
  const b3 = win(470, 500, 700, 730);
  const b4 = win(715, 745, 915, 930);

  // Animated counters
  const upVal = interpolate(frame, [250, 330], [0, 0.4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const dnVal = interpolate(frame, [320, 400], [0, -0.4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Impact shake on the payoff beat
  const shake = frame > 745 && frame < 775 ? Math.sin(frame * 2.4) * 8 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${28 + Math.sin(frame * 0.01) * 8}% 22%, #FF5A6E 0%, ${RED} 42%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers — large transparent geometry drifting */}
      <div
        style={{
          position: 'absolute', width: 1200, height: 1200, borderRadius: '50%',
          border: `3px solid ${BLACK}`, opacity: 0.06,
          left: -260, top: -300,
          transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute', width: 760, height: 760, backgroundColor: BLACK, opacity: 0.05,
          right: -180, bottom: -220,
          transform: `rotate(${-frame * 0.05}deg)`,
        }}
      />
      <svg style={{ position: 'absolute', left: 1180, top: 90, opacity: 0.07, transform: `rotate(${frame * 0.12}deg)` }} width="420" height="420" viewBox="0 0 100 100">
        <polygon points="50,4 96,92 4,92" fill="none" stroke={WHITE} strokeWidth="2" />
      </svg>

      {/* Motion texture — halftone dot grid (punk print feel) */}
      <AbsoluteFill
        style={{
          backgroundImage: `radial-gradient(${BLACK} 1.6px, transparent 1.7px)`,
          backgroundSize: '24px 24px',
          backgroundPosition: `${(frame * 0.4) % 24}px ${(frame * 0.25) % 24}px`,
          opacity: 0.1,
        }}
      />

      {/* Floating particles */}
      {new Array(26).fill(0).map((_, i) => {
        const s = seededRandom(i + 3) * 100;
        const size = 4 + seededRandom(i + 11) * 8;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: `${s}%`,
              top: `${(seededRandom(i + 21) * 110 - (frame * (0.14 + seededRandom(i + 31) * 0.2))) % 110}%`,
              width: size, height: size,
              backgroundColor: i % 3 === 0 ? BLACK : WHITE,
              opacity: 0.28,
              borderRadius: i % 2 === 0 ? '50%' : 0,
            }}
          />
        );
      })}

      {/* Main content — camera wrapper */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translate(${camX + shake}px, ${camY}px)`,
          justifyContent: 'center', alignItems: 'center',
        }}
      >
        {/* BEAT 1 — hero statement */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center', width: 1500 }}>
          <div
            style={{
              display: 'inline-block', backgroundColor: BLACK, color: WHITE,
              fontFamily: 'Space Mono, monospace', fontSize: 30, letterSpacing: 6,
              padding: '10px 26px', transform: `rotate(-2deg) translateY(${(1 - sp(6)) * -60}px)`,
            }}
          >
            MATURES FINANCE — 2025
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 210, lineHeight: 0.9,
              color: WHITE, fontWeight: 800, letterSpacing: -4, marginTop: 24,
              transform: `scale(${0.7 + sp(18) * 0.3})`, textShadow: `10px 10px 0px ${BLACK}`,
            }}
          >
            UN CHIFFRE
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif', fontSize: 100, color: BLACK, marginTop: 6,
              transform: `translateX(${(1 - sp(42)) * 220}px) rotate(-1.5deg)`,
              backgroundColor: WHITE, display: 'inline-block', padding: '4px 34px',
            }}
          >
            UN SEUL
          </div>
          <div
            style={{
              fontFamily: 'Montserrat, Poppins, sans-serif', fontSize: 44, color: WHITE, marginTop: 34,
              opacity: interpolate(frame, [70, 105], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            Il résume tout ce qui se passe dans le portefeuille des Français
          </div>
        </div>

        {/* BEAT 2 — split panels: consommation vs pouvoir d'achat */}
        <div style={{ position: 'absolute', opacity: b2, display: 'flex', gap: 60 }}>
          {[
            { t: 'CONSOMMATION', v: `+${upVal.toFixed(1).replace('.', ',')} %`, d: 'up' as const, bg: WHITE, fg: BLACK, delay: 230 },
            { t: "POUVOIR D'ACHAT", v: `${dnVal.toFixed(1).replace('.', ',')} %`, d: 'down' as const, bg: BLACK, fg: WHITE, delay: 300 },
          ].map((c, i) => (
            <div
              key={i}
              style={{
                width: 640, padding: '48px 46px', backgroundColor: c.bg, color: c.fg,
                transform: `translateY(${(1 - sp(c.delay)) * 320}px) rotate(${i === 0 ? -2 : 2}deg)`,
                boxShadow: `16px 16px 0px ${i === 0 ? BLACK : WHITE}`,
              }}
            >
              <ArrowGlyph dir={c.d} color={c.fg} size={74} />
              <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 30, letterSpacing: 4, marginTop: 12 }}>{c.t}</div>
              <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 150, fontWeight: 800, lineHeight: 1 }}>{c.v}</div>
              <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 32, opacity: 0.75 }}>
                {i === 0 ? 'en volume — ménages' : 'revenu disponible brut'}
              </div>
            </div>
          ))}
        </div>

        {/* BEAT 3 — seesaw graph: two forces, opposite directions */}
        <div style={{ position: 'absolute', opacity: b3, width: 1420 }}>
          <div style={{ fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif', fontSize: 60, color: BLACK, textAlign: 'center', marginBottom: 40 }}>
            Deux courbes qui partent en sens inverse
          </div>
          <svg width="1420" height="420" viewBox="0 0 1420 420">
            <line x1="60" y1="210" x2="1360" y2="210" stroke={BLACK} strokeWidth="3" strokeDasharray="14 12" />
            <path
              d="M60 260 L400 232 L760 200 L1120 168 L1360 140"
              stroke={WHITE} strokeWidth="12" fill="none" strokeLinecap="round"
              strokeDasharray="1600"
              strokeDashoffset={interpolate(frame, [500, 620], [1600, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })}
            />
            <path
              d="M60 160 L400 194 L760 226 L1120 268 L1360 300"
              stroke={BLACK} strokeWidth="12" fill="none" strokeLinecap="round"
              strokeDasharray="1600"
              strokeDashoffset={interpolate(frame, [540, 660], [1600, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })}
            />
            <circle cx="1360" cy="140" r={10 + Math.sin(frame * 0.2) * 3} fill={WHITE} />
            <circle cx="1360" cy="300" r={10 + Math.sin(frame * 0.2 + 2) * 3} fill={BLACK} />
          </svg>
          <div style={{ display: 'flex', justifyContent: 'space-between', fontFamily: 'Space Mono, monospace', fontSize: 28, color: WHITE, marginTop: 10 }}>
            <span>DÉPENSES ▲</span>
            <span style={{ color: BLACK }}>CAPACITÉ RÉELLE ▼</span>
          </div>
        </div>

        {/* BEAT 4 — payoff typography */}
        <div style={{ position: 'absolute', opacity: b4, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: 'Anton, sans-serif', fontSize: 190, color: WHITE, lineHeight: 0.92,
              transform: `scale(${0.8 + sp(730) * 0.2})`, textShadow: `12px 12px 0px ${BLACK}`,
            }}
          >
            ON DÉPENSE PLUS
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif', fontSize: 190, color: BLACK, lineHeight: 0.92,
              backgroundColor: WHITE, display: 'inline-block', padding: '0 30px', marginTop: 18,
              transform: `rotate(-1.5deg) scale(${0.8 + sp(780) * 0.2})`,
            }}
          >
            ON PEUT MOINS
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE,
          opacity: interpolate(frame, [0, 40], [0, 0.5], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene01UnChiffre;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02InflationBrutale',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#7A0A1A';

const FlameIcon: React.FC<{ color: string; size: number }> = ({ color, size }) => (
  <svg width={size} height={size} viewBox="0 0 48 48" fill="none">
    <path d="M24 4c6 8 12 11 12 20a12 12 0 0 1-24 0c0-5 3-8 6-12 1 3 3 5 5 5 2 0 3-2 3-5 0-3-1-5-2-8Z" stroke={color} strokeWidth={3.5} strokeLinejoin="round" />
  </svg>
);

const Scene02InflationBrutale: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: lateral pan with slow tilt
  const camX = interpolate(frame, [0, 900], [40, -40]);
  const camRot = Math.sin(frame * 0.008) * 0.6;

  const b1 = win(-1, 20, 220, 250);
  const b2 = win(235, 265, 520, 550);
  const b3 = win(535, 565, 915, 930);

  const bars = [
    { y: '2022', v: 5.9, c: BLACK },
    { y: '2023', v: 4.9, c: BLACK },
    { y: 'SALAIRES', v: 2.4, c: WHITE },
  ];

  const steps = [
    { y: '2024', v: '+0,8 %', l: 'correction amorcée', d: 570 },
    { y: '2025', v: '+1,1 %', l: 'accélération', d: 640 },
    { y: '2026', v: '+1,6 pt', l: 'plus haut depuis 2019', d: 710 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${115 + Math.sin(frame * 0.01) * 12}deg, ${DEEP} 0%, ${RED} 48%, #FF6478 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <div
        style={{
          position: 'absolute', left: 640, top: -420, width: 900, height: 900,
          border: `4px solid ${WHITE}`, opacity: 0.07, borderRadius: 40,
          transform: `rotate(${18 + frame * 0.04}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute', left: -220, bottom: -260, width: 700, height: 700, borderRadius: '50%',
          backgroundColor: BLACK, opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.024) * 0.06})`,
        }}
      />

      {/* Motion texture — moving vertical bars (punk print) */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(90deg, ${BLACK} 0px, ${BLACK} 3px, transparent 3px, transparent 46px)`,
          backgroundPosition: `${(frame * 0.7) % 46}px 0px`,
          opacity: 0.09,
        }}
      />

      {/* Floating particles */}
      {new Array(22).fill(0).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 7) * 100}%`,
            top: `${(seededRandom(i + 17) * 110 + frame * (0.1 + seededRandom(i + 27) * 0.18)) % 110}%`,
            width: 6, height: 18,
            backgroundColor: i % 2 === 0 ? WHITE : BLACK,
            opacity: 0.24,
            transform: `rotate(${seededRandom(i + 37) * 60 - 30}deg)`,
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px) rotate(${camRot}deg)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* BEAT 1 — brutal years */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center' }}>
          <div style={{ transform: `scale(${0.6 + sp(4) * 0.4})`, display: 'flex', justifyContent: 'center', gap: 22 }}>
            <FlameIcon color={BLACK} size={90} />
            <FlameIcon color={WHITE} size={90} />
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif', fontSize: 230, color: WHITE, lineHeight: 0.9, marginTop: 10,
              transform: `translateY(${(1 - sp(20)) * 120}px)`, textShadow: `10px 10px 0px ${BLACK}`,
            }}
          >
            2022 · 2023
          </div>
          <div
            style={{
              display: 'inline-block', backgroundColor: BLACK, color: WHITE, marginTop: 24,
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 96, padding: '8px 40px',
              transform: `rotate(-2deg) scale(${0.7 + sp(52) * 0.3})`,
            }}
          >
            BRUTALES
          </div>
        </div>

        {/* BEAT 2 — inflation vs salaires bar chart */}
        <div style={{ position: 'absolute', opacity: b2, width: 1460 }}>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 32, color: BLACK, letterSpacing: 5, marginBottom: 18 }}>
            INFLATION CUMULÉE VS SALAIRES — 2 ANS
          </div>
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 80, height: 480 }}>
            {bars.map((b, i) => {
              const grow = interpolate(frame, [270 + i * 45, 350 + i * 45], [0, 1], {
                extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
              });
              return (
                <div key={i} style={{ flex: 1, textAlign: 'center' }}>
                  <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 78, color: b.c }}>
                    {(b.v * grow).toFixed(1).replace('.', ',')} %
                  </div>
                  <div
                    style={{
                      height: b.v * 58 * grow, backgroundColor: b.c, marginTop: 12,
                      boxShadow: `10px 10px 0px ${b.c === BLACK ? WHITE : BLACK}`,
                    }}
                  />
                  <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 36, fontWeight: 700, color: WHITE, marginTop: 16 }}>{b.y}</div>
                </div>
              );
            })}
          </div>
          <div
            style={{
              marginTop: 34, fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif', fontSize: 52, color: BLACK,
              opacity: interpolate(frame, [420, 470], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            +10 % de prix — les salaires n'ont pas suivi
          </div>
        </div>

        {/* BEAT 3 — recovery step cards */}
        <div style={{ position: 'absolute', opacity: b3, width: 1620 }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 84, color: WHITE, marginBottom: 34, textShadow: `8px 8px 0px ${BLACK}` }}>
            LA REMONTÉE, PALIER PAR PALIER
          </div>
          <div style={{ display: 'flex', gap: 40, alignItems: 'flex-end' }}>
            {steps.map((s, i) => (
              <div
                key={i}
                style={{
                  flex: 1, padding: '38px 30px',
                  backgroundColor: i === 2 ? BLACK : WHITE,
                  color: i === 2 ? WHITE : BLACK,
                  marginBottom: i * 46,
                  transform: `translateY(${(1 - sp(s.d)) * 300}px) rotate(${i === 1 ? 1.6 : -1.4}deg)`,
                  boxShadow: `14px 14px 0px ${i === 2 ? WHITE : BLACK}`,
                }}
              >
                <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 30, letterSpacing: 4 }}>{s.y}</div>
                <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 118, lineHeight: 1 }}>{s.v}</div>
                <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 30, opacity: 0.8 }}>{s.l}</div>
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 45%, rgba(0,0,0,0) 40%, rgba(0,0,0,0.58) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE, opacity: 0.45 }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene02InflationBrutale;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03LeMotPresque',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#8E0B1C';

const Scene03LeMotPresque: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: orbit-style rotation around the content
  const camRot = Math.sin(frame * 0.006) * 1.4;
  const camScale = interpolate(frame, [0, 900], [1.12, 1.02]);

  const b1 = win(-1, 22, 230, 260);
  const b2 = win(245, 275, 540, 570);
  const b3 = win(555, 585, 915, 930);

  // Donut: 48% of Français expected a drop
  const donutPct = interpolate(frame, [290, 400], [0, 48], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const circ = 2 * Math.PI * 150;

  // Invoice bars climbing on beat 3
  const invoices = [0.4, 0.55, 0.68, 0.82, 1];

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(ellipse at 70% ${70 + Math.sin(frame * 0.012) * 10}%, #FF6377 0%, ${RED} 45%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <svg style={{ position: 'absolute', left: -140, top: 120, opacity: 0.06, transform: `rotate(${-frame * 0.07}deg)` }} width="640" height="640" viewBox="0 0 100 100">
        <rect x="6" y="6" width="88" height="88" fill="none" stroke={BLACK} strokeWidth="2.4" />
        <rect x="24" y="24" width="52" height="52" fill="none" stroke={BLACK} strokeWidth="2.4" />
      </svg>
      <div
        style={{
          position: 'absolute', right: -180, top: 60, width: 620, height: 620, borderRadius: '50%',
          border: `5px dashed ${WHITE}`, opacity: 0.08,
          transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.018) * 0.05})`,
        }}
      />

      {/* Motion texture — concentric ripple rings */}
      {new Array(5).fill(0).map((_, i) => {
        const r = ((frame * 1.4 + i * 180) % 900) + 60;
        return (
          <div
            key={i}
            style={{
              position: 'absolute', left: 960 - r / 2, top: 540 - r / 2, width: r, height: r,
              borderRadius: '50%', border: `2px solid ${WHITE}`, opacity: 0.07,
            }}
          />
        );
      })}

      {/* Floating particles */}
      {new Array(24).fill(0).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 5) * 100}%`,
            top: `${(seededRandom(i + 15) * 110 - frame * (0.12 + seededRandom(i + 25) * 0.16)) % 110}%`,
            width: 5 + seededRandom(i + 35) * 7, height: 5 + seededRandom(i + 35) * 7,
            backgroundColor: i % 3 === 0 ? BLACK : WHITE, opacity: 0.26, borderRadius: '50%',
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `rotate(${camRot}deg) scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        {/* BEAT 1 — "PRESQUE" stamp */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center' }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 46, color: WHITE, fontWeight: 600 }}>
            Trois ans de gains compensent les pertes...
          </div>
          <div
            style={{
              display: 'inline-block', marginTop: 26, padding: '14px 60px',
              border: `10px solid ${BLACK}`, backgroundColor: WHITE,
              transform: `rotate(-4deg) scale(${0.5 + sp(30, { stiffness: 90 }) * 0.5})`,
            }}
          >
            <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 220, color: BLACK, lineHeight: 1 }}>PRESQUE</div>
          </div>
          <div
            style={{
              fontFamily: 'Space Mono, monospace', fontSize: 34, color: BLACK, letterSpacing: 6, marginTop: 30,
              opacity: interpolate(frame, [95, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            LE MOT-CLÉ EST LÀ
          </div>
        </div>

        {/* BEAT 2 — donut chart, la moitié des Français */}
        <div style={{ position: 'absolute', opacity: b2, display: 'flex', alignItems: 'center', gap: 90 }}>
          <div style={{ transform: `scale(${0.75 + sp(255) * 0.25})` }}>
            <svg width="420" height="420" viewBox="0 0 400 400">
              <circle cx="200" cy="200" r="150" fill="none" stroke={BLACK} strokeWidth="46" opacity="0.35" />
              <circle
                cx="200" cy="200" r="150" fill="none" stroke={WHITE} strokeWidth="46"
                strokeDasharray={circ} strokeDashoffset={circ - (circ * donutPct) / 100}
                transform="rotate(-90 200 200)" strokeLinecap="butt"
              />
              <text x="200" y="218" textAnchor="middle" fontFamily="Clash Display, Anton, sans-serif" fontSize="104" fill={WHITE}>
                {donutPct.toFixed(0)} %
              </text>
            </svg>
          </div>
          <div style={{ width: 760 }}>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 96, color: BLACK, lineHeight: 0.95 }}>
              PRÈS DE LA MOITIÉ
            </div>
            <div
              style={{
                fontFamily: 'Montserrat, sans-serif', fontSize: 44, color: WHITE, marginTop: 20, fontWeight: 600,
                transform: `translateX(${(1 - sp(330)) * 200}px)`,
              }}
            >
              des Français pensaient que leur pouvoir d'achat allait baisser en 2025
            </div>
            <div
              style={{
                display: 'inline-block', marginTop: 26, backgroundColor: BLACK, color: WHITE,
                fontFamily: 'Space Mono, monospace', fontSize: 28, padding: '10px 22px', letterSpacing: 3,
                opacity: interpolate(frame, [400, 440], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}
            >
              DERRIÈRE LA MOYENNE — CADRES &amp; PROFILS TECH
            </div>
          </div>
        </div>

        {/* BEAT 3 — perception nourrie par les factures */}
        <div style={{ position: 'absolute', opacity: b3, width: 1500, textAlign: 'center' }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 118, color: WHITE, textShadow: `9px 9px 0px ${BLACK}` }}>
            CE N'EST PAS DU PESSIMISME
          </div>
          <div style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'center', gap: 34, height: 340, marginTop: 40 }}>
            {invoices.map((v, i) => {
              const grow = interpolate(frame, [600 + i * 34, 680 + i * 34], [0, 1], {
                extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
              });
              return (
                <div key={i} style={{ width: 130, textAlign: 'center' }}>
                  {/* invoice icon on top of each bar */}
                  <svg width="56" height="56" viewBox="0 0 48 48" style={{ opacity: grow }}>
                    <path d="M12 4h24v40l-6-4-6 4-6-4-6 4V4Z" fill="none" stroke={BLACK} strokeWidth="3" />
                    <path d="M18 16h12M18 24h12" stroke={BLACK} strokeWidth="3" strokeLinecap="round" />
                  </svg>
                  <div
                    style={{
                      height: 260 * v * grow, backgroundColor: i === invoices.length - 1 ? BLACK : WHITE,
                      marginTop: 8, boxShadow: `8px 8px 0px ${i === invoices.length - 1 ? WHITE : BLACK}`,
                    }}
                  />
                </div>
              );
            })}
          </div>
          <div
            style={{
              fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif', fontSize: 56, color: BLACK, marginTop: 34,
              opacity: interpolate(frame, [800, 840], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            Des factures qui grimpent, mois après mois
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.56) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE, opacity: 0.45 }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene03LeMotPresque;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04LeTerrainABouge',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#7A0A1A';

const CardIcon: React.FC<{ color: string; size: number }> = ({ color, size }) => (
  <svg width={size} height={size} viewBox="0 0 48 32" fill="none">
    <rect x="2" y="2" width="44" height="28" rx="4" stroke={color} strokeWidth="3" />
    <path d="M2 12h44" stroke={color} strokeWidth="5" />
    <path d="M8 22h10" stroke={color} strokeWidth="3" strokeLinecap="round" />
  </svg>
);

const Scene04LeTerrainABouge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: vertical tilt + gentle zoom out
  const camY = interpolate(frame, [0, 900], [-26, 26]);
  const camScale = interpolate(frame, [0, 900], [1.1, 1.03]);

  const b1 = win(-1, 22, 250, 280);
  const b2 = win(265, 295, 560, 590);
  const b3 = win(575, 605, 915, 930);

  const leaks = [
    { l: 'Abonnements', v: '−' },
    { l: 'Livraisons', v: '−' },
    { l: 'Mensualités', v: '−' },
    { l: 'Frais divers', v: '−' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${200 + Math.cos(frame * 0.01) * 15}deg, ${RED} 0%, #D4142E 55%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <div
        style={{
          position: 'absolute', left: 300, top: -260, width: 1000, height: 1000,
          border: `4px solid ${BLACK}`, opacity: 0.05,
          transform: `rotate(${45 + frame * 0.03}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute', right: 120, bottom: -300, width: 640, height: 640, borderRadius: '50%',
          backgroundColor: WHITE, opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.021) * 0.07})`,
        }}
      />

      {/* Motion texture — drifting grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `linear-gradient(${WHITE} 1.5px, transparent 1.5px), linear-gradient(90deg, ${WHITE} 1.5px, transparent 1.5px)`,
          backgroundSize: '90px 90px',
          backgroundPosition: `${(frame * 0.5) % 90}px ${(frame * 0.35) % 90}px`,
          opacity: 0.07,
        }}
      />

      {/* Floating particles */}
      {new Array(20).fill(0).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 9) * 100}%`,
            top: `${(seededRandom(i + 19) * 110 + frame * (0.1 + seededRandom(i + 29) * 0.2)) % 110}%`,
            width: 8, height: 8, backgroundColor: i % 2 ? BLACK : WHITE, opacity: 0.25,
            transform: `rotate(45deg)`,
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px) scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        {/* BEAT 1 — money leaving the account */}
        <div style={{ position: 'absolute', opacity: b1, display: 'flex', alignItems: 'center', gap: 70 }}>
          <div
            style={{
              backgroundColor: BLACK, padding: '46px 42px', width: 520,
              transform: `rotate(-2deg) translateY(${(1 - sp(8)) * 220}px)`, boxShadow: `16px 16px 0px ${WHITE}`,
            }}
          >
            <CardIcon color={WHITE} size={110} />
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 28, color: WHITE, letterSpacing: 4, marginTop: 20 }}>
              COMPTE COURANT
            </div>
            <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 88, color: WHITE, lineHeight: 1.05 }}>
              CHAQUE MOIS
            </div>
          </div>
          <div style={{ width: 760 }}>
            {leaks.map((l, i) => {
              const s = sp(40 + i * 26);
              return (
                <div
                  key={i}
                  style={{
                    display: 'flex', alignItems: 'center', gap: 20, marginBottom: 18,
                    backgroundColor: WHITE, padding: '16px 26px',
                    transform: `translateX(${(1 - s) * 320}px) rotate(${i % 2 ? 1 : -1}deg)`,
                    boxShadow: `10px 10px 0px ${BLACK}`,
                  }}
                >
                  <svg width="40" height="40" viewBox="0 0 48 48">
                    <path d="M8 24h28M36 24l-10-10M36 24l-10 10" stroke={BLACK} strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" />
                  </svg>
                  <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 54, color: BLACK }}>{l.l}</span>
                  <span style={{ marginLeft: 'auto', fontFamily: 'Space Mono, monospace', fontSize: 44, color: RED }}>SORTIE</span>
                </div>
              );
            })}
            <div
              style={{
                fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif', fontSize: 42, color: BLACK, marginTop: 16,
                opacity: interpolate(frame, [170, 210], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}
            >
              Ça ne rapporte rien — ou ça coûte ta richesse future
            </div>
          </div>
        </div>

        {/* BEAT 2 — intro card Lucas */}
        <div style={{ position: 'absolute', opacity: b2, textAlign: 'center' }}>
          <div
            style={{
              display: 'inline-block', backgroundColor: WHITE, padding: '40px 80px',
              transform: `rotate(-1.5deg) scale(${0.72 + sp(275) * 0.28})`, boxShadow: `20px 20px 0px ${BLACK}`,
            }}
          >
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 30, letterSpacing: 8, color: RED }}>MOI, C'EST</div>
            <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 200, color: BLACK, lineHeight: 0.95 }}>LUCAS</div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 40, fontWeight: 700, color: BLACK, letterSpacing: 2 }}>
              MATURES FINANCE
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Montserrat, sans-serif', fontSize: 46, color: WHITE, marginTop: 40, fontWeight: 600,
              opacity: interpolate(frame, [370, 410], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              transform: `translateY(${(1 - sp(370)) * 60}px)`,
            }}
          >
            Tu sens que quelque chose cloche dans ta façon de dépenser
          </div>
        </div>

        {/* BEAT 3 — le terrain a bougé (tilting ground graphic) */}
        <div style={{ position: 'absolute', opacity: b3, width: 1560, textAlign: 'center' }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 82, color: BLACK }}>
            PAS PARCE QUE TU VIS AU-DESSUS DE TES MOYENS
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 168, color: WHITE, lineHeight: 1,
              marginTop: 20, textShadow: `12px 12px 0px ${BLACK}`,
              transform: `rotate(${interpolate(frame, [620, 720], [0, -3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) })}deg)`,
            }}
          >
            LE TERRAIN A BOUGÉ
          </div>
          <svg width="1200" height="220" viewBox="0 0 1200 220" style={{ marginTop: 30 }}>
            <path
              d="M40 150 L1160 150"
              stroke={BLACK} strokeWidth="14" strokeLinecap="round"
              transform={`rotate(${interpolate(frame, [640, 760], [0, 7], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} 600 150)`}
            />
            {[220, 480, 740, 980].map((x, i) => (
              <rect
                key={i} x={x} y={100} width={70} height={44} fill={WHITE}
                transform={`rotate(${interpolate(frame, [660 + i * 20, 800 + i * 20], [0, 14 + i * 4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} ${x + 35} 144)`}
              />
            ))}
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE, opacity: 0.45 }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene04LeTerrainABouge;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05ExtractionMaximale',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#8E0B1C';

const GearIcon: React.FC<{ color: string; size: number; rot: number }> = ({ color, size, rot }) => (
  <svg width={size} height={size} viewBox="0 0 48 48" style={{ transform: `rotate(${rot}deg)` }}>
    <circle cx="24" cy="24" r="8" fill="none" stroke={color} strokeWidth="3" />
    {new Array(8).fill(0).map((_, i) => (
      <rect key={i} x="22" y="1" width="4" height="9" fill={color} transform={`rotate(${i * 45} 24 24)`} />
    ))}
    <circle cx="24" cy="24" r="16" fill="none" stroke={color} strokeWidth="3" />
  </svg>
);

const Scene05ExtractionMaximale: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: diagonal drift
  const camX = Math.sin(frame * 0.011) * 26;
  const camY = Math.cos(frame * 0.014) * 18;
  const camScale = interpolate(frame, [0, 900], [1.04, 1.12]);

  const b1 = win(-1, 22, 250, 280);
  const b2 = win(265, 295, 570, 600);
  const b3 = win(585, 615, 915, 930);

  // funnel extraction amount
  const extracted = interpolate(frame, [60, 220], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `conic-gradient(from ${frame * 0.15}deg at 50% 50%, ${RED} 0deg, #D4142E 90deg, ${DEEP} 200deg, ${RED} 360deg)`,
        }}
      />

      {/* Animated shape layers */}
      <svg style={{ position: 'absolute', right: -100, top: 200, opacity: 0.06, transform: `rotate(${frame * 0.06}deg)` }} width="760" height="760" viewBox="0 0 100 100">
        <polygon points="50,2 98,50 50,98 2,50" fill="none" stroke={BLACK} strokeWidth="2" />
      </svg>
      <div
        style={{
          position: 'absolute', left: -200, top: -160, width: 720, height: 720,
          backgroundColor: BLACK, opacity: 0.05, borderRadius: 60,
          transform: `rotate(${-20 - frame * 0.03}deg)`,
        }}
      />

      {/* Motion texture — diagonal punk stripes */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(45deg, ${BLACK} 0px, ${BLACK} 6px, transparent 6px, transparent 40px)`,
          backgroundPosition: `${(frame * 0.9) % 40}px 0px`,
          opacity: 0.08,
        }}
      />

      {/* Floating particles */}
      {new Array(26).fill(0).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 13) * 100}%`,
            top: `${(seededRandom(i + 23) * 110 - frame * (0.16 + seededRandom(i + 33) * 0.2)) % 110}%`,
            width: 4, height: 22, backgroundColor: i % 3 ? WHITE : BLACK, opacity: 0.22,
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translate(${camX}px, ${camY}px) scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        {/* BEAT 1 — extraction machine (funnel + gears) */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center' }}>
          <div style={{ display: 'flex', justifyContent: 'center', gap: 40, marginBottom: 18 }}>
            <GearIcon color={BLACK} size={96} rot={frame * 1.2} />
            <GearIcon color={WHITE} size={96} rot={-frame * 1.2} />
            <GearIcon color={BLACK} size={96} rot={frame * 1.2} />
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif', fontSize: 120, color: WHITE, lineHeight: 0.95,
              transform: `scale(${0.75 + sp(14) * 0.25})`, textShadow: `10px 10px 0px ${BLACK}`,
            }}
          >
            EXTRAIRE LE MAXIMUM
          </div>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 48, color: BLACK, fontWeight: 700, marginTop: 12 }}>
            de chaque transaction
          </div>
          {/* extraction gauge */}
          <div style={{ width: 1000, height: 40, backgroundColor: 'rgba(0,0,0,0.35)', marginTop: 40, position: 'relative' }}>
            <div style={{ width: `${extracted}%`, height: '100%', backgroundColor: WHITE }} />
            <div style={{ position: 'absolute', right: 14, top: 4, fontFamily: 'Space Mono, monospace', fontSize: 26, color: BLACK }}>
              {extracted.toFixed(0)} %
            </div>
          </div>
        </div>

        {/* BEAT 2 — habits 2019 vs 2026 */}
        <div style={{ position: 'absolute', opacity: b2, display: 'flex', alignItems: 'center', gap: 50 }}>
          {[
            { y: '2019', l: 'TES HABITUDES', bg: WHITE, fg: BLACK, d: 280 },
            { y: '2026', l: 'LE MÊME RÉFLEXE', bg: BLACK, fg: WHITE, d: 360 },
          ].map((c, i) => (
            <React.Fragment key={i}>
              <div
                style={{
                  width: 560, padding: '46px 40px', backgroundColor: c.bg, color: c.fg,
                  transform: `translateY(${(1 - sp(c.d)) * 300}px) rotate(${i ? 2 : -2}deg)`,
                  boxShadow: `16px 16px 0px ${i ? WHITE : BLACK}`,
                }}
              >
                <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 150, lineHeight: 1 }}>{c.y}</div>
                <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 30, letterSpacing: 4, marginTop: 8 }}>{c.l}</div>
              </div>
              {i === 0 ? (
                <svg width="140" height="90" viewBox="0 0 140 90" style={{ opacity: sp(330) }}>
                  <path d="M10 45h110M120 45l-26-22M120 45l-26 22" stroke={BLACK} strokeWidth="9" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
              ) : null}
            </React.Fragment>
          ))}
        </div>

        {/* BEAT 3 — sabotage tape + programme */}
        <div style={{ position: 'absolute', opacity: b3, width: 1620, textAlign: 'center' }}>
          <div
            style={{
              display: 'inline-block', backgroundColor: BLACK, color: WHITE, padding: '16px 50px',
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 150, lineHeight: 1.05,
              transform: `rotate(-2deg) scale(${0.8 + sp(600) * 0.2})`,
            }}
          >
            SABOTAGES FINANCIERS
          </div>
          <div style={{ display: 'flex', gap: 26, marginTop: 54, justifyContent: 'center' }}>
            {['DÉPENSES "NORMALES"', 'JAMAIS QUESTIONNÉES', 'PASSÉES EN REVUE'].map((t, i) => (
              <div
                key={i}
                style={{
                  flex: 1, backgroundColor: WHITE, color: BLACK, padding: '26px 20px',
                  fontFamily: 'Montserrat, sans-serif', fontSize: 34, fontWeight: 700,
                  transform: `translateY(${(1 - sp(660 + i * 45)) * 200}px) rotate(${i === 1 ? 1.4 : -1.2}deg)`,
                  boxShadow: `10px 10px 0px ${BLACK}`,
                }}
              >
                <svg width="46" height="46" viewBox="0 0 48 48" style={{ display: 'block', margin: '0 auto 10px' }}>
                  <circle cx="24" cy="24" r="18" fill="none" stroke={RED} strokeWidth="4" />
                  <path d="M16 24l6 6 10-12" stroke={RED} strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
                {t}
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE, opacity: 0.45 }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene05ExtractionMaximale;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06TroisLeviers',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#7A0A1A';

const ClockIcon: React.FC<{ c: string; s: number; f: number }> = ({ c, s, f }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none">
    <circle cx="24" cy="24" r="19" stroke={c} strokeWidth="3.5" />
    <line x1="24" y1="24" x2="24" y2="12" stroke={c} strokeWidth="3.5" strokeLinecap="round" transform={`rotate(${f * 2} 24 24)`} />
    <line x1="24" y1="24" x2="33" y2="24" stroke={c} strokeWidth="3.5" strokeLinecap="round" transform={`rotate(${f * 0.4} 24 24)`} />
  </svg>
);

const CrownIcon: React.FC<{ c: string; s: number }> = ({ c, s }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none">
    <path d="M4 38V14l10 8 10-14 10 14 10-8v24H4Z" stroke={c} strokeWidth="3.5" strokeLinejoin="round" />
  </svg>
);

const AnchorIcon: React.FC<{ c: string; s: number }> = ({ c, s }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none">
    <circle cx="24" cy="9" r="5" stroke={c} strokeWidth="3.5" />
    <path d="M24 14v28M12 26H6c0 10 8 16 18 16s18-6 18-16h-6M14 20h20" stroke={c} strokeWidth="3.5" strokeLinecap="round" />
  </svg>
);

const Scene06TroisLeviers: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow zoom out with micro-rotation
  const camScale = interpolate(frame, [0, 900], [1.14, 1.0]);
  const camRot = Math.sin(frame * 0.007) * 0.9;

  const b1 = win(-1, 20, 200, 230);
  const b2 = win(215, 245, 560, 590);
  const b3 = win(575, 605, 915, 930);

  const cards = [
    { t: 'COMMODITÉ', s: 'On te vend du temps', I: ClockIcon, d: 250 },
    { t: 'STATUT', s: 'On te vend un regard', I: CrownIcon, d: 320 },
    { t: 'INERTIE', s: 'On te vend ton oubli', I: AnchorIcon, d: 390 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% ${18 + Math.sin(frame * 0.013) * 8}%, #FF6E80 0%, ${RED} 46%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: 120 + i * 620, top: 120 + Math.sin(frame * 0.02 + i) * 40,
            width: 460, height: 460, border: `4px solid ${i === 1 ? WHITE : BLACK}`,
            opacity: 0.06, borderRadius: i === 1 ? '50%' : 30,
            transform: `rotate(${frame * (0.04 + i * 0.02)}deg)`,
          }}
        />
      ))}

      {/* Motion texture — crosshatch collage */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(135deg, ${WHITE} 0px, ${WHITE} 2px, transparent 2px, transparent 30px)`,
          backgroundPosition: `${(frame * -0.6) % 30}px 0px`,
          opacity: 0.08,
        }}
      />

      {/* Floating particles */}
      {new Array(24).fill(0).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 41) * 100}%`,
            top: `${(seededRandom(i + 51) * 110 - frame * (0.13 + seededRandom(i + 61) * 0.17)) % 110}%`,
            width: 10, height: 10, borderRadius: '50%',
            border: `2px solid ${i % 2 ? WHITE : BLACK}`, opacity: 0.3,
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* BEAT 1 — ne valent plus ce qu'elles coûtent */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center' }}>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 32, letterSpacing: 8, color: BLACK }}>EN 2026</div>
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 176, color: WHITE, lineHeight: 0.95, marginTop: 14,
              transform: `translateY(${(1 - sp(10)) * 140}px)`, textShadow: `12px 12px 0px ${BLACK}`,
            }}
          >
            ELLES NE VALENT
          </div>
          <div
            style={{
              display: 'inline-block', backgroundColor: BLACK, color: WHITE, padding: '6px 40px', marginTop: 14,
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 176, lineHeight: 1.05,
              transform: `rotate(-1.6deg) scale(${0.8 + sp(46) * 0.2})`,
            }}
          >
            PLUS LEUR PRIX
          </div>
        </div>

        {/* BEAT 2 — three exploitation levers (animated cards) */}
        <div style={{ position: 'absolute', opacity: b2, width: 1700 }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 76, color: BLACK, textAlign: 'center', marginBottom: 40 }}>
            UN SEUL POINT COMMUN
          </div>
          <div style={{ display: 'flex', gap: 46, justifyContent: 'center' }}>
            {cards.map((c, i) => {
              const s = sp(c.d, { stiffness: 100 });
              const Icon = c.I;
              return (
                <div
                  key={i}
                  style={{
                    width: 500, padding: '44px 36px',
                    backgroundColor: i === 1 ? BLACK : WHITE, color: i === 1 ? WHITE : BLACK,
                    transform: `translateY(${(1 - s) * 340}px) rotate(${(i - 1) * 2.2}deg)`,
                    boxShadow: `16px 16px 0px ${i === 1 ? WHITE : BLACK}`,
                  }}
                >
                  {i === 0 ? <Icon c={i === 1 ? WHITE : BLACK} s={92} f={frame} /> : <Icon c={i === 1 ? WHITE : BLACK} s={92} f={frame} />}
                  <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 96, lineHeight: 1.02, marginTop: 18 }}>{c.t}</div>
                  <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 34, fontWeight: 600, opacity: 0.8, marginTop: 6 }}>{c.s}</div>
                  <div
                    style={{
                      height: 12, marginTop: 22, backgroundColor: RED,
                      width: `${interpolate(frame, [c.d + 40, c.d + 140], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })}%`,
                    }}
                  />
                </div>
              );
            })}
          </div>
        </div>

        {/* BEAT 3 — liberté financière payoff */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center' }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 46, color: BLACK, fontWeight: 700 }}>
            Elles t'empêchent de construire
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 190, color: WHITE, lineHeight: 0.94, marginTop: 16,
              transform: `scale(${0.78 + sp(600) * 0.22})`, textShadow: `14px 14px 0px ${BLACK}`,
            }}
          >
            TA LIBERTÉ
          </div>
          <div
            style={{
              display: 'inline-block', backgroundColor: WHITE, color: BLACK, padding: '4px 44px', marginTop: 10,
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 190, lineHeight: 1.05,
              transform: `rotate(-2deg) translateX(${(1 - sp(660)) * -300}px)`,
            }}
          >
            FINANCIÈRE
          </div>
          {/* ascending value line, micro-animated */}
          <svg width="1200" height="150" viewBox="0 0 1200 150" style={{ marginTop: 30 }}>
            <path
              d="M20 130 L260 112 L500 88 L740 62 L980 34 L1180 12"
              stroke={BLACK} strokeWidth="10" fill="none" strokeLinecap="round"
              strokeDasharray="1400"
              strokeDashoffset={interpolate(frame, [700, 820], [1400, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
            />
            <circle cx="1180" cy="12" r={9 + Math.sin(frame * 0.25) * 3} fill={WHITE} />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE, opacity: 0.45 }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene06TroisLeviers;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07AvertissementAMF',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#8E0B1C';

const ShieldIcon: React.FC<{ c: string; s: number }> = ({ c, s }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none">
    <path d="M24 4l16 6v14c0 10-7 17-16 20-9-3-16-10-16-20V10l16-6Z" stroke={c} strokeWidth="3.5" strokeLinejoin="round" />
    <path d="M16 24l6 6 11-13" stroke={c} strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round" />
  </svg>
);

const DocIcon: React.FC<{ c: string; s: number }> = ({ c, s }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none">
    <path d="M10 4h20l8 8v32H10V4Z" stroke={c} strokeWidth="3.5" strokeLinejoin="round" />
    <path d="M30 4v9h8M17 24h14M17 32h14" stroke={c} strokeWidth="3.2" strokeLinecap="round" />
  </svg>
);

const ScooterIcon: React.FC<{ c: string; s: number; f: number }> = ({ c, s, f }) => (
  <svg width={s} height={s} viewBox="0 0 64 48" fill="none">
    <circle cx="14" cy="36" r="8" stroke={c} strokeWidth="3.5" />
    <circle cx="50" cy="36" r="8" stroke={c} strokeWidth="3.5" />
    <path d="M14 36h22l8-20h8M36 16h10" stroke={c} strokeWidth="3.5" strokeLinecap="round" />
    <rect x="24" y="6" width="16" height="12" stroke={c} strokeWidth="3.5" transform={`rotate(${Math.sin(f * 0.2) * 4} 32 12)`} />
  </svg>
);

const Scene07AvertissementAMF: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: static-ish institutional feel, tiny breathing
  const camScale = 1.02 + Math.sin(frame * 0.02) * 0.012;

  const b1 = win(-1, 22, 300, 330);
  const b2 = win(315, 345, 560, 590);
  const b3 = win(575, 605, 915, 930);

  const notes = [
    { I: DocIcon, t: 'DONNÉES PUBLIQUES', s: 'sources ouvertes, chiffres vérifiables' },
    { I: ShieldIcon, t: 'PAS DE CONSEIL PERSO', s: 'chaque situation est unique' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${160 + Math.sin(frame * 0.008) * 10}deg, #D4142E 0%, ${RED} 50%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <div
        style={{
          position: 'absolute', left: 760, top: -300, width: 800, height: 800, borderRadius: '50%',
          border: `6px solid ${BLACK}`, opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.017) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute', left: -260, bottom: -200, width: 760, height: 500,
          backgroundColor: WHITE, opacity: 0.05,
          transform: `rotate(${12 + Math.sin(frame * 0.012) * 3}deg)`,
        }}
      />

      {/* Motion texture — horizontal ticker lines */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(0deg, ${BLACK} 0px, ${BLACK} 2px, transparent 2px, transparent 26px)`,
          backgroundPosition: `0px ${(frame * 0.8) % 26}px`,
          opacity: 0.08,
        }}
      />

      {/* Floating particles */}
      {new Array(18).fill(0).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 71) * 100}%`,
            top: `${(seededRandom(i + 81) * 110 + frame * (0.12 + seededRandom(i + 91) * 0.14)) % 110}%`,
            width: 12, height: 3, backgroundColor: i % 2 ? WHITE : BLACK, opacity: 0.28,
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        {/* BEAT 1 — disclaimer cards */}
        <div style={{ position: 'absolute', opacity: b1, width: 1560 }}>
          <div
            style={{
              display: 'inline-block', backgroundColor: BLACK, color: WHITE, padding: '10px 30px',
              fontFamily: 'Space Mono, monospace', fontSize: 30, letterSpacing: 6,
              transform: `rotate(-1.5deg) translateY(${(1 - sp(6)) * -80}px)`,
            }}
          >
            AVERTISSEMENT
          </div>
          <div style={{ display: 'flex', gap: 40, marginTop: 34 }}>
            {notes.map((n, i) => {
              const Icon = n.I;
              const s = sp(40 + i * 60);
              return (
                <div
                  key={i}
                  style={{
                    flex: 1, backgroundColor: i ? BLACK : WHITE, color: i ? WHITE : BLACK, padding: '40px 34px',
                    transform: `translateY(${(1 - s) * 280}px) rotate(${i ? 1.6 : -1.6}deg)`,
                    boxShadow: `14px 14px 0px ${i ? WHITE : BLACK}`,
                  }}
                >
                  <Icon c={i ? WHITE : BLACK} s={82} />
                  <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 74, lineHeight: 1.02, marginTop: 16 }}>{n.t}</div>
                  <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 32, opacity: 0.8, marginTop: 8 }}>{n.s}</div>
                </div>
              );
            })}
          </div>
          <div
            style={{
              marginTop: 40, fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif', fontSize: 44, color: WHITE,
              opacity: interpolate(frame, [190, 230], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            Besoin d'un accompagnement adapté ? Un conseiller en gestion de patrimoine agréé AMF.
          </div>
        </div>

        {/* BEAT 2 — AMF badge focus */}
        <div style={{ position: 'absolute', opacity: b2, textAlign: 'center' }}>
          <div style={{ transform: `scale(${0.7 + sp(330) * 0.3})`, display: 'inline-block' }}>
            <div
              style={{
                border: `12px solid ${BLACK}`, backgroundColor: WHITE, padding: '34px 70px',
                transform: `rotate(-2deg)`,
              }}
            >
              <ShieldIcon c={RED} s={140} />
              <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 170, color: BLACK, lineHeight: 1 }}>AMF</div>
              <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 28, letterSpacing: 4, color: BLACK }}>
                CONSEILLER AGRÉÉ
              </div>
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif', fontSize: 92, color: WHITE, marginTop: 34,
              transform: `translateY(${(1 - sp(420)) * 90}px)`, textShadow: `8px 8px 0px ${BLACK}`,
            }}
          >
            BON, ON Y VA.
          </div>
        </div>

        {/* BEAT 3 — chapter card: dépense n°01 */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center' }}>
          <div style={{ opacity: sp(600), transform: `translateX(${(1 - sp(600)) * -260}px)` }}>
            <ScooterIcon c={BLACK} s={220} f={frame} />
          </div>
          <div
            style={{
              fontFamily: 'Space Mono, monospace', fontSize: 40, letterSpacing: 12, color: WHITE, marginTop: 6,
            }}
          >
            DÉPENSE N°01
          </div>
          <div
            style={{
              display: 'inline-block', backgroundColor: WHITE, color: BLACK, padding: '10px 46px', marginTop: 16,
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 172, lineHeight: 1.02,
              transform: `rotate(-1.8deg) scale(${0.78 + sp(650) * 0.22})`, boxShadow: `18px 18px 0px ${BLACK}`,
            }}
          >
            LIVRAISON DE REPAS
          </div>
          <div
            style={{
              fontFamily: 'Montserrat, sans-serif', fontSize: 46, color: BLACK, fontWeight: 700, marginTop: 34,
              opacity: interpolate(frame, [740, 780], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            Et là, je vais peut-être te piquer un peu.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE, opacity: 0.45 }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene07AvertissementAMF;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08QuatreTaps',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#7A0A1A';

const BellIcon: React.FC<{ c: string; s: number; f: number }> = ({ c, s, f }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none" style={{ transform: `rotate(${Math.sin(f * 0.5) * 8}deg)` }}>
    <path d="M12 34V22a12 12 0 0 1 24 0v12l4 5H8l4-5Z" stroke={c} strokeWidth="3.5" strokeLinejoin="round" />
    <path d="M20 41a4 4 0 0 0 8 0" stroke={c} strokeWidth="3.5" strokeLinecap="round" />
  </svg>
);

const Scene08QuatreTaps: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: push-in toward the phone, then pull back
  const camScale = interpolate(frame, [0, 330, 900], [1.0, 1.12, 1.04], { extrapolateRight: 'clamp' });
  const camX = Math.sin(frame * 0.01) * 16;

  const b1 = win(-1, 22, 330, 360);
  const b2 = win(345, 375, 580, 610);
  const b3 = win(595, 625, 915, 930);

  const steps = ['TU OUVRES L\'APP', 'TU FAIS DÉFILER', 'TU CHOISIS', 'TU VALIDES'];

  // Commission ring 30%
  const pct = interpolate(frame, [640, 760], [0, 30], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const circ = 2 * Math.PI * 140;

  // Screen shake on "RUINEUX"
  const shake = frame > 400 && frame < 430 ? Math.sin(frame * 2.8) * 10 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${75 + Math.sin(frame * 0.011) * 6}% 76%, #FF6478 0%, ${RED} 44%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <div
        style={{
          position: 'absolute', left: 60, top: 80, width: 520, height: 520, borderRadius: 40,
          border: `5px solid ${WHITE}`, opacity: 0.07,
          transform: `rotate(${-14 + frame * 0.05}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute', right: -140, bottom: -140, width: 620, height: 620, borderRadius: '50%',
          backgroundColor: BLACK, opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.019) * 0.06})`,
        }}
      />

      {/* Motion texture — scanline sweep */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(0deg, rgba(0,0,0,0.35) 0px, rgba(0,0,0,0.35) 2px, transparent 2px, transparent 8px)`,
          opacity: 0.12,
          transform: `translateY(${(frame * 1.2) % 8}px)`,
        }}
      />

      {/* Floating particles */}
      {new Array(22).fill(0).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 101) * 100}%`,
            top: `${(seededRandom(i + 111) * 110 - frame * (0.14 + seededRandom(i + 121) * 0.18)) % 110}%`,
            width: 7, height: 7, backgroundColor: i % 3 ? WHITE : BLACK, opacity: 0.26, borderRadius: 2,
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX + shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* BEAT 1 — phone mockup + 4 taps */}
        <div style={{ position: 'absolute', opacity: b1, display: 'flex', alignItems: 'center', gap: 90 }}>
          {/* phone frame with scrolling restaurant cards */}
          <div
            style={{
              width: 420, height: 800, borderRadius: 46, backgroundColor: BLACK, padding: 18,
              transform: `rotate(-3deg) translateY(${(1 - sp(8)) * 340}px)`, boxShadow: `18px 18px 0px ${WHITE}`,
            }}
          >
            <div style={{ width: '100%', height: '100%', backgroundColor: WHITE, borderRadius: 32, overflow: 'hidden', position: 'relative' }}>
              <div style={{ position: 'absolute', top: 0, left: 0, right: 0, height: 62, backgroundColor: RED }} />
              <div style={{ position: 'absolute', top: 16, left: 22, fontFamily: 'Space Mono, monospace', fontSize: 24, color: WHITE, letterSpacing: 3 }}>
                LIVRAISON
              </div>
              <div style={{ position: 'absolute', top: 70, left: 0, right: 0, transform: `translateY(${-((frame * 1.6) % 520)}px)` }}>
                {new Array(10).fill(0).map((_, i) => (
                  <div key={i} style={{ margin: '14px 20px' }}>
                    <div style={{ height: 92, backgroundColor: i % 2 ? '#1C1C1C' : '#D4142E', opacity: 0.85 }} />
                    <div style={{ height: 12, width: '60%', backgroundColor: BLACK, opacity: 0.25, marginTop: 8 }} />
                  </div>
                ))}
              </div>
              {/* tap indicator micro-animation */}
              <div
                style={{
                  position: 'absolute', left: 170, top: 520,
                  width: 60, height: 60, borderRadius: '50%', border: `5px solid ${BLACK}`,
                  transform: `scale(${1 + Math.sin(frame * 0.3) * 0.25})`, opacity: 0.7,
                }}
              />
            </div>
          </div>
          <div style={{ width: 900 }}>
            {steps.map((s, i) => (
              <div
                key={i}
                style={{
                  display: 'flex', alignItems: 'center', gap: 24, marginBottom: 22,
                  transform: `translateX(${(1 - sp(50 + i * 40)) * 380}px)`,
                }}
              >
                <div
                  style={{
                    width: 78, height: 78, backgroundColor: BLACK, color: WHITE,
                    fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 46,
                    display: 'flex', alignItems: 'center', justifyContent: 'center',
                  }}
                >
                  0{i + 1}
                </div>
                <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 70, color: WHITE, textShadow: `6px 6px 0px ${BLACK}` }}>{s}</div>
              </div>
            ))}
            <div
              style={{
                display: 'flex', alignItems: 'center', gap: 18, marginTop: 18,
                opacity: interpolate(frame, [240, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}
            >
              <BellIcon c={BLACK} s={72} f={frame} />
              <span style={{ fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif', fontSize: 48, color: BLACK, fontWeight: 700 }}>
                20 minutes plus tard, on sonne en bas
              </span>
            </div>
          </div>
        </div>

        {/* BEAT 2 — simple / rapide / ruineux */}
        <div style={{ position: 'absolute', opacity: b2, textAlign: 'center' }}>
          <div style={{ display: 'flex', gap: 40, justifyContent: 'center' }}>
            {['SIMPLE', 'RAPIDE'].map((t, i) => (
              <div
                key={i}
                style={{
                  backgroundColor: WHITE, color: BLACK, padding: '14px 52px',
                  fontFamily: 'Anton, sans-serif', fontSize: 118,
                  transform: `rotate(${i ? 2 : -2}deg) translateY(${(1 - sp(360 + i * 40)) * 200}px)`,
                  boxShadow: `12px 12px 0px ${BLACK}`,
                }}
              >
                {t}
              </div>
            ))}
          </div>
          <div
            style={{
              marginTop: 50, display: 'inline-block', backgroundColor: BLACK, color: WHITE,
              padding: '18px 70px', fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 210, lineHeight: 1.02,
              transform: `rotate(-2.5deg) scale(${0.6 + sp(430, { stiffness: 90 }) * 0.4})`,
            }}
          >
            RUINEUX
          </div>
        </div>

        {/* BEAT 3 — commission ring 30% */}
        <div style={{ position: 'absolute', opacity: b3, display: 'flex', alignItems: 'center', gap: 100 }}>
          <svg width="400" height="400" viewBox="0 0 380 380" style={{ transform: `scale(${0.85 + sp(610) * 0.15})` }}>
            <circle cx="190" cy="190" r="140" fill="none" stroke={BLACK} strokeWidth="42" opacity="0.4" />
            <circle
              cx="190" cy="190" r="140" fill="none" stroke={WHITE} strokeWidth="42"
              strokeDasharray={circ} strokeDashoffset={circ - (circ * pct) / 100}
              transform="rotate(-90 190 190)"
            />
            <text x="190" y="212" textAnchor="middle" fontFamily="Clash Display, Anton, sans-serif" fontSize="112" fill={WHITE}>
              {pct.toFixed(0)}%
            </text>
          </svg>
          <div style={{ width: 900 }}>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 32, letterSpacing: 6, color: BLACK }}>UBER EATS — FRANCE</div>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 104, color: WHITE, lineHeight: 0.98, marginTop: 10, textShadow: `8px 8px 0px ${BLACK}` }}>
              COMMISSION DE BASE
            </div>
            <div
              style={{
                fontFamily: 'Montserrat, sans-serif', fontSize: 42, color: BLACK, fontWeight: 700, marginTop: 18,
                transform: `translateY(${(1 - sp(700)) * 80}px)`,
              }}
            >
              environ 30 % sur les commandes en livraison
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 43%, rgba(0,0,0,0.57) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE, opacity: 0.45 }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene08QuatreTaps;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09QuiPayeLaCommission',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#8E0B1C';

const PlateIcon: React.FC<{ c: string; s: number }> = ({ c, s }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none">
    <circle cx="24" cy="24" r="19" stroke={c} strokeWidth="3.5" />
    <circle cx="24" cy="24" r="10" stroke={c} strokeWidth="3" />
  </svg>
);

const Scene09QuiPayeLaCommission: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow orbit-pan
  const camX = interpolate(frame, [0, 900], [-34, 34]);
  const camRot = Math.cos(frame * 0.009) * 1.1;

  const b1 = win(-1, 22, 280, 310);
  const b2 = win(295, 325, 560, 590);
  const b3 = win(575, 605, 915, 930);

  const platforms = [
    { n: 'UBER EATS', lo: 30, hi: 30, d: 30 },
    { n: 'DELIVEROO', lo: 25, hi: 32, d: 80 },
  ];

  const priceSalle = 14;
  const priceApp = interpolate(frame, [640, 760], [14, 19], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${300 + Math.sin(frame * 0.01) * 20}deg, ${DEEP} 0%, ${RED} 52%, #FF6478 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <svg style={{ position: 'absolute', left: 200, bottom: -220, opacity: 0.06, transform: `rotate(${frame * 0.05}deg)` }} width="700" height="700" viewBox="0 0 100 100">
        <polygon points="50,4 96,92 4,92" fill={BLACK} />
      </svg>
      <div
        style={{
          position: 'absolute', right: 60, top: -180, width: 600, height: 600,
          border: `5px solid ${WHITE}`, opacity: 0.07, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.022) * 0.06})`,
        }}
      />

      {/* Motion texture — halftone + drift */}
      <AbsoluteFill
        style={{
          backgroundImage: `radial-gradient(${WHITE} 2px, transparent 2.2px)`,
          backgroundSize: '34px 34px',
          backgroundPosition: `${(frame * -0.5) % 34}px ${(frame * 0.3) % 34}px`,
          opacity: 0.09,
        }}
      />

      {/* Floating particles */}
      {new Array(20).fill(0).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 131) * 100}%`,
            top: `${(seededRandom(i + 141) * 110 + frame * (0.11 + seededRandom(i + 151) * 0.19)) % 110}%`,
            width: 9, height: 9, border: `2px solid ${i % 2 ? BLACK : WHITE}`, opacity: 0.28,
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px) rotate(${camRot}deg)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* BEAT 1 — platform commission bars */}
        <div style={{ position: 'absolute', opacity: b1, width: 1500 }}>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 32, letterSpacing: 6, color: BLACK, marginBottom: 26 }}>
            COMMISSIONS PLATEFORMES — FRANCE
          </div>
          {platforms.map((p, i) => {
            const grow = interpolate(frame, [40 + i * 60, 150 + i * 60], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
            });
            return (
              <div key={i} style={{ marginBottom: 40 }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline' }}>
                  <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 74, color: WHITE, textShadow: `6px 6px 0px ${BLACK}` }}>{p.n}</span>
                  <span style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 92, color: BLACK }}>
                    {p.lo === p.hi ? `≈ ${(p.hi * grow).toFixed(0)} %` : `${(p.lo * grow).toFixed(0)}–${(p.hi * grow).toFixed(0)} %`}
                  </span>
                </div>
                <div style={{ height: 60, backgroundColor: 'rgba(0,0,0,0.3)', marginTop: 10 }}>
                  <div style={{ width: `${p.hi * 3 * grow}%`, height: '100%', backgroundColor: i ? BLACK : WHITE }} />
                </div>
              </div>
            );
          })}
          <div
            style={{
              display: 'inline-block', marginTop: 10, backgroundColor: BLACK, color: WHITE, padding: '12px 30px',
              fontFamily: 'Montserrat, sans-serif', fontSize: 38, fontWeight: 700,
              transform: `rotate(-1.4deg) translateY(${(1 - sp(200)) * 120}px)`,
            }}
          >
            Prélevé sur chaque commande, sans exception
          </div>
        </div>

        {/* BEAT 2 — qui absorbe ? */}
        <div style={{ position: 'absolute', opacity: b2, textAlign: 'center' }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 96, color: BLACK, lineHeight: 1 }}>
            LE RESTAURATEUR ABSORBE ?
          </div>
          <div
            style={{
              display: 'inline-block', marginTop: 24, backgroundColor: WHITE, color: RED,
              padding: '10px 70px', fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 230, lineHeight: 1.02,
              transform: `rotate(-3deg) scale(${0.6 + sp(340, { stiffness: 95 }) * 0.4})`, boxShadow: `20px 20px 0px ${BLACK}`,
            }}
          >
            NON
          </div>
          <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 30, marginTop: 44, opacity: sp(420) }}>
            <PlateIcon c={WHITE} s={80} />
            <svg width="180" height="60" viewBox="0 0 180 60">
              <path d="M10 30h150M160 30l-26-18M160 30l-26 18" stroke={WHITE} strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 40, color: WHITE, letterSpacing: 3 }}>PRIX APP ▲</div>
          </div>
        </div>

        {/* BEAT 3 — 14 € salle vs 18–19 € app */}
        <div style={{ position: 'absolute', opacity: b3, width: 1560 }}>
          <div style={{ fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif', fontSize: 56, color: BLACK, textAlign: 'center', marginBottom: 40 }}>
            Le surcoût est répercuté sur le prix affiché
          </div>
          <div style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'center', gap: 120, height: 420 }}>
            {[
              { l: 'EN SALLE', v: priceSalle, h: 210, bg: WHITE, fg: BLACK },
              { l: 'SUR L\'APP', v: priceApp, h: 210 + (priceApp - 14) * 34, bg: BLACK, fg: WHITE },
            ].map((c, i) => (
              <div key={i} style={{ textAlign: 'center', width: 420 }}>
                <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 122, color: i ? BLACK : WHITE }}>
                  {c.v.toFixed(0)} €
                </div>
                <div
                  style={{
                    height: c.h, backgroundColor: c.bg, marginTop: 14,
                    boxShadow: `14px 14px 0px ${i ? WHITE : BLACK}`,
                  }}
                />
                <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 32, letterSpacing: 4, color: WHITE, marginTop: 18 }}>{c.l}</div>
              </div>
            ))}
          </div>
          <div
            style={{
              textAlign: 'center', marginTop: 34,
              opacity: interpolate(frame, [790, 830], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            <span
              style={{
                backgroundColor: WHITE, color: BLACK, padding: '10px 34px',
                fontFamily: 'Anton, sans-serif', fontSize: 78, display: 'inline-block', transform: 'rotate(-1.6deg)',
              }}
            >
              +4 À +5 € AVANT MÊME LES FRAIS
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.56) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE, opacity: 0.45 }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene09QuiPayeLaCommission;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10LaNoteFinale',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WHITE = '#F4F2F2';
const BLACK = '#000000';
const RED = '#F62440';
const DEEP = '#7A0A1A';

const PinIcon: React.FC<{ c: string; s: number }> = ({ c, s }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none">
    <path d="M24 44s14-14 14-24A14 14 0 0 0 10 20c0 10 14 24 14 24Z" stroke={c} strokeWidth="3.5" strokeLinejoin="round" />
    <circle cx="24" cy="20" r="5" stroke={c} strokeWidth="3.5" />
  </svg>
);

const PercentIcon: React.FC<{ c: string; s: number; f: number }> = ({ c, s, f }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none" style={{ transform: `rotate(${Math.sin(f * 0.08) * 10}deg)` }}>
    <circle cx="14" cy="14" r="7" stroke={c} strokeWidth="3.5" />
    <circle cx="34" cy="34" r="7" stroke={c} strokeWidth="3.5" />
    <path d="M8 40 40 8" stroke={c} strokeWidth="3.5" strokeLinecap="round" />
  </svg>
);

const PlusIcon: React.FC<{ c: string; s: number }> = ({ c, s }) => (
  <svg width={s} height={s} viewBox="0 0 48 48" fill="none">
    <path d="M24 8v32M8 24h32" stroke={c} strokeWidth="5" strokeLinecap="round" />
  </svg>
);

const Scene10LaNoteFinale: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const win = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: receipt-style vertical travel then hold
  const camY = interpolate(frame, [0, 560, 900], [0, -20, 10], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, 900], [1.02, 1.1]);

  const b1 = win(-1, 22, 330, 360);
  const b2 = win(345, 375, 620, 650);
  const b3 = win(635, 665, 915, 930);

  const lines = [
    { I: PinIcon, l: 'FRAIS DE LIVRAISON', v: '0,49 – 5,49 €', s: 'selon la distance', d: 30 },
    { I: PercentIcon, l: 'FRAIS DE SERVICE', v: '10 %', s: 'du montant de la commande', d: 100 },
    { I: PlusIcon, l: 'SUPPLÉMENT', v: 'PARFOIS', s: 'commande minimum non atteinte', d: 170 },
  ];

  const total = interpolate(frame, [400, 540], [12, 24], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const shake = frame > 545 && frame < 575 ? Math.sin(frame * 2.6) * 9 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: RED, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(ellipse at 30% ${80 - Math.sin(frame * 0.012) * 10}%, #FF6E80 0%, ${RED} 46%, ${DEEP} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <div
        style={{
          position: 'absolute', left: -180, top: 200, width: 680, height: 680,
          backgroundColor: BLACK, opacity: 0.05, borderRadius: 50,
          transform: `rotate(${24 + Math.sin(frame * 0.014) * 6}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute', right: -220, top: -120, width: 780, height: 780, borderRadius: '50%',
          border: `6px solid ${WHITE}`, opacity: 0.07,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />

      {/* Motion texture — receipt perforation lines */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(90deg, ${BLACK} 0px, ${BLACK} 8px, transparent 8px, transparent 22px)`,
          backgroundSize: '100% 4px',
          backgroundPosition: `${(frame * 0.6) % 22}px 0px`,
          opacity: 0.1,
        }}
      />

      {/* Floating particles */}
      {new Array(24).fill(0).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 161) * 100}%`,
            top: `${(seededRandom(i + 171) * 110 - frame * (0.15 + seededRandom(i + 181) * 0.2)) % 110}%`,
            width: 6, height: 14, backgroundColor: i % 3 ? WHITE : BLACK, opacity: 0.24,
            transform: `rotate(${seededRandom(i + 191) * 40 - 20}deg)`,
          }}
        />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateY(${camY + shake}px) scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        {/* BEAT 1 — fee stack building like a receipt */}
        <div style={{ position: 'absolute', opacity: b1, width: 1480 }}>
          <div
            style={{
              display: 'inline-block', backgroundColor: BLACK, color: WHITE, padding: '10px 28px',
              fontFamily: 'Space Mono, monospace', fontSize: 30, letterSpacing: 6,
              transform: `translateY(${(1 - sp(4)) * -70}px)`,
            }}
          >
            CE N'EST QUE LE PRIX DU PLAT
          </div>
          <div style={{ marginTop: 30 }}>
            {lines.map((n, i) => {
              const Icon = n.I;
              const s = sp(n.d);
              return (
                <div
                  key={i}
                  style={{
                    display: 'flex', alignItems: 'center', gap: 26, padding: '22px 30px', marginBottom: 20,
                    backgroundColor: i === 1 ? BLACK : WHITE, color: i === 1 ? WHITE : BLACK,
                    transform: `translateX(${(1 - s) * (i % 2 ? 400 : -400)}px) rotate(${i % 2 ? 1.2 : -1.2}deg)`,
                    boxShadow: `12px 12px 0px ${i === 1 ? WHITE : BLACK}`,
                  }}
                >
                  <Icon c={i === 1 ? WHITE : BLACK} s={62} f={frame} />
                  <div>
                    <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 60, lineHeight: 1 }}>{n.l}</div>
                    <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 28, opacity: 0.75 }}>{n.s}</div>
                  </div>
                  <div style={{ marginLeft: 'auto', fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 76 }}>{n.v}</div>
                </div>
              );
            })}
          </div>
        </div>

        {/* BEAT 2 — total counter 12 € → 22-25 € */}
        <div style={{ position: 'absolute', opacity: b2, textAlign: 'center' }}>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 34, letterSpacing: 8, color: BLACK }}>
            AU COMPTOIR 12 € — LIVRÉ CHEZ TOI
          </div>
          <div
            style={{
              fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 260, color: WHITE, lineHeight: 0.95, marginTop: 10,
              textShadow: `16px 16px 0px ${BLACK}`,
            }}
          >
            {total.toFixed(0)} €
          </div>
          <div
            style={{
              display: 'inline-block', backgroundColor: BLACK, color: WHITE, padding: '10px 44px',
              fontFamily: 'Anton, sans-serif', fontSize: 96, transform: `rotate(-2deg) scale(${0.7 + sp(560) * 0.3})`,
            }}
          >
            22 À 25 € RÉELS
          </div>
        </div>

        {/* BEAT 3 — comparison stack + verdict */}
        <div style={{ position: 'absolute', opacity: b3, width: 1560 }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 82, color: BLACK, textAlign: 'center' }}>
            LE MÊME REPAS, DEUX PRIX
          </div>
          <div style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'center', gap: 140, height: 430, marginTop: 30 }}>
            {[
              { l: 'COMPTOIR', v: '12 €', h: 190, bg: WHITE, fg: BLACK },
              { l: 'LIVRÉ', v: '24 €', h: 380, bg: BLACK, fg: WHITE },
            ].map((c, i) => {
              const grow = interpolate(frame, [670 + i * 50, 780 + i * 50], [0, 1], {
                extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
              });
              return (
                <div key={i} style={{ width: 400, textAlign: 'center' }}>
                  <div style={{ fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 118, color: i ? BLACK : WHITE }}>{c.v}</div>
                  <div style={{ height: c.h * grow, backgroundColor: c.bg, marginTop: 12, boxShadow: `14px 14px 0px ${i ? WHITE : BLACK}` }} />
                  <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 30, letterSpacing: 4, color: WHITE, marginTop: 16 }}>{c.l}</div>
                </div>
              );
            })}
          </div>
          <div
            style={{
              textAlign: 'center', marginTop: 26,
              opacity: interpolate(frame, [830, 870], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            <span
              style={{
                backgroundColor: WHITE, color: RED, padding: '8px 40px', display: 'inline-block',
                fontFamily: 'Clash Display, Anton, sans-serif', fontSize: 110, transform: 'rotate(-1.5deg)',
                boxShadow: `12px 12px 0px ${BLACK}`,
              }}
            >
              LE DOUBLE
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 43%, rgba(0,0,0,0.57) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600, color: WHITE, opacity: 0.45 }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene10LaNoteFinale;
```

