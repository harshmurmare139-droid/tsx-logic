```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11HousingMachine',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11HousingMachine: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1.14], { extrapolateRight: 'clamp' });

  // Rising rent bars
  const bars = [40, 70, 110, 160, 220];
  const houseDrop = sp(6, { damping: 9 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#163029', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, #2f5f56 0%, #23483f 45%, #10231e 100%)',
        transform: `scale(${camZoom})`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '10%', left: '60%', width: 620, height: 620,
          border: '2px solid #EBEDE3', borderRadius: '50%', opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame / 60) * 0.05}) rotate(${frame * 0.2}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '-8%', left: '-6%', width: 520, height: 520,
          background: '#FFF449', opacity: 0.04, borderRadius: 40,
          transform: `rotate(${45 + frame * 0.1}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (grid lines) */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: `${i * 7.5}%`, top: 0, width: 1, height: '100%',
            background: '#EBEDE3', transform: `translateY(${Math.sin(frame / 40 + i) * 12}px)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 26 }).map((_, i) => {
          const r = seededRandom(i + 1);
          const y = (r * 1080 + frame * (0.4 + r)) % 1080;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${seededRandom(i + 9) * 100}%`, top: y,
              width: 3 + r * 4, height: 3 + r * 4, borderRadius: '50%',
              background: i % 3 === 0 ? '#FFF449' : '#EBEDE3', opacity: 0.25,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ padding: '90px 120px', justifyContent: 'center' }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace', fontSize: 30, letterSpacing: 6,
          color: '#FFF449', opacity: sp(2), marginBottom: 14,
          transform: `translateX(${interpolate(sp(2), [0, 1], [-40, 0])}px)`,
        }}>MACHINE 02</div>

        <div style={{
          fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 128, lineHeight: 0.95,
          color: '#EBEDE3', opacity: sp(6),
          transform: `translateY(${interpolate(sp(6), [0, 1], [60, 0])}px)`,
        }}>HOUSING</div>

        {/* Rent rising bars + house icon */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 22, marginTop: 60, height: 260 }}>
          {bars.map((h, i) => {
            const grow = sp(20 + i * 8, { damping: 11 });
            return (
              <div key={i} style={{
                width: 70, height: h * grow, borderRadius: '8px 8px 0 0',
                background: i === bars.length - 1 ? '#FFF449' : '#5FB89F', opacity: 0.9,
              }} />
            );
          })}
          {/* House icon SVG */}
          <svg width="180" height="180" viewBox="0 0 100 100" style={{
            marginLeft: 40, opacity: houseDrop,
            transform: `translateY(${interpolate(houseDrop, [0, 1], [-120, 0])}px)`,
          }}>
            <path d="M50 15 L88 45 L78 45 L78 85 L22 85 L22 45 L12 45 Z"
              fill="none" stroke="#FFF449" strokeWidth="4" strokeLinejoin="round" />
            <rect x="42" y="60" width="16" height="25" fill="#FFF449" opacity="0.85" />
          </svg>
        </div>

        <div style={{
          fontFamily: 'Inter, sans-serif', fontWeight: 600, fontSize: 48, color: '#EBEDE3',
          marginTop: 46, opacity: sp(60), maxWidth: 1200,
        }}>Bidding wars, soaring rent & depleted savings push households to the edge.</div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        boxShadow: 'inset 0 0 320px 90px rgba(0,0,0,0.6)', pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene11HousingMachine;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12FundDrain',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12FundDrain: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Draining gauge 100% -> ~50%
  const drain = interpolate(frame, [30, 150], [1, 0.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const bnplReveal = sp(180, { damping: 10 });
  const camPan = interpolate(frame, [0, durationInFrames], [-20, 20], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#12241f', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #10231e 0%, #2c5951 55%, #163029 100%)',
        transform: `translateX(${camPan}px) scale(1.1)`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '20%', left: '12%', width: 480, height: 480,
          border: '2px solid #FFF449', opacity: 0.05, borderRadius: 30,
          transform: `rotate(${frame * -0.15}deg) scale(${1 + Math.cos(frame / 55) * 0.06})`,
        }} />
        <div style={{
          position: 'absolute', bottom: '-10%', right: '5%', width: 0, height: 0,
          borderLeft: '260px solid transparent', borderRight: '260px solid transparent',
          borderBottom: '440px solid #EBEDE3', opacity: 0.04,
          transform: `rotate(${frame * 0.1}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (concentric circles) */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {Array.from({ length: 6 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: '50%', left: '25%',
            width: 120 + i * 130, height: 120 + i * 130, marginTop: -(60 + i * 65), marginLeft: -(60 + i * 65),
            border: '1px solid #EBEDE3', borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame / 50 + i) * 0.04})`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const r = seededRandom(i + 3);
          const y = (r * 1080 - frame * (0.5 + r)) % 1080;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${seededRandom(i + 5) * 100}%`, top: (y + 1080) % 1080,
              width: 2 + r * 5, height: 2 + r * 5, borderRadius: '50%',
              background: '#EBEDE3', opacity: 0.2,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ padding: '80px 110px', justifyContent: 'center' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 80 }}>
          {/* Draining battery/fund gauge */}
          <div style={{ opacity: sp(10) }}>
            <svg width="260" height="420" viewBox="0 0 100 160">
              <rect x="10" y="10" width="80" height="140" rx="10" fill="none" stroke="#EBEDE3" strokeWidth="4" />
              <rect x="20" y={20 + (120 * (1 - drain))} width="60" height={120 * drain} rx="4"
                fill={drain < 0.6 ? '#E8664F' : '#5FB89F'} />
            </svg>
          </div>

          <div>
            <div style={{
              fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 200, lineHeight: 0.9,
              color: '#FFF449', opacity: sp(30),
            }}>{Math.round(interpolate(drain, [1, 0.5], [0, 50]))}%</div>
            <div style={{
              fontFamily: 'Inter, sans-serif', fontWeight: 700, fontSize: 52, color: '#EBEDE3',
              opacity: sp(50), maxWidth: 760,
            }}>of lower-income households exhausted their emergency funds.</div>
          </div>
        </div>

        {/* BNPL reveal */}
        <div style={{
          marginTop: 60, opacity: bnplReveal,
          transform: `translateY(${interpolate(bnplReveal, [0, 1], [50, 0])}px)`,
          display: 'flex', alignItems: 'center', gap: 24,
        }}>
          <div style={{ width: 14, height: 14, background: '#E8664F', borderRadius: '50%' }} />
          <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 42, letterSpacing: 4, color: '#FFF449' }}>
            MACHINE 03 → BUY NOW, PAY LATER
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px 80px rgba(0,0,0,0.62)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene12FundDrain;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13EcommerceTriple',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13EcommerceTriple: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const bar2020 = sp(40, { damping: 11 });
  const bar2024 = sp(70, { damping: 11 });
  const camTilt = interpolate(frame, [0, durationInFrames], [0, -3], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#14271f', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 80%, #316054 0%, #22463d 50%, #0f221c 100%)',
        transform: `rotate(${camTilt}deg) scale(1.12)`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '5%', right: '10%', width: 400, height: 400,
          background: '#5FB89F', opacity: 0.05, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame / 45) * 0.07})`,
        }} />
        <div style={{
          position: 'absolute', bottom: '8%', left: '4%', width: 360, height: 360,
          border: '2px solid #FFF449', opacity: 0.05,
          transform: `rotate(${frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (diagonal lines) */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: `${i * 9}%`, left: '-10%', width: '120%', height: 1,
            background: '#EBEDE3', transform: `rotate(${-8}deg) translateX(${Math.sin(frame / 50 + i) * 20}px)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const r = seededRandom(i + 7);
          const x = (r * 1920 + frame * (0.6 + r)) % 1920;
          return (
            <div key={i} style={{
              position: 'absolute', top: `${seededRandom(i + 2) * 100}%`, left: x,
              width: 3 + r * 4, height: 3 + r * 4, borderRadius: '50%',
              background: i % 4 === 0 ? '#FFF449' : '#EBEDE3', opacity: 0.22,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ padding: '80px 120px', justifyContent: 'center' }}>
        <div style={{
          fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 96, color: '#EBEDE3',
          opacity: sp(4), lineHeight: 1,
        }}>1 IN 4 <span style={{ color: '#FFF449' }}>USE BNPL</span></div>

        {/* Growth bars 2% -> 6% */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 120, marginTop: 70, height: 320 }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{
              width: 130, height: 80 * bar2020, background: '#5FB89F', borderRadius: 8, opacity: 0.9,
            }} />
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 40, color: '#EBEDE3', marginTop: 16 }}>2% · 2020</div>
          </div>

          {/* Arrow icon */}
          <svg width="120" height="120" viewBox="0 0 100 100" style={{ opacity: sp(90), marginBottom: 80 }}>
            <path d="M15 60 L70 60 M70 60 L52 42 M70 60 L52 78" stroke="#FFF449" strokeWidth="6" fill="none" strokeLinecap="round" />
          </svg>

          <div style={{ textAlign: 'center' }}>
            <div style={{
              width: 130, height: 260 * bar2024, background: '#FFF449', borderRadius: 8,
            }} />
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 40, color: '#FFF449', marginTop: 16 }}>6% · 2024</div>
          </div>

          {/* Cart icon */}
          <svg width="170" height="170" viewBox="0 0 100 100" style={{
            opacity: sp(110), marginLeft: 60, marginBottom: 40,
            transform: `translateY(${Math.sin(frame / 20) * 8}px)`,
          }}>
            <path d="M15 20 H28 L36 62 H78 L86 32 H32" fill="none" stroke="#EBEDE3" strokeWidth="5" strokeLinejoin="round" />
            <circle cx="40" cy="78" r="6" fill="#EBEDE3" />
            <circle cx="72" cy="78" r="6" fill="#EBEDE3" />
          </svg>
        </div>

        <div style={{
          fontFamily: 'Inter, sans-serif', fontWeight: 600, fontSize: 46, color: '#EBEDE3',
          marginTop: 40, opacity: sp(130),
        }}>Now used for clothing & everyday groceries.</div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px 80px rgba(0,0,0,0.6)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene13EcommerceTriple;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14GroceryDebt',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14GroceryDebt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const shake = frame > 40 && frame < 55 ? Math.sin(frame * 3) * 6 : 0;
  const camZoom = interpolate(frame, [0, durationInFrames], [1.15, 1.05], { extrapolateRight: 'clamp' });

  // Count-up 60%
  const pct60 = Math.round(interpolate(sp(150), [0, 1], [0, 60]));

  return (
    <AbsoluteFill style={{ backgroundColor: '#132520', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #0f221c 0%, #2b564c 60%, #17302a 100%)',
        transform: `scale(${camZoom}) translateX(${shake}px)`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '15%', left: '55%', width: 500, height: 500,
          border: '3px solid #E8664F', opacity: 0.05, borderRadius: 40,
          transform: `rotate(${frame * 0.12}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '-6%', left: '-4%', width: 420, height: 420,
          background: '#FFF449', opacity: 0.04, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame / 50) * 0.06})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (dots grid) */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 60 }).map((_, i) => {
          const col = i % 10, row = Math.floor(i / 10);
          return (
            <div key={i} style={{
              position: 'absolute', left: `${5 + col * 10}%`, top: `${8 + row * 15}%`,
              width: 4, height: 4, borderRadius: '50%', background: '#EBEDE3',
              opacity: 0.5 + Math.sin(frame / 30 + i) * 0.5,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 20 }).map((_, i) => {
          const r = seededRandom(i + 11);
          const y = (r * 1080 + frame * (0.5 + r)) % 1080;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${seededRandom(i + 4) * 100}%`, top: y,
              width: 3 + r * 3, height: 3 + r * 3, borderRadius: '50%',
              background: '#EBEDE3', opacity: 0.2,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ padding: '80px 120px', justifyContent: 'center' }}>
        {/* Grocery bag icon with debt tag */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 60, marginBottom: 40 }}>
          <svg width="200" height="200" viewBox="0 0 100 100" style={{
            opacity: sp(6), transform: `translateY(${interpolate(sp(6), [0, 1], [-60, 0])}px)`,
          }}>
            <path d="M25 35 H75 L70 88 H30 Z" fill="none" stroke="#EBEDE3" strokeWidth="4" strokeLinejoin="round" />
            <path d="M38 35 V26 A12 12 0 0 1 62 26 V35" fill="none" stroke="#FFF449" strokeWidth="4" />
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 100, color: '#FFF449',
            opacity: sp(20), lineHeight: 0.95,
          }}>FINANCING<br /><span style={{ color: '#EBEDE3' }}>GROCERIES</span></div>
        </div>

        {/* Two stats */}
        <div style={{ display: 'flex', gap: 100, marginTop: 30 }}>
          <div style={{ opacity: sp(90) }}>
            <div style={{ fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 130, color: '#EBEDE3' }}>1/4</div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 42, color: '#EBEDE3', opacity: 0.85 }}>regret using BNPL</div>
          </div>
          <div style={{ opacity: sp(140), borderLeft: '3px solid #E8664F', paddingLeft: 60 }}>
            <div style={{ fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 130, color: '#E8664F' }}>{pct60}%</div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 42, color: '#EBEDE3', opacity: 0.85 }}>bought what they couldn't afford</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 310px 85px rgba(0,0,0,0.62)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene14GroceryDebt;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15InvisibleDebt',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15InvisibleDebt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Eye blink / invisibility flicker
  const flicker = 0.4 + Math.abs(Math.sin(frame / 25)) * 0.6;
  const camZoom = interpolate(frame, [0, durationInFrames], [1.06, 1.16], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#0f201b', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 45%, #2a544a 0%, #1a352e 55%, #0b1a16 100%)',
        transform: `scale(${camZoom})`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '50%', left: '50%', width: 900, height: 900,
          marginLeft: -450, marginTop: -450, border: '1px dashed #EBEDE3', borderRadius: '50%',
          opacity: 0.06, transform: `rotate(${frame * 0.15}deg)`,
        }} />
        <div style={{
          position: 'absolute', top: '50%', left: '50%', width: 600, height: 600,
          marginLeft: -300, marginTop: -300, border: '1px dashed #FFF449', borderRadius: '50%',
          opacity: 0.06, transform: `rotate(${-frame * 0.22}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (scanning line) */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        <div style={{
          position: 'absolute', left: 0, width: '100%', height: 3, background: '#FFF449',
          top: `${(frame * 1.2) % 100}%`,
        }} />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 28 }).map((_, i) => {
          const r = seededRandom(i + 13);
          const y = (r * 1080 - frame * (0.4 + r)) % 1080;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${seededRandom(i + 6) * 100}%`, top: (y + 1080) % 1080,
              width: 2 + r * 4, height: 2 + r * 4, borderRadius: '50%',
              background: '#EBEDE3', opacity: 0.18,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        {/* Invisible eye icon */}
        <svg width="260" height="150" viewBox="0 0 120 70" style={{ opacity: sp(6) * flicker, marginBottom: 40 }}>
          <path d="M10 35 Q60 5 110 35 Q60 65 10 35 Z" fill="none" stroke="#FFF449" strokeWidth="4" />
          <circle cx="60" cy="35" r="14" fill="none" stroke="#EBEDE3" strokeWidth="4" />
          <line x1="20" y1="60" x2="100" y2="10" stroke="#E8664F" strokeWidth="4"
            strokeDasharray="90" strokeDashoffset={interpolate(sp(30), [0, 1], [90, 0])} />
        </svg>

        <div style={{
          fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 150, lineHeight: 0.9,
          color: '#EBEDE3', opacity: sp(20), textAlign: 'center',
          textShadow: '0 0 40px rgba(255,244,73,0.15)',
        }}>PARALLEL<br /><span style={{ color: '#FFF449', opacity: flicker }}>DEBT UNIVERSE</span></div>

        <div style={{
          fontFamily: 'Inter, sans-serif', fontWeight: 600, fontSize: 46, color: '#EBEDE3',
          marginTop: 44, opacity: sp(70), textAlign: 'center', maxWidth: 1100,
        }}>Sold as flexibility · invisible until payments fail · a massive regulatory blind spot.</div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 340px 100px rgba(0,0,0,0.68)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene15InvisibleDebt;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16GenerationSqueeze',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ring: React.FC<{ pct: number; color: string; progress: number; label: string; sub: string }> = ({ pct, color, progress, label, sub }) => {
  const R = 90, C = 2 * Math.PI * R;
  const shown = pct * progress;
  return (
    <div style={{ textAlign: 'center' }}>
      <svg width="260" height="260" viewBox="0 0 220 220">
        <circle cx="110" cy="110" r={R} fill="none" stroke="#EBEDE3" strokeWidth="16" opacity="0.15" />
        <circle cx="110" cy="110" r={R} fill="none" stroke={color} strokeWidth="16" strokeLinecap="round"
          strokeDasharray={C} strokeDashoffset={C - (C * shown) / 100} transform="rotate(-90 110 110)" />
        <text x="110" y="122" textAnchor="middle" fontFamily="Mont Heavy, sans-serif" fontWeight="900"
          fontSize="56" fill={color}>{Math.round(shown)}%</text>
      </svg>
      <div style={{ fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 52, color: '#EBEDE3', marginTop: 8 }}>{label}</div>
      <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 32, color: '#EBEDE3', opacity: 0.7 }}>{sub}</div>
    </div>
  );
};

const Scene16GenerationSqueeze: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [15, -15], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#142720', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(120deg, #0f221c 0%, #2d5a50 55%, #142c26 100%)',
        transform: `translateX(${camPan}px) scale(1.1)`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '-8%', left: '20%', width: 480, height: 480,
          background: '#FFF449', opacity: 0.04, borderRadius: 60,
          transform: `rotate(${30 + frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '5%', right: '8%', width: 420, height: 420,
          border: '2px solid #EBEDE3', opacity: 0.05, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame / 55) * 0.06})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (vertical bars) */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: `${i * 5}%`, bottom: 0, width: 2,
            height: `${20 + Math.abs(Math.sin(frame / 30 + i)) * 40}%`, background: '#EBEDE3',
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const r = seededRandom(i + 17);
          const y = (r * 1080 + frame * (0.45 + r)) % 1080;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${seededRandom(i + 8) * 100}%`, top: y,
              width: 3 + r * 3, height: 3 + r * 3, borderRadius: '50%',
              background: i % 3 === 0 ? '#FFF449' : '#EBEDE3', opacity: 0.2,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ padding: '70px 110px', justifyContent: 'center' }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace', fontSize: 32, letterSpacing: 5, color: '#FFF449',
          opacity: sp(2), marginBottom: 30,
        }}>LIVING PAYCHECK TO PAYCHECK</div>

        <div style={{ display: 'flex', gap: 140, marginBottom: 40, opacity: sp(20) }}>
          <Ring pct={72} color="#FFF449" progress={sp(24)} label="GEN Z" sub="72%" />
          <Ring pct={65} color="#5FB89F" progress={sp(50)} label="MILLENNIALS" sub="65%" />
        </div>

        <div style={{
          fontFamily: 'Inter, sans-serif', fontWeight: 600, fontSize: 46, color: '#EBEDE3',
          opacity: sp(90), maxWidth: 1300,
        }}>They entered adulthood through a pandemic, inflation, unaffordable housing & BNPL.</div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px 80px rgba(0,0,0,0.6)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene16GenerationSqueeze;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17EmergencyQuestion',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17EmergencyQuestion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const pulse = 1 + Math.sin(frame / 15) * 0.03;
  const camZoom = interpolate(frame, [0, durationInFrames], [1.12, 1.04], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#12241e', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 60%, #305c51 0%, #1d3b33 55%, #0d1e19 100%)',
        transform: `scale(${camZoom})`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '10%', left: '5%', width: 520, height: 520,
          border: '2px solid #E8664F', opacity: 0.05, borderRadius: 40,
          transform: `rotate(${frame * -0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '-10%', right: '10%', width: 560, height: 560,
          background: '#FFF449', opacity: 0.04, borderRadius: '50%',
          transform: `scale(${1 + Math.cos(frame / 50) * 0.06})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (radar rings) */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {[0, 1, 2].map((i) => {
          const s = ((frame + i * 40) % 120) / 120;
          return (
            <div key={i} style={{
              position: 'absolute', top: '50%', left: '50%',
              width: 200 + s * 900, height: 200 + s * 900,
              marginLeft: -(100 + s * 450), marginTop: -(100 + s * 450),
              border: '1px solid #FFF449', borderRadius: '50%', opacity: 1 - s,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const r = seededRandom(i + 19);
          const y = (r * 1080 - frame * (0.5 + r)) % 1080;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${seededRandom(i + 3) * 100}%`, top: (y + 1080) % 1080,
              width: 3 + r * 3, height: 3 + r * 3, borderRadius: '50%',
              background: '#EBEDE3', opacity: 0.2,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ padding: '70px 110px', justifyContent: 'center' }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontWeight: 700, fontSize: 46, color: '#5FB89F',
          opacity: sp(2), marginBottom: 20,
        }}>Younger consumers · 2× more likely to pay late</div>

        <div style={{ display: 'flex', alignItems: 'center', gap: 50 }}>
          {/* Warning + question icon */}
          <svg width="180" height="180" viewBox="0 0 100 100" style={{
            opacity: sp(30), transform: `scale(${pulse})`,
          }}>
            <circle cx="50" cy="50" r="42" fill="none" stroke="#E8664F" strokeWidth="5" />
            <text x="50" y="72" textAnchor="middle" fontFamily="Mont Heavy" fontWeight="900" fontSize="60" fill="#E8664F">?</text>
          </svg>

          <div style={{
            fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 180, lineHeight: 0.9,
            color: '#FFF449', opacity: sp(24),
            transform: `translateX(${interpolate(sp(24), [0, 1], [80, 0])}px) scale(${pulse})`,
          }}>$1,000</div>
        </div>

        <div style={{
          fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 72, color: '#EBEDE3',
          marginTop: 30, opacity: sp(70),
        }}>EMERGENCY TOMORROW.</div>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontWeight: 600, fontSize: 50, color: '#EBEDE3',
          marginTop: 18, opacity: sp(100),
        }}>Where would the money come from?</div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px 90px rgba(0,0,0,0.64)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene17EmergencyQuestion;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18CreditCardAPR',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const IconCard: React.FC<{ progress: number; delay: number; label: string; children: React.ReactNode }> = ({ progress, label, children }) => (
  <div style={{
    background: 'rgba(235,237,227,0.06)', border: '1px solid rgba(235,237,227,0.2)', borderRadius: 20,
    padding: '30px 26px', width: 250, textAlign: 'center',
    opacity: progress, transform: `translateY(${interpolate(progress, [0, 1], [50, 0])}px)`,
  }}>
    {children}
    <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 600, fontSize: 34, color: '#EBEDE3', marginTop: 16 }}>{label}</div>
  </div>
);

const Scene18CreditCardAPR: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camTilt = interpolate(frame, [0, durationInFrames], [2, -2], { extrapolateRight: 'clamp' });
  const apr = Math.round(interpolate(sp(150), [0, 1], [0, 22]));

  return (
    <AbsoluteFill style={{ backgroundColor: '#132522', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(140deg, #0e201b 0%, #2b564d 55%, #16302a 100%)',
        transform: `rotate(${camTilt}deg) scale(1.12)`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '6%', right: '6%', width: 460, height: 460,
          border: '3px solid #FFF449', opacity: 0.05, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame / 48) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute', bottom: '-8%', left: '-4%', width: 500, height: 500,
          background: '#E8664F', opacity: 0.04, borderRadius: 50,
          transform: `rotate(${frame * 0.12}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (horizontal lines) */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: `${i * 6.5}%`, left: 0, width: '100%', height: 1,
            background: '#EBEDE3', transform: `translateX(${Math.sin(frame / 40 + i) * 15}px)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 20 }).map((_, i) => {
          const r = seededRandom(i + 23);
          const x = (r * 1920 + frame * (0.6 + r)) % 1920;
          return (
            <div key={i} style={{
              position: 'absolute', top: `${seededRandom(i + 7) * 100}%`, left: x,
              width: 3 + r * 3, height: 3 + r * 3, borderRadius: '50%',
              background: '#EBEDE3', opacity: 0.2,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ padding: '70px 110px', justifyContent: 'center' }}>
        {/* Three emergency icons */}
        <div style={{ display: 'flex', gap: 40, marginBottom: 60 }}>
          <IconCard progress={sp(10)} delay={10} label="Broken transmission">
            <svg width="90" height="90" viewBox="0 0 100 100">
              <rect x="18" y="40" width="64" height="30" rx="6" fill="none" stroke="#FFF449" strokeWidth="5" />
              <path d="M30 40 L38 26 H62 L70 40" fill="none" stroke="#FFF449" strokeWidth="5" />
              <circle cx="32" cy="72" r="7" fill="#FFF449" />
              <circle cx="68" cy="72" r="7" fill="#FFF449" />
            </svg>
          </IconCard>
          <IconCard progress={sp(30)} delay={30} label="ER visit">
            <svg width="90" height="90" viewBox="0 0 100 100">
              <rect x="20" y="20" width="60" height="60" rx="10" fill="none" stroke="#E8664F" strokeWidth="5" />
              <path d="M50 34 V66 M34 50 H66" stroke="#E8664F" strokeWidth="7" strokeLinecap="round" />
            </svg>
          </IconCard>
          <IconCard progress={sp(50)} delay={50} label="Failed furnace">
            <svg width="90" height="90" viewBox="0 0 100 100">
              <rect x="28" y="18" width="44" height="64" rx="6" fill="none" stroke="#5FB89F" strokeWidth="5" />
              <path d="M50 34 Q42 44 50 54 Q58 44 50 34" fill="#5FB89F" />
            </svg>
          </IconCard>
        </div>

        {/* Arrow to credit card */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 50, opacity: sp(90) }}>
          <svg width="200" height="130" viewBox="0 0 160 100">
            <rect x="10" y="20" width="140" height="70" rx="10" fill="#FFF449" opacity="0.9" />
            <rect x="10" y="36" width="140" height="14" fill="#132522" />
            <rect x="24" y="66" width="46" height="8" rx="2" fill="#132522" />
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 170, color: '#E8664F', lineHeight: 0.9,
          }}>{apr}% <span style={{ fontSize: 60, color: '#EBEDE3' }}>APR</span></div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 310px 85px rgba(0,0,0,0.62)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene18CreditCardAPR;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19BuffettRule',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19BuffettRule: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // escalating number 1000 -> 1700
  const amount = Math.round(interpolate(frame, [30, 130], [1000, 1700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const quoteReveal = sp(150, { damping: 11 });
  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1.15], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#122420', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 25% 30%, #316055 0%, #21453c 50%, #0e1f1a 100%)',
        transform: `scale(${camZoom})`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '12%', right: '12%', width: 480, height: 480,
          background: '#FFF449', opacity: 0.04, borderRadius: 40,
          transform: `rotate(${frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '-6%', left: '5%', width: 420, height: 420,
          border: '2px solid #EBEDE3', opacity: 0.05, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame / 52) * 0.06})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (rising graph line) */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        <svg width="100%" height="100%" viewBox="0 0 1920 1080" preserveAspectRatio="none">
          <path d="M0 900 L400 820 L800 700 L1200 520 L1600 320 L1920 180" fill="none" stroke="#E8664F" strokeWidth="3"
            strokeDasharray="3000" strokeDashoffset={interpolate(frame, [20, 130], [3000, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} />
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const r = seededRandom(i + 29);
          const y = (r * 1080 - frame * (0.5 + r)) % 1080;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${seededRandom(i + 5) * 100}%`, top: (y + 1080) % 1080,
              width: 3 + r * 3, height: 3 + r * 3, borderRadius: '50%',
              background: i % 3 === 0 ? '#FFF449' : '#EBEDE3', opacity: 0.2,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ padding: '70px 110px', justifyContent: 'center' }}>
        <div style={{ display: 'flex', alignItems: 'baseline', gap: 30, opacity: sp(6) }}>
          <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 44, color: '#5FB89F' }}>$1,000 →</span>
          <span style={{
            fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 200, color: '#E8664F', lineHeight: 0.9,
          }}>${amount.toLocaleString()}</span>
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontWeight: 600, fontSize: 46, color: '#EBEDE3',
          opacity: sp(40), marginTop: 6,
        }}>and it threatens next month's rent.</div>

        {/* Buffett rule card */}
        <div style={{
          marginTop: 60, background: 'rgba(255,244,73,0.08)', borderLeft: '6px solid #FFF449',
          borderRadius: '0 20px 20px 0', padding: '34px 44px', maxWidth: 1250,
          opacity: quoteReveal, transform: `translateX(${interpolate(quoteReveal, [0, 1], [-60, 0])}px)`,
        }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 28, letterSpacing: 4, color: '#FFF449' }}>WARREN BUFFETT'S RULE</div>
          <div style={{ fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 72, color: '#EBEDE3', marginTop: 12, lineHeight: 1 }}>
            SAVE FIRST → SPEND WHAT REMAINS
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 310px 85px rgba(0,0,0,0.62)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene19BuffettRule;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20EarnSpendFinance',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CycleNode: React.FC<{ x: number; y: number; label: string; color: string; progress: number }> = ({ x, y, label, color, progress }) => (
  <div style={{
    position: 'absolute', left: x, top: y, transform: `translate(-50%,-50%) scale(${progress})`,
    width: 220, height: 220, borderRadius: '50%', border: `4px solid ${color}`,
    background: 'rgba(19,37,32,0.7)', display: 'flex', alignItems: 'center', justifyContent: 'center',
    fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 46, color, textAlign: 'center', opacity: progress,
  }}>{label}</div>
);

const Scene20EarnSpendFinance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const rot = frame * 0.4;
  const camZoom = interpolate(frame, [0, durationInFrames], [1.08, 1.02], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#122420', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #0e1f1a 0%, #2c5850 55%, #142c26 100%)',
        transform: `scale(${camZoom})`,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '50%', left: '30%', width: 760, height: 760,
          marginLeft: -380, marginTop: -380, border: '2px dashed #EBEDE3', borderRadius: '50%',
          opacity: 0.05, transform: `rotate(${rot}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '-8%', right: '4%', width: 460, height: 460,
          background: '#FFF449', opacity: 0.04, borderRadius: 40, transform: `rotate(${-frame * 0.1}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture (dot ring) */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 24 }).map((_, i) => {
          const a = (i / 24) * Math.PI * 2 + frame / 60;
          return (
            <div key={i} style={{
              position: 'absolute', left: `calc(30% + ${Math.cos(a) * 320}px)`, top: `calc(50% + ${Math.sin(a) * 320}px)`,
              width: 6, height: 6, borderRadius: '50%', background: '#EBEDE3',
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 20 }).map((_, i) => {
          const r = seededRandom(i + 31);
          const y = (r * 1080 + frame * (0.5 + r)) % 1080;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${seededRandom(i + 9) * 100}%`, top: y,
              width: 3 + r * 3, height: 3 + r * 3, borderRadius: '50%',
              background: '#EBEDE3', opacity: 0.18,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill>
        {/* Cycle diagram on left */}
        <div style={{ position: 'absolute', left: '30%', top: '50%', transform: 'translate(-50%,-50%)', width: 1, height: 1 }}>
          <CycleNode x={0} y={-220} label="EARN" color="#5FB89F" progress={sp(10)} />
          <CycleNode x={200} y={120} label="SPEND" color="#FFF449" progress={sp(30)} />
          <CycleNode x={-200} y={120} label="FINANCE THE GAP" color="#E8664F" progress={sp(50)} />
          {/* Rotating arrow ring */}
          <svg width="600" height="600" viewBox="0 0 600 600" style={{ position: 'absolute', left: -300, top: -300, opacity: 0.5, transform: `rotate(${rot}deg)` }}>
            <circle cx="300" cy="300" r="250" fill="none" stroke="#EBEDE3" strokeWidth="2" strokeDasharray="12 20" />
          </svg>
        </div>

        {/* Right message */}
        <div style={{ position: 'absolute', right: 110, top: '50%', transform: 'translateY(-50%)', width: 640 }}>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif', fontWeight: 900, fontSize: 92, color: '#EBEDE3', lineHeight: 0.95,
            opacity: sp(70),
          }}>THE SYSTEM<br />TEACHES THE<br /><span style={{ color: '#E8664F' }}>OPPOSITE</span></div>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontWeight: 600, fontSize: 42, color: '#EBEDE3', marginTop: 28,
            opacity: sp(110),
          }}>Buffett rejected lifestyle inflation — despite being one of the world's wealthiest people.</div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px 90px rgba(0,0,0,0.64)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene20EarnSpendFinance;
```

