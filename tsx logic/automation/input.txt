```tsx
import React from 'react';
Audio: "To understand why claiming at 62 is usually a trap, you have to understand one concept, full retirement age. For most people turning 62 right now, your full retirement age, or FRA, is 67."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11_FRAConcept',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_FRAConcept: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const cameraZoom = interpolate(frame, [0, durationInFrames], [1, 1.04], { extrapolateRight: 'clamp' });

  // Label entrance
  const labelOp = sp(10);
  const trapWordOp = sp(25);
  const trapWordScale = sp(25, { damping: 8 });

  // Concept reveal
  const conceptBoxScale = sp(50);
  const conceptTextOp = sp(65);

  // FRA = 67 reveal
  const fraLabelOp = sp(110);
  const number67Scale = sp(130, { damping: 7, stiffness: 100 });
  const number67Op = sp(130);

  // Underline draw
  const underlineWidth = interpolate(frame, [150, 180], [0, 600], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Pulse on 67
  const pulse = 1 + Math.sin(frame * 0.12) * 0.025;

  // Shake on "trap"
  const shakeFrame = frame - 25;
  const shakeX = shakeFrame > 0 && shakeFrame < 12 ? Math.sin(shakeFrame * 3) * 4 : 0;

  // Particles
  const particles = Array.from({ length: 28 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 20%, rgba(254,236,65,0.18) 0%, rgba(252,209,209,0) 55%), radial-gradient(ellipse at 75% 85%, rgba(0,0,0,0.12) 0%, rgba(252,209,209,0) 60%)',
        }}
      />

      {/* Animated shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '12%',
          left: '8%',
          width: 520,
          height: 520,
          borderRadius: '50%',
          border: '3px solid #000000',
          opacity: 0.06,
          transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '8%',
          right: '6%',
          width: 380,
          height: 380,
          backgroundColor: '#FEEC41',
          opacity: 0.08,
          transform: `rotate(${-frame * 0.1 + 25}deg)`,
          borderRadius: 40,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '55%',
          left: '60%',
          width: 0,
          height: 0,
          borderLeft: '180px solid transparent',
          borderRight: '180px solid transparent',
          borderBottom: '300px solid #000000',
          opacity: 0.05,
          transform: `rotate(${frame * 0.08}deg)`,
        }}
      />

      {/* Motion texture - grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 20 }, (_, i) => (
          <line
            key={`v-${i}`}
            x1={i * 100 + (frame * 0.3) % 100}
            y1={0}
            x2={i * 100 + (frame * 0.3) % 100}
            y2={1080}
            stroke="#000000"
            strokeWidth={1}
          />
        ))}
        {Array.from({ length: 12 }, (_, i) => (
          <line
            key={`h-${i}`}
            x1={0}
            y1={i * 100}
            x2={1920}
            y2={i * 100}
            stroke="#000000"
            strokeWidth={1}
          />
        ))}
      </svg>

      {/* Floating particles */}
      {particles.map((i) => {
        const seed = i + 1;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * (0.4 + seededRandom(seed * 3) * 0.6)) % 1080;
        const size = 4 + seededRandom(seed * 4) * 8;
        const color = i % 3 === 0 ? '#FEEC41' : '#000000';
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: size,
              height: size,
              backgroundColor: color,
              borderRadius: '50%',
              opacity: 0.35,
            }}
          />
        );
      })}

      {/* Main content wrapper with camera zoom */}
      <AbsoluteFill style={{ transform: `scale(${cameraZoom}) translateX(${shakeX}px)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: '50%',
            transform: `translateX(-50%) translateY(${(1 - labelOp) * -30}px)`,
            opacity: labelOp,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <div style={{ width: 60, height: 4, backgroundColor: '#000000' }} />
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            Claiming At 62
          </div>
          <div style={{ width: 60, height: 4, backgroundColor: '#000000' }} />
        </div>

        {/* "It's a TRAP" headline */}
        <div
          style={{
            position: 'absolute',
            top: 170,
            left: '50%',
            transform: `translateX(-50%)`,
            display: 'flex',
            alignItems: 'baseline',
            gap: 24,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 90,
              fontWeight: 900,
              color: '#000000',
              opacity: labelOp,
            }}
          >
            It's a
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 900,
              color: '#000000',
              backgroundColor: '#FEEC41',
              padding: '0 32px',
              transform: `scale(${trapWordScale})`,
              opacity: trapWordOp,
              border: '5px solid #000000',
              boxShadow: '12px 12px 0 #000000',
              letterSpacing: 4,
            }}
          >
            TRAP
          </div>
        </div>

        {/* Concept box - "ONE CONCEPT" */}
        <div
          style={{
            position: 'absolute',
            top: 400,
            left: '50%',
            transform: `translateX(-50%) scale(${conceptBoxScale})`,
            opacity: conceptBoxScale,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 700,
              color: '#000000',
              opacity: conceptTextOp,
              letterSpacing: 3,
              marginBottom: 12,
            }}
          >
            UNDERSTAND ONE CONCEPT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 76,
              fontWeight: 900,
              color: '#FCD1D1',
              backgroundColor: '#000000',
              padding: '14px 50px',
              opacity: conceptTextOp,
              letterSpacing: 4,
            }}
          >
            FULL RETIREMENT AGE
          </div>
        </div>

        {/* FRA equation block */}
        <div
          style={{
            position: 'absolute',
            top: 640,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            alignItems: 'center',
            gap: 40,
            opacity: fraLabelOp,
          }}
        >
          {/* FRA label */}
          <div
            style={{
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'flex-end',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 119,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1,
                letterSpacing: 6,
              }}
            >
              FRA
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 30,
                fontWeight: 600,
                color: '#000000',
                opacity: 0.65,
                letterSpacing: 2,
              }}
            >
              Full Retirement Age
            </div>
          </div>

          {/* Equals sign */}
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 900,
              color: '#000000',
              transform: `scaleX(${fraLabelOp})`,
            }}
          >
            =
          </div>

          {/* Number 67 */}
          <div
            style={{
              position: 'relative',
              transform: `scale(${number67Scale * pulse})`,
              opacity: number67Op,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 221,
                fontWeight: 900,
                color: '#000000',
                backgroundColor: '#FEEC41',
                padding: '0 48px',
                lineHeight: 1.05,
                border: '6px solid #000000',
                boxShadow: '16px 16px 0 #000000',
              }}
            >
              67
            </div>
            <div
              style={{
                position: 'absolute',
                top: -28,
                right: -28,
                backgroundColor: '#000000',
                color: '#FEEC41',
                fontFamily: 'Inter, Poppins',
                fontSize: 26,
                fontWeight: 800,
                padding: '6px 14px',
                letterSpacing: 2,
                transform: `rotate(${10 + Math.sin(frame * 0.1) * 3}deg)`,
              }}
            >
              YEARS
            </div>
          </div>
        </div>

        {/* Underline accent */}
        <div
          style={{
            position: 'absolute',
            top: 920,
            left: '50%',
            transform: 'translateX(-50%)',
            width: underlineWidth,
            height: 8,
            backgroundColor: '#000000',
          }}
        />

        {/* Bottom subtitle */}
        <div
          style={{
            position: 'absolute',
            top: 950,
            left: '50%',
            transform: `translateX(-50%) translateY(${(1 - sp(170)) * 20}px)`,
            opacity: sp(170),
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 600,
            color: '#000000',
            letterSpacing: 2,
          }}
        >
          The magic number for your standard benefit
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.28) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.5,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_FRAConcept;
```

```tsx
import React from 'react';
Audio: "That is the magic number the government uses to calculate your standard benefit. Let's say the SSA calculates that at age 67, you are entitled to exactly $2,000 a month, is your baseline."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12_BaselineCheck',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_BaselineCheck: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Top label
  const headerOp = sp(8);

  // SSA badge
  const badgeScale = sp(25);

  // Check entrance (slides in from right)
  const checkX = interpolate(sp(55), [0, 1], [800, 0]);
  const checkOp = sp(55);
  const checkRotate = interpolate(sp(55), [0, 1], [-8, -3]);

  // Amount counter
  const amountProgress = interpolate(frame, [100, 145], [0, 2000], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const displayAmount = Math.floor(amountProgress);

  // Stamp animation
  const stampScale = sp(160, { damping: 6, stiffness: 90 });
  const stampOp = sp(160);

  // Baseline arrow
  const arrowOp = sp(200);
  const arrowX = interpolate(sp(200), [0, 1], [-50, 0]);

  // Camera drift
  const camY = Math.sin(frame * 0.02) * 6;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 80% 30%, rgba(254,236,65,0.22) 0%, transparent 50%), radial-gradient(circle at 15% 90%, rgba(0,0,0,0.1) 0%, transparent 55%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '10%',
          right: '10%',
          width: 460,
          height: 460,
          borderRadius: 80,
          backgroundColor: '#FEEC41',
          opacity: 0.07,
          transform: `rotate(${15 + frame * 0.06}deg) scale(${1 + Math.sin(frame * 0.025) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '10%',
          left: '12%',
          width: 400,
          height: 400,
          borderRadius: '50%',
          border: '4px solid #000000',
          opacity: 0.07,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />

      {/* Motion texture - diagonal lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 30 }, (_, i) => (
          <line
            key={i}
            x1={i * 80 - 200 + (frame * 0.4) % 80}
            y1={0}
            x2={i * 80 + 400 + (frame * 0.4) % 80}
            y2={1080}
            stroke="#000000"
            strokeWidth={1.5}
          />
        ))}
      </svg>

      {/* Floating money particles */}
      {Array.from({ length: 22 }, (_, i) => {
        const seed = i + 5;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 - frame * (0.5 + seededRandom(seed * 3) * 0.5)) % 1080;
        const yPos = y < 0 ? y + 1080 : y;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: yPos,
              fontSize: 18 + seededRandom(seed * 4) * 14,
              fontFamily: 'Inter',
              fontWeight: 900,
              color: i % 2 === 0 ? '#FEEC41' : '#000000',
              opacity: 0.3,
              transform: `rotate(${seededRandom(seed * 5) * 360}deg)`,
            }}
          >
            $
          </div>
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: '50%',
            transform: `translateX(-50%) translateY(${(1 - headerOp) * -20}px)`,
            opacity: headerOp,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          {/* SSA badge */}
          <div
            style={{
              transform: `scale(${badgeScale})`,
              backgroundColor: '#000000',
              color: '#FEEC41',
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 900,
              padding: '10px 22px',
              letterSpacing: 4,
              border: '3px solid #000000',
            }}
          >
            SSA
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 2,
            }}
          >
            STANDARD BENEFIT CALCULATION
          </div>
        </div>

        {/* Check / Document Visual */}
        <div
          style={{
            position: 'absolute',
            top: 230,
            left: '50%',
            transform: `translateX(calc(-50% + ${checkX}px)) rotate(${checkRotate}deg)`,
            opacity: checkOp,
            width: 1280,
            height: 540,
            backgroundColor: '#FEEC41',
            border: '6px solid #000000',
            boxShadow: '20px 20px 0 #000000',
            padding: 50,
            display: 'flex',
            flexDirection: 'column',
          }}
        >
          {/* Check header */}
          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center',
              borderBottom: '3px solid #000000',
              paddingBottom: 18,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 32,
                fontWeight: 800,
                color: '#000000',
                letterSpacing: 2,
              }}
            >
              SOCIAL SECURITY ADMINISTRATION
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono, Inter',
                fontSize: 26,
                fontWeight: 700,
                color: '#000000',
                backgroundColor: '#FCD1D1',
                padding: '6px 16px',
                border: '2px solid #000000',
              }}
            >
              AGE 67
            </div>
          </div>

          {/* Pay to */}
          <div style={{ marginTop: 28, display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end' }}>
            <div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 30,
                  fontWeight: 700,
                  color: '#000000',
                  opacity: 0.7,
                  letterSpacing: 1,
                }}
              >
                PAY TO RETIREE — MONTHLY
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 64,
                  fontWeight: 900,
                  color: '#000000',
                  marginTop: 8,
                  letterSpacing: 1,
                }}
              >
                You, the worker
              </div>
            </div>
          </div>

          {/* Amount */}
          <div
            style={{
              marginTop: 32,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              gap: 12,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 187,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1,
              }}
            >
              ${displayAmount.toLocaleString()}
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 51,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.7,
                marginTop: 60,
              }}
            >
              /mo
            </div>
          </div>

          {/* Stamp */}
          <div
            style={{
              position: 'absolute',
              top: 60,
              right: 80,
              transform: `rotate(${-15 + (1 - stampScale) * 30}deg) scale(${stampScale})`,
              opacity: stampOp,
              border: '6px solid #000000',
              padding: '14px 32px',
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 900,
              color: '#000000',
              backgroundColor: 'rgba(252,209,209,0.4)',
              letterSpacing: 4,
            }}
          >
            BASELINE
          </div>
        </div>

        {/* Bottom callout */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: '50%',
            transform: `translateX(calc(-50% + ${arrowX}px))`,
            opacity: arrowOp,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          {/* Arrow SVG */}
          <svg width="80" height="60" viewBox="0 0 80 60">
            <path
              d="M 5 30 L 65 30 M 50 12 L 70 30 L 50 48"
              stroke="#000000"
              strokeWidth="6"
              fill="none"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 49,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 1,
            }}
          >
            Every other number is calculated from this
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.5,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_BaselineCheck;
```

```tsx
import React from 'react';
Audio: "But the government also knows that some people want their money early, so they offer you a deal. You can have your money right now at 62, but we are going to charge you a penalty for every single month you claim before your 67th birthday."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13_TheDeal',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_TheDeal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = sp(10);
  const dealScale = sp(25, { damping: 7 });

  // Two cards
  const card62Scale = sp(60);
  const card67Scale = sp(80);

  // Penalty stamp on 62 card
  const penaltyScale = sp(140, { damping: 6, stiffness: 80 });
  const penaltyRotate = interpolate(sp(140), [0, 1], [-30, -12]);

  // VS divider
  const vsScale = sp(110, { damping: 8 });

  // Bottom warning
  const warningOp = sp(190);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(254,236,65,0.12) 0%, rgba(252,209,209,0) 50%, rgba(0,0,0,0.1) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          left: '5%',
          width: 300,
          height: 600,
          backgroundColor: '#000000',
          opacity: 0.04,
          transform: `rotate(${-15 + frame * 0.05}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '15%',
          right: '5%',
          width: 480,
          height: 480,
          borderRadius: '50%',
          backgroundColor: '#FEEC41',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />

      {/* Texture - dots grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {Array.from({ length: 20 }, (_, i) =>
          Array.from({ length: 12 }, (_, j) => (
            <circle
              key={`${i}-${j}`}
              cx={i * 100 + 50 + Math.sin(frame * 0.02 + i) * 3}
              cy={j * 100 + 50 + Math.cos(frame * 0.02 + j) * 3}
              r={2}
              fill="#000000"
            />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }, (_, i) => {
        const seed = i + 10;
        const x = (seededRandom(seed) * 1920 + frame * 0.6) % 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 6,
              height: 6,
              backgroundColor: '#000000',
              opacity: 0.25,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: '50%',
          transform: `translateX(-50%)`,
          opacity: titleOp,
          display: 'flex',
          alignItems: 'baseline',
          gap: 20,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 700,
            color: '#000000',
            letterSpacing: 2,
          }}
        >
          The Government's
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 102,
            fontWeight: 900,
            color: '#000000',
            backgroundColor: '#FEEC41',
            padding: '0 30px',
            transform: `scale(${dealScale})`,
            border: '5px solid #000000',
            boxShadow: '10px 10px 0 #000000',
            letterSpacing: 4,
          }}
        >
          "DEAL"
        </div>
      </div>

      {/* Two cards container */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 40,
        }}
      >
        {/* Card 62 - The Trap */}
        <div
          style={{
            width: 600,
            height: 600,
            backgroundColor: '#000000',
            border: '6px solid #000000',
            boxShadow: '14px 14px 0 #FEEC41',
            padding: 40,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'space-between',
            transform: `scale(${card62Scale})`,
            position: 'relative',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 800,
              color: '#FCD1D1',
              letterSpacing: 3,
            }}
          >
            CLAIM NOW
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 272,
              fontWeight: 900,
              color: '#FEEC41',
              lineHeight: 1,
            }}
          >
            62
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 700,
              color: '#FCD1D1',
              textAlign: 'center',
              opacity: 0.85,
            }}
          >
            "Get your money<br />right now"
          </div>

          {/* Penalty stamp */}
          <div
            style={{
              position: 'absolute',
              top: 40,
              right: -30,
              transform: `rotate(${penaltyRotate}deg) scale(${penaltyScale})`,
              backgroundColor: '#FEEC41',
              color: '#000000',
              border: '5px solid #000000',
              padding: '10px 24px',
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              letterSpacing: 3,
              boxShadow: '6px 6px 0 #000000',
            }}
          >
            PENALTY!
          </div>
        </div>

        {/* VS */}
        <div
          style={{
            transform: `scale(${vsScale}) rotate(${Math.sin(frame * 0.1) * 5}deg)`,
            fontFamily: 'Inter, Poppins',
            fontSize: 110,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: 2,
          }}
        >
          VS
        </div>

        {/* Card 67 - The Reward */}
        <div
          style={{
            width: 600,
            height: 600,
            backgroundColor: '#FEEC41',
            border: '6px solid #000000',
            boxShadow: '14px 14px 0 #000000',
            padding: 40,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'space-between',
            transform: `scale(${card67Scale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 3,
            }}
          >
            WAIT FOR FRA
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 272,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1,
            }}
          >
            67
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 700,
              color: '#000000',
              textAlign: 'center',
              opacity: 0.85,
            }}
          >
            "Full standard<br />benefit"
          </div>
        </div>
      </div>

      {/* Bottom warning bar */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - warningOp) * 40}px)`,
          opacity: warningOp,
          backgroundColor: '#000000',
          padding: '20px 50px',
          display: 'flex',
          alignItems: 'center',
          gap: 24,
        }}
      >
        {/* Warning icon SVG */}
        <svg width="50" height="50" viewBox="0 0 50 50">
          <path
            d="M 25 5 L 47 43 L 3 43 Z"
            fill="#FEEC41"
            stroke="#FEEC41"
            strokeWidth="2"
            strokeLinejoin="round"
          />
          <text x="25" y="38" textAnchor="middle" fontSize="26" fontWeight="900" fill="#000000" fontFamily="Inter">!</text>
        </svg>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 47,
            fontWeight: 800,
            color: '#FEEC41',
            letterSpacing: 2,
          }}
        >
          Penalty charged for EVERY month before 67
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.28) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#FEEC41',
          opacity: 0.6,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_TheDeal;
```

```tsx
import React from 'react';
Audio: "And this is not a temporary penalty. This is not a fine that goes away when you turn 67. This is a permanent, irreversible reduction that will follow you to the grave."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14_Permanent',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_Permanent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // "NOT" crossouts
  const notOp1 = sp(15);
  const strikeWidth1 = interpolate(frame, [25, 45], [0, 700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const notOp2 = sp(55);
  const strikeWidth2 = interpolate(frame, [65, 85], [0, 700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // PERMANENT reveal
  const permanentScale = sp(115, { damping: 6, stiffness: 70 });
  const permanentOp = sp(115);

  // Lock icon
  const lockScale = sp(155, { damping: 7 });

  // Timeline arrow (lifetime)
  const timelineProgress = interpolate(frame, [195, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Screen shake on PERMANENT
  const shakeFrame = frame - 115;
  const shake = shakeFrame > 0 && shakeFrame < 14 ? Math.sin(shakeFrame * 4) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 50%, rgba(254,236,65,0.15) 0%, rgba(252,209,209,0) 60%)',
        }}
      />

      {/* Dark warning shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '-10%',
          left: '50%',
          transform: `translateX(-50%) rotate(${frame * 0.04}deg)`,
          width: 1400,
          height: 1400,
          border: '3px solid #000000',
          borderRadius: '50%',
          opacity: 0.05,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '20%',
          left: '20%',
          width: 200,
          height: 200,
          backgroundColor: '#FEEC41',
          opacity: 0.08,
          transform: `rotate(${45 + frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '15%',
          right: '15%',
          width: 250,
          height: 250,
          backgroundColor: '#000000',
          opacity: 0.07,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
        }}
      />

      {/* Texture - thick crosshatch */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 25 }, (_, i) => (
          <line key={i} x1={i * 80} y1={0} x2={i * 80 + 600} y2={1080} stroke="#000000" strokeWidth={1.5} />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }, (_, i) => {
        const seed = i + 20;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.5) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 5,
              height: 5,
              backgroundColor: i % 2 ? '#000000' : '#FEEC41',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${shake}px)` }}>
        {/* NOT temporary - struck out */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            opacity: notOp1,
            position: 'relative',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 64,
              fontWeight: 900,
              color: '#000000',
              padding: '4px 16px',
              backgroundColor: '#FEEC41',
              border: '3px solid #000000',
            }}
          >
            NOT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 64,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
              position: 'relative',
            }}
          >
            TEMPORARY
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                width: strikeWidth1,
                maxWidth: '100%',
                height: 8,
                backgroundColor: '#000000',
              }}
            />
          </div>
        </div>

        {/* NOT a fine - struck out */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            opacity: notOp2,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 64,
              fontWeight: 900,
              color: '#000000',
              padding: '4px 16px',
              backgroundColor: '#FEEC41',
              border: '3px solid #000000',
            }}
          >
            NOT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 64,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
              position: 'relative',
            }}
          >
            A FINE THAT GOES AWAY
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                width: strikeWidth2,
                maxWidth: '100%',
                height: 8,
                backgroundColor: '#000000',
              }}
            />
          </div>
        </div>

        {/* IT IS small label */}
        <div
          style={{
            position: 'absolute',
            top: 370,
            left: '50%',
            transform: 'translateX(-50%)',
            opacity: permanentOp,
            fontFamily: 'Inter, Poppins',
            fontSize: 49,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 4,
          }}
        >
          IT IS A —
        </div>

        {/* PERMANENT IRREVERSIBLE - main impact */}
        <div
          style={{
            position: 'absolute',
            top: 450,
            left: '50%',
            transform: `translateX(-50%) scale(${permanentScale})`,
            opacity: permanentOp,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 4,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 187,
              fontWeight: 900,
              color: '#FEEC41',
              backgroundColor: '#000000',
              padding: '0 36px',
              lineHeight: 1.05,
              letterSpacing: 6,
              border: '6px solid #000000',
              boxShadow: '14px 14px 0 #FEEC41',
            }}
          >
            PERMANENT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 8,
              marginTop: 14,
            }}
          >
            IRREVERSIBLE
          </div>
        </div>

        {/* Lock icon left */}
        <div
          style={{
            position: 'absolute',
            top: 480,
            left: 140,
            transform: `scale(${lockScale}) rotate(${Math.sin(frame * 0.08) * 4}deg)`,
          }}
        >
          <svg width="140" height="180" viewBox="0 0 140 180">
            <rect x="20" y="70" width="100" height="100" rx="8" fill="#000000" />
            <path d="M 40 70 L 40 45 A 30 30 0 0 1 100 45 L 100 70" fill="none" stroke="#000000" strokeWidth="14" />
            <circle cx="70" cy="115" r="10" fill="#FEEC41" />
            <rect x="65" y="115" width="10" height="25" fill="#FEEC41" />
          </svg>
        </div>

        {/* Lock icon right */}
        <div
          style={{
            position: 'absolute',
            top: 480,
            right: 140,
            transform: `scale(${lockScale}) rotate(${-Math.sin(frame * 0.08) * 4}deg)`,
          }}
        >
          <svg width="140" height="180" viewBox="0 0 140 180">
            <rect x="20" y="70" width="100" height="100" rx="8" fill="#000000" />
            <path d="M 40 70 L 40 45 A 30 30 0 0 1 100 45 L 100 70" fill="none" stroke="#000000" strokeWidth="14" />
            <circle cx="70" cy="115" r="10" fill="#FEEC41" />
            <rect x="65" y="115" width="10" height="25" fill="#FEEC41" />
          </svg>
        </div>

        {/* Lifetime timeline */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 200,
            right: 200,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 800,
              color: '#000000',
              marginBottom: 12,
              letterSpacing: 3,
              opacity: timelineProgress > 0 ? 1 : 0,
            }}
          >
            FOLLOWS YOU TO THE GRAVE →
          </div>
          <div
            style={{
              width: '100%',
              height: 14,
              backgroundColor: 'rgba(0,0,0,0.15)',
              position: 'relative',
              border: '3px solid #000000',
            }}
          >
            <div
              style={{
                width: `${timelineProgress * 100}%`,
                height: '100%',
                backgroundColor: '#000000',
              }}
            />
            {/* Markers */}
            {['62', '67', '75', '85', '95'].map((age, i) => (
              <div
                key={age}
                style={{
                  position: 'absolute',
                  left: `${(i / 4) * 100}%`,
                  top: -8,
                  transform: 'translateX(-50%)',
                  width: 4,
                  height: 30,
                  backgroundColor: '#000000',
                }}
              >
                <div
                  style={{
                    position: 'absolute',
                    top: 32,
                    left: '50%',
                    transform: 'translateX(-50%)',
                    fontFamily: 'Inter, Poppins',
                    fontSize: 26,
                    fontWeight: 800,
                    color: '#000000',
                  }}
                >
                  {age}
                </div>
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(0,0,0,0.3) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.5,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_Permanent;
```

```tsx
import React from 'react';
Audio: "If your full benefit at 67 is $2,000 and you claim at 62, the SSA will slash your check by exactly 30%. Your new monthly payment is $1,400. You just lost $600 a month."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15_TheCut',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_TheCut: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bar chart bars
  const bar2000Height = interpolate(sp(40), [0, 1], [0, 500]);
  const bar1400Height = interpolate(sp(120), [0, 1], [0, 350]);

  // Counter for $2000 -> $1400
  const showCut = frame > 165;
  const counterProgress = interpolate(frame, [165, 200], [2000, 1400], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const displayAmount = Math.floor(showCut ? counterProgress : 2000);

  // Slash mark
  const slashProgress = interpolate(frame, [165, 195], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // 30% callout
  const cutLabelScale = sp(180, { damping: 6 });
  const cutLabelOp = sp(180);

  // Lost amount
  const lostOp = sp(245);
  const lostScale = sp(245, { damping: 7 });

  // Screen shake on slash
  const shakeFrame = frame - 175;
  const shake = shakeFrame > 0 && shakeFrame < 10 ? Math.sin(shakeFrame * 4) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(254,236,65,0.1) 0%, rgba(252,209,209,0) 40%, rgba(0,0,0,0.08) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          left: '5%',
          width: 350,
          height: 350,
          borderRadius: '50%',
          border: '4px solid #000000',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '10%',
          right: '5%',
          width: 280,
          height: 480,
          backgroundColor: '#FEEC41',
          opacity: 0.06,
          transform: `rotate(${-10 + frame * 0.08}deg)`,
        }}
      />

      {/* Texture - horizontal lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 30 }, (_, i) => (
          <line
            key={i}
            x1={0}
            y1={i * 40 + ((frame * 0.4) % 40)}
            x2={1920}
            y2={i * 40 + ((frame * 0.4) % 40)}
            stroke="#000000"
            strokeWidth={1}
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }, (_, i) => {
        const seed = i + 30;
        const x = seededRandom(seed) * 1920;
        const startY = seededRandom(seed * 2) * 1080;
        const y = showCut ? (startY + (frame - 165) * 3) % 1080 : startY;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 8,
              height: 8,
              backgroundColor: '#000000',
              opacity: 0.3,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${shake}px) translateY(${shake * 0.5}px)` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: '50%',
            transform: 'translateX(-50%)',
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 3,
            opacity: sp(5),
          }}
        >
          THE 30% PENALTY — VISUALIZED
        </div>

        {/* Bar Chart Container */}
        <div
          style={{
            position: 'absolute',
            bottom: 180,
            left: 200,
            display: 'flex',
            alignItems: 'flex-end',
            gap: 90,
          }}
        >
          {/* Bar at 67 - Full $2000 */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 64,
                fontWeight: 900,
                color: '#000000',
                marginBottom: 8,
                opacity: sp(40),
              }}
            >
              $2,000
            </div>
            <div
              style={{
                width: 240,
                height: bar2000Height,
                backgroundColor: '#FEEC41',
                border: '5px solid #000000',
                boxShadow: '10px 10px 0 #000000',
                position: 'relative',
              }}
            />
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 47,
                fontWeight: 900,
                color: '#000000',
                marginTop: 16,
                backgroundColor: '#FEEC41',
                padding: '4px 18px',
                border: '3px solid #000000',
                opacity: sp(40),
              }}
            >
              AGE 67
            </div>
          </div>

          {/* Arrow with 30% cut label */}
          <div
            style={{
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              marginBottom: 200,
              transform: `scale(${cutLabelScale})`,
              opacity: cutLabelOp,
            }}
          >
            <div
              style={{
                backgroundColor: '#000000',
                color: '#FEEC41',
                padding: '14px 28px',
                fontFamily: 'Inter, Poppins',
                fontSize: 76,
                fontWeight: 900,
                letterSpacing: 2,
                border: '5px solid #000000',
                boxShadow: '10px 10px 0 #FEEC41',
              }}
            >
              -30%
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 32,
                fontWeight: 800,
                color: '#000000',
                marginTop: 10,
                letterSpacing: 2,
              }}
            >
              SLASHED
            </div>
            {/* Down arrow */}
            <svg width="60" height="80" viewBox="0 0 60 80" style={{ marginTop: 8 }}>
              <path d="M 30 5 L 30 60 M 10 45 L 30 70 L 50 45" stroke="#000000" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>

          {/* Bar at 62 - Cut $1400 */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', position: 'relative' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 64,
                fontWeight: 900,
                color: '#000000',
                marginBottom: 8,
                opacity: sp(120),
              }}
            >
              ${displayAmount.toLocaleString()}
            </div>
            <div
              style={{
                width: 240,
                height: bar1400Height,
                backgroundColor: '#000000',
                border: '5px solid #000000',
                boxShadow: '10px 10px 0 #FEEC41',
                position: 'relative',
                overflow: 'hidden',
              }}
            >
              {/* Diagonal slash overlay */}
              <svg
                width="240"
                height="350"
                viewBox="0 0 240 350"
                style={{ position: 'absolute', top: 0, left: 0 }}
              >
                <line
                  x1={0}
                  y1={350}
                  x2={240 * slashProgress}
                  y2={350 - 350 * slashProgress}
                  stroke="#FEEC41"
                  strokeWidth="10"
                />
              </svg>
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 47,
                fontWeight: 900,
                color: '#FEEC41',
                marginTop: 16,
                backgroundColor: '#000000',
                padding: '4px 18px',
                border: '3px solid #000000',
                opacity: sp(120),
              }}
            >
              AGE 62
            </div>
          </div>
        </div>

        {/* Lost amount callout - right side */}
        <div
          style={{
            position: 'absolute',
            top: 230,
            right: 140,
            transform: `scale(${lostScale})`,
            opacity: lostOp,
            backgroundColor: '#000000',
            padding: 30,
            border: '6px solid #000000',
            boxShadow: '14px 14px 0 #FEEC41',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 800,
              color: '#FEEC41',
              letterSpacing: 3,
            }}
          >
            YOU JUST LOST
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 153,
              fontWeight: 900,
              color: '#FEEC41',
              lineHeight: 1,
              marginTop: 6,
            }}
          >
            $600
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 700,
              color: '#FCD1D1',
              letterSpacing: 2,
            }}
          >
            EVERY MONTH
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.28) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.5,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_TheCut;
```

```tsx
import React from 'react';
Audio: "That is $7,200 a year. And because Social Security gives you cost of living adjustments based on a percentage, you will lose that raise on a smaller base every single year for the rest of your life."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16_AnnualLoss',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_AnnualLoss: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 600 x 12 calculation
  const eq1Op = sp(10);
  const eq2Op = sp(35);
  const eq3Op = sp(60);

  // 7200 counter
  const annualLossProgress = interpolate(frame, [85, 130], [0, 7200], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const displayAnnual = Math.floor(annualLossProgress);

  // 7200 scale
  const annualScale = sp(85, { damping: 6 });

  // COLA section
  const colaLabelOp = sp(150);
  const colaBox1 = sp(165);
  const colaBox2 = sp(180);
  const colaBox3 = sp(195);

  // Compounding arrow
  const arrowProgress = interpolate(frame, [220, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 20% 20%, rgba(254,236,65,0.2) 0%, transparent 50%), radial-gradient(circle at 80% 80%, rgba(0,0,0,0.1) 0%, transparent 50%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '8%',
          right: '8%',
          width: 320,
          height: 320,
          borderRadius: 40,
          backgroundColor: '#000000',
          opacity: 0.05,
          transform: `rotate(${20 + frame * 0.06}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '5%',
          left: '5%',
          width: 0,
          height: 0,
          borderLeft: '200px solid transparent',
          borderRight: '200px solid transparent',
          borderBottom: '350px solid #FEEC41',
          opacity: 0.07,
          transform: `rotate(${frame * 0.08}deg)`,
        }}
      />

      {/* Texture - dot grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 25 }, (_, i) =>
          Array.from({ length: 15 }, (_, j) => (
            <circle key={`${i}-${j}`} cx={i * 80 + 40} cy={j * 80 + 40} r={1.8} fill="#000000" />
          ))
        )}
      </svg>

      {/* Header */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: '50%',
          transform: 'translateX(-50%)',
          fontFamily: 'Inter, Poppins',
          fontSize: 47,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 3,
          opacity: sp(5),
        }}
      >
        DO THE MATH ON A FULL YEAR
      </div>

      {/* Equation row */}
      <div
        style={{
          position: 'absolute',
          top: 180,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          alignItems: 'center',
          gap: 30,
        }}
      >
        {/* $600 */}
        <div
          style={{
            opacity: eq1Op,
            fontFamily: 'Inter, Poppins',
            fontSize: 119,
            fontWeight: 900,
            color: '#000000',
            backgroundColor: '#FEEC41',
            padding: '8px 28px',
            border: '5px solid #000000',
            boxShadow: '10px 10px 0 #000000',
          }}
        >
          $600
        </div>
        <div style={{ opacity: eq1Op, fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 700, color: '#000000' }}>
          /month
        </div>
        <div
          style={{
            opacity: eq2Op,
            fontFamily: 'Inter, Poppins',
            fontSize: 102,
            fontWeight: 900,
            color: '#000000',
          }}
        >
          ×
        </div>
        <div
          style={{
            opacity: eq2Op,
            fontFamily: 'Inter, Poppins',
            fontSize: 119,
            fontWeight: 900,
            color: '#000000',
            backgroundColor: '#FCD1D1',
            border: '5px solid #000000',
            padding: '8px 28px',
            boxShadow: '10px 10px 0 #000000',
          }}
        >
          12
        </div>
        <div style={{ opacity: eq2Op, fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 700, color: '#000000' }}>
          months
        </div>
        <div
          style={{
            opacity: eq3Op,
            fontFamily: 'Inter, Poppins',
            fontSize: 102,
            fontWeight: 900,
            color: '#000000',
          }}
        >
          =
        </div>
      </div>

      {/* The annual loss number - BIG */}
      <div
        style={{
          position: 'absolute',
          top: 350,
          left: '50%',
          transform: `translateX(-50%) scale(${annualScale})`,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            fontWeight: 800,
            color: '#FEEC41',
            backgroundColor: '#000000',
            padding: '4px 20px',
            letterSpacing: 4,
            marginBottom: 8,
          }}
        >
          LOST EVERY YEAR
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 221,
            fontWeight: 900,
            color: '#000000',
            backgroundColor: '#FEEC41',
            padding: '0 50px',
            lineHeight: 1.05,
            border: '8px solid #000000',
            boxShadow: '18px 18px 0 #000000',
            letterSpacing: 2,
          }}
        >
          ${displayAnnual.toLocaleString()}
        </div>
      </div>

      {/* COLA section */}
      <div
        style={{
          position: 'absolute',
          bottom: 200,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: 16,
        }}
      >
        <div
          style={{
            opacity: colaLabelOp,
            fontFamily: 'Inter, Poppins',
            fontSize: 36,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 3,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
          }}
        >
          <div style={{ width: 50, height: 4, backgroundColor: '#000000' }} />
          AND IT COMPOUNDS EVERY YEAR (COLA)
          <div style={{ width: 50, height: 4, backgroundColor: '#000000' }} />
        </div>

        <div style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
          {[
            { label: 'YEAR 1', amount: '$7,200', op: colaBox1 },
            { label: 'YEAR 10', amount: '$9,300', op: colaBox2 },
            { label: 'YEAR 20', amount: '$12,500+', op: colaBox3 },
          ].map((item, i) => (
            <React.Fragment key={item.label}>
              <div
                style={{
                  opacity: item.op,
                  transform: `scale(${item.op})`,
                  backgroundColor: i === 2 ? '#000000' : '#FEEC41',
                  border: '4px solid #000000',
                  padding: '14px 24px',
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  boxShadow: '6px 6px 0 #000000',
                }}
              >
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 26,
                    fontWeight: 800,
                    color: i === 2 ? '#FEEC41' : '#000000',
                    letterSpacing: 2,
                  }}
                >
                  {item.label}
                </div>
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 51,
                    fontWeight: 900,
                    color: i === 2 ? '#FEEC41' : '#000000',
                  }}
                >
                  {item.amount}
                </div>
              </div>
              {i < 2 && (
                <div
                  style={{
                    opacity: i === 0 ? colaBox2 : colaBox3,
                    fontFamily: 'Inter, Poppins',
                    fontSize: 51,
                    fontWeight: 900,
                    color: '#000000',
                  }}
                >
                  →
                </div>
              )}
            </React.Fragment>
          ))}
        </div>
      </div>

      {/* Bottom rising arrow indicator */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: 'translateX(-50%)',
          width: 800,
          height: 30,
          opacity: arrowProgress,
        }}
      >
        <svg width="800" height="30" viewBox="0 0 800 30">
          <line x1="0" y1="20" x2={800 * arrowProgress} y2="20" stroke="#000000" strokeWidth="5" />
          <polygon
            points={`${800 * arrowProgress - 18},${10} ${800 * arrowProgress},${20} ${800 * arrowProgress - 18},${30}`}
            fill="#000000"
            style={{ opacity: arrowProgress }}
          />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.5,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_AnnualLoss;
```

```tsx
import React from 'react';
Audio: "The government doesn't send you a warning label explaining this. They just mail you the check. You have to know the math to fight back. This brings us to the most important mathematical concept in Social Security."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17_NoWarning',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_NoWarning: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Envelope entrance
  const envelopeScale = sp(20);
  const envelopeY = interpolate(sp(20), [0, 1], [-200, 0]);

  // "NO WARNING" stamp
  const stampScale = sp(70, { damping: 6 });
  const stampRotate = interpolate(sp(70), [0, 1], [-45, -18]);

  // Strike through warning text
  const strikeWidth = interpolate(frame, [90, 115], [0, 480], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // "KNOW THE MATH" reveal
  const mathOp = sp(150);
  const mathScale = sp(150, { damping: 7 });

  // Boxing glove / fight back element
  const fightOp = sp(200);
  const fightRotate = Math.sin(frame * 0.15) * 6;

  // Bottom CTA
  const ctaOp = sp(240);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 70% 30%, rgba(254,236,65,0.18) 0%, rgba(252,209,209,0) 55%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '5%',
          left: '8%',
          width: 260,
          height: 260,
          backgroundColor: '#FEEC41',
          opacity: 0.08,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '12%',
          right: '8%',
          width: 380,
          height: 380,
          border: '5px solid #000000',
          opacity: 0.06,
          transform: `rotate(${30 + frame * 0.08}deg)`,
        }}
      />

      {/* Texture - paper grain */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 100 }, (_, i) => {
          const seed = i + 50;
          return (
            <circle
              key={i}
              cx={seededRandom(seed) * 1920}
              cy={seededRandom(seed * 2) * 1080}
              r={seededRandom(seed * 3) * 3 + 1}
              fill="#000000"
            />
          );
        })}
      </svg>

      {/* Header */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: '50%',
          transform: 'translateX(-50%)',
          fontFamily: 'Inter, Poppins',
          fontSize: 51,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 3,
          opacity: sp(5),
          textAlign: 'center',
        }}
      >
        WHAT THE GOVERNMENT MAILS YOU
      </div>

      {/* Envelope / Check */}
      <div
        style={{
          position: 'absolute',
          top: 180,
          left: 130,
          width: 900,
          height: 580,
          transform: `scale(${envelopeScale}) translateY(${envelopeY}px) rotate(-3deg)`,
        }}
      >
        {/* Envelope */}
        <div
          style={{
            width: '100%',
            height: '100%',
            backgroundColor: '#FEEC41',
            border: '6px solid #000000',
            boxShadow: '16px 16px 0 #000000',
            padding: 40,
            display: 'flex',
            flexDirection: 'column',
            position: 'relative',
          }}
        >
          {/* Postage stamp area */}
          <div
            style={{
              position: 'absolute',
              top: 30,
              right: 30,
              width: 100,
              height: 130,
              border: '3px dashed #000000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Inter, Poppins',
              fontSize: 22,
              fontWeight: 800,
              color: '#000000',
              textAlign: 'center',
              padding: 6,
            }}
          >
            SSA<br/>USA
          </div>

          {/* From line */}
          <div
            style={{
              fontFamily: 'JetBrains Mono, Inter',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.7,
            }}
          >
            FROM: Social Security Administration
          </div>

          {/* TO line */}
          <div
            style={{
              fontFamily: 'JetBrains Mono, Inter',
              fontSize: 30,
              fontWeight: 800,
              color: '#000000',
              marginTop: 20,
            }}
          >
            TO: You (Age 62)
          </div>

          {/* Check content */}
          <div
            style={{
              marginTop: 50,
              padding: 30,
              backgroundColor: '#FCD1D1',
              border: '3px solid #000000',
              display: 'flex',
              flexDirection: 'column',
              gap: 14,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 51,
                fontWeight: 900,
                color: '#000000',
              }}
            >
              Your Monthly Check: $1,400
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 30,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.7,
                position: 'relative',
                display: 'inline-block',
                width: 'fit-content',
              }}
            >
              ⚠ Warning about the 30% permanent cut
              <div
                style={{
                  position: 'absolute',
                  top: '50%',
                  left: 0,
                  width: strikeWidth,
                  maxWidth: '100%',
                  height: 6,
                  backgroundColor: '#000000',
                }}
              />
            </div>
          </div>
        </div>

        {/* NO WARNING stamp */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: -60,
            transform: `rotate(${stampRotate}deg) scale(${stampScale})`,
            backgroundColor: '#000000',
            color: '#FEEC41',
            border: '7px solid #FEEC41',
            padding: '14px 28px',
            fontFamily: 'Inter, Poppins',
            fontSize: 49,
            fontWeight: 900,
            letterSpacing: 4,
            boxShadow: '0 0 0 4px #000000',
          }}
        >
          NO WARNING
        </div>
      </div>

      {/* Right side - KNOW THE MATH */}
      <div
        style={{
          position: 'absolute',
          top: 220,
          right: 100,
          width: 700,
          opacity: mathOp,
          transform: `scale(${mathScale})`,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'flex-start',
          gap: 18,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 2,
          }}
        >
          You have to —
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 153,
            fontWeight: 900,
            color: '#FEEC41',
            backgroundColor: '#000000',
            padding: '0 30px',
            lineHeight: 1.05,
            border: '6px solid #000000',
            boxShadow: '14px 14px 0 #FEEC41',
            letterSpacing: 3,
          }}
        >
          KNOW THE
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 187,
            fontWeight: 900,
            color: '#000000',
            backgroundColor: '#FEEC41',
            padding: '0 30px',
            lineHeight: 1.05,
            border: '6px solid #000000',
            boxShadow: '14px 14px 0 #000000',
            letterSpacing: 3,
          }}
        >
          MATH
        </div>

        {/* Fight back element */}
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            opacity: fightOp,
            marginTop: 18,
            transform: `rotate(${fightRotate * 0.3}deg)`,
          }}
        >
          {/* Fist SVG */}
          <svg width="80" height="80" viewBox="0 0 80 80">
            <rect x="15" y="25" width="50" height="40" rx="10" fill="#000000" />
            <rect x="22" y="22" width="8" height="8" rx="2" fill="#000000" />
            <rect x="33" y="20" width="8" height="10" rx="2" fill="#000000" />
            <rect x="44" y="22" width="8" height="8" rx="2" fill="#000000" />
            <rect x="55" y="24" width="8" height="6" rx="2" fill="#000000" />
            <rect x="10" y="60" width="60" height="14" rx="4" fill="#FEEC41" stroke="#000000" strokeWidth="3" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 47,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 2,
            }}
          >
            FIGHT BACK
          </div>
        </div>
      </div>

      {/* Bottom CTA */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - ctaOp) * 30}px)`,
          opacity: ctaOp,
          backgroundColor: '#000000',
          padding: '14px 36px',
          fontFamily: 'Inter, Poppins',
          fontSize: 36,
          fontWeight: 800,
          color: '#FEEC41',
          letterSpacing: 3,
        }}
      >
        NEXT: The most important concept → BREAK-EVEN AGE
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.28) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.5,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_NoWarning;
```

```tsx
import React from 'react';
Audio: "The break-even age. The SSA will tell you that if you claim early, you will collect more checks over your lifetime because you are collecting them for a longer period of time."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18_BreakEvenIntro',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_BreakEvenIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big title reveal
  const titleOp = sp(15);
  const titleScale = sp(15, { damping: 7 });

  // Chart container
  const chartOp = sp(80);

  // Two lines drawing
  const earlyLineProgress = interpolate(frame, [100, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const lateLineProgress = interpolate(frame, [130, 230], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Early advantage callout
  const calloutOp = sp(240);

  // Chart dimensions
  const chartX = 200;
  const chartY = 380;
  const chartW = 1520;
  const chartH = 480;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(254,236,65,0.1) 0%, transparent 50%, rgba(0,0,0,0.08) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '10%',
          left: '5%',
          width: 250,
          height: 250,
          borderRadius: '50%',
          backgroundColor: '#FEEC41',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '8%',
          right: '5%',
          width: 200,
          height: 400,
          backgroundColor: '#000000',
          opacity: 0.05,
          transform: `rotate(${20 + frame * 0.05}deg)`,
        }}
      />

      {/* Texture - graph paper */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 25 }, (_, i) => (
          <line key={`v${i}`} x1={i * 80} y1={0} x2={i * 80} y2={1080} stroke="#000000" strokeWidth={0.5} />
        ))}
        {Array.from({ length: 14 }, (_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 80} x2={1920} y2={i * 80} stroke="#000000" strokeWidth={0.5} />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }, (_, i) => {
        const seed = i + 40;
        const x = (seededRandom(seed) * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 5,
              height: 5,
              backgroundColor: '#000000',
              opacity: 0.25,
            }}
          />
        );
      })}

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: '50%',
          transform: 'translateX(-50%)',
          fontFamily: 'Inter, Poppins',
          fontSize: 32,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 5,
          opacity: sp(8),
          backgroundColor: '#FEEC41',
          padding: '6px 24px',
          border: '3px solid #000000',
        }}
      >
        THE MOST IMPORTANT CONCEPT
      </div>

      {/* MAIN TITLE: BREAK-EVEN AGE */}
      <div
        style={{
          position: 'absolute',
          top: 150,
          left: '50%',
          transform: `translateX(-50%) scale(${titleScale})`,
          opacity: titleOp,
          display: 'flex',
          alignItems: 'baseline',
          gap: 20,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 187,
            fontWeight: 900,
            color: '#000000',
            backgroundColor: '#FEEC41',
            padding: '0 30px',
            lineHeight: 1,
            border: '7px solid #000000',
            boxShadow: '16px 16px 0 #000000',
            letterSpacing: 3,
          }}
        >
          BREAK-EVEN
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 119,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: 4,
          }}
        >
          AGE
        </div>
      </div>

      {/* Chart */}
      <div style={{ opacity: chartOp }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {/* Axes */}
          <line x1={chartX} y1={chartY + chartH} x2={chartX + chartW} y2={chartY + chartH} stroke="#000000" strokeWidth="5" />
          <line x1={chartX} y1={chartY} x2={chartX} y2={chartY + chartH} stroke="#000000" strokeWidth="5" />

          {/* Y axis label */}
          <text x={chartX - 30} y={chartY - 20} fontFamily="Inter" fontSize="26" fontWeight="800" fill="#000000" textAnchor="start">
            Total $ Collected
          </text>

          {/* X axis labels */}
          {['62', '67', '72', '78', '85'].map((age, i) => (
            <g key={age}>
              <line
                x1={chartX + (chartW / 4) * i}
                y1={chartY + chartH}
                x2={chartX + (chartW / 4) * i}
                y2={chartY + chartH + 14}
                stroke="#000000"
                strokeWidth="4"
              />
              <text
                x={chartX + (chartW / 4) * i}
                y={chartY + chartH + 50}
                fontFamily="Inter"
                fontSize="32"
                fontWeight="900"
                fill="#000000"
                textAnchor="middle"
              >
                {age}
              </text>
            </g>
          ))}

          {/* Early claim line - starts higher, climbs slower */}
          <line
            x1={chartX}
            y1={chartY + chartH - 50}
            x2={chartX + chartW * earlyLineProgress}
            y2={chartY + chartH - 50 - (chartW * earlyLineProgress) * 0.22}
            stroke="#000000"
            strokeWidth="9"
          />
          {/* Late claim line - starts at zero, climbs faster */}
          <line
            x1={chartX + chartW * 0.3}
            y1={chartY + chartH - 20}
            x2={chartX + chartW * 0.3 + (chartW * 0.7) * lateLineProgress}
            y2={chartY + chartH - 20 - (chartW * 0.7 * lateLineProgress) * 0.42}
            stroke="#FEEC41"
            strokeWidth="9"
            strokeDasharray="0"
            filter="drop-shadow(0 0 0 #000)"
          />
          {/* Yellow stroke wrap */}
          <line
            x1={chartX + chartW * 0.3}
            y1={chartY + chartH - 20}
            x2={chartX + chartW * 0.3 + (chartW * 0.7) * lateLineProgress}
            y2={chartY + chartH - 20 - (chartW * 0.7 * lateLineProgress) * 0.42}
            stroke="#000000"
            strokeWidth="3"
          />
        </svg>

        {/* Line labels */}
        <div
          style={{
            position: 'absolute',
            top: chartY + chartH - 130,
            left: chartX + 30,
            fontFamily: 'Inter, Poppins',
            fontSize: 30,
            fontWeight: 800,
            color: '#000000',
            opacity: earlyLineProgress,
            backgroundColor: '#FCD1D1',
            border: '3px solid #000000',
            padding: '4px 12px',
          }}
        >
          CLAIM @ 62 (early start)
        </div>
        <div
          style={{
            position: 'absolute',
            top: chartY + 60,
            right: 180,
            fontFamily: 'Inter, Poppins',
            fontSize: 30,
            fontWeight: 800,
            color: '#000000',
            opacity: lateLineProgress,
            backgroundColor: '#FEEC41',
            border: '3px solid #000000',
            padding: '4px 12px',
          }}
        >
          WAIT TILL 67 (steeper climb)
        </div>
      </div>

      {/* Early advantage callout */}
      <div
        style={{
          position: 'absolute',
          top: chartY + chartH - 150,
          left: chartX + 60,
          opacity: calloutOp,
          backgroundColor: '#000000',
          padding: '10px 18px',
          fontFamily: 'Inter, Poppins',
          fontSize: 32,
          fontWeight: 800,
          color: '#FEEC41',
          letterSpacing: 1,
          transform: `translateY(${(1 - calloutOp) * 20}px)`,
        }}
      >
        Early claim WINS for first 8 years
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.5,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_BreakEvenIntro;
```

```tsx
import React from 'react';
Audio: "And mathematically, they are right. For a while. If you claim at 62, you get 8 extra years of checks. But those checks are 30% smaller."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19_ForAWhile',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_ForAWhile: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // "They're right" stamp
  const rightOp = sp(15);
  const rightScale = sp(15, { damping: 7 });

  // "BUT" plot twist
  const butScale = sp(80, { damping: 6 });
  const butOp = sp(80);

  // "For a while" subscript
  const whileOp = sp(50);

  // 8 check icons reveal
  const check1 = sp(135);
  const check2 = sp(145);
  const check3 = sp(155);
  const check4 = sp(165);
  const check5 = sp(175);
  const check6 = sp(185);
  const check7 = sp(195);
  const check8 = sp(205);

  // 30% smaller stamp
  const smallerOp = sp(255);
  const smallerScale = sp(255, { damping: 6 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 50%, rgba(254,236,65,0.15) 0%, transparent 50%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          right: '8%',
          width: 350,
          height: 350,
          borderRadius: '50%',
          border: '5px solid #000000',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '8%',
          left: '5%',
          width: 200,
          height: 320,
          backgroundColor: '#FEEC41',
          opacity: 0.07,
          transform: `rotate(${-25 + frame * 0.06}deg)`,
          borderRadius: 30,
        }}
      />

      {/* Texture - vertical lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 40 }, (_, i) => (
          <line key={i} x1={i * 50} y1={0} x2={i * 50} y2={1080} stroke="#000000" strokeWidth={1} />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }, (_, i) => {
        const seed = i + 60;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.6) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 6,
              height: 6,
              backgroundColor: '#000000',
              opacity: 0.3,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* TOP: "Mathematically, they're right" */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${rightScale})`,
          opacity: rightOp,
          display: 'flex',
          alignItems: 'center',
          gap: 18,
        }}
      >
        {/* Checkmark */}
        <svg width="80" height="80" viewBox="0 0 80 80">
          <circle cx="40" cy="40" r="36" fill="#FEEC41" stroke="#000000" strokeWidth="5" />
          <path d="M 22 42 L 35 55 L 60 28" stroke="#000000" strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 2,
          }}
        >
          Mathematically, they ARE right
        </div>
      </div>

      {/* "For a while" italic small */}
      <div
        style={{
          position: 'absolute',
          top: 190,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: whileOp,
          fontFamily: 'Inter, Poppins',
          fontSize: 38,
          fontWeight: 700,
          color: '#000000',
          fontStyle: 'italic',
          opacity: whileOp * 0.7,
        }}
      >
        — for a while —
      </div>

      {/* HUGE "BUT" - the twist */}
      <div
        style={{
          position: 'absolute',
          top: 270,
          left: '50%',
          transform: `translateX(-50%) scale(${butScale})`,
          opacity: butOp,
          fontFamily: 'Inter, Poppins',
          fontSize: 221,
          fontWeight: 900,
          color: '#FEEC41',
          backgroundColor: '#000000',
          padding: '0 60px',
          lineHeight: 1,
          border: '8px solid #000000',
          boxShadow: '18px 18px 0 #FEEC41',
          letterSpacing: 8,
        }}
      >
        BUT...
      </div>

      {/* 8 checks visualization */}
      <div
        style={{
          position: 'absolute',
          top: 540,
          left: 0,
          right: 0,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: 16,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 800,
            color: '#000000',
            opacity: sp(120),
            letterSpacing: 3,
          }}
        >
          Yes, you get 8 EXTRA YEARS of checks:
        </div>

        {/* Check icons row */}
        <div style={{ display: 'flex', gap: 14, marginTop: 16 }}>
          {[check1, check2, check3, check4, check5, check6, check7, check8].map((opVal, i) => (
            <div
              key={i}
              style={{
                width: 130,
                height: 90,
                backgroundColor: '#FEEC41',
                border: '4px solid #000000',
                boxShadow: '6px 6px 0 #000000',
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                transform: `scale(${opVal}) translateY(${(1 - opVal) * 30}px)`,
                opacity: opVal,
              }}
            >
              <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 800, color: '#000000' }}>
                YEAR {i + 1}
              </div>
              <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 900, color: '#000000' }}>
                $
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* 30% smaller stamp - bottom right */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: `translateX(-50%) scale(${smallerScale}) rotate(${interpolate(smallerScale, [0, 1], [-15, -3])}deg)`,
          opacity: smallerOp,
          backgroundColor: '#000000',
          padding: '18px 36px',
          border: '6px solid #000000',
          boxShadow: '12px 12px 0 #FEEC41',
          display: 'flex',
          alignItems: 'center',
          gap: 18,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 76,
            fontWeight: 900,
            color: '#FEEC41',
            letterSpacing: 2,
          }}
        >
          ...but every check is
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 102,
            fontWeight: 900,
            color: '#FCD1D1',
            backgroundColor: '#FEEC41',
            color: '#000000',
            padding: '0 20px',
            letterSpacing: 2,
          }}
        >
          30% SMALLER
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.5,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_ForAWhile;
```

```tsx
import React from 'react';
Audio: "The break-even age is the exact age where the total amount of money you would have received by waiting finally catches up to the total amount of money you received by claiming early."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20_TheDefinition',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20_TheDefinition: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Definition label
  const labelOp = sp(10);

  // Two stacks - early vs late
  const earlyStackOp = sp(50);
  const lateStackOp = sp(80);

  // Animated stack heights
  const stackProgress = interpolate(frame, [100, 250], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Early bar: starts tall, grows slowly
  const earlyHeight = interpolate(stackProgress, [0, 1], [80, 380]);
  // Late bar: starts at 0, grows fast to catch up
  const lateHeight = interpolate(stackProgress, [0, 1], [0, 380]);

  // Convergence flash
  const convergeFlash = interpolate(frame, [240, 260, 280], [0, 1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // BREAK-EVEN POINT label
  const breakEvenOp = sp(265);
  const breakEvenScale = sp(265, { damping: 6 });

  // Catch up arrows
  const arrowOp = sp(140);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 70%, rgba(254,236,65,0.18) 0%, transparent 55%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '8%',
          left: '8%',
          width: 300,
          height: 300,
          borderRadius: 60,
          backgroundColor: '#000000',
          opacity: 0.05,
          transform: `rotate(${frame * 0.07}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '12%',
          right: '8%',
          width: 0,
          height: 0,
          borderLeft: '180px solid transparent',
          borderRight: '180px solid transparent',
          borderBottom: '300px solid #FEEC41',
          opacity: 0.08,
          transform: `rotate(${-frame * 0.08}deg)`,
        }}
      />

      {/* Texture - hexagon pattern */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 12 }, (_, i) =>
          Array.from({ length: 8 }, (_, j) => {
            const cx = i * 170 + (j % 2) * 85;
            const cy = j * 150;
            return (
              <polygon
                key={`${i}-${j}`}
                points={`${cx + 40},${cy} ${cx + 60},${cy + 35} ${cx + 40},${cy + 70} ${cx},${cy + 70} ${cx - 20},${cy + 35} ${cx},${cy}`}
                fill="none"
                stroke="#000000"
                strokeWidth="1"
              />
            );
          })
        )}
      </svg>

      {/* Header */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: labelOp,
          display: 'flex',
          alignItems: 'center',
          flexDirection: 'column',
          gap: 8,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 32,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 5,
            backgroundColor: '#FEEC41',
            padding: '4px 20px',
            border: '3px solid #000000',
          }}
        >
          DEFINITION
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: 3,
          }}
        >
          When the LATE claim CATCHES UP
        </div>
      </div>

      {/* Two stacks comparison */}
      <div
        style={{
          position: 'absolute',
          bottom: 200,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'flex-end',
          gap: 120,
        }}
      >
        {/* Early claim stack */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            opacity: earlyStackOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 800,
              color: '#000000',
              marginBottom: 10,
              letterSpacing: 2,
            }}
          >
            CLAIMED EARLY
          </div>
          {/* Bar */}
          <div
            style={{
              width: 280,
              height: earlyHeight,
              backgroundColor: '#000000',
              border: '5px solid #000000',
              boxShadow: '10px 10px 0 #FEEC41',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              transition: 'all 0.1s linear',
              position: 'relative',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 64,
                fontWeight: 900,
                color: '#FEEC41',
                writingMode: 'vertical-rl',
                transform: 'rotate(180deg)',
                letterSpacing: 2,
              }}
            >
              TOTAL $
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 47,
              fontWeight: 900,
              color: '#000000',
              backgroundColor: '#FEEC41',
              padding: '4px 18px',
              border: '4px solid #000000',
              marginTop: 14,
            }}
          >
            AGE 62
          </div>
        </div>

        {/* Catch-up arrow */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            marginBottom: 200,
            opacity: arrowOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 2,
              backgroundColor: '#FEEC41',
              padding: '6px 18px',
              border: '4px solid #000000',
              boxShadow: '6px 6px 0 #000000',
            }}
          >
            CATCH UP
          </div>
          <svg width="120" height="60" viewBox="0 0 120 60" style={{ marginTop: 8 }}>
            <path d="M 10 30 L 105 30 M 85 12 L 110 30 L 85 48" stroke="#000000" strokeWidth="7" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* Late claim stack */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            opacity: lateStackOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 800,
              color: '#000000',
              marginBottom: 10,
              letterSpacing: 2,
            }}
          >
            WAITED
          </div>
          {/* Bar */}
          <div
            style={{
              width: 280,
              height: lateHeight,
              backgroundColor: '#FEEC41',
              border: '5px solid #000000',
              boxShadow: '10px 10px 0 #000000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              transition: 'all 0.1s linear',
              position: 'relative',
              opacity: lateHeight > 30 ? 1 : 0,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 64,
                fontWeight: 900,
                color: '#000000',
                writingMode: 'vertical-rl',
                transform: 'rotate(180deg)',
                letterSpacing: 2,
              }}
            >
              TOTAL $
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 47,
              fontWeight: 900,
              color: '#FEEC41',
              backgroundColor: '#000000',
              padding: '4px 18px',
              border: '4px solid #000000',
              marginTop: 14,
            }}
          >
            AGE 67
          </div>
        </div>
      </div>

      {/* Convergence flash */}
      <div
        style={{
          position: 'absolute',
          top: '40%',
          left: '50%',
          transform: 'translate(-50%, -50%)',
          width: 800,
          height: 800,
          borderRadius: '50%',
          backgroundColor: '#FEEC41',
          opacity: convergeFlash * 0.4,
          pointerEvents: 'none',
        }}
      />

      {/* BREAK-EVEN POINT callout */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${breakEvenScale})`,
          opacity: breakEvenOp,
          backgroundColor: '#000000',
          padding: '14px 36px',
          border: '6px solid #000000',
          boxShadow: '12px 12px 0 #FEEC41',
          display: 'flex',
          alignItems: 'center',
          gap: 16,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 900,
            color: '#FEEC41',
            letterSpacing: 3,
          }}
        >
          ★ BREAK-EVEN POINT
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 700,
            color: '#FCD1D1',
            letterSpacing: 1,
          }}
        >
          — totals are equal here
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.5,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene20_TheDefinition;
```

