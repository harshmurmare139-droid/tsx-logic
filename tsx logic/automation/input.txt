```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_SonaDilMein',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_SonaDilMein: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  // Heart pulse
  const heartPulse = 1 + Math.sin(frame * 0.15) * 0.08;
  const heartGlow = 0.5 + Math.sin(frame * 0.12) * 0.3;

  // Main text entrance
  const sonaScale = sp(20, { stiffness: 80 });
  const lineGrow = interpolate(frame, [40, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const dilOpacity = sp(70);
  const bharatScale = sp(95, { stiffness: 90 });

  // Gold particles count
  const particles = Array.from({ length: 18 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 45%, rgba(250,185,91,0.18) 0%, rgba(143,221,223,0) 60%)',
      }} />

      {/* Animated shape layer - large drifting circles */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '2px solid rgba(232,237,242,0.08)',
        left: -200 + Math.sin(frame * 0.01) * 40,
        top: -200 + Math.cos(frame * 0.012) * 30,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        border: '2px solid rgba(250,185,91,0.1)',
        right: -150 + Math.cos(frame * 0.015) * 40,
        bottom: -200 + Math.sin(frame * 0.013) * 30,
        transform: `rotate(${-frame * 0.15}deg)`,
      }} />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="grid01" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid01)" />
      </svg>

      {/* Floating gold particles */}
      {particles.map(i => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 50) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 15;
        const float = Math.cos(frame * 0.025 + i * 0.5) * 20;
        const sz = 6 + seededRandom(i + 100) * 8;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + drift,
            top: py + float,
            width: sz, height: sz, borderRadius: '50%',
            background: '#FAB95B',
            opacity: 0.4 + Math.sin(frame * 0.05 + i) * 0.3,
            boxShadow: '0 0 20px rgba(250,185,91,0.6)',
          }} />
        );
      })}

      {/* Main content wrapper with camera */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>

        {/* Giant SONA text */}
        <div style={{
          position: 'absolute', top: '18%', left: '50%',
          transform: `translateX(-50%) scale(${sonaScale})`,
          fontFamily: 'Yantra One',
          fontSize: 221, // 85% of 260
          color: '#FAB95B',
          textShadow: '0 8px 30px rgba(250,185,91,0.5), 0 0 80px rgba(250,185,91,0.3)',
          letterSpacing: '8px',
          fontWeight: 900,
        }}>
          सोना
        </div>

        {/* Decorative line under SONA */}
        <div style={{
          position: 'absolute', top: '38%', left: '50%',
          transform: 'translateX(-50%)',
          width: 400 * lineGrow,
          height: 4,
          background: 'linear-gradient(90deg, transparent, #E8EDF2, transparent)',
          opacity: lineGrow,
        }} />

        {/* Heart with gold inside */}
        <div style={{
          position: 'absolute', top: '48%', left: '50%',
          transform: `translate(-50%, -50%) scale(${heartPulse * dilOpacity})`,
          opacity: dilOpacity,
        }}>
          <svg width="280" height="260" viewBox="0 0 280 260">
            <defs>
              <radialGradient id="heartGrad" cx="50%" cy="40%" r="60%">
                <stop offset="0%" stopColor="#FAB95B" stopOpacity="1" />
                <stop offset="60%" stopColor="#FFC107" stopOpacity="0.9" />
                <stop offset="100%" stopColor="#ff0000" stopOpacity="0.85" />
              </radialGradient>
              <filter id="heartGlow1" x="-50%" y="-50%" width="200%" height="200%">
                <feGaussianBlur stdDeviation="8" />
              </filter>
            </defs>
            <path
              d="M140 230 C 50 170, 10 110, 50 60 C 80 25, 130 35, 140 70 C 150 35, 200 25, 230 60 C 270 110, 230 170, 140 230 Z"
              fill="url(#heartGrad)"
              filter="url(#heartGlow1)"
              opacity={heartGlow}
            />
            <path
              d="M140 230 C 50 170, 10 110, 50 60 C 80 25, 130 35, 140 70 C 150 35, 200 25, 230 60 C 270 110, 230 170, 140 230 Z"
              fill="url(#heartGrad)"
              stroke="#E8EDF2"
              strokeWidth="3"
            />
            {/* Gold coin inside */}
            <circle cx="140" cy="120" r="35" fill="#FAB95B" stroke="#E8EDF2" strokeWidth="2" />
            <text x="140" y="135" textAnchor="middle" fontSize="40" fontFamily="Yantra One" fill="#1C1C1C" fontWeight="900">सो</text>
          </svg>
        </div>

        {/* "हर भारतीय के दिल में" text */}
        <div style={{
          position: 'absolute', top: '75%', left: '50%',
          transform: `translateX(-50%) scale(${bharatScale})`,
          fontFamily: 'Yantra One',
          fontSize: 76, // 85% of 90
          color: '#E8EDF2',
          letterSpacing: '3px',
          textAlign: 'center',
          textShadow: '0 4px 20px rgba(28,28,28,0.3)',
        }}>
          हर भारतीय के <span style={{ color: '#FAB95B' }}>दिल</span> में
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(28,28,28,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Scene01_SonaDilMein;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_DadiAurLocker',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_DadiAurLocker: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split panel animations
  const leftPanel = sp(10, { stiffness: 70 });
  const rightPanel = sp(140, { stiffness: 70 });
  const dividerGrow = interpolate(frame, [30, 80], [0, 1], { extrapolateRight: 'clamp' });

  // Bangle rotation
  const bangleRotate = frame * 0.5;
  const bangleScale = sp(40);

  // Locker open
  const lockerScale = sp(160);
  const biscuitFloat = Math.sin(frame * 0.08) * 8;
  const biscuitGlow = 0.6 + Math.sin(frame * 0.1) * 0.4;

  // Label texts
  const leftLabel = sp(70);
  const rightLabel = sp(200);

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(250,185,91,0.08) 0%, rgba(143,221,223,0) 100%)',
      }} />

      {/* Diagonal stripe texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="stripe02" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#E8EDF2" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#stripe02)" />
      </svg>

      {/* Drifting triangles */}
      <svg style={{
        position: 'absolute', top: 100, left: 150,
        transform: `rotate(${frame * 0.3}deg)`,
        opacity: 0.08,
      }} width="300" height="300">
        <polygon points="150,20 280,260 20,260" fill="none" stroke="#E8EDF2" strokeWidth="3" />
      </svg>
      <svg style={{
        position: 'absolute', bottom: 80, right: 200,
        transform: `rotate(${-frame * 0.25}deg)`,
        opacity: 0.08,
      }} width="250" height="250">
        <polygon points="125,10 240,220 10,220" fill="none" stroke="#FAB95B" strokeWidth="3" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const px = seededRandom(i + 2) * 1920;
        const py = seededRandom(i + 60) * 1080;
        const drift = Math.sin(frame * 0.03 + i) * 12;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px, top: py + drift,
            width: 6, height: 6, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.5,
            boxShadow: '0 0 12px #FAB95B',
          }} />
        );
      })}

      {/* Central divider line */}
      <div style={{
        position: 'absolute', top: '15%', left: '50%',
        transform: 'translateX(-50%)',
        width: 3, height: `${70 * dividerGrow}%`,
        background: 'linear-gradient(180deg, transparent, #E8EDF2 20%, #FAB95B 50%, #E8EDF2 80%, transparent)',
        opacity: 0.6,
      }} />

      {/* LEFT PANEL — Dadi ki Almari */}
      <div style={{
        position: 'absolute', top: '50%', left: '25%',
        transform: `translate(-50%, -50%) scale(${leftPanel})`,
        textAlign: 'center',
      }}>
        {/* Almari / Cupboard SVG */}
        <svg width="360" height="420" viewBox="0 0 360 420">
          <defs>
            <linearGradient id="cupGrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stopColor="#2D2D2D" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </linearGradient>
          </defs>
          {/* Cupboard body */}
          <rect x="40" y="40" width="280" height="360" fill="url(#cupGrad)" stroke="#E8EDF2" strokeWidth="3" rx="8" />
          {/* Door split */}
          <line x1="180" y1="50" x2="180" y2="390" stroke="#E8EDF2" strokeWidth="2" opacity="0.4" />
          {/* Handles */}
          <circle cx="160" cy="220" r="6" fill="#FAB95B" />
          <circle cx="200" cy="220" r="6" fill="#FAB95B" />
          {/* Shelf */}
          <line x1="50" y1="180" x2="310" y2="180" stroke="#E8EDF2" strokeWidth="2" opacity="0.3" />
          {/* Top decorative */}
          <rect x="30" y="30" width="300" height="15" fill="#FAB95B" rx="3" />
        </svg>

        {/* Bangles floating */}
        <div style={{
          position: 'absolute', top: 90, left: '50%',
          transform: `translateX(-50%) scale(${bangleScale}) rotate(${bangleRotate}deg)`,
        }}>
          <svg width="180" height="180" viewBox="0 0 180 180">
            <circle cx="90" cy="90" r="70" fill="none" stroke="#FAB95B" strokeWidth="10" />
            <circle cx="90" cy="90" r="55" fill="none" stroke="#FFC107" strokeWidth="8" />
            <circle cx="90" cy="90" r="42" fill="none" stroke="#FAB95B" strokeWidth="6" />
            {/* Sparkle dots */}
            {[0, 60, 120, 180, 240, 300].map(a => (
              <circle
                key={a}
                cx={90 + Math.cos(a * Math.PI / 180) * 70}
                cy={90 + Math.sin(a * Math.PI / 180) * 70}
                r="4" fill="#E8EDF2"
              />
            ))}
          </svg>
        </div>

        {/* Label */}
        <div style={{
          marginTop: 30,
          fontFamily: 'Yantra One',
          fontSize: 60, // 85%
          color: '#E8EDF2',
          opacity: leftLabel,
          letterSpacing: '2px',
        }}>
          दादी की अलमारी
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 38,
          color: '#FAB95B',
          opacity: leftLabel * 0.85,
          marginTop: 8,
          letterSpacing: '1px',
        }}>
          सोने की चूड़ियाँ
        </div>
      </div>

      {/* RIGHT PANEL — Bank Locker */}
      <div style={{
        position: 'absolute', top: '50%', left: '75%',
        transform: `translate(-50%, -50%) scale(${rightPanel})`,
        textAlign: 'center',
      }}>
        {/* Bank locker SVG */}
        <svg width="360" height="420" viewBox="0 0 360 420">
          <defs>
            <linearGradient id="lockGrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stopColor="#2D2D2D" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </linearGradient>
          </defs>
          <rect x="40" y="40" width="280" height="360" fill="url(#lockGrad)" stroke="#E8EDF2" strokeWidth="3" rx="8" />
          {/* Locker grid */}
          {[0, 1, 2].map(row =>
            [0, 1, 2].map(col => (
              <g key={`${row}-${col}`}>
                <rect
                  x={60 + col * 80}
                  y={70 + row * 100}
                  width="70" height="85"
                  fill="#1C1C1C"
                  stroke="#FAB95B"
                  strokeWidth={row === 1 && col === 1 ? 4 : 1.5}
                  opacity={row === 1 && col === 1 ? 1 : 0.6}
                />
                <circle
                  cx={95 + col * 80}
                  cy={130 + row * 100}
                  r="4"
                  fill="#FAB95B"
                />
              </g>
            ))
          )}
        </svg>

        {/* Floating gold biscuit */}
        <div style={{
          position: 'absolute', top: 165 + biscuitFloat, left: 142,
          transform: `scale(${lockerScale})`,
        }}>
          <svg width="80" height="50" viewBox="0 0 80 50">
            <defs>
              <linearGradient id="biscGrad" x1="0" y1="0" x2="0" y2="1">
                <stop offset="0%" stopColor="#FFC107" />
                <stop offset="100%" stopColor="#FAB95B" />
              </linearGradient>
            </defs>
            <rect x="5" y="5" width="70" height="40" fill="url(#biscGrad)" stroke="#E8EDF2" strokeWidth="2" rx="4" />
            <text x="40" y="30" textAnchor="middle" fontFamily="Inter" fontSize="14" fontWeight="900" fill="#1C1C1C">999.9</text>
            <text x="40" y="42" textAnchor="middle" fontFamily="Inter" fontSize="8" fontWeight="700" fill="#1C1C1C">GOLD</text>
          </svg>
          {/* Glow */}
          <div style={{
            position: 'absolute', inset: -20,
            background: 'radial-gradient(circle, rgba(250,185,91,0.6), transparent 70%)',
            opacity: biscuitGlow,
            zIndex: -1,
          }} />
        </div>

        <div style={{
          marginTop: 30,
          fontFamily: 'Yantra One',
          fontSize: 60,
          color: '#E8EDF2',
          opacity: rightLabel,
          letterSpacing: '2px',
        }}>
          बैंक लॉकर
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 38,
          color: '#FAB95B',
          opacity: rightLabel * 0.85,
          marginTop: 8,
          letterSpacing: '1px',
        }}>
          सोने की बिस्किट
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(28,28,28,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_DadiAurLocker;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_TaxKaJaal',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_TaxKaJaal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Warning entrance
  const lekinScale = sp(15, { stiffness: 100 });
  const shake = frame < 50 ? Math.sin(frame * 0.8) * (50 - frame) * 0.3 : 0;

  // Net web drawing animation
  const netDraw = interpolate(frame, [60, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Gold coin in trap
  const coinDrop = interpolate(frame, [120, 180], [-300, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bounce(Easing.out) });
  const coinOpacity = sp(120);

  // TAX text
  const taxScale = sp(220, { stiffness: 90 });
  const taxPulse = 1 + Math.sin(frame * 0.2) * 0.05;

  // Bottom text
  const jaalText = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      {/* Darker gradient for warning mood */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(255,0,0,0.12) 0%, rgba(143,221,223,0) 70%)',
      }} />

      {/* Hexagonal grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="hex03" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,2 56,17 56,45 30,60 4,45 4,17" fill="none" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex03)" />
      </svg>

      {/* Drifting shapes */}
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '2px solid rgba(232,237,242,0.06)',
        top: -100, left: -100,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: '50%',
        border: '2px solid rgba(255,0,0,0.08)',
        bottom: -80, right: -80,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />

      {/* Red warning particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i + 3) * 1920;
        const py = seededRandom(i + 70) * 1080;
        const drift = Math.sin(frame * 0.04 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px, top: py + drift,
            width: 5, height: 5, borderRadius: '50%',
            background: i % 3 === 0 ? '#ff0000' : '#FAB95B',
            opacity: 0.5,
            boxShadow: `0 0 15px ${i % 3 === 0 ? '#ff0000' : '#FAB95B'}`,
          }} />
        );
      })}

      {/* Top warning text */}
      <div style={{
        position: 'absolute', top: '8%', left: '50%',
        transform: `translateX(-50%) scale(${lekinScale}) translateX(${shake}px)`,
        fontFamily: 'Yantra One',
        fontSize: 119, // 85% of 140
        color: '#E8EDF2',
        letterSpacing: '4px',
        textShadow: '0 6px 25px rgba(28,28,28,0.4)',
      }}>
        लेकिन <span style={{ color: '#ff0000' }}>रुको!</span>
      </div>

      {/* Spider web / Net - central */}
      <div style={{
        position: 'absolute', top: '52%', left: '50%',
        transform: 'translate(-50%, -50%)',
      }}>
        <svg width="600" height="600" viewBox="0 0 600 600">
          <defs>
            <filter id="webGlow" x="-20%" y="-20%" width="140%" height="140%">
              <feGaussianBlur stdDeviation="2" />
            </filter>
          </defs>
          {/* Radial lines */}
          {Array.from({ length: 12 }).map((_, i) => {
            const angle = (i * 30) * Math.PI / 180;
            const x2 = 300 + Math.cos(angle) * 280 * netDraw;
            const y2 = 300 + Math.sin(angle) * 280 * netDraw;
            return (
              <line
                key={i}
                x1="300" y1="300"
                x2={x2} y2={y2}
                stroke="#ff0000"
                strokeWidth="2"
                opacity="0.7"
              />
            );
          })}
          {/* Concentric circles */}
          {[60, 120, 180, 240].map((r, i) => (
            <circle
              key={r}
              cx="300" cy="300" r={r * netDraw}
              fill="none"
              stroke="#E8EDF2"
              strokeWidth="1.5"
              opacity={0.6 - i * 0.1}
              strokeDasharray="4 4"
            />
          ))}
          {/* Center danger marker */}
          <circle cx="300" cy="300" r={30 * netDraw} fill="#ff0000" opacity="0.3" />
          <circle cx="300" cy="300" r={15 * netDraw} fill="#ff0000" />
        </svg>
      </div>

      {/* Gold coin trapped in web */}
      <div style={{
        position: 'absolute', top: `calc(52% + ${coinDrop}px)`, left: '50%',
        transform: 'translate(-50%, -50%)',
        opacity: coinOpacity,
      }}>
        <svg width="140" height="140" viewBox="0 0 140 140">
          <defs>
            <radialGradient id="coinGrad03" cx="50%" cy="40%" r="60%">
              <stop offset="0%" stopColor="#FFC107" />
              <stop offset="100%" stopColor="#FAB95B" />
            </radialGradient>
          </defs>
          <circle cx="70" cy="70" r="55" fill="url(#coinGrad03)" stroke="#E8EDF2" strokeWidth="3" />
          <circle cx="70" cy="70" r="45" fill="none" stroke="#1C1C1C" strokeWidth="1.5" opacity="0.3" />
          <text x="70" y="85" textAnchor="middle" fontFamily="Yantra One" fontSize="50" fontWeight="900" fill="#1C1C1C">₹</text>
        </svg>
      </div>

      {/* TAX label badge */}
      <div style={{
        position: 'absolute', top: '78%', left: '50%',
        transform: `translateX(-50%) scale(${taxScale * taxPulse})`,
        background: '#ff0000',
        padding: '14px 50px',
        borderRadius: 12,
        boxShadow: '0 8px 30px rgba(255,0,0,0.5)',
        border: '3px solid #E8EDF2',
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 68, // 85% of 80
          color: '#E8EDF2',
          letterSpacing: '8px',
          fontWeight: 900,
        }}>
          TAX TRAP
        </div>
      </div>

      {/* Sub text */}
      <div style={{
        position: 'absolute', top: '92%', left: '50%',
        transform: 'translate(-50%, -50%)',
        fontFamily: 'Noto Sans Devanagari',
        fontSize: 41,
        color: '#E8EDF2',
        opacity: jaalText,
        letterSpacing: '2px',
      }}>
        सरकार का नया जाल
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(28,28,28,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_TaxKaJaal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_SGBReveal',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_SGBReveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Letter-by-letter reveal of SGB
  const sgbLetters = ['S', 'G', 'B'];
  const haanScale = sp(10, { stiffness: 90 });
  const certEntry = sp(50, { stiffness: 70 });
  const certRotate = interpolate(frame, [50, 110], [-12, 0], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Stat bar
  const barFill = interpolate(frame, [180, 270], [0, 87], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const statLabel = sp(180);
  const percentNum = Math.floor(interpolate(frame, [180, 270], [0, 87], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.03, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(250,185,91,0.1) 0%, rgba(143,221,223,0) 50%, rgba(232,237,242,0.05) 100%)',
      }} />

      {/* Dot grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="1920" height="1080">
        <defs>
          <pattern id="dots04" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#E8EDF2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots04)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        border: '3px dashed rgba(232,237,242,0.06)',
        top: -200, right: -200,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400,
        border: '2px solid rgba(250,185,91,0.1)',
        bottom: 100, left: 100,
        transform: `rotate(${-frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const px = seededRandom(i + 4) * 1920;
        const py = seededRandom(i + 80) * 1080;
        const drift = Math.sin(frame * 0.025 + i) * 14;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px, top: py + drift,
            width: 5, height: 5, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.5,
            boxShadow: '0 0 10px #FAB95B',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Top "हाँ भाई" */}
        <div style={{
          position: 'absolute', top: '8%', left: '50%',
          transform: `translateX(-50%) scale(${haanScale})`,
          fontFamily: 'Yantra One',
          fontSize: 95,
          color: '#E8EDF2',
          letterSpacing: '4px',
        }}>
          हाँ <span style={{ color: '#FAB95B' }}>भाई!</span>
        </div>

        {/* Certificate / Bond visual */}
        <div style={{
          position: 'absolute', top: '50%', left: '50%',
          transform: `translate(-50%, -50%) scale(${certEntry}) rotate(${certRotate}deg)`,
        }}>
          <svg width="780" height="440" viewBox="0 0 780 440">
            <defs>
              <linearGradient id="certBg" x1="0" y1="0" x2="1" y2="1">
                <stop offset="0%" stopColor="#2D2D2D" />
                <stop offset="100%" stopColor="#1C1C1C" />
              </linearGradient>
              <pattern id="certPattern" width="20" height="20" patternUnits="userSpaceOnUse">
                <circle cx="10" cy="10" r="0.8" fill="#FAB95B" opacity="0.3" />
              </pattern>
            </defs>
            {/* Main bg */}
            <rect x="20" y="20" width="740" height="400" fill="url(#certBg)" stroke="#FAB95B" strokeWidth="4" rx="12" />
            <rect x="20" y="20" width="740" height="400" fill="url(#certPattern)" rx="12" />
            {/* Inner border */}
            <rect x="40" y="40" width="700" height="360" fill="none" stroke="#FAB95B" strokeWidth="1.5" strokeDasharray="6 4" rx="6" />

            {/* RBI badge top */}
            <circle cx="100" cy="100" r="40" fill="#FAB95B" stroke="#E8EDF2" strokeWidth="2" />
            <text x="100" y="108" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#1C1C1C">RBI</text>

            {/* Title */}
            <text x="400" y="115" textAnchor="middle" fontFamily="Inter" fontSize="32" fontWeight="700" fill="#E8EDF2" letterSpacing="3">SOVEREIGN GOLD BOND</text>
            <text x="400" y="148" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="500" fill="#9E9E9E" letterSpacing="2">SCHEME OF INDIA</text>

            {/* Big SGB */}
            <text x="400" y="280" textAnchor="middle" fontFamily="Yantra One" fontSize="170" fontWeight="900" fill="#FAB95B" letterSpacing="12">SGB</text>

            {/* Bottom stamp */}
            <rect x="280" y="320" width="240" height="50" fill="none" stroke="#ff0000" strokeWidth="3" rx="4" />
            <text x="400" y="354" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#ff0000" letterSpacing="4">DISCONTINUED</text>
          </svg>
        </div>

      </AbsoluteFill>

      {/* Bottom stat: 87% don't know */}
      <div style={{
        position: 'absolute', bottom: '8%', left: '50%',
        transform: 'translateX(-50%)',
        width: 900,
        opacity: statLabel,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 38,
          color: '#E8EDF2',
          marginBottom: 12,
          textAlign: 'center',
          letterSpacing: '1px',
        }}>
          ज़्यादातर लोगों को अभी तक <span style={{ color: '#FAB95B' }}>पता नहीं</span>
        </div>
        {/* Progress bar */}
        <div style={{
          width: '100%',
          height: 28,
          background: 'rgba(28,28,28,0.4)',
          borderRadius: 14,
          overflow: 'hidden',
          border: '2px solid #E8EDF2',
          position: 'relative',
        }}>
          <div style={{
            width: `${barFill}%`,
            height: '100%',
            background: 'linear-gradient(90deg, #FAB95B, #ff0000)',
            borderRadius: 12,
            boxShadow: '0 0 20px rgba(255,0,0,0.5)',
          }} />
          <div style={{
            position: 'absolute', top: 0, right: 12,
            fontFamily: 'Inter', fontSize: 18, fontWeight: 900,
            color: '#E8EDF2', lineHeight: '28px',
          }}>
            {percentNum}% UNAWARE
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(28,28,28,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_SGBReveal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_TeenSawal',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_TeenSawal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Three question cards
  const card1 = sp(20, { stiffness: 75 });
  const card2 = sp(80, { stiffness: 75 });
  const card3 = sp(140, { stiffness: 75 });

  // Title
  const titleScale = sp(0);

  // Question marks pulse
  const qPulse1 = 1 + Math.sin(frame * 0.15) * 0.05;
  const qPulse2 = 1 + Math.sin(frame * 0.15 + 1) * 0.05;
  const qPulse3 = 1 + Math.sin(frame * 0.15 + 2) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(250,185,91,0.12) 0%, rgba(143,221,223,0) 60%)',
      }} />

      {/* Concentric circle texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        {[100, 200, 300, 400, 500, 600, 700, 800].map(r => (
          <circle key={r} cx="960" cy="540" r={r} fill="none" stroke="#E8EDF2" strokeWidth="1" />
        ))}
      </svg>

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 350, height: 350,
        border: '2px solid rgba(250,185,91,0.08)',
        top: 80, left: 80,
        transform: `rotate(${frame * 0.4}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 250, height: 250, borderRadius: '50%',
        border: '2px solid rgba(232,237,242,0.07)',
        bottom: 100, right: 150,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
      }} />

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i + 5) * 1920;
        const py = seededRandom(i + 90) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px, top: py + Math.sin(frame * 0.03 + i) * 12,
            width: 5, height: 5, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.5,
            boxShadow: '0 0 10px #FAB95B',
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: '8%', left: '50%',
        transform: `translateX(-50%) scale(${titleScale})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 95,
          color: '#E8EDF2',
          letterSpacing: '4px',
        }}>
          तीन <span style={{ color: '#FAB95B' }}>बड़े सवाल</span>
        </div>
        <div style={{
          width: 250,
          height: 4,
          background: 'linear-gradient(90deg, transparent, #FAB95B, transparent)',
          margin: '15px auto 0',
        }} />
      </div>

      {/* Three cards */}
      <div style={{
        position: 'absolute', top: '52%', left: '50%',
        transform: 'translate(-50%, -50%)',
        display: 'flex',
        gap: 40,
      }}>
        {/* Card 1 */}
        <div style={{
          width: 380, height: 440,
          background: 'linear-gradient(180deg, #2D2D2D, #1C1C1C)',
          border: '3px solid #FAB95B',
          borderRadius: 18,
          padding: '40px 30px',
          textAlign: 'center',
          transform: `scale(${card1}) translateY(${(1 - card1) * 50}px)`,
          boxShadow: '0 12px 40px rgba(28,28,28,0.3)',
          position: 'relative',
        }}>
          {/* Number badge */}
          <div style={{
            position: 'absolute', top: -25, left: '50%', transform: 'translateX(-50%)',
            width: 60, height: 60, borderRadius: '50%',
            background: '#FAB95B', color: '#1C1C1C',
            fontFamily: 'Yantra One', fontSize: 38, fontWeight: 900,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            border: '3px solid #E8EDF2',
          }}>1</div>

          {/* Question mark with rupee */}
          <div style={{ marginTop: 20, transform: `scale(${qPulse1})` }}>
            <svg width="120" height="120" viewBox="0 0 120 120">
              <circle cx="60" cy="60" r="50" fill="none" stroke="#FAB95B" strokeWidth="3" strokeDasharray="4 4" />
              <text x="60" y="80" textAnchor="middle" fontFamily="Yantra One" fontSize="72" fontWeight="900" fill="#FAB95B">₹?</text>
            </svg>
          </div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 51, // 85% of 60
            color: '#E8EDF2',
            marginTop: 20,
            letterSpacing: '2px',
          }}>
            कितना?
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 29,
            color: '#9E9E9E',
            marginTop: 8,
          }}>
            टैक्स कितना लगेगा
          </div>
        </div>

        {/* Card 2 */}
        <div style={{
          width: 380, height: 440,
          background: 'linear-gradient(180deg, #2D2D2D, #1C1C1C)',
          border: '3px solid #FAB95B',
          borderRadius: 18,
          padding: '40px 30px',
          textAlign: 'center',
          transform: `scale(${card2}) translateY(${(1 - card2) * 50}px)`,
          boxShadow: '0 12px 40px rgba(28,28,28,0.3)',
          position: 'relative',
        }}>
          <div style={{
            position: 'absolute', top: -25, left: '50%', transform: 'translateX(-50%)',
            width: 60, height: 60, borderRadius: '50%',
            background: '#FAB95B', color: '#1C1C1C',
            fontFamily: 'Yantra One', fontSize: 38, fontWeight: 900,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            border: '3px solid #E8EDF2',
          }}>2</div>

          <div style={{ marginTop: 20, transform: `scale(${qPulse2})` }}>
            <svg width="120" height="120" viewBox="0 0 120 120">
              <circle cx="60" cy="60" r="50" fill="none" stroke="#FAB95B" strokeWidth="3" strokeDasharray="4 4" />
              <path d="M 35 50 Q 60 30, 85 50 Q 60 90, 35 50 Z" fill="none" stroke="#FAB95B" strokeWidth="4" />
              <circle cx="60" cy="50" r="6" fill="#FAB95B" />
              <text x="60" y="100" textAnchor="middle" fontFamily="Yantra One" fontSize="22" fill="#FAB95B" fontWeight="900">?</text>
            </svg>
          </div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 51,
            color: '#E8EDF2',
            marginTop: 20,
            letterSpacing: '2px',
          }}>
            कैसे?
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 29,
            color: '#9E9E9E',
            marginTop: 8,
          }}>
            किस तरीके से लगेगा
          </div>
        </div>

        {/* Card 3 */}
        <div style={{
          width: 380, height: 440,
          background: 'linear-gradient(180deg, #2D2D2D, #1C1C1C)',
          border: '3px solid #FAB95B',
          borderRadius: 18,
          padding: '40px 30px',
          textAlign: 'center',
          transform: `scale(${card3}) translateY(${(1 - card3) * 50}px)`,
          boxShadow: '0 12px 40px rgba(28,28,28,0.3)',
          position: 'relative',
        }}>
          <div style={{
            position: 'absolute', top: -25, left: '50%', transform: 'translateX(-50%)',
            width: 60, height: 60, borderRadius: '50%',
            background: '#FAB95B', color: '#1C1C1C',
            fontFamily: 'Yantra One', fontSize: 38, fontWeight: 900,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            border: '3px solid #E8EDF2',
          }}>3</div>

          <div style={{ marginTop: 20, transform: `scale(${qPulse3})` }}>
            <svg width="120" height="120" viewBox="0 0 120 120">
              <circle cx="60" cy="60" r="50" fill="none" stroke="#FAB95B" strokeWidth="3" strokeDasharray="4 4" />
              <path d="M 40 60 L 55 75 L 85 45" fill="none" stroke="#FAB95B" strokeWidth="6" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 51,
            color: '#E8EDF2',
            marginTop: 20,
            letterSpacing: '2px',
          }}>
            समझदारी?
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 29,
            color: '#9E9E9E',
            marginTop: 8,
          }}>
            पैसा लगाएँ या नहीं
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(28,28,28,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_TeenSawal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_KunalIntro',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_KunalIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Brand badge entrance
  const badgeScale = sp(10, { stiffness: 80 });
  const nameSlide = interpolate(frame, [40, 90], [-200, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const nameOp = sp(40);

  // Tag pills
  const pill1 = sp(120);
  const pill2 = sp(155);
  const pill3 = sp(190);

  // Big TASVEER text
  const tasveerScale = sp(220);

  // Camera slow pan
  const camY = interpolate(frame, [0, durationInFrames], [-10, 10], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(250,185,91,0.12) 0%, rgba(143,221,223,0) 50%)',
      }} />

      {/* Diagonal lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="diag06" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform="rotate(-30)">
            <line x1="0" y1="30" x2="60" y2="30" stroke="#E8EDF2" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag06)" />
      </svg>

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        border: '3px solid rgba(232,237,242,0.06)',
        top: -250, left: -150,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        border: '2px solid rgba(250,185,91,0.08)',
        bottom: -150, right: -100,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const px = seededRandom(i + 6) * 1920;
        const py = seededRandom(i + 100) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px, top: py + Math.sin(frame * 0.025 + i) * 16,
            width: 5, height: 5, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.5,
            boxShadow: '0 0 12px #FAB95B',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>

        {/* Brand badge top */}
        <div style={{
          position: 'absolute', top: '10%', left: '50%',
          transform: `translateX(-50%) scale(${badgeScale})`,
          background: '#1C1C1C',
          border: '3px solid #FAB95B',
          padding: '16px 50px',
          borderRadius: 50,
          display: 'flex', alignItems: 'center', gap: 20,
          boxShadow: '0 10px 30px rgba(28,28,28,0.4)',
        }}>
          <svg width="50" height="50" viewBox="0 0 50 50">
            <circle cx="25" cy="25" r="22" fill="#FAB95B" stroke="#E8EDF2" strokeWidth="2" />
            <text x="25" y="34" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#1C1C1C">PB</text>
          </svg>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 41,
            fontWeight: 800,
            color: '#E8EDF2',
            letterSpacing: '2px',
          }}>
            ProBuffet <span style={{ color: '#FAB95B' }}>HQ</span>
          </div>
        </div>

        {/* "मैं कुनाल" intro */}
        <div style={{
          position: 'absolute', top: '28%', left: '50%',
          transform: `translateX(${-50 + (nameSlide / 12)}%) translateX(-50%)`,
          opacity: nameOp,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 41,
            color: '#E8EDF2',
            letterSpacing: '2px',
          }}>
            मैं हूँ
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 153, // 85% of 180
            color: '#FAB95B',
            letterSpacing: '6px',
            textShadow: '0 8px 30px rgba(250,185,91,0.4)',
            marginTop: 5,
          }}>
            कुनाल
          </div>
        </div>

        {/* Big TASVEER text */}
        <div style={{
          position: 'absolute', top: '62%', left: '50%',
          transform: `translateX(-50%) scale(${tasveerScale})`,
          fontFamily: 'Yantra One',
          fontSize: 102, // 85% of 120
          color: '#E8EDF2',
          letterSpacing: '4px',
          textAlign: 'center',
        }}>
          पूरी <span style={{ color: '#FAB95B' }}>तस्वीर</span> दिखाऊँगा
        </div>

        {/* Tag pills */}
        <div style={{
          position: 'absolute', top: '82%', left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 25,
        }}>
          {/* Pill 1 - NUMBERS */}
          <div style={{
            transform: `scale(${pill1})`,
            background: '#1C1C1C',
            border: '2px solid #FAB95B',
            padding: '14px 30px',
            borderRadius: 30,
            display: 'flex', alignItems: 'center', gap: 12,
          }}>
            <svg width="28" height="28" viewBox="0 0 28 28">
              <text x="14" y="22" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#FAB95B">#</text>
            </svg>
            <span style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#E8EDF2', letterSpacing: '2px' }}>NUMBERS</span>
          </div>

          {/* Pill 2 - FACTS */}
          <div style={{
            transform: `scale(${pill2})`,
            background: '#1C1C1C',
            border: '2px solid #FAB95B',
            padding: '14px 30px',
            borderRadius: 30,
            display: 'flex', alignItems: 'center', gap: 12,
          }}>
            <svg width="28" height="28" viewBox="0 0 28 28">
              <path d="M 6 14 L 12 20 L 22 8" fill="none" stroke="#FAB95B" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <span style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#E8EDF2', letterSpacing: '2px' }}>FACTS</span>
          </div>

          {/* Pill 3 - NO BS */}
          <div style={{
            transform: `scale(${pill3})`,
            background: '#ff0000',
            border: '2px solid #E8EDF2',
            padding: '14px 30px',
            borderRadius: 30,
            display: 'flex', alignItems: 'center', gap: 12,
          }}>
            <svg width="28" height="28" viewBox="0 0 28 28">
              <circle cx="14" cy="14" r="11" fill="none" stroke="#E8EDF2" strokeWidth="3" />
              <line x1="7" y1="21" x2="21" y2="7" stroke="#E8EDF2" strokeWidth="3" strokeLinecap="round" />
            </svg>
            <span style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#E8EDF2', letterSpacing: '2px' }}>NO JUMLA</span>
          </div>
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(28,28,28,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_KunalIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_SGBKyaTha',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_SGBKyaTha: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleScale = sp(0);

  // RBI building entrance
  const rbiScale = sp(40, { stiffness: 70 });

  // Arrow flow
  const arrowProgress = interpolate(frame, [110, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Bond document
  const bondScale = sp(180, { stiffness: 80 });

  // Connection label
  const connLabel = sp(240);

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(250,185,91,0.1) 0%, rgba(143,221,223,0) 60%)',
      }} />

      {/* Cross-hatch texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="cross07" width="50" height="50" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="50" y2="50" stroke="#E8EDF2" strokeWidth="1" />
            <line x1="50" y1="0" x2="0" y2="50" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross07)" />
      </svg>

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 600, height: 600, borderRadius: '50%',
        border: '2px solid rgba(232,237,242,0.06)',
        top: 50, right: -200,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400,
        border: '3px dashed rgba(250,185,91,0.08)',
        bottom: -100, left: -100,
        transform: `rotate(${frame * 0.25}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const px = seededRandom(i + 7) * 1920;
        const py = seededRandom(i + 110) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px, top: py + Math.sin(frame * 0.03 + i) * 14,
            width: 5, height: 5, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.5,
            boxShadow: '0 0 10px #FAB95B',
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: '8%', left: '50%',
        transform: `translateX(-50%) scale(${titleScale})`,
        fontFamily: 'Yantra One',
        fontSize: 85,
        color: '#E8EDF2',
        textAlign: 'center',
        letterSpacing: '3px',
      }}>
        SGB <span style={{ color: '#FAB95B' }}>असल में क्या था?</span>
      </div>

      {/* RBI Building - left */}
      <div style={{
        position: 'absolute', top: '52%', left: '22%',
        transform: `translate(-50%, -50%) scale(${rbiScale})`,
        textAlign: 'center',
      }}>
        <svg width="320" height="380" viewBox="0 0 320 380">
          <defs>
            <linearGradient id="rbiGrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stopColor="#2D2D2D" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </linearGradient>
          </defs>
          {/* Base */}
          <rect x="20" y="280" width="280" height="80" fill="url(#rbiGrad)" stroke="#FAB95B" strokeWidth="3" />
          {/* Steps */}
          <rect x="40" y="260" width="240" height="22" fill="#2D2D2D" stroke="#FAB95B" strokeWidth="2" />
          {/* Pillars */}
          {[60, 110, 160, 210, 260].map(x => (
            <rect key={x} x={x - 12} y="140" width="24" height="120" fill="#E8EDF2" stroke="#FAB95B" strokeWidth="1.5" />
          ))}
          {/* Top entablature */}
          <rect x="20" y="120" width="280" height="22" fill="#FAB95B" stroke="#E8EDF2" strokeWidth="2" />
          {/* Pediment */}
          <polygon points="20,120 160,40 300,120" fill="url(#rbiGrad)" stroke="#FAB95B" strokeWidth="3" />
          {/* RBI Seal */}
          <circle cx="160" cy="90" r="22" fill="#FAB95B" stroke="#E8EDF2" strokeWidth="2" />
          <text x="160" y="98" textAnchor="middle" fontFamily="Inter" fontSize="14" fontWeight="900" fill="#1C1C1C">RBI</text>
        </svg>
        <div style={{
          marginTop: 15,
          fontFamily: 'Inter',
          fontSize: 41,
          fontWeight: 800,
          color: '#E8EDF2',
          letterSpacing: '2px',
        }}>
          RBI
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 27,
          color: '#FAB95B',
          marginTop: 4,
        }}>
          भारत का सेंट्रल बैंक
        </div>
      </div>

      {/* Arrow flow */}
      <svg style={{
        position: 'absolute', top: '45%', left: '36%',
        width: 500, height: 200,
      }} viewBox="0 0 500 200">
        <defs>
          <marker id="arrowH07" markerWidth="14" markerHeight="14" refX="10" refY="7" orient="auto">
            <polygon points="0 0, 14 7, 0 14" fill="#FAB95B" />
          </marker>
        </defs>
        <path
          d={`M 30 100 Q 250 ${50 + Math.sin(frame * 0.1) * 8} 470 100`}
          fill="none"
          stroke="#FAB95B"
          strokeWidth="5"
          strokeDasharray="800"
          strokeDashoffset={800 - 800 * arrowProgress}
          markerEnd="url(#arrowH07)"
          strokeLinecap="round"
        />
        {/* Moving dot on path */}
        {arrowProgress > 0.1 && (
          <circle r="8" fill="#E8EDF2">
            <animateMotion dur="2s" repeatCount="indefinite"
              path={`M 30 100 Q 250 50 470 100`} />
          </circle>
        )}
        <text x="250" y="35" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="700" fill="#1C1C1C" opacity={arrowProgress}>ISSUES</text>
      </svg>

      {/* Bond document - right */}
      <div style={{
        position: 'absolute', top: '52%', left: '78%',
        transform: `translate(-50%, -50%) scale(${bondScale})`,
        textAlign: 'center',
      }}>
        <svg width="320" height="380" viewBox="0 0 320 380">
          <defs>
            <linearGradient id="bondGrad07" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stopColor="#FAB95B" />
              <stop offset="100%" stopColor="#FFC107" />
            </linearGradient>
          </defs>
          {/* Document */}
          <rect x="30" y="20" width="260" height="340" fill="url(#bondGrad07)" stroke="#1C1C1C" strokeWidth="3" rx="6" />
          {/* Inner border */}
          <rect x="50" y="40" width="220" height="300" fill="none" stroke="#1C1C1C" strokeWidth="1.5" strokeDasharray="5 3" rx="3" />
          {/* Folded corner */}
          <polygon points="290,20 290,55 255,20" fill="#E8EDF2" stroke="#1C1C1C" strokeWidth="2" />
          {/* RBI stamp */}
          <circle cx="160" cy="80" r="22" fill="none" stroke="#1C1C1C" strokeWidth="2.5" />
          <text x="160" y="88" textAnchor="middle" fontFamily="Inter" fontSize="16" fontWeight="900" fill="#1C1C1C">RBI</text>
          {/* Title lines */}
          <rect x="70" y="130" width="180" height="6" fill="#1C1C1C" />
          <rect x="90" y="148" width="140" height="4" fill="#1C1C1C" opacity="0.7" />
          {/* Gold symbol */}
          <text x="160" y="220" textAnchor="middle" fontFamily="Yantra One" fontSize="60" fontWeight="900" fill="#1C1C1C">Au</text>
          {/* Bottom lines */}
          <rect x="70" y="260" width="180" height="3" fill="#1C1C1C" opacity="0.6" />
          <rect x="70" y="275" width="180" height="3" fill="#1C1C1C" opacity="0.6" />
          <rect x="70" y="290" width="120" height="3" fill="#1C1C1C" opacity="0.6" />
          {/* Signature line */}
          <line x1="170" y1="320" x2="260" y2="320" stroke="#1C1C1C" strokeWidth="1.5" />
          <text x="215" y="335" textAnchor="middle" fontFamily="Inter" fontSize="10" fill="#1C1C1C">Authorized</text>
        </svg>
        <div style={{
          marginTop: 15,
          fontFamily: 'Inter',
          fontSize: 41,
          fontWeight: 800,
          color: '#E8EDF2',
          letterSpacing: '2px',
        }}>
          SGB BOND
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 27,
          color: '#FAB95B',
          marginTop: 4,
        }}>
          सोने की कीमत से लिंक
        </div>
      </div>

      {/* Bottom label */}
      <div style={{
        position: 'absolute', bottom: '8%', left: '50%',
        transform: 'translateX(-50%)',
        opacity: connLabel,
        background: '#1C1C1C',
        border: '2px solid #FAB95B',
        padding: '14px 40px',
        borderRadius: 30,
        fontFamily: 'Inter',
        fontSize: 32,
        fontWeight: 700,
        color: '#E8EDF2',
        letterSpacing: '2px',
      }}>
        Government Backed <span style={{ color: '#FAB95B' }}>Gold Investment</span>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(28,28,28,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_SGBKyaTha;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_LinkedNoHassle',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_LinkedNoHassle: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Chart line drawing
  const lineProgress = interpolate(frame, [30, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Chart axis
  const axisOp = sp(10);

  // Cross icons (no need icons)
  const cross1 = sp(180, { stiffness: 100 });
  const cross2 = sp(220, { stiffness: 100 });
  const cross3 = sp(260, { stiffness: 100 });

  // Title
  const titleScale = sp(0);

  // Chart data - mock gold price trend
  const chartPoints = [
    { x: 50, y: 250 },
    { x: 150, y: 220 },
    { x: 250, y: 230 },
    { x: 350, y: 180 },
    { x: 450, y: 150 },
    { x: 550, y: 170 },
    { x: 650, y: 120 },
    { x: 750, y: 90 },
    { x: 850, y: 70 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(250,185,91,0.08) 0%, rgba(143,221,223,0) 70%)',
      }} />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="grid08" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid08)" />
      </svg>

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: '50%',
        border: '3px solid rgba(250,185,91,0.08)',
        top: -150, left: -100,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const px = seededRandom(i + 8) * 1920;
        const py = seededRandom(i + 120) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px, top: py + Math.sin(frame * 0.03 + i) * 12,
            width: 5, height: 5, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.5,
            boxShadow: '0 0 10px #FAB95B',
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: '6%', left: '50%',
        transform: `translateX(-50%) scale(${titleScale})`,
        fontFamily: 'Yantra One',
        fontSize: 76,
        color: '#E8EDF2',
        textAlign: 'center',
        letterSpacing: '3px',
      }}>
        Gold Price <span style={{ color: '#FAB95B' }}>Linked</span> Bond
      </div>

      {/* Chart container */}
      <div style={{
        position: 'absolute', top: '20%', left: '50%',
        transform: 'translateX(-50%)',
        width: 1000, height: 360,
        background: 'rgba(28,28,28,0.85)',
        border: '3px solid #FAB95B',
        borderRadius: 18,
        padding: '24px 30px',
        opacity: axisOp,
        boxShadow: '0 12px 40px rgba(28,28,28,0.3)',
      }}>
        {/* Chart header */}
        <div style={{
          display: 'flex',
          justifyContent: 'space-between',
          alignItems: 'center',
          marginBottom: 12,
        }}>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 14, color: '#9E9E9E', letterSpacing: '2px' }}>GOLD PRICE</div>
            <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 800, color: '#FAB95B' }}>
              ₹ {Math.floor(interpolate(frame, [30, 200], [4200, 7240], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })).toLocaleString('en-IN')}/g
            </div>
          </div>
          <div style={{
            padding: '8px 16px',
            background: 'rgba(0,255,100,0.15)',
            border: '2px solid #4ade80',
            borderRadius: 8,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 800,
            color: '#4ade80',
          }}>
            ▲ +72.4%
          </div>
        </div>

        {/* Chart SVG */}
        <svg width="940" height="280" viewBox="0 0 940 280">
          {/* Y-axis grid */}
          {[60, 120, 180, 240].map(y => (
            <line key={y} x1="40" y1={y} x2="900" y2={y} stroke="#E8EDF2" strokeWidth="1" opacity="0.15" />
          ))}
          {/* X-axis labels */}
          {['2018', '2020', '2022', '2024', '2026'].map((yr, i) => (
            <text key={yr} x={60 + i * 210} y="275" textAnchor="middle" fontFamily="Inter" fontSize="14" fill="#9E9E9E">{yr}</text>
          ))}

          {/* Area under line */}
          <path
            d={`M ${chartPoints[0].x} 260 L ${chartPoints.map(p => `${p.x} ${p.y}`).join(' L ')} L ${chartPoints[chartPoints.length - 1].x} 260 Z`}
            fill="#FAB95B"
            opacity="0.15"
          />

          {/* Line */}
          <path
            d={`M ${chartPoints.map(p => `${p.x} ${p.y}`).join(' L ')}`}
            fill="none"
            stroke="#FAB95B"
            strokeWidth="4"
            strokeLinecap="round"
            strokeLinejoin="round"
            strokeDasharray="1500"
            strokeDashoffset={1500 - 1500 * lineProgress}
          />

          {/* Points */}
          {chartPoints.map((p, i) => {
            const pointAppear = interpolate(lineProgress, [i / chartPoints.length, (i + 1) / chartPoints.length], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <circle
                key={i}
                cx={p.x} cy={p.y}
                r={6 * pointAppear}
                fill="#E8EDF2"
                stroke="#FAB95B"
                strokeWidth="3"
              />
            );
          })}

          {/* SGB linked indicator */}
          <text x="800" y="50" fontFamily="Inter" fontSize="16" fontWeight="800" fill="#FAB95B" textAnchor="end">SGB linked</text>
          <line x1="700" y1="55" x2="780" y2="55" stroke="#FAB95B" strokeWidth="3" strokeDasharray="4 3" />
        </svg>
      </div>

      {/* Bottom 3 benefits */}
      <div style={{
        position: 'absolute', bottom: '7%', left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex',
        gap: 30,
      }}>
        {/* No Physical */}
        <div style={{
          transform: `scale(${cross1})`,
          background: 'rgba(28,28,28,0.9)',
          border: '2px solid #4ade80',
          borderRadius: 14,
          padding: '16px 28px',
          display: 'flex', alignItems: 'center', gap: 14,
          minWidth: 270,
        }}>
          <svg width="36" height="36" viewBox="0 0 36 36">
            <circle cx="18" cy="18" r="15" fill="#4ade80" />
            <path d="M 11 18 L 16 23 L 25 13" fill="none" stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 12, color: '#9E9E9E', letterSpacing: '2px' }}>NO NEED</div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 800, color: '#E8EDF2' }}>Physical Gold</div>
          </div>
        </div>

        {/* No Locker */}
        <div style={{
          transform: `scale(${cross2})`,
          background: 'rgba(28,28,28,0.9)',
          border: '2px solid #4ade80',
          borderRadius: 14,
          padding: '16px 28px',
          display: 'flex', alignItems: 'center', gap: 14,
          minWidth: 270,
        }}>
          <svg width="36" height="36" viewBox="0 0 36 36">
            <circle cx="18" cy="18" r="15" fill="#4ade80" />
            <path d="M 11 18 L 16 23 L 25 13" fill="none" stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 12, color: '#9E9E9E', letterSpacing: '2px' }}>NO NEED</div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 800, color: '#E8EDF2' }}>Locker Rent</div>
          </div>
        </div>

        {/* No Theft */}
        <div style={{
          transform: `scale(${cross3})`,
          background: 'rgba(28,28,28,0.9)',
          border: '2px solid #4ade80',
          borderRadius: 14,
          padding: '16px 28px',
          display: 'flex', alignItems: 'center', gap: 14,
          minWidth: 270,
        }}>
          <svg width="36" height="36" viewBox="0 0 36 36">
            <circle cx="18" cy="18" r="15" fill="#4ade80" />
            <path d="M 11 18 L 16 23 L 25 13" fill="none" stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 12, color: '#9E9E9E', letterSpacing: '2px' }}>NO NEED</div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 800, color: '#E8EDF2' }}>Theft Fear</div>
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(28,28,28,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_LinkedNoHassle;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_InterestPlusTaxFree',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_InterestPlusTaxFree: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 2.5% counter
  const counterValue = interpolate(frame, [20, 90], [0, 2.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const percentScale = sp(10, { stiffness: 80 });

  // Timeline progress 8 years
  const timelineProgress = interpolate(frame, [110, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // TAX FREE stamp
  const stampScale = sp(290, { stiffness: 100 });
  const stampRotate = interpolate(frame, [290, 340], [-30, -12], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(250,185,91,0.12) 0%, rgba(143,221,223,0) 60%)',
      }} />

      {/* Wave texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="wave09" width="100" height="40" patternUnits="userSpaceOnUse">
            <path d="M 0 20 Q 25 5, 50 20 T 100 20" fill="none" stroke="#E8EDF2" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#wave09)" />
      </svg>

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 450, height: 450,
        border: '3px solid rgba(232,237,242,0.06)',
        top: 60, right: 80,
        transform: `rotate(${frame * 0.3}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 350, height: 350, borderRadius: '50%',
        border: '2px dashed rgba(250,185,91,0.1)',
        bottom: 50, left: 50,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
      }} />

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i + 9) * 1920;
        const py = seededRandom(i + 130) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px, top: py + Math.sin(frame * 0.03 + i) * 14,
            width: 5, height: 5, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.5,
            boxShadow: '0 0 10px #FAB95B',
          }} />
        );
      })}

      {/* LEFT: 2.5% interest counter */}
      <div style={{
        position: 'absolute', top: '32%', left: '24%',
        transform: `translate(-50%, -50%) scale(${percentScale})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 22,
          color: '#9E9E9E',
          letterSpacing: '4px',
          marginBottom: 8,
        }}>
          ANNUAL INTEREST
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 221, // 85% of 260
          color: '#FAB95B',
          letterSpacing: '4px',
          textShadow: '0 8px 30px rgba(250,185,91,0.5)',
          lineHeight: 1,
        }}>
          {counterValue.toFixed(1)}<span style={{ fontSize: 119 }}>%</span>
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 38,
          color: '#E8EDF2',
          marginTop: 14,
          letterSpacing: '2px',
        }}>
          हर साल मिलता था
        </div>
      </div>

      {/* RIGHT: Calendar 8 years */}
      <div style={{
        position: 'absolute', top: '32%', left: '72%',
        transform: 'translate(-50%, -50%)',
      }}>
        <svg width="420" height="380" viewBox="0 0 420 380">
          {/* Calendar body */}
          <rect x="20" y="40" width="380" height="320" fill="#1C1C1C" stroke="#FAB95B" strokeWidth="4" rx="12" />
          {/* Top header */}
          <rect x="20" y="40" width="380" height="60" fill="#FAB95B" rx="12" />
          <rect x="20" y="80" width="380" height="20" fill="#FAB95B" />
          <text x="210" y="80" textAnchor="middle" fontFamily="Inter" fontSize="26" fontWeight="900" fill="#1C1C1C" letterSpacing="3">8 YEARS HOLD</text>
          {/* Binder rings */}
          <circle cx="110" cy="35" r="10" fill="#9E9E9E" />
          <circle cx="310" cy="35" r="10" fill="#9E9E9E" />

          {/* Year boxes 1-8 */}
          {[0, 1, 2, 3].map(row =>
            [0, 1].map(col => {
              const yearNum = row * 2 + col + 1;
              const yearActive = timelineProgress > (yearNum - 1) / 8;
              const partialFill = Math.max(0, Math.min(1, timelineProgress * 8 - (yearNum - 1)));
              return (
                <g key={`${row}-${col}`}>
                  <rect
                    x={50 + col * 170}
                    y={120 + row * 60}
                    width="150"
                    height="50"
                    fill={yearActive ? '#FAB95B' : 'rgba(250,185,91,0.1)'}
                    stroke="#FAB95B"
                    strokeWidth="2"
                    rx="6"
                    opacity={yearActive ? partialFill : 0.4}
                  />
                  <text
                    x={125 + col * 170}
                    y={152 + row * 60}
                    textAnchor="middle"
                    fontFamily="Inter"
                    fontSize="22"
                    fontWeight="800"
                    fill={yearActive ? '#1C1C1C' : '#9E9E9E'}
                  >YEAR {yearNum}</text>
                </g>
              );
            })
          )}
        </svg>

        <div style={{
          marginTop: 8,
          textAlign: 'center',
          fontFamily: 'Inter',
          fontSize: 26,
          fontWeight: 700,
          color: '#E8EDF2',
          letterSpacing: '2px',
        }}>
          MATURITY PERIOD
        </div>
      </div>

      {/* Bottom: TAX FREE stamp */}
      <div style={{
        position: 'absolute', bottom: '8%', left: '50%',
        transform: `translateX(-50%) scale(${stampScale}) rotate(${stampRotate}deg)`,
      }}>
        <div style={{
          background: 'transparent',
          border: '6px solid #4ade80',
          padding: '18px 60px',
          borderRadius: 12,
          fontFamily: 'Inter',
          fontSize: 81, // 85% of 95
          fontWeight: 900,
          color: '#4ade80',
          letterSpacing: '10px',
          textShadow: '0 0 30px rgba(74,222,128,0.4)',
          boxShadow: '0 0 40px rgba(74,222,128,0.3)',
          background: 'rgba(74,222,128,0.08)',
        }}>
          TAX FREE!
        </div>
        <div style={{
          marginTop: 10,
          textAlign: 'center',
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 32,
          color: '#E8EDF2',
          letterSpacing: '2px',
        }}>
          पूरा कैपिटल गेन — ज़ीरो टैक्स
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(28,28,28,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_InterestPlusTaxFree;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_LakhonNiveshak',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_LakhonNiveshak: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big counter
  const counter = Math.floor(interpolate(frame, [20, 120], [0, 1250000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const counterScale = sp(15, { stiffness: 80 });

  // Three pillars
  const pillar1 = sp(150, { stiffness: 70 });
  const pillar2 = sp(195, { stiffness: 70 });
  const pillar3 = sp(240, { stiffness: 70 });

  // People dots growing
  const peopleCount = Math.floor(interpolate(frame, [40, 180], [0, 80], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#8FDDDF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(250,185,91,0.15) 0%, rgba(143,221,223,0) 60%)',
      }} />

      {/* Honeycomb texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="honey10" width="56" height="48" patternUnits="userSpaceOnUse">
            <polygon points="28,2 52,16 52,40 28,54 4,40 4,16" fill="none" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#honey10)" />
      </svg>

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        border: '2px solid rgba(250,185,91,0.08)',
        top: -200, left: -200,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        border: '3px dashed rgba(232,237,242,0.07)',
        bottom: -150, right: -100,
        transform: `rotate(${-frame * 0.15}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const px = seededRandom(i + 10) * 1920;
        const py = seededRandom(i + 140) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px, top: py + Math.sin(frame * 0.03 + i) * 14,
            width: 6, height: 6, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.5,
            boxShadow: '0 0 12px #FAB95B',
          }} />
        );
      })}

      {/* TOP: Counter */}
      <div style={{
        position: 'absolute', top: '8%', left: '50%',
        transform: `translateX(-50%) scale(${counterScale})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 24,
          color: '#9E9E9E',
          letterSpacing: '5px',
          marginBottom: 8,
        }}>
          TOTAL INVESTORS
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 187, // 85% of 220
          color: '#FAB95B',
          letterSpacing: '4px',
          textShadow: '0 8px 35px rgba(250,185,91,0.5)',
          lineHeight: 1,
        }}>
          {counter.toLocaleString('en-IN')}+
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 41,
          color: '#E8EDF2',
          marginTop: 12,
          letterSpacing: '2px',
        }}>
          लाखों भारतीयों ने <span style={{ color: '#FAB95B' }}>निवेश किया</span>
        </div>
      </div>

      {/* People dots grid - decorative */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: 'translate(-50%, -50%)',
        width: 700, height: 100,
        display: 'flex',
        flexWrap: 'wrap',
        gap: 8,
        justifyContent: 'center',
      }}>
        {Array.from({ length: peopleCount }).map((_, i) => (
          <svg key={i} width="22" height="22" viewBox="0 0 22 22">
            <circle cx="11" cy="7" r="4" fill="#FAB95B" />
            <path d="M 3 22 Q 3 13, 11 13 Q 19 13, 19 22 Z" fill="#FAB95B" />
          </svg>
        ))}
      </div>

      {/* BOTTOM: Three pillars */}
      <div style={{
        position: 'absolute', bottom: '8%', left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex',
        gap: 35,
      }}>
        {/* Pillar 1 - Govt Guarantee */}
        <div style={{
          transform: `scale(${pillar1}) translateY(${(1 - pillar1) * 40}px)`,
          background: 'linear-gradient(180deg, #2D2D2D, #1C1C1C)',
          border: '3px solid #FAB95B',
          borderRadius: 16,
          padding: '24px 30px',
          width: 360,
          textAlign: 'center',
          boxShadow: '0 10px 30px rgba(28,28,28,0.3)',
        }}>
          <svg width="64" height="64" viewBox="0 0 64 64" style={{ margin: '0 auto', display: 'block' }}>
            {/* Shield */}
            <path d="M 32 6 L 56 16 L 56 36 Q 56 50, 32 60 Q 8 50, 8 36 L 8 16 Z" fill="#FAB95B" stroke="#E8EDF2" strokeWidth="2" />
            <path d="M 22 32 L 30 40 L 44 24" fill="none" stroke="#1C1C1C" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 13,
            color: '#9E9E9E',
            letterSpacing: '3px',
            marginTop: 12,
          }}>
            PILLAR 1
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 41,
            color: '#E8EDF2',
            marginTop: 4,
            letterSpacing: '1px',
          }}>
            सरकारी गारंटी
          </div>
        </div>

        {/* Pillar 2 - Gold Returns */}
        <div style={{
          transform: `scale(${pillar2}) translateY(${(1 - pillar2) * 40}px)`,
          background: 'linear-gradient(180deg, #2D2D2D, #1C1C1C)',
          border: '3px solid #FAB95B',
          borderRadius: 16,
          padding: '24px 30px',
          width: 360,
          textAlign: 'center',
          boxShadow: '0 10px 30px rgba(28,28,28,0.3)',
        }}>
          <svg width="64" height="64" viewBox="0 0 64 64" style={{ margin: '0 auto', display: 'block' }}>
            {/* Trending up */}
            <path d="M 8 50 L 22 32 L 32 42 L 56 16" fill="none" stroke="#FAB95B" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" />
            <polygon points="46,16 56,16 56,26" fill="#FAB95B" />
            <circle cx="22" cy="32" r="4" fill="#E8EDF2" />
            <circle cx="32" cy="42" r="4" fill="#E8EDF2" />
          </svg>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 13,
            color: '#9E9E9E',
            letterSpacing: '3px',
            marginTop: 12,
          }}>
            PILLAR 2
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 41,
            color: '#E8EDF2',
            marginTop: 4,
            letterSpacing: '1px',
          }}>
            सोने जैसा रिटर्न
          </div>
        </div>

        {/* Pillar 3 - No Tax */}
        <div style={{
          transform: `scale(${pillar3}) translateY(${(1 - pillar3) * 40}px)`,
          background: 'linear-gradient(180deg, #2D2D2D, #1C1C1C)',
          border: '3px solid #FAB95B',
          borderRadius: 16,
          padding: '24px 30px',
          width: 360,
          textAlign: 'center',
          boxShadow: '0 10px 30px rgba(28,28,28,0.3)',
        }}>
          <svg width="64" height="64" viewBox="0 0 64 64" style={{ margin: '0 auto', display: 'block' }}>
            {/* Tax symbol with strike */}
            <circle cx="32" cy="32" r="26" fill="none" stroke="#FAB95B" strokeWidth="4" />
            <text x="32" y="42" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="900" fill="#FAB95B">TAX</text>
            <line x1="12" y1="52" x2="52" y2="12" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
          </svg>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 13,
            color: '#9E9E9E',
            letterSpacing: '3px',
            marginTop: 12,
          }}>
            PILLAR 3
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 41,
            color: '#E8EDF2',
            marginTop: 4,
            letterSpacing: '1px',
          }}>
            ज़ीरो टैक्स
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(28,28,28,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: '1px',
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_LakhonNiveshak;
```

