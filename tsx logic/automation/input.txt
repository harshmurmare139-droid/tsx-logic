```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11_PlainEnglish',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_PlainEnglish: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.04]);
  const camPanX = Math.sin(frame * 0.008) * 6;

  const headlineIn = sp(8);
  const subIn = sp(30);
  const card1 = sp(55);
  const card2 = sp(75);
  const card3 = sp(95);
  const stamp = sp(130, { damping: 8, stiffness: 180 });

  const breathe = 1 + Math.sin(frame * 0.04) * 0.015;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient backdrop */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(250,185,91,0.12) 0%, rgba(44,57,71,0) 55%), radial-gradient(circle at 75% 70%, rgba(232,237,242,0.06) 0%, rgba(44,57,71,0) 60%)',
      }} />

      {/* Animated shape layer — drifting transparent geometry */}
      <AbsoluteFill style={{ transform: `scale(${camZoom}) translateX(${camPanX}px)` }}>
        <div style={{
          position: 'absolute', left: 120, top: 140, width: 380, height: 380,
          border: '2px solid #FAB95B', opacity: 0.06, borderRadius: '50%',
          transform: `rotate(${frame * 0.15}deg) scale(${breathe})`,
        }} />
        <div style={{
          position: 'absolute', right: 80, bottom: 120, width: 460, height: 460,
          border: '2px solid #E8EDF2', opacity: 0.04,
          transform: `rotate(${-frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', left: '55%', top: 60, width: 240, height: 240,
          background: '#FAB95B', opacity: 0.05, borderRadius: 28,
          transform: `rotate(${frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — vertical grid lines */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: `${(i / 24) * 100}%`, top: 0, bottom: 0,
            width: 1, background: '#E8EDF2',
            transform: `translateY(${Math.sin(frame * 0.02 + i) * 8}px)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const r1 = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        const yDrift = Math.sin(frame * 0.02 + i) * 14;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 4, height: 4, borderRadius: '50%',
            background: i % 3 === 0 ? '#FAB95B' : '#E8EDF2',
            opacity: 0.35,
            transform: `translateY(${yDrift}px)`,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ padding: '90px 110px', transform: `scale(${camZoom})` }}>
        {/* Top label */}
        <div style={{
          opacity: headlineIn,
          transform: `translateY(${(1 - headlineIn) * -30}px)`,
          display: 'flex', alignItems: 'center', gap: 18, marginBottom: 30,
        }}>
          <div style={{ width: 60, height: 4, background: '#FAB95B' }} />
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 700,
            color: '#FAB95B', letterSpacing: 6, textTransform: 'uppercase',
          }}>
            Plain English. No Jargon.
          </div>
        </div>

        {/* HEADLINE */}
        <div style={{
          opacity: headlineIn,
          transform: `translateY(${(1 - headlineIn) * 40}px) scale(${0.95 + headlineIn * 0.05})`,
          fontFamily: 'Inter, Poppins', fontWeight: 900,
          fontSize: 119, lineHeight: 1.02, color: '#E8EDF2',
          letterSpacing: -2, marginBottom: 22,
        }}>
          KNOW WHAT YOU'RE
        </div>
        <div style={{
          opacity: subIn,
          transform: `translateY(${(1 - subIn) * 40}px)`,
          fontFamily: 'Inter, Poppins', fontWeight: 900,
          fontSize: 119, lineHeight: 1.02,
          color: '#FAB95B', letterSpacing: -2, marginBottom: 60,
        }}>
          ENTITLED TO.
        </div>

        {/* Three info cards row */}
        <div style={{ display: 'flex', gap: 30, marginTop: 20 }}>
          {/* CARD 1 */}
          <div style={{
            flex: 1, padding: '34px 30px',
            background: 'rgba(232,237,242,0.05)',
            border: '2px solid rgba(232,237,242,0.18)',
            borderRadius: 18,
            opacity: card1,
            transform: `translateY(${(1 - card1) * 60}px)`,
          }}>
            <svg width="60" height="60" viewBox="0 0 60 60" style={{ marginBottom: 16 }}>
              <circle cx="30" cy="30" r="26" fill="none" stroke="#FAB95B" strokeWidth="3" />
              <path d="M20 30 L27 37 L42 22" stroke="#FAB95B" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 800, color: '#E8EDF2', lineHeight: 1.1 }}>
              Plain Language
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 500, color: '#9E9E9E', marginTop: 8 }}>
              No fine print confusion
            </div>
          </div>

          {/* CARD 2 */}
          <div style={{
            flex: 1, padding: '34px 30px',
            background: 'rgba(250,185,91,0.10)',
            border: '2px solid #FAB95B',
            borderRadius: 18,
            opacity: card2,
            transform: `translateY(${(1 - card2) * 60}px) scale(${0.96 + card2 * 0.04})`,
            boxShadow: `0 0 ${30 + Math.sin(frame * 0.08) * 10}px rgba(250,185,91,0.25)`,
          }}>
            <svg width="60" height="60" viewBox="0 0 60 60" style={{ marginBottom: 16 }}>
              <rect x="10" y="14" width="40" height="32" rx="4" fill="none" stroke="#FAB95B" strokeWidth="3" />
              <line x1="16" y1="24" x2="44" y2="24" stroke="#FAB95B" strokeWidth="3" />
              <line x1="16" y1="32" x2="38" y2="32" stroke="#FAB95B" strokeWidth="3" />
              <line x1="16" y1="40" x2="34" y2="40" stroke="#FAB95B" strokeWidth="3" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 800, color: '#E8EDF2', lineHeight: 1.1 }}>
              Real Numbers
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 500, color: '#E8EDF2', opacity: 0.75, marginTop: 8 }}>
              2026 SSA & Medicare data
            </div>
          </div>

          {/* CARD 3 */}
          <div style={{
            flex: 1, padding: '34px 30px',
            background: 'rgba(232,237,242,0.05)',
            border: '2px solid rgba(232,237,242,0.18)',
            borderRadius: 18,
            opacity: card3,
            transform: `translateY(${(1 - card3) * 60}px)`,
          }}>
            <svg width="60" height="60" viewBox="0 0 60 60" style={{ marginBottom: 16 }}>
              <circle cx="30" cy="30" r="26" fill="none" stroke="#FAB95B" strokeWidth="3" />
              <path d="M30 16 L30 30 L40 36" stroke="#FAB95B" strokeWidth="4" fill="none" strokeLinecap="round" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 800, color: '#E8EDF2', lineHeight: 1.1 }}>
              Act In Time
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 500, color: '#9E9E9E', marginTop: 8 }}>
              Before the window closes
            </div>
          </div>
        </div>

        {/* Stamp tag bottom */}
        <div style={{
          position: 'absolute', bottom: 80, left: 110,
          opacity: stamp,
          transform: `scale(${stamp}) rotate(-2deg)`,
          padding: '14px 28px',
          background: '#FAB95B', color: '#2C3947',
          fontFamily: 'Inter', fontSize: 28, fontWeight: 900,
          letterSpacing: 2, borderRadius: 6,
        }}>
          STAY AHEAD — OR FIND OUT TOO LATE
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_PlainEnglish;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12_NotJustBirthday',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_NotJustBirthday: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tilt = interpolate(frame, [0, durationInFrames], [-1, 1]);

  const word1 = sp(10);
  const word2 = sp(35);
  const strikeProgress = interpolate(sp(60), [0, 1], [0, 100]);
  const word3 = sp(85);
  const ageBubble = sp(105, { damping: 9, stiffness: 150 });
  const tag = sp(135);

  const shake = frame > 55 && frame < 75 ? Math.sin(frame * 1.4) * 3 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 80% 20%, rgba(250,185,91,0.15) 0%, rgba(44,57,71,0) 55%)',
      }} />

      {/* Shape layer */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: -120, top: -120, width: 600, height: 600,
          border: '3px solid #FAB95B', opacity: 0.05, borderRadius: '50%',
          transform: `rotate(${frame * 0.08}deg)`,
        }} />
        <div style={{
          position: 'absolute', left: -80, bottom: -80, width: 400, height: 400,
          background: '#E8EDF2', opacity: 0.03, borderRadius: 40,
          transform: `rotate(${frame * 0.12}deg)`,
        }} />
        <div style={{
          position: 'absolute', left: '60%', top: '60%', width: 0, height: 0,
          borderLeft: '120px solid transparent',
          borderRight: '120px solid transparent',
          borderBottom: '200px solid #FAB95B',
          opacity: 0.06,
          transform: `rotate(${frame * 0.15}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — diagonal lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: -200, top: `${i * 8}%`,
            width: '140%', height: 1, background: '#E8EDF2',
            transform: `rotate(-12deg) translateX(${Math.sin(frame * 0.02 + i) * 20}px)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 10);
        const r2 = seededRandom(i + 60);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.4,
            transform: `translateY(${Math.sin(frame * 0.03 + i) * 12}px)`,
          }} />
        );
      })}

      {/* MAIN — centered typographic */}
      <AbsoluteFill style={{
        justifyContent: 'center', alignItems: 'center',
        transform: `translateX(${shake}px) rotate(${tilt * 0.2}deg)`,
      }}>
        {/* Top tag */}
        <div style={{
          opacity: word1,
          transform: `translateY(${(1 - word1) * -20}px)`,
          marginBottom: 40, display: 'flex', alignItems: 'center', gap: 16,
        }}>
          <div style={{
            width: 14, height: 14, background: '#FAB95B', borderRadius: '50%',
            boxShadow: `0 0 ${15 + Math.sin(frame * 0.15) * 8}px #FAB95B`,
          }} />
          <div style={{
            fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
            color: '#FAB95B', letterSpacing: 5, textTransform: 'uppercase',
          }}>
            Reality Check
          </div>
          <div style={{
            width: 14, height: 14, background: '#FAB95B', borderRadius: '50%',
            boxShadow: `0 0 ${15 + Math.sin(frame * 0.15) * 8}px #FAB95B`,
          }} />
        </div>

        {/* Big stacked line 1 */}
        <div style={{
          opacity: word1,
          transform: `translateY(${(1 - word1) * 30}px)`,
          fontFamily: 'Inter', fontWeight: 900,
          fontSize: 102, color: '#E8EDF2',
          letterSpacing: -1, lineHeight: 1,
        }}>
          TURNING 65 ISN'T
        </div>

        {/* "just a birthday" with strike-through */}
        <div style={{
          position: 'relative',
          opacity: word2,
          transform: `translateY(${(1 - word2) * 30}px)`,
          marginTop: 12,
        }}>
          <div style={{
            fontFamily: 'Inter', fontWeight: 900,
            fontSize: 102, color: '#9E9E9E',
            letterSpacing: -1, lineHeight: 1,
          }}>
            JUST A BIRTHDAY
          </div>
          {/* strike line */}
          <div style={{
            position: 'absolute', top: '50%', left: 0,
            height: 8, background: '#ff0000',
            width: `${strikeProgress}%`,
            transform: 'translateY(-50%)',
            boxShadow: '0 0 18px rgba(255,0,0,0.6)',
          }} />
        </div>

        {/* Line 3 - punchline */}
        <div style={{
          opacity: word3,
          transform: `translateY(${(1 - word3) * 40}px) scale(${0.92 + word3 * 0.08})`,
          marginTop: 40,
          fontFamily: 'Inter', fontWeight: 900,
          fontSize: 119, color: '#FAB95B',
          letterSpacing: -2, lineHeight: 1,
          textShadow: `0 0 ${25 + Math.sin(frame * 0.1) * 10}px rgba(250,185,91,0.4)`,
        }}>
          IT'S A CROSSROADS.
        </div>

        {/* Bottom row — age bubble + tag */}
        <div style={{
          marginTop: 60, display: 'flex', alignItems: 'center', gap: 28,
        }}>
          <div style={{
            opacity: ageBubble,
            transform: `scale(${ageBubble})`,
            width: 140, height: 140, borderRadius: '50%',
            background: '#FAB95B', color: '#2C3947',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Inter', fontSize: 68, fontWeight: 900,
            boxShadow: '0 12px 40px rgba(0,0,0,0.4)',
          }}>
            65
          </div>
          <div style={{
            opacity: tag,
            transform: `translateX(${(1 - tag) * -30}px)`,
            fontFamily: 'Inter', fontSize: 38, fontWeight: 600,
            color: '#E8EDF2', maxWidth: 600, lineHeight: 1.2,
          }}>
            The most critical financial<br />year of your retirement.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_NotJustBirthday;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13_Crossroads',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_Crossroads: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headerIn = sp(8);
  const roadDraw = interpolate(sp(25), [0, 1], [0, 1]);
  const leftPath = sp(60);
  const rightPath = sp(85);
  const centerDot = sp(110, { damping: 8, stiffness: 180 });
  const footer = sp(135);

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(250,185,91,0.10) 0%, rgba(44,57,71,0) 60%)',
      }} />

      {/* Shape layer */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: '50%', top: '50%',
          width: 900, height: 900, marginLeft: -450, marginTop: -450,
          border: '2px solid #E8EDF2', opacity: 0.04, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
        }} />
        <div style={{
          position: 'absolute', left: 60, top: 80, width: 300, height: 300,
          border: '2px solid #FAB95B', opacity: 0.05,
          transform: `rotate(${frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: 80, bottom: 60, width: 250, height: 250,
          background: '#FAB95B', opacity: 0.04, borderRadius: 30,
          transform: `rotate(${-frame * 0.12}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — concentric rings */}
      <AbsoluteFill style={{ opacity: 0.05, justifyContent: 'center', alignItems: 'center' }}>
        {[1, 2, 3, 4].map((i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 300 * i, height: 300 * i,
            border: '1px solid #E8EDF2', borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.02})`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r1 = seededRandom(i + 20);
        const r2 = seededRandom(i + 70);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 4, height: 4, borderRadius: '50%',
            background: i % 2 === 0 ? '#FAB95B' : '#E8EDF2',
            opacity: 0.35,
            transform: `translateY(${Math.sin(frame * 0.025 + i) * 14}px)`,
          }} />
        );
      })}

      {/* HEADER */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0,
        textAlign: 'center',
        opacity: headerIn,
        transform: `translateY(${(1 - headerIn) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 6, textTransform: 'uppercase', marginBottom: 18,
        }}>
          The Financial Crossroads
        </div>
        <div style={{
          fontFamily: 'Inter', fontWeight: 900, fontSize: 102,
          color: '#E8EDF2', letterSpacing: -2, lineHeight: 1,
        }}>
          AGE 65 — THE FORK
        </div>
      </div>

      {/* CENTER GRAPHIC — crossroads SVG */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', paddingTop: 120 }}>
        <svg width="1200" height="600" viewBox="0 0 1200 600" style={{ overflow: 'visible' }}>
          {/* base trunk line */}
          <line
            x1="600" y1="560" x2="600" y2="320"
            stroke="#E8EDF2" strokeWidth="6"
            strokeDasharray="700"
            strokeDashoffset={700 - 700 * roadDraw}
            strokeLinecap="round"
          />
          {/* left branch — Medicare */}
          <line
            x1="600" y1="320" x2="220" y2="120"
            stroke="#FAB95B" strokeWidth="6"
            strokeDasharray="500"
            strokeDashoffset={500 - 500 * leftPath}
            strokeLinecap="round"
          />
          {/* right branch — Social Security */}
          <line
            x1="600" y1="320" x2="980" y2="120"
            stroke="#FAB95B" strokeWidth="6"
            strokeDasharray="500"
            strokeDashoffset={500 - 500 * rightPath}
            strokeLinecap="round"
          />
          {/* center node */}
          <circle
            cx="600" cy="320" r={28 * centerDot}
            fill="#FAB95B"
            style={{ filter: 'drop-shadow(0 0 20px rgba(250,185,91,0.6))' }}
          />
          <circle cx="600" cy="320" r="12" fill="#2C3947" opacity={centerDot} />

          {/* left endpoint icon — medical cross */}
          <g transform="translate(220,120)" opacity={leftPath}>
            <circle r="50" fill="#2C3947" stroke="#FAB95B" strokeWidth="4" />
            <rect x="-8" y="-26" width="16" height="52" fill="#FAB95B" />
            <rect x="-26" y="-8" width="52" height="16" fill="#FAB95B" />
          </g>

          {/* right endpoint icon — dollar */}
          <g transform="translate(980,120)" opacity={rightPath}>
            <circle r="50" fill="#2C3947" stroke="#FAB95B" strokeWidth="4" />
            <text x="0" y="18" textAnchor="middle" fontFamily="Inter" fontSize="60" fontWeight="900" fill="#FAB95B">$</text>
          </g>
        </svg>
      </AbsoluteFill>

      {/* Branch labels */}
      <div style={{
        position: 'absolute', left: 120, top: 220,
        opacity: leftPath,
        transform: `translateX(${(1 - leftPath) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 3, textTransform: 'uppercase',
        }}>
          PATH ONE
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 60, fontWeight: 900,
          color: '#E8EDF2', marginTop: 6,
        }}>
          MEDICARE
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 26, fontWeight: 500,
          color: '#FAB95B', marginTop: 4,
        }}>
          Eligibility opens
        </div>
      </div>

      <div style={{
        position: 'absolute', right: 120, top: 220, textAlign: 'right',
        opacity: rightPath,
        transform: `translateX(${(1 - rightPath) * 30}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 3, textTransform: 'uppercase',
        }}>
          PATH TWO
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 60, fontWeight: 900,
          color: '#E8EDF2', marginTop: 6,
        }}>
          SOCIAL SECURITY
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 26, fontWeight: 500,
          color: '#FAB95B', marginTop: 4,
        }}>
          Strategy locks in
        </div>
      </div>

      {/* FOOTER */}
      <div style={{
        position: 'absolute', bottom: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: footer,
        transform: `translateY(${(1 - footer) * 20}px)`,
      }}>
        <div style={{
          display: 'inline-block', padding: '14px 36px',
          background: 'rgba(250,185,91,0.15)',
          border: '2px solid #FAB95B', borderRadius: 8,
          fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 1,
        }}>
          One year. Two doors. A lifetime of impact.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_Crossroads;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14_TheMistake',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_TheMistake: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headerIn = sp(8);
  const envelope = sp(28, { damping: 10, stiffness: 130 });
  const flap = sp(60);
  const list1 = sp(75);
  const list2 = sp(92);
  const list3 = sp(108);
  const stamp = sp(135, { damping: 7, stiffness: 200 });

  const stampShake = frame > 135 && frame < 150 ? Math.sin(frame * 1.8) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 25% 60%, rgba(250,185,91,0.10) 0%, rgba(44,57,71,0) 55%), radial-gradient(circle at 75% 30%, rgba(255,0,0,0.06) 0%, rgba(44,57,71,0) 50%)',
      }} />

      {/* Shape layer */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: -100, top: 60, width: 420, height: 420,
          border: '2px solid #FAB95B', opacity: 0.05, borderRadius: 40,
          transform: `rotate(${frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: -80, bottom: -80, width: 500, height: 500,
          border: '2px solid #E8EDF2', opacity: 0.04, borderRadius: '50%',
          transform: `rotate(${-frame * 0.08}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: 200, top: 100, width: 200, height: 200,
          background: '#ff0000', opacity: 0.04, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.05) * 0.1})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — horizontal scan lines */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: `${(i / 30) * 100}%`, left: 0, right: 0,
            height: 1, background: '#E8EDF2',
            opacity: 0.5 + Math.sin(frame * 0.02 + i) * 0.5,
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r1 = seededRandom(i + 30);
        const r2 = seededRandom(i + 80);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 3, height: 3, borderRadius: '50%',
            background: '#E8EDF2', opacity: 0.3,
            transform: `translateY(${Math.sin(frame * 0.03 + i) * 10}px)`,
          }} />
        );
      })}

      {/* HEADER */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0, textAlign: 'center',
        opacity: headerIn,
        transform: `translateY(${(1 - headerIn) * -25}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 6, textTransform: 'uppercase', marginBottom: 16,
        }}>
          What Most People Do
        </div>
        <div style={{
          fontFamily: 'Inter', fontWeight: 900, fontSize: 94,
          color: '#E8EDF2', letterSpacing: -1, lineHeight: 1,
        }}>
          THE DEFAULT TRAP
        </div>
      </div>

      {/* MAIN — envelope left, list right */}
      <div style={{
        position: 'absolute', top: 320, left: 110, right: 110,
        display: 'flex', alignItems: 'center', gap: 70,
      }}>
        {/* ENVELOPE SVG */}
        <div style={{
          opacity: envelope,
          transform: `scale(${envelope}) rotate(${-3 + flap * 3}deg)`,
        }}>
          <svg width="460" height="320" viewBox="0 0 460 320">
            {/* shadow */}
            <rect x="20" y="40" width="420" height="270" rx="8" fill="rgba(0,0,0,0.35)" />
            {/* envelope body */}
            <rect x="10" y="30" width="420" height="270" rx="8" fill="#E8EDF2" stroke="#FAB95B" strokeWidth="3" />
            {/* flap */}
            <path
              d="M 10 30 L 220 180 L 430 30 Z"
              fill="#FAB95B"
              opacity="0.95"
            />
            {/* address lines */}
            <line x1="60" y1="210" x2="380" y2="210" stroke="#2C3947" strokeWidth="3" opacity="0.4" />
            <line x1="60" y1="240" x2="320" y2="240" stroke="#2C3947" strokeWidth="3" opacity="0.4" />
            <line x1="60" y1="270" x2="280" y2="270" stroke="#2C3947" strokeWidth="3" opacity="0.4" />
            {/* official seal */}
            <circle cx="380" cy="260" r="22" fill="none" stroke="#ff0000" strokeWidth="3" />
            <text x="380" y="267" textAnchor="middle" fontFamily="Inter" fontSize="14" fontWeight="900" fill="#ff0000">SSA</text>
          </svg>
        </div>

        {/* RIGHT — action list */}
        <div style={{ flex: 1 }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
            color: '#9E9E9E', letterSpacing: 3, textTransform: 'uppercase', marginBottom: 22,
          }}>
            Their 3-step Routine
          </div>

          {[
            { n: '01', t: 'Wait for the card', s: 'Government sends it. They open it.' },
            { n: '02', t: 'Sign the front page', s: 'No questions. No comparison.' },
            { n: '03', t: 'Assume best deal', s: 'It almost never is.' },
          ].map((item, i) => {
            const op = i === 0 ? list1 : i === 1 ? list2 : list3;
            return (
              <div key={i} style={{
                opacity: op,
                transform: `translateX(${(1 - op) * -30}px)`,
                display: 'flex', alignItems: 'center', gap: 22, marginBottom: 22,
                padding: '18px 22px',
                background: 'rgba(232,237,242,0.05)',
                borderLeft: '5px solid #FAB95B', borderRadius: 8,
              }}>
                <div style={{
                  fontFamily: 'Inter', fontSize: 54, fontWeight: 900,
                  color: '#FAB95B', minWidth: 80,
                }}>
                  {item.n}
                </div>
                <div>
                  <div style={{
                    fontFamily: 'Inter', fontSize: 40, fontWeight: 800,
                    color: '#E8EDF2', lineHeight: 1.1,
                  }}>
                    {item.t}
                  </div>
                  <div style={{
                    fontFamily: 'Inter', fontSize: 24, fontWeight: 500,
                    color: '#9E9E9E', marginTop: 4,
                  }}>
                    {item.s}
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </div>

      {/* MISTAKE STAMP */}
      <div style={{
        position: 'absolute', bottom: 90, left: '50%',
        opacity: stamp,
        transform: `translateX(-50%) scale(${stamp}) rotate(-6deg) translateX(${stampShake}px)`,
        padding: '18px 50px',
        border: '6px solid #ff0000', borderRadius: 4,
        background: 'rgba(255,0,0,0.08)',
        fontFamily: 'Inter', fontSize: 64, fontWeight: 900,
        color: '#ff0000', letterSpacing: 8,
        boxShadow: '0 0 40px rgba(255,0,0,0.3)',
      }}>
        THAT'S A MISTAKE
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_TheMistake;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15_TwelveMonths',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_TwelveMonths: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headerIn = sp(8);
  const circleIn = sp(25);
  const monthsProgress = interpolate(sp(35), [0, 1], [0, 1]);
  const leftPanel = sp(75);
  const rightPanel = sp(95);
  const divider = sp(115);

  // 12 month dots around clock
  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,0,0,0.05) 0%, rgba(44,57,71,0) 40%, rgba(250,185,91,0.08) 100%)',
      }} />

      {/* Shape layer */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 50, top: 50, width: 350, height: 350,
          border: '2px solid #E8EDF2', opacity: 0.04, borderRadius: '50%',
          transform: `rotate(${frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: 60, bottom: 80, width: 280, height: 280,
          background: '#FAB95B', opacity: 0.04, borderRadius: 30,
          transform: `rotate(${-frame * 0.12}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {Array.from({ length: 12 }).map((_, row) =>
          Array.from({ length: 22 }).map((_, col) => (
            <div key={`${row}-${col}`} style={{
              position: 'absolute',
              left: `${(col / 22) * 100}%`,
              top: `${(row / 12) * 100}%`,
              width: 3, height: 3, borderRadius: '50%',
              background: '#E8EDF2',
              opacity: 0.4 + Math.sin(frame * 0.03 + row + col) * 0.3,
            }} />
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r1 = seededRandom(i + 40);
        const r2 = seededRandom(i + 90);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.35,
            transform: `translateY(${Math.sin(frame * 0.025 + i) * 12}px)`,
          }} />
        );
      })}

      {/* HEADER */}
      <div style={{
        position: 'absolute', top: 60, left: 0, right: 0, textAlign: 'center',
        opacity: headerIn,
        transform: `translateY(${(1 - headerIn) * -25}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 30, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 6, textTransform: 'uppercase', marginBottom: 14,
        }}>
          The Critical Window
        </div>
        <div style={{
          fontFamily: 'Inter', fontWeight: 900, fontSize: 94,
          color: '#E8EDF2', letterSpacing: -1, lineHeight: 1,
        }}>
          12 MONTHS. <span style={{ color: '#FAB95B' }}>TWO FUTURES.</span>
        </div>
      </div>

      {/* CENTER CLOCK */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', paddingTop: 80 }}>
        <div style={{
          opacity: circleIn,
          transform: `scale(${circleIn})`,
          position: 'relative', width: 380, height: 380,
        }}>
          {/* outer ring */}
          <svg width="380" height="380" viewBox="0 0 380 380" style={{ position: 'absolute' }}>
            <circle cx="190" cy="190" r="170" fill="none" stroke="rgba(232,237,242,0.15)" strokeWidth="8" />
            <circle
              cx="190" cy="190" r="170" fill="none"
              stroke="#FAB95B" strokeWidth="8"
              strokeDasharray={2 * Math.PI * 170}
              strokeDashoffset={2 * Math.PI * 170 * (1 - monthsProgress)}
              strokeLinecap="round"
              transform="rotate(-90 190 190)"
              style={{ filter: 'drop-shadow(0 0 12px rgba(250,185,91,0.5))' }}
            />
            {/* 12 ticks */}
            {Array.from({ length: 12 }).map((_, i) => {
              const angle = (i / 12) * Math.PI * 2 - Math.PI / 2;
              const x1 = 190 + Math.cos(angle) * 150;
              const y1 = 190 + Math.sin(angle) * 150;
              const x2 = 190 + Math.cos(angle) * 165;
              const y2 = 190 + Math.sin(angle) * 165;
              return (
                <line key={i} x1={x1} y1={y1} x2={x2} y2={y2}
                  stroke="#E8EDF2" strokeWidth="3" opacity={0.6} />
              );
            })}
          </svg>
          {/* center text */}
          <div style={{
            position: 'absolute', inset: 0,
            display: 'flex', flexDirection: 'column',
            justifyContent: 'center', alignItems: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 150, fontWeight: 900,
              color: '#E8EDF2', lineHeight: 0.9,
            }}>
              12
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 30, fontWeight: 700,
              color: '#FAB95B', letterSpacing: 4, textTransform: 'uppercase',
            }}>
              Months
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* LEFT PANEL — Struggle */}
      <div style={{
        position: 'absolute', left: 90, bottom: 130, width: 480,
        opacity: leftPanel,
        transform: `translateX(${(1 - leftPanel) * -50}px)`,
        padding: '28px 30px',
        background: 'rgba(255,0,0,0.10)',
        border: '2px solid rgba(255,0,0,0.5)',
        borderRadius: 16,
      }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 10 }}>
          <svg width="50" height="50" viewBox="0 0 50 50">
            <path d="M25 8 L25 30 M25 38 L25 42" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
            <circle cx="25" cy="25" r="22" fill="none" stroke="#ff0000" strokeWidth="4" />
          </svg>
          <div style={{
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
            color: '#ff0000', letterSpacing: 3, textTransform: 'uppercase',
          }}>
            Wrong Move
          </div>
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 48, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1.05,
        }}>
          Struggle to pay<br />heating bills.
        </div>
      </div>

      {/* RIGHT PANEL — Travel */}
      <div style={{
        position: 'absolute', right: 90, bottom: 130, width: 480,
        opacity: rightPanel,
        transform: `translateX(${(1 - rightPanel) * 50}px)`,
        padding: '28px 30px',
        background: 'rgba(250,185,91,0.12)',
        border: '2px solid #FAB95B',
        borderRadius: 16,
        boxShadow: `0 0 ${20 + Math.sin(frame * 0.08) * 8}px rgba(250,185,91,0.25)`,
      }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 10 }}>
          <svg width="50" height="50" viewBox="0 0 50 50">
            <path d="M10 32 L25 12 L40 32 Z" fill="#FAB95B" />
            <rect x="22" y="32" width="6" height="10" fill="#FAB95B" />
          </svg>
          <div style={{
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
            color: '#FAB95B', letterSpacing: 3, textTransform: 'uppercase',
          }}>
            Right Move
          </div>
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 48, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1.05,
        }}>
          Travel & help your<br />grandchildren.
        </div>
      </div>

      {/* Center divider VS */}
      <div style={{
        position: 'absolute', left: '50%', bottom: 200,
        transform: `translateX(-50%) scale(${divider})`,
        opacity: divider,
        fontFamily: 'Inter', fontSize: 60, fontWeight: 900,
        color: '#9E9E9E', letterSpacing: 4,
      }}>
        VS
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_TwelveMonths;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16_Bonus1Intro',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_Bonus1Intro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const numberIn = sp(8, { damping: 9, stiffness: 140 });
  const bonusBand = sp(35);
  const titleIn = sp(55);
  const iconIn = sp(80, { damping: 8, stiffness: 160 });
  const subIn = sp(100);

  const numberPulse = 1 + Math.sin(frame * 0.06) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(250,185,91,0.18) 0%, rgba(44,57,71,0) 60%)',
      }} />

      {/* Shape layer */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: '50%', top: '50%',
          width: 1100, height: 1100, marginLeft: -550, marginTop: -550,
          border: '3px solid #FAB95B', opacity: 0.06, borderRadius: '50%',
          transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.03) * 0.03})`,
        }} />
        <div style={{
          position: 'absolute', left: '50%', top: '50%',
          width: 800, height: 800, marginLeft: -400, marginTop: -400,
          border: '2px solid #E8EDF2', opacity: 0.04, borderRadius: '50%',
          transform: `rotate(${-frame * 0.07}deg)`,
        }} />
        {/* corner shapes */}
        <div style={{
          position: 'absolute', left: 60, top: 60, width: 100, height: 100,
          background: '#FAB95B', opacity: 0.08, borderRadius: 12,
          transform: `rotate(${frame * 0.2}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: 60, bottom: 60, width: 100, height: 100,
          background: '#FAB95B', opacity: 0.08, borderRadius: 12,
          transform: `rotate(${-frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — radial lines from center */}
      <AbsoluteFill style={{ opacity: 0.05, justifyContent: 'center', alignItems: 'center' }}>
        <div style={{ position: 'relative', width: 0, height: 0 }}>
          {Array.from({ length: 24 }).map((_, i) => {
            const angle = (i / 24) * 360;
            return (
              <div key={i} style={{
                position: 'absolute', left: 0, top: 0,
                width: 800, height: 1, background: '#FAB95B',
                transformOrigin: '0 0',
                transform: `rotate(${angle + frame * 0.1}deg)`,
              }} />
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const r1 = seededRandom(i + 50);
        const r2 = seededRandom(i + 100);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.4,
            transform: `translateY(${Math.sin(frame * 0.025 + i) * 14}px)`,
            boxShadow: '0 0 6px rgba(250,185,91,0.5)',
          }} />
        );
      })}

      {/* MAIN */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        {/* Big #1 */}
        <div style={{
          position: 'absolute', left: 130, top: '50%',
          transform: `translateY(-50%) scale(${numberIn * numberPulse})`,
          opacity: numberIn,
          fontFamily: 'Inter', fontSize: 600, fontWeight: 900,
          color: 'transparent',
          WebkitTextStroke: '6px #FAB95B',
          lineHeight: 0.9,
          textShadow: `0 0 ${30 + Math.sin(frame * 0.1) * 15}px rgba(250,185,91,0.4)`,
        }}>
          1
        </div>

        {/* Right side stack */}
        <div style={{
          position: 'absolute', left: 600, right: 110,
          top: '50%', transform: 'translateY(-50%)',
        }}>
          {/* Band */}
          <div style={{
            opacity: bonusBand,
            transform: `translateX(${(1 - bonusBand) * 40}px)`,
            display: 'inline-block',
            padding: '12px 28px',
            background: '#FAB95B', color: '#2C3947',
            fontFamily: 'Inter', fontSize: 28, fontWeight: 900,
            letterSpacing: 5, marginBottom: 24,
          }}>
            BONUS NUMBER ONE
          </div>

          {/* Title */}
          <div style={{
            opacity: titleIn,
            transform: `translateY(${(1 - titleIn) * 30}px)`,
            fontFamily: 'Inter', fontWeight: 900, fontSize: 153,
            color: '#E8EDF2', lineHeight: 0.95, letterSpacing: -3, marginBottom: 16,
          }}>
            THE
          </div>
          <div style={{
            opacity: titleIn,
            transform: `translateY(${(1 - titleIn) * 30}px)`,
            fontFamily: 'Inter', fontWeight: 900, fontSize: 153,
            color: '#FAB95B', lineHeight: 0.95, letterSpacing: -3, marginBottom: 28,
            textShadow: `0 0 ${25 + Math.sin(frame * 0.1) * 10}px rgba(250,185,91,0.35)`,
          }}>
            WORKING<br />BONUS
          </div>

          {/* Icon + tag */}
          <div style={{
            display: 'flex', alignItems: 'center', gap: 20,
            opacity: subIn,
            transform: `translateY(${(1 - subIn) * 20}px)`,
          }}>
            <div style={{
              opacity: iconIn, transform: `scale(${iconIn})`,
            }}>
              <svg width="80" height="80" viewBox="0 0 80 80">
                <rect x="12" y="26" width="56" height="40" rx="4" fill="none" stroke="#FAB95B" strokeWidth="4" />
                <rect x="28" y="18" width="24" height="10" rx="2" fill="none" stroke="#FAB95B" strokeWidth="4" />
                <line x1="12" y1="44" x2="68" y2="44" stroke="#FAB95B" strokeWidth="4" />
                <circle cx="40" cy="44" r="5" fill="#FAB95B" />
              </svg>
            </div>
            <div>
              <div style={{
                fontFamily: 'Inter', fontSize: 30, fontWeight: 700,
                color: '#9E9E9E', letterSpacing: 3, textTransform: 'uppercase',
              }}>
                Topic
              </div>
              <div style={{
                fontFamily: 'Inter', fontSize: 42, fontWeight: 800,
                color: '#E8EDF2', lineHeight: 1,
              }}>
                Claim & keep working
              </div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_Bonus1Intro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17_TheMyth',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_TheMyth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beliefIn = sp(8);
  const beliefBox = sp(20);
  const reality = sp(70);
  const cost = sp(110, { damping: 8, stiffness: 160 });

  const beliefCrack = frame > 90;
  const crackOpacity = beliefCrack ? interpolate(frame, [90, 110], [0, 0.7], { extrapolateRight: 'clamp' }) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(255,0,0,0.06) 0%, rgba(44,57,71,0) 40%, rgba(250,185,91,0.08) 100%)',
      }} />

      {/* Shape layer */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 80, top: 100, width: 350, height: 350,
          border: '2px solid #E8EDF2', opacity: 0.04, borderRadius: 30,
          transform: `rotate(${frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: 100, bottom: 100, width: 400, height: 400,
          border: '2px solid #FAB95B', opacity: 0.05, borderRadius: '50%',
          transform: `rotate(${-frame * 0.08}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — diagonal grid */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: -100, top: `${i * 6}%`,
            width: '130%', height: 1, background: '#E8EDF2',
            transform: `rotate(8deg) translateY(${Math.sin(frame * 0.02 + i) * 6}px)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 60);
        const r2 = seededRandom(i + 110);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 3, height: 3, borderRadius: '50%',
            background: i % 2 === 0 ? '#FAB95B' : '#E8EDF2',
            opacity: 0.35,
            transform: `translateY(${Math.sin(frame * 0.03 + i) * 12}px)`,
          }} />
        );
      })}

      {/* HEADER */}
      <div style={{
        position: 'absolute', top: 60, left: 0, right: 0, textAlign: 'center',
        opacity: beliefIn,
        transform: `translateY(${(1 - beliefIn) * -25}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 30, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 6, textTransform: 'uppercase', marginBottom: 14,
        }}>
          What People Believe
        </div>
        <div style={{
          fontFamily: 'Inter', fontWeight: 900, fontSize: 94,
          color: '#E8EDF2', letterSpacing: -1, lineHeight: 1,
        }}>
          "THE GOVERNMENT WILL<br />
          <span style={{ color: '#ff0000' }}>TAKE MY MONEY."</span>
        </div>
      </div>

      {/* MYTH BOX */}
      <div style={{
        position: 'absolute', left: 110, right: 110, top: 360,
        opacity: beliefBox,
        transform: `translateY(${(1 - beliefBox) * 30}px)`,
        padding: '34px 50px',
        background: 'rgba(255,0,0,0.08)',
        border: '3px solid rgba(255,0,0,0.45)',
        borderRadius: 18,
        display: 'flex', alignItems: 'center', gap: 40,
        position: 'relative',
        overflow: 'hidden',
      }}>
        {/* crack line overlay */}
        <div style={{
          position: 'absolute', inset: 0,
          opacity: crackOpacity, pointerEvents: 'none',
        }}>
          <svg width="100%" height="100%" viewBox="0 0 1700 200" preserveAspectRatio="none">
            <path d="M 0 100 L 400 80 L 500 130 L 800 60 L 900 140 L 1200 70 L 1300 130 L 1700 90"
              stroke="#ff0000" strokeWidth="3" fill="none" opacity="0.6" />
            <path d="M 200 0 L 220 100 L 180 200" stroke="#ff0000" strokeWidth="2" fill="none" opacity="0.5" />
            <path d="M 850 0 L 870 100 L 830 200" stroke="#ff0000" strokeWidth="2" fill="none" opacity="0.5" />
            <path d="M 1400 0 L 1420 100 L 1380 200" stroke="#ff0000" strokeWidth="2" fill="none" opacity="0.5" />
          </svg>
        </div>

        <div style={{ position: 'relative', flex: 1 }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
            color: '#ff0000', letterSpacing: 4, textTransform: 'uppercase', marginBottom: 8,
          }}>
            Common Belief
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 56, fontWeight: 800,
            color: '#E8EDF2', lineHeight: 1.1,
          }}>
            "If I claim Social Security and keep working,<br />
            they'll penalize me."
          </div>
        </div>

        {/* X mark */}
        <div style={{ position: 'relative' }}>
          <svg width="140" height="140" viewBox="0 0 140 140">
            <circle cx="70" cy="70" r="62" fill="none" stroke="#ff0000" strokeWidth="6" />
            <line x1="42" y1="42" x2="98" y2="98" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" />
            <line x1="98" y1="42" x2="42" y2="98" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" />
          </svg>
        </div>
      </div>

      {/* REALITY STAMP */}
      <div style={{
        position: 'absolute', left: '50%', top: 620,
        opacity: reality,
        transform: `translateX(-50%) translateY(${(1 - reality) * 30}px) scale(${0.9 + reality * 0.1})`,
        textAlign: 'center',
      }}>
        <div style={{
          display: 'inline-block',
          padding: '12px 40px',
          background: '#FAB95B', color: '#2C3947',
          fontFamily: 'Inter', fontSize: 36, fontWeight: 900,
          letterSpacing: 6, marginBottom: 20,
          boxShadow: '0 8px 30px rgba(0,0,0,0.4)',
        }}>
          REALITY
        </div>
        <div style={{
          fontFamily: 'Inter', fontWeight: 900, fontSize: 85,
          color: '#FAB95B', lineHeight: 1, letterSpacing: -1,
          textShadow: `0 0 ${20 + Math.sin(frame * 0.1) * 8}px rgba(250,185,91,0.35)`,
        }}>
          IT'S A MYTH.
        </div>
      </div>

      {/* COST BANNER */}
      <div style={{
        position: 'absolute', bottom: 90, left: '50%',
        opacity: cost,
        transform: `translateX(-50%) scale(${cost})`,
        padding: '18px 44px',
        background: 'rgba(255,0,0,0.15)',
        border: '3px solid #ff0000',
        borderRadius: 10,
        display: 'flex', alignItems: 'center', gap: 18,
      }}>
        <svg width="44" height="44" viewBox="0 0 44 44">
          <path d="M22 6 L22 24 M22 32 L22 36" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
          <circle cx="22" cy="22" r="19" fill="none" stroke="#ff0000" strokeWidth="4" />
        </svg>
        <div style={{
          fontFamily: 'Inter', fontSize: 38, fontWeight: 800,
          color: '#E8EDF2', letterSpacing: 1,
        }}>
          This myth costs people <span style={{ color: '#ff0000' }}>THOUSANDS.</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_TheMyth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18_FullRetirementAge',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_FullRetirementAge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headIn = sp(8);
  const lineIn = sp(25);
  const age65 = sp(45, { damping: 8, stiffness: 160 });
  const arrow = sp(75);
  const age67 = sp(90, { damping: 8, stiffness: 160 });
  const earningsBox = sp(115);
  const counter = interpolate(sp(125), [0, 1], [0, 23000]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(250,185,91,0.12) 0%, rgba(44,57,71,0) 60%)',
      }} />

      {/* Shape layer */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 60, bottom: 80, width: 380, height: 380,
          background: '#FAB95B', opacity: 0.04, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute', right: 80, top: 80, width: 320, height: 320,
          border: '2px solid #E8EDF2', opacity: 0.05,
          transform: `rotate(${frame * 0.12}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — vertical bars */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: `${(i / 30) * 100}%`, top: 0, bottom: 0,
            width: 2, background: '#FAB95B',
            opacity: 0.4 + Math.sin(frame * 0.04 + i * 0.5) * 0.4,
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 70);
        const r2 = seededRandom(i + 120);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.35,
            transform: `translateY(${Math.sin(frame * 0.025 + i) * 14}px)`,
          }} />
        );
      })}

      {/* HEADER */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0, textAlign: 'center',
        opacity: headIn,
        transform: `translateY(${(1 - headIn) * -25}px)`,
      }}>
        <div style={{
          display: 'inline-block', padding: '10px 26px',
          background: 'rgba(250,185,91,0.18)',
          border: '2px solid #FAB95B', borderRadius: 6,
          fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 5, marginBottom: 18,
        }}>
          THE RULE EXPLAINED
        </div>
        <div style={{
          fontFamily: 'Inter', fontWeight: 900, fontSize: 85,
          color: '#E8EDF2', letterSpacing: -1, lineHeight: 1,
        }}>
          FULL RETIREMENT AGE = <span style={{ color: '#FAB95B' }}>67</span>
        </div>
      </div>

      {/* AGE TRANSITION */}
      <div style={{
        position: 'absolute', top: 340, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 80,
      }}>
        {/* 65 circle */}
        <div style={{
          opacity: age65,
          transform: `scale(${age65})`,
          width: 240, height: 240, borderRadius: '50%',
          background: 'rgba(232,237,242,0.08)',
          border: '4px solid #E8EDF2',
          display: 'flex', flexDirection: 'column',
          alignItems: 'center', justifyContent: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 24, fontWeight: 700,
            color: '#9E9E9E', letterSpacing: 3, textTransform: 'uppercase',
          }}>
            You are
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 130, fontWeight: 900,
            color: '#E8EDF2', lineHeight: 0.9,
          }}>
            65
          </div>
        </div>

        {/* Arrow */}
        <div style={{
          opacity: arrow,
          transform: `translateX(${(1 - arrow) * -30}px)`,
        }}>
          <svg width="160" height="60" viewBox="0 0 160 60">
            <line x1="0" y1="30" x2="130" y2="30" stroke="#FAB95B" strokeWidth="8" strokeLinecap="round" />
            <polygon points="130,15 160,30 130,45" fill="#FAB95B" />
          </svg>
        </div>

        {/* 67 circle */}
        <div style={{
          opacity: age67,
          transform: `scale(${age67})`,
          width: 280, height: 280, borderRadius: '50%',
          background: '#FAB95B',
          color: '#2C3947',
          display: 'flex', flexDirection: 'column',
          alignItems: 'center', justifyContent: 'center',
          boxShadow: `0 0 ${30 + Math.sin(frame * 0.1) * 12}px rgba(250,185,91,0.5)`,
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 24, fontWeight: 700,
            letterSpacing: 3, textTransform: 'uppercase',
          }}>
            Full age
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 150, fontWeight: 900,
            lineHeight: 0.9,
          }}>
            67
          </div>
        </div>
      </div>

      {/* EARNINGS LIMIT BOX */}
      <div style={{
        position: 'absolute', bottom: 100, left: '50%',
        opacity: earningsBox,
        transform: `translateX(-50%) translateY(${(1 - earningsBox) * 30}px)`,
        padding: '30px 60px',
        background: 'rgba(255,0,0,0.10)',
        border: '3px solid #ff0000',
        borderRadius: 16,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 26, fontWeight: 700,
          color: '#ff0000', letterSpacing: 4, textTransform: 'uppercase', marginBottom: 10,
        }}>
          2026 Earnings Limit (Before age 67)
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 119, fontWeight: 900,
          color: '#FAB95B', lineHeight: 1, letterSpacing: -2,
          textShadow: `0 0 ${20 + Math.sin(frame * 0.1) * 8}px rgba(250,185,91,0.35)`,
        }}>
          ${Math.floor(counter).toLocaleString()}
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 28, fontWeight: 600,
          color: '#E8EDF2', marginTop: 8, opacity: 0.85,
        }}>
          Earn more — they withhold part of your check
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_FullRetirementAge;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19_NotGoneForever',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_NotGoneForever: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const labelIn = sp(8);
  const vault = sp(28);
  const checks = sp(55);
  const secretLabel = sp(85);
  const punch = sp(110, { damping: 7, stiffness: 200 });

  const punchPulse = 1 + Math.sin(frame * 0.1) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 50%, rgba(250,185,91,0.15) 0%, rgba(44,57,71,0) 55%)',
      }} />

      {/* Shape layer */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: -100, top: 100, width: 500, height: 500,
          border: '3px solid #FAB95B', opacity: 0.05, borderRadius: '50%',
          transform: `rotate(${frame * 0.08}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: -80, bottom: 60, width: 380, height: 380,
          background: '#E8EDF2', opacity: 0.04, borderRadius: 40,
          transform: `rotate(${-frame * 0.1}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — hex pattern */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 8 }).map((_, row) =>
          Array.from({ length: 14 }).map((_, col) => (
            <div key={`${row}-${col}`} style={{
              position: 'absolute',
              left: `${col * 7.5 + (row % 2) * 3.75}%`,
              top: `${row * 14}%`,
              width: 12, height: 12,
              border: '1px solid #FAB95B',
              transform: `rotate(45deg) scale(${0.8 + Math.sin(frame * 0.03 + row + col) * 0.2})`,
            }} />
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r1 = seededRandom(i + 80);
        const r2 = seededRandom(i + 130);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.4,
            transform: `translateY(${Math.sin(frame * 0.025 + i) * 12}px)`,
          }} />
        );
      })}

      {/* LEFT — VAULT GRAPHIC */}
      <div style={{
        position: 'absolute', left: 120, top: '50%',
        transform: `translateY(-50%) scale(${vault})`,
        opacity: vault,
      }}>
        <svg width="500" height="500" viewBox="0 0 500 500">
          {/* outer vault */}
          <rect x="40" y="40" width="420" height="420" rx="16" fill="#1C1C1C" stroke="#FAB95B" strokeWidth="6" />
          <rect x="60" y="60" width="380" height="380" rx="12" fill="rgba(250,185,91,0.06)" stroke="#FAB95B" strokeWidth="3" />
          {/* circle dial */}
          <circle cx="250" cy="250" r="100" fill="none" stroke="#FAB95B" strokeWidth="5" />
          <circle cx="250" cy="250" r="70" fill="rgba(250,185,91,0.15)" stroke="#FAB95B" strokeWidth="3" />
          <circle cx="250" cy="250" r="14" fill="#FAB95B" />
          {/* dial ticks */}
          {Array.from({ length: 12 }).map((_, i) => {
            const angle = (i / 12) * Math.PI * 2;
            const x1 = 250 + Math.cos(angle) * 85;
            const y1 = 250 + Math.sin(angle) * 85;
            const x2 = 250 + Math.cos(angle) * 95;
            const y2 = 250 + Math.sin(angle) * 95;
            return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#FAB95B" strokeWidth="3" />;
          })}
          {/* spinning indicator */}
          <line
            x1="250" y1="250"
            x2={250 + Math.cos(frame * 0.08) * 80}
            y2={250 + Math.sin(frame * 0.08) * 80}
            stroke="#E8EDF2" strokeWidth="4" strokeLinecap="round"
          />
          {/* handle */}
          <circle cx="380" cy="250" r="18" fill="none" stroke="#FAB95B" strokeWidth="4" />
          {/* dollar inside */}
          <text x="250" y="160" textAnchor="middle" fontFamily="Inter" fontSize="40" fontWeight="900" fill="#FAB95B">$</text>
          <text x="250" y="370" textAnchor="middle" fontFamily="Inter" fontSize="40" fontWeight="900" fill="#FAB95B">$</text>
        </svg>
      </div>

      {/* Floating checks coming out */}
      <div style={{
        position: 'absolute', left: 480, top: 200,
        opacity: checks,
      }}>
        {Array.from({ length: 3 }).map((_, i) => {
          const off = Math.sin(frame * 0.05 + i) * 8;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: i * 30, top: i * 50 + off,
              width: 180, height: 90,
              background: '#E8EDF2', borderRadius: 6,
              transform: `rotate(${-5 + i * 4}deg)`,
              boxShadow: '0 6px 20px rgba(0,0,0,0.3)',
              padding: 12,
            }}>
              <div style={{ fontFamily: 'Inter', fontSize: 14, fontWeight: 800, color: '#2C3947' }}>SSA CHECK</div>
              <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#FAB95B', marginTop: 4 }}>$$$</div>
            </div>
          );
        })}
      </div>

      {/* RIGHT — TYPOGRAPHY */}
      <div style={{
        position: 'absolute', right: 110, top: 140, width: 760,
      }}>
        <div style={{
          opacity: labelIn,
          transform: `translateX(${(1 - labelIn) * 30}px)`,
          display: 'inline-block',
          padding: '10px 24px',
          background: '#FAB95B', color: '#2C3947',
          fontFamily: 'Inter', fontSize: 26, fontWeight: 900,
          letterSpacing: 4, marginBottom: 30,
        }}>
          THE SSA WON'T TELL YOU
        </div>

        <div style={{
          opacity: secretLabel,
          transform: `translateY(${(1 - secretLabel) * 30}px)`,
          fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 4, textTransform: 'uppercase', marginBottom: 16,
        }}>
          The withheld money...
        </div>

        <div style={{
          opacity: punch,
          transform: `translateY(${(1 - punch) * 40}px) scale(${0.92 + punch * 0.08 * punchPulse})`,
          fontFamily: 'Inter', fontWeight: 900, fontSize: 136,
          color: '#FAB95B', lineHeight: 0.95, letterSpacing: -3,
          textShadow: `0 0 ${30 + Math.sin(frame * 0.1) * 12}px rgba(250,185,91,0.4)`,
        }}>
          ISN'T GONE<br />FOREVER.
        </div>

        <div style={{
          marginTop: 30,
          opacity: punch,
          display: 'flex', alignItems: 'center', gap: 16,
        }}>
          <div style={{ width: 60, height: 4, background: '#FAB95B' }} />
          <div style={{
            fontFamily: 'Inter', fontSize: 32, fontWeight: 600,
            color: '#E8EDF2',
          }}>
            You just have to ask.
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_NotGoneForever;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20_AnnualRecalculation',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20_AnnualRecalculation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headerIn = sp(8);
  const step1 = sp(28);
  const step2 = sp(52);
  const step3 = sp(76);
  const step4 = sp(100);
  const footer = sp(130);

  // animated gear rotation for "calculation"
  const gearRot = frame * 0.6;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(250,185,91,0.10) 0%, rgba(44,57,71,0) 50%, rgba(232,237,242,0.05) 100%)',
      }} />

      {/* Shape layer */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 80, top: 80, width: 280, height: 280,
          border: '2px solid #FAB95B', opacity: 0.05, borderRadius: 30,
          transform: `rotate(${frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: 100, bottom: 100, width: 320, height: 320,
          border: '2px solid #E8EDF2', opacity: 0.04, borderRadius: '50%',
          transform: `rotate(${-frame * 0.08}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — checker dots */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 10 }).map((_, row) =>
          Array.from({ length: 18 }).map((_, col) => (
            <div key={`${row}-${col}`} style={{
              position: 'absolute',
              left: `${(col / 18) * 100}%`,
              top: `${(row / 10) * 100}%`,
              width: 4, height: 4, borderRadius: '50%',
              background: (row + col) % 2 === 0 ? '#FAB95B' : '#E8EDF2',
              opacity: 0.5,
            }} />
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r1 = seededRandom(i + 90);
        const r2 = seededRandom(i + 140);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.35,
            transform: `translateY(${Math.sin(frame * 0.025 + i) * 12}px)`,
          }} />
        );
      })}

      {/* HEADER */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0, textAlign: 'center',
        opacity: headerIn,
        transform: `translateY(${(1 - headerIn) * -25}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 30, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 6, textTransform: 'uppercase', marginBottom: 14,
        }}>
          The SSA's Quiet Process
        </div>
        <div style={{
          fontFamily: 'Inter', fontWeight: 900, fontSize: 94,
          color: '#E8EDF2', letterSpacing: -1, lineHeight: 1,
        }}>
          THE <span style={{ color: '#FAB95B' }}>ANNUAL RECALCULATION</span>
        </div>
      </div>

      {/* Center gear */}
      <div style={{
        position: 'absolute', left: '50%', top: 320,
        transform: 'translateX(-50%)',
      }}>
        <svg width="240" height="240" viewBox="0 0 240 240">
          <g transform={`translate(120,120) rotate(${gearRot})`}>
            {Array.from({ length: 12 }).map((_, i) => {
              const angle = (i / 12) * 360;
              return (
                <rect key={i} x="-12" y="-110" width="24" height="28"
                  fill="#FAB95B" opacity="0.85"
                  transform={`rotate(${angle})`} />
              );
            })}
            <circle r="80" fill="#FAB95B" />
            <circle r="50" fill="#2C3947" />
            <text y="14" textAnchor="middle" fontFamily="Inter" fontSize="48" fontWeight="900" fill="#FAB95B">%</text>
          </g>
        </svg>
      </div>

      {/* 4 STEPS — horizontal flow */}
      <div style={{
        position: 'absolute', bottom: 180, left: 80, right: 80,
        display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start',
      }}>
        {[
          { op: step1, n: '01', t: 'Every year', s: 'SSA reviews automatically' },
          { op: step2, n: '02', t: 'Hit full age', s: 'You reach 67' },
          { op: step3, n: '03', t: 'They look back', s: 'Withheld months tallied' },
          { op: step4, n: '04', t: 'Permanent raise', s: 'Higher check forever' },
        ].map((item, i) => (
          <div key={i} style={{
            opacity: item.op,
            transform: `translateY(${(1 - item.op) * 40}px)`,
            width: 380, position: 'relative',
          }}>
            <div style={{
              padding: '24px 26px',
              background: i === 3 ? 'rgba(250,185,91,0.15)' : 'rgba(232,237,242,0.05)',
              border: i === 3 ? '2px solid #FAB95B' : '2px solid rgba(232,237,242,0.15)',
              borderRadius: 14,
              boxShadow: i === 3 ? `0 0 ${20 + Math.sin(frame * 0.1) * 8}px rgba(250,185,91,0.25)` : 'none',
            }}>
              <div style={{
                fontFamily: 'Inter', fontSize: 48, fontWeight: 900,
                color: '#FAB95B', lineHeight: 1, marginBottom: 8,
              }}>
                {item.n}
              </div>
              <div style={{
                fontFamily: 'Inter', fontSize: 34, fontWeight: 800,
                color: '#E8EDF2', lineHeight: 1.05,
              }}>
                {item.t}
              </div>
              <div style={{
                fontFamily: 'Inter', fontSize: 22, fontWeight: 500,
                color: '#9E9E9E', marginTop: 6,
              }}>
                {item.s}
              </div>
            </div>
            {/* arrow to next */}
            {i < 3 && (
              <div style={{
                position: 'absolute', right: -32, top: '50%',
                transform: 'translateY(-50%)',
                opacity: item.op * 0.7,
              }}>
                <svg width="28" height="20" viewBox="0 0 28 20">
                  <polygon points="0,4 18,4 18,0 28,10 18,20 18,16 0,16" fill="#FAB95B" />
                </svg>
              </div>
            )}
          </div>
        ))}
      </div>

      {/* FOOTER */}
      <div style={{
        position: 'absolute', bottom: 70, left: 0, right: 0, textAlign: 'center',
        opacity: footer,
        transform: `translateY(${(1 - footer) * 20}px)`,
      }}>
        <div style={{
          display: 'inline-block', padding: '12px 30px',
          background: 'rgba(250,185,91,0.12)',
          border: '2px dashed #FAB95B', borderRadius: 8,
          fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 1,
        }}>
          The system is designed to make you whole.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene20_AnnualRecalculation;
```

