```tsx
CHAPTER 2: दौलत और पैसा — एक कागजी वादा | [01:05-02:27] | 10 scenes
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene1',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.3, stiffness: 180, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Micro animations
  const scale1 = interpolate(frame, [0, 30], [0.8, 1], { extrapolateLeft: 'clamp' });
  const rotate1 = interpolate(frame, [0, 60], [-2, 2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const yFloat = Math.sin(frame * 0.08) * 5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFFFFF', overflow: 'hidden', opacity: exitOp }}>
      {/* Minimalist Grid Lines */}
      <div style={{
        position: 'absolute', top: 0, left: 0, right: 0, bottom: 0,
        backgroundImage: 'linear-gradient(to right, #f0f0f0 1px, transparent 1px), linear-gradient(to bottom, #f0f0f0 1px, transparent 1px)',
        backgroundSize: '60px 60px',
        opacity: 0.5,
      }} />

      {/* Floating Question Mark Icon */}
      <div style={{
        position: 'absolute',
        top: '15%',
        left: '50%',
        transform: `translateX(-50%) translateY(${yFloat}px) rotate(${rotate1}deg) scale(${scale1})`,
      }}>
        <svg width="180" height="180" viewBox="0 0 24 24" fill="none" stroke="#FF6B6B" strokeWidth="2">
          <circle cx="12" cy="12" r="10" />
          <path d="M9 9a3 3 0 1 1 3.5 2.9c-.5.3-.9.7-.9 1.1v2" />
          <circle cx="12" cy="17" r="0.5" fill="#FF6B6B" />
        </svg>
      </div>

      {/* Main Question */}
      <div style={{
        position: 'absolute',
        top: '45%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '72px',
        fontWeight: 700,
        color: '#2D2D2D',
        textAlign: 'center',
        opacity: sp(15, { damping: 10 }),
        letterSpacing: '-1px',
      }}>
        दौलत और पैसा
      </div>

      {/* Emphasis Word */}
      <div style={{
        position: 'absolute',
        top: '62%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '90px',
        fontWeight: 800,
        color: '#FF6B6B',
        textAlign: 'center',
        opacity: sp(25, { damping: 12 }),
      }}>
        एक ही चीज़?
      </div>

      {/* Subtle Underline Animation */}
      <div style={{
        position: 'absolute',
        top: '74%',
        left: '50%',
        transform: `translateX(-50%)`,
        width: interpolate(frame, [30, 60], [0, 400], { extrapolateLeft: 'clamp' }),
        height: '4px',
        backgroundColor: '#FFC107',
        borderRadius: '2px',
      }} />

      {/* Small decorative dots */}
      {[...Array(5)].map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          top: `${80 + i * 5}%`,
          left: `${20 + i * 15}%`,
          width: '8px',
          height: '8px',
          borderRadius: '50%',
          backgroundColor: '#FFC107',
          opacity: interpolate(frame, [i * 5, i * 5 + 20], [0, 0.6], { extrapolateLeft: 'clamp' }),
          transform: `scale(${interpolate(frame, [i * 5, i * 5 + 15], [0, 1], { extrapolateLeft: 'clamp' })})`,
        }} />
      ))}

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene2',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene2: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 15, mass: 0.35, stiffness: 160, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Equal sign animation
  const equalsWidth = interpolate(frame, [0, 40], [0, 200], { extrapolateLeft: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FAFAFA', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient accent bar */}
      <div style={{
        position: 'absolute',
        top: 0,
        left: 0,
        right: 0,
        height: '6px',
        background: 'linear-gradient(90deg, #FF6B6B, #FFC107, #4ECDC4)',
        opacity: 0.8,
      }} />

      {/* Two overlapping circles representing equality */}
      <div style={{
        position: 'absolute',
        top: '30%',
        left: '30%',
        opacity: sp(10, { damping: 8 }),
      }}>
        <svg width="280" height="280" viewBox="0 0 100 100">
          <circle cx="35" cy="50" r="30" fill="#FF6B6B" fillOpacity="0.7" />
          <circle cx="65" cy="50" r="30" fill="#4ECDC4" fillOpacity="0.7" />
        </svg>
      </div>

      {/* Text below */}
      <div style={{
        position: 'absolute',
        top: '62%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '64px',
        fontWeight: 600,
        color: '#2D2D2D',
        textAlign: 'center',
        opacity: sp(20, { damping: 10 }),
      }}>
        ज़्यादातर लोग इन दोनों को
      </div>

      <div style={{
        position: 'absolute',
        top: '73%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '80px',
        fontWeight: 800,
        color: '#4ECDC4',
        textAlign: 'center',
        opacity: sp(30, { damping: 12 }),
      }}>
        एक समझते हैं
      </div>

      {/* Micro dots pattern */}
      {[...Array(20)].map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          top: `${10 + (i % 5) * 20}%`,
          right: `${5 + (i % 4) * 12}%`,
          width: '4px',
          height: '4px',
          borderRadius: '50%',
          backgroundColor: '#FFC107',
          opacity: interpolate(frame, [i * 2, i * 2 + 30], [0, 0.4], { extrapolateLeft: 'clamp' }),
        }} />
      ))}

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene3',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene3: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 140, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const items = [
    { icon: '💳', text: 'बैंक जमा', color: '#FF6B6B', x: '15%' },
    { icon: '📊', text: 'म्यूच्यूअल फंड', color: '#4ECDC4', x: '45%' },
    { icon: '💰', text: 'सैलरी अकाउंट', color: '#FFC107', x: '75%' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFFFFF', overflow: 'hidden', opacity: exitOp }}>
      {/* Header */}
      <div style={{
        position: 'absolute',
        top: '8%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '48px',
        fontWeight: 700,
        color: '#9E9E9E',
        opacity: sp(5),
      }}>
        यही दौलत लगती है
      </div>

      {/* Three cards */}
      {items.map((item, index) => (
        <div key={index} style={{
          position: 'absolute',
          top: `${28 + index * 18}%`,
          left: '50%',
          transform: `translateX(-50%) translateX(${index === 0 ? '-180px' : index === 1 ? '0px' : '180px'})`,
          display: 'flex',
          alignItems: 'center',
          gap: '20px',
          backgroundColor: '#F8F9FA',
          padding: '20px 40px',
          borderRadius: '16px',
          border: `3px solid ${item.color}`,
          opacity: sp(10 + index * 8, { damping: 10 }),
          transform: `translateX(-50%) translateX(${index === 0 ? '-180px' : index === 1 ? '0px' : '180px'}) scale(${sp(10 + index * 8, { damping: 10 })})`,
        }}>
          <div style={{
            fontSize: '48px',
            filter: 'grayscale(100%)',
          }}>
            {index === 0 && (
              <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke={item.color} strokeWidth="2">
                <rect x="2" y="5" width="20" height="14" rx="2" />
                <line x1="2" y1="10" x2="22" y2="10" />
              </svg>
            )}
            {index === 1 && (
              <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke={item.color} strokeWidth="2">
                <line x1="18" y1="20" x2="18" y2="10" />
                <line x1="12" y1="20" x2="12" y2="4" />
                <line x1="6" y1="20" x2="6" y2="14" />
              </svg>
            )}
            {index === 2 && (
              <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke={item.color} strokeWidth="2">
                <line x1="12" y1="1" x2="12" y2="23" />
                <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6" />
              </svg>
            )}
          </div>
          <span style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: '36px',
            fontWeight: 600,
            color: '#2D2D2D',
          }}>
            {item.text}
          </span>
        </div>
      ))}

      {/* Arrow pointing down */}
      <div style={{
        position: 'absolute',
        bottom: '18%',
        left: '50%',
        transform: `translateX(-50%)`,
        opacity: sp(35, { damping: 8 }),
      }}>
        <svg width="60" height="60" viewBox="0 0 24 24" fill="none" stroke="#9E9E9E" strokeWidth="2">
          <line x1="12" y1="5" x2="12" y2="19" />
          <polyline points="19 12 12 19 5 12" />
        </svg>
      </div>

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene4',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene4: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 11, mass: 0.35, stiffness: 150, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Paper tear animation
  const tearProgress = interpolate(frame, [0, 50], [0, 1], { extrapolateLeft: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFF9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Paper Document Icon */}
      <div style={{
        position: 'absolute',
        top: '20%',
        left: '50%',
        transform: `translateX(-50%) rotate(${interpolate(frame, [0, 30], [-3, 0], { extrapolateLeft: 'clamp' }) * (1 - tearProgress)}deg)`,
        opacity: sp(5, { damping: 8 }),
      }}>
        <svg width="200" height="260" viewBox="0 0 24 32">
          <path d="M4 0 L16 0 L20 4 L20 32 L4 32 Z" fill="#FFFFFF" stroke="#FFC107" strokeWidth="2" />
          <line x1="7" y1="8" x2="17" y2="8" stroke="#E0E0E0" strokeWidth="2" />
          <line x1="7" y1="13" x2="17" y2="13" stroke="#E0E0E0" strokeWidth="2" />
          <line x1="7" y1="18" x2="14" y2="18" stroke="#E0E0E0" strokeWidth="2" />
          {/* Tear effect */}
          <path d={`M${4 + tearProgress * 20} 32 Q${8 + tearProgress * 10} ${20 + tearProgress * 10} ${12 + tearProgress * 5} 32`} fill="#FFF9F0" stroke="#FF6B6B" strokeWidth="1" strokeDasharray="4 2" />
        </svg>
      </div>

      {/* Key Word: वादा */}
      <div style={{
        position: 'absolute',
        top: '55%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '120px',
        fontWeight: 800,
        color: '#FF6B6B',
        opacity: sp(20, { damping: 10 }),
        letterSpacing: '-3px',
      }}>
        वादा
      </div>

      {/* Subtext */}
      <div style={{
        position: 'absolute',
        top: '72%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '52px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: sp(30, { damping: 12 }),
      }}>
        एक कागजी वादा
      </div>

      {/* Shaking animation for "paper" feel */}
      <div style={{
        position: 'absolute',
        top: '45%',
        left: `${20 + Math.sin(frame * 0.1) * 2}%`,
        opacity: 0.3,
      }}>
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#FFC107" strokeWidth="2">
          <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
          <line x1="12" y1="9" x2="12" y2="13" />
          <line x1="12" y1="17" x2="12.01" y2="17" />
        </svg>
      </div>

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene5',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene5: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.3, stiffness: 170, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Trust/Handshake animation
  const trustScale = interpolate(frame, [0, 40], [0.5, 1], { extrapolateLeft: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFFFFF', overflow: 'hidden', opacity: exitOp }}>
      {/* Abstract trust symbol */}
      <div style={{
        position: 'absolute',
        top: '15%',
        left: '50%',
        transform: `translateX(-50%) scale(${trustScale})`,
        opacity: sp(5, { damping: 10 }),
      }}>
        <svg width="160" height="160" viewBox="0 0 100 100">
          {/* Interlocking hands / bridge symbol */}
          <path d="M20 70 Q30 50 50 50 Q70 50 80 70" fill="none" stroke="#4ECDC4" strokeWidth="4" />
          <path d="M30 60 Q50 40 70 60" fill="none" stroke="#FF6B6B" strokeWidth="4" />
          <circle cx="50" cy="30" r="15" fill="none" stroke="#FFC107" strokeWidth="3" />
          <path d="M45 28 L50 35 L58 25" fill="none" stroke="#FFC107" strokeWidth="2" />
        </svg>
      </div>

      {/* Main statement */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '58px',
        fontWeight: 700,
        color: '#2D2D2D',
        textAlign: 'center',
        lineHeight: 1.4,
        opacity: sp(15, { damping: 10 }),
      }}>
        बैंक अकाउंट का नंबर
      </div>

      {/* Arrow down */}
      <div style={{
        position: 'absolute',
        top: '62%',
        left: '50%',
        transform: `translateX(-50%)`,
        opacity: sp(22, { damping: 8 }),
      }}>
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#4ECDC4" strokeWidth="3">
          <line x1="12" y1="5" x2="12" y2="19" />
          <polyline points="19 12 12 19 5 12" />
        </svg>
      </div>

      {/* KEY WORD */}
      <div style={{
        position: 'absolute',
        top: '70%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '100px',
        fontWeight: 800,
        color: '#4ECDC4',
        opacity: sp(28, { damping: 12 }),
      }}>
        भरोसे पर
      </div>

      {/* Floating micro elements */}
      {[...Array(6)].map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          top: `${20 + (i * 12)}%`,
          left: `${10 + (i % 3) * 30}%`,
          width: '12px',
          height: '12px',
          borderRadius: '50%',
          backgroundColor: ['#FF6B6B', '#FFC107', '#4ECDC4'][i % 3],
          opacity: interpolate(frame, [i * 3, i * 3 + 25], [0, 0.5], { extrapolateLeft: 'clamp' }),
          transform: `translateY(${Math.sin(frame * 0.05 + i) * 10}px)`,
        }} />
      ))}

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene6',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.35, stiffness: 150, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const items = [
    { text: 'रोटी', subtext: 'खाने की ज़रूरत', color: '#FF6B6B', icon: '🍞' },
    { text: 'पेट्रोल', subtext: 'चलने की ज़रूरत', color: '#4ECDC4', icon: '⛽' },
    { text: 'फीस', subtext: 'पढ़ने की ज़रूरत', color: '#FFC107', icon: '📚' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#F8FFFE', overflow: 'hidden', opacity: exitOp }}>
      {/* Header */}
      <div style={{
        position: 'absolute',
        top: '5%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '42px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: sp(5),
      }}>
        आज भी...
      </div>

      {/* Three circular items */}
      {items.map((item, index) => (
        <div key={index} style={{
          position: 'absolute',
          top: `${25 + index * 22}%`,
          left: '50%',
          transform: `translateX(-50%)`,
          display: 'flex',
          alignItems: 'center',
          gap: '24px',
          opacity: sp(10 + index * 8, { damping: 10 }),
          scale: sp(10 + index * 8, { damping: 10 }),
        }}>
          {/* Circle with icon */}
          <div style={{
            width: '70px',
            height: '70px',
            borderRadius: '50%',
            backgroundColor: item.color,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            opacity: 0.9,
          }}>
            {index === 0 && (
              <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="#FFF" strokeWidth="2">
                <ellipse cx="12" cy="12" rx="8" ry="5" />
                <path d="M8 12 Q12 6 16 12" />
              </svg>
            )}
            {index === 1 && (
              <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="#FFF" strokeWidth="2">
                <path d="M3 22h18" />
                <path d="M6 22V10a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v12" />
                <path d="M6 8h12" />
              </svg>
            )}
            {index === 2 && (
              <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="#FFF" strokeWidth="2">
                <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20" />
                <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z" />
              </svg>
            )}
          </div>

          {/* Text */}
          <div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: '48px',
              fontWeight: 700,
              color: '#2D2D2D',
            }}>
              {item.text}
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: '28px',
              color: '#9E9E9E',
            }}>
              {item.subtext}
            </div>
          </div>
        </div>
      ))}

      {/* Connecting dots */}
      <div style={{
        position: 'absolute',
        top: '30%',
        left: '25%',
        width: '4px',
        height: '50%',
        backgroundColor: '#E0E0E0',
        opacity: 0.5,
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene7',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene7: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.3, stiffness: 160, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // System checkmark animation
  const checkScale = interpolate(frame, [0, 30], [0, 1], { extrapolateLeft: 'clamp' });
  const checkRotate = interpolate(frame, [0, 20], [-10, 0], { extrapolateLeft: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFFFFF', overflow: 'hidden', opacity: exitOp }}>
      {/* Success/Checkmark Circle */}
      <div style={{
        position: 'absolute',
        top: '18%',
        left: '50%',
        transform: `translateX(-50%) scale(${checkScale}) rotate(${checkRotate}deg)`,
      }}>
        <svg width="180" height="180" viewBox="0 0 100 100">
          <circle cx="50" cy="50" r="45" fill="none" stroke="#4ECDC4" strokeWidth="6" />
          <path d="M30 50 L45 65 L70 35" fill="none" stroke="#4ECDC4" strokeWidth="6" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>

      {/* Status Text */}
      <div style={{
        position: 'absolute',
        top: '48%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '52px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: sp(15, { damping: 10 }),
      }}>
        सिस्टम ठीक है
      </div>

      {/* Big Statement */}
      <div style={{
        position: 'absolute',
        top: '62%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '72px',
        fontWeight: 800,
        color: '#4ECDC4',
        opacity: sp(25, { damping: 12 }),
      }}>
        ✓ वादा पूरा
      </div>

      {/* Green bar animation */}
      <div style={{
        position: 'absolute',
        bottom: '20%',
        left: '50%',
        transform: `translateX(-50%)`,
        width: interpolate(frame, [30, 80], [0, 500], { extrapolateLeft: 'clamp' }),
        height: '8px',
        backgroundColor: '#4ECDC4',
        borderRadius: '4px',
        opacity: 0.3,
      }} />

      {/* Pulse effect */}
      <div style={{
        position: 'absolute',
        top: '18%',
        left: '50%',
        transform: `translateX(-50%)`,
        width: `${200 + Math.sin(frame * 0.1) * 20}px`,
        height: `${200 + Math.sin(frame * 0.1) * 20}px`,
        borderRadius: '50%',
        border: '3px solid #4ECDC4',
        opacity: interpolate(frame, [0, 30], [0.3, 0], { extrapolateLeft: 'clamp' }),
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene8',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene8: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 10, mass: 0.4, stiffness: 130, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Shake effect
  const shakeX = Math.sin(frame * 0.5) * (frame > 20 ? 8 : 0);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFF5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Warning Triangle */}
      <div style={{
        position: 'absolute',
        top: '15%',
        left: '50%',
        transform: `translateX(-50%) translateX(${shakeX}px)`,
        opacity: sp(5, { damping: 8 }),
      }}>
        <svg width="200" height="200" viewBox="0 0 100 100">
          <path d="M50 10 L90 85 L10 85 Z" fill="none" stroke="#FF6B6B" strokeWidth="5" />
          <line x1="50" y1="35" x2="50" y2="55" stroke="#FF6B6B" strokeWidth="5" strokeLinecap="round" />
          <circle cx="50" cy="70" r="4" fill="#FF6B6B" />
        </svg>
      </div>

      {/* Status Text */}
      <div style={{
        position: 'absolute',
        top: '48%',
        left: '50%',
        transform: `translateX(-50%) translateX(${shakeX}px)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '52px',
        fontWeight: 600,
        color: '#FF6B6B',
        opacity: sp(15, { damping: 10 }),
      }}>
        सिस्टम डगमगा रहा है
      </div>

      {/* Big Statement */}
      <div style={{
        position: 'absolute',
        top: '62%',
        left: '50%',
        transform: `translateX(-50%) translateX(${shakeX}px)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '80px',
        fontWeight: 800,
        color: '#FF6B6B',
        opacity: sp(25, { damping: 12 }),
      }}>
        ✗ वादा टूटा
      </div>

      {/* Breaking glass effect */}
      <div style={{
        position: 'absolute',
        top: '70%',
        left: '50%',
        transform: `translateX(-50%)`,
      }}>
        {[...Array(8)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: `${30 + i * 15}px`,
            height: '3px',
            backgroundColor: '#FF6B6B',
            opacity: interpolate(frame, [30 + i * 3, 60 + i * 3], [0, 0.5], { extrapolateLeft: 'clamp' }),
            transform: `rotate(${i * 45}deg)`,
          }} />
        ))}
      </div>

      {/* Red warning bars */}
      <div style={{
        position: 'absolute',
        top: 0,
        left: 0,
        right: 0,
        height: '4px',
        backgroundColor: '#FF6B6B',
        opacity: interpolate(frame, [10, 40], [0, 0.8], { extrapolateLeft: 'clamp' }),
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene9',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene9: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.35, stiffness: 145, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Generation chain animation
  const generations = [
    { label: 'दादा', color: '#FF6B6B', y: '20%' },
    { label: 'पिता', color: '#FFC107', y: '45%' },
    { label: 'आप', color: '#4ECDC4', y: '70%' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFFFFF', overflow: 'hidden', opacity: exitOp }}>
      {/* Title */}
      <div style={{
        position: 'absolute',
        top: '5%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '44px',
        fontWeight: 700,
        color: '#2D2D2D',
        opacity: sp(5),
      }}>
        पीढ़ी दर पीढ़ी
      </div>

      {/* Generation chain */}
      {generations.map((gen, index) => (
        <div key={index} style={{
          position: 'absolute',
          top: gen.y,
          left: '50%',
          transform: `translateX(-50%)`,
          display: 'flex',
          alignItems: 'center',
          gap: '20px',
          opacity: sp(10 + index * 8, { damping: 10 }),
        }}>
          {/* Circle with person icon */}
          <div style={{
            width: '80px',
            height: '80px',
            borderRadius: '50%',
            backgroundColor: gen.color,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `scale(${sp(10 + index * 8, { damping: 10 })})`,
          }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#FFF" strokeWidth="2">
              <circle cx="12" cy="7" r="4" />
              <path d="M5.5 21a8.5 8.5 0 0 1 13 0" />
            </svg>
          </div>
          
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: '52px',
            fontWeight: 700,
            color: gen.color,
          }}>
            {gen.label}
          </div>
        </div>
      ))}

      {/* Connecting lines */}
      <div style={{
        position: 'absolute',
        top: '26%',
        left: '50%',
        transform: 'translateX(-50%)',
        width: '4px',
        height: '46%',
        backgroundColor: '#E0E0E0',
      }} />

      {/* Arrow down */}
      <div style={{
        position: 'absolute',
        top: '90%',
        left: '50%',
        transform: `translateX(-50%)`,
        opacity: sp(35, { damping: 8 }),
      }}>
        <svg width="50" height="50" viewBox="0 0 24 24" fill="none" stroke="#2D2D2D" strokeWidth="2">
          <polyline points="6 9 12 15 18 9" />
        </svg>
      </div>

      {/* Key insight */}
      <div style={{
        position: 'absolute',
        top: '92%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '36px',
        fontWeight: 600,
        color: '#4ECDC4',
        opacity: sp(40, { damping: 10 }),
      }}>
        संपत्ति बचाते आए हैं
      </div>

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter2_Scene10',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene10: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.3, stiffness: 155, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Balance scale animation
  const balanceAngle = interpolate(frame, [0, 50], [-5, 0], { extrapolateLeft: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Scale/Balance visual */}
      <div style={{
        position: 'absolute',
        top: '10%',
        left: '50%',
        transform: `translateX(-50%) rotate(${balanceAngle}deg)`,
        opacity: sp(5, { damping: 8 }),
      }}>
        <svg width="400" height="200" viewBox="0 0 200 100">
          {/* Center pole */}
          <line x1="100" y1="0" x2="100" y2="80" stroke="#2D2D2D" strokeWidth="4" />
          {/* Base */}
          <path d="M60 80 L140 80 L130 90 L70 90 Z" fill="#2D2D2D" />
          {/* Balance beam */}
          <line x1="30" y1="50" x2="170" y2="50" stroke="#4ECDC4" strokeWidth="6" strokeLinecap="round" />
          {/* Left pan - Paper */}
          <line x1="30" y1="50" x2="30" y2="70" stroke="#9E9E9E" strokeWidth="2" />
          <ellipse cx="30" cy="75" rx="25" ry="8" fill="#E0E0E0" stroke="#9E9E9E" strokeWidth="2" />
          <text x="30" y="95" textAnchor="middle" fontSize="12" fill="#9E9E9E">कागज</text>
          {/* Right pan - System */}
          <line x1="170" y1="50" x2="170" y2="70" stroke="#9E9E9E" strokeWidth="2" />
          <ellipse cx="170" cy="75" rx="25" ry="8" fill="#4ECDC4" fillOpacity="0.3" stroke="#4ECDC4" strokeWidth="2" />
          <text x="170" y="95" textAnchor="middle" fontSize="12" fill="#4ECDC4">सिस्टम</text>
        </svg>
      </div>

      {/* Key statement */}
      <div style={{
        position: 'absolute',
        top: '55%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '50px',
        fontWeight: 700,
        color: '#2D2D2D',
        textAlign: 'center',
        lineHeight: 1.5,
        opacity: sp(20, { damping: 10 }),
      }}>
        कागजी वादे उतने ही मजबूत
      </div>

      <div style={{
        position: 'absolute',
        top: '68%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '60px',
        fontWeight: 800,
        color: '#4ECDC4',
        textAlign: 'center',
        opacity: sp(28, { damping: 12 }),
      }}>
        जितना सिस्टम
      </div>

      {/* Subtle background pattern */}
      <div style={{
        position: 'absolute',
        top: 0,
        left: 0,
        right: 0,
        bottom: 0,
        backgroundImage: 'radial-gradient(circle at 20% 80%, #4ECDC4 1px, transparent 1px), radial-gradient(circle at 80% 20%, #FF6B6B 1px, transparent 1px)',
        backgroundSize: '50px 50px',
        opacity: 0.1,
      }} />

      {/* Transition hint */}
      <div style={{
        position: 'absolute',
        bottom: '12%',
        left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: '32px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [50, 70], [0, 0.6], { extrapolateLeft: 'clamp' }),
      }}>
        →
      </div>

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: '22px',
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10;
```

