```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11_FourReasons',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_FourReasons: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.06]);

  // Title reveal
  const titleY = interpolate(sp(10), [0, 1], [80, 0]);
  const titleOp = interpolate(sp(10), [0, 1], [0, 1]);

  // Four numbered cards stagger
  const card1 = sp(40);
  const card2 = sp(70);
  const card3 = sp(100);
  const card4 = sp(130);

  // Bottom tagline reveal
  const taglineOp = interpolate(sp(180), [0, 1], [0, 1]);
  const taglineY = interpolate(sp(180), [0, 1], [40, 0]);

  // Underline sweep on title
  const underlineW = interpolate(frame, [30, 60], [0, 100], { extrapolateRight: 'clamp' });

  const reasons = [
    { num: '01', text: 'साइकोलॉजी' },
    { num: '02', text: 'इकोनॉमिक्स' },
    { num: '03', text: 'सोशल कोड' },
    { num: '04', text: 'फितरत' },
  ];
  const cardProgress = [card1, card2, card3, card4];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 30%, rgba(238,238,238,0.18) 0%, rgba(8,6,22,0.35) 80%)',
        }}
      />

      {/* Animated shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 900,
          height: 900,
          borderRadius: '50%',
          border: '2px solid #EEEEEE',
          opacity: 0.06,
          top: -200,
          left: -150,
          transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: 40,
          border: '3px solid #080616',
          opacity: 0.05,
          bottom: -200,
          right: -150,
          transform: `rotate(${-frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          background: '#080616',
          opacity: 0.04,
          top: 300,
          right: 200,
          transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.cos(frame * 0.03) * 0.08})`,
        }}
      />

      {/* Diagonal grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid11" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#080616" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid11)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const seed = i + 1;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        const drift = Math.sin(frame * 0.02 + seed) * 30;
        const size = 4 + seededRandom(seed * 3) * 6;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + drift,
              top: y + Math.cos(frame * 0.015 + seed) * 25,
              width: size,
              height: size,
              borderRadius: '50%',
              background: i % 2 === 0 ? '#EEEEEE' : '#080616',
              opacity: 0.4,
            }}
          />
        );
      })}

      {/* CONTENT */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          transform: `scale(${camScale})`,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'flex-start',
          paddingTop: 120,
        }}
      >
        {/* Top label */}
        <div
          style={{
            opacity: titleOp,
            background: '#080616',
            color: '#EEEEEE',
            fontFamily: 'Inter, Poppins',
            fontSize: 32,
            fontWeight: 700,
            padding: '14px 36px',
            borderRadius: 50,
            letterSpacing: 4,
            marginBottom: 30,
            transform: `translateY(${titleY * 0.3}px)`,
          }}
        >
          THIS VIDEO • चार ठोस कारण
        </div>

        {/* Main title */}
        <div
          style={{
            transform: `translateY(${titleY}px)`,
            opacity: titleOp,
            fontFamily: 'Yantra One',
            fontSize: 195,
            fontWeight: 900,
            color: '#080616',
            lineHeight: 1,
            textAlign: 'center',
            letterSpacing: -2,
          }}
        >
          गरीब दिखना
        </div>
        <div
          style={{
            transform: `translateY(${titleY}px)`,
            opacity: titleOp,
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#EEEEEE',
            lineHeight: 1,
            marginTop: 10,
            textShadow: '4px 4px 0 #080616',
          }}
        >
          = सबसे बड़ी ताक़त
        </div>

        {/* Underline */}
        <div
          style={{
            width: `${underlineW * 6}px`,
            height: 8,
            background: '#080616',
            marginTop: 28,
            borderRadius: 4,
          }}
        />

        {/* Four reason cards */}
        <div
          style={{
            display: 'flex',
            gap: 30,
            marginTop: 80,
          }}
        >
          {reasons.map((r, i) => {
            const p = cardProgress[i];
            return (
              <div
                key={r.num}
                style={{
                  transform: `translateY(${interpolate(p, [0, 1], [120, 0])}px) scale(${interpolate(p, [0, 1], [0.7, 1])})`,
                  opacity: p,
                  width: 320,
                  height: 260,
                  background: i % 2 === 0 ? '#EEEEEE' : '#080616',
                  color: i % 2 === 0 ? '#080616' : '#EEEEEE',
                  borderRadius: 24,
                  padding: 28,
                  display: 'flex',
                  flexDirection: 'column',
                  justifyContent: 'space-between',
                  border: '4px solid #080616',
                  boxShadow: '10px 10px 0 rgba(8,6,22,0.4)',
                }}
              >
                <div
                  style={{
                    fontFamily: 'JetBrains Mono',
                    fontSize: 36,
                    fontWeight: 700,
                    opacity: 0.6,
                  }}
                >
                  REASON
                </div>
                <div
                  style={{
                    fontFamily: 'Yantra One',
                    fontSize: 136,
                    fontWeight: 900,
                    lineHeight: 1,
                  }}
                >
                  {r.num}
                </div>
                <div
                  style={{
                    fontFamily: 'Noto Sans Devanagari',
                    fontSize: 51,
                    fontWeight: 800,
                  }}
                >
                  {r.text}
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom tagline */}
        <div
          style={{
            opacity: taglineOp,
            transform: `translateY(${taglineY}px)`,
            marginTop: 50,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 60,
            fontWeight: 700,
            color: '#080616',
            background: '#EEEEEE',
            padding: '18px 50px',
            borderRadius: 16,
            border: '4px solid #080616',
          }}
        >
          ये फिलॉसफी नहीं है — ये <span style={{ color: '#FF61F8', background: '#080616', padding: '0 14px' }}>विज्ञान</span> है
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(8,6,22,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_FourReasons;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12_Mallya_Intro',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_Mallya_Intro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan left to right
  const camX = interpolate(frame, [0, durationInFrames], [-20, 20]);

  // Three pillar cards
  const pillar1 = sp(15);
  const pillar2 = sp(40);
  const pillar3 = sp(65);

  // Divider line
  const dividerW = interpolate(sp(100), [0, 1], [0, 1600]);

  // CASE STUDY tag
  const tagY = interpolate(sp(140), [0, 1], [-80, 0]);
  const tagOp = interpolate(sp(140), [0, 1], [0, 1]);

  // Mallya name reveal
  const nameScale = sp(170);
  const nameOp = interpolate(sp(170), [0, 1], [0, 1]);

  // Shake on name impact
  const shake = frame > 175 && frame < 200 ? Math.sin(frame * 2) * 4 : 0;

  // Subtitle
  const subOp = interpolate(sp(220), [0, 1], [0, 1]);

  const pillars = [
    { label: 'Psychology', hindi: 'साइकोलॉजी', icon: 'brain' },
    { label: 'Economics', hindi: 'इकोनॉमिक्स', icon: 'graph' },
    { label: 'Human Nature', hindi: 'इंसानी फितरत', icon: 'dna' },
  ];
  const pillarProgress = [pillar1, pillar2, pillar3];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(255,97,248,0) 0%, rgba(8,6,22,0.25) 100%)',
        }}
      />

      {/* Big rotating square shape */}
      <div
        style={{
          position: 'absolute',
          width: 1000,
          height: 1000,
          border: '3px solid #080616',
          opacity: 0.05,
          top: -300,
          right: -200,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          borderRadius: '50%',
          background: '#EEEEEE',
          opacity: 0.04,
          bottom: -100,
          left: -100,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />

      {/* Dots texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots12" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="2" cy="2" r="2" fill="#080616" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots12)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = i + 10;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + seed) * 25,
              top: y + Math.cos(frame * 0.025 + seed) * 30,
              width: 6,
              height: 6,
              background: '#EEEEEE',
              opacity: 0.5,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `translateX(${camX}px)`,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          paddingTop: 90,
        }}
      >
        {/* TOP HEADING */}
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 60,
            fontWeight: 800,
            color: '#080616',
            textAlign: 'center',
            lineHeight: 1.1,
          }}
        >
          इसके पीछे है <span style={{ background: '#080616', color: '#FF61F8', padding: '0 18px' }}>तीन ताक़तें</span>
        </div>

        {/* Three pillars */}
        <div style={{ display: 'flex', gap: 60, marginTop: 70 }}>
          {pillars.map((p, i) => {
            const prog = pillarProgress[i];
            return (
              <div
                key={p.label}
                style={{
                  opacity: prog,
                  transform: `translateY(${interpolate(prog, [0, 1], [150, 0])}px)`,
                  width: 380,
                  height: 380,
                  background: i === 1 ? '#080616' : '#EEEEEE',
                  color: i === 1 ? '#EEEEEE' : '#080616',
                  borderRadius: 30,
                  border: '5px solid #080616',
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  justifyContent: 'center',
                  padding: 30,
                  boxShadow: i === 1 ? '12px 12px 0 #EEEEEE' : '12px 12px 0 #080616',
                }}
              >
                {/* Icon */}
                <svg width="120" height="120" viewBox="0 0 100 100" style={{ marginBottom: 20 }}>
                  {p.icon === 'brain' && (
                    <g stroke={i === 1 ? '#EEEEEE' : '#080616'} strokeWidth="4" fill="none">
                      <path d="M30 50 Q30 25 50 25 Q70 25 70 50 Q70 75 50 75 Q30 75 30 50 Z" />
                      <path d="M50 25 L50 75" />
                      <path d="M40 40 Q50 45 60 40" />
                      <path d="M40 60 Q50 55 60 60" />
                    </g>
                  )}
                  {p.icon === 'graph' && (
                    <g stroke={i === 1 ? '#EEEEEE' : '#080616'} strokeWidth="5" fill="none">
                      <polyline points="15,80 35,55 55,65 75,30 85,40" />
                      <line x1="15" y1="80" x2="85" y2="80" />
                      <line x1="15" y1="80" x2="15" y2="20" />
                    </g>
                  )}
                  {p.icon === 'dna' && (
                    <g stroke={i === 1 ? '#EEEEEE' : '#080616'} strokeWidth="4" fill="none">
                      <path d="M30 20 Q70 40 30 60 Q70 80 30 100" />
                      <path d="M70 20 Q30 40 70 60 Q30 80 70 100" />
                      <line x1="35" y1="30" x2="65" y2="30" />
                      <line x1="35" y1="50" x2="65" y2="50" />
                      <line x1="35" y1="70" x2="65" y2="70" />
                    </g>
                  )}
                </svg>

                <div
                  style={{
                    fontFamily: 'JetBrains Mono',
                    fontSize: 26,
                    fontWeight: 700,
                    letterSpacing: 3,
                    opacity: 0.7,
                  }}
                >
                  {p.label.toUpperCase()}
                </div>
                <div
                  style={{
                    fontFamily: 'Yantra One',
                    fontSize: 68,
                    fontWeight: 900,
                    marginTop: 14,
                    textAlign: 'center',
                  }}
                >
                  {p.hindi}
                </div>
              </div>
            );
          })}
        </div>

        {/* Divider */}
        <div
          style={{
            width: dividerW,
            height: 6,
            background: '#080616',
            marginTop: 50,
            borderRadius: 3,
          }}
        />

        {/* CASE STUDY tag */}
        <div
          style={{
            opacity: tagOp,
            transform: `translateY(${tagY}px)`,
            marginTop: 30,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <div
            style={{
              background: '#080616',
              color: '#FF61F8',
              fontFamily: 'JetBrains Mono',
              fontWeight: 800,
              fontSize: 30,
              padding: '12px 28px',
              letterSpacing: 4,
              borderRadius: 8,
            }}
          >
            CASE #01
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 48,
              fontWeight: 700,
              color: '#080616',
            }}
          >
            किंगफिशर के मालिक →
          </div>
        </div>

        {/* Big Name */}
        <div
          style={{
            opacity: nameOp,
            transform: `scale(${nameScale}) translateX(${shake}px)`,
            fontFamily: 'Yantra One',
            fontSize: 187,
            fontWeight: 900,
            color: '#EEEEEE',
            marginTop: 10,
            textShadow: '6px 6px 0 #080616',
            letterSpacing: -2,
          }}
        >
          विजय माल्या
        </div>

        <div
          style={{
            opacity: subOp,
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 600,
            color: '#080616',
            marginTop: 5,
            letterSpacing: 2,
          }}
        >
          THE FALLEN KING — एक चेतावनी
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(8,6,22,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_Mallya_Intro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13_ThenVsNow',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_ThenVsNow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // THEN side
  const thenOp = interpolate(sp(10), [0, 1], [0, 1]);
  const thenX = interpolate(sp(10), [0, 1], [-100, 0]);

  // Luxury items stagger
  const item1 = sp(30);
  const item2 = sp(50);
  const item3 = sp(70);

  // NOW side flip in
  const nowOp = interpolate(sp(110), [0, 1], [0, 1]);
  const nowX = interpolate(sp(110), [0, 1], [100, 0]);

  // Arrow sweep
  const arrowProg = interpolate(sp(150), [0, 1], [0, 1]);

  // Nirav Modi entry
  const niravScale = sp(220);
  const niravOp = interpolate(sp(220), [0, 1], [0, 1]);

  // Jail bars slide
  const barsX = interpolate(sp(260), [0, 1], [-200, 0]);
  const barsOp = interpolate(sp(260), [0, 1], [0, 1]);

  const thenItems = [
    { label: 'Private Jets', icon: 'jet' },
    { label: 'Goa Island', icon: 'island' },
    { label: 'F1 Team', icon: 'car' },
  ];
  const itemProg = [item1, item2, item3];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      {/* Split background tint */}
      <div
        style={{
          position: 'absolute',
          left: 0,
          top: 0,
          width: '50%',
          height: '100%',
          background: 'linear-gradient(180deg, rgba(238,238,238,0.12) 0%, rgba(238,238,238,0) 100%)',
        }}
      />
      <div
        style={{
          position: 'absolute',
          right: 0,
          top: 0,
          width: '50%',
          height: '100%',
          background: 'linear-gradient(180deg, rgba(8,6,22,0.25) 0%, rgba(8,6,22,0.05) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          border: '2px solid #EEEEEE',
          borderRadius: '50%',
          opacity: 0.06,
          top: -200,
          left: -300,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          background: '#080616',
          opacity: 0.05,
          bottom: -250,
          right: -200,
          transform: `rotate(${-frame * 0.15}deg)`,
        }}
      />

      {/* Vertical lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="vline13" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="60" stroke="#080616" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vline13)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 20;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + seed) * 25,
              top: y + Math.cos(frame * 0.02 + seed) * 25,
              width: 5,
              height: 5,
              background: i % 2 === 0 ? '#EEEEEE' : '#080616',
              opacity: 0.5,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <div style={{ width: '100%', height: '100%', transform: `scale(${camScale})` }}>
        {/* HEADING */}
        <div
          style={{
            position: 'absolute',
            top: 50,
            left: 0,
            width: '100%',
            textAlign: 'center',
            fontFamily: 'Yantra One',
            fontSize: 80,
            fontWeight: 900,
            color: '#080616',
          }}
        >
          चमक vs <span style={{ color: '#EEEEEE', textShadow: '4px 4px 0 #080616' }}>हक़ीक़त</span>
        </div>

        {/* THEN PANEL */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 60,
            width: 750,
            opacity: thenOp,
            transform: `translateX(${thenX}px)`,
          }}
        >
          <div
            style={{
              background: '#080616',
              color: '#FF61F8',
              fontFamily: 'JetBrains Mono',
              fontSize: 32,
              fontWeight: 800,
              padding: '12px 28px',
              letterSpacing: 4,
              borderRadius: 8,
              display: 'inline-block',
            }}
          >
            THEN • कभी
          </div>

          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 110,
              fontWeight: 900,
              color: '#EEEEEE',
              marginTop: 15,
              textShadow: '4px 4px 0 #080616',
              lineHeight: 1,
            }}
          >
            माल्या
          </div>

          <div style={{ marginTop: 35, display: 'flex', flexDirection: 'column', gap: 16 }}>
            {thenItems.map((it, i) => {
              const p = itemProg[i];
              return (
                <div
                  key={it.label}
                  style={{
                    opacity: p,
                    transform: `translateX(${interpolate(p, [0, 1], [-80, 0])}px)`,
                    background: '#EEEEEE',
                    border: '4px solid #080616',
                    borderRadius: 16,
                    padding: '18px 24px',
                    display: 'flex',
                    alignItems: 'center',
                    gap: 20,
                  }}
                >
                  <svg width="60" height="60" viewBox="0 0 60 60">
                    {it.icon === 'jet' && (
                      <g fill="#080616">
                        <path d="M5 30 L50 28 L55 32 L50 36 L5 34 Z" />
                        <path d="M25 28 L30 10 L34 10 L32 28 Z" />
                        <path d="M25 36 L30 50 L34 50 L32 36 Z" />
                      </g>
                    )}
                    {it.icon === 'island' && (
                      <g fill="#080616">
                        <ellipse cx="30" cy="48" rx="25" ry="6" />
                        <path d="M28 48 L28 20 L35 25 L40 18 L42 25 L45 22 L40 30 L30 30 Z" />
                        <circle cx="22" cy="42" r="3" />
                      </g>
                    )}
                    {it.icon === 'car' && (
                      <g fill="#080616">
                        <path d="M5 35 L10 25 L20 22 L40 22 L50 28 L55 35 L55 42 L5 42 Z" />
                        <circle cx="15" cy="42" r="6" fill="#EEEEEE" stroke="#080616" strokeWidth="2" />
                        <circle cx="45" cy="42" r="6" fill="#EEEEEE" stroke="#080616" strokeWidth="2" />
                      </g>
                    )}
                  </svg>
                  <div
                    style={{
                      fontFamily: 'Inter',
                      fontSize: 45,
                      fontWeight: 800,
                      color: '#080616',
                    }}
                  >
                    {it.label}
                  </div>
                </div>
              );
            })}
          </div>
        </div>

        {/* ARROW DIVIDER */}
        <div
          style={{
            position: 'absolute',
            top: 460,
            left: '50%',
            transform: 'translateX(-50%)',
          }}
        >
          <svg width="200" height="100" viewBox="0 0 200 100">
            <line
              x1="10"
              y1="50"
              x2={10 + arrowProg * 160}
              y2="50"
              stroke="#080616"
              strokeWidth="8"
              strokeLinecap="round"
            />
            {arrowProg > 0.8 && (
              <polygon
                points="170,30 190,50 170,70"
                fill="#080616"
              />
            )}
          </svg>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 24,
              fontWeight: 800,
              color: '#080616',
              textAlign: 'center',
              marginTop: 5,
              opacity: arrowProg,
            }}
          >
            CRASH
          </div>
        </div>

        {/* NOW PANEL — NIRAV MODI */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            right: 60,
            width: 750,
            opacity: nowOp,
            transform: `translateX(${nowX}px)`,
          }}
        >
          <div
            style={{
              background: '#EEEEEE',
              color: '#080616',
              fontFamily: 'JetBrains Mono',
              fontSize: 32,
              fontWeight: 800,
              padding: '12px 28px',
              letterSpacing: 4,
              borderRadius: 8,
              display: 'inline-block',
            }}
          >
            NOW • आज
          </div>

          <div
            style={{
              opacity: niravOp,
              transform: `scale(${niravScale})`,
              fontFamily: 'Yantra One',
              fontSize: 110,
              fontWeight: 900,
              color: '#080616',
              marginTop: 15,
              lineHeight: 1,
            }}
          >
            नीरव मोदी
          </div>

          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 51,
              fontWeight: 700,
              color: '#EEEEEE',
              background: '#080616',
              padding: '12px 22px',
              marginTop: 25,
              borderRadius: 12,
              display: 'inline-block',
            }}
          >
            हीरों का बादशाह →
          </div>

          {/* JAIL VISUAL */}
          <div
            style={{
              opacity: barsOp,
              transform: `translateX(${barsX}px)`,
              marginTop: 30,
              background: '#080616',
              borderRadius: 16,
              padding: 30,
              border: '4px solid #EEEEEE',
              position: 'relative',
              overflow: 'hidden',
            }}
          >
            {/* Bars */}
            <div style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', display: 'flex', justifyContent: 'space-around', opacity: 0.4 }}>
              {Array.from({ length: 8 }).map((_, i) => (
                <div key={i} style={{ width: 6, height: '100%', background: '#EEEEEE' }} />
              ))}
            </div>
            <div
              style={{
                position: 'relative',
                fontFamily: 'Inter',
                fontSize: 55,
                fontWeight: 900,
                color: '#FF61F8',
                textAlign: 'center',
                letterSpacing: 2,
              }}
            >
              LONDON JAIL
            </div>
            <div
              style={{
                position: 'relative',
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 36,
                fontWeight: 600,
                color: '#EEEEEE',
                textAlign: 'center',
                marginTop: 8,
              }}
            >
              लंदन की जेल में बंद
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(8,6,22,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_ThenVsNow;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14_Damani',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_Damani: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  // Flip card
  const flip = sp(10);
  const nameY = interpolate(flip, [0, 1], [-150, 0]);

  // Badge top 5
  const badgeScale = sp(50);

  // Profile card
  const profileOp = interpolate(sp(90), [0, 1], [0, 1]);
  const profileY = interpolate(sp(90), [0, 1], [60, 0]);

  // Three trait reveals
  const t1 = sp(140);
  const t2 = sp(170);
  const t3 = sp(200);

  // Bottom quote
  const quoteOp = interpolate(sp(260), [0, 1], [0, 1]);

  // Pulse on TOP 5
  const pulse = 1 + Math.sin(frame * 0.1) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 70% 40%, rgba(238,238,238,0.18) 0%, rgba(8,6,22,0.2) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 900,
          height: 900,
          borderRadius: 60,
          border: '3px solid #080616',
          opacity: 0.05,
          top: -200,
          left: -200,
          transform: `rotate(${frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: '#EEEEEE',
          opacity: 0.06,
          bottom: -100,
          right: -100,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="grid14" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#080616" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid14)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const seed = i + 30;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + seed) * 22,
              top: y + Math.cos(frame * 0.02 + seed) * 28,
              width: 5,
              height: 5,
              background: '#EEEEEE',
              opacity: 0.55,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <div style={{ width: '100%', height: '100%', transform: `scale(${camScale})`, padding: 80 }}>
        {/* TOP TAG */}
        <div
          style={{
            display: 'flex',
            justifyContent: 'center',
            gap: 16,
            marginBottom: 30,
          }}
        >
          <div
            style={{
              background: '#080616',
              color: '#FF61F8',
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 800,
              padding: '12px 26px',
              letterSpacing: 4,
              borderRadius: 8,
            }}
          >
            CASE #02
          </div>
          <div
            style={{
              background: '#EEEEEE',
              color: '#080616',
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 700,
              padding: '12px 26px',
              borderRadius: 8,
              border: '3px solid #080616',
            }}
          >
            दूसरी तरफ देखिए →
          </div>
        </div>

        {/* MAIN NAME */}
        <div
          style={{
            transform: `translateY(${nameY}px)`,
            opacity: flip,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 170,
              fontWeight: 900,
              color: '#080616',
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            राधाकिशन दामानी
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 47,
              fontWeight: 800,
              color: '#EEEEEE',
              background: '#080616',
              display: 'inline-block',
              padding: '10px 30px',
              borderRadius: 12,
              marginTop: 12,
              letterSpacing: 3,
            }}
          >
            FOUNDER • D-MART
          </div>
        </div>

        {/* TOP 5 BADGE + Profile */}
        <div
          style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 60,
            marginTop: 60,
          }}
        >
          {/* Badge */}
          <div
            style={{
              transform: `scale(${badgeScale * pulse})`,
              width: 320,
              height: 320,
              background: '#EEEEEE',
              border: '6px solid #080616',
              borderRadius: '50%',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '10px 10px 0 #080616',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 28,
                fontWeight: 800,
                color: '#080616',
                letterSpacing: 3,
              }}
            >
              INDIA RANK
            </div>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 195,
                fontWeight: 900,
                color: '#080616',
                lineHeight: 0.9,
                marginTop: -10,
              }}
            >
              #5
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 30,
                fontWeight: 700,
                color: '#080616',
              }}
            >
              अमीर लोगों में
            </div>
          </div>

          {/* Traits column */}
          <div
            style={{
              opacity: profileOp,
              transform: `translateY(${profileY}px)`,
              display: 'flex',
              flexDirection: 'column',
              gap: 20,
              width: 700,
            }}
          >
            {[
              { p: t1, icon: 'shirt', text: 'सफेद शर्ट + सादी पैंट' },
              { p: t2, icon: 'train', text: 'मुंबई लोकल ट्रेन' },
              { p: t3, icon: 'nobling', text: 'ज़ीरो दिखावा' },
            ].map((it, i) => (
              <div
                key={i}
                style={{
                  opacity: it.p,
                  transform: `translateX(${interpolate(it.p, [0, 1], [80, 0])}px)`,
                  background: i === 1 ? '#080616' : '#EEEEEE',
                  color: i === 1 ? '#EEEEEE' : '#080616',
                  padding: '20px 28px',
                  borderRadius: 16,
                  border: '4px solid #080616',
                  display: 'flex',
                  alignItems: 'center',
                  gap: 24,
                  boxShadow: '6px 6px 0 rgba(8,6,22,0.4)',
                }}
              >
                <svg width="70" height="70" viewBox="0 0 60 60">
                  {it.icon === 'shirt' && (
                    <g fill={i === 1 ? '#EEEEEE' : '#080616'}>
                      <path d="M15 15 L25 10 L35 10 L45 15 L50 20 L45 25 L42 22 L42 50 L18 50 L18 22 L15 25 L10 20 Z" />
                    </g>
                  )}
                  {it.icon === 'train' && (
                    <g fill={i === 1 ? '#EEEEEE' : '#080616'}>
                      <rect x="8" y="15" width="44" height="28" rx="6" />
                      <rect x="13" y="20" width="12" height="10" fill={i === 1 ? '#080616' : '#EEEEEE'} />
                      <rect x="35" y="20" width="12" height="10" fill={i === 1 ? '#080616' : '#EEEEEE'} />
                      <circle cx="18" cy="48" r="4" />
                      <circle cx="42" cy="48" r="4" />
                    </g>
                  )}
                  {it.icon === 'nobling' && (
                    <g stroke={i === 1 ? '#EEEEEE' : '#080616'} strokeWidth="4" fill="none">
                      <circle cx="30" cy="30" r="18" />
                      <line x1="17" y1="17" x2="43" y2="43" />
                    </g>
                  )}
                </svg>
                <div
                  style={{
                    fontFamily: 'Noto Sans Devanagari',
                    fontSize: 51,
                    fontWeight: 800,
                  }}
                >
                  {it.text}
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Quote */}
        <div
          style={{
            opacity: quoteOp,
            textAlign: 'center',
            marginTop: 40,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 51,
            fontWeight: 700,
            color: '#080616',
          }}
        >
          देखकर कोई कह नहीं सकता — <span style={{ background: '#080616', color: '#FF61F8', padding: '0 16px' }}>₹70,000 करोड़</span> का मालिक है
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(8,6,22,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_Damani;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15_Premji',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_Premji: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera orbit subtle
  const camRot = Math.sin(frame * 0.01) * 0.5;

  // Tag
  const tagOp = interpolate(sp(5), [0, 1], [0, 1]);

  // Name
  const nameScale = sp(25);

  // Wipro logo card
  const logoOp = interpolate(sp(60), [0, 1], [0, 1]);
  const logoY = interpolate(sp(60), [0, 1], [40, 0]);

  // Plane comparison
  const planeProg = sp(120);

  // Toilet paper card (legendary frugality)
  const tpOp = interpolate(sp(200), [0, 1], [0, 1]);
  const tpScale = sp(200);

  // Bottom impact text
  const impactOp = interpolate(sp(280), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(160deg, rgba(238,238,238,0.15) 0%, rgba(8,6,22,0.25) 100%)',
        }}
      />

      {/* Big triangle shape */}
      <svg width="1920" height="1080" style={{ position: 'absolute' }}>
        <polygon
          points="200,1080 1200,200 1700,1080"
          fill="#080616"
          opacity="0.05"
          transform={`rotate(${frame * 0.05} 1000 600)`}
        />
        <circle cx="1700" cy="200" r="300" fill="#EEEEEE" opacity="0.06" />
      </svg>

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="cross15" width="50" height="50" patternUnits="userSpaceOnUse">
            <line x1="25" y1="20" x2="25" y2="30" stroke="#080616" strokeWidth="2" />
            <line x1="20" y1="25" x2="30" y2="25" stroke="#080616" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross15)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 40;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + seed) * 30,
              top: y + Math.cos(frame * 0.02 + seed) * 25,
              width: 6,
              height: 6,
              background: i % 3 === 0 ? '#080616' : '#EEEEEE',
              opacity: 0.5,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <div style={{ width: '100%', height: '100%', transform: `rotate(${camRot}deg)`, padding: '60px 80px' }}>
        {/* Tag */}
        <div
          style={{
            opacity: tagOp,
            display: 'flex',
            justifyContent: 'center',
            gap: 16,
            marginBottom: 25,
          }}
        >
          <div
            style={{
              background: '#080616',
              color: '#FF61F8',
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 800,
              padding: '10px 24px',
              letterSpacing: 4,
              borderRadius: 8,
            }}
          >
            CASE #03
          </div>
          <div
            style={{
              background: '#EEEEEE',
              color: '#080616',
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              padding: '10px 24px',
              letterSpacing: 2,
              borderRadius: 8,
              border: '3px solid #080616',
            }}
          >
            THE LEGENDARY MISER
          </div>
        </div>

        {/* Name */}
        <div
          style={{
            transform: `scale(${nameScale})`,
            textAlign: 'center',
            fontFamily: 'Yantra One',
            fontSize: 170,
            fontWeight: 900,
            color: '#080616',
            lineHeight: 1,
            letterSpacing: -3,
          }}
        >
          अज़ीम प्रेमजी
        </div>

        {/* Wipro tag */}
        <div
          style={{
            opacity: logoOp,
            transform: `translateY(${logoY}px)`,
            display: 'flex',
            justifyContent: 'center',
            gap: 18,
            marginTop: 18,
          }}
        >
          <div
            style={{
              background: '#080616',
              color: '#EEEEEE',
              fontFamily: 'Inter',
              fontSize: 42,
              fontWeight: 800,
              padding: '12px 30px',
              borderRadius: 10,
            }}
          >
            WIPRO FOUNDER
          </div>
          <div
            style={{
              background: '#EEEEEE',
              color: '#080616',
              fontFamily: 'Yantra One',
              fontSize: 42,
              fontWeight: 900,
              padding: '12px 30px',
              borderRadius: 10,
              border: '3px solid #080616',
            }}
          >
            अरबों के मालिक
          </div>
        </div>

        {/* COMPARISON: Plane classes */}
        <div
          style={{
            display: 'flex',
            justifyContent: 'center',
            gap: 50,
            marginTop: 60,
          }}
        >
          {/* Business class - struck out */}
          <div
            style={{
              opacity: planeProg * 0.7,
              transform: `scale(${planeProg})`,
              width: 480,
              padding: 28,
              background: '#EEEEEE',
              border: '4px dashed #080616',
              borderRadius: 20,
              position: 'relative',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 24,
                fontWeight: 800,
                color: '#080616',
                letterSpacing: 3,
                opacity: 0.6,
              }}
            >
              BUSINESS CLASS
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 60,
                fontWeight: 900,
                color: '#080616',
                opacity: 0.5,
                textDecoration: 'line-through',
              }}
            >
              बिज़नेस क्लास
            </div>
            <svg width="80" height="80" viewBox="0 0 80 80" style={{ marginTop: 10, opacity: 0.5 }}>
              <path d="M10 40 L65 38 L72 42 L65 46 L10 44 Z" fill="#080616" />
              <path d="M30 38 L36 18 L40 18 L38 38 Z" fill="#080616" />
              <path d="M30 46 L36 64 L40 64 L38 46 Z" fill="#080616" />
            </svg>
          </div>

          {/* Economy - chosen */}
          <div
            style={{
              opacity: planeProg,
              transform: `scale(${planeProg})`,
              width: 480,
              padding: 28,
              background: '#080616',
              color: '#EEEEEE',
              borderRadius: 20,
              border: '4px solid #EEEEEE',
              boxShadow: '10px 10px 0 rgba(238,238,238,0.4)',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 24,
                fontWeight: 800,
                color: '#FF61F8',
                letterSpacing: 3,
              }}
            >
              CHOSEN ✓
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 60,
                fontWeight: 900,
              }}
            >
              इकोनॉमी क्लास
            </div>
            <svg width="80" height="80" viewBox="0 0 80 80" style={{ marginTop: 10 }}>
              <path d="M10 40 L65 38 L72 42 L65 46 L10 44 Z" fill="#EEEEEE" />
              <path d="M30 38 L36 18 L40 18 L38 38 Z" fill="#EEEEEE" />
              <path d="M30 46 L36 64 L40 64 L38 46 Z" fill="#EEEEEE" />
            </svg>
          </div>
        </div>

        {/* Toilet paper legendary card */}
        <div
          style={{
            opacity: tpOp,
            transform: `scale(${tpScale})`,
            marginTop: 40,
            width: 1100,
            marginLeft: 'auto',
            marginRight: 'auto',
            background: '#FF61F8',
            border: '5px solid #080616',
            borderRadius: 20,
            padding: '24px 32px',
            display: 'flex',
            alignItems: 'center',
            gap: 30,
            boxShadow: '10px 10px 0 #080616',
          }}
        >
          <svg width="90" height="90" viewBox="0 0 80 80">
            <g fill="#080616" stroke="#080616" strokeWidth="2">
              <ellipse cx="40" cy="20" rx="25" ry="8" fill="#EEEEEE" />
              <rect x="15" y="20" width="50" height="40" fill="#EEEEEE" />
              <ellipse cx="40" cy="60" rx="25" ry="8" />
              <circle cx="40" cy="20" r="6" fill="#FF61F8" />
            </g>
          </svg>
          <div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 24,
                fontWeight: 800,
                color: '#080616',
                letterSpacing: 3,
              }}
            >
              LEGENDARY FRUGALITY
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 51,
                fontWeight: 900,
                color: '#080616',
                lineHeight: 1.1,
              }}
            >
              टॉयलेट पेपर तक नाप-तौलकर
            </div>
          </div>
        </div>

        {/* Impact line */}
        <div
          style={{
            opacity: impactOp,
            textAlign: 'center',
            marginTop: 25,
            fontFamily: 'Inter',
            fontSize: 36,
            fontWeight: 700,
            color: '#080616',
            letterSpacing: 2,
          }}
        >
          BILLIONS IN BANK • SIMPLICITY IN LIFE
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(8,6,22,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_Premji;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16_BuffettHouse',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_BuffettHouse: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Headline
  const headOp = interpolate(sp(8), [0, 1], [0, 1]);
  const headY = interpolate(sp(8), [0, 1], [-60, 0]);

  // House drawing
  const houseOp = interpolate(sp(40), [0, 1], [0, 1]);
  const houseScale = sp(40);

  // Year counter
  const yearProg = interpolate(frame, [90, 150], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });
  const yearStart = 1958;
  const yearNow = 2026;
  const currentYear = Math.floor(yearStart + (yearNow - yearStart) * yearProg);

  // Price
  const priceOp = interpolate(sp(160), [0, 1], [0, 1]);

  // VS divider
  const vsOp = interpolate(sp(200), [0, 1], [0, 1]);

  // Networth count
  const nwProg = interpolate(frame, [240, 320], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });
  const networth = Math.floor(140 * nwProg);

  // Bottom line
  const lineOp = interpolate(sp(360), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 60%, rgba(238,238,238,0.2) 0%, rgba(8,6,22,0.3) 100%)',
        }}
      />

      {/* Big circle shape */}
      <div
        style={{
          position: 'absolute',
          width: 1100,
          height: 1100,
          borderRadius: '50%',
          border: '4px solid #080616',
          opacity: 0.05,
          top: -300,
          right: -300,
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          background: '#EEEEEE',
          opacity: 0.05,
          bottom: -200,
          left: -200,
          borderRadius: 50,
          transform: `rotate(${-frame * 0.1}deg)`,
        }}
      />

      {/* Dots texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots16" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="3" cy="3" r="2" fill="#080616" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots16)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = i + 50;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + seed) * 30,
              top: y + Math.cos(frame * 0.02 + seed) * 25,
              width: 5,
              height: 5,
              background: '#EEEEEE',
              opacity: 0.55,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <div style={{ width: '100%', height: '100%', transform: `scale(${camScale})`, padding: 70 }}>
        {/* Headline */}
        <div
          style={{
            opacity: headOp,
            transform: `translateY(${headY}px)`,
            textAlign: 'center',
            marginBottom: 30,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 800,
              color: '#080616',
              letterSpacing: 5,
              background: '#EEEEEE',
              display: 'inline-block',
              padding: '10px 24px',
              borderRadius: 8,
              border: '3px solid #080616',
            }}
          >
            GLOBAL ICON • दुनिया के सबसे बड़े निवेशक
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 144,
              fontWeight: 900,
              color: '#080616',
              marginTop: 12,
              lineHeight: 1,
              letterSpacing: -3,
            }}
          >
            वॉरेन बफे
          </div>
        </div>

        {/* HOUSE + STATS LAYOUT */}
        <div
          style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 60,
            marginTop: 30,
          }}
        >
          {/* House illustration */}
          <div
            style={{
              opacity: houseOp,
              transform: `scale(${houseScale})`,
              width: 480,
              height: 420,
              background: '#EEEEEE',
              borderRadius: 20,
              border: '5px solid #080616',
              padding: 30,
              boxShadow: '12px 12px 0 #080616',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="280" height="240" viewBox="0 0 200 180">
              {/* Roof */}
              <polygon points="100,20 30,80 170,80" fill="#080616" />
              {/* Body */}
              <rect x="40" y="80" width="120" height="90" fill="#FF61F8" stroke="#080616" strokeWidth="4" />
              {/* Door */}
              <rect x="85" y="115" width="30" height="55" fill="#080616" />
              <circle cx="108" cy="145" r="2" fill="#FF61F8" />
              {/* Windows */}
              <rect x="52" y="95" width="22" height="22" fill="#080616" />
              <rect x="126" y="95" width="22" height="22" fill="#080616" />
              <line x1="63" y1="95" x2="63" y2="117" stroke="#FF61F8" strokeWidth="1.5" />
              <line x1="52" y1="106" x2="74" y2="106" stroke="#FF61F8" strokeWidth="1.5" />
              <line x1="137" y1="95" x2="137" y2="117" stroke="#FF61F8" strokeWidth="1.5" />
              <line x1="126" y1="106" x2="148" y2="106" stroke="#FF61F8" strokeWidth="1.5" />
              {/* Chimney */}
              <rect x="130" y="35" width="14" height="30" fill="#080616" />
            </svg>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 30,
                fontWeight: 700,
                color: '#080616',
                marginTop: 10,
              }}
            >
              ओमाहा, नेब्रास्का
            </div>
          </div>

          {/* Stats column */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 22, width: 700 }}>
            {/* Year */}
            <div
              style={{
                opacity: houseOp,
                background: '#080616',
                color: '#EEEEEE',
                padding: '22px 30px',
                borderRadius: 16,
                border: '4px solid #EEEEEE',
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 24,
                  fontWeight: 800,
                  letterSpacing: 4,
                  color: '#FF61F8',
                }}
              >
                YEAR BOUGHT
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 102,
                  fontWeight: 900,
                  lineHeight: 1,
                  marginTop: 4,
                }}
              >
                {currentYear}
              </div>
            </div>

            {/* Price */}
            <div
              style={{
                opacity: priceOp,
                background: '#EEEEEE',
                color: '#080616',
                padding: '22px 30px',
                borderRadius: 16,
                border: '4px solid #080616',
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 24,
                  fontWeight: 800,
                  letterSpacing: 4,
                }}
              >
                PRICE PAID
              </div>
              <div
                style={{
                  fontFamily: 'Yantra One',
                  fontSize: 81,
                  fontWeight: 900,
                  lineHeight: 1,
                  marginTop: 4,
                }}
              >
                $35,000
              </div>
            </div>

            {/* VS */}
            <div
              style={{
                opacity: vsOp,
                textAlign: 'center',
                fontFamily: 'Yantra One',
                fontSize: 60,
                fontWeight: 900,
                color: '#080616',
                background: '#FF61F8',
                padding: '6px 0',
                borderRadius: 12,
                border: '3px solid #080616',
              }}
            >
              आज की NETWORTH
            </div>

            {/* Networth */}
            <div
              style={{
                opacity: vsOp,
                background: '#080616',
                color: '#FF61F8',
                padding: '24px 30px',
                borderRadius: 16,
                border: '4px solid #EEEEEE',
                textAlign: 'center',
              }}
            >
              <div
                style={{
                  fontFamily: 'Yantra One',
                  fontSize: 119,
                  fontWeight: 900,
                  lineHeight: 1,
                }}
              >
                ${networth}B+
              </div>
              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 30,
                  fontWeight: 700,
                  color: '#EEEEEE',
                  marginTop: 6,
                }}
              >
                = ₹11+ लाख करोड़
              </div>
            </div>
          </div>
        </div>

        {/* Bottom line */}
        <div
          style={{
            opacity: lineOp,
            textAlign: 'center',
            marginTop: 30,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 47,
            fontWeight: 800,
            color: '#080616',
          }}
        >
          68 साल बाद भी — <span style={{ background: '#080616', color: '#FF61F8', padding: '0 16px' }}>वही घर, वही ज़मीन</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(8,6,22,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_BuffettHouse;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17_TwoGames',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_TwoGames: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Quote intro
  const quoteOp = interpolate(sp(8), [0, 1], [0, 1]);
  const quoteScale = sp(8);

  // Split reveal
  const splitProg = interpolate(sp(60), [0, 1], [0, 1]);

  // Left card (game 1)
  const leftX = interpolate(sp(80), [0, 1], [-200, 0]);
  const leftOp = interpolate(sp(80), [0, 1], [0, 1]);

  // Game 1 items
  const g1a = sp(110);
  const g1b = sp(135);
  const g1c = sp(160);

  // VS badge
  const vsScale = sp(200);

  // Right placeholder (next scene continues)
  const rightTease = interpolate(sp(240), [0, 1], [0, 0.4]);

  // Camera zoom out
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(238,238,238,0.15) 0%, rgba(8,6,22,0.25) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          borderRadius: '50%',
          border: '3px solid #EEEEEE',
          opacity: 0.06,
          top: -150,
          left: -150,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          background: '#080616',
          opacity: 0.05,
          bottom: -200,
          right: -100,
          borderRadius: 40,
          transform: `rotate(${-frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />

      {/* Diagonal stripes texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="diag17" width="30" height="30" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="30" stroke="#080616" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag17)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 60;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + seed) * 28,
              top: y + Math.cos(frame * 0.02 + seed) * 25,
              width: 5,
              height: 5,
              background: '#EEEEEE',
              opacity: 0.5,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <div style={{ width: '100%', height: '100%', transform: `scale(${camScale})`, padding: 70 }}>
        {/* Buffett quote tag */}
        <div
          style={{
            opacity: quoteOp,
            transform: `scale(${quoteScale})`,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              background: '#080616',
              color: '#FF61F8',
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 800,
              padding: '12px 30px',
              letterSpacing: 4,
              borderRadius: 10,
              display: 'inline-block',
            }}
          >
            BUFFETT WISDOM • दो खेल
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 110,
              fontWeight: 900,
              color: '#080616',
              marginTop: 15,
              lineHeight: 1,
            }}
          >
            अमीर बनना ≠ अमीर रहना
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              fontWeight: 600,
              color: '#EEEEEE',
              background: '#080616',
              padding: '8px 24px',
              borderRadius: 10,
              display: 'inline-block',
              marginTop: 10,
            }}
          >
            दो बिल्कुल अलग खेल हैं
          </div>
        </div>

        {/* Two-game split */}
        <div
          style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'stretch',
            gap: 30,
            marginTop: 50,
          }}
        >
          {/* GAME 1 — Become Rich */}
          <div
            style={{
              opacity: leftOp,
              transform: `translateX(${leftX}px)`,
              width: 750,
              background: '#EEEEEE',
              color: '#080616',
              borderRadius: 24,
              border: '5px solid #080616',
              padding: 36,
              boxShadow: '12px 12px 0 #080616',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 30,
                fontWeight: 800,
                letterSpacing: 4,
                opacity: 0.7,
              }}
            >
              GAME #01
            </div>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 85,
                fontWeight: 900,
                lineHeight: 1,
                marginTop: 6,
              }}
            >
              अमीर बनना
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 30,
                fontWeight: 700,
                color: '#FF61F8',
                background: '#080616',
                padding: '6px 16px',
                borderRadius: 8,
                display: 'inline-block',
                marginTop: 10,
                letterSpacing: 3,
              }}
            >
              BECOMING RICH
            </div>

            <div style={{ marginTop: 30, display: 'flex', flexDirection: 'column', gap: 14 }}>
              {[
                { p: g1a, icon: 'fire', text: 'हिम्मत' },
                { p: g1b, icon: 'dice', text: 'रिस्क लेने की ताक़त' },
                { p: g1c, icon: 'star', text: 'बड़े सपने' },
              ].map((it, i) => (
                <div
                  key={i}
                  style={{
                    opacity: it.p,
                    transform: `translateX(${interpolate(it.p, [0, 1], [-60, 0])}px)`,
                    display: 'flex',
                    alignItems: 'center',
                    gap: 18,
                    background: '#FF61F8',
                    padding: '14px 20px',
                    borderRadius: 12,
                    border: '3px solid #080616',
                  }}
                >
                  <svg width="60" height="60" viewBox="0 0 60 60">
                    {it.icon === 'fire' && (
                      <path
                        d="M30 10 Q35 20 32 28 Q40 25 42 35 Q48 32 45 45 Q42 55 30 55 Q18 55 15 45 Q12 32 18 35 Q20 25 28 28 Q25 20 30 10 Z"
                        fill="#080616"
                      />
                    )}
                    {it.icon === 'dice' && (
                      <g fill="#080616">
                        <rect x="12" y="12" width="36" height="36" rx="6" />
                        <circle cx="22" cy="22" r="3" fill="#FF61F8" />
                        <circle cx="38" cy="22" r="3" fill="#FF61F8" />
                        <circle cx="30" cy="30" r="3" fill="#FF61F8" />
                        <circle cx="22" cy="38" r="3" fill="#FF61F8" />
                        <circle cx="38" cy="38" r="3" fill="#FF61F8" />
                      </g>
                    )}
                    {it.icon === 'star' && (
                      <polygon
                        points="30,8 36,24 53,24 39,34 44,50 30,40 16,50 21,34 7,24 24,24"
                        fill="#080616"
                      />
                    )}
                  </svg>
                  <div
                    style={{
                      fontFamily: 'Noto Sans Devanagari',
                      fontSize: 60,
                      fontWeight: 900,
                    }}
                  >
                    {it.text}
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* VS BADGE */}
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <div
              style={{
                transform: `scale(${vsScale})`,
                width: 130,
                height: 130,
                borderRadius: '50%',
                background: '#080616',
                color: '#FF61F8',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: 'Yantra One',
                fontSize: 60,
                fontWeight: 900,
                border: '5px solid #EEEEEE',
                boxShadow: '0 0 0 8px #080616',
              }}
            >
              VS
            </div>
          </div>

          {/* GAME 2 placeholder (next scene reveals) */}
          <div
            style={{
              opacity: rightTease,
              width: 750,
              background: 'rgba(8,6,22,0.4)',
              color: '#EEEEEE',
              borderRadius: 24,
              border: '5px dashed #EEEEEE',
              padding: 36,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 30,
                fontWeight: 800,
                letterSpacing: 4,
                color: '#FF61F8',
              }}
            >
              GAME #02
            </div>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 85,
                fontWeight: 900,
                lineHeight: 1,
                marginTop: 6,
                textAlign: 'center',
              }}
            >
              अमीर रहना
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 30,
                fontWeight: 700,
                marginTop: 15,
                opacity: 0.8,
              }}
            >
              COMING UP →
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(8,6,22,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_TwoGames;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18_StayingRich',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_StayingRich: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = interpolate(sp(8), [0, 1], [0, 1]);
  const titleY = interpolate(sp(8), [0, 1], [-60, 0]);

  // Three trait pillars
  const t1 = sp(40);
  const t2 = sp(70);
  const t3 = sp(100);

  // Divider line
  const divW = interpolate(sp(160), [0, 1], [0, 1500]);

  // Bottom message
  const msgOp = interpolate(sp(210), [0, 1], [0, 1]);
  const msgScale = sp(210);

  // Last big quote
  const finalOp = interpolate(sp(290), [0, 1], [0, 1]);

  // Pulse trait icons
  const pulse = 1 + Math.sin(frame * 0.1) * 0.03;

  const traits = [
    { icon: 'shield', en: 'DISCIPLINE', hi: 'अनुशासन', tone: 'light' },
    { icon: 'eye', en: 'CAUTION', hi: 'सावधानी', tone: 'dark' },
    { icon: 'down', en: 'RESPECT FOR FALL', hi: 'गिरने का सम्मान', tone: 'light' },
  ];
  const tp = [t1, t2, t3];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 70%, rgba(238,238,238,0.18) 0%, rgba(8,6,22,0.25) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 1000,
          height: 1000,
          border: '3px solid #080616',
          opacity: 0.05,
          top: -300,
          right: -300,
          borderRadius: 80,
          transform: `rotate(${frame * 0.07}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          background: '#EEEEEE',
          opacity: 0.06,
          bottom: -150,
          left: -150,
          borderRadius: '50%',
          transform: `scale(${1 + Math.cos(frame * 0.03) * 0.05})`,
        }}
      />

      {/* Hex texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="hex18" width="70" height="60" patternUnits="userSpaceOnUse">
            <polygon points="35,5 65,20 65,40 35,55 5,40 5,20" fill="none" stroke="#080616" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex18)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = i + 70;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + seed) * 25,
              top: y + Math.cos(frame * 0.02 + seed) * 25,
              width: 5,
              height: 5,
              background: i % 2 === 0 ? '#EEEEEE' : '#080616',
              opacity: 0.5,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <div style={{ width: '100%', height: '100%', padding: 70 }}>
        {/* Title */}
        <div
          style={{
            opacity: titleOp,
            transform: `translateY(${titleY}px)`,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              background: '#080616',
              color: '#FF61F8',
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 800,
              padding: '10px 28px',
              letterSpacing: 4,
              borderRadius: 10,
              display: 'inline-block',
            }}
          >
            GAME #02 • STAYING RICH
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 119,
              fontWeight: 900,
              color: '#080616',
              marginTop: 12,
              lineHeight: 1,
            }}
          >
            अमीर बने रहने के लिए
          </div>
        </div>

        {/* Three traits */}
        <div
          style={{
            display: 'flex',
            justifyContent: 'center',
            gap: 32,
            marginTop: 50,
          }}
        >
          {traits.map((t, i) => {
            const p = tp[i];
            const dark = t.tone === 'dark';
            return (
              <div
                key={i}
                style={{
                  opacity: p,
                  transform: `translateY(${interpolate(p, [0, 1], [120, 0])}px) scale(${p * pulse})`,
                  width: 400,
                  height: 380,
                  background: dark ? '#080616' : '#EEEEEE',
                  color: dark ? '#EEEEEE' : '#080616',
                  border: '5px solid #080616',
                  borderRadius: 24,
                  padding: 30,
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  justifyContent: 'space-around',
                  boxShadow: dark ? '10px 10px 0 #EEEEEE' : '10px 10px 0 #080616',
                }}
              >
                <svg width="130" height="130" viewBox="0 0 100 100">
                  {t.icon === 'shield' && (
                    <g fill={dark ? '#FF61F8' : '#080616'}>
                      <path d="M50 10 L80 25 L80 55 Q80 80 50 92 Q20 80 20 55 L20 25 Z" />
                      <polyline points="35,52 47,64 68,40" fill="none" stroke={dark ? '#080616' : '#FF61F8'} strokeWidth="6" strokeLinecap="round" strokeLinejoin="round" />
                    </g>
                  )}
                  {t.icon === 'eye' && (
                    <g>
                      <path d="M10 50 Q50 15 90 50 Q50 85 10 50 Z" fill={dark ? '#FF61F8' : '#080616'} />
                      <circle cx="50" cy="50" r="18" fill={dark ? '#080616' : '#EEEEEE'} />
                      <circle cx="50" cy="50" r="9" fill={dark ? '#FF61F8' : '#080616'} />
                    </g>
                  )}
                  {t.icon === 'down' && (
                    <g stroke={dark ? '#FF61F8' : '#080616'} strokeWidth="6" fill="none" strokeLinecap="round">
                      <polyline points="20,25 50,75 80,25" />
                      <line x1="50" y1="75" x2="50" y2="92" />
                    </g>
                  )}
                </svg>
                <div
                  style={{
                    fontFamily: 'JetBrains Mono',
                    fontSize: 26,
                    fontWeight: 800,
                    letterSpacing: 3,
                    opacity: 0.7,
                  }}
                >
                  {t.en}
                </div>
                <div
                  style={{
                    fontFamily: 'Yantra One',
                    fontSize: 68,
                    fontWeight: 900,
                    textAlign: 'center',
                    lineHeight: 1,
                  }}
                >
                  {t.hi}
                </div>
              </div>
            );
          })}
        </div>

        {/* Divider */}
        <div
          style={{
            width: divW,
            height: 5,
            background: '#080616',
            margin: '40px auto 0',
            borderRadius: 3,
          }}
        />

        {/* Message */}
        <div
          style={{
            opacity: msgOp,
            transform: `scale(${msgScale})`,
            textAlign: 'center',
            marginTop: 25,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 55,
            fontWeight: 800,
            color: '#080616',
          }}
        >
          असली अमीर — किसी को कुछ <span style={{ background: '#080616', color: '#FF61F8', padding: '0 18px' }}>साबित नहीं करते</span>
        </div>

        {/* Final */}
        <div
          style={{
            opacity: finalOp,
            textAlign: 'center',
            marginTop: 20,
            fontFamily: 'Inter',
            fontSize: 36,
            fontWeight: 700,
            color: '#EEEEEE',
            background: '#080616',
            padding: '12px 30px',
            borderRadius: 10,
            display: 'inline-block',
            position: 'relative',
            left: '50%',
            transform: `translateX(-50%) scale(${finalOp})`,
            letterSpacing: 2,
          }}
        >
          THEIR PRIORITIES ARE CLEAR
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(8,6,22,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_StayingRich;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19_USAResearch',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_USAResearch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Headline (no validation needed)
  const lineOp = interpolate(sp(5), [0, 1], [0, 1]);
  const lineY = interpolate(sp(5), [0, 1], [-40, 0]);

  // Crossed out cheer
  const cheerOp = interpolate(sp(35), [0, 1], [0, 1]);
  const strikeProg = interpolate(sp(60), [0, 1], [0, 1]);

  // Research badge
  const badgeScale = sp(110);

  // Stat reveal
  const statOp = interpolate(sp(150), [0, 1], [0, 1]);
  const incomeCount = Math.floor(interpolate(frame, [150, 220], [0, 250], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' }));

  // Question hook
  const qOp = interpolate(sp(250), [0, 1], [0, 1]);

  // Camera subtle zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(170deg, rgba(238,238,238,0.16) 0%, rgba(8,6,22,0.3) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 900,
          height: 900,
          borderRadius: '50%',
          border: '4px solid #EEEEEE',
          opacity: 0.06,
          top: -250,
          left: -250,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          background: '#080616',
          opacity: 0.05,
          bottom: -250,
          right: -200,
          transform: `rotate(${-frame * 0.08}deg)`,
        }}
      />

      {/* Bar chart grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="bars19" width="80" height="80" patternUnits="userSpaceOnUse">
            <line x1="0" y1="80" x2="80" y2="80" stroke="#080616" strokeWidth="1" />
            <line x1="80" y1="0" x2="80" y2="80" stroke="#080616" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#bars19)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 80;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + seed) * 28,
              top: y + Math.cos(frame * 0.02 + seed) * 25,
              width: 5,
              height: 5,
              background: '#EEEEEE',
              opacity: 0.55,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <div style={{ width: '100%', height: '100%', transform: `scale(${camScale})`, padding: 70 }}>
        {/* HEAD */}
        <div
          style={{
            opacity: lineOp,
            transform: `translateY(${lineY}px)`,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 60,
              fontWeight: 800,
              color: '#080616',
            }}
          >
            उन्हें पता है — <span style={{ background: '#080616', color: '#FF61F8', padding: '0 14px' }}>क्या ज़रूरी है</span>
          </div>
        </div>

        {/* Crossed out */}
        <div
          style={{
            opacity: cheerOp,
            textAlign: 'center',
            marginTop: 20,
            position: 'relative',
            display: 'inline-block',
            left: '50%',
            transform: 'translateX(-50%)',
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 81,
              fontWeight: 900,
              color: '#080616',
              background: '#EEEEEE',
              padding: '8px 32px',
              borderRadius: 14,
              border: '4px solid #080616',
              position: 'relative',
              display: 'inline-block',
            }}
          >
            मोहल्ले की वाह-वाही
            <div
              style={{
                position: 'absolute',
                left: 20,
                right: 20,
                top: '50%',
                height: 8,
                background: '#FF61F8',
                border: '2px solid #080616',
                transform: `scaleX(${strikeProg})`,
                transformOrigin: 'left',
              }}
            />
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 700,
              color: '#EEEEEE',
              background: '#080616',
              padding: '8px 24px',
              borderRadius: 10,
              marginTop: 10,
              letterSpacing: 3,
              display: 'inline-block',
            }}
          >
            NOT NEEDED
          </div>
        </div>

        {/* RESEARCH BADGE */}
        <div
          style={{
            transform: `scale(${badgeScale})`,
            marginTop: 40,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 30,
          }}
        >
          <div
            style={{
              background: '#080616',
              color: '#FF61F8',
              fontFamily: 'JetBrains Mono',
              fontSize: 32,
              fontWeight: 800,
              padding: '14px 30px',
              letterSpacing: 4,
              borderRadius: 10,
              border: '4px solid #EEEEEE',
            }}
          >
            🇺🇸 USA RESEARCH
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 45,
              fontWeight: 800,
              color: '#080616',
            }}
          >
            एक दिलचस्प स्टडी →
          </div>
        </div>

        {/* STAT CARD */}
        <div
          style={{
            opacity: statOp,
            marginTop: 40,
            width: 1500,
            marginLeft: 'auto',
            marginRight: 'auto',
            background: '#EEEEEE',
            border: '6px solid #080616',
            borderRadius: 28,
            padding: 40,
            boxShadow: '14px 14px 0 #080616',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 800,
              color: '#080616',
              letterSpacing: 4,
              opacity: 0.7,
            }}
          >
            ANNUAL INCOME BRACKET
          </div>
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 30,
              marginTop: 10,
            }}
          >
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 195,
                fontWeight: 900,
                color: '#080616',
                lineHeight: 1,
              }}
            >
              ${incomeCount}K+
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 47,
                fontWeight: 800,
                color: '#080616',
              }}
            >
              सालाना कमाने वाले
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 32,
                  color: '#EEEEEE',
                  background: '#080616',
                  padding: '4px 16px',
                  borderRadius: 8,
                  marginTop: 10,
                  letterSpacing: 2,
                  display: 'inline-block',
                }}
              >
                ≈ ₹2 करोड़+ / year
              </div>
            </div>
          </div>
        </div>

        {/* Question hook */}
        <div
          style={{
            opacity: qOp,
            textAlign: 'center',
            marginTop: 30,
            fontFamily: 'Yantra One',
            fontSize: 68,
            fontWeight: 900,
            color: '#EEEEEE',
            textShadow: '4px 4px 0 #080616',
          }}
        >
          वो लोग कौन सी कार चलाते हैं?
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(8,6,22,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_USAResearch;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20_61Percent',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20_61Percent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  // Big % count up
  const pctProg = interpolate(frame, [10, 100], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });
  const pct = Math.floor(61 * pctProg);

  // Donut chart progress
  const donutProg = interpolate(frame, [10, 100], [0, 0.61], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });

  // Choose group reveal
  const c1 = sp(120);
  const c2 = sp(145);
  const c3 = sp(170);

  // Reject group reveal
  const r1 = sp(220);
  const r2 = sp(245);

  // Final big takeaway
  const finalOp = interpolate(sp(310), [0, 1], [0, 1]);
  const finalScale = sp(310);

  // Shake on final
  const shake = frame > 320 && frame < 345 ? Math.sin(frame * 1.8) * 4 : 0;

  // Donut math
  const radius = 130;
  const circumference = 2 * Math.PI * radius;
  const dashOffset = circumference * (1 - donutProg);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF61F8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 60% 40%, rgba(238,238,238,0.18) 0%, rgba(8,6,22,0.3) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 900,
          height: 900,
          borderRadius: 60,
          border: '3px solid #080616',
          opacity: 0.05,
          bottom: -250,
          left: -250,
          transform: `rotate(${frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          background: '#EEEEEE',
          opacity: 0.05,
          top: -200,
          right: -150,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="g20" width="60" height="60" patternUnits="userSpaceOnUse">
            <circle cx="30" cy="30" r="1.5" fill="#080616" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#g20)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const seed = i + 90;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + seed) * 28,
              top: y + Math.cos(frame * 0.02 + seed) * 25,
              width: 5,
              height: 5,
              background: i % 2 === 0 ? '#EEEEEE' : '#080616',
              opacity: 0.55,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <div style={{ width: '100%', height: '100%', transform: `scale(${camScale})`, padding: 60 }}>
        {/* LEFT — Donut + % */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 80,
            width: 560,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              background: '#080616',
              color: '#FF61F8',
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 800,
              padding: '10px 22px',
              letterSpacing: 4,
              borderRadius: 8,
              marginBottom: 20,
            }}
          >
            THE FINDING
          </div>

          {/* Donut */}
          <svg width="380" height="380" viewBox="0 0 320 320">
            <circle cx="160" cy="160" r={radius} fill="none" stroke="#EEEEEE" strokeWidth="40" opacity="0.6" />
            <circle
              cx="160"
              cy="160"
              r={radius}
              fill="none"
              stroke="#080616"
              strokeWidth="40"
              strokeDasharray={circumference}
              strokeDashoffset={dashOffset}
              strokeLinecap="round"
              transform="rotate(-90 160 160)"
            />
            <text
              x="160"
              y="155"
              textAnchor="middle"
              fontFamily="Yantra One"
              fontSize="110"
              fontWeight="900"
              fill="#080616"
            >
              {pct}%
            </text>
            <text
              x="160"
              y="200"
              textAnchor="middle"
              fontFamily="JetBrains Mono"
              fontSize="22"
              fontWeight="700"
              fill="#080616"
              letterSpacing="3"
            >
              OF THE RICH
            </text>
          </svg>

          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 45,
              fontWeight: 800,
              color: '#080616',
              textAlign: 'center',
              marginTop: 10,
              lineHeight: 1.1,
            }}
          >
            61% अमीर लोग चलाते हैं<br />
            <span style={{ background: '#080616', color: '#FF61F8', padding: '2px 14px' }}>आम कारें</span>
          </div>
        </div>

        {/* RIGHT — Brand columns */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            right: 80,
            width: 1000,
          }}
        >
          {/* CHOSEN */}
          <div
            style={{
              background: '#EEEEEE',
              border: '5px solid #080616',
              borderRadius: 20,
              padding: 24,
              boxShadow: '10px 10px 0 #080616',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 26,
                fontWeight: 800,
                color: '#080616',
                letterSpacing: 4,
                marginBottom: 14,
              }}
            >
              ✓ DRIVEN BY RICH
            </div>
            <div style={{ display: 'flex', gap: 14 }}>
              {[
                { p: c1, name: 'HONDA' },
                { p: c2, name: 'TOYOTA' },
                { p: c3, name: 'FORD' },
              ].map((b, i) => (
                <div
                  key={i}
                  style={{
                    opacity: b.p,
                    transform: `scale(${b.p})`,
                    flex: 1,
                    background: '#080616',
                    color: '#FF61F8',
                    padding: '20px 14px',
                    borderRadius: 14,
                    textAlign: 'center',
                    border: '4px solid #FF61F8',
                  }}
                >
                  <svg width="60" height="40" viewBox="0 0 60 40" style={{ marginBottom: 6 }}>
                    <g fill="#FF61F8">
                      <path d="M5 25 L10 16 L20 14 L40 14 L50 18 L55 25 L55 30 L5 30 Z" />
                      <circle cx="15" cy="32" r="4" fill="#080616" stroke="#FF61F8" strokeWidth="2" />
                      <circle cx="45" cy="32" r="4" fill="#080616" stroke="#FF61F8" strokeWidth="2" />
                    </g>
                  </svg>
                  <div
                    style={{
                      fontFamily: 'Inter',
                      fontSize: 38,
                      fontWeight: 900,
                      letterSpacing: 2,
                    }}
                  >
                    {b.name}
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* REJECTED */}
          <div
            style={{
              background: '#080616',
              border: '5px solid #EEEEEE',
              borderRadius: 20,
              padding: 24,
              marginTop: 20,
              boxShadow: '10px 10px 0 #EEEEEE',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 26,
                fontWeight: 800,
                color: '#FF61F8',
                letterSpacing: 4,
                marginBottom: 14,
              }}
            >
              ✗ NOT THIS
            </div>
            <div style={{ display: 'flex', gap: 14 }}>
              {[
                { p: r1, name: 'BMW' },
                { p: r2, name: 'MERCEDES' },
              ].map((b, i) => (
                <div
                  key={i}
                  style={{
                    opacity: b.p * 0.7,
                    transform: `scale(${b.p})`,
                    flex: 1,
                    background: '#EEEEEE',
                    color: '#080616',
                    padding: '20px 14px',
                    borderRadius: 14,
                    textAlign: 'center',
                    border: '4px dashed #080616',
                    position: 'relative',
                  }}
                >
                  <div
                    style={{
                      fontFamily: 'Inter',
                      fontSize: 47,
                      fontWeight: 900,
                      letterSpacing: 2,
                      textDecoration: 'line-through',
                    }}
                  >
                    {b.name}
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* FINAL LINE */}
          <div
            style={{
              opacity: finalOp,
              transform: `scale(${finalScale}) translateX(${shake}px)`,
              marginTop: 26,
              background: '#FF61F8',
              border: '5px solid #080616',
              borderRadius: 18,
              padding: '20px 28px',
              boxShadow: '8px 8px 0 #080616',
            }}
          >
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 47,
                fontWeight: 900,
                color: '#080616',
                lineHeight: 1.15,
              }}
            >
              असली खेल — <span style={{ background: '#080616', color: '#FF61F8', padding: '0 14px' }}>बैंक बैलेंस</span>
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 32,
                fontWeight: 700,
                color: '#080616',
                marginTop: 6,
                letterSpacing: 1,
              }}
            >
              बाहर की चमक का नहीं
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(8,6,22,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#080616',
          opacity: 0.5,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene20_61Percent;
```

