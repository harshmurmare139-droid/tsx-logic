```tsx
import React from 'react';
Audio: "To understand how to spot a fake, you have to understand the golden rule of IRS contact. The Real Internal Revenue Service is a massive, slow, bureaucratic government agency."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11_GoldenRule',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_GoldenRule: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  // Title reveal
  const titleY = interpolate(sp(10), [0, 1], [80, 0]);
  const titleOp = interpolate(sp(10), [0, 1], [0, 1]);

  // GOLDEN RULE big text
  const goldenScale = sp(35, { damping: 10, stiffness: 140 });
  const goldenOp = interpolate(sp(35), [0, 1], [0, 1]);

  // Underline
  const underlineW = interpolate(sp(55), [0, 1], [0, 900]);

  // Subtitle
  const subOp = interpolate(sp(70), [0, 1], [0, 1]);
  const subY = interpolate(sp(70), [0, 1], [30, 0]);

  // Stamp impact
  const stampScale = interpolate(sp(90, { damping: 8, stiffness: 180 }), [0, 1], [3, 1]);
  const stampOp = interpolate(sp(90), [0, 1], [0, 1]);
  const stampRot = interpolate(sp(90), [0, 1], [-25, -12]);

  // Floating particles
  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 2) * 1080,
    s: 4 + seededRandom(i + 3) * 8,
    sp: 0.3 + seededRandom(i + 4) * 0.6,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(254, 236, 65, 0.18) 0%, transparent 55%), radial-gradient(circle at 75% 70%, rgba(0, 0, 0, 0.08) 0%, transparent 50%)',
      }} />

      {/* Animated shape layer 1 - large circle */}
      <div style={{
        position: 'absolute',
        width: 700,
        height: 700,
        borderRadius: '50%',
        border: '3px solid #000000',
        opacity: 0.05,
        top: -150,
        right: -150,
        transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />

      {/* Shape layer 2 - rectangle */}
      <div style={{
        position: 'absolute',
        width: 500,
        height: 500,
        backgroundColor: '#FEEC41',
        opacity: 0.06,
        bottom: -100,
        left: -100,
        transform: `rotate(${-frame * 0.15 + 15}deg)`,
      }} />

      {/* Shape layer 3 - triangle */}
      <svg style={{ position: 'absolute', top: '40%', left: '5%', opacity: 0.04 }} width="300" height="300">
        <polygon points="150,20 280,260 20,260" fill="#000000" transform={`rotate(${frame * 0.3} 150 150)`} />
      </svg>

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="grid11" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid11)" />
      </svg>

      {/* Floating particles */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x,
          top: (p.y + frame * p.sp) % 1080,
          width: p.s,
          height: p.s,
          borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? '#FEEC41' : '#000000',
          opacity: 0.4,
        }} />
      ))}

      {/* Main content with camera scale */}
      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        transform: `scale(${camScale})`,
      }}>
        {/* Top tag */}
        <div style={{
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 20,
          marginBottom: 30,
        }}>
          <div style={{ width: 60, height: 4, backgroundColor: '#000000' }} />
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 700,
            letterSpacing: 8,
            color: '#000000',
            textTransform: 'uppercase',
          }}>
            Rule of Rules
          </div>
          <div style={{ width: 60, height: 4, backgroundColor: '#000000' }} />
        </div>

        {/* GOLDEN RULE - mega text */}
        <div style={{
          opacity: goldenOp,
          transform: `scale(${goldenScale})`,
          fontFamily: 'Inter, Poppins',
          fontSize: 221,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -6,
          lineHeight: 0.9,
          textShadow: '8px 8px 0px #FEEC41',
        }}>
          GOLDEN RULE
        </div>

        {/* Underline */}
        <div style={{
          width: underlineW,
          height: 8,
          backgroundColor: '#FEEC41',
          marginTop: 20,
          boxShadow: '0 4px 0 #000000',
        }} />

        {/* Subtitle */}
        <div style={{
          opacity: subOp,
          transform: `translateY(${subY}px)`,
          marginTop: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 55,
          fontWeight: 600,
          color: '#000000',
          textAlign: 'center',
          maxWidth: 1300,
          lineHeight: 1.2,
        }}>
          IRS = <span style={{ backgroundColor: '#FEEC41', padding: '4px 18px' }}>Slow</span> + <span style={{ backgroundColor: '#000000', color: '#FEEC41', padding: '4px 18px' }}>Bureaucratic</span>
        </div>
      </div>

      {/* Stamp visual element */}
      <div style={{
        position: 'absolute',
        top: 120,
        right: 140,
        opacity: stampOp,
        transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
      }}>
        <div style={{
          border: '6px solid #000000',
          padding: '18px 36px',
          fontFamily: 'Inter, Poppins',
          fontSize: 42,
          fontWeight: 900,
          color: '#000000',
          backgroundColor: '#FEEC41',
          letterSpacing: 3,
        }}>
          OFFICIAL
        </div>
      </div>

      {/* Bureaucratic icon - stacked papers SVG */}
      <div style={{
        position: 'absolute',
        bottom: 140,
        left: 120,
        opacity: interpolate(sp(60), [0, 1], [0, 0.9]),
        transform: `translateY(${Math.sin(frame * 0.04) * 6}px)`,
      }}>
        <svg width="180" height="180" viewBox="0 0 180 180">
          <rect x="30" y="40" width="120" height="140" fill="#000000" opacity="0.25" />
          <rect x="20" y="30" width="120" height="140" fill="#000000" opacity="0.45" />
          <rect x="10" y="20" width="120" height="140" fill="#FEEC41" stroke="#000000" strokeWidth="4" />
          <line x1="25" y1="50" x2="115" y2="50" stroke="#000000" strokeWidth="3" />
          <line x1="25" y1="75" x2="115" y2="75" stroke="#000000" strokeWidth="3" />
          <line x1="25" y1="100" x2="115" y2="100" stroke="#000000" strokeWidth="3" />
          <line x1="25" y1="125" x2="90" y2="125" stroke="#000000" strokeWidth="3" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_GoldenRule;
```

```tsx
import React from 'react';
Audio: "They do things by the book. And the book says that all initial contact with a taxpayer must be made through the United States Postal Service."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12_ByTheBook',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_ByTheBook: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Book opening
  const bookScale = sp(15);
  const bookOp = interpolate(sp(15), [0, 1], [0, 1]);
  const pageFlip = interpolate(sp(45), [0, 1], [0, 1]);

  // Label reveals
  const label1Op = interpolate(sp(60), [0, 1], [0, 1]);
  const label1X = interpolate(sp(60), [0, 1], [-100, 0]);

  // Arrow
  const arrowOp = interpolate(sp(90), [0, 1], [0, 1]);
  const arrowX = interpolate(sp(90), [0, 1], [-50, 0]);

  // Envelope
  const envScale = sp(115, { damping: 10, stiffness: 140 });
  const envOp = interpolate(sp(115), [0, 1], [0, 1]);

  // USPS stamp
  const stampScale = interpolate(sp(150, { damping: 7, stiffness: 200 }), [0, 1], [4, 1]);
  const stampOp = interpolate(sp(150), [0, 1], [0, 1]);
  const stampRot = interpolate(sp(150), [0, 1], [-30, -8]);

  // Caption
  const captionOp = interpolate(sp(180), [0, 1], [0, 1]);
  const captionY = interpolate(sp(180), [0, 1], [40, 0]);

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-15, 15], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(254, 236, 65, 0.2) 0%, transparent 60%)',
      }} />

      {/* Drifting shapes */}
      <div style={{
        position: 'absolute',
        width: 600,
        height: 600,
        borderRadius: '50%',
        backgroundColor: '#FEEC41',
        opacity: 0.07,
        top: '20%',
        left: '-10%',
        transform: `translateY(${Math.sin(frame * 0.02) * 30}px)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 400,
        height: 400,
        backgroundColor: '#000000',
        opacity: 0.04,
        bottom: '-10%',
        right: '5%',
        transform: `rotate(${frame * 0.25}deg)`,
      }} />

      {/* Diagonal line texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="lines12" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 0 40 L 40 0" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#lines12)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const x = seededRandom(i + 1) * 1920;
        const y = (seededRandom(i + 5) * 1080 + frame * (0.4 + seededRandom(i + 7) * 0.5)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 6,
            height: 6,
            backgroundColor: '#000000',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Top headline */}
      <div style={{
        position: 'absolute',
        top: 100,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: interpolate(sp(0), [0, 1], [0, 1]),
        transform: `translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px) translateX(${camX}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 110,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -3,
          lineHeight: 1,
        }}>
          BY <span style={{
            backgroundColor: '#FEEC41',
            padding: '0 24px',
            display: 'inline-block',
            transform: `rotate(${Math.sin(frame * 0.05) * 1}deg)`,
          }}>THE BOOK</span>
        </div>
      </div>

      {/* Main book SVG */}
      <div style={{
        position: 'absolute',
        left: '8%',
        top: '40%',
        transform: `translateX(${camX}px) scale(${bookScale}) translateY(${Math.sin(frame * 0.04) * 5}px)`,
        opacity: bookOp,
      }}>
        <svg width="500" height="380" viewBox="0 0 500 380">
          {/* Book shadow */}
          <ellipse cx="250" cy="365" rx="220" ry="12" fill="#000000" opacity="0.2" />
          {/* Left page */}
          <rect x="40" y="40" width="210" height="300" fill="#F5F5F5" stroke="#000000" strokeWidth="5" />
          {/* Right page - flipping */}
          <g style={{ transform: `rotateY(${pageFlip * 20}deg)`, transformOrigin: '250px 190px' }}>
            <rect x="250" y="40" width="210" height="300" fill="#FEEC41" stroke="#000000" strokeWidth="5" />
            <line x1="270" y1="80" x2="440" y2="80" stroke="#000000" strokeWidth="3" />
            <line x1="270" y1="120" x2="440" y2="120" stroke="#000000" strokeWidth="3" />
            <line x1="270" y1="160" x2="420" y2="160" stroke="#000000" strokeWidth="3" />
            <line x1="270" y1="200" x2="440" y2="200" stroke="#000000" strokeWidth="3" />
            <line x1="270" y1="240" x2="400" y2="240" stroke="#000000" strokeWidth="3" />
            <line x1="270" y1="280" x2="440" y2="280" stroke="#000000" strokeWidth="3" />
          </g>
          {/* Spine */}
          <rect x="245" y="40" width="10" height="300" fill="#000000" />
          {/* Left page lines */}
          <line x1="60" y1="80" x2="230" y2="80" stroke="#000000" strokeWidth="3" />
          <line x1="60" y1="120" x2="230" y2="120" stroke="#000000" strokeWidth="3" />
          <line x1="60" y1="160" x2="210" y2="160" stroke="#000000" strokeWidth="3" />
          <line x1="60" y1="200" x2="230" y2="200" stroke="#000000" strokeWidth="3" />
        </svg>
      </div>

      {/* Arrow connecting */}
      <div style={{
        position: 'absolute',
        left: '40%',
        top: '55%',
        opacity: arrowOp,
        transform: `translateX(${arrowX}px) translateX(${camX}px)`,
      }}>
        <svg width="220" height="80" viewBox="0 0 220 80">
          <line x1="10" y1="40" x2="180" y2="40" stroke="#000000" strokeWidth="8" strokeLinecap="round" />
          <polygon points="180,20 215,40 180,60" fill="#000000" />
        </svg>
      </div>

      {/* Envelope (USPS mail) */}
      <div style={{
        position: 'absolute',
        right: '10%',
        top: '40%',
        opacity: envOp,
        transform: `translateX(${camX}px) scale(${envScale}) translateY(${Math.sin(frame * 0.05 + 1) * 6}px)`,
      }}>
        <svg width="460" height="320" viewBox="0 0 460 320">
          {/* shadow */}
          <ellipse cx="230" cy="305" rx="200" ry="10" fill="#000000" opacity="0.2" />
          {/* envelope body */}
          <rect x="30" y="50" width="400" height="240" fill="#F5F5F5" stroke="#000000" strokeWidth="6" />
          {/* flap */}
          <polygon points="30,50 430,50 230,200" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
          {/* address lines */}
          <line x1="70" y1="230" x2="280" y2="230" stroke="#000000" strokeWidth="4" />
          <line x1="70" y1="255" x2="240" y2="255" stroke="#000000" strokeWidth="4" />
          {/* IRS logo placeholder */}
          <rect x="320" y="220" width="80" height="50" fill="none" stroke="#000000" strokeWidth="4" />
          <text x="360" y="252" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#000000">IRS</text>
        </svg>
      </div>

      {/* USPS Stamp */}
      <div style={{
        position: 'absolute',
        right: '6%',
        top: '32%',
        opacity: stampOp,
        transform: `rotate(${stampRot}deg) scale(${stampScale})`,
        transformOrigin: 'center',
      }}>
        <div style={{
          border: '5px dashed #000000',
          padding: '14px 22px',
          backgroundColor: '#FEEC41',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 30,
          color: '#000000',
          letterSpacing: 2,
          textAlign: 'center',
        }}>
          USPS<br />
          <span style={{ fontSize: 18, letterSpacing: 1 }}>ONLY</span>
        </div>
      </div>

      {/* Labels */}
      <div style={{
        position: 'absolute',
        left: '11%',
        top: '78%',
        opacity: label1Op,
        transform: `translateX(${label1X}px) translateX(${camX}px)`,
        fontFamily: 'Inter, Poppins',
        fontSize: 38,
        fontWeight: 800,
        color: '#000000',
      }}>
        THE RULE BOOK
      </div>

      <div style={{
        position: 'absolute',
        right: '12%',
        top: '78%',
        opacity: envOp,
        transform: `translateX(${camX}px)`,
        fontFamily: 'Inter, Poppins',
        fontSize: 38,
        fontWeight: 800,
        color: '#000000',
      }}>
        PHYSICAL LETTER
      </div>

      {/* Bottom caption */}
      <div style={{
        position: 'absolute',
        bottom: 120,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: captionOp,
        transform: `translateY(${captionY}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          color: '#FEEC41',
          fontFamily: 'Inter, Poppins',
          fontSize: 50,
          fontWeight: 800,
          padding: '14px 40px',
          letterSpacing: 1,
        }}>
          Initial Contact = Mail. Period.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_ByTheBook;
```

```tsx
import React from 'react';
Audio: "The IRS will never call you out of the blue to demand money. They will always start by mailing you a formal, physical letter to your home address."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13_NeverCall',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_NeverCall: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Phone shake intensity
  const phoneShake = Math.sin(frame * 0.8) * 8 * Math.max(0, 1 - frame / 90);
  const phoneScale = sp(5);
  const phoneOp = interpolate(sp(5), [0, 1], [0, 1]);

  // X mark slam
  const xScale = interpolate(sp(45, { damping: 6, stiffness: 200 }), [0, 1], [5, 1]);
  const xOp = interpolate(sp(45), [0, 1], [0, 1]);
  const xRot = interpolate(sp(45), [0, 1], [45, 0]);

  // NEVER text
  const neverY = interpolate(sp(75), [0, 1], [60, 0]);
  const neverOp = interpolate(sp(75), [0, 1], [0, 1]);

  // VS line
  const vsScale = sp(100);
  const vsOp = interpolate(sp(100), [0, 1], [0, 1]);

  // Letter (right side)
  const letterX = interpolate(sp(125), [0, 1], [200, 0]);
  const letterOp = interpolate(sp(125), [0, 1], [0, 1]);

  // Checkmark
  const checkScale = interpolate(sp(160, { damping: 8, stiffness: 180 }), [0, 1], [4, 1]);
  const checkOp = interpolate(sp(160), [0, 1], [0, 1]);

  // Caption
  const capOp = interpolate(sp(195), [0, 1], [0, 1]);

  // Screen flash on impact at frame 45
  const flash = interpolate(frame, [45, 50, 60], [0, 0.4, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(254, 236, 65, 0.15) 0%, transparent 50%, rgba(0,0,0,0.06) 100%)',
      }} />

      {/* Flash overlay */}
      <AbsoluteFill style={{ backgroundColor: '#FEEC41', opacity: flash }} />

      {/* Drifting shapes */}
      <div style={{
        position: 'absolute',
        width: 500,
        height: 500,
        backgroundColor: '#000000',
        opacity: 0.05,
        top: -100,
        left: '30%',
        transform: `rotate(${frame * 0.2 + 25}deg)`,
        borderRadius: 40,
      }} />
      <svg style={{ position: 'absolute', bottom: -200, right: -150, opacity: 0.06 }} width="500" height="500">
        <polygon points="250,30 470,470 30,470" fill="#FEEC41" transform={`rotate(${-frame * 0.15} 250 250)`} />
      </svg>

      {/* Dot grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="1920" height="1080">
        <defs>
          <pattern id="dots13" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots13)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const x = (seededRandom(i + 11) * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(i + 22) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            backgroundColor: '#FEEC41',
            opacity: 0.5,
            borderRadius: '50%',
            transform: `translateY(${Math.sin(frame * 0.04 + i) * 12}px)`,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: interpolate(sp(0), [0, 1], [0, 1]),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 36,
          fontWeight: 700,
          letterSpacing: 12,
          color: '#000000',
          textTransform: 'uppercase',
        }}>
          Out of the Blue Calls?
        </div>
      </div>

      {/* LEFT SIDE - Phone with X */}
      <div style={{
        position: 'absolute',
        left: '12%',
        top: '38%',
        opacity: phoneOp,
        transform: `scale(${phoneScale}) translate(${phoneShake}px, ${phoneShake * 0.6}px)`,
      }}>
        <svg width="320" height="420" viewBox="0 0 320 420">
          {/* phone body */}
          <rect x="40" y="20" width="240" height="380" rx="30" fill="#000000" />
          <rect x="55" y="60" width="210" height="320" fill="#FEEC41" />
          {/* incoming call UI */}
          <circle cx="160" cy="160" r="40" fill="#000000" />
          <text x="160" y="172" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="900" fill="#FEEC41">!</text>
          <rect x="80" y="220" width="160" height="6" fill="#000000" />
          <rect x="80" y="240" width="120" height="6" fill="#000000" />
          {/* speaker */}
          <rect x="140" y="35" width="40" height="6" rx="3" fill="#FEEC41" />
        </svg>
      </div>

      {/* Big X over phone */}
      <div style={{
        position: 'absolute',
        left: '10%',
        top: '36%',
        opacity: xOp,
        transform: `scale(${xScale}) rotate(${xRot}deg)`,
        transformOrigin: 'center',
      }}>
        <svg width="380" height="460" viewBox="0 0 380 460">
          <line x1="40" y1="40" x2="340" y2="420" stroke="#000000" strokeWidth="32" strokeLinecap="round" />
          <line x1="340" y1="40" x2="40" y2="420" stroke="#000000" strokeWidth="32" strokeLinecap="round" />
        </svg>
      </div>

      {/* NEVER label */}
      <div style={{
        position: 'absolute',
        left: '8%',
        bottom: 200,
        opacity: neverOp,
        transform: `translateY(${neverY}px)`,
      }}>
        <div style={{
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '12px 36px',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 68,
          letterSpacing: 2,
          display: 'inline-block',
          transform: `rotate(${Math.sin(frame * 0.06) * 2}deg)`,
        }}>
          NEVER CALLS
        </div>
      </div>

      {/* CENTER - VS divider */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '40%',
        transform: `translateX(-50%) scale(${vsOp})`,
        opacity: vsOp,
      }}>
        <div style={{
          width: 6,
          height: 380,
          backgroundColor: '#000000',
          margin: '0 auto',
        }} />
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 60,
          fontWeight: 900,
          color: '#000000',
          textAlign: 'center',
          marginTop: -210,
          backgroundColor: '#EEE3CB',
          padding: '0 14px',
          width: 'fit-content',
          marginLeft: 'auto',
          marginRight: 'auto',
        }}>
          VS
        </div>
      </div>

      {/* RIGHT SIDE - Letter */}
      <div style={{
        position: 'absolute',
        right: '12%',
        top: '36%',
        opacity: letterOp,
        transform: `translateX(${letterX}px) translateY(${Math.sin(frame * 0.04) * 5}px)`,
      }}>
        <svg width="380" height="460" viewBox="0 0 380 460">
          <rect x="30" y="40" width="320" height="400" fill="#F5F5F5" stroke="#000000" strokeWidth="6" />
          {/* IRS header */}
          <rect x="30" y="40" width="320" height="70" fill="#000000" />
          <text x="190" y="88" textAnchor="middle" fontFamily="Inter" fontSize="38" fontWeight="900" fill="#FEEC41">IRS</text>
          {/* address lines */}
          <line x1="60" y1="150" x2="320" y2="150" stroke="#000000" strokeWidth="4" />
          <line x1="60" y1="180" x2="270" y2="180" stroke="#000000" strokeWidth="4" />
          {/* content */}
          <line x1="60" y1="230" x2="320" y2="230" stroke="#000000" strokeWidth="3" />
          <line x1="60" y1="260" x2="320" y2="260" stroke="#000000" strokeWidth="3" />
          <line x1="60" y1="290" x2="290" y2="290" stroke="#000000" strokeWidth="3" />
          <line x1="60" y1="320" x2="320" y2="320" stroke="#000000" strokeWidth="3" />
          {/* yellow stamp */}
          <rect x="240" y="370" width="90" height="50" fill="#FEEC41" stroke="#000000" strokeWidth="4" />
          <text x="285" y="402" textAnchor="middle" fontFamily="Inter" fontSize="16" fontWeight="900" fill="#000000">CP-14</text>
        </svg>
      </div>

      {/* Big Checkmark */}
      <div style={{
        position: 'absolute',
        right: '8%',
        top: '34%',
        opacity: checkOp,
        transform: `scale(${checkScale})`,
        transformOrigin: 'center',
      }}>
        <svg width="200" height="200" viewBox="0 0 200 200">
          <circle cx="100" cy="100" r="80" fill="#FEEC41" stroke="#000000" strokeWidth="8" />
          <path d="M 55 100 L 90 135 L 150 65" fill="none" stroke="#000000" strokeWidth="14" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>

      {/* ALWAYS label */}
      <div style={{
        position: 'absolute',
        right: '8%',
        bottom: 200,
        opacity: letterOp,
      }}>
        <div style={{
          backgroundColor: '#FEEC41',
          color: '#000000',
          padding: '12px 36px',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 68,
          letterSpacing: 2,
          border: '5px solid #000000',
          display: 'inline-block',
          transform: `rotate(${Math.sin(frame * 0.06 + 1) * 2}deg)`,
        }}>
          ALWAYS MAILS
        </div>
      </div>

      {/* Bottom caption */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: capOp,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 42,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 1,
        }}>
          Formal letter → Home address → Real IRS protocol
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_NeverCall;
```

```tsx
import React from 'react';
Audio: "If you have never received a piece of paper in the mail from the IRS, but suddenly you are getting aggressive phone calls, you are talking to a criminal. It is that simple."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14_Criminal',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_Criminal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Equation parts: NO MAIL + PHONE CALL = CRIMINAL
  const part1Op = interpolate(sp(10), [0, 1], [0, 1]);
  const part1X = interpolate(sp(10), [0, 1], [-80, 0]);

  const plusScale = sp(35);
  const plusOp = interpolate(sp(35), [0, 1], [0, 1]);

  const part2Op = interpolate(sp(55), [0, 1], [0, 1]);
  const part2X = interpolate(sp(55), [0, 1], [80, 0]);

  const equalsOp = interpolate(sp(85), [0, 1], [0, 1]);
  const equalsScale = sp(85);

  // CRIMINAL hammer slam
  const crimScale = interpolate(sp(110, { damping: 6, stiffness: 220 }), [0, 1], [4, 1]);
  const crimOp = interpolate(sp(110), [0, 1], [0, 1]);

  // Shake on impact
  const shake = interpolate(frame, [110, 115, 125], [0, 12, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bottom tagline
  const taglineOp = interpolate(sp(150), [0, 1], [0, 1]);
  const taglineY = interpolate(sp(150), [0, 1], [40, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp, transform: `translate(${shake}px, ${shake * 0.5}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 60%, rgba(254, 236, 65, 0.25) 0%, transparent 65%)',
      }} />

      {/* Animated shapes */}
      <div style={{
        position: 'absolute',
        width: 800,
        height: 800,
        borderRadius: '50%',
        border: '5px solid #000000',
        opacity: 0.04,
        top: '50%',
        left: '50%',
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.025) * 0.08}) rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 400,
        height: 400,
        backgroundColor: '#FEEC41',
        opacity: 0.06,
        top: '10%',
        right: '8%',
        transform: `rotate(${frame * 0.3}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 300,
        height: 300,
        backgroundColor: '#000000',
        opacity: 0.05,
        bottom: '12%',
        left: '8%',
        borderRadius: 30,
        transform: `rotate(${-frame * 0.2 + 15}deg)`,
      }} />

      {/* Cross hatch texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="cross14" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 0 0 L 60 60 M 60 0 L 0 60" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross14)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const x = (seededRandom(i + 14) * 1920) % 1920;
        const y = (seededRandom(i + 24) * 1080 + frame * 0.6) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 7,
            height: 7,
            backgroundColor: i % 3 === 0 ? '#FEEC41' : '#000000',
            opacity: 0.4,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Top tag */}
      <div style={{
        position: 'absolute',
        top: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '10px 32px',
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 36,
          letterSpacing: 4,
        }}>
          THE SIMPLE TEST
        </div>
      </div>

      {/* Equation Row */}
      <div style={{
        position: 'absolute',
        top: '32%',
        left: 0,
        right: 0,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 30,
      }}>
        {/* NO MAIL block */}
        <div style={{
          opacity: part1Op,
          transform: `translateX(${part1X}px)`,
          textAlign: 'center',
        }}>
          <svg width="180" height="160" viewBox="0 0 180 160">
            <rect x="20" y="40" width="140" height="100" fill="#F5F5F5" stroke="#000000" strokeWidth="5" />
            <polygon points="20,40 160,40 90,100" fill="#FEEC41" stroke="#000000" strokeWidth="5" />
            <line x1="35" y1="45" x2="145" y2="135" stroke="#FF0000" strokeWidth="10" strokeLinecap="round" />
            <line x1="145" y1="45" x2="35" y2="135" stroke="#FF0000" strokeWidth="10" strokeLinecap="round" />
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 47,
            color: '#000000',
            marginTop: 10,
            letterSpacing: 1,
          }}>
            NO MAIL
          </div>
        </div>

        {/* Plus */}
        <div style={{
          opacity: plusOp,
          transform: `scale(${plusScale})`,
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 119,
          color: '#000000',
        }}>
          +
        </div>

        {/* PHONE CALL */}
        <div style={{
          opacity: part2Op,
          transform: `translateX(${part2X}px)`,
          textAlign: 'center',
        }}>
          <svg width="180" height="160" viewBox="0 0 180 160">
            <rect x="40" y="20" width="100" height="140" rx="14" fill="#000000" />
            <rect x="50" y="40" width="80" height="100" fill="#FEEC41" />
            <circle cx="90" cy="150" r="6" fill="#FEEC41" />
            {/* sound waves */}
            <path d="M 145 50 Q 165 80 145 110" stroke="#000000" strokeWidth="4" fill="none" />
            <path d="M 155 35 Q 180 80 155 125" stroke="#000000" strokeWidth="4" fill="none" opacity={Math.abs(Math.sin(frame * 0.2))} />
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 47,
            color: '#000000',
            marginTop: 10,
            letterSpacing: 1,
          }}>
            PHONE CALL
          </div>
        </div>

        {/* Equals */}
        <div style={{
          opacity: equalsOp,
          transform: `scale(${equalsScale})`,
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 119,
          color: '#000000',
        }}>
          =
        </div>

        {/* CRIMINAL */}
        <div style={{
          opacity: crimOp,
          transform: `scale(${crimScale})`,
        }}>
          <div style={{
            backgroundColor: '#000000',
            color: '#FEEC41',
            padding: '24px 44px',
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 102,
            letterSpacing: 2,
            border: '8px solid #FEEC41',
            boxShadow: '12px 12px 0 #000000',
          }}>
            CRIMINAL
          </div>
        </div>
      </div>

      {/* Warning icons floating */}
      <div style={{
        position: 'absolute',
        left: '20%',
        bottom: 280,
        opacity: interpolate(sp(140), [0, 1], [0, 0.8]),
        transform: `translateY(${Math.sin(frame * 0.06) * 8}px)`,
      }}>
        <svg width="80" height="80" viewBox="0 0 80 80">
          <polygon points="40,8 75,68 5,68" fill="#FEEC41" stroke="#000000" strokeWidth="5" />
          <text x="40" y="58" textAnchor="middle" fontFamily="Inter" fontSize="40" fontWeight="900" fill="#000000">!</text>
        </svg>
      </div>
      <div style={{
        position: 'absolute',
        right: '20%',
        bottom: 280,
        opacity: interpolate(sp(155), [0, 1], [0, 0.8]),
        transform: `translateY(${Math.sin(frame * 0.06 + 2) * 8}px)`,
      }}>
        <svg width="80" height="80" viewBox="0 0 80 80">
          <polygon points="40,8 75,68 5,68" fill="#FEEC41" stroke="#000000" strokeWidth="5" />
          <text x="40" y="58" textAnchor="middle" fontFamily="Inter" fontSize="40" fontWeight="900" fill="#000000">!</text>
        </svg>
      </div>

      {/* Bottom tagline */}
      <div style={{
        position: 'absolute',
        bottom: 110,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: taglineOp,
        transform: `translateY(${taglineY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 58,
          fontWeight: 800,
          color: '#000000',
        }}>
          It is <span style={{ backgroundColor: '#FEEC41', padding: '4px 18px' }}>THAT</span> simple.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_Criminal;
```

```tsx
import React from 'react';
Audio: "The scammers rely on your fear of the government to make you panic and stop thinking logically. They want you to act before you can verify. But now that you know the golden rule, you have the power to pause."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15_PowerToPause',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_PowerToPause: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Phase 1: Panic word stack (chaotic)
  const fearOp = interpolate(sp(5), [0, 1], [0, 1]);
  const panicOp = interpolate(sp(20), [0, 1], [0, 1]);
  const rushOp = interpolate(sp(35), [0, 1], [0, 1]);

  // Phase 2: Transition - all words shrink and converge
  const transitionStart = 100;
  const transitionProg = interpolate(frame, [transitionStart, transitionStart + 30], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Phase 3: PAUSE button reveal
  const pauseScale = sp(140, { damping: 8, stiffness: 150 });
  const pauseOp = interpolate(sp(140), [0, 1], [0, 1]);

  // Outer ring pulse
  const ringScale = 1 + Math.sin(frame * 0.08) * 0.06;

  // Bottom text
  const btmOp = interpolate(sp(180), [0, 1], [0, 1]);
  const btmY = interpolate(sp(180), [0, 1], [50, 0]);

  // Camera zoom out
  const camScale = interpolate(transitionProg, [0, 1], [1.08, 1]);

  // Chaotic word positions
  const wordJitter = (i: number) => Math.sin(frame * 0.15 + i) * 6 * (1 - transitionProg);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient — calmer as time goes */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% 50%, rgba(254, 236, 65, ${0.15 + transitionProg * 0.15}) 0%, transparent 60%)`,
      }} />

      {/* Spinning shapes - slow down in phase 3 */}
      <div style={{
        position: 'absolute',
        width: 600,
        height: 600,
        borderRadius: '50%',
        border: '4px solid #000000',
        opacity: 0.05,
        top: '50%',
        left: '50%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.3 * (1 - transitionProg * 0.7)}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 900,
        height: 900,
        borderRadius: '50%',
        border: '2px dashed #000000',
        opacity: 0.04,
        top: '50%',
        left: '50%',
        transform: `translate(-50%, -50%) rotate(${-frame * 0.2 * (1 - transitionProg * 0.7)}deg)`,
      }} />

      {/* Texture - vertical stripes */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        <defs>
          <pattern id="stripes15" width="30" height="30" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="30" stroke="#000000" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#stripes15)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const x = (seededRandom(i + 15) * 1920 + frame * (0.3 + seededRandom(i + 25) * 0.5)) % 1920;
        const y = seededRandom(i + 35) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 6,
            height: 6,
            backgroundColor: '#000000',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `scale(${camScale})`,
      }}>
        {/* PANIC WORDS — chaotic stack */}
        <div style={{
          position: 'absolute',
          top: '18%',
          left: '8%',
          opacity: fearOp * (1 - transitionProg),
          transform: `translate(${wordJitter(1)}px, ${wordJitter(2)}px) rotate(-6deg) scale(${1 - transitionProg * 0.5})`,
          fontFamily: 'Inter, Poppins',
          fontSize: 136,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -2,
        }}>
          FEAR!
        </div>

        <div style={{
          position: 'absolute',
          top: '38%',
          right: '8%',
          opacity: panicOp * (1 - transitionProg),
          transform: `translate(${wordJitter(3)}px, ${wordJitter(4)}px) rotate(8deg) scale(${1 - transitionProg * 0.5})`,
          fontFamily: 'Inter, Poppins',
          fontSize: 153,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -2,
          backgroundColor: '#FEEC41',
          padding: '6px 24px',
        }}>
          PANIC!
        </div>

        <div style={{
          position: 'absolute',
          bottom: '20%',
          left: '14%',
          opacity: rushOp * (1 - transitionProg),
          transform: `translate(${wordJitter(5)}px, ${wordJitter(6)}px) rotate(-4deg) scale(${1 - transitionProg * 0.5})`,
          fontFamily: 'Inter, Poppins',
          fontSize: 119,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -2,
        }}>
          RUSH!!!
        </div>

        <div style={{
          position: 'absolute',
          top: '55%',
          left: '40%',
          opacity: interpolate(sp(50), [0, 1], [0, 1]) * (1 - transitionProg),
          transform: `translate(${wordJitter(7)}px, ${wordJitter(8)}px) rotate(3deg) scale(${1 - transitionProg * 0.5})`,
          fontFamily: 'Inter, Poppins',
          fontSize: 102,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -2,
        }}>
          ACT NOW!
        </div>

        {/* PAUSE BUTTON - center */}
        <div style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          transform: `translate(-50%, -50%) scale(${pauseScale}) scale(${ringScale})`,
          opacity: pauseOp,
        }}>
          {/* outer ring */}
          <div style={{
            width: 460,
            height: 460,
            borderRadius: '50%',
            border: '10px solid #000000',
            backgroundColor: '#FEEC41',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            boxShadow: '0 16px 0 #000000',
          }}>
            {/* pause icon */}
            <div style={{ display: 'flex', gap: 40 }}>
              <div style={{ width: 60, height: 200, backgroundColor: '#000000', borderRadius: 12 }} />
              <div style={{ width: 60, height: 200, backgroundColor: '#000000', borderRadius: 12 }} />
            </div>
          </div>
        </div>

        {/* Glow behind pause */}
        <div style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 600,
          height: 600,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(254, 236, 65, 0.5) 0%, transparent 60%)',
          transform: 'translate(-50%, -50%)',
          opacity: pauseOp,
          zIndex: -1,
        }} />
      </div>

      {/* Top tag */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: interpolate(sp(0), [0, 1], [0, 1]),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 36,
          fontWeight: 700,
          letterSpacing: 10,
          color: '#000000',
          textTransform: 'uppercase',
        }}>
          {transitionProg < 0.5 ? 'Their Weapon' : 'Your Power'}
        </div>
      </div>

      {/* Bottom — POWER TO PAUSE */}
      <div style={{
        position: 'absolute',
        bottom: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: btmOp,
        transform: `translateY(${btmY}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '14px 44px',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 68,
          letterSpacing: 3,
        }}>
          POWER TO PAUSE
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_PowerToPause;
```

```tsx
import React from 'react';
Audio: "Let's get right into the first thing they will never do. Rule number one is that the IRS will never demand immediate payment over the phone."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16_Rule1',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_Rule1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Rule label
  const ruleOp = interpolate(sp(8), [0, 1], [0, 1]);
  const ruleY = interpolate(sp(8), [0, 1], [-60, 0]);

  // Big "01" number
  const numScale = sp(25, { damping: 9, stiffness: 140 });
  const numOp = interpolate(sp(25), [0, 1], [0, 1]);
  const numRot = interpolate(sp(25), [0, 1], [-15, 0]);

  // Headline reveal letter-by-letter approach via spring stagger
  const headOp = interpolate(sp(60), [0, 1], [0, 1]);
  const headY = interpolate(sp(60), [0, 1], [40, 0]);

  // Underline
  const ulW = interpolate(sp(85), [0, 1], [0, 1200]);

  // X stamp
  const xStampScale = interpolate(sp(115, { damping: 7, stiffness: 200 }), [0, 1], [4, 1]);
  const xStampOp = interpolate(sp(115), [0, 1], [0, 1]);
  const xStampRot = interpolate(sp(115), [0, 1], [40, 12]);

  // Phone icon shake
  const phoneOp = interpolate(sp(150), [0, 1], [0, 1]);
  const phoneShake = Math.sin(frame * 0.4) * 4;

  // Bottom subtext
  const subOp = interpolate(sp(180), [0, 1], [0, 1]);

  // Impact flash
  const flash = interpolate(frame, [115, 120, 135], [0, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Flash */}
      <AbsoluteFill style={{ backgroundColor: '#FEEC41', opacity: flash }} />

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(254, 236, 65, 0.18) 0%, transparent 50%, rgba(0,0,0,0.08) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        width: 700,
        height: 700,
        borderRadius: 60,
        backgroundColor: '#FEEC41',
        opacity: 0.06,
        top: '15%',
        right: '-15%',
        transform: `rotate(${frame * 0.18 + 20}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 500,
        height: 500,
        borderRadius: '50%',
        border: '4px solid #000000',
        opacity: 0.05,
        bottom: '-15%',
        left: '-8%',
        transform: `rotate(${-frame * 0.25}deg)`,
      }} />

      {/* Concentric circles texture from corner */}
      <svg style={{ position: 'absolute', top: -300, left: -300, opacity: 0.07 }} width="800" height="800">
        {[100, 200, 300, 400, 500, 600, 700].map((r, i) => (
          <circle key={i} cx="400" cy="400" r={r} fill="none" stroke="#000000" strokeWidth="2" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const x = (seededRandom(i + 16) * 1920) % 1920;
        const y = (seededRandom(i + 26) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            backgroundColor: '#FEEC41',
            opacity: 0.55,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* RULE label */}
      <div style={{
        position: 'absolute',
        top: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: ruleOp,
        transform: `translateY(${ruleY}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '10px 36px',
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 40,
          letterSpacing: 8,
        }}>
          RULE
        </div>
      </div>

      {/* Big "01" */}
      <div style={{
        position: 'absolute',
        top: '22%',
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: numOp,
        transform: `scale(${numScale}) rotate(${numRot}deg)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 374,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.85,
          letterSpacing: -10,
          textShadow: '14px 14px 0 #FEEC41',
        }}>
          01
        </div>
      </div>

      {/* Headline */}
      <div style={{
        position: 'absolute',
        top: '66%',
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: headOp,
        transform: `translateY(${headY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 80,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 1.05,
          letterSpacing: -1,
        }}>
          NO <span style={{ backgroundColor: '#FEEC41', padding: '0 16px' }}>IMMEDIATE</span> PAYMENT
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 50,
          fontWeight: 700,
          color: '#000000',
          marginTop: 16,
        }}>
          Over The Phone — Ever.
        </div>
      </div>

      {/* Underline */}
      <div style={{
        position: 'absolute',
        bottom: 190,
        left: '50%',
        transform: 'translateX(-50%)',
        width: ulW,
        height: 6,
        backgroundColor: '#000000',
      }} />

      {/* X Stamp top right */}
      <div style={{
        position: 'absolute',
        top: 200,
        right: 140,
        opacity: xStampOp,
        transform: `scale(${xStampScale}) rotate(${xStampRot}deg)`,
        transformOrigin: 'center',
      }}>
        <svg width="240" height="240" viewBox="0 0 240 240">
          <circle cx="120" cy="120" r="100" fill="#000000" />
          <circle cx="120" cy="120" r="100" fill="none" stroke="#FEEC41" strokeWidth="6" strokeDasharray="8 6" />
          <line x1="65" y1="65" x2="175" y2="175" stroke="#FEEC41" strokeWidth="20" strokeLinecap="round" />
          <line x1="175" y1="65" x2="65" y2="175" stroke="#FEEC41" strokeWidth="20" strokeLinecap="round" />
        </svg>
      </div>

      {/* Phone icon bottom left */}
      <div style={{
        position: 'absolute',
        bottom: 240,
        left: 100,
        opacity: phoneOp,
        transform: `translate(${phoneShake}px, ${phoneShake * 0.5}px) rotate(-12deg)`,
      }}>
        <svg width="160" height="160" viewBox="0 0 160 160">
          <rect x="45" y="20" width="80" height="120" rx="14" fill="#000000" />
          <rect x="55" y="35" width="60" height="80" fill="#FEEC41" />
          <text x="85" y="85" textAnchor="middle" fontFamily="Inter" fontSize="48" fontWeight="900" fill="#000000">$</text>
        </svg>
      </div>

      {/* Bottom subtext */}
      <div style={{
        position: 'absolute',
        bottom: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: subOp,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 38,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 1,
        }}>
          The first thing they will <span style={{ textDecoration: 'underline', textDecorationColor: '#FEEC41', textDecorationThickness: 8 }}>NEVER</span> do.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_Rule1;
```

```tsx
import React from 'react';
Audio: "Think about how the real government works. If you owe taxes, the IRS calculates the amount, they mail you a bill, and they give you a reasonable amount of time to pay it or set up a payment plan."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17_GovProcess',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_GovProcess: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);
  const titleY = interpolate(sp(5), [0, 1], [-40, 0]);

  // 4-step flow
  const step1Op = interpolate(sp(30), [0, 1], [0, 1]);
  const step1Y = interpolate(sp(30), [0, 1], [50, 0]);

  const conn1W = interpolate(sp(55), [0, 1], [0, 110]);

  const step2Op = interpolate(sp(75), [0, 1], [0, 1]);
  const step2Y = interpolate(sp(75), [0, 1], [50, 0]);

  const conn2W = interpolate(sp(100), [0, 1], [0, 110]);

  const step3Op = interpolate(sp(120), [0, 1], [0, 1]);
  const step3Y = interpolate(sp(120), [0, 1], [50, 0]);

  const conn3W = interpolate(sp(145), [0, 1], [0, 110]);

  const step4Op = interpolate(sp(165), [0, 1], [0, 1]);
  const step4Y = interpolate(sp(165), [0, 1], [50, 0]);

  // Bottom badge
  const badgeScale = sp(210, { damping: 8 });
  const badgeOp = interpolate(sp(210), [0, 1], [0, 1]);

  // Subtle pan
  const camY = interpolate(frame, [0, durationInFrames], [-8, 8], { extrapolateRight: 'clamp' });

  const Step = ({ num, label, icon, op, ty, accent }: any) => (
    <div style={{
      opacity: op,
      transform: `translateY(${ty}px)`,
      width: 290,
      textAlign: 'center',
    }}>
      <div style={{
        width: 290,
        height: 290,
        backgroundColor: accent ? '#FEEC41' : '#EEE3CB',
        border: '6px solid #000000',
        borderRadius: 28,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        boxShadow: '10px 10px 0 #000000',
        transform: `translateY(${Math.sin(frame * 0.04 + num) * 4}px)`,
      }}>
        {icon}
      </div>
      <div style={{
        marginTop: 18,
        fontFamily: 'Inter, Poppins',
        fontSize: 36,
        fontWeight: 900,
        color: '#000000',
        letterSpacing: 1,
      }}>
        {`0${num}. ${label}`}
      </div>
    </div>
  );

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(254, 236, 65, 0.12) 0%, transparent 40%, rgba(0,0,0,0.06) 100%)',
      }} />

      {/* Big background shapes */}
      <div style={{
        position: 'absolute',
        width: 800,
        height: 800,
        borderRadius: '50%',
        backgroundColor: '#FEEC41',
        opacity: 0.05,
        top: '-30%',
        left: '20%',
      }} />
      <div style={{
        position: 'absolute',
        width: 500,
        height: 500,
        backgroundColor: '#000000',
        opacity: 0.04,
        bottom: '-15%',
        right: '10%',
        transform: `rotate(${frame * 0.15}deg)`,
        borderRadius: 40,
      }} />

      {/* Grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="grid17" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid17)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = (seededRandom(i + 17) * 1920 + frame * 0.3) % 1920;
        const y = seededRandom(i + 27) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            backgroundColor: '#000000',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: titleOp,
        transform: `translateY(${titleY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 38,
          fontWeight: 700,
          letterSpacing: 10,
          color: '#000000',
          textTransform: 'uppercase',
          marginBottom: 10,
        }}>
          How Real Government Works
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 102,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -2,
          lineHeight: 1,
        }}>
          THE <span style={{ backgroundColor: '#FEEC41', padding: '0 18px' }}>SLOW</span> PROCESS
        </div>
      </div>

      {/* Steps row */}
      <div style={{
        position: 'absolute',
        top: '42%',
        left: 0,
        right: 0,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 0,
        transform: `translateY(${camY}px)`,
      }}>
        <Step num={1} label="CALCULATE" op={step1Op} ty={step1Y} accent={false}
          icon={
            <svg width="170" height="170" viewBox="0 0 170 170">
              <rect x="25" y="20" width="120" height="130" rx="14" fill="#000000" />
              <rect x="35" y="35" width="100" height="40" fill="#FEEC41" />
              <text x="125" y="65" textAnchor="end" fontFamily="JetBrains Mono" fontSize="22" fontWeight="900" fill="#000000">$1,250</text>
              {/* buttons */}
              {[0,1,2,3].map(r =>
                [0,1,2].map(c =>
                  <rect key={`${r}-${c}`} x={42 + c*30} y={88 + r*14} width="20" height="10" fill="#FEEC41" />
                )
              )}
            </svg>
          }
        />

        <div style={{
          height: 6,
          width: conn1W,
          backgroundColor: '#000000',
          margin: '0 -10px',
          marginTop: -50,
        }} />

        <Step num={2} label="MAIL BILL" op={step2Op} ty={step2Y} accent={true}
          icon={
            <svg width="170" height="170" viewBox="0 0 170 170">
              <rect x="20" y="40" width="130" height="100" fill="#000000" />
              <polygon points="20,40 150,40 85,100" fill="#EEE3CB" stroke="#000000" strokeWidth="4" />
              <rect x="20" y="40" width="130" height="100" fill="none" stroke="#000000" strokeWidth="4" />
              <rect x="100" y="110" width="40" height="22" fill="#FEEC41" stroke="#000000" strokeWidth="3" />
            </svg>
          }
        />

        <div style={{
          height: 6,
          width: conn2W,
          backgroundColor: '#000000',
          margin: '0 -10px',
          marginTop: -50,
        }} />

        <Step num={3} label="TIME GIVEN" op={step3Op} ty={step3Y} accent={false}
          icon={
            <svg width="170" height="170" viewBox="0 0 170 170">
              <circle cx="85" cy="85" r="65" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
              <line x1="85" y1="85" x2="85" y2="35" stroke="#000000" strokeWidth="6" strokeLinecap="round"
                transform={`rotate(${frame * 1.5} 85 85)`} />
              <line x1="85" y1="85" x2="120" y2="85" stroke="#000000" strokeWidth="4" strokeLinecap="round"
                transform={`rotate(${frame * 0.15} 85 85)`} />
              <circle cx="85" cy="85" r="6" fill="#000000" />
            </svg>
          }
        />

        <div style={{
          height: 6,
          width: conn3W,
          backgroundColor: '#000000',
          margin: '0 -10px',
          marginTop: -50,
        }} />

        <Step num={4} label="PAY / PLAN" op={step4Op} ty={step4Y} accent={true}
          icon={
            <svg width="170" height="170" viewBox="0 0 170 170">
              <rect x="20" y="40" width="130" height="90" rx="10" fill="#000000" />
              <rect x="30" y="55" width="40" height="30" fill="#FEEC41" />
              <text x="100" y="80" fontFamily="Inter" fontSize="26" fontWeight="900" fill="#FEEC41">PAY</text>
              <line x1="30" y1="100" x2="140" y2="100" stroke="#FEEC41" strokeWidth="3" />
              <line x1="30" y1="115" x2="100" y2="115" stroke="#FEEC41" strokeWidth="3" />
            </svg>
          }
        />
      </div>

      {/* Bottom badge */}
      <div style={{
        position: 'absolute',
        bottom: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: badgeOp,
        transform: `scale(${badgeScale})`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '14px 40px',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 51,
          letterSpacing: 2,
        }}>
          REASONABLE. WRITTEN. NEVER RUSHED.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_GovProcess;
```

```tsx
import React from 'react';
Audio: "They do not call you and say you have one hour to pay or they will arrest you. That is a complete fabrication designed to induce panic."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18_OneHour',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_OneHour: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Countdown clock
  const clockScale = sp(15, { damping: 8 });
  const clockOp = interpolate(sp(15), [0, 1], [0, 1]);

  // Hands sweep aggressively
  const minuteAngle = frame * 6;
  const secondAngle = frame * 24;

  // Fake speech bubble
  const bubbleOp = interpolate(sp(50), [0, 1], [0, 1]);
  const bubbleX = interpolate(sp(50), [0, 1], [120, 0]);

  // FABRICATION rubber stamp
  const fabScale = interpolate(sp(110, { damping: 6, stiffness: 220 }), [0, 1], [5, 1]);
  const fabOp = interpolate(sp(110), [0, 1], [0, 1]);
  const fabRot = interpolate(sp(110), [0, 1], [-25, -8]);

  // Shake on stamp impact
  const shake = interpolate(frame, [110, 115, 125], [0, 14, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bottom text
  const bottomOp = interpolate(sp(150), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp, transform: `translate(${shake}px, ${shake * 0.5}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 35% 50%, rgba(254, 236, 65, 0.2) 0%, transparent 55%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 700,
        height: 700,
        borderRadius: '50%',
        border: '6px solid #000000',
        opacity: 0.05,
        top: '50%',
        left: '20%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.4}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 400,
        height: 400,
        backgroundColor: '#000000',
        opacity: 0.04,
        bottom: '-10%',
        right: '-5%',
        transform: `rotate(${frame * 0.25 + 30}deg)`,
        borderRadius: 30,
      }} />

      {/* Radial line texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        {Array.from({ length: 24 }).map((_, i) => (
          <line key={i} x1="960" y1="540" x2={960 + Math.cos(i * Math.PI / 12) * 1500} y2={540 + Math.sin(i * Math.PI / 12) * 1500} stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const x = (seededRandom(i + 18) * 1920 + frame * 0.4) % 1920;
        const y = seededRandom(i + 28) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 6,
            height: 6,
            backgroundColor: '#FEEC41',
            opacity: 0.5,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Top header */}
      <div style={{
        position: 'absolute',
        top: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: interpolate(sp(0), [0, 1], [0, 1]),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 95,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -2,
          lineHeight: 1,
        }}>
          "ONE HOUR OR ARREST!"
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 32,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 6,
          marginTop: 10,
        }}>
          — WHAT THE SCAMMER SAYS
        </div>
      </div>

      {/* Clock (left) */}
      <div style={{
        position: 'absolute',
        left: '12%',
        top: '45%',
        opacity: clockOp,
        transform: `scale(${clockScale}) translateY(${Math.sin(frame * 0.05) * 5}px)`,
      }}>
        <svg width="400" height="400" viewBox="0 0 400 400">
          {/* shadow */}
          <ellipse cx="200" cy="385" rx="170" ry="10" fill="#000000" opacity="0.2" />
          {/* outer */}
          <circle cx="200" cy="200" r="180" fill="#000000" />
          <circle cx="200" cy="200" r="160" fill="#FEEC41" stroke="#000000" strokeWidth="8" />
          {/* hour markers */}
          {Array.from({ length: 12 }).map((_, i) => {
            const angle = (i * 30 - 90) * Math.PI / 180;
            const x1 = 200 + Math.cos(angle) * 140;
            const y1 = 200 + Math.sin(angle) * 140;
            const x2 = 200 + Math.cos(angle) * 155;
            const y2 = 200 + Math.sin(angle) * 155;
            return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#000000" strokeWidth="5" />;
          })}
          {/* "1 HOUR" big number */}
          <text x="200" y="170" textAnchor="middle" fontFamily="Inter" fontSize="48" fontWeight="900" fill="#000000">1</text>
          <text x="200" y="210" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="800" fill="#000000">HOUR</text>
          {/* minute hand */}
          <line x1="200" y1="200" x2="200" y2="70" stroke="#000000" strokeWidth="10" strokeLinecap="round"
            transform={`rotate(${minuteAngle} 200 200)`} />
          {/* second hand */}
          <line x1="200" y1="200" x2="200" y2="60" stroke="#FF0000" strokeWidth="4" strokeLinecap="round"
            transform={`rotate(${secondAngle} 200 200)`} />
          <circle cx="200" cy="200" r="12" fill="#000000" />
        </svg>
      </div>

      {/* Speech bubble (right) */}
      <div style={{
        position: 'absolute',
        right: '8%',
        top: '40%',
        opacity: bubbleOp,
        transform: `translateX(${bubbleX}px)`,
      }}>
        <div style={{
          backgroundColor: '#000000',
          border: '6px solid #000000',
          padding: '24px 36px',
          maxWidth: 560,
          position: 'relative',
          borderRadius: 24,
          boxShadow: '10px 10px 0 #FEEC41',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 36,
            fontWeight: 800,
            color: '#FEEC41',
            lineHeight: 1.3,
          }}>
            "Pay in 60 minutes
            <br /> OR police will
            <br /> arrest you NOW!"
          </div>
          {/* tail */}
          <div style={{
            position: 'absolute',
            left: -30,
            top: '40%',
            width: 0,
            height: 0,
            borderTop: '20px solid transparent',
            borderBottom: '20px solid transparent',
            borderRight: '30px solid #000000',
          }} />
        </div>
        <div style={{
          marginTop: 16,
          fontFamily: 'Inter, Poppins',
          fontSize: 28,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 4,
        }}>
          — FAKE AGENT VOICE
        </div>
      </div>

      {/* FABRICATION stamp */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '60%',
        opacity: fabOp,
        transform: `translate(-50%, -50%) scale(${fabScale}) rotate(${fabRot}deg)`,
      }}>
        <div style={{
          border: '8px solid #000000',
          padding: '20px 50px',
          backgroundColor: '#FEEC41',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 80,
          color: '#000000',
          letterSpacing: 6,
          boxShadow: '10px 10px 0 #000000',
        }}>
          FABRICATION
        </div>
      </div>

      {/* Bottom message */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: bottomOp,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 42,
          fontWeight: 700,
          color: '#000000',
        }}>
          Designed to <span style={{ backgroundColor: '#000000', color: '#FEEC41', padding: '4px 16px' }}>INDUCE PANIC</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_OneHour;
```

```tsx
import React from 'react';
Audio: "When a scammer calls, they want you to be so terrified that you don't have time to think. They will say your account is about to be frozen or a warrant is being typed up right now, and the only way to stop it is to give them your credit card number immediately."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19_ScammerPlaybook',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_ScammerPlaybook: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headY = interpolate(sp(5), [0, 1], [-50, 0]);

  // 3 fake threats appearing one by one
  const t1Op = interpolate(sp(35), [0, 1], [0, 1]);
  const t1X = interpolate(sp(35), [0, 1], [-100, 0]);

  const t2Op = interpolate(sp(75), [0, 1], [0, 1]);
  const t2X = interpolate(sp(75), [0, 1], [-100, 0]);

  const t3Op = interpolate(sp(115), [0, 1], [0, 1]);
  const t3X = interpolate(sp(115), [0, 1], [-100, 0]);

  // Trap arrow + card
  const cardOp = interpolate(sp(165), [0, 1], [0, 1]);
  const cardScale = sp(165, { damping: 10 });
  const cardShake = Math.sin(frame * 0.5) * 3;

  // Bottom warning
  const warnOp = interpolate(sp(205), [0, 1], [0, 1]);

  const ThreatRow = ({ icon, text, op, x, idx }: any) => (
    <div style={{
      opacity: op,
      transform: `translateX(${x}px)`,
      display: 'flex',
      alignItems: 'center',
      gap: 32,
      marginBottom: 26,
    }}>
      {/* index */}
      <div style={{
        width: 80,
        height: 80,
        borderRadius: '50%',
        backgroundColor: '#000000',
        color: '#FEEC41',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'Inter, Poppins',
        fontSize: 42,
        fontWeight: 900,
        flexShrink: 0,
      }}>
        {idx}
      </div>
      {/* icon */}
      <div style={{
        width: 100,
        height: 100,
        backgroundColor: '#FEEC41',
        border: '5px solid #000000',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        flexShrink: 0,
        transform: `translateY(${Math.sin(frame * 0.05 + idx) * 3}px)`,
      }}>
        {icon}
      </div>
      {/* text */}
      <div style={{
        fontFamily: 'Inter, Poppins',
        fontSize: 50,
        fontWeight: 800,
        color: '#000000',
        letterSpacing: -1,
      }}>
        {text}
      </div>
    </div>
  );

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, rgba(254, 236, 65, 0.18) 0%, transparent 50%, rgba(0,0,0,0.1) 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 500,
        height: 500,
        backgroundColor: '#FEEC41',
        opacity: 0.06,
        top: -150,
        left: -100,
        borderRadius: 50,
        transform: `rotate(${frame * 0.18}deg)`,
      }} />
      <svg style={{ position: 'absolute', bottom: -100, right: -50, opacity: 0.05 }} width="500" height="500">
        <polygon points="250,30 470,470 30,470" fill="#000000" transform={`rotate(${frame * 0.12} 250 250)`} />
      </svg>
      <div style={{
        position: 'absolute',
        width: 350,
        height: 350,
        borderRadius: '50%',
        border: '4px solid #000000',
        opacity: 0.05,
        top: '20%',
        right: '5%',
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
      }} />

      {/* Texture: dots */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="dots19" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots19)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = (seededRandom(i + 19) * 1920) % 1920;
        const y = (seededRandom(i + 29) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            backgroundColor: '#000000',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: headOp,
        transform: `translateY(${headY}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'Inter, Poppins',
          fontSize: 36,
          fontWeight: 700,
          letterSpacing: 8,
          color: '#000000',
          textTransform: 'uppercase',
          marginBottom: 14,
        }}>
          The Scammer's Playbook
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 87,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -2,
        }}>
          THREE <span style={{ backgroundColor: '#FEEC41', padding: '0 18px' }}>FAKE</span> THREATS
        </div>
      </div>

      {/* Threats list */}
      <div style={{
        position: 'absolute',
        top: '36%',
        left: '10%',
      }}>
        <ThreatRow
          idx="1"
          op={t1Op}
          x={t1X}
          icon={
            <svg width="60" height="60" viewBox="0 0 60 60">
              <rect x="10" y="20" width="40" height="30" fill="#000000" rx="4" />
              <path d="M 18 20 L 18 14 Q 18 6 30 6 Q 42 6 42 14 L 42 20" fill="none" stroke="#000000" strokeWidth="4" />
              <line x1="30" y1="30" x2="30" y2="42" stroke="#FEEC41" strokeWidth="4" />
            </svg>
          }
          text='"Account FROZEN!"'
        />
        <ThreatRow
          idx="2"
          op={t2Op}
          x={t2X}
          icon={
            <svg width="60" height="60" viewBox="0 0 60 60">
              <rect x="10" y="8" width="40" height="48" fill="#000000" />
              <line x1="14" y1="18" x2="46" y2="18" stroke="#FEEC41" strokeWidth="2" />
              <line x1="14" y1="26" x2="42" y2="26" stroke="#FEEC41" strokeWidth="2" />
              <line x1="14" y1="34" x2="46" y2="34" stroke="#FEEC41" strokeWidth="2" />
              <text x="30" y="50" textAnchor="middle" fontFamily="Inter" fontSize="12" fontWeight="900" fill="#FEEC41">WARRANT</text>
            </svg>
          }
          text='"Warrant being TYPED now!"'
        />
        <ThreatRow
          idx="3"
          op={t3Op}
          x={t3X}
          icon={
            <svg width="60" height="60" viewBox="0 0 60 60">
              <polygon points="30,5 55,52 5,52" fill="#000000" />
              <text x="30" y="44" textAnchor="middle" fontFamily="Inter" fontSize="30" fontWeight="900" fill="#FEEC41">!</text>
            </svg>
          }
          text='"Stop it with CREDIT CARD!"'
        />
      </div>

      {/* Right side: Credit Card trap */}
      <div style={{
        position: 'absolute',
        right: '8%',
        top: '46%',
        opacity: cardOp,
        transform: `scale(${cardScale}) translate(${cardShake}px, ${cardShake * 0.5}px) rotate(-8deg)`,
      }}>
        <svg width="420" height="280" viewBox="0 0 420 280">
          {/* shadow */}
          <rect x="20" y="20" width="380" height="240" rx="22" fill="#000000" opacity="0.3" />
          {/* card */}
          <rect x="10" y="10" width="380" height="240" rx="22" fill="#000000" />
          {/* gold strip */}
          <rect x="40" y="80" width="80" height="60" rx="6" fill="#FEEC41" />
          {/* chip lines */}
          <line x1="50" y1="100" x2="110" y2="100" stroke="#000000" strokeWidth="2" />
          <line x1="50" y1="120" x2="110" y2="120" stroke="#000000" strokeWidth="2" />
          {/* number */}
          <text x="40" y="180" fontFamily="JetBrains Mono" fontSize="28" fontWeight="900" fill="#FEEC41">**** **** ****</text>
          <text x="40" y="225" fontFamily="Inter" fontSize="18" fontWeight="700" fill="#FEEC41">VICTIM CARD</text>
          {/* big red X */}
          <line x1="60" y1="60" x2="340" y2="220" stroke="#FF0000" strokeWidth="14" strokeLinecap="round" />
          <line x1="340" y1="60" x2="60" y2="220" stroke="#FF0000" strokeWidth="14" strokeLinecap="round" />
        </svg>
        <div style={{
          marginTop: 16,
          fontFamily: 'Inter, Poppins',
          fontSize: 32,
          fontWeight: 900,
          color: '#000000',
          textAlign: 'center',
        }}>
          THE TRAP
        </div>
      </div>

      {/* Bottom warning */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: warnOp,
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'Inter, Poppins',
          fontSize: 51,
          fontWeight: 800,
          color: '#000000',
          backgroundColor: '#FEEC41',
          padding: '10px 32px',
          border: '5px solid #000000',
        }}>
          Goal: Stop You From Thinking.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_ScammerPlaybook;
```

```tsx
import React from 'react';
Audio: "The real IRS will never do this. They will never ask for your credit card number over an unsolicited phone call. They will never demand that you wire money through Western Union."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20_NeverList',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20_NeverList: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headScale = sp(5);

  // Card 1 (credit card)
  const c1Op = interpolate(sp(35), [0, 1], [0, 1]);
  const c1Y = interpolate(sp(35), [0, 1], [60, 0]);
  const c1Stamp = interpolate(sp(70, { damping: 7, stiffness: 200 }), [0, 1], [0, 1]);

  // Card 2 (Western Union)
  const c2Op = interpolate(sp(110), [0, 1], [0, 1]);
  const c2Y = interpolate(sp(110), [0, 1], [60, 0]);
  const c2Stamp = interpolate(sp(145, { damping: 7, stiffness: 200 }), [0, 1], [0, 1]);

  // Big NEVER banner
  const neverScale = sp(190, { damping: 9, stiffness: 140 });
  const neverOp = interpolate(sp(190), [0, 1], [0, 1]);

  // Subtle camera
  const camS = 1 + Math.sin(frame * 0.025) * 0.01;

  const ForbiddenCard = ({ children, op, ty, stampVal, label }: any) => (
    <div style={{
      opacity: op,
      transform: `translateY(${ty}px)`,
      width: 580,
      backgroundColor: '#EEE3CB',
      border: '6px solid #000000',
      borderRadius: 24,
      padding: '36px 30px',
      position: 'relative',
      boxShadow: '12px 12px 0 #000000',
      transform: `translateY(${ty + Math.sin(frame * 0.04) * 3}px)`,
    }}>
      {children}
      {/* FORBIDDEN stamp */}
      <div style={{
        position: 'absolute',
        top: 30,
        right: 20,
        opacity: stampVal,
        transform: `rotate(-18deg) scale(${interpolate(stampVal, [0, 1], [3, 1])})`,
        transformOrigin: 'center',
      }}>
        <div style={{
          border: '5px solid #FF0000',
          padding: '6px 18px',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 30,
          color: '#FF0000',
          letterSpacing: 2,
          backgroundColor: 'rgba(238, 227, 203, 0.85)',
        }}>
          FORBIDDEN
        </div>
      </div>
      <div style={{
        marginTop: 20,
        fontFamily: 'Inter, Poppins',
        fontSize: 40,
        fontWeight: 900,
        color: '#000000',
        textAlign: 'center',
        letterSpacing: 1,
      }}>
        {label}
      </div>
    </div>
  );

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, rgba(254, 236, 65, 0.2) 0%, transparent 60%)',
      }} />

      {/* Background shapes */}
      <div style={{
        position: 'absolute',
        width: 600,
        height: 600,
        borderRadius: '50%',
        backgroundColor: '#FEEC41',
        opacity: 0.05,
        top: '40%',
        left: '50%',
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute',
        width: 350,
        height: 350,
        backgroundColor: '#000000',
        opacity: 0.04,
        top: '10%',
        right: '8%',
        transform: `rotate(${frame * 0.2 + 12}deg)`,
        borderRadius: 30,
      }} />
      <div style={{
        position: 'absolute',
        width: 250,
        height: 250,
        borderRadius: '50%',
        border: '4px solid #000000',
        opacity: 0.05,
        bottom: '10%',
        left: '8%',
        transform: `scale(${1 + Math.sin(frame * 0.05) * 0.1})`,
      }} />

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="grid20" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid20)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const x = (seededRandom(i + 20) * 1920 + frame * 0.3) % 1920;
        const y = seededRandom(i + 30) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            backgroundColor: '#FEEC41',
            opacity: 0.5,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: headOp,
        transform: `scale(${headScale * camS})`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 36,
          fontWeight: 700,
          letterSpacing: 10,
          color: '#000000',
          textTransform: 'uppercase',
          marginBottom: 8,
        }}>
          Real IRS Will...
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 119,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -3,
          lineHeight: 1,
        }}>
          NEVER<span style={{ color: '#FEEC41', WebkitTextStroke: '4px #000000' }}>×2</span>
        </div>
      </div>

      {/* Two cards */}
      <div style={{
        position: 'absolute',
        top: '38%',
        left: 0,
        right: 0,
        display: 'flex',
        justifyContent: 'center',
        gap: 60,
      }}>
        <ForbiddenCard op={c1Op} ty={c1Y} stampVal={c1Stamp} label="Ask Credit Card on Call">
          <svg width="280" height="180" viewBox="0 0 280 180" style={{ display: 'block', margin: '0 auto' }}>
            <rect x="10" y="10" width="260" height="160" rx="16" fill="#000000" />
            <rect x="30" y="60" width="60" height="40" rx="4" fill="#FEEC41" />
            <line x1="40" y1="74" x2="80" y2="74" stroke="#000000" strokeWidth="2" />
            <line x1="40" y1="86" x2="80" y2="86" stroke="#000000" strokeWidth="2" />
            <text x="30" y="135" fontFamily="JetBrains Mono" fontSize="20" fontWeight="900" fill="#FEEC41">**** ****</text>
            <text x="30" y="160" fontFamily="Inter" fontSize="14" fontWeight="700" fill="#FEEC41">SCAMMER WANTS</text>
          </svg>
        </ForbiddenCard>

        <ForbiddenCard op={c2Op} ty={c2Y} stampVal={c2Stamp} label="Wire via Western Union">
          <svg width="280" height="180" viewBox="0 0 280 180" style={{ display: 'block', margin: '0 auto' }}>
            {/* Wire transfer terminal */}
            <rect x="40" y="20" width="200" height="140" rx="12" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
            <text x="140" y="60" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#000000">WIRE</text>
            <text x="140" y="88" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#000000">TRANSFER</text>
            <line x1="60" y1="105" x2="220" y2="105" stroke="#000000" strokeWidth="3" />
            {/* arrow */}
            <line x1="70" y1="135" x2="200" y2="135" stroke="#000000" strokeWidth="6" />
            <polygon points="200,125 220,135 200,145" fill="#000000" />
          </svg>
        </ForbiddenCard>
      </div>

      {/* Big NEVER banner */}
      <div style={{
        position: 'absolute',
        bottom: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: neverOp,
        transform: `scale(${neverScale})`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '18px 60px',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 77,
          letterSpacing: 4,
          boxShadow: '0 10px 0 #FEEC41',
        }}>
          THESE ARE NON-NEGOTIABLE
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene20_NeverList;
```

