```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S1_MeethaZahar',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S1_MeethaZahar: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Honey drip animation
  const dripY = interpolate(frame, [0, durationInFrames], [0, 60], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pulsing danger glow
  const dangerPulse = Math.sin(frame * 0.08) * 0.3 + 0.7;

  // Warning text shake
  const shakeX = frame > 50 ? Math.sin(frame * 0.3) * 3 : 0;

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 7) * 1920,
    y: seededRandom(i * 13) * 1080,
    size: seededRandom(i * 3) * 6 + 2,
    op: seededRandom(i * 17) * 0.06 + 0.02,
    speed: seededRandom(i * 11) * 0.04 + 0.01,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient Background — deep red-black danger zone */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 30% 50%, #3D0000 0%, #1C1C1C 55%, #0D0D0D 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — large translucent triangle warning */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <polygon
            points="960,80 200,950 1720,950"
            fill="none"
            stroke="#ff0000"
            strokeWidth="1"
            opacity={0.04 + Math.sin(frame * 0.03) * 0.02}
            transform={`scale(${1 + Math.sin(frame * 0.02) * 0.01}) translate(${Math.sin(frame * 0.015) * 4} 0)`}
            style={{ transformOrigin: '960px 540px' }}
          />
          <circle
            cx="960" cy="540"
            r={380 + Math.sin(frame * 0.04) * 8}
            fill="none"
            stroke="#e9d700"
            strokeWidth="0.8"
            opacity={0.05}
          />
          <rect
            x="100" y="100"
            width="300" height="300"
            fill="none"
            stroke="#ff0000"
            strokeWidth="0.5"
            opacity={0.03}
            transform={`rotate(${frame * 0.1} 250 250)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — diagonal grid lines */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 20 }, (_, i) => (
            <line
              key={i}
              x1={i * 100 - 50 + Math.sin(frame * 0.02 + i) * 3}
              y1="0"
              x2={i * 100 - 50 + Math.sin(frame * 0.02 + i) * 3 - 200}
              y2="1080"
              stroke="#ff0000"
              strokeWidth="0.8"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x + Math.sin(frame * p.speed + i) * 15,
              top: p.y + Math.cos(frame * p.speed + i) * 10,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#ff0000' : i % 3 === 1 ? '#e9d700' : '#F5F5F5',
              opacity: p.op,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill>

        {/* Left side — TRAP NUMBER badge */}
        <div style={{
          position: 'absolute',
          left: 100,
          top: 120,
          opacity: sp(0),
          transform: `translateX(${(1 - sp(0)) * -80}px)`,
        }}>
          <div style={{
            backgroundColor: '#ff0000',
            color: '#F5F5F5',
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 800,
            letterSpacing: 4,
            padding: '10px 28px',
            textTransform: 'uppercase',
          }}>
            जाल #2
          </div>
        </div>

        {/* Central big warning icon */}
        <div style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) scale(${sp(8)})`,
          opacity: sp(8),
        }}>
          <svg width="200" height="200" viewBox="0 0 200 200">
            <polygon
              points="100,15 185,175 15,175"
              fill="none"
              stroke="#e9d700"
              strokeWidth="6"
              opacity={dangerPulse}
            />
            <text
              x="100" y="140"
              textAnchor="middle"
              fontSize="80"
              fontFamily="Inter, Poppins"
              fontWeight="900"
              fill="#e9d700"
              opacity={dangerPulse}
            >!</text>
          </svg>
        </div>

        {/* Main Hindi text */}
        <div style={{
          position: 'absolute',
          left: 160,
          bottom: 260,
          maxWidth: 900,
          transform: `translateY(${(1 - sp(18)) * 60}px) translateX(${shakeX}px)`,
          opacity: sp(18),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 96,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.15,
          }}>
            दूसरा जाल
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 54,
            fontWeight: 600,
            color: '#9E9E9E',
            marginTop: 8,
          }}>
            उतना ही खतरनाक है...
          </div>
        </div>

        {/* Right side — "मीठा" highlight */}
        <div style={{
          position: 'absolute',
          right: 120,
          top: '50%',
          transform: `translateY(-50%) scale(${sp(30)})`,
          opacity: sp(30),
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 130,
            fontWeight: 900,
            color: '#e9d700',
            textShadow: `0 0 ${40 + dangerPulse * 20}px #e9d70088`,
          }}>
            मीठा
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            fontWeight: 500,
            color: '#F5F5F5',
            opacity: 0.6,
            marginTop: -10,
          }}>
            लगता है...
          </div>
        </div>

        {/* Honey drip SVG decoration */}
        <div style={{
          position: 'absolute',
          right: 280,
          top: 120,
          opacity: interpolate(frame, [20, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <svg width="60" height={80 + dripY} viewBox={`0 0 60 ${80 + dripY}`}>
            <path
              d={`M30 0 Q35 ${20 + dripY * 0.3} 30 ${60 + dripY}`}
              stroke="#e9d700"
              strokeWidth="4"
              fill="none"
              opacity={0.6}
            />
            <ellipse cx="30" cy={60 + dripY} rx="10" ry="14" fill="#e9d700" opacity={0.5} />
          </svg>
        </div>

      </AbsoluteFill>

      {/* Stick Figure — Warren Buffett character */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 80,
          bottom: 60,
          opacity: sp(40),
          transform: `translateY(${(1 - sp(40)) * 80}px)`,
        }}>
          <svg width="90" height="160" viewBox="0 0 90 160">
            {/* Golden glow */}
            <circle cx="45" cy="28" r="32" fill="#e9d700" opacity={0.08 + Math.sin(frame * 0.06) * 0.04} />
            {/* Head */}
            <circle
              cx="45" cy="28" r="20"
              fill="none" stroke="#F5F5F5" strokeWidth="2.5"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 45 28)`}
            />
            {/* Golden glasses */}
            <circle cx="38" cy="27" r="6" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="52" cy="27" r="6" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="44" y1="27" x2="46" y2="27" stroke="#e9d700" strokeWidth="1.5" />
            {/* Body */}
            <line
              x1="45" y1="48"
              x2="45" y2={105 + Math.sin(frame * 0.05) * 2}
              stroke="#F5F5F5" strokeWidth="2.5"
            />
            {/* Arms — one raised in warning */}
            <line x1="45" y1="65" x2="15" y2="50" stroke="#F5F5F5" strokeWidth="2.2" />
            <line
              x1="45" y1="65"
              x2={72 + Math.sin(frame * 0.08) * 4}
              y2={50 + Math.sin(frame * 0.08) * 4}
              stroke="#F5F5F5" strokeWidth="2.2"
            />
            {/* Legs */}
            <line x1="45" y1="105" x2="25" y2="148" stroke="#F5F5F5" strokeWidth="2.2" />
            <line x1="45" y1="105" x2="65" y2="148" stroke="#F5F5F5" strokeWidth="2.2" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.75) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S1_MeethaZahar;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S2_LifestyleInflation',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S2_LifestyleInflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Inflation arrow rising
  const arrowScale = sp(15);
  const arrowHeight = interpolate(arrowScale, [0, 1], [0, 320], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Text reveal character by character feel via clip
  const textReveal = interpolate(frame, [25, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 5) * 1920,
    y: seededRandom(i * 9) * 1080,
    size: seededRandom(i * 2) * 5 + 2,
    op: seededRandom(i * 19) * 0.05 + 0.02,
    speed: seededRandom(i * 7) * 0.05 + 0.02,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient Background — amber-gold tone, aspirational but loaded */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 70% 30%, #2A1F00 0%, #1C1C1C 50%, #0E0E0E 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — large circle + rotated rectangles */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle
            cx="1400" cy="200"
            r={260 + Math.sin(frame * 0.025) * 12}
            fill="#e9d700"
            opacity={0.04}
          />
          <rect
            x="80" y="600"
            width="400" height="400"
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${15 + frame * 0.05} 280 800)`}
          />
          <line x1="0" y1="540" x2="1920" y2="540" stroke="#9E9E9E" strokeWidth="0.5" opacity={0.06} />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — rising bar chart grid */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
          {Array.from({ length: 12 }, (_, i) => (
            <line
              key={i}
              x1="0" y1={100 * i}
              x2="1920" y2={100 * i}
              stroke="#e9d700"
              strokeWidth="0.6"
            />
          ))}
          {Array.from({ length: 20 }, (_, i) => (
            <line
              key={`v${i}`}
              x1={100 * i} y1="0"
              x2={100 * i} y2="1080"
              stroke="#e9d700"
              strokeWidth="0.4"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: p.x + Math.sin(frame * p.speed + i) * 12,
            top: p.y - frame * p.speed * 0.5,
            width: p.size, height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#e9d700' : '#FFC107',
            opacity: p.op,
          }} />
        ))}
      </AbsoluteFill>

      {/* Rising Arrow — Lifestyle Inflation visualization */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 120,
          bottom: 160,
        }}>
          <svg width="180" height="400" viewBox="0 0 180 400">
            {/* Arrow shaft */}
            <rect
              x="75" y={400 - arrowHeight}
              width="30" height={arrowHeight}
              fill="#e9d700"
              opacity={0.9}
              rx={4}
            />
            {/* Arrow head */}
            <polygon
              points={`90,${Math.max(5, 395 - arrowHeight)} 50,${Math.max(55, 445 - arrowHeight)} 130,${Math.max(55, 445 - arrowHeight)}`}
              fill="#e9d700"
              opacity={arrowScale}
            />
            {/* Label */}
            <text
              x="90" y="390"
              textAnchor="middle"
              fontSize="18"
              fontFamily="Inter, Poppins"
              fill="#9E9E9E"
              fontWeight="600"
            >INCOME</text>
          </svg>
        </div>

        {/* Second arrow — expenses rising faster */}
        <div style={{
          position: 'absolute',
          left: 260,
          bottom: 160,
          opacity: sp(20),
        }}>
          <svg width="180" height="400" viewBox="0 0 180 400">
            <rect
              x="75" y={400 - Math.min(arrowHeight * 1.35, 370)}
              width="30" height={Math.min(arrowHeight * 1.35, 370)}
              fill="#ff0000"
              opacity={0.9}
              rx={4}
            />
            <polygon
              points={`90,${Math.max(5, 395 - arrowHeight * 1.35)} 50,${Math.max(55, 445 - arrowHeight * 1.35)} 130,${Math.max(55, 445 - arrowHeight * 1.35)}`}
              fill="#ff0000"
              opacity={sp(20)}
            />
            <text
              x="90" y="390"
              textAnchor="middle"
              fontSize="18"
              fontFamily="Inter, Poppins"
              fill="#9E9E9E"
              fontWeight="600"
            >EXPENSES</text>
          </svg>
        </div>
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill>

        {/* Chapter tag */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 120,
          opacity: sp(0),
          transform: `translateY(${(1 - sp(0)) * -30}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 20,
            color: '#9E9E9E',
            letterSpacing: 3,
            fontWeight: 600,
          }}>
            // FINANCIAL TRAP #2
          </div>
        </div>

        {/* Big name reveal */}
        <div style={{
          position: 'absolute',
          right: 120,
          top: '50%',
          transform: `translateY(-50%)`,
          textAlign: 'right',
          overflow: 'hidden',
          maxWidth: 900,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 160,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1,
            clipPath: `inset(0 ${(1 - textReveal) * 100}% 0 0)`,
            letterSpacing: -4,
          }}>
            LIFESTYLE
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 160,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
            clipPath: `inset(0 ${(1 - Math.max(0, sp(35))) * 100}% 0 0)`,
            letterSpacing: -4,
          }}>
            INFLATION
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            fontWeight: 600,
            color: '#9E9E9E',
            marginTop: 16,
            opacity: sp(50),
            transform: `translateY(${(1 - sp(50)) * 20}px)`,
          }}>
            इसका नाम है —
          </div>
        </div>

      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 60,
          bottom: 50,
          opacity: sp(45),
          transform: `translateY(${(1 - sp(45)) * 60}px)`,
        }}>
          <svg width="85" height="155" viewBox="0 0 85 155">
            <circle cx="42" cy="26" r="28" fill="#e9d700" opacity={0.07 + Math.sin(frame * 0.05) * 0.03} />
            <circle cx="42" cy="26" r="19" fill="none" stroke="#F5F5F5" strokeWidth="2.4"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 42 26)`} />
            <circle cx="35" cy="25" r="5.5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="49" cy="25" r="5.5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="40.5" y1="25" x2="43.5" y2="25" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="42" y1="45" x2="42" y2={100 + Math.sin(frame * 0.05) * 2} stroke="#F5F5F5" strokeWidth="2.4" />
            {/* arms — both raised, celebrating (ironic) */}
            <line x1="42" y1="62" x2="12" y2="42" stroke="#F5F5F5" strokeWidth="2.2" />
            <line x1="42" y1="62" x2="72" y2="42" stroke="#F5F5F5" strokeWidth="2.2" />
            <line x1="42" y1="100" x2="22" y2="143" stroke="#F5F5F5" strokeWidth="2.2" />
            <line x1="42" y1="100" x2="62" y2="143" stroke="#F5F5F5" strokeWidth="2.2" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.7) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S2_LifestyleInflation;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S3_IncomeBadhtaHai',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S3_IncomeBadhtaHai: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Income vs expense bar animation
  const barProgress = interpolate(frame, [10, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });

  const incomeH = barProgress * 280;
  const expenseH = barProgress * 420; // expenses grow 1.5x faster

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 11) * 1920,
    y: seededRandom(i * 7) * 1080,
    size: seededRandom(i * 3) * 4 + 2,
    op: seededRandom(i * 17) * 0.05 + 0.02,
    speed: seededRandom(i * 13) * 0.04 + 0.02,
  }));

  // Race visual — two circles racing across screen
  const incomeX = interpolate(frame, [0, durationInFrames - 30], [0, 600], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const expenseX = interpolate(frame, [0, durationInFrames - 30], [0, 860], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — teal-dark, fresh vs danger */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'linear-gradient(135deg, #0A1628 0%, #1C1C1C 50%, #1A0A00 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <ellipse
            cx="960" cy="900"
            rx={700 + Math.sin(frame * 0.02) * 20}
            ry="120"
            fill="none"
            stroke="#e9d700"
            strokeWidth="0.8"
            opacity={0.06}
          />
          <rect
            x="1500" y="50"
            width="350" height="350"
            fill="#ff0000"
            opacity={0.03}
            transform={`rotate(${frame * 0.08} 1675 225)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — horizontal race track lines */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {[400, 500, 600, 700].map((y, i) => (
            <line key={i} x1="0" y1={y} x2="1920" y2={y} stroke="#F5F5F5" strokeWidth="1" strokeDasharray="20 10" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: p.x + Math.sin(frame * p.speed + i) * 10,
            top: p.y + Math.cos(frame * p.speed + i) * 8,
            width: p.size, height: p.size,
            borderRadius: i % 3 === 0 ? '0%' : '50%',
            backgroundColor: i % 2 === 0 ? '#e9d700' : '#ff0000',
            opacity: p.op,
          }} />
        ))}
      </AbsoluteFill>

      {/* RACE visualization — top half */}
      <AbsoluteFill>
        {/* Income racer */}
        <div style={{
          position: 'absolute',
          left: 100 + incomeX,
          top: 200,
          opacity: sp(0),
        }}>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 12,
          }}>
            <div style={{
              width: 56, height: 56,
              borderRadius: '50%',
              backgroundColor: '#e9d700',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}>
              <svg width="28" height="28" viewBox="0 0 28 28">
                <path d="M14 4 L14 24 M6 12 L14 4 L22 12" stroke="#1C1C1C" strokeWidth="3" fill="none" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
            <span style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 800,
              color: '#e9d700',
            }}>Income</span>
          </div>
        </div>

        {/* Expense racer — faster */}
        <div style={{
          position: 'absolute',
          left: 100 + expenseX,
          top: 310,
          opacity: sp(10),
        }}>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 12,
          }}>
            <div style={{
              width: 56, height: 56,
              borderRadius: '50%',
              backgroundColor: '#ff0000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}>
              <svg width="28" height="28" viewBox="0 0 28 28">
                <path d="M14 24 L14 4 M6 16 L14 24 L22 16" stroke="#F5F5F5" strokeWidth="3" fill="none" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
            <span style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 800,
              color: '#ff0000',
            }}>Expenses</span>
          </div>
        </div>

        {/* Speed indicator */}
        {frame > 60 && (
          <div style={{
            position: 'absolute',
            right: 120,
            top: 280,
            opacity: interpolate(frame, [60, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#ff0000',
              fontWeight: 700,
              letterSpacing: 2,
            }}>
              खर्च 1.5× तेज ↑
            </div>
          </div>
        )}
      </AbsoluteFill>

      {/* Bottom bar chart comparison */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          bottom: 150,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          alignItems: 'flex-end',
          gap: 40,
        }}>
          {/* Income bar */}
          <div style={{ textAlign: 'center' }}>
            <div style={{
              width: 110,
              height: incomeH,
              backgroundColor: '#e9d700',
              borderRadius: '8px 8px 0 0',
              transition: 'height 0.1s',
            }} />
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              fontWeight: 700,
              color: '#e9d700',
              marginTop: 10,
            }}>Income</div>
          </div>

          {/* Expense bar */}
          <div style={{ textAlign: 'center' }}>
            <div style={{
              width: 110,
              height: Math.min(expenseH, 420),
              backgroundColor: '#ff0000',
              borderRadius: '8px 8px 0 0',
            }} />
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              fontWeight: 700,
              color: '#ff0000',
              marginTop: 10,
            }}>खर्च</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Main Text */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 120,
          bottom: 80,
          opacity: sp(55),
          transform: `translateY(${(1 - sp(55)) * 30}px)`,
          maxWidth: 860,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 58,
            fontWeight: 800,
            color: '#F5F5F5',
            lineHeight: 1.3,
          }}>
            <span style={{ color: '#e9d700' }}>Income</span> बढ़ती है,
            <span style={{ color: '#ff0000' }}> खर्च </span>
            और तेज बढ़ता है।
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 75,
          bottom: 45,
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 50}px)`,
        }}>
          <svg width="80" height="148" viewBox="0 0 80 148">
            <circle cx="40" cy="25" r="26" fill="#e9d700" opacity={0.07 + Math.sin(frame * 0.06) * 0.03} />
            <circle cx="40" cy="25" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 40 25)`} />
            <circle cx="33" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="47" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="38" y1="24" x2="42" y2="24" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="40" y1="43" x2="40" y2={98 + Math.sin(frame * 0.05) * 2} stroke="#F5F5F5" strokeWidth="2.3" />
            <line x1="40" y1="60" x2="12" y2="48" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="40" y1="60" x2={68 + Math.sin(frame * 0.07) * 4} y2={48 + Math.sin(frame * 0.07) * 4} stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="40" y1="98" x2="20" y2="138" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="40" y1="98" x2="60" y2="138" stroke="#F5F5F5" strokeWidth="2.1" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.72) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S3_IncomeBadhtaHai;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S4_EMIWali',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S4_EMIWali: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Items cascade in one by one
  const items = [
    { label: 'EMI गाड़ी', icon: 'car', delay: 5, color: '#ff0000' },
    { label: 'बड़ा फ्लैट', icon: 'home', delay: 20, color: '#e9d700' },
    { label: 'Foreign Vacation', icon: 'plane', delay: 35, color: '#FFC107' },
    { label: 'महंगा School', icon: 'school', delay: 50, color: '#ff0000' },
    { label: 'नया Gadget', icon: 'phone', delay: 65, color: '#9E9E9E' },
  ];

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 8) * 1920,
    y: seededRandom(i * 6) * 1080,
    size: seededRandom(i * 4) * 5 + 2,
    op: seededRandom(i * 15) * 0.05 + 0.02,
    speed: seededRandom(i * 12) * 0.04 + 0.02,
  }));

  // Cascading weight visual
  const weightPull = interpolate(frame, [30, 120], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — charcoal to deep maroon */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'linear-gradient(160deg, #1C1C1C 0%, #2D2D2D 40%, #1A0A00 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <rect x="1300" y="0" width="620" height="1080" fill="#ff0000" opacity={0.02} />
          <circle cx="200" cy="200" r={180 + Math.sin(frame * 0.03) * 10} fill="none" stroke="#e9d700" strokeWidth="0.8" opacity={0.05} />
          <line x1="960" y1="0" x2="960" y2="1080" stroke="#9E9E9E" strokeWidth="0.5" opacity={0.05} />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — diagonal crosshatch */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 15 }, (_, i) => (
            <React.Fragment key={i}>
              <line x1={i * 140} y1="0" x2={i * 140 + 500} y2="1080" stroke="#ff0000" strokeWidth="1" />
              <line x1={i * 140 + 500} y1="0" x2={i * 140} y2="1080" stroke="#9E9E9E" strokeWidth="0.5" />
            </React.Fragment>
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: p.x + Math.sin(frame * p.speed + i) * 10,
            top: p.y + Math.cos(frame * p.speed + i) * 8,
            width: p.size, height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#ff0000' : i % 3 === 1 ? '#e9d700' : '#F5F5F5',
            opacity: p.op,
          }} />
        ))}
      </AbsoluteFill>

      {/* Left side — headline */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 100,
          top: 80,
          opacity: sp(0),
          transform: `translateX(${(1 - sp(0)) * -60}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 19,
            color: '#9E9E9E',
            letterSpacing: 3,
            fontWeight: 600,
          }}>
            // हर Income Hike के साथ...
          </div>
        </div>

        <div style={{
          position: 'absolute',
          left: 100,
          top: 130,
          opacity: sp(5),
          transform: `translateY(${(1 - sp(5)) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 110,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
          }}>
            एक नया
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 110,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 1,
          }}>
            खर्च जुड़ा।
          </div>
        </div>
      </AbsoluteFill>

      {/* Right side — cascading expense cards */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 100,
          top: 80,
          display: 'flex',
          flexDirection: 'column',
          gap: 16,
        }}>
          {items.map((item, i) => {
            const itemSp = sp(item.delay);
            const icons: Record<string, React.ReactNode> = {
              car: <svg width="32" height="32" viewBox="0 0 32 32"><path d="M4 20 L6 12 L10 8 L22 8 L26 12 L28 20 L28 24 L4 24 Z" fill="none" stroke={item.color} strokeWidth="2" /><circle cx="10" cy="24" r="3" fill={item.color} /><circle cx="22" cy="24" r="3" fill={item.color} /></svg>,
              home: <svg width="32" height="32" viewBox="0 0 32 32"><path d="M16 4 L28 16 L24 16 L24 28 L8 28 L8 16 L4 16 Z" fill="none" stroke={item.color} strokeWidth="2" /></svg>,
              plane: <svg width="32" height="32" viewBox="0 0 32 32"><path d="M6 20 L16 8 L26 20 L20 18 L18 26 L14 26 L12 18 Z" fill="none" stroke={item.color} strokeWidth="2" /></svg>,
              school: <svg width="32" height="32" viewBox="0 0 32 32"><rect x="6" y="12" width="20" height="16" fill="none" stroke={item.color} strokeWidth="2" /><path d="M2 14 L16 4 L30 14" fill="none" stroke={item.color} strokeWidth="2" /><rect x="13" y="20" width="6" height="8" fill={item.color} opacity={0.4} /></svg>,
              phone: <svg width="32" height="32" viewBox="0 0 32 32"><rect x="10" y="2" width="12" height="28" rx="3" fill="none" stroke={item.color} strokeWidth="2" /><circle cx="16" cy="26" r="1.5" fill={item.color} /></svg>,
            };

            return (
              <div key={i} style={{
                display: 'flex',
                alignItems: 'center',
                gap: 16,
                opacity: itemSp,
                transform: `translateX(${(1 - itemSp) * 80}px)`,
                backgroundColor: 'rgba(255,255,255,0.04)',
                borderLeft: `4px solid ${item.color}`,
                padding: '12px 24px',
                borderRadius: '0 8px 8px 0',
                minWidth: 360,
              }}>
                {icons[item.icon]}
                <span style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 38,
                  fontWeight: 700,
                  color: '#F5F5F5',
                }}>
                  {item.label}
                </span>
                <span style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 22,
                  color: item.color,
                  fontWeight: 700,
                  marginLeft: 'auto',
                }}>
                  + EMI
                </span>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Weight chain visual — pulling down */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 430,
          bottom: 80,
          opacity: interpolate(frame, [80, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <svg width="200" height="120" viewBox="0 0 200 120">
            <line x1="100" y1="0" x2="100" y2={80 * weightPull} stroke="#9E9E9E" strokeWidth="3" strokeDasharray="8 4" />
            <circle cx="100" cy={80 * weightPull} r={20 * weightPull} fill="#ff0000" opacity={0.7} />
            <text x="100" y={80 * weightPull + 7} textAnchor="middle" fill="#F5F5F5" fontSize="16" fontWeight="700" fontFamily="Inter, Poppins">
              DEBT
            </text>
          </svg>
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 80,
          bottom: 50,
          opacity: sp(70),
          transform: `translateY(${(1 - sp(70)) * 50}px) translateY(${weightPull * 8}px)`,
        }}>
          <svg width="85" height="155" viewBox="0 0 85 155">
            <circle cx="42" cy="26" r="28" fill="#e9d700" opacity={0.07 + Math.sin(frame * 0.05) * 0.03} />
            <circle cx="42" cy="26" r="19" fill="none" stroke="#F5F5F5" strokeWidth="2.4"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 42 26)`} />
            <circle cx="35" cy="25" r="5.5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="49" cy="25" r="5.5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="40.5" y1="25" x2="43.5" y2="25" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="42" y1="45" x2="42" y2={100 + Math.sin(frame * 0.05) * 2} stroke="#F5F5F5" strokeWidth="2.4" />
            {/* arms drooping down under weight */}
            <line x1="42" y1="62" x2={16 + weightPull * 6} y2={78 + weightPull * 10} stroke="#F5F5F5" strokeWidth="2.2" />
            <line x1="42" y1="62" x2={68 - weightPull * 6} y2={78 + weightPull * 10} stroke="#F5F5F5" strokeWidth="2.2" />
            <line x1="42" y1="100" x2="22" y2="143" stroke="#F5F5F5" strokeWidth="2.2" />
            <line x1="42" y1="100" x2="62" y2="143" stroke="#F5F5F5" strokeWidth="2.2" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.72) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S4_EMIWali;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S5_PachasLakh',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S5_PachasLakh: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Wallet emptying animation
  const walletDrain = interpolate(frame, [20, 110], [1, 0.02], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.in(Easing.quad),
  });

  // Shock reveal
  const shockScale = sp(15, { damping: 6, stiffness: 200 });

  const particles = Array.from({ length: 15 }, (_, i) => ({
    x: seededRandom(i * 9) * 1920,
    y: seededRandom(i * 5) * 1080,
    size: seededRandom(i * 3) * 4 + 2,
    op: seededRandom(i * 17) * 0.04 + 0.02,
    speed: seededRandom(i * 11) * 0.04 + 0.02,
  }));

  // Coin fall particles
  const coinParticles = Array.from({ length: 8 }, (_, i) => ({
    startX: 760 + i * 30,
    delay: i * 8,
    speed: 2.5 + i * 0.3,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — deep green wealth fading to dark */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: `radial-gradient(ellipse at 50% 40%, rgba(${Math.round(20 * walletDrain)},${Math.round(60 * walletDrain)},${Math.round(20 * walletDrain)},1) 0%, #1C1C1C 60%)`,
        }} />
      </AbsoluteFill>

      {/* Shape Layer */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle cx="960" cy="480" r={300 + Math.sin(frame * 0.025) * 15} fill="none" stroke="#e9d700" strokeWidth="1" opacity={0.05} />
          <circle cx="960" cy="480" r={200 + Math.sin(frame * 0.03) * 10} fill="none" stroke="#e9d700" strokeWidth="0.5" opacity={0.04} />
          <rect x="0" y="900" width="1920" height="180" fill="#ff0000" opacity={0.03} />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — coin/rupee symbols floating */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 10 }, (_, i) => (
            <text key={i}
              x={seededRandom(i * 7) * 1800}
              y={seededRandom(i * 11) * 1000 + 40}
              fontSize="60"
              fontFamily="Inter, Poppins"
              fill="#e9d700"
            >₹</text>
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: p.x + Math.sin(frame * p.speed + i) * 10,
            top: p.y + Math.cos(frame * p.speed + i) * 8,
            width: p.size, height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#e9d700' : '#9E9E9E',
            opacity: p.op * walletDrain,
          }} />
        ))}
      </AbsoluteFill>

      {/* CENTER — Big Salary Figure draining */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: '50%',
          top: '18%',
          transform: `translateX(-50%) scale(${shockScale})`,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 34,
            color: '#9E9E9E',
            fontWeight: 600,
            letterSpacing: 2,
            marginBottom: 8,
          }}>
            ANNUAL INCOME
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 200,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1,
            letterSpacing: -6,
            textShadow: `0 0 ${60 * walletDrain}px #e9d70066`,
          }}>
            ₹50L+
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 48,
            fontWeight: 600,
            color: '#9E9E9E',
            marginTop: 8,
            opacity: sp(10),
          }}>
            सालाना कमाई
          </div>
        </div>
      </AbsoluteFill>

      {/* Wallet empty at month end */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: '50%',
          bottom: 180,
          transform: 'translateX(-50%)',
          textAlign: 'center',
          opacity: sp(25),
        }}>
          {/* Wallet SVG */}
          <svg width="220" height="140" viewBox="0 0 220 140">
            <rect x="10" y="30" width="200" height="100" rx="12" fill="none" stroke="#9E9E9E" strokeWidth="3" />
            <rect x="10" y="30" width="200" height={100 * walletDrain} rx="12" fill="#e9d700" opacity={0.2} />
            <rect x="60" y="10" width="100" height="35" rx="8" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
            <circle cx="170" cy="80" r="18" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
            <circle cx="170" cy="80" r={18 * walletDrain} fill="#e9d700" opacity={0.5} />
          </svg>

          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 56,
            fontWeight: 900,
            color: '#ff0000',
            marginTop: 12,
            transform: `scale(${1 + (1 - walletDrain) * 0.1})`,
          }}>
            महीने के अंत में — खाली
          </div>
        </div>
      </AbsoluteFill>

      {/* Coin fall animation */}
      <AbsoluteFill>
        {coinParticles.map((c, i) => {
          const coinFrame = Math.max(0, frame - c.delay);
          const coinY = (coinFrame * c.speed) % 800;
          const opacity = coinY > 600 ? interpolate(coinY, [600, 800], [0.4, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) : 0.4;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: c.startX,
              top: 200 + coinY,
              opacity,
            }}>
              <svg width="24" height="24" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="10" fill="none" stroke="#e9d700" strokeWidth="2" />
                <text x="12" y="17" textAnchor="middle" fontSize="13" fill="#e9d700" fontFamily="Inter, Poppins" fontWeight="700">₹</text>
              </svg>
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 75,
          bottom: 50,
          opacity: sp(55),
          transform: `translateY(${(1 - sp(55)) * 50}px)`,
        }}>
          <svg width="82" height="150" viewBox="0 0 82 150">
            <circle cx="41" cy="25" r="26" fill="#e9d700" opacity={0.07 * walletDrain + 0.02} />
            <circle cx="41" cy="25" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 41 25)`} />
            <circle cx="34" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="48" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="39" y1="24" x2="43" y2="24" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="41" y1="43" x2="41" y2={97 + Math.sin(frame * 0.05) * 2} stroke="#F5F5F5" strokeWidth="2.3" />
            {/* arms — one checking empty pockets */}
            <line x1="41" y1="60" x2="14" y2="80" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="41" y1="60" x2="66" y2="72" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="41" y1="97" x2="21" y2="138" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="41" y1="97" x2="61" y2="138" stroke="#F5F5F5" strokeWidth="2.1" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.78) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S5_PachasLakh;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S6_PayYourselfFirst',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S6_PayYourselfFirst: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pie chart animation
  const pieProgress = interpolate(frame, [20, 90], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // 50% arc
  const halfArcAngle = pieProgress * 180; // 180 degrees = 50%
  const arcRad = (halfArcAngle * Math.PI) / 180;
  const arcX = 200 + 160 * Math.sin(arcRad);
  const arcY = 200 - 160 * Math.cos(arcRad);
  const largeArc = halfArcAngle > 180 ? 1 : 0;

  // Transfer animation
  const transferX = interpolate(frame, [60, 110], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 6) * 1920,
    y: seededRandom(i * 8) * 1080,
    size: seededRandom(i * 4) * 5 + 2,
    op: seededRandom(i * 14) * 0.04 + 0.02,
    speed: seededRandom(i * 10) * 0.04 + 0.02,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — positive, empowering green-black */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 40% 50%, #0A1F0A 0%, #1C1C1C 55%, #1C1C1C 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle cx="300" cy="540" r={280 + Math.sin(frame * 0.02) * 12} fill="none" stroke="#e9d700" strokeWidth="1" opacity={0.06} />
          <rect x="1400" y="200" width="500" height="680" fill="#e9d700" opacity={0.02} rx="16" />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — flow lines */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 8 }, (_, i) => (
            <path
              key={i}
              d={`M 0 ${200 + i * 100} Q 480 ${200 + i * 100 + 30} 960 ${200 + i * 100} Q 1440 ${200 + i * 100 - 30} 1920 ${200 + i * 100}`}
              fill="none"
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: p.x + Math.sin(frame * p.speed + i) * 12,
            top: p.y + Math.cos(frame * p.speed + i) * 8,
            width: p.size, height: p.size,
            borderRadius: '50%',
            backgroundColor: '#e9d700',
            opacity: p.op,
          }} />
        ))}
      </AbsoluteFill>

      {/* Left — Pie Chart */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 100,
          top: '50%',
          transform: `translateY(-50%) scale(${sp(8)})`,
          opacity: sp(8),
        }}>
          <svg width="400" height="400" viewBox="0 0 400 400">
            {/* Full circle background */}
            <circle cx="200" cy="200" r="160" fill="none" stroke="#2D2D2D" strokeWidth="3" />

            {/* 50% invest slice — yellow */}
            <path
              d={`M 200 200 L 200 40 A 160 160 0 ${largeArc} 1 ${arcX} ${arcY} Z`}
              fill="#e9d700"
              opacity={0.85}
            />

            {/* Remaining 50% — grey */}
            {pieProgress > 0 && (
              <path
                d={`M 200 200 L ${arcX} ${arcY} A 160 160 0 ${1 - largeArc} 1 200 40 Z`}
                fill="#2D2D2D"
                opacity={0.6}
              />
            )}

            {/* Center text */}
            <circle cx="200" cy="200" r="70" fill="#1C1C1C" />
            <text x="200" y="190" textAnchor="middle" fontSize="36" fontFamily="Inter, Poppins" fontWeight="900" fill="#e9d700">50%</text>
            <text x="200" y="218" textAnchor="middle" fontSize="18" fontFamily="Inter, Poppins" fontWeight="600" fill="#9E9E9E">INVEST</text>

            {/* Labels */}
            <text x="280" y="120" fontSize="22" fontFamily="Noto Sans Devanagari" fill="#e9d700" fontWeight="700" opacity={pieProgress}>
              निवेश →
            </text>
            <text x="80" y="300" fontSize="22" fontFamily="Noto Sans Devanagari" fill="#9E9E9E" fontWeight="600" opacity={pieProgress}>
              बाकी खर्च
            </text>
          </svg>
        </div>
      </AbsoluteFill>

      {/* Transfer animation — salary to investment */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: '50%',
          top: '30%',
          transform: 'translateX(-50%)',
          opacity: sp(45),
          display: 'flex',
          alignItems: 'center',
          gap: 30,
        }}>
          {/* Salary box */}
          <div style={{
            backgroundColor: '#2D2D2D',
            border: '2px solid #9E9E9E',
            borderRadius: 12,
            padding: '20px 32px',
            textAlign: 'center',
          }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 800, color: '#F5F5F5' }}>SALARY</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 900, color: '#e9d700' }}>₹</div>
          </div>

          {/* Arrow */}
          <div style={{
            position: 'relative',
            width: 200,
            height: 4,
            overflow: 'hidden',
            opacity: sp(55),
          }}>
            <div style={{
              position: 'absolute',
              left: 0,
              top: 0,
              width: `${transferX * 100}%`,
              height: '100%',
              backgroundColor: '#e9d700',
              borderRadius: 2,
            }} />
            {/* Arrowhead */}
            <div style={{
              position: 'absolute',
              right: 0,
              top: -8,
              opacity: transferX,
            }}>
              <svg width="20" height="20" viewBox="0 0 20 20">
                <path d="M0 10 L20 10 M12 2 L20 10 L12 18" stroke="#e9d700" strokeWidth="3" fill="none" strokeLinecap="round" />
              </svg>
            </div>
          </div>

          {/* Investment box */}
          <div style={{
            backgroundColor: '#e9d700',
            border: '2px solid #e9d700',
            borderRadius: 12,
            padding: '20px 32px',
            textAlign: 'center',
            opacity: sp(65),
            transform: `scale(${sp(65)})`,
          }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 800, color: '#1C1C1C' }}>INVEST</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 900, color: '#1C1C1C' }}>50%</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Main text — bottom */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 100,
          bottom: 130,
          maxWidth: 820,
          textAlign: 'right',
          opacity: sp(70),
          transform: `translateY(${(1 - sp(70)) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 62,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.25,
          }}>
            जो पैसा <span style={{ color: '#e9d700' }}>दिखता नहीं</span>,
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 62,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.25,
          }}>
            वो <span style={{ color: '#ff0000' }}>खर्च नहीं</span> होता।
          </div>
        </div>

        {/* Rule tag */}
        <div style={{
          position: 'absolute',
          right: 100,
          bottom: 70,
          opacity: sp(80),
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#9E9E9E',
            letterSpacing: 3,
          }}>
            // PAY YOURSELF FIRST RULE
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 520,
          bottom: 50,
          opacity: sp(60),
          transform: `translateY(${(1 - sp(60)) * 50}px)`,
        }}>
          <svg width="82" height="148" viewBox="0 0 82 148">
            <circle cx="41" cy="25" r="26" fill="#e9d700" opacity={0.1 + Math.sin(frame * 0.05) * 0.03} />
            <circle cx="41" cy="25" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 41 25)`} />
            <circle cx="34" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="48" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="39" y1="24" x2="43" y2="24" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="41" y1="43" x2="41" y2={97 + Math.sin(frame * 0.05) * 2} stroke="#F5F5F5" strokeWidth="2.3" />
            {/* arms up — thumbs up */}
            <line x1="41" y1="60" x2="12" y2="40" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="41" y1="60" x2="70" y2="40" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="41" y1="97" x2="21" y2="138" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="41" y1="97" x2="61" y2="138" stroke="#F5F5F5" strokeWidth="2.1" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.72) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S6_PayYourselfFirst;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S7_PehleDinHi',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S7_PehleDinHi: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Calendar animation — day 1 highlight
  const calendarReveal = interpolate(frame, [10, 50], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Impact pulse on "पहले दिन"
  const impactPulse = frame > 20 ? Math.sin((frame - 20) * 0.15) * 0.08 + 1 : 0;

  // Screen shake on impact
  const shakeX = frame > 18 && frame < 30 ? Math.sin(frame * 0.8) * 6 : 0;
  const shakeY = frame > 18 && frame < 30 ? Math.sin(frame * 1.1) * 4 : 0;

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 9) * 1920,
    y: seededRandom(i * 5) * 1080,
    size: seededRandom(i * 3) * 5 + 2,
    op: seededRandom(i * 17) * 0.05 + 0.02,
    speed: seededRandom(i * 11) * 0.04 + 0.02,
  }));

  // Calendar days
  const days = Array.from({ length: 31 }, (_, i) => i + 1);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — clean golden authority */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 60% 40%, #1F1600 0%, #1C1C1C 55%, #0E0E0E 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <rect
            x="900" y="80"
            width="900" height="920"
            rx="24"
            fill="#e9d700"
            opacity={0.02 + Math.sin(frame * 0.02) * 0.01}
          />
          <circle cx="250" cy="800" r={200 + Math.sin(frame * 0.025) * 10} fill="none" stroke="#e9d700" strokeWidth="0.8" opacity={0.05} />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — clock ticks */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 12 }, (_, i) => {
            const angle = (i / 12) * Math.PI * 2;
            const cx = 250, cy = 540, r = 160;
            return (
              <line
                key={i}
                x1={cx + (r - 20) * Math.cos(angle)}
                y1={cy + (r - 20) * Math.sin(angle)}
                x2={cx + r * Math.cos(angle)}
                y2={cy + r * Math.sin(angle)}
                stroke="#e9d700"
                strokeWidth="3"
              />
            );
          })}
          <circle cx="250" cy="540" r="160" fill="none" stroke="#e9d700" strokeWidth="1" />
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: p.x + Math.sin(frame * p.speed + i) * 10,
            top: p.y + Math.cos(frame * p.speed + i) * 8,
            width: p.size, height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#e9d700' : '#FFC107',
            opacity: p.op,
          }} />
        ))}
      </AbsoluteFill>

      {/* LEFT — Calendar visual */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 100,
          top: '50%',
          transform: `translateY(-50%) scale(${calendarReveal})`,
          opacity: calendarReveal,
        }}>
          <div style={{
            backgroundColor: '#2D2D2D',
            borderRadius: 20,
            padding: '24px 28px',
            width: 380,
            border: '1px solid #3D3D3D',
          }}>
            {/* Calendar header */}
            <div style={{
              backgroundColor: '#e9d700',
              borderRadius: '12px 12px 0 0',
              padding: '12px 0',
              marginBottom: 16,
              textAlign: 'center',
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 800,
              color: '#1C1C1C',
              margin: '-24px -28px 16px -28px',
              borderRadius: '20px 20px 0 0',
            }}>
              SALARY MONTH
            </div>
            {/* Days grid */}
            <div style={{
              display: 'grid',
              gridTemplateColumns: 'repeat(7, 1fr)',
              gap: 6,
            }}>
              {days.map((d) => (
                <div key={d} style={{
                  width: 42, height: 42,
                  borderRadius: d === 1 ? 8 : '50%',
                  backgroundColor: d === 1 ? '#e9d700' : 'transparent',
                  border: d === 1 ? 'none' : '1px solid #3D3D3D',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'Inter, Poppins',
                  fontSize: d === 1 ? 20 : 16,
                  fontWeight: d === 1 ? 900 : 400,
                  color: d === 1 ? '#1C1C1C' : '#9E9E9E',
                  transform: d === 1 ? `scale(${impactPulse})` : 'scale(1)',
                }}>
                  {d}
                </div>
              ))}
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Center & Right — Main message */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 560,
          top: '50%',
          transform: `translateY(-50%) translateX(${shakeX}px) translateY(${shakeY}px)`,
        }}>
          {/* "पहले दिन" — massive impact */}
          <div style={{
            fontFamily: 'Yantra One, Noto Sans Devanagari',
            fontSize: 240,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 0.9,
            transform: `scale(${sp(5)})`,
            opacity: sp(5),
            textShadow: `0 0 80px #e9d70044`,
          }}>
            पहले
          </div>
          <div style={{
            fontFamily: 'Yantra One, Noto Sans Devanagari',
            fontSize: 240,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 0.9,
            transform: `scale(${sp(12)})`,
            opacity: sp(12),
          }}>
            दिन
          </div>

          {/* Exclamation */}
          <div style={{
            display: 'flex',
            alignItems: 'baseline',
            gap: 8,
            marginTop: 20,
            opacity: sp(22),
            transform: `translateX(${(1 - sp(22)) * 40}px)`,
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 80,
              fontWeight: 900,
              color: '#ff0000',
            }}>ही!</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 44,
              fontWeight: 600,
              color: '#9E9E9E',
              paddingBottom: 8,
            }}>
              — बाद में मत सोचना
            </div>
          </div>

          {/* Bottom rule */}
          <div style={{
            marginTop: 30,
            opacity: sp(45),
            transform: `translateY(${(1 - sp(45)) * 20}px)`,
          }}>
            <div style={{
              width: `${sp(45) * 600}px`,
              height: 4,
              backgroundColor: '#e9d700',
              borderRadius: 2,
              marginBottom: 16,
            }} />
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 46,
              fontWeight: 700,
              color: '#F5F5F5',
            }}>
              <span style={{ color: '#e9d700' }}>Automate</span> करो — तुरंत।
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 75,
          bottom: 50,
          opacity: sp(55),
          transform: `translateY(${(1 - sp(55)) * 50}px)`,
        }}>
          <svg width="80" height="148" viewBox="0 0 80 148">
            <circle cx="40" cy="25" r="26" fill="#e9d700" opacity={0.1 + Math.sin(frame * 0.05) * 0.04} />
            <circle cx="40" cy="25" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 40 25)`} />
            <circle cx="33" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="47" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="38" y1="24" x2="42" y2="24" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="40" y1="43" x2="40" y2={96 + Math.sin(frame * 0.05) * 2} stroke="#F5F5F5" strokeWidth="2.3" />
            {/* Both arms pointing forward — go! */}
            <line x1="40" y1="60" x2="12" y2="50" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="40" y1="60" x2={68 + Math.sin(frame * 0.1) * 5} y2={50 + Math.sin(frame * 0.1) * 3} stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="40" y1="96" x2="20" y2="136" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="40" y1="96" x2="60" y2="136" stroke="#F5F5F5" strokeWidth="2.1" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 42%, rgba(0,0,0,0.72) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S7_PehleDinHi;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S8_PsychTrap',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S8_PsychTrap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Brain glitch animation
  const glitchOffset = frame > 30 && frame % 20 < 3 ? Math.random() * 8 - 4 : 0;
  const glitchColor = frame > 30 && frame % 20 < 2;

  // Chart data — income vs savings divergence
  const years = [2018, 2019, 2020, 2021, 2022, 2023, 2024];
  const incomeData = [100, 130, 160, 210, 270, 340, 420];
  const savingsData = [30, 35, 38, 42, 44, 45, 46];

  const chartProgress = interpolate(frame, [20, 100], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });

  const particles = Array.from({ length: 15 }, (_, i) => ({
    x: seededRandom(i * 6) * 1920,
    y: seededRandom(i * 9) * 1080,
    size: seededRandom(i * 4) * 5 + 2,
    op: seededRandom(i * 15) * 0.04 + 0.02,
    speed: seededRandom(i * 12) * 0.04 + 0.02,
  }));

  const chartW = 700;
  const chartH = 300;
  const maxIncome = 420;
  const maxSavings = 100;

  const incomePoints = incomeData.map((v, i) => {
    const x = (i / (incomeData.length - 1)) * chartW * chartProgress;
    const y = chartH - (v / maxIncome) * chartH;
    return `${x},${y}`;
  }).slice(0, Math.ceil(years.length * chartProgress) + 1).join(' ');

  const savingsPoints = savingsData.map((v, i) => {
    const x = (i / (savingsData.length - 1)) * chartW * chartProgress;
    const y = chartH - (v / maxIncome) * chartH;
    return `${x},${y}`;
  }).slice(0, Math.ceil(years.length * chartProgress) + 1).join(' ');

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — purple-dark psychological */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 50% 30%, #1A0A2E 0%, #1C1C1C 55%, #0E0E0E 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle cx="960" cy="540" r={400 + Math.sin(frame * 0.02) * 15} fill="none" stroke="#ff0000" strokeWidth="0.6" opacity={0.04} />
          <circle cx="960" cy="540" r={250 + Math.sin(frame * 0.03) * 10} fill="none" stroke="#9E9E9E" strokeWidth="0.4" opacity={0.04} />
          <polygon points="100,100 300,50 400,200 250,300 50,250" fill="none" stroke="#e9d700" strokeWidth="0.6" opacity={0.04}
            transform={`rotate(${frame * 0.04} 225 175)`} />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — brain wave lines */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
          {Array.from({ length: 5 }, (_, i) => (
            <path
              key={i}
              d={`M 0 ${300 + i * 80} ${Array.from({ length: 20 }, (_, j) => `Q ${j * 100 + 50} ${300 + i * 80 + (Math.sin(j * 1.2 + frame * 0.03 + i) * 25)} ${(j + 1) * 100} ${300 + i * 80}`).join(' ')}`}
              fill="none"
              stroke="#ff0000"
              strokeWidth="1.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: p.x + Math.sin(frame * p.speed + i) * 10 + (i === 3 ? glitchOffset : 0),
            top: p.y + Math.cos(frame * p.speed + i) * 8,
            width: p.size, height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#ff0000' : i % 3 === 1 ? '#e9d700' : '#9E9E9E',
            opacity: p.op,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main — PSYCHOLOGICAL TRAP label */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 100,
          top: 80,
          opacity: sp(0),
          transform: `translateX(${(1 - sp(0)) * -50}px) translateX(${glitchOffset}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            fontWeight: 800,
            letterSpacing: 5,
            color: glitchColor ? '#ff0000' : '#9E9E9E',
            textTransform: 'uppercase',
          }}>
            ⚠ PSYCHOLOGICAL TRAP
          </div>
        </div>

        {/* Big headline */}
        <div style={{
          position: 'absolute',
          left: 100,
          top: 140,
          maxWidth: 860,
          transform: `translateY(${(1 - sp(8)) * 40}px) translateX(${glitchOffset * 0.5}px)`,
          opacity: sp(8),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 100,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
          }}>
            <span style={{ color: '#ff0000' }}>Savings</span> उसी
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 100,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
          }}>
            अनुपात में नहीं बढ़ीं।
          </div>
        </div>
      </AbsoluteFill>

      {/* Chart — divergence visual */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 100,
          top: '50%',
          transform: `translateY(-50%) scale(${sp(15)})`,
          opacity: sp(15),
        }}>
          <svg width={chartW + 80} height={chartH + 80} viewBox={`-60 -20 ${chartW + 80} ${chartH + 80}`}>
            {/* Axes */}
            <line x1="0" y1={chartH} x2={chartW} y2={chartH} stroke="#9E9E9E" strokeWidth="1.5" opacity={0.4} />
            <line x1="0" y1="0" x2="0" y2={chartH} stroke="#9E9E9E" strokeWidth="1.5" opacity={0.4} />

            {/* Income line */}
            {incomePoints && (
              <polyline
                points={incomePoints}
                fill="none"
                stroke="#e9d700"
                strokeWidth="3.5"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
            )}

            {/* Savings line */}
            {savingsPoints && (
              <polyline
                points={savingsPoints}
                fill="none"
                stroke="#ff0000"
                strokeWidth="3.5"
                strokeLinecap="round"
                strokeLinejoin="round"
                strokeDasharray="8 4"
              />
            )}

            {/* Legend */}
            <rect x="0" y={-15} width="16" height="4" fill="#e9d700" rx="2" />
            <text x="22" y={-8} fontSize="18" fill="#e9d700" fontFamily="Inter, Poppins" fontWeight="700">Income ↑↑</text>
            <rect x="160" y={-15} width="16" height="4" fill="#ff0000" rx="2" />
            <text x="182" y={-8} fontSize="18" fill="#ff0000" fontFamily="Inter, Poppins" fontWeight="700">Savings →</text>

            {/* Gap annotation */}
            {chartProgress > 0.8 && (
              <>
                <line x1={chartW * 0.85} y1={chartH - (incomeData[5] / maxIncome) * chartH} x2={chartW * 0.85} y2={chartH - (savingsData[5] / maxIncome) * chartH} stroke="#FFC107" strokeWidth="2" strokeDasharray="4 2" />
                <text x={chartW * 0.87} y={chartH - ((incomeData[5] + savingsData[5]) / 2 / maxIncome) * chartH} fontSize="20" fill="#FFC107" fontFamily="Inter, Poppins" fontWeight="800">GAP!</text>
              </>
            )}
          </svg>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — confused */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 100,
          bottom: 50,
          opacity: sp(65),
          transform: `translateY(${(1 - sp(65)) * 50}px) translateX(${glitchOffset}px)`,
        }}>
          <svg width="82" height="148" viewBox="0 0 82 148">
            <circle cx="41" cy="25" r="26" fill="#ff0000" opacity={0.06 + Math.sin(frame * 0.07) * 0.03} />
            <circle cx="41" cy="25" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.3"
              transform={`rotate(${Math.sin(frame * 0.06) * 5} 41 25)`} />
            <circle cx="34" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="48" cy="24" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="39" y1="24" x2="43" y2="24" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="41" y1="43" x2="41" y2={97 + Math.sin(frame * 0.05) * 2} stroke="#F5F5F5" strokeWidth="2.3" />
            {/* arms raised in confusion */}
            <line x1="41" y1="60" x2={14 + Math.sin(frame * 0.1) * 4} y2={45 + Math.sin(frame * 0.1) * 4} stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="41" y1="60" x2={68 + Math.sin(frame * 0.12) * 4} y2={45 + Math.sin(frame * 0.12) * 4} stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="41" y1="97" x2="21" y2="138" stroke="#F5F5F5" strokeWidth="2.1" />
            <line x1="41" y1="97" x2="61" y2="138" stroke="#F5F5F5" strokeWidth="2.1" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.75) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S8_PsychTrap;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S9_DiversifyRule',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S9_DiversifyRule: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Portfolio slices
  const segments = [
    { label: 'Stock A', pct: 10, color: '#e9d700' },
    { label: 'Stock B', pct: 10, color: '#FFC107' },
    { label: 'Stock C', pct: 10, color: '#F5F5F5' },
    { label: 'Index', pct: 50, color: '#2D8C4E' },
    { label: 'Others', pct: 20, color: '#9E9E9E' },
  ];

  // Bad scenario — one stock takes over
  const badSegments = [
    { label: 'One Stock', pct: 70, color: '#ff0000' },
    { label: 'Rest', pct: 30, color: '#2D2D2D' },
  ];

  const chartReveal = interpolate(frame, [10, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 7) * 1920,
    y: seededRandom(i * 4) * 1080,
    size: seededRandom(i * 3) * 4 + 2,
    op: seededRandom(i * 18) * 0.04 + 0.02,
    speed: seededRandom(i * 13) * 0.04 + 0.02,
  }));

  // Build pie arcs
  const buildPieArcs = (segs: typeof segments, cx: number, cy: number, r: number, progress: number) => {
    const arcs: React.ReactNode[] = [];
    let cumAngle = -Math.PI / 2;
    const total = segs.reduce((s, sg) => s + sg.pct, 0);

    segs.forEach((seg, i) => {
      const pct = seg.pct / total;
      const sweep = pct * 2 * Math.PI * progress;
      const endAngle = cumAngle + sweep;
      const x1 = cx + r * Math.cos(cumAngle);
      const y1 = cy + r * Math.sin(cumAngle);
      const x2 = cx + r * Math.cos(endAngle);
      const y2 = cy + r * Math.sin(endAngle);
      const largeArc = sweep > Math.PI ? 1 : 0;

      if (sweep > 0.01) {
        arcs.push(
          <path
            key={i}
            d={`M ${cx} ${cy} L ${x1} ${y1} A ${r} ${r} 0 ${largeArc} 1 ${x2} ${y2} Z`}
            fill={seg.color}
            opacity={0.85}
            stroke="#1C1C1C"
            strokeWidth="2"
          />
        );
      }
      cumAngle = endAngle;
    });
    return arcs;
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — two tone split */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'linear-gradient(90deg, #0A1A0A 0%, #1C1C1C 50%, #1A0000 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <line x1="960" y1="0" x2="960" y2="1080" stroke="#9E9E9E" strokeWidth="0.8" opacity={0.08} strokeDasharray="12 8" />
          <circle cx="450" cy="480" r={220 + Math.sin(frame * 0.02) * 8} fill="none" stroke="#e9d700" strokeWidth="0.6" opacity={0.06} />
          <circle cx="1450" cy="540" r={220 + Math.sin(frame * 0.025) * 8} fill="none" stroke="#ff0000" strokeWidth="0.6" opacity={0.06} />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 10 }, (_, i) => (
            <circle key={i}
              cx={960 + Math.cos(i * 0.628 + frame * 0.01) * 350}
              cy={540 + Math.sin(i * 0.628 + frame * 0.01) * 200}
              r="4"
              fill="#e9d700"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: p.x + Math.sin(frame * p.speed + i) * 10,
            top: p.y + Math.cos(frame * p.speed + i) * 8,
            width: p.size, height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#e9d700' : '#ff0000',
            opacity: p.op,
          }} />
        ))}
      </AbsoluteFill>

      {/* LEFT — Good diversified portfolio */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          transform: `translateY(-50%) scale(${sp(5)})`,
          opacity: sp(5),
          textAlign: 'center',
        }}>
          <svg width="380" height="380" viewBox="0 0 380 380">
            {buildPieArcs(segments, 190, 190, 160, chartReveal)}
            {/* Center */}
            <circle cx="190" cy="190" r="55" fill="#1C1C1C" />
            <text x="190" y="186" textAnchor="middle" fontSize="18" fill="#e9d700" fontFamily="Inter, Poppins" fontWeight="800">10%</text>
            <text x="190" y="206" textAnchor="middle" fontSize="14" fill="#9E9E9E" fontFamily="Inter, Poppins">max/stock</text>
          </svg>

          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 32,
            fontWeight: 800,
            color: '#e9d700',
            marginTop: 10,
          }}>✓ Diversified Portfolio</div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            color: '#9E9E9E',
            marginTop: 4,
          }}>Safe & Balanced</div>
        </div>
      </AbsoluteFill>

      {/* Divider */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: '50%',
          top: 80,
          transform: 'translateX(-50%)',
          textAlign: 'center',
          opacity: sp(0),
        }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, color: '#9E9E9E', letterSpacing: 3 }}>VS</div>
        </div>
      </AbsoluteFill>

      {/* RIGHT — Concentrated bad portfolio */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 80,
          top: '50%',
          transform: `translateY(-50%) scale(${sp(20)})`,
          opacity: sp(20),
          textAlign: 'center',
        }}>
          <svg width="380" height="380" viewBox="0 0 380 380">
            {buildPieArcs(badSegments, 190, 190, 160, chartReveal)}
            {/* Center */}
            <circle cx="190" cy="190" r="55" fill="#1C1C1C" />
            <text x="190" y="186" textAnchor="middle" fontSize="18" fill="#ff0000" fontFamily="Inter, Poppins" fontWeight="800">70%!</text>
            <text x="190" y="206" textAnchor="middle" fontSize="14" fill="#9E9E9E" fontFamily="Inter, Poppins">1 stock</text>
          </svg>

          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 32,
            fontWeight: 800,
            color: '#ff0000',
            marginTop: 10,
          }}>✗ Concentrated = खतरा</div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            color: '#9E9E9E',
            marginTop: 4,
          }}>High Risk</div>
        </div>
      </AbsoluteFill>

      {/* Bottom Rule */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(65)) * 30}px)`,
          opacity: sp(65),
          textAlign: 'center',
          whiteSpace: 'nowrap',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 52,
            fontWeight: 900,
            color: '#e9d700',
            letterSpacing: -1,
          }}>
            NIFTY 50 INDEX FUND
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 38,
            fontWeight: 600,
            color: '#F5F5F5',
            marginTop: 4,
          }}>
            = Automatic Diversification
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: '50%',
          bottom: 50,
          transform: `translateX(-50%) scale(${sp(55)})`,
          opacity: sp(55),
        }}>
          <svg width="78" height="142" viewBox="0 0 78 142">
            <circle cx="39" cy="24" r="24" fill="#e9d700" opacity={0.09 + Math.sin(frame * 0.05) * 0.03} />
            <circle cx="39" cy="24" r="17" fill="none" stroke="#F5F5F5" strokeWidth="2.2"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 39 24)`} />
            <circle cx="32" cy="23" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <circle cx="46" cy="23" r="5" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="37" y1="23" x2="41" y2="23" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="39" y1="41" x2="39" y2={93 + Math.sin(frame * 0.05) * 2} stroke="#F5F5F5" strokeWidth="2.2" />
            <line x1="39" y1="58" x2="12" y2="42" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="39" y1="58" x2="66" y2="42" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="39" y1="93" x2="19" y2="132" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="39" y1="93" x2="59" y2="132" stroke="#F5F5F5" strokeWidth="2" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.72) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S9_DiversifyRule;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C2S10_DiversificationShield',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C2S10_DiversificationShield: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Shield impact animation
  const shieldScale = sp(15, { damping: 8, stiffness: 180 });
  const impactRipple = interpolate(frame, [40, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });
  const rippleR = impactRipple * 200;
  const rippleOp = 1 - impactRipple;

  // Lightning bolts hitting shield
  const boltTiming = frame > 35;
  const boltOpacity = boltTiming ? Math.max(0, Math.sin((frame - 35) * 0.4) * 0.7) : 0;

  // Wealth growth line
  const wealthProgress = interpolate(frame, [60, durationInFrames - 20], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 8) * 1920,
    y: seededRandom(i * 6) * 1080,
    size: seededRandom(i * 3) * 5 + 2,
    op: seededRandom(i * 14) * 0.05 + 0.02,
    speed: seededRandom(i * 11) * 0.04 + 0.02,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — epic gold-dark conclusion */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at 50% 50%, #1F1600 0%, #1C1C1C 50%, #0A0A0A 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — radiating lines from center */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 12 }, (_, i) => {
            const angle = (i / 12) * Math.PI * 2 + frame * 0.005;
            return (
              <line
                key={i}
                x1="960" y1="540"
                x2={960 + Math.cos(angle) * 800}
                y2={540 + Math.sin(angle) * 600}
                stroke="#e9d700"
                strokeWidth="0.6"
                opacity={0.04}
              />
            );
          })}
          <circle cx="960" cy="540" r={350 + Math.sin(frame * 0.02) * 12} fill="none" stroke="#e9d700" strokeWidth="0.8" opacity={0.06} />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — hexagonal grid (shield pattern) */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 8 }, (_, row) =>
            Array.from({ length: 12 }, (_, col) => {
              const x = col * 120 + (row % 2) * 60;
              const y = row * 104;
              return (
                <polygon
                  key={`${row}-${col}`}
                  points={`${x + 60},${y} ${x + 90},${y + 52} ${x + 60},${y + 104} ${x},${y + 104} ${x - 30},${y + 52} ${x},${y}`}
                  fill="none"
                  stroke="#e9d700"
                  strokeWidth="0.8"
                />
              );
            })
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: p.x + Math.sin(frame * p.speed + i) * 12,
            top: p.y + Math.cos(frame * p.speed + i) * 8,
            width: p.size, height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#FFC107' : '#F5F5F5',
            opacity: p.op,
          }} />
        ))}
      </AbsoluteFill>

      {/* CENTER — Massive Shield */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -60%) scale(${shieldScale})`,
          opacity: shieldScale,
        }}>
          <svg width="360" height="400" viewBox="0 0 360 400">
            {/* Shield glow */}
            <ellipse cx="180" cy="200" rx="160" ry="180" fill="#e9d700" opacity={0.06 + Math.sin(frame * 0.05) * 0.03} />

            {/* Shield body */}
            <path
              d="M180 20 L340 90 L340 230 Q340 350 180 400 Q20 350 20 230 L20 90 Z"
              fill="none"
              stroke="#e9d700"
              strokeWidth="5"
              opacity={0.9}
            />
            <path
              d="M180 50 L310 110 L310 230 Q310 330 180 375 Q50 330 50 230 L50 110 Z"
              fill="#e9d700"
              opacity={0.12}
            />

            {/* Ripple on impact */}
            <circle cx="180" cy="200" r={rippleR} fill="none" stroke="#e9d700" strokeWidth="3" opacity={rippleOp * 0.6} />

            {/* Center text */}
            <text x="180" y="190" textAnchor="middle" fontSize="32" fontFamily="Inter, Poppins" fontWeight="900" fill="#e9d700">DIVERSI</text>
            <text x="180" y="228" textAnchor="middle" fontSize="32" fontFamily="Inter, Poppins" fontWeight="900" fill="#e9d700">FICATION</text>
          </svg>
        </div>
      </AbsoluteFill>

      {/* Lightning bolts hitting shield */}
      {boltTiming && (
        <AbsoluteFill>
          <svg width="1920" height="1080" style={{ position: 'absolute', opacity: boltOpacity }}>
            {/* Left bolt */}
            <path d="M200 300 L450 480 L380 480 L600 700" stroke="#ff0000" strokeWidth="4" fill="none" strokeLinecap="round" />
            {/* Right bolt */}
            <path d="M1720 280 L1470 460 L1540 460 L1320 680" stroke="#ff0000" strokeWidth="4" fill="none" strokeLinecap="round" />
            {/* Top bolt */}
            <path d="M960 80 L940 320 L960 320 L940 520" stroke="#ff0000" strokeWidth="3" fill="none" strokeLinecap="round" />
          </svg>
        </AbsoluteFill>
      )}

      {/* Left text */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          transform: `translateY(-50%) translateX(${(1 - sp(5)) * -60}px)`,
          opacity: sp(5),
          maxWidth: 500,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 80,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
          }}>
            Long Term
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 80,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1.1,
          }}>
            Wealth
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 80,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
          }}>
            Protection
          </div>
          <div style={{
            marginTop: 20,
            width: `${sp(30) * 400}px`,
            height: 4,
            backgroundColor: '#e9d700',
            borderRadius: 2,
          }} />
        </div>
      </AbsoluteFill>

      {/* Right text */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 80,
          bottom: 160,
          maxWidth: 440,
          textAlign: 'right',
          opacity: sp(70),
          transform: `translateY(${(1 - sp(70)) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            fontWeight: 700,
            color: '#9E9E9E',
            lineHeight: 1.3,
          }}>
            बड़े <span style={{ color: '#ff0000' }}>झटकों</span> से
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            fontWeight: 700,
            color: '#9E9E9E',
            lineHeight: 1.3,
          }}>
            बचाने वाली
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 68,
            fontWeight: 900,
            color: '#e9d700',
          }}>
            SHIELD
          </div>
        </div>
      </AbsoluteFill>

      {/* Bottom CTA */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(90)) * 20}px)`,
          opacity: sp(90),
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 24,
            color: '#9E9E9E',
            letterSpacing: 4,
          }}>
            // Comment करो — कौन सा trap सबसे ज्यादा relatable है?
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — confident stance */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 80,
          bottom: 110,
          opacity: sp(80),
          transform: `translateY(${(1 - sp(80)) * 40}px)`,
        }}>
          <svg width="84" height="152" viewBox="0 0 84 152">
            {/* Extra glow for finale */}
            <circle cx="42" cy="26" r="34" fill="#e9d700" opacity={0.12 + Math.sin(frame * 0.06) * 0.04} />
            <circle cx="42" cy="26" r="19" fill="none" stroke="#F5F5F5" strokeWidth="2.5"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 42 26)`} />
            <circle cx="35" cy="25" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2" />
            <circle cx="49" cy="25" r="5.5" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="40" y1="25" x2="44" y2="25" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="42" y1="45" x2="42" y2={100 + Math.sin(frame * 0.05) * 2} stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Victory pose — both arms up */}
            <line x1="42" y1="62" x2="12" y2="35" stroke="#F5F5F5" strokeWidth="2.3" />
            <line x1="42" y1="62" x2="72" y2="35" stroke="#F5F5F5" strokeWidth="2.3" />
            <line x1="42" y1="100" x2="22" y2="142" stroke="#F5F5F5" strokeWidth="2.3" />
            <line x1="42" y1="100" x2="62" y2="142" stroke="#F5F5F5" strokeWidth="2.3" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.78) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C2S10_DiversificationShield;
```

