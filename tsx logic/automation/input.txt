```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11VaporizationForecast',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11VaporizationForecast: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const gauge = interpolate(frame, [18, 118], [0, 50], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const unemployment = interpolate(frame, [125, 210], [10, 20], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const camera = interpolate(frame, [0, durationInFrames], [1.04, 1.13]);
  const orbit = interpolate(frame, [0, durationInFrames], [-8, 8]);
  const impact = frame > 102 && frame < 126 ? Math.sin(frame * 1.6) * 7 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 18% 18%, rgba(244,242,242,0.42), transparent 28%), radial-gradient(circle at 82% 72%, rgba(29,33,40,0.28), transparent 36%), linear-gradient(135deg, #FF9E20 0%, #F97812 58%, #C94B0A 100%)',
          transform: `scale(${camera}) translate(${impact}px, ${impact * -0.4}px)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.16,
          backgroundImage:
            'linear-gradient(rgba(29,33,40,0.22) 2px, transparent 2px), linear-gradient(90deg, rgba(29,33,40,0.22) 2px, transparent 2px)',
          backgroundSize: '76px 76px',
          transform: `translateX(${interpolate(frame, [0, durationInFrames], [0, -76])}px) rotate(${orbit * 0.15}deg)`,
        }}
      />

      {[
        { w: 640, h: 640, l: -160, t: 150, r: 45, b: 0.04, br: '50%' },
        { w: 760, h: 360, l: 1210, t: -80, r: -18, b: 0.05, br: 80 },
        { w: 430, h: 430, l: 1430, t: 690, r: 38, b: 0.06, br: 0 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            width: s.w,
            height: s.h,
            left: s.l,
            top: s.t,
            borderRadius: s.br,
            border: '6px solid #1D2128',
            opacity: s.b,
            transform: `rotate(${s.r + Math.sin(frame / 55 + i) * 9}deg) scale(${1 + Math.sin(frame / 45 + i) * 0.035})`,
          }}
        />
      ))}

      {Array.from({ length: 34 }).map((_, i) => {
        const x = seededRandom(i + 21) * 1920;
        const y = (seededRandom(i + 61) * 1080 + frame * (0.22 + seededRandom(i) * 0.65)) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 5 + seededRandom(i + 7) * 9,
              height: 5 + seededRandom(i + 8) * 9,
              borderRadius: seededRandom(i) > 0.45 ? '50%' : 2,
              background: i % 3 === 0 ? '#F4F2F2' : '#1D2128',
              opacity: 0.15 + seededRandom(i + 11) * 0.22,
            }}
          />
        );
      })}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `translateY(${interpolate(frame, [0, durationInFrames], [22, -18])}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 92,
            color: '#1D2128',
            fontFamily: '"Mont Heavy", Inter, sans-serif',
            fontSize: 92,
            letterSpacing: -4,
            transform: `translateY(${(1 - sp(4)) * 60}px)`,
            opacity: sp(4),
          }}
        >
          ENTRY-LEVEL WIPEOUT
        </div>

        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 212,
            width: 840,
            height: 640,
            borderRadius: 58,
            background: 'rgba(244,242,242,0.88)',
            boxShadow: '0 42px 100px rgba(29,33,40,0.26)',
            transform: `scale(${0.86 + sp(18) * 0.14}) rotate(${-2 + sp(18) * 2}deg)`,
            transformOrigin: 'center',
          }}
        >
          <svg width="840" height="640" viewBox="0 0 840 640">
            <path d="M140 470 A280 280 0 0 1 700 470" fill="none" stroke="#1D2128" strokeWidth="48" opacity="0.16" strokeLinecap="round" />
            <path
              d="M140 470 A280 280 0 0 1 700 470"
              fill="none"
              stroke="#ff0000"
              strokeWidth="48"
              strokeLinecap="round"
              strokeDasharray={`${gauge * 8.8} 900`}
            />
            <line
              x1="420"
              y1="470"
              x2={420 + Math.cos((-180 + gauge * 3.6) * Math.PI / 180) * 230}
              y2={470 + Math.sin((-180 + gauge * 3.6) * Math.PI / 180) * 230}
              stroke="#1D2128"
              strokeWidth="18"
              strokeLinecap="round"
            />
            <circle cx="420" cy="470" r="24" fill="#1D2128" />
            <text x="420" y="328" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="138" fill="#1D2128">{Math.round(gauge)}%</text>
            <text x="420" y="390" textAnchor="middle" fontFamily="Inter" fontSize="38" fontWeight="800" fill="#C94B0A">JOB VAPORIZATION RISK</text>
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 118,
            top: 192,
            width: 620,
            height: 640,
            borderRadius: 52,
            background: '#1D2128',
            color: '#F4F2F2',
            overflow: 'hidden',
            boxShadow: '0 42px 110px rgba(29,33,40,0.36)',
            transform: `translateX(${(1 - sp(38)) * 90}px)`,
            opacity: sp(38),
          }}
        >
          <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 80% 20%, rgba(255,158,32,0.38), transparent 36%)' }} />
          <div style={{ position: 'absolute', left: 48, top: 44, fontFamily: '"Mont Heavy", Inter', fontSize: 58, letterSpacing: -2 }}>
            FIVE-YEAR FORECAST
          </div>

          {Array.from({ length: 5 }).map((_, i) => {
            const y = 150 + i * 82;
            const fill = interpolate(frame, [60 + i * 16, 110 + i * 16], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <div key={i} style={{ position: 'absolute', left: 54, top: y, width: 500, height: 48 }}>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#FFCB82', marginBottom: 8 }}>YEAR {i + 1}</div>
                <div style={{ width: 500, height: 18, background: 'rgba(244,242,242,0.16)', borderRadius: 20, overflow: 'hidden' }}>
                  <div style={{ width: `${fill * (34 + i * 13)}%`, height: '100%', background: i > 2 ? '#ff0000' : '#FF9E20', borderRadius: 20 }} />
                </div>
              </div>
            );
          })}

          <div
            style={{
              position: 'absolute',
              left: 54,
              bottom: 44,
              fontFamily: '"Mont Heavy", Inter',
              fontSize: 82,
              color: '#F4F2F2',
              transform: `scale(${0.9 + sp(126) * 0.1})`,
            }}
          >
            {Math.round(unemployment)}%
          </div>
          <div style={{ position: 'absolute', left: 224, bottom: 68, fontFamily: 'Inter', fontWeight: 900, fontSize: 34, color: '#FFCB82' }}>
            UNEMPLOYMENT SHOCK
          </div>
        </div>

        {Array.from({ length: 10 }).map((_, i) => {
          const vanish = interpolate(frame, [92 + i * 7, 150 + i * 7], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: 160 + i * 72,
                bottom: 108,
                width: 48,
                height: 42,
                borderRadius: 8,
                background: '#1D2128',
                opacity: vanish,
                transform: `translateY(${(1 - vanish) * -42}px) scale(${vanish})`,
              }}
            >
              <div style={{ position: 'absolute', left: 13, top: -8, width: 22, height: 12, border: '5px solid #1D2128', borderBottom: 0, borderRadius: '8px 8px 0 0' }} />
            </div>
          );
        })}
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 48%, rgba(29,33,40,0.34) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene11VaporizationForecast;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12GravityDepression',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12GravityDepression: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const sink = interpolate(frame, [48, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const camera = interpolate(frame, [0, durationInFrames], [1.08, 1.0]);
  const tilt = interpolate(frame, [0, durationInFrames], [-2, 2]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 78%, rgba(29,33,40,0.42), transparent 32%), radial-gradient(circle at 16% 16%, rgba(244,242,242,0.42), transparent 26%), linear-gradient(145deg, #FF9E20 0%, #F38318 48%, #A8340A 100%)',
          transform: `scale(${camera}) rotate(${tilt}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.18,
          backgroundImage:
            'repeating-linear-gradient(0deg, rgba(29,33,40,0.2) 0px, rgba(29,33,40,0.2) 3px, transparent 3px, transparent 34px)',
          transform: `translateY(${interpolate(frame, [0, durationInFrames], [0, 34])}px)`,
        }}
      />

      {[
        { w: 540, h: 540, l: -90, t: -90, br: '50%', r: 0 },
        { w: 470, h: 820, l: 1560, t: 180, br: 90, r: 20 },
        { w: 560, h: 260, l: 670, t: 830, br: 44, r: -8 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            width: s.w,
            height: s.h,
            left: s.l,
            top: s.t,
            borderRadius: s.br,
            background: '#1D2128',
            opacity: 0.035 + i * 0.012,
            transform: `rotate(${s.r + Math.sin(frame / 50 + i) * 12}deg) scale(${1 + Math.sin(frame / 42 + i) * 0.04})`,
          }}
        />
      ))}

      {Array.from({ length: 30 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: seededRandom(i + 100) * 1920,
            top: (seededRandom(i + 200) * 1080 + frame * (0.18 + seededRandom(i) * 0.5)) % 1080,
            width: 3 + seededRandom(i) * 10,
            height: 3 + seededRandom(i + 1) * 10,
            borderRadius: '50%',
            background: i % 2 ? '#F4F2F2' : '#1D2128',
            opacity: 0.15,
          }}
        />
      ))}

      <div style={{ position: 'absolute', left: 95, top: 86, fontFamily: '"Mont Heavy", Inter', fontSize: 86, color: '#1D2128', letterSpacing: -4, opacity: sp(4), transform: `translateY(${(1 - sp(4)) * 60}px)` }}>
        NOT A FACTORY STORY
      </div>

      <div
        style={{
          position: 'absolute',
          left: 98,
          top: 232,
          width: 660,
          height: 572,
          borderRadius: 54,
          background: 'rgba(244,242,242,0.82)',
          boxShadow: '0 35px 90px rgba(29,33,40,0.26)',
          transform: `translateX(${(1 - sp(18)) * -80}px)`,
        }}
      >
        <div style={{ position: 'absolute', left: 44, top: 34, fontFamily: 'Inter', fontWeight: 900, fontSize: 34, color: '#C94B0A' }}>OLD AUTOMATION TARGET</div>
        <svg width="660" height="572" viewBox="0 0 660 572">
          <rect x="70" y="330" width="520" height="50" rx="25" fill="#1D2128" opacity="0.9" />
          {Array.from({ length: 6 }).map((_, i) => (
            <circle key={i} cx={120 + i * 88} cy="356" r="18" fill="#F4F2F2" opacity="0.7" />
          ))}
          <rect x="166" y="142" width="126" height="146" rx="22" fill="#1D2128" opacity="0.85" />
          <rect x="198" y="106" width="62" height="44" rx="12" fill="#1D2128" />
          <circle cx="210" cy="190" r="10" fill="#FF9E20" />
          <circle cx="248" cy="190" r="10" fill="#FF9E20" />
          <path d="M292 210 L420 152 L448 200 L320 270 Z" fill="#1D2128" opacity="0.75" />
          <path d="M450 194 L520 232" stroke="#1D2128" strokeWidth="22" strokeLinecap="round" />
          <rect x="502" y="214" width="72" height="42" rx="10" fill="#1D2128" />
          <text x="330" y="462" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="54" fill="#1D2128">ASSEMBLY LINE</text>
        </svg>
      </div>

      <div
        style={{
          position: 'absolute',
          right: 98,
          top: 232,
          width: 920,
          height: 572,
          borderRadius: 54,
          background: '#1D2128',
          boxShadow: '0 42px 110px rgba(29,33,40,0.38)',
          transform: `translateX(${(1 - sp(34)) * 90}px)`,
          overflow: 'hidden',
        }}
      >
        <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 55% 65%, rgba(255,0,0,0.26), transparent 34%)' }} />
        <div style={{ position: 'absolute', left: 46, top: 34, fontFamily: 'Inter', fontWeight: 900, fontSize: 34, color: '#FFCB82' }}>NEW AUTOMATION TARGET</div>

        <svg width="920" height="572" viewBox="0 0 920 572">
          <ellipse cx="460" cy={390 + sink * 64} rx={260 + sink * 100} ry={74 + sink * 38} fill="#0D1016" opacity="0.82" />
          <ellipse cx="460" cy={390 + sink * 64} rx={160 + sink * 110} ry={38 + sink * 44} fill="#ff0000" opacity="0.35" />
          {Array.from({ length: 9 }).map((_, i) => {
            const fall = interpolate(frame, [50 + i * 8, 150 + i * 8], [0, 160 + i * 10], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <g key={i} transform={`translate(${165 + (i % 3) * 160}, ${118 + Math.floor(i / 3) * 86 + fall}) rotate(${Math.sin(frame / 18 + i) * 6})`} opacity={1 - sink * 0.42}>
                <rect width="108" height="66" rx="10" fill="#F4F2F2" />
                <path d="M18 22 H90 M18 38 H76" stroke="#1D2128" strokeWidth="7" strokeLinecap="round" />
              </g>
            );
          })}
          <circle cx="460" cy="214" r="84" fill="#FF9E20" stroke="#F4F2F2" strokeWidth="10" />
          <path d="M414 214 C430 170 490 170 506 214 C490 258 430 258 414 214 Z" fill="#1D2128" />
          <circle cx="460" cy="214" r="18" fill="#F4F2F2" />
          <text x="460" y="506" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="54" fill="#F4F2F2">OFFICE GRAVITY WELL</text>
        </svg>
      </div>

      <div
        style={{
          position: 'absolute',
          left: 500,
          bottom: 72,
          width: 920,
          height: 86,
          borderRadius: 26,
          background: '#F4F2F2',
          color: '#1D2128',
          fontFamily: '"Mont Heavy", Inter',
          fontSize: 48,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          letterSpacing: -1,
          boxShadow: '0 24px 70px rgba(29,33,40,0.25)',
          transform: `scale(${0.9 + sp(118) * 0.1})`,
          opacity: sp(118),
        }}
      >
        ECONOMIC DEVASTATION IS MOVING UPSTAIRS
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.38) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene12GravityDepression;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13SalaryTaskCore',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13SalaryTaskCore: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const rotate = interpolate(frame, [0, durationInFrames], [0, 360], { easing: Easing.linear });
  const scan = interpolate(frame, [0, durationInFrames], [-240, 1120]);
  const salaryDrop = interpolate(frame, [130, 230], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const tasks = [
    { label: 'DATA', x: 240, y: 210, path: 'M22 74 H98 M22 50 H70 M22 26 H112', icon: 'bars' },
    { label: 'CONTRACT', x: 1300, y: 200, path: 'M40 20 H104 V116 H40 Z M56 48 H90 M56 70 H90 M56 92 H78', icon: 'doc' },
    { label: 'COPY', x: 1420, y: 680, path: 'M34 36 H112 M34 62 H96 M34 88 H118', icon: 'lines' },
    { label: 'CODE', x: 250, y: 690, path: 'M70 38 L38 70 L70 102 M102 38 L134 70 L102 102', icon: 'code' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 48%, rgba(244,242,242,0.48), transparent 24%), radial-gradient(circle at 86% 18%, rgba(29,33,40,0.28), transparent 30%), linear-gradient(135deg, #FF9E20 0%, #F77D13 54%, #D85807 100%)',
          transform: `scale(${interpolate(frame, [0, durationInFrames], [1.02, 1.09])})`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.14,
          backgroundImage:
            'radial-gradient(circle, rgba(29,33,40,0.34) 2px, transparent 3px)',
          backgroundSize: '38px 38px',
          transform: `translate(${interpolate(frame, [0, durationInFrames], [0, 38])}px, ${interpolate(frame, [0, durationInFrames], [0, -38])}px)`,
        }}
      />

      {[
        { l: 80, t: 110, w: 420, h: 420, br: '50%', r: 12 },
        { l: 1220, t: -120, w: 580, h: 580, br: 70, r: -18 },
        { l: 760, t: 720, w: 700, h: 220, br: 44, r: 6 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.l,
            top: s.t,
            width: s.w,
            height: s.h,
            borderRadius: s.br,
            border: '7px solid #1D2128',
            opacity: 0.04 + i * 0.01,
            transform: `rotate(${s.r + Math.sin(frame / 46 + i) * 10}deg)`,
          }}
        />
      ))}

      {Array.from({ length: 32 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: seededRandom(i + 31) * 1920,
            top: (seededRandom(i + 71) * 1080 + frame * (0.25 + seededRandom(i + 4) * 0.52)) % 1080,
            width: 4 + seededRandom(i + 2) * 8,
            height: 4 + seededRandom(i + 3) * 8,
            borderRadius: '50%',
            background: i % 4 === 0 ? '#F4F2F2' : '#1D2128',
            opacity: 0.14 + seededRandom(i) * 0.18,
          }}
        />
      ))}

      <svg style={{ position: 'absolute', inset: 0 }} width="1920" height="1080" viewBox="0 0 1920 1080">
        {tasks.map((t, i) => {
          const draw = sp(30 + i * 16);
          return (
            <g key={t.label} opacity={draw}>
              <path d={`M960 540 C${960 + (t.x - 960) * 0.45} ${540}, ${t.x} ${t.y}, ${t.x + 92} ${t.y + 76}`} fill="none" stroke="#1D2128" strokeWidth="5" opacity="0.42" strokeDasharray="14 18" />
              <circle cx={t.x + 92} cy={t.y + 76} r={88 * draw} fill="#F4F2F2" stroke="#1D2128" strokeWidth="8" />
              <path d={t.path} stroke="#1D2128" strokeWidth="10" strokeLinecap="round" strokeLinejoin="round" fill="none" transform={`translate(${t.x + 18}, ${t.y + 14})`} />
              <text x={t.x + 92} y={t.y + 194} textAnchor="middle" fontFamily="Inter" fontSize="30" fontWeight="900" fill="#1D2128">{t.label}</text>
            </g>
          );
        })}
      </svg>

      <div
        style={{
          position: 'absolute',
          left: 690,
          top: 270,
          width: 540,
          height: 540,
          borderRadius: '50%',
          background: '#1D2128',
          boxShadow: '0 0 0 22px rgba(244,242,242,0.55), 0 45px 120px rgba(29,33,40,0.45)',
          transform: `scale(${0.78 + sp(12, { damping: 10 }) * 0.22}) rotate(${rotate * 0.08}deg)`,
          overflow: 'hidden',
        }}
      >
        <div style={{ position: 'absolute', inset: 0, background: `conic-gradient(from ${rotate}deg, #FF9E20, #F4F2F2, #FF9E20, #ff0000, #FF9E20)` }} />
        <div style={{ position: 'absolute', inset: 28, borderRadius: '50%', background: '#1D2128' }} />
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: scan,
            width: 300,
            height: 80,
            background: 'linear-gradient(180deg, transparent, rgba(255,158,32,0.72), transparent)',
            filter: 'blur(4px)',
          }}
        />
        <div style={{ position: 'absolute', inset: 0, display: 'flex', alignItems: 'center', justifyContent: 'center', flexDirection: 'column' }}>
          <svg width="190" height="160" viewBox="0 0 190 160">
            <path d="M95 12 C128 12 154 38 154 70 C154 104 128 134 95 148 C62 134 36 104 36 70 C36 38 62 12 95 12 Z" fill="#F4F2F2" />
            <circle cx="72" cy="72" r="10" fill="#1D2128" />
            <circle cx="118" cy="72" r="10" fill="#1D2128" />
            <path d="M70 106 Q95 126 120 106" stroke="#1D2128" strokeWidth="8" strokeLinecap="round" fill="none" />
          </svg>
          <div style={{ fontFamily: '"Mont Heavy", Inter', fontSize: 66, color: '#F4F2F2', letterSpacing: -2 }}>LANGUAGE MODEL</div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          left: 615,
          bottom: 70,
          width: 690,
          height: 118,
          borderRadius: 32,
          background: '#F4F2F2',
          boxShadow: '0 30px 90px rgba(29,33,40,0.28)',
          transform: `translateY(${salaryDrop * 34}px) scale(${1 - salaryDrop * 0.04})`,
        }}
      >
        <div style={{ position: 'absolute', left: 38, top: 23, fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 900, color: '#C94B0A' }}>SALARY JUSTIFICATION</div>
        <div style={{ position: 'absolute', right: 40, top: 10, fontFamily: '"Mont Heavy", Inter', fontSize: 82, color: '#1D2128', letterSpacing: -4 }}>$80,000</div>
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.36) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene13SalaryTaskCore;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14PennyAutomationMachine',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14PennyAutomationMachine: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const belt = interpolate(frame, [0, durationInFrames], [0, -240], { easing: Easing.linear });
  const clock = interpolate(frame, [0, durationInFrames], [0, 720], { easing: Easing.linear });
  const crush = interpolate(frame, [76, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 72% 25%, rgba(244,242,242,0.38), transparent 28%), radial-gradient(circle at 22% 75%, rgba(29,33,40,0.34), transparent 34%), linear-gradient(135deg, #FF9E20 0%, #FA7F15 52%, #B33B07 100%)',
          transform: `scale(${interpolate(frame, [0, durationInFrames], [1, 1.08])}) translateX(${interpolate(frame, [0, durationInFrames], [-10, 12])}px)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.16,
          backgroundImage:
            'repeating-linear-gradient(90deg, rgba(29,33,40,0.22) 0px, rgba(29,33,40,0.22) 4px, transparent 4px, transparent 60px)',
          transform: `translateX(${interpolate(frame, [0, durationInFrames], [0, 60])}px)`,
        }}
      />

      {[
        { l: 105, t: 120, w: 480, h: 480, br: '50%', r: -16 },
        { l: 1320, t: 610, w: 520, h: 280, br: 52, r: 14 },
        { l: 710, t: -160, w: 520, h: 520, br: 0, r: 45 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.l,
            top: s.t,
            width: s.w,
            height: s.h,
            borderRadius: s.br,
            background: '#1D2128',
            opacity: 0.035 + i * 0.014,
            transform: `rotate(${s.r + Math.sin(frame / 42 + i) * 11}deg) scale(${1 + Math.sin(frame / 44 + i) * 0.04})`,
          }}
        />
      ))}

      {Array.from({ length: 28 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: seededRandom(i + 400) * 1920,
            top: (seededRandom(i + 500) * 1080 + frame * (0.28 + seededRandom(i) * 0.48)) % 1080,
            width: 4 + seededRandom(i) * 11,
            height: 4 + seededRandom(i + 1) * 11,
            borderRadius: 2,
            background: i % 3 === 0 ? '#F4F2F2' : '#1D2128',
            opacity: 0.13 + seededRandom(i + 2) * 0.2,
          }}
        />
      ))}

      <div style={{ position: 'absolute', left: 96, top: 82, fontFamily: '"Mont Heavy", Inter', fontSize: 86, color: '#1D2128', letterSpacing: -4, opacity: sp(2), transform: `translateY(${(1 - sp(2)) * 58}px)` }}>
        FRACTION-OF-A-PENNY MACHINE
      </div>

      <div
        style={{
          position: 'absolute',
          left: 120,
          top: 258,
          width: 1100,
          height: 460,
          borderRadius: 58,
          background: '#1D2128',
          boxShadow: '0 44px 120px rgba(29,33,40,0.36)',
          transform: `scale(${0.92 + sp(20) * 0.08})`,
          overflow: 'hidden',
        }}
      >
        <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 72% 50%, rgba(255,158,32,0.32), transparent 38%)' }} />
        <div style={{ position: 'absolute', left: 0, bottom: 88, width: 1300, height: 86, background: '#F4F2F2', transform: `translateX(${belt}px)` }}>
          {Array.from({ length: 12 }).map((_, i) => (
            <div key={i} style={{ position: 'absolute', left: i * 120, top: 0, width: 60, height: 86, background: '#FF9E20', opacity: 0.55 }} />
          ))}
        </div>

        {['COPY', 'CODE', 'MODEL', 'SUCCESS', 'REVIEW', 'DATA'].map((label, i) => {
          const x = ((i * 180 + frame * 2.1) % 1240) - 80;
          return (
            <div
              key={label}
              style={{
                position: 'absolute',
                left: x,
                bottom: 198,
                width: 150,
                height: 96,
                borderRadius: 18,
                background: '#F4F2F2',
                border: '6px solid #FF9E20',
                color: '#1D2128',
                fontFamily: 'JetBrains Mono',
                fontSize: 23,
                fontWeight: 900,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                transform: `rotate(${Math.sin(frame / 14 + i) * 2}deg)`,
              }}
            >
              {label}
            </div>
          );
        })}

        <svg style={{ position: 'absolute', right: 64, top: 82 }} width="250" height="250" viewBox="0 0 250 250">
          <circle cx="125" cy="125" r="104" fill="#FF9E20" stroke="#F4F2F2" strokeWidth="12" />
          <text x="125" y="144" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="62" fill="#1D2128">24/7</text>
          <line x1="125" y1="125" x2="125" y2="58" stroke="#1D2128" strokeWidth="10" strokeLinecap="round" transform={`rotate(${clock} 125 125)`} />
          <line x1="125" y1="125" x2="176" y2="125" stroke="#1D2128" strokeWidth="8" strokeLinecap="round" transform={`rotate(${clock * 12} 125 125)`} />
        </svg>

        <div style={{ position: 'absolute', left: 62, top: 50, fontFamily: '"Mont Heavy", Inter', fontSize: 62, color: '#F4F2F2' }}>
          ALWAYS ON
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          right: 108,
          top: 246,
          width: 520,
          height: 486,
          borderRadius: 54,
          background: 'rgba(244,242,242,0.9)',
          boxShadow: '0 38px 100px rgba(29,33,40,0.27)',
          transform: `translateX(${(1 - sp(48)) * 88}px)`,
          opacity: sp(48),
        }}
      >
        <div style={{ position: 'absolute', left: 40, top: 34, fontFamily: '"Mont Heavy", Inter', fontSize: 54, color: '#1D2128' }}>NO BENEFITS</div>

        {[
          { label: 'HEALTH', y: 126, d: 'M72 22 V96 M35 59 H109' },
          { label: '401K', y: 236, d: 'M32 100 V54 H54 V100 M76 100 V30 H98 V100 M120 100 V70 H142 V100' },
          { label: 'PTO', y: 346, d: 'M34 34 H136 V108 H34 Z M34 58 H136' },
        ].map((item, i) => (
          <div key={item.label} style={{ position: 'absolute', left: 42, top: item.y, width: 436, height: 84, borderRadius: 22, background: '#1D2128' }}>
            <svg width="162" height="84" viewBox="0 0 162 126" style={{ position: 'absolute', left: 8, top: -20 }}>
              <path d={item.d} stroke="#F4F2F2" strokeWidth="10" strokeLinecap="round" strokeLinejoin="round" fill="none" />
              <path d="M22 112 L146 14" stroke="#ff0000" strokeWidth="12" strokeLinecap="round" />
            </svg>
            <div style={{ position: 'absolute', left: 172, top: 22, fontFamily: 'Inter', fontSize: 30, fontWeight: 900, color: '#F4F2F2' }}>{item.label}</div>
          </div>
        ))}
      </div>

      <div
        style={{
          position: 'absolute',
          left: 690,
          bottom: 70,
          fontFamily: '"Mont Heavy", Inter',
          fontSize: 100,
          color: '#1D2128',
          letterSpacing: -5,
          transform: `scale(${1 + crush * 0.1})`,
        }}
      >
        $0.0001
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.36) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene14PennyAutomationMachine;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15StrongBuyReplacement',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15StrongBuyReplacement: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const merge = interpolate(frame, [38, 122], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const stock = interpolate(frame, [116, 230], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const shake = frame > 112 && frame < 130 ? Math.sin(frame * 1.9) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 22% 32%, rgba(244,242,242,0.4), transparent 28%), radial-gradient(circle at 78% 66%, rgba(29,33,40,0.34), transparent 36%), linear-gradient(135deg, #FF9E20 0%, #EF7512 55%, #9F3308 100%)',
          transform: `scale(${interpolate(frame, [0, durationInFrames], [1.03, 1.12])}) translate(${shake}px, ${shake * 0.25}px)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.16,
          backgroundImage:
            'linear-gradient(45deg, rgba(29,33,40,0.18) 25%, transparent 25%), linear-gradient(-45deg, rgba(29,33,40,0.18) 25%, transparent 25%)',
          backgroundSize: '54px 54px',
          transform: `translateX(${interpolate(frame, [0, durationInFrames], [0, -54])}px)`,
        }}
      />

      {[
        { l: -120, t: 610, w: 600, h: 340, br: 60, r: 18 },
        { l: 1450, t: 90, w: 420, h: 420, br: '50%', r: 0 },
        { l: 790, t: -180, w: 480, h: 480, br: 40, r: 45 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.l,
            top: s.t,
            width: s.w,
            height: s.h,
            borderRadius: s.br,
            border: '8px solid #1D2128',
            opacity: 0.04 + i * 0.01,
            transform: `rotate(${s.r + Math.sin(frame / 48 + i) * 12}deg)`,
          }}
        />
      ))}

      {Array.from({ length: 30 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: seededRandom(i + 810) * 1920,
            top: (seededRandom(i + 910) * 1080 + frame * (0.24 + seededRandom(i) * 0.42)) % 1080,
            width: 5 + seededRandom(i) * 8,
            height: 5 + seededRandom(i + 1) * 8,
            borderRadius: seededRandom(i) > 0.5 ? '50%' : 1,
            background: i % 2 ? '#1D2128' : '#F4F2F2',
            opacity: 0.12 + seededRandom(i + 2) * 0.18,
          }}
        />
      ))}

      <div style={{ position: 'absolute', left: 100, top: 78, fontFamily: '"Mont Heavy", Inter', fontSize: 86, color: '#1D2128', letterSpacing: -4, opacity: sp(2), transform: `translateY(${(1 - sp(2)) * 58}px)` }}>
        WALL STREET SEES MARGIN
      </div>

      <div
        style={{
          position: 'absolute',
          left: 100,
          top: 230,
          width: 760,
          height: 600,
          borderRadius: 56,
          background: 'rgba(244,242,242,0.88)',
          boxShadow: '0 40px 110px rgba(29,33,40,0.28)',
          overflow: 'hidden',
          transform: `translateX(${(1 - sp(18)) * -80}px)`,
        }}
      >
        <div style={{ position: 'absolute', left: 44, top: 34, fontFamily: 'Inter', fontWeight: 900, fontSize: 32, color: '#C94B0A' }}>10 JUNIOR ANALYST SEATS</div>
        {Array.from({ length: 10 }).map((_, i) => {
          const col = i % 5;
          const row = Math.floor(i / 5);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: 70 + col * 128 + merge * (300 - col * 128),
                top: 130 + row * 150 + merge * (120 - row * 150),
                width: 82,
                height: 82,
                borderRadius: 18,
                background: '#1D2128',
                opacity: 1 - merge * 0.82,
                transform: `scale(${1 - merge * 0.65}) rotate(${merge * 90}deg)`,
              }}
            >
              <div style={{ position: 'absolute', left: 16, top: 22, width: 50, height: 10, borderRadius: 10, background: '#FF9E20' }} />
              <div style={{ position: 'absolute', left: 16, top: 42, width: 36, height: 10, borderRadius: 10, background: '#FF9E20' }} />
            </div>
          );
        })}

        <div
          style={{
            position: 'absolute',
            left: 230,
            top: 238,
            width: 300,
            height: 190,
            borderRadius: 30,
            background: '#1D2128',
            opacity: merge,
            transform: `scale(${0.45 + merge * 0.55})`,
          }}
        >
          <svg width="300" height="190" viewBox="0 0 300 190">
            <rect x="38" y="50" width="96" height="94" rx="18" fill="#F4F2F2" />
            <path d="M168 46 H248 V144 H168 Z M184 72 H232 M184 98 H232 M184 124 H220" stroke="#F4F2F2" strokeWidth="10" strokeLinecap="round" fill="none" />
            <circle cx="86" cy="98" r="26" fill="#FF9E20" />
          </svg>
          <div style={{ position: 'absolute', bottom: 16, left: 0, right: 0, textAlign: 'center', fontFamily: 'Inter', fontWeight: 900, fontSize: 24, color: '#F4F2F2' }}>
            1 MANAGER + AI
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          right: 100,
          top: 230,
          width: 860,
          height: 600,
          borderRadius: 56,
          background: '#1D2128',
          color: '#F4F2F2',
          boxShadow: '0 44px 120px rgba(29,33,40,0.38)',
          transform: `translateX(${(1 - sp(48)) * 90}px)`,
          overflow: 'hidden',
        }}
      >
        <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 80% 20%, rgba(255,158,32,0.38), transparent 36%)' }} />
        <div style={{ position: 'absolute', left: 48, top: 38, fontFamily: 'Inter', fontWeight: 900, fontSize: 32, color: '#FFCB82' }}>ANALYST NOTE</div>
        <div style={{ position: 'absolute', left: 48, top: 92, fontFamily: '"Mont Heavy", Inter', fontSize: 82, letterSpacing: -4 }}>STRONG BUY</div>

        <svg width="760" height="330" viewBox="0 0 760 330" style={{ position: 'absolute', left: 54, top: 220 }}>
          <path d="M40 270 H720" stroke="#F4F2F2" strokeWidth="6" opacity="0.25" />
          <path d="M40 270 C150 242 210 246 300 196 C395 142 470 150 548 90 C610 42 660 62 720 28" fill="none" stroke="#FF9E20" strokeWidth="18" strokeLinecap="round" strokeDasharray={`${stock * 980} 980`} />
          <path d="M658 30 H720 V92" fill="none" stroke="#FF9E20" strokeWidth="18" strokeLinecap="round" strokeLinejoin="round" opacity={stock} />
          {Array.from({ length: 6 }).map((_, i) => (
            <rect key={i} x={70 + i * 105} y={270 - (40 + i * 28) * stock} width="52" height={(40 + i * 28) * stock} rx="10" fill="#F4F2F2" opacity="0.22" />
          ))}
        </svg>

        <div style={{ position: 'absolute', left: 54, bottom: 42, fontFamily: 'JetBrains Mono', fontWeight: 900, fontSize: 28, color: '#FFCB82' }}>
          JOB LOSSES IGNORED. MARGINS REWARDED.
        </div>
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.36) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene15StrongBuyReplacement;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16MarginsLaborHawks',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16MarginsLaborHawks: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const bars = interpolate(frame, [30, 145], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const eye = interpolate(frame, [80, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const sweep = interpolate(frame, [0, durationInFrames], [-220, 1920]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 75% 28%, rgba(244,242,242,0.42), transparent 25%), radial-gradient(circle at 20% 78%, rgba(29,33,40,0.34), transparent 36%), linear-gradient(135deg, #FF9E20 0%, #F27613 54%, #A63708 100%)',
          transform: `scale(${interpolate(frame, [0, durationInFrames], [1.05, 1.1])}) translateY(${interpolate(frame, [0, durationInFrames], [12, -10])}px)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.15,
          backgroundImage:
            'repeating-radial-gradient(circle at 50% 50%, rgba(29,33,40,0.25) 0px, rgba(29,33,40,0.25) 3px, transparent 3px, transparent 52px)',
          transform: `scale(${interpolate(frame, [0, durationInFrames], [1, 1.18])})`,
        }}
      />

      <div style={{ position: 'absolute', left: sweep, top: 0, width: 220, height: 1080, background: 'linear-gradient(90deg, transparent, rgba(244,242,242,0.34), transparent)', transform: 'skewX(-14deg)' }} />

      {[
        { l: -160, t: 160, w: 620, h: 620, br: '50%', r: 0 },
        { l: 1340, t: 580, w: 530, h: 310, br: 60, r: -10 },
        { l: 740, t: -150, w: 500, h: 500, br: 20, r: 45 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.l,
            top: s.t,
            width: s.w,
            height: s.h,
            borderRadius: s.br,
            background: '#1D2128',
            opacity: 0.035 + i * 0.014,
            transform: `rotate(${s.r + Math.sin(frame / 46 + i) * 10}deg) scale(${1 + Math.sin(frame / 40 + i) * 0.035})`,
          }}
        />
      ))}

      {Array.from({ length: 30 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: seededRandom(i + 141) * 1920,
            top: (seededRandom(i + 241) * 1080 + frame * (0.2 + seededRandom(i) * 0.55)) % 1080,
            width: 4 + seededRandom(i) * 10,
            height: 4 + seededRandom(i + 1) * 10,
            borderRadius: '50%',
            background: i % 3 === 0 ? '#F4F2F2' : '#1D2128',
            opacity: 0.14,
          }}
        />
      ))}

      <div style={{ position: 'absolute', left: 94, top: 74, fontFamily: '"Mont Heavy", Inter', fontSize: 86, color: '#1D2128', letterSpacing: -4, opacity: sp(2), transform: `translateY(${(1 - sp(2)) * 60}px)` }}>
        PROFIT MARGINS EXPLODE
      </div>

      <div
        style={{
          position: 'absolute',
          left: 110,
          top: 220,
          width: 860,
          height: 640,
          borderRadius: 58,
          background: '#1D2128',
          boxShadow: '0 42px 120px rgba(29,33,40,0.38)',
          overflow: 'hidden',
          transform: `translateX(${(1 - sp(18)) * -90}px)`,
        }}
      >
        <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 40% 30%, rgba(255,158,32,0.34), transparent 38%)' }} />
        <div style={{ position: 'absolute', left: 54, top: 44, fontFamily: 'Inter', fontWeight: 900, fontSize: 32, color: '#FFCB82' }}>OPERATING MARGIN</div>

        <svg width="760" height="430" viewBox="0 0 760 430" style={{ position: 'absolute', left: 50, top: 150 }}>
          <path d="M42 360 H720 M42 70 V360" stroke="#F4F2F2" strokeWidth="8" opacity="0.3" />
          {[
            { label: 'PAYROLL', h: 110, c: '#F4F2F2' },
            { label: 'COMPUTE', h: 250, c: '#FF9E20' },
            { label: 'MARGIN', h: 330, c: '#ff0000' },
          ].map((b, i) => (
            <g key={b.label}>
              <rect x={110 + i * 200} y={360 - b.h * bars} width="118" height={b.h * bars} rx="20" fill={b.c} />
              <text x={170 + i * 200} y="404" textAnchor="middle" fontFamily="Inter" fontSize="24" fontWeight="900" fill="#F4F2F2">{b.label}</text>
            </g>
          ))}
          <path d={`M110 ${315 - 90 * bars} C260 ${290 - 110 * bars}, 386 ${244 - 180 * bars}, 628 ${90 - 40 * bars}`} fill="none" stroke="#FF9E20" strokeWidth="12" strokeLinecap="round" opacity={bars} />
        </svg>
      </div>

      <div
        style={{
          position: 'absolute',
          right: 110,
          top: 220,
          width: 760,
          height: 640,
          borderRadius: 58,
          background: 'rgba(244,242,242,0.9)',
          boxShadow: '0 40px 110px rgba(29,33,40,0.28)',
          overflow: 'hidden',
          transform: `translateX(${(1 - sp(42)) * 90}px)`,
        }}
      >
        <div style={{ position: 'absolute', left: 46, top: 42, fontFamily: 'Inter', fontWeight: 900, fontSize: 32, color: '#C94B0A' }}>LABOR DATA SURVEILLANCE</div>
        <svg width="760" height="640" viewBox="0 0 760 640">
          <path d="M106 304 C220 154 540 154 654 304 C540 454 220 454 106 304 Z" fill="#1D2128" opacity={eye} />
          <circle cx="380" cy="304" r={112 * eye} fill="#FF9E20" />
          <circle cx="380" cy="304" r={46 * eye} fill="#1D2128" />
          <path d={`M380 304 L${380 + Math.cos(frame / 18) * 240} ${304 + Math.sin(frame / 18) * 170}`} stroke="#ff0000" strokeWidth="10" strokeLinecap="round" opacity={eye} />
          <path d="M198 500 H566" stroke="#1D2128" strokeWidth="12" strokeLinecap="round" />
          <path d="M234 500 C258 430 310 420 346 500 C378 392 440 384 496 500" fill="none" stroke="#C94B0A" strokeWidth="12" strokeLinecap="round" />
          <text x="380" y="584" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="56" fill="#1D2128">WATCHING LIKE HAWKS</text>
        </svg>
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.36) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene16MarginsLaborHawks;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17DebtDependencyCycle',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17DebtDependencyCycle: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const spiral = interpolate(frame, [0, durationInFrames], [0, 360], { easing: Easing.linear });
  const lock = interpolate(frame, [72, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const interest = interpolate(frame, [36, 145], [8, 29], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 50%, rgba(29,33,40,0.42), transparent 34%), radial-gradient(circle at 18% 12%, rgba(244,242,242,0.4), transparent 26%), linear-gradient(135deg, #FF9E20 0%, #F07A14 56%, #8F2F08 100%)',
          transform: `scale(${interpolate(frame, [0, durationInFrames], [1.02, 1.12])}) rotate(${interpolate(frame, [0, durationInFrames], [-1.5, 1.5])}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.15,
          backgroundImage:
            'repeating-linear-gradient(135deg, rgba(29,33,40,0.2) 0px, rgba(29,33,40,0.2) 3px, transparent 3px, transparent 42px)',
          transform: `translate(${interpolate(frame, [0, durationInFrames], [0, 42])}px, ${interpolate(frame, [0, durationInFrames], [0, 42])}px)`,
        }}
      />

      {[
        { l: 120, t: 700, w: 500, h: 250, br: 52, r: 10 },
        { l: 1340, t: 90, w: 460, h: 460, br: '50%', r: 0 },
        { l: 720, t: -130, w: 520, h: 520, br: 30, r: 45 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.l,
            top: s.t,
            width: s.w,
            height: s.h,
            borderRadius: s.br,
            border: '8px solid #1D2128',
            opacity: 0.04 + i * 0.012,
            transform: `rotate(${s.r + Math.sin(frame / 46 + i) * 12}deg)`,
          }}
        />
      ))}

      {Array.from({ length: 34 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: seededRandom(i + 330) * 1920,
            top: (seededRandom(i + 430) * 1080 + frame * (0.18 + seededRandom(i) * 0.58)) % 1080,
            width: 4 + seededRandom(i) * 9,
            height: 4 + seededRandom(i + 1) * 9,
            borderRadius: '50%',
            background: i % 2 ? '#1D2128' : '#F4F2F2',
            opacity: 0.14 + seededRandom(i) * 0.16,
          }}
        />
      ))}

      <div style={{ position: 'absolute', left: 98, top: 76, fontFamily: '"Mont Heavy", Inter', fontSize: 86, color: '#1D2128', letterSpacing: -4, opacity: sp(2), transform: `translateY(${(1 - sp(2)) * 60}px)` }}>
        DEBT DEPENDENCY LOOP
      </div>

      <svg style={{ position: 'absolute', left: 140, top: 190 }} width="1040" height="760" viewBox="0 0 1040 760">
        <g transform={`translate(520 382) rotate(${spiral})`}>
          {Array.from({ length: 7 }).map((_, i) => (
            <circle key={i} cx="0" cy="0" r={70 + i * 58} fill="none" stroke={i % 2 ? '#F4F2F2' : '#1D2128'} strokeWidth="12" opacity={0.12 + i * 0.045} strokeDasharray="40 32" />
          ))}
        </g>
        {[0, 1, 2, 3].map((i) => {
          const a = (spiral * 0.7 + i * 90) * Math.PI / 180;
          const x = 520 + Math.cos(a) * (150 + i * 42);
          const y = 382 + Math.sin(a) * (150 + i * 42);
          return (
            <g key={i} transform={`translate(${x - 95}, ${y - 58}) rotate(${Math.sin(frame / 18 + i) * 10})`}>
              <rect width="190" height="116" rx="22" fill="#1D2128" stroke="#F4F2F2" strokeWidth="6" />
              <rect x="18" y="28" width="154" height="18" rx="9" fill="#FF9E20" />
              <circle cx="48" cy="78" r="14" fill="#F4F2F2" opacity="0.76" />
              <circle cx="82" cy="78" r="14" fill="#F4F2F2" opacity="0.46" />
            </g>
          );
        })}
        <g transform={`translate(384 244) scale(${0.55 + sp(46) * 0.45})`}>
          <rect x="0" y="0" width="274" height="274" rx="48" fill="#F4F2F2" stroke="#1D2128" strokeWidth="12" />
          <text x="137" y="118" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="88" fill="#ff0000">{Math.round(interest)}%</text>
          <text x="137" y="174" textAnchor="middle" fontFamily="Inter" fontSize="34" fontWeight="900" fill="#1D2128">APR</text>
        </g>
      </svg>

      <div
        style={{
          position: 'absolute',
          right: 120,
          top: 250,
          width: 560,
          height: 560,
          borderRadius: 58,
          background: '#1D2128',
          boxShadow: '0 42px 120px rgba(29,33,40,0.4)',
          transform: `translateX(${(1 - sp(60)) * 90}px) scale(${0.95 + lock * 0.05})`,
          overflow: 'hidden',
        }}
      >
        <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 50% 20%, rgba(255,158,32,0.4), transparent 42%)' }} />
        <svg width="560" height="560" viewBox="0 0 560 560">
          <path d="M176 250 V190 C176 126 224 86 280 86 C336 86 384 126 384 190 V250" fill="none" stroke="#F4F2F2" strokeWidth="34" strokeLinecap="round" opacity={lock} />
          <rect x="132" y="238" width="296" height="232" rx="42" fill="#FF9E20" stroke="#F4F2F2" strokeWidth="12" opacity={lock} />
          <circle cx="280" cy="336" r="38" fill="#1D2128" opacity={lock} />
          <rect x="264" y="366" width="32" height="62" rx="16" fill="#1D2128" opacity={lock} />
          <text x="280" y="524" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="48" fill="#F4F2F2">TRAPPED</text>
        </svg>
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.36) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene17DebtDependencyCycle;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18RugPullEconomy',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18RugPullEconomy: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const erosion = interpolate(frame, [28, 128], [1, 0.28], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const pull = interpolate(frame, [92, 172], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const tumble = interpolate(frame, [110, 205], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const shake = frame > 100 && frame < 132 ? Math.sin(frame * 2) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 18% 22%, rgba(244,242,242,0.4), transparent 28%), radial-gradient(circle at 74% 78%, rgba(29,33,40,0.36), transparent 36%), linear-gradient(135deg, #FF9E20 0%, #F06D12 52%, #8A2A07 100%)',
          transform: `scale(${interpolate(frame, [0, durationInFrames], [1.03, 1.11])}) translate(${shake}px, ${shake * 0.35}px)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.15,
          backgroundImage:
            'linear-gradient(rgba(29,33,40,0.22) 1px, transparent 1px), linear-gradient(90deg, rgba(29,33,40,0.22) 1px, transparent 1px)',
          backgroundSize: '46px 46px',
          transform: `rotate(${interpolate(frame, [0, durationInFrames], [-1, 1])}deg) translateY(${interpolate(frame, [0, durationInFrames], [0, 46])}px)`,
        }}
      />

      {[
        { l: 120, t: 120, w: 470, h: 470, br: '50%', r: 0 },
        { l: 1260, t: 70, w: 560, h: 270, br: 52, r: -12 },
        { l: 1370, t: 720, w: 420, h: 420, br: 18, r: 45 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.l,
            top: s.t,
            width: s.w,
            height: s.h,
            borderRadius: s.br,
            background: '#1D2128',
            opacity: 0.035 + i * 0.014,
            transform: `rotate(${s.r + Math.sin(frame / 48 + i) * 12}deg) scale(${1 + Math.sin(frame / 44 + i) * 0.04})`,
          }}
        />
      ))}

      {Array.from({ length: 32 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: seededRandom(i + 610) * 1920,
            top: (seededRandom(i + 710) * 1080 + frame * (0.2 + seededRandom(i) * 0.56)) % 1080,
            width: 4 + seededRandom(i) * 9,
            height: 4 + seededRandom(i + 1) * 9,
            borderRadius: '50%',
            background: i % 3 ? '#1D2128' : '#F4F2F2',
            opacity: 0.13 + seededRandom(i + 2) * 0.18,
          }}
        />
      ))}

      <div style={{ position: 'absolute', left: 96, top: 74, fontFamily: '"Mont Heavy", Inter', fontSize: 86, color: '#1D2128', letterSpacing: -4, opacity: sp(2), transform: `translateY(${(1 - sp(2)) * 58}px)` }}>
        THE RUG PULL
      </div>

      <div
        style={{
          position: 'absolute',
          left: 112,
          top: 224,
          width: 650,
          height: 570,
          borderRadius: 56,
          background: 'rgba(244,242,242,0.9)',
          boxShadow: '0 40px 110px rgba(29,33,40,0.28)',
          overflow: 'hidden',
          transform: `translateX(${(1 - sp(18)) * -80}px)`,
        }}
      >
        <div style={{ position: 'absolute', left: 42, top: 38, fontFamily: 'Inter', fontWeight: 900, fontSize: 30, color: '#C94B0A' }}>PURCHASING POWER</div>
        <svg width="650" height="570" viewBox="0 0 650 570">
          <circle cx="325" cy="292" r="182" fill="#1D2128" opacity="0.12" />
          <path d="M325 292 L325 110 A182 182 0 1 1 153 350 Z" fill="#1D2128" opacity="0.16" />
          <circle cx="325" cy="292" r={170 * erosion} fill="#FF9E20" stroke="#1D2128" strokeWidth="14" />
          <text x="325" y="300" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="94" fill="#1D2128">{Math.round(erosion * 100)}%</text>
          <text x="325" y="370" textAnchor="middle" fontFamily="Inter" fontSize="30" fontWeight="900" fill="#1D2128">REAL SPENDING POWER</text>
          <path d="M144 480 H506" stroke="#ff0000" strokeWidth="16" strokeLinecap="round" opacity={1 - erosion} />
        </svg>
      </div>

      <div
        style={{
          position: 'absolute',
          right: 112,
          top: 224,
          width: 930,
          height: 570,
          borderRadius: 56,
          background: '#1D2128',
          boxShadow: '0 44px 120px rgba(29,33,40,0.4)',
          overflow: 'hidden',
          transform: `translateX(${(1 - sp(40)) * 90}px)`,
        }}
      >
        <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 78% 18%, rgba(255,158,32,0.42), transparent 34%)' }} />
        <div
          style={{
            position: 'absolute',
            left: 70 - pull * 520,
            bottom: 110,
            width: 780,
            height: 110,
            borderRadius: 22,
            background: 'repeating-linear-gradient(90deg, #F4F2F2 0px, #F4F2F2 42px, #FF9E20 42px, #FF9E20 84px)',
            boxShadow: '0 18px 55px rgba(0,0,0,0.35)',
          }}
        />

        {[
          { label: 'STUDENT LOAN', x: 130, y: 160, w: 260 },
          { label: 'RENT', x: 430, y: 170, w: 170 },
          { label: 'CARD DEBT', x: 640, y: 155, w: 220 },
        ].map((b, i) => (
          <div
            key={b.label}
            style={{
              position: 'absolute',
              left: b.x + tumble * (i - 1) * 90,
              top: b.y + tumble * (190 + i * 40),
              width: b.w,
              height: 126,
              borderRadius: 24,
              background: '#F4F2F2',
              color: '#1D2128',
              fontFamily: 'Inter',
              fontWeight: 900,
              fontSize: 27,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              textAlign: 'center',
              transform: `rotate(${tumble * (i % 2 ? 18 : -22)}deg)`,
            }}
          >
            {b.label}
          </div>
        ))}

        <svg width="930" height="570" viewBox="0 0 930 570">
          <path d="M96 470 C230 398 350 466 474 402 C600 336 720 408 842 326" fill="none" stroke="#FF9E20" strokeWidth="12" strokeLinecap="round" strokeDasharray="24 24" opacity="0.7" />
          <text x="470" y="84" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="58" fill="#F4F2F2">SUPPORT REMOVED</text>
        </svg>
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.36) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene18RugPullEconomy;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19WorkplaceSignalScan',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19WorkplaceSignalScan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const scan = interpolate(frame, [0, durationInFrames], [150, 860], { easing: Easing.linear });
  const pressure = interpolate(frame, [64, 178], [0, 86], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const pulse = 1 + Math.sin(frame / 8) * 0.035;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 78% 24%, rgba(244,242,242,0.42), transparent 28%), radial-gradient(circle at 18% 78%, rgba(29,33,40,0.34), transparent 36%), linear-gradient(135deg, #FF9E20 0%, #F57A12 54%, #A63808 100%)',
          transform: `scale(${interpolate(frame, [0, durationInFrames], [1.02, 1.09])}) translateX(${interpolate(frame, [0, durationInFrames], [-12, 12])}px)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.15,
          backgroundImage:
            'radial-gradient(circle at center, rgba(29,33,40,0.28) 2px, transparent 3px)',
          backgroundSize: '32px 32px',
          transform: `translateY(${interpolate(frame, [0, durationInFrames], [0, -32])}px)`,
        }}
      />

      {[
        { l: -140, t: 220, w: 520, h: 520, br: '50%', r: 0 },
        { l: 1270, t: 620, w: 570, h: 290, br: 60, r: -14 },
        { l: 780, t: -180, w: 480, h: 480, br: 24, r: 45 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.l,
            top: s.t,
            width: s.w,
            height: s.h,
            borderRadius: s.br,
            border: '8px solid #1D2128',
            opacity: 0.038 + i * 0.012,
            transform: `rotate(${s.r + Math.sin(frame / 46 + i) * 13}deg) scale(${1 + Math.sin(frame / 42 + i) * 0.035})`,
          }}
        />
      ))}

      {Array.from({ length: 34 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: seededRandom(i + 910) * 1920,
            top: (seededRandom(i + 1010) * 1080 + frame * (0.22 + seededRandom(i) * 0.58)) % 1080,
            width: 4 + seededRandom(i) * 9,
            height: 4 + seededRandom(i + 1) * 9,
            borderRadius: seededRandom(i) > 0.4 ? '50%' : 2,
            background: i % 2 ? '#1D2128' : '#F4F2F2',
            opacity: 0.13 + seededRandom(i) * 0.18,
          }}
        />
      ))}

      <div style={{ position: 'absolute', left: 96, top: 74, fontFamily: '"Mont Heavy", Inter', fontSize: 86, color: '#1D2128', letterSpacing: -4, opacity: sp(2), transform: `translateY(${(1 - sp(2)) * 60}px)` }}>
        WHAT ARE YOU SEEING?
      </div>

      <div
        style={{
          position: 'absolute',
          left: 108,
          top: 218,
          width: 1010,
          height: 620,
          borderRadius: 58,
          background: '#1D2128',
          boxShadow: '0 44px 120px rgba(29,33,40,0.38)',
          overflow: 'hidden',
          transform: `translateX(${(1 - sp(18)) * -90}px)`,
        }}
      >
        <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 64% 28%, rgba(255,158,32,0.36), transparent 34%)' }} />
        <div style={{ position: 'absolute', left: 50, top: 42, fontFamily: 'Inter', fontWeight: 900, fontSize: 30, color: '#FFCB82' }}>WORKFLOW AUDIT SCREEN</div>

        {[
          { title: 'HIRING FREEZE', y: 125, fill: 0.92 },
          { title: 'AI TOOL ROLLOUT', y: 255, fill: 0.76 },
          { title: 'WORKFLOW AUDIT', y: 385, fill: 0.84 },
        ].map((r, i) => (
          <div key={r.title} style={{ position: 'absolute', left: 54, top: r.y, width: 640, height: 94, borderRadius: 24, background: 'rgba(244,242,242,0.12)' }}>
            <div style={{ position: 'absolute', left: 28, top: 19, fontFamily: 'Inter', fontWeight: 900, fontSize: 28, color: '#F4F2F2' }}>{r.title}</div>
            <div style={{ position: 'absolute', left: 28, bottom: 18, width: 560, height: 13, borderRadius: 12, background: 'rgba(244,242,242,0.18)' }}>
              <div style={{ width: `${r.fill * pressure}%`, height: '100%', borderRadius: 12, background: i === 0 ? '#ff0000' : '#FF9E20' }} />
            </div>
          </div>
        ))}

        <svg width="300" height="300" viewBox="0 0 300 300" style={{ position: 'absolute', right: 58, top: 150, transform: `scale(${pulse})` }}>
          <circle cx="150" cy="150" r="128" fill="#F4F2F2" />
          <path d="M150 150 L150 48" stroke="#1D2128" strokeWidth="12" strokeLinecap="round" transform={`rotate(${pressure * 1.65 - 50} 150 150)`} />
          <circle cx="150" cy="150" r="20" fill="#1D2128" />
          <path d="M66 188 A92 92 0 0 1 234 188" fill="none" stroke="#1D2128" strokeWidth="16" strokeLinecap="round" />
          <text x="150" y="246" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="42" fill="#1D2128">PRESSURE</text>
        </svg>

        <div style={{ position: 'absolute', left: 0, top: scan, width: '100%', height: 72, background: 'linear-gradient(180deg, transparent, rgba(255,158,32,0.55), transparent)' }} />
      </div>

      <div
        style={{
          position: 'absolute',
          right: 108,
          top: 218,
          width: 620,
          height: 620,
          borderRadius: 58,
          background: 'rgba(244,242,242,0.9)',
          boxShadow: '0 40px 110px rgba(29,33,40,0.28)',
          overflow: 'hidden',
          transform: `translateX(${(1 - sp(52)) * 90}px)`,
        }}
      >
        <div style={{ position: 'absolute', left: 42, top: 38, fontFamily: '"Mont Heavy", Inter', fontSize: 54, color: '#1D2128' }}>DROP THE SIGNAL</div>
        {Array.from({ length: 5 }).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: 48 + (i % 2) * 78,
              top: 132 + i * 82,
              width: 470 - (i % 2) * 70,
              height: 58,
              borderRadius: 24,
              background: i % 2 ? '#1D2128' : '#FF9E20',
              transform: `translateX(${Math.sin(frame / 18 + i) * 8}px)`,
            }}
          />
        ))}
        <svg width="122" height="122" viewBox="0 0 122 122" style={{ position: 'absolute', right: 46, bottom: 42 }}>
          <path d="M18 22 H104 V76 H58 L32 100 V76 H18 Z" fill="#1D2128" />
          <circle cx="46" cy="51" r="7" fill="#F4F2F2" />
          <circle cx="66" cy="51" r="7" fill="#F4F2F2" />
          <circle cx="86" cy="51" r="7" fill="#F4F2F2" />
        </svg>
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.36) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene19WorkplaceSignalScan;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20GroundTruthMoat',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20GroundTruthMoat: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: Record<string, number>) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const split = interpolate(frame, [52, 138], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const moat = interpolate(frame, [118, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const camera = interpolate(frame, [0, durationInFrames], [1.04, 1.1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 20% 25%, rgba(244,242,242,0.4), transparent 28%), radial-gradient(circle at 78% 72%, rgba(29,33,40,0.36), transparent 36%), linear-gradient(135deg, #FF9E20 0%, #F47812 55%, #9A3208 100%)',
          transform: `scale(${camera}) translateY(${interpolate(frame, [0, durationInFrames], [8, -10])}px)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.15,
          backgroundImage:
            'repeating-linear-gradient(0deg, rgba(29,33,40,0.2) 0px, rgba(29,33,40,0.2) 2px, transparent 2px, transparent 28px), repeating-linear-gradient(90deg, rgba(244,242,242,0.14) 0px, rgba(244,242,242,0.14) 2px, transparent 2px, transparent 28px)',
          backgroundSize: '56px 56px',
          transform: `translate(${interpolate(frame, [0, durationInFrames], [0, -56])}px, ${interpolate(frame, [0, durationInFrames], [0, 56])}px)`,
        }}
      />

      {[
        { l: 80, t: 660, w: 540, h: 300, br: 56, r: 12 },
        { l: 1380, t: 90, w: 430, h: 430, br: '50%', r: 0 },
        { l: 750, t: -160, w: 500, h: 500, br: 22, r: 45 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.l,
            top: s.t,
            width: s.w,
            height: s.h,
            borderRadius: s.br,
            background: '#1D2128',
            opacity: 0.035 + i * 0.014,
            transform: `rotate(${s.r + Math.sin(frame / 48 + i) * 12}deg) scale(${1 + Math.sin(frame / 44 + i) * 0.04})`,
          }}
        />
      ))}

      {Array.from({ length: 34 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: seededRandom(i + 1210) * 1920,
            top: (seededRandom(i + 1310) * 1080 + frame * (0.18 + seededRandom(i) * 0.55)) % 1080,
            width: 4 + seededRandom(i) * 9,
            height: 4 + seededRandom(i + 1) * 9,
            borderRadius: seededRandom(i) > 0.45 ? '50%' : 2,
            background: i % 2 ? '#1D2128' : '#F4F2F2',
            opacity: 0.13 + seededRandom(i + 2) * 0.18,
          }}
        />
      ))}

      <div style={{ position: 'absolute', left: 96, top: 74, fontFamily: '"Mont Heavy", Inter', fontSize: 86, color: '#1D2128', letterSpacing: -4, opacity: sp(2), transform: `translateY(${(1 - sp(2)) * 60}px)` }}>
        GROUND TRUTH BEATS PR
      </div>

      <div
        style={{
          position: 'absolute',
          left: 108,
          top: 218,
          width: 760,
          height: 620,
          borderRadius: 58,
          background: '#1D2128',
          boxShadow: '0 44px 120px rgba(29,33,40,0.38)',
          overflow: 'hidden',
          transform: `translateX(${(1 - sp(18)) * -90}px)`,
        }}
      >
        <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 40% 24%, rgba(255,158,32,0.38), transparent 34%)' }} />
        <div style={{ position: 'absolute', left: 48, top: 38, fontFamily: 'Inter', fontWeight: 900, fontSize: 30, color: '#FFCB82' }}>LIVE COMMENTS</div>

        {Array.from({ length: 7 }).map((_, i) => {
          const live = sp(32 + i * 13);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: 48 + (i % 2) * 74,
                top: 112 + i * 66,
                width: 560 - (i % 2) * 76,
                height: 46,
                borderRadius: 24,
                background: i % 3 === 0 ? '#FF9E20' : '#F4F2F2',
                opacity: live,
                transform: `translateX(${(1 - live) * -60 + Math.sin(frame / 16 + i) * 7}px)`,
              }}
            >
              <div style={{ position: 'absolute', left: 22, top: 15, width: 280 + seededRandom(i) * 180, height: 10, borderRadius: 10, background: '#1D2128', opacity: 0.72 }} />
            </div>
          );
        })}

        <svg width="180" height="180" viewBox="0 0 180 180" style={{ position: 'absolute', right: 40, bottom: 34, transform: `scale(${1 + Math.sin(frame / 10) * 0.05})` }}>
          <circle cx="90" cy="90" r="78" fill="#F4F2F2" />
          <path d="M38 54 H142 V112 H88 L58 142 V112 H38 Z" fill="#1D2128" />
          <circle cx="70" cy="84" r="7" fill="#FF9E20" />
          <circle cx="90" cy="84" r="7" fill="#FF9E20" />
          <circle cx="110" cy="84" r="7" fill="#FF9E20" />
        </svg>
      </div>

      <div
        style={{
          position: 'absolute',
          left: 910,
          top: 218,
          width: 900,
          height: 620,
          borderRadius: 58,
          background: 'rgba(244,242,242,0.9)',
          boxShadow: '0 40px 110px rgba(29,33,40,0.28)',
          overflow: 'hidden',
          transform: `translateX(${(1 - sp(46)) * 90}px)`,
        }}
      >
        <div style={{ position: 'absolute', left: 48, top: 38, fontFamily: 'Inter', fontWeight: 900, fontSize: 30, color: '#C94B0A' }}>MANICURED PRESS RELEASE</div>
        <div
          style={{
            position: 'absolute',
            left: 56,
            top: 114,
            width: 340,
            height: 414,
            borderRadius: 34,
            background: '#F4F2F2',
            border: '8px solid #1D2128',
            transform: `translateX(${split * -24}px) rotate(${-split * 3}deg)`,
          }}
        >
          {Array.from({ length: 8 }).map((_, i) => (
            <div key={i} style={{ position: 'absolute', left: 36, top: 42 + i * 40, width: 250 - (i % 3) * 46, height: 14, borderRadius: 14, background: '#1D2128', opacity: 0.22 }} />
          ))}
          <div style={{ position: 'absolute', left: 36, bottom: 38, width: 180, height: 42, borderRadius: 18, background: '#FF9E20' }} />
        </div>

        <svg width="430" height="430" viewBox="0 0 430 430" style={{ position: 'absolute', right: 48, top: 122 }}>
          <ellipse cx="215" cy="306" rx={174 * moat} ry={58 * moat} fill="#FF9E20" stroke="#1D2128" strokeWidth="10" opacity="0.92" />
          <ellipse cx="215" cy="306" rx={112 * moat} ry={30 * moat} fill="#F4F2F2" opacity="0.72" />
          <rect x="146" y="178" width="138" height="122" rx="14" fill="#1D2128" opacity={moat} />
          <rect x="166" y="126" width="98" height="70" rx="12" fill="#1D2128" opacity={moat} />
          <rect x="122" y="228" width="42" height="72" fill="#1D2128" opacity={moat} />
          <rect x="266" y="228" width="42" height="72" fill="#1D2128" opacity={moat} />
          <path d="M160 178 L215 120 L270 178" fill="#1D2128" opacity={moat} />
          <text x="215" y="396" textAnchor="middle" fontFamily="Mont Heavy, Inter" fontSize="50" fill="#1D2128" opacity={moat}>ECONOMIC MOAT</text>
        </svg>

        <div style={{ position: 'absolute', left: 414, top: 114, width: 6, height: 414, background: '#1D2128', opacity: 0.22 }} />
        <div style={{ position: 'absolute', left: 46, bottom: 34, fontFamily: '"Mont Heavy", Inter', fontSize: 46, color: '#1D2128' }}>
          BUFFETT FRAMEWORK LOADING
        </div>
      </div>

      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.36) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 + Math.sin(frame / 24) * 0.08 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene20GroundTruthMoat;
```

