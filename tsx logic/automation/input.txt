```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11_UnderstandPhrase',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_UnderstandPhrase: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Magnifying glass sweep
  const lensX = interpolate(frame, [20, 90], [-200, 0], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const lensScale = sp(20);

  // Word reveal stagger
  const word1 = sp(40);
  const word2 = sp(55);
  const word3 = sp(70);
  const word4 = sp(85);

  // Underline draw
  const underlineW = interpolate(frame, [100, 140], [0, 900], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Bottom hint
  const hintOp = sp(150);

  // Camera zoom
  const camScale = interpolate(frame, [0, 90], [1.05, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(233,215,0,0.08), transparent 60%), radial-gradient(circle at 80% 90%, rgba(48,56,65,0.06), transparent 55%)',
      }} />

      {/* Shape Layers — drifting transparent geometry */}
      <div style={{
        position: 'absolute', top: '8%', left: '6%', width: 420, height: 420,
        border: '2px solid #303841', opacity: 0.06, borderRadius: '50%',
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', right: '8%', width: 320, height: 320,
        border: '2px solid #303841', opacity: 0.05,
        transform: `rotate(${-frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '55%', left: '75%', width: 180, height: 180,
        backgroundColor: '#e9d700', opacity: 0.07,
        transform: `rotate(45deg) translateY(${Math.sin(frame * 0.04) * 12}px)`,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="grid11" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid11)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 50) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + drift,
            width: 6, height: 6, borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : '#303841',
            opacity: 0.25,
          }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center' }}>
        {/* Top label */}
        <div style={{
          position: 'absolute', top: 120, left: 0, right: 0, textAlign: 'center',
          opacity: sp(10),
        }}>
          <span style={{
            fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 700, letterSpacing: 8,
            color: '#303841', backgroundColor: 'rgba(233,215,0,0.25)',
            padding: '10px 28px', borderRadius: 6,
          }}>
            THE FOUNDATION
          </span>
        </div>

        {/* Magnifying glass icon */}
        <div style={{
          position: 'absolute', top: 260, left: '50%',
          transform: `translateX(-50%) translateX(${lensX}px) scale(${lensScale})`,
        }}>
          <svg width="120" height="120" viewBox="0 0 120 120">
            <circle cx="48" cy="48" r="36" fill="none" stroke="#303841" strokeWidth="6" />
            <circle cx="48" cy="48" r="36" fill="rgba(233,215,0,0.15)" />
            <line x1="76" y1="76" x2="108" y2="108" stroke="#303841" strokeWidth="8" strokeLinecap="round" />
          </svg>
        </div>

        {/* Headline — staggered words */}
        <div style={{
          position: 'absolute', top: 440, left: 0, right: 0, textAlign: 'center',
          fontFamily: 'Inter, Poppins', fontWeight: 900,
          fontSize: 119, lineHeight: 1.05, color: '#000000',
        }}>
          <span style={{ display: 'inline-block', opacity: word1, transform: `translateY(${(1 - word1) * 30}px)` }}>YOU&nbsp;</span>
          <span style={{ display: 'inline-block', opacity: word2, transform: `translateY(${(1 - word2) * 30}px)` }}>MUST&nbsp;</span>
          <span style={{ display: 'inline-block', opacity: word3, transform: `translateY(${(1 - word3) * 30}px)` }}>UNDERSTAND&nbsp;</span>
          <span style={{
            display: 'inline-block', opacity: word4,
            transform: `translateY(${(1 - word4) * 30}px)`,
            backgroundColor: '#e9d700', padding: '0 20px', color: '#000000',
          }}>ONE PHRASE</span>
        </div>

        {/* Underline */}
        <div style={{
          position: 'absolute', top: 600, left: '50%', transform: 'translateX(-50%)',
          width: underlineW, height: 8, backgroundColor: '#303841',
        }} />

        {/* Bottom hint card */}
        <div style={{
          position: 'absolute', bottom: 160, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - hintOp) * 40}px)`,
          opacity: hintOp,
          padding: '28px 60px', border: '3px dashed #303841',
          backgroundColor: 'rgba(245,245,245,0.6)',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 38, color: '#303841', letterSpacing: 4 }}>
            → THE IRS SECRET KEYWORD ←
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_UnderstandPhrase;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12_ProvisionalIncome',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_ProvisionalIncome: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Hero text reveal
  const heroScale = sp(20, { damping: 14, stiffness: 100 });
  const heroOp = sp(20);

  // Highlight bar grows
  const highlightW = interpolate(frame, [50, 90], [0, 1100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Sub label
  const subOp = sp(80);

  // Stamp
  const stampScale = sp(110, { damping: 8, stiffness: 180 });
  const stampRot = interpolate(frame, [110, 140], [-25, -12], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Pulse
  const pulse = 1 + Math.sin(frame * 0.1) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(233,215,0,0.06) 0%, transparent 50%, rgba(48,56,65,0.05) 100%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '-10%', right: '-5%', width: 600, height: 600,
        backgroundColor: '#303841', opacity: 0.04, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', left: '-8%', width: 700, height: 700,
        backgroundColor: '#e9d700', opacity: 0.08,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="diag12" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M0,40 L40,0" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag12)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 10) * 1920;
        const py = (seededRandom(i + 60) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#e9d700', opacity: 0.4,
          }} />
        );
      })}

      {/* Top tag */}
      <div style={{
        position: 'absolute', top: 100, left: '50%', transform: 'translateX(-50%)',
        opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 32, color: '#F5F5F5',
          backgroundColor: '#303841', padding: '12px 36px', letterSpacing: 4,
        }}>
          IRS FORMULA KEY
        </div>
      </div>

      {/* Highlight bar behind hero */}
      <div style={{
        position: 'absolute', top: 400, left: '50%',
        transform: 'translateX(-50%)',
        width: highlightW, height: 180, backgroundColor: '#e9d700',
      }} />

      {/* Hero phrase */}
      <div style={{
        position: 'absolute', top: 380, left: 0, right: 0, textAlign: 'center',
        opacity: heroOp, transform: `scale(${heroScale * pulse})`,
        fontFamily: 'Inter, Poppins', fontWeight: 900,
        fontSize: 204, lineHeight: 1, color: '#000000', letterSpacing: -4,
      }}>
        PROVISIONAL
      </div>
      <div style={{
        position: 'absolute', top: 590, left: 0, right: 0, textAlign: 'center',
        opacity: sp(45), transform: `scale(${sp(45)})`,
        fontFamily: 'Inter, Poppins', fontWeight: 900,
        fontSize: 204, lineHeight: 1, color: '#303841', letterSpacing: -4,
      }}>
        INCOME
      </div>

      {/* Sub label */}
      <div style={{
        position: 'absolute', top: 820, left: 0, right: 0, textAlign: 'center',
        opacity: subOp,
        fontFamily: 'Inter', fontSize: 51, fontWeight: 600, color: '#303841',
        letterSpacing: 3,
      }}>
        THE MAGIC PHRASE THAT DECIDES YOUR TAX BILL
      </div>

      {/* Stamp */}
      <div style={{
        position: 'absolute', top: 340, right: 140,
        transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
      }}>
        <div style={{
          padding: '20px 36px',
          border: '6px solid #ff0000',
          fontFamily: 'Inter', fontWeight: 900, fontSize: 51,
          color: '#ff0000', letterSpacing: 4,
        }}>
          OFFICIAL
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_ProvisionalIncome;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13_FormulaBreakdown',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_FormulaBreakdown: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Three cards stagger
  const card1 = sp(20);
  const card2 = sp(55);
  const card3 = sp(90);

  // Plus signs
  const plus1 = sp(45);
  const plus2 = sp(80);

  // Equals + result
  const equals = sp(125);
  const result = sp(140, { damping: 10 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top, rgba(233,215,0,0.1), transparent 60%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '5%', width: 280, height: 280,
        border: '3px solid #303841', opacity: 0.06,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '15%', right: '8%', width: 360, height: 360,
        border: '3px solid #303841', opacity: 0.05, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="dot13" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#303841" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dot13)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const px = seededRandom(i + 20) * 1920;
        const py = seededRandom(i + 70) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.03 + i) * 15,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#e9d700', opacity: 0.4,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 90, left: 0, right: 0, textAlign: 'center',
        opacity: sp(0),
        fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 76,
        color: '#000000',
      }}>
        THE <span style={{ backgroundColor: '#e9d700', padding: '0 16px' }}>FORMULA</span>
      </div>
      <div style={{
        position: 'absolute', top: 200, left: 0, right: 0, textAlign: 'center',
        opacity: sp(10),
        fontFamily: 'JetBrains Mono', fontSize: 32, color: '#303841', letterSpacing: 3,
      }}>
        — HOW THE IRS DOES THE MATH —
      </div>

      {/* CARD 1 — AGI */}
      <div style={{
        position: 'absolute', top: 340, left: 100, width: 460, height: 380,
        backgroundColor: '#303841', color: '#F5F5F5',
        opacity: card1, transform: `translateY(${(1 - card1) * 60}px) scale(${card1})`,
        padding: 40, boxShadow: '12px 12px 0 #e9d700',
      }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#e9d700', letterSpacing: 3 }}>
          PART 01
        </div>
        <svg width="80" height="80" style={{ marginTop: 20 }} viewBox="0 0 80 80">
          <rect x="10" y="20" width="60" height="50" fill="none" stroke="#e9d700" strokeWidth="4" />
          <line x1="10" y1="35" x2="70" y2="35" stroke="#e9d700" strokeWidth="4" />
          <text x="40" y="58" textAnchor="middle" fill="#e9d700" fontSize="20" fontWeight="900" fontFamily="Inter">$</text>
        </svg>
        <div style={{ fontFamily: 'Inter', fontSize: 68, fontWeight: 900, marginTop: 18, lineHeight: 1 }}>
          AGI
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 27, fontWeight: 500, marginTop: 14, color: '#9E9E9E' }}>
          Adjusted Gross Income
        </div>
      </div>

      {/* PLUS 1 */}
      <div style={{
        position: 'absolute', top: 470, left: 580, width: 80, height: 80,
        opacity: plus1, transform: `scale(${plus1}) rotate(${plus1 * 180}deg)`,
        fontFamily: 'Inter', fontWeight: 900, fontSize: 102, color: '#ff0000',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
      }}>
        +
      </div>

      {/* CARD 2 — Tax Exempt */}
      <div style={{
        position: 'absolute', top: 340, left: 730, width: 460, height: 380,
        backgroundColor: '#F5F5F5', color: '#000000',
        opacity: card2, transform: `translateY(${(1 - card2) * 60}px) scale(${card2})`,
        padding: 40, border: '5px solid #303841', boxShadow: '12px 12px 0 #303841',
      }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#ff0000', letterSpacing: 3 }}>
          PART 02
        </div>
        <svg width="80" height="80" style={{ marginTop: 20 }} viewBox="0 0 80 80">
          <circle cx="40" cy="40" r="30" fill="none" stroke="#303841" strokeWidth="4" />
          <text x="40" y="50" textAnchor="middle" fill="#303841" fontSize="32" fontWeight="900" fontFamily="Inter">%</text>
        </svg>
        <div style={{ fontFamily: 'Inter', fontSize: 60, fontWeight: 900, marginTop: 18, lineHeight: 1 }}>
          TAX-EXEMPT
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 27, fontWeight: 500, marginTop: 14, color: '#303841' }}>
          Interest (Muni Bonds)
        </div>
      </div>

      {/* PLUS 2 */}
      <div style={{
        position: 'absolute', top: 470, left: 1210, width: 80, height: 80,
        opacity: plus2, transform: `scale(${plus2}) rotate(${plus2 * 180}deg)`,
        fontFamily: 'Inter', fontWeight: 900, fontSize: 102, color: '#ff0000',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
      }}>
        +
      </div>

      {/* CARD 3 — 50% SS */}
      <div style={{
        position: 'absolute', top: 340, left: 1360, width: 460, height: 380,
        backgroundColor: '#e9d700', color: '#000000',
        opacity: card3, transform: `translateY(${(1 - card3) * 60}px) scale(${card3})`,
        padding: 40, boxShadow: '12px 12px 0 #303841',
      }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#303841', letterSpacing: 3 }}>
          PART 03
        </div>
        <svg width="80" height="80" style={{ marginTop: 20 }} viewBox="0 0 80 80">
          <circle cx="40" cy="40" r="32" fill="none" stroke="#000000" strokeWidth="4" />
          <path d="M 40 8 A 32 32 0 0 1 40 72 Z" fill="#000000" />
        </svg>
        <div style={{ fontFamily: 'Inter', fontSize: 102, fontWeight: 900, marginTop: 8, lineHeight: 1 }}>
          50%
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 27, fontWeight: 500, marginTop: 8 }}>
          Of Social Security
        </div>
      </div>

      {/* EQUALS + RESULT */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0, textAlign: 'center',
        opacity: equals,
      }}>
        <span style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 76, color: '#303841' }}>=</span>
        <span style={{
          fontFamily: 'Inter', fontWeight: 900, fontSize: 76, color: '#000000',
          marginLeft: 30, opacity: result, display: 'inline-block',
          transform: `scale(${result})`,
        }}>
          <span style={{ backgroundColor: '#e9d700', padding: '0 24px' }}>PROVISIONAL INCOME</span>
        </span>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_FormulaBreakdown;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14_ThresholdCross',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_ThresholdCross: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bar fills up and crosses line
  const barProgress = interpolate(frame, [30, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const barFillWidth = barProgress * 1300;

  // Line draws
  const lineH = interpolate(frame, [10, 50], [0, 600], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // ALERT triggers when bar crosses
  const crossPoint = 0.65;
  const crossed = barProgress > crossPoint;
  const alertScale = crossed ? sp(115, { damping: 8, stiffness: 200 }) : 0;
  const shake = crossed ? Math.sin(frame * 0.8) * 6 : 0;

  // Labels
  const labelOp = sp(10);
  const thresholdLabelOp = sp(60);
  const irsLabelOp = sp(140);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient — turns reddish after cross */}
      <AbsoluteFill style={{
        background: crossed
          ? 'radial-gradient(circle at center, rgba(255,0,0,0.08), transparent 70%)'
          : 'radial-gradient(circle at center, rgba(233,215,0,0.06), transparent 60%)',
        transition: 'background 0.5s',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '5%', right: '5%', width: 380, height: 380,
        backgroundColor: crossed ? '#ff0000' : '#303841', opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '8%', left: '5%', width: 280, height: 280,
        border: '3px solid #303841', opacity: 0.07, borderRadius: '50%',
      }} />

      {/* Bar chart texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="vbar14" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="30" y1="0" x2="30" y2="60" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vbar14)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const px = seededRandom(i + 30) * 1920;
        const py = seededRandom(i + 80) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 12,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: crossed ? '#ff0000' : '#e9d700',
            opacity: 0.35,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center',
        opacity: labelOp, transform: `translateX(${shake}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 76, color: '#000000',
        }}>
          CROSS THE LINE → <span style={{ color: '#ff0000' }}>TAX BEGINS</span>
        </div>
      </div>

      {/* Bar track */}
      <div style={{
        position: 'absolute', top: 480, left: 200, width: 1500, height: 120,
        backgroundColor: '#F5F5F5', border: '4px solid #303841',
      }}>
        {/* Filled portion */}
        <div style={{
          width: barFillWidth, height: '100%',
          background: crossed
            ? 'linear-gradient(90deg, #e9d700 0%, #e9d700 60%, #ff0000 100%)'
            : '#e9d700',
        }} />

        {/* Animated ticker on tip */}
        <div style={{
          position: 'absolute', top: -50, left: barFillWidth - 80,
          fontFamily: 'JetBrains Mono', fontSize: 32, color: '#000000', fontWeight: 700,
          backgroundColor: '#F5F5F5', padding: '4px 12px', border: '2px solid #303841',
        }}>
          ${Math.floor(barProgress * 50000).toLocaleString()}
        </div>
      </div>

      {/* Threshold vertical line */}
      <div style={{
        position: 'absolute', top: 360, left: 200 + 1300 * crossPoint,
        width: 6, height: lineH, backgroundColor: '#ff0000',
        boxShadow: '0 0 20px rgba(255,0,0,0.6)',
      }} />
      <div style={{
        position: 'absolute', top: 320, left: 200 + 1300 * crossPoint - 130,
        opacity: thresholdLabelOp,
        fontFamily: 'Inter', fontWeight: 900, fontSize: 38, color: '#ff0000',
        backgroundColor: '#F5F5F5', padding: '6px 16px', border: '3px solid #ff0000',
      }}>
        THRESHOLD
      </div>

      {/* Zone labels */}
      <div style={{
        position: 'absolute', top: 640, left: 200,
        fontFamily: 'JetBrains Mono', fontSize: 27, color: '#303841', fontWeight: 700,
        opacity: labelOp,
      }}>
        ◄ SAFE ZONE — NO TAX
      </div>
      <div style={{
        position: 'absolute', top: 640, right: 220,
        fontFamily: 'JetBrains Mono', fontSize: 27, color: '#ff0000', fontWeight: 700,
        opacity: labelOp,
      }}>
        DANGER ZONE — IRS TAKES ►
      </div>

      {/* IRS ALERT badge */}
      <div style={{
        position: 'absolute', top: 770, left: '50%',
        transform: `translateX(-50%) scale(${alertScale}) translateX(${shake}px)`,
        opacity: irsLabelOp,
      }}>
        <div style={{
          backgroundColor: '#ff0000', color: '#F5F5F5',
          padding: '24px 60px',
          fontFamily: 'Inter', fontWeight: 900, fontSize: 64,
          letterSpacing: 4, border: '4px solid #000000',
        }}>
          ⚠  IRS STARTS TAXING  ⚠
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_ThresholdCross;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15_SingleFiler50',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_SingleFiler50: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header reveal
  const headerOp = sp(10);

  // Range bracket
  const bracketScale = sp(40);

  // Low / High numbers stagger
  const lowOp = sp(60);
  const highOp = sp(85);

  // Donut fills
  const donutProgress = interpolate(frame, [110, 170], [0, 0.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const donutOp = sp(100);

  // 50% number
  const pctScale = sp(120, { damping: 10 });
  const pctCount = interpolate(frame, [120, 180], [0, 50], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Donut math
  const radius = 130;
  const circ = 2 * Math.PI * radius;
  const dash = circ * donutProgress;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 0%, rgba(233,215,0,0.08) 100%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '-5%', width: 500, height: 500,
        backgroundColor: '#303841', opacity: 0.04, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '-3%', width: 400, height: 400,
        border: '4px solid #e9d700', opacity: 0.15,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Texture: concentric circles */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="circ15" width="120" height="120" patternUnits="userSpaceOnUse">
            <circle cx="60" cy="60" r="40" fill="none" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#circ15)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i + 40) * 1920;
        const py = seededRandom(i + 90) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.03 + i) * 14,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#303841', opacity: 0.3,
          }} />
        );
      })}

      {/* Header tag */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center',
        opacity: headerOp,
      }}>
        <span style={{
          fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 700,
          color: '#F5F5F5', backgroundColor: '#303841',
          padding: '12px 36px', letterSpacing: 4,
        }}>
          BRACKET 01 — SINGLE FILER
        </span>
      </div>

      {/* LEFT — Range visual */}
      <div style={{
        position: 'absolute', top: 240, left: 100, width: 900,
        opacity: bracketScale, transform: `scale(${bracketScale})`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 38, fontWeight: 700, color: '#303841',
          marginBottom: 30, letterSpacing: 2,
        }}>
          PROVISIONAL INCOME RANGE
        </div>

        {/* Bracket bar */}
        <div style={{
          height: 16, backgroundColor: '#e9d700', position: 'relative',
          marginBottom: 60,
        }}>
          <div style={{ position: 'absolute', left: 0, top: -20, width: 6, height: 56, backgroundColor: '#000000' }} />
          <div style={{ position: 'absolute', right: 0, top: -20, width: 6, height: 56, backgroundColor: '#000000' }} />
        </div>

        {/* LOW */}
        <div style={{
          position: 'absolute', left: -30, top: 130,
          opacity: lowOp, transform: `translateY(${(1 - lowOp) * 20}px)`,
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 27, color: '#303841' }}>FROM</div>
          <div style={{ fontFamily: 'Inter', fontSize: 102, fontWeight: 900, color: '#000000', lineHeight: 1 }}>
            $25K
          </div>
        </div>

        {/* TO */}
        <div style={{
          position: 'absolute', left: 380, top: 170,
          fontFamily: 'Inter', fontSize: 76, fontWeight: 900, color: '#e9d700',
          opacity: sp(75),
        }}>
          →
        </div>

        {/* HIGH */}
        <div style={{
          position: 'absolute', right: -20, top: 130,
          opacity: highOp, transform: `translateY(${(1 - highOp) * 20}px)`,
          textAlign: 'right',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 27, color: '#303841' }}>UP TO</div>
          <div style={{ fontFamily: 'Inter', fontSize: 102, fontWeight: 900, color: '#000000', lineHeight: 1 }}>
            $34K
          </div>
        </div>
      </div>

      {/* RIGHT — Donut */}
      <div style={{
        position: 'absolute', top: 280, right: 140,
        opacity: donutOp, transform: `scale(${donutOp})`,
      }}>
        <svg width="360" height="360" viewBox="0 0 360 360">
          {/* Background ring */}
          <circle cx="180" cy="180" r={radius} fill="none" stroke="#303841" strokeWidth="40" opacity="0.15" />
          {/* Progress ring */}
          <circle
            cx="180" cy="180" r={radius}
            fill="none" stroke="#e9d700" strokeWidth="40"
            strokeDasharray={`${dash} ${circ}`}
            transform="rotate(-90 180 180)"
            strokeLinecap="butt"
          />
        </svg>
        {/* Center percent */}
        <div style={{
          position: 'absolute', top: 0, left: 0, width: 360, height: 360,
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          flexDirection: 'column',
          transform: `scale(${pctScale})`,
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 119, fontWeight: 900,
            color: '#000000', lineHeight: 1,
          }}>
            {Math.floor(pctCount)}%
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 22, color: '#303841',
            marginTop: 4, letterSpacing: 2,
          }}>
            TAXABLE
          </div>
        </div>
      </div>

      {/* Bottom verdict */}
      <div style={{
        position: 'absolute', bottom: 100, left: 0, right: 0, textAlign: 'center',
        opacity: sp(160),
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 51, fontWeight: 800, color: '#000000',
        }}>
          Up to <span style={{ backgroundColor: '#e9d700', padding: '0 18px' }}>HALF</span> of your benefit becomes taxable
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_SingleFiler50;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16_JumpTo85',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_JumpTo85: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Threshold marker
  const thresholdOp = sp(10);

  // 50% step
  const step50 = sp(35);

  // JUMP arrow
  const jumpY = interpolate(frame, [70, 110], [0, -100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const arrowOp = sp(70);

  // 85% Hero
  const heroScale = sp(110, { damping: 8, stiffness: 180 });
  const heroCount = interpolate(frame, [110, 170], [50, 85], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Shake on impact
  const impactShake = frame > 110 && frame < 140 ? Math.sin(frame * 1.2) * 8 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient — intensifies */}
      <AbsoluteFill style={{
        background: frame > 100
          ? 'radial-gradient(circle at center, rgba(255,0,0,0.12), transparent 65%)'
          : 'radial-gradient(circle at center, rgba(233,215,0,0.08), transparent 60%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '-10%', left: '40%', width: 500, height: 500,
        backgroundColor: '#ff0000', opacity: frame > 100 ? 0.06 : 0.03,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', right: '-5%', width: 600, height: 600,
        border: '4px solid #303841', opacity: 0.05,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />

      {/* Texture: bold stripes */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="stripe16" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M0 80 L80 0" stroke="#303841" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#stripe16)" />
      </svg>

      {/* Particles — explode on impact */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 50) * 1920;
        const py = seededRandom(i + 100) * 1080;
        const explode = frame > 110 ? (frame - 110) * 2 : 0;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py - explode,
            width: 6, height: 6, borderRadius: '50%',
            backgroundColor: frame > 110 ? '#ff0000' : '#e9d700',
            opacity: 0.4,
          }} />
        );
      })}

      {/* Threshold pill */}
      <div style={{
        position: 'absolute', top: 100, left: '50%',
        transform: `translateX(-50%)`, opacity: thresholdOp,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 700,
          color: '#F5F5F5', backgroundColor: '#303841',
          padding: '14px 40px', letterSpacing: 4,
        }}>
          INCOME &gt; $34,000
        </div>
      </div>

      {/* Step staircase visualization */}
      <div style={{ position: 'absolute', top: 260, left: 240 }}>
        {/* 50% step */}
        <div style={{
          position: 'absolute', left: 0, top: 220,
          width: 380, height: 200,
          backgroundColor: '#e9d700',
          opacity: step50, transform: `scale(${step50})`, transformOrigin: 'bottom left',
          display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 102, fontWeight: 900, color: '#000000', lineHeight: 1 }}>
            50%
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#000000', marginTop: 8 }}>
            OLD ZONE
          </div>
        </div>

        {/* Jump arrow */}
        <div style={{
          position: 'absolute', left: 420, top: 120,
          opacity: arrowOp,
          transform: `translateY(${jumpY}px)`,
        }}>
          <svg width="120" height="280" viewBox="0 0 120 280">
            <path
              d="M 60 250 L 60 80 M 30 110 L 60 80 L 90 110"
              stroke="#ff0000" strokeWidth="14"
              fill="none" strokeLinecap="round" strokeLinejoin="round"
            />
          </svg>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 30, fontWeight: 900,
            color: '#ff0000', marginTop: 8, textAlign: 'center',
          }}>
            JUMP
          </div>
        </div>

        {/* 85% step */}
        <div style={{
          position: 'absolute', left: 580, top: 0,
          width: 480, height: 420,
          backgroundColor: '#ff0000',
          opacity: sp(105), transform: `scale(${sp(105)}) translateX(${impactShake}px)`,
          transformOrigin: 'bottom left',
          display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
          color: '#F5F5F5', boxShadow: '14px 14px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 221, fontWeight: 900,
            lineHeight: 1, transform: `scale(${heroScale})`,
          }}>
            {Math.floor(heroCount)}%
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 32, marginTop: 12, letterSpacing: 4,
          }}>
            DANGER ZONE
          </div>
        </div>
      </div>

      {/* Bottom subtext */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0, textAlign: 'center',
        opacity: sp(160),
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 51, fontWeight: 800, color: '#000000',
        }}>
          From half taxable → to <span style={{ color: '#ff0000' }}>almost everything taxed</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_JumpTo85;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17_MarriedBrackets',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_MarriedBrackets: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headerOp = sp(5);

  // Two rings of married symbols
  const ringScale = sp(25, { damping: 10 });

  // Table row reveals
  const row1 = sp(60);
  const row2 = sp(110);

  // Highlight sweep
  const sweepX = interpolate(frame, [160, 220], [-300, 1700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top, rgba(233,215,0,0.08), transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '8%', right: '6%', width: 360, height: 360,
        border: '3px solid #303841', opacity: 0.07,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', left: '5%', width: 300, height: 300,
        backgroundColor: '#e9d700', opacity: 0.08, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />

      {/* Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="cross17" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 30 10 L 30 50 M 10 30 L 50 30" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross17)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i + 60) * 1920;
        const py = seededRandom(i + 110) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.03 + i) * 12,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#303841', opacity: 0.3,
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center',
        opacity: headerOp,
      }}>
        <span style={{
          fontFamily: 'JetBrains Mono', fontSize: 32, color: '#F5F5F5',
          backgroundColor: '#303841', padding: '12px 36px', letterSpacing: 4,
        }}>
          BRACKET 02 — MARRIED / JOINT
        </span>
      </div>

      {/* Two rings icon */}
      <div style={{
        position: 'absolute', top: 180, left: '50%',
        transform: `translateX(-50%) scale(${ringScale})`,
      }}>
        <svg width="200" height="120" viewBox="0 0 200 120">
          <circle cx="65" cy="60" r="40" fill="none" stroke="#e9d700" strokeWidth="8" />
          <circle cx="135" cy="60" r="40" fill="none" stroke="#303841" strokeWidth="8" />
        </svg>
      </div>

      {/* TABLE — two rows */}
      <div style={{ position: 'absolute', top: 360, left: 160, right: 160 }}>
        {/* Header row */}
        <div style={{
          display: 'flex', borderBottom: '4px solid #303841',
          paddingBottom: 16, marginBottom: 24,
          fontFamily: 'JetBrains Mono', fontSize: 30, color: '#303841', letterSpacing: 3,
          opacity: sp(40),
        }}>
          <div style={{ flex: 2 }}>INCOME RANGE</div>
          <div style={{ flex: 1, textAlign: 'center' }}>BENEFITS TAXED</div>
          <div style={{ flex: 1, textAlign: 'right' }}>STATUS</div>
        </div>

        {/* Row 1 — 50% tier */}
        <div style={{
          display: 'flex', alignItems: 'center',
          backgroundColor: '#e9d700', padding: '36px 40px',
          marginBottom: 24,
          opacity: row1, transform: `translateX(${(1 - row1) * -80}px)`,
        }}>
          <div style={{ flex: 2 }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#303841' }}>
              STARTS AT
            </div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 102, color: '#000000', lineHeight: 1 }}>
              $32,000
            </div>
          </div>
          <div style={{
            flex: 1, textAlign: 'center',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 119, color: '#000000',
          }}>
            50%
          </div>
          <div style={{
            flex: 1, textAlign: 'right',
            fontFamily: 'JetBrains Mono', fontSize: 27, color: '#000000', fontWeight: 700,
          }}>
            CAUTION
          </div>
        </div>

        {/* Row 2 — 85% tier */}
        <div style={{
          display: 'flex', alignItems: 'center',
          backgroundColor: '#000000', color: '#F5F5F5', padding: '36px 40px',
          opacity: row2, transform: `translateX(${(1 - row2) * 80}px)`,
          position: 'relative', overflow: 'hidden',
        }}>
          {/* Sweep highlight */}
          <div style={{
            position: 'absolute', top: 0, left: sweepX,
            width: 200, height: '100%',
            background: 'linear-gradient(90deg, transparent, rgba(255,0,0,0.4), transparent)',
          }} />

          <div style={{ flex: 2, position: 'relative' }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#e9d700' }}>
              OVER
            </div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 102, color: '#F5F5F5', lineHeight: 1 }}>
              $44,000
            </div>
          </div>
          <div style={{
            flex: 1, textAlign: 'center', position: 'relative',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 119, color: '#ff0000',
          }}>
            85%
          </div>
          <div style={{
            flex: 1, textAlign: 'right', position: 'relative',
            fontFamily: 'JetBrains Mono', fontSize: 27, color: '#ff0000', fontWeight: 700,
          }}>
            DANGER
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_MarriedBrackets;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18_DontBeFooled',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_DontBeFooled: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // "BUT" callout
  const butScale = sp(15, { damping: 9, stiffness: 180 });

  // Strike through
  const strikeW = interpolate(frame, [50, 90], [0, 700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Split visualization
  const splitOp = sp(100);
  const splitWidth = interpolate(frame, [100, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Wiggle
  const wiggle = Math.sin(frame * 0.15) * 2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(48,56,65,0.08) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '12%', right: '8%', width: 400, height: 400,
        backgroundColor: '#303841', opacity: 0.05,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', left: '4%', width: 280, height: 280,
        border: '3px solid #e9d700', opacity: 0.2, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />

      {/* Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="wave18" width="100" height="40" patternUnits="userSpaceOnUse">
            <path d="M 0 20 Q 25 0 50 20 T 100 20" stroke="#303841" strokeWidth="1" fill="none" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#wave18)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i + 70) * 1920;
        const py = seededRandom(i + 120) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.03 + i) * 12,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#e9d700', opacity: 0.35,
          }} />
        );
      })}

      {/* BUT callout — top left big */}
      <div style={{
        position: 'absolute', top: 110, left: 120,
        transform: `scale(${butScale}) rotate(${-4 + wiggle}deg)`,
      }}>
        <div style={{
          backgroundColor: '#ff0000', color: '#F5F5F5',
          padding: '20px 50px',
          fontFamily: 'Inter', fontWeight: 900, fontSize: 119,
          letterSpacing: 4, boxShadow: '10px 10px 0 #000000',
        }}>
          BUT WAIT
        </div>
      </div>

      {/* Crossed out misconception */}
      <div style={{
        position: 'absolute', top: 360, left: 120, right: 120,
        opacity: sp(40),
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 27, color: '#9E9E9E', letterSpacing: 3,
          marginBottom: 14,
        }}>
          WHAT YOU THINK ↓
        </div>
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <div style={{
            fontFamily: 'Inter', fontWeight: 800, fontSize: 76,
            color: '#9E9E9E',
          }}>
            "I only make $30K from my job"
          </div>
          {/* strike line */}
          <div style={{
            position: 'absolute', top: '50%', left: 0,
            width: strikeW, height: 8, backgroundColor: '#ff0000',
            transform: 'translateY(-50%)',
          }} />
        </div>
      </div>

      {/* The reality */}
      <div style={{
        position: 'absolute', top: 580, left: 120, right: 120,
        opacity: splitOp,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 27, color: '#303841', letterSpacing: 3,
          marginBottom: 18,
        }}>
          WHAT THE IRS SEES ↓
        </div>

        {/* Stack visualization */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 20 }}>
          {/* Your income */}
          <div style={{
            width: 380 * splitWidth, height: 140,
            backgroundColor: '#303841', color: '#F5F5F5',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            flexDirection: 'column', overflow: 'hidden',
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22 }}>YOUR INCOME</div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 60 }}>$30K</div>
          </div>

          {/* PLUS */}
          <div style={{
            fontFamily: 'Inter', fontWeight: 900, fontSize: 76, color: '#ff0000',
            opacity: sp(135),
          }}>
            +
          </div>

          {/* SS half */}
          <div style={{
            width: 380 * splitWidth, height: 140,
            backgroundColor: '#e9d700',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            flexDirection: 'column', overflow: 'hidden',
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#000000' }}>50% OF SS</div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 60, color: '#000000' }}>+ $12K</div>
          </div>

          {/* EQUALS */}
          <div style={{
            fontFamily: 'Inter', fontWeight: 900, fontSize: 76, color: '#303841',
            opacity: sp(155),
          }}>
            =
          </div>

          {/* TOTAL */}
          <div style={{
            width: 420 * splitWidth, height: 200,
            backgroundColor: '#000000', color: '#F5F5F5',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            flexDirection: 'column', overflow: 'hidden',
            boxShadow: '8px 8px 0 #e9d700',
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#e9d700' }}>PROVISIONAL</div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 76, color: '#F5F5F5' }}>$42K</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 20, color: '#ff0000', marginTop: 4 }}>
              ABOVE THRESHOLD
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_DontBeFooled;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19_EasyToCross',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_EasyToCross: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Calendar pages flip
  const yearFlip = (delay: number) => sp(delay, { damping: 14, stiffness: 100 });

  // Old vs Today
  const oldOp = yearFlip(20);
  const arrowOp = sp(80);
  const todayOp = yearFlip(110);

  // Lock icon
  const lockScale = sp(160, { damping: 8, stiffness: 180 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(48,56,65,0.06), transparent 65%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '8%', width: 320, height: 320,
        border: '3px solid #303841', opacity: 0.07,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '15%', right: '6%', width: 380, height: 380,
        backgroundColor: '#e9d700', opacity: 0.07, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />

      {/* Texture — calendar grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="cal19" width="100" height="100" patternUnits="userSpaceOnUse">
            <rect x="10" y="10" width="80" height="80" fill="none" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cal19)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const px = seededRandom(i + 80) * 1920;
        const py = seededRandom(i + 130) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.03 + i) * 12,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#303841', opacity: 0.3,
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 90, left: 0, right: 0, textAlign: 'center',
        opacity: sp(0),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 76, color: '#000000',
        }}>
          SAME RULES, <span style={{ backgroundColor: '#e9d700', padding: '0 16px' }}>FOR 40+ YEARS</span>
        </div>
      </div>

      {/* TIMELINE — Two cards */}
      <div style={{ position: 'absolute', top: 320, left: 0, right: 0, display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 80 }}>

        {/* OLD — 1983 */}
        <div style={{
          width: 460, height: 480,
          backgroundColor: '#303841', color: '#F5F5F5',
          padding: 40,
          opacity: oldOp, transform: `translateY(${(1 - oldOp) * 60}px) scale(${oldOp})`,
          position: 'relative',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#e9d700', letterSpacing: 4 }}>
            CREATED IN
          </div>
          <div style={{
            fontFamily: 'Inter', fontWeight: 900, fontSize: 187, lineHeight: 1, marginTop: 8,
          }}>
            1983
          </div>
          <div style={{ marginTop: 30, height: 4, backgroundColor: '#e9d700', width: 80 }} />
          <div style={{
            fontFamily: 'Inter', fontSize: 32, fontWeight: 600, marginTop: 30, lineHeight: 1.3,
          }}>
            Original IRS<br/>tax formula written
          </div>
          {/* dusty stamp */}
          <div style={{
            position: 'absolute', bottom: 30, right: 30,
            fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E',
            border: '2px solid #9E9E9E', padding: '4px 10px',
            transform: 'rotate(-8deg)',
          }}>
            VINTAGE
          </div>
        </div>

        {/* Arrow with year tick marks */}
        <div style={{
          opacity: arrowOp, display: 'flex', flexDirection: 'column', alignItems: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 24, color: '#303841', marginBottom: 16, letterSpacing: 3,
          }}>
            43 YEARS LATER
          </div>
          <svg width="200" height="80" viewBox="0 0 200 80">
            <line x1="10" y1="40" x2="180" y2="40" stroke="#ff0000" strokeWidth="6" />
            <path d="M 160 20 L 190 40 L 160 60" stroke="#ff0000" strokeWidth="6" fill="none" strokeLinejoin="round" />
            {/* tick marks */}
            {[40, 70, 100, 130].map((x, i) => (
              <line key={i} x1={x} y1="32" x2={x} y2="48" stroke="#303841" strokeWidth="2" />
            ))}
          </svg>
        </div>

        {/* TODAY — 2026 */}
        <div style={{
          width: 460, height: 480,
          backgroundColor: '#e9d700', color: '#000000',
          padding: 40,
          opacity: todayOp, transform: `translateY(${(1 - todayOp) * 60}px) scale(${todayOp})`,
          boxShadow: '14px 14px 0 #000000',
          position: 'relative',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#303841', letterSpacing: 4 }}>
            STILL USED IN
          </div>
          <div style={{
            fontFamily: 'Inter', fontWeight: 900, fontSize: 187, lineHeight: 1, marginTop: 8,
          }}>
            2026
          </div>
          <div style={{ marginTop: 30, height: 4, backgroundColor: '#000000', width: 80 }} />
          <div style={{
            fontFamily: 'Inter', fontSize: 32, fontWeight: 600, marginTop: 30, lineHeight: 1.3,
          }}>
            EXACT same math<br/>still active today
          </div>
          {/* Lock */}
          <div style={{
            position: 'absolute', bottom: 30, right: 30,
            transform: `scale(${lockScale})`,
          }}>
            <svg width="70" height="80" viewBox="0 0 70 80">
              <rect x="10" y="35" width="50" height="40" fill="#000000" rx="4" />
              <path d="M 20 35 L 20 22 A 15 15 0 0 1 50 22 L 50 35" stroke="#000000" strokeWidth="6" fill="none" />
              <circle cx="35" cy="55" r="5" fill="#e9d700" />
            </svg>
          </div>
        </div>
      </div>

      {/* Bottom punchline */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0, textAlign: 'center',
        opacity: sp(170),
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 47, fontWeight: 800, color: '#000000',
        }}>
          The formula is <span style={{ color: '#ff0000' }}>frozen in time</span> — your wallet isn't
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_EasyToCross;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20_Rule1Transition',
  durationInSeconds: 5,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20_Rule1Transition: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big "01" sweeps in
  const numX = interpolate(frame, [5, 50], [-800, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const numScale = sp(5, { damping: 14, stiffness: 100 });

  // RULE label
  const labelOp = sp(35);

  // Title
  const titleOp = sp(55);

  // Slash sweeps
  const slashX = interpolate(frame, [0, 80], [-2400, 2400], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(233,215,0,0.1) 0%, transparent 50%, rgba(48,56,65,0.08) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '-10%', right: '-10%', width: 700, height: 700,
        backgroundColor: '#e9d700', opacity: 0.1, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.05) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-20%', left: '-10%', width: 600, height: 600,
        backgroundColor: '#303841', opacity: 0.06,
        transform: `rotate(${frame * 0.3}deg)`,
      }} />

      {/* Diagonal stripes */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="diag20" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M0 60 L60 0" stroke="#303841" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag20)" />
      </svg>

      {/* Sweeping slash */}
      <div style={{
        position: 'absolute', top: 0, left: 0,
        width: 600, height: 1500,
        backgroundColor: '#e9d700', opacity: 0.5,
        transform: `translateX(${slashX}px) rotate(15deg)`,
        transformOrigin: 'center',
      }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 90) * 1920;
        const py = seededRandom(i + 140) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.05 + i) * 10,
            width: 6, height: 6, borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#e9d700' : '#303841',
            opacity: 0.4,
          }} />
        );
      })}

      {/* Big "01" — left */}
      <div style={{
        position: 'absolute', top: '50%', left: 180,
        transform: `translateY(-50%) translateX(${numX}px) scale(${numScale})`,
        fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 612,
        color: '#000000', lineHeight: 1, letterSpacing: -20,
      }}>
        01
      </div>

      {/* RIGHT — RULE label + Title */}
      <div style={{ position: 'absolute', top: '50%', right: 120, width: 900, transform: 'translateY(-50%)' }}>
        <div style={{
          opacity: labelOp, transform: `translateX(${(1 - labelOp) * 80}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#000000', color: '#e9d700',
            padding: '14px 36px',
            fontFamily: 'JetBrains Mono', fontSize: 38, fontWeight: 700,
            letterSpacing: 6, marginBottom: 30,
          }}>
            RULE
          </div>
        </div>

        <div style={{
          opacity: titleOp, transform: `translateX(${(1 - titleOp) * 80}px)`,
          fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 153,
          color: '#303841', lineHeight: 0.95, letterSpacing: -3,
        }}>
          FROZEN<br/>
          <span style={{ backgroundColor: '#ff0000', color: '#F5F5F5', padding: '0 16px' }}>
            THRESHOLDS
          </span>
        </div>

        <div style={{
          opacity: sp(75),
          fontFamily: 'JetBrains Mono', fontSize: 30, color: '#303841',
          marginTop: 24, letterSpacing: 3,
        }}>
          → THE SILENT KILLER
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene20_Rule1Transition;
```

