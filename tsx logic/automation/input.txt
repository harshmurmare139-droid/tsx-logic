```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11_CASA',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_CASA: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.0], { extrapolateRight: 'clamp' });

  const titleIn = sp(6);
  const caIn = sp(55);
  const saIn = sp(70);
  const tagPop = sp(95, { stiffness: 150 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 35%, #C9CEFB 0%, #B7BDF7 50%, #8E96E6 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 25);
          return <div key={i} style={{ position: 'absolute', left: `${s * 78}%`, top: `${seededRandom(i + 100) * 65}%`, width: 400 + s * 240, height: 400 + s * 240, borderRadius: i % 2 ? '50%' : '24%', background: '#FFA3FD', opacity: 0.05, transform: `rotate(${frame * 0.09 + i * 30}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — vertical lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(16)].map((_, i) => <div key={i} style={{ position: 'absolute', left: `${(i / 16) * 100}%`, top: 0, bottom: 0, width: 1, background: '#000' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(18)].map((_, i) => {
        const s = seededRandom(i + 250);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontSize: 46, fontWeight: 700, color: '#1A1A2E', opacity: titleIn }}>
          सिर्फ़ रफ़्तार नहीं, Deposits का <span style={{ color: '#7C5BE0' }}>चरित्र</span> भी बदल रहा
        </div>

        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 170, lineHeight: 1, color: '#000', fontWeight: 700, marginTop: 20, transform: `scale(${sp(20)})`, textShadow: '0 6px 0 #FFA3FD' }}>
          CASA
        </div>

        {/* CA + SA cards */}
        <div style={{ display: 'flex', gap: 50, marginTop: 44 }}>
          <div style={{ opacity: caIn, transform: `translateY(${interpolate(caIn,[0,1],[40,0])}px)`, background: '#fff', border: '5px solid #000', borderRadius: 18, padding: '28px 44px', textAlign: 'center' }}>
            <svg width="70" height="70" viewBox="0 0 60 60"><rect x="8" y="14" width="44" height="34" rx="5" fill="#FFA3FD" stroke="#000" strokeWidth="4" /><rect x="8" y="22" width="44" height="8" fill="#000" /></svg>
            <div style={{ fontSize: 40, fontWeight: 700, color: '#000', marginTop: 10 }}>Current A/C</div>
          </div>
          <div style={{ fontSize: 90, fontWeight: 700, color: '#000', alignSelf: 'center' }}>+</div>
          <div style={{ opacity: saIn, transform: `translateY(${interpolate(saIn,[0,1],[40,0])}px)`, background: '#fff', border: '5px solid #000', borderRadius: 18, padding: '28px 44px', textAlign: 'center' }}>
            <svg width="70" height="70" viewBox="0 0 60 60"><path d="M30 8 L52 20 L52 46 L8 46 L8 20 Z" fill="#FFA3FD" stroke="#000" strokeWidth="4" strokeLinejoin="round" /><rect x="24" y="30" width="12" height="16" fill="#000" /></svg>
            <div style={{ fontSize: 40, fontWeight: 700, color: '#000', marginTop: 10 }}>Savings A/C</div>
          </div>
        </div>

        <div style={{ marginTop: 40, transform: `scale(${tagPop})`, fontSize: 48, fontWeight: 700, color: '#000', background: '#FFA3FD', padding: '10px 30px', borderRadius: 12 }}>
          सबसे सस्ता पैसा · न के बराबर ब्याज
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_CASA;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12_CASADrop',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_CASADrop: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.0], { extrapolateRight: 'clamp' });

  // bar heights: 42% -> 36%
  const bar1 = interpolate(sp(30, { stiffness: 40 }), [0, 1], [0, 320]);
  const bar2 = interpolate(sp(55, { stiffness: 40 }), [0, 1], [0, 274]);
  const num1 = (interpolate(sp(30), [0, 1], [0, 42])).toFixed(0);
  const num2 = (interpolate(sp(55), [0, 1], [0, 36])).toFixed(0);

  const stampPop = sp(95, { stiffness: 200 });
  const stampRot = interpolate(sp(95), [0, 1], [-18, -8]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(140deg, #C9CEFB 0%, #B7BDF7 55%, #9098E8 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 27);
          return <div key={i} style={{ position: 'absolute', left: `${s * 80}%`, top: `${seededRandom(i + 110) * 65}%`, width: 380 + s * 240, height: 380 + s * 240, borderRadius: '28%', border: '3px solid #FFA3FD', opacity: 0.06, transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(8)].map((_, r) => [...Array(16)].map((_, c) => (
          <div key={`${r}-${c}`} style={{ position: 'absolute', left: `${(c / 16) * 100}%`, top: `${(r / 8) * 100}%`, width: 4, height: 4, borderRadius: '50%', background: '#000' }} />
        )))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 270);
        const y = (s * 1080 + frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${y}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 78, color: '#000', fontWeight: 700, opacity: sp(4), marginBottom: 30 }}>
          CASA Ratio गिर रहा
        </div>

        {/* Bar chart drop */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 90, height: 360 }}>
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 64, fontWeight: 700, color: '#000', marginBottom: 8 }}>{num1}%</div>
            <div style={{ width: 150, height: bar1, background: '#fff', border: '5px solid #000', borderRadius: '10px 10px 0 0' }} />
            <div style={{ fontSize: 32, fontWeight: 700, color: '#1A1A2E', marginTop: 14 }}>मार्च 2022</div>
          </div>
          {/* down arrow */}
          <svg width="120" height="120" style={{ alignSelf: 'center', opacity: sp(70) }}><path d="M20 20 L80 90" stroke="#000" strokeWidth="8" strokeLinecap="round" /><path d="M80 90 l4 -30 l-30 4 z" fill="#000" /></svg>
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 64, fontWeight: 700, color: '#000', marginBottom: 8 }}>{num2}%</div>
            <div style={{ width: 150, height: bar2, background: '#FFA3FD', border: '5px solid #000', borderRadius: '10px 10px 0 0' }} />
            <div style={{ fontSize: 32, fontWeight: 700, color: '#1A1A2E', marginTop: 14 }}>जून 2025</div>
          </div>
        </div>

        {/* Crisil stamp */}
        <div style={{ marginTop: 40, transform: `scale(${stampPop}) rotate(${stampRot}deg)`, border: '6px solid #000', color: '#000', padding: '14px 36px', borderRadius: 14, fontFamily: 'JetBrains Mono, monospace', fontSize: 44, fontWeight: 700, background: '#FFA3FD' }}>
          CRISIL: "Structural Shift"
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_CASADrop;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13_Climate',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_Climate: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.07], { extrapolateRight: 'clamp' });

  // cross-out weather, reveal climate
  const strike = interpolate(sp(45, { stiffness: 60 }), [0, 1], [0, 1]);
  const qIn = sp(90, { stiffness: 140 });
  const qMark = 1 + Math.sin(frame * 0.15) * 0.03;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 40% 40%, #C9CEFB 0%, #B7BDF7 48%, #8A92E4 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(4)].map((_, i) => {
          const s = seededRandom(i + 29);
          return <div key={i} style={{ position: 'absolute', left: `${s * 82}%`, top: `${seededRandom(i + 120) * 70}%`, width: 320 + s * 260, height: 320 + s * 260, borderRadius: i % 2 ? '50%' : '20%', background: '#FFA3FD', opacity: 0.05, transform: `rotate(${frame * 0.09}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — concentric */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', opacity: 0.05 }}>
        {[...Array(5)].map((_, i) => <div key={i} style={{ position: 'absolute', width: 380 + i * 240, height: 380 + i * 240, border: '2px solid #000', borderRadius: '50%' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 290);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        {/* weather crossed out */}
        <div style={{ position: 'relative', opacity: sp(6) }}>
          <span style={{ fontSize: 70, fontWeight: 700, color: '#1A1A2E' }}>आने-जाने वाला मौसम</span>
          <div style={{ position: 'absolute', top: '50%', left: 0, height: 8, background: '#E5484D', width: `${strike * 100}%`, borderRadius: 4 }} />
        </div>

        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 116, lineHeight: 1, color: '#000', fontWeight: 700, marginTop: 30, transform: `scale(${sp(50)})`, textShadow: '0 6px 0 #FFA3FD' }}>
          पूरा <span style={{ background: '#FFA3FD', padding: '0 16px', borderRadius: 14 }}>Climate</span> बदल गया
        </div>

        {/* Big question */}
        <div style={{ marginTop: 60, transform: `scale(${qIn})`, display: 'flex', alignItems: 'center', gap: 24 }}>
          <span style={{ fontSize: 56, fontWeight: 700, color: '#000' }}>अब ये पैसा जा कहाँ रहा है</span>
          <span style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 130, fontWeight: 700, color: '#7C5BE0', transform: `scale(${qMark})` }}>?</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 340px rgba(0,0,0,0.3)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_Climate;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14_Phone',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_Phone: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.0], { extrapolateRight: 'clamp' });

  const phoneIn = sp(6);
  const dests = [sp(50), sp(65), sp(80)];
  const flowDash = -(frame * 4) % 20;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #C9CEFB 0%, #B7BDF7 50%, #949CEA 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 31);
          return <div key={i} style={{ position: 'absolute', left: `${s * 78}%`, top: `${seededRandom(i + 130) * 65}%`, width: 400 + s * 240, height: 400 + s * 240, borderRadius: i % 2 ? '50%' : '24%', background: '#FFA3FD', opacity: 0.05, transform: `rotate(${frame * 0.09 + i * 30}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — vertical lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(16)].map((_, i) => <div key={i} style={{ position: 'absolute', left: `${(i / 16) * 100}%`, top: 0, bottom: 0, width: 1, background: '#000' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 310);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 70 }}>
        {/* Phone */}
        <div style={{ transform: `scale(${phoneIn})` }}>
          <svg width="260" height="500" viewBox="0 0 200 400">
            <rect x="20" y="10" width="160" height="380" rx="28" fill="#000" />
            <rect x="30" y="40" width="140" height="320" rx="8" fill="#B7BDF7" />
            <circle cx="100" cy="25" r="5" fill="#FFA3FD" />
            {/* app tiles */}
            {[...Array(6)].map((_, i) => (
              <rect key={i} x={45 + (i % 2) * 62} y={60 + Math.floor(i / 2) * 62} width="48" height="48" rx="10" fill={i % 2 ? '#FFA3FD' : '#fff'} stroke="#000" strokeWidth="3" />
            ))}
            <rect x="45" y="290" width="110" height="40" rx="10" fill="#FFA3FD" stroke="#000" strokeWidth="3" />
            <text x="100" y="317" textAnchor="middle" fontSize="20" fontWeight="700" fill="#000" fontFamily="Inter, sans-serif">SIP</text>
          </svg>
        </div>

        {/* flow arrows to 3 destinations */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 26 }}>
          <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 60, color: '#000', fontWeight: 700, marginBottom: 10 }}>पैसा खिसक रहा →</div>
          {['Mutual Fund', 'शेयर बाज़ार', 'ऊँची FD'].map((d, i) => (
            <div key={i} style={{ opacity: dests[i], transform: `translateX(${interpolate(dests[i],[0,1],[50,0])}px)`, display: 'flex', alignItems: 'center', gap: 20 }}>
              <svg width="90" height="30"><line x1="0" y1="15" x2="70" y2="15" stroke="#000" strokeWidth="5" strokeDasharray="8 6" strokeDashoffset={flowDash} /><path d="M70 15 l-18 -11 l0 22 z" fill="#000" transform="translate(16,0)" /></svg>
              <span style={{ fontSize: 50, fontWeight: 700, color: '#000', background: '#FFA3FD', padding: '6px 22px', borderRadius: 12 }}>{d}</span>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Bottom punch line */}
      <div style={{ position: 'absolute', bottom: 90, left: 0, right: 0, textAlign: 'center', fontSize: 42, fontWeight: 700, color: '#1A1A2E', opacity: sp(95) }}>
        SIP शुरू करना अब <span style={{ color: '#7C5BE0' }}>पिज़्ज़ा मंगवाने</span> जितना आसान
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_Phone;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15_Samajhdari',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_Samajhdari: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.0], { extrapolateRight: 'clamp' });

  // check draw
  const checkLen = interpolate(sp(40, { stiffness: 50 }), [0, 1], [0, 100]);
  const bulbGlow = 0.4 + Math.sin(frame * 0.12) * 0.3;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 40%, #C9CEFB 0%, #B7BDF7 50%, #8E96E6 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 33);
          return <div key={i} style={{ position: 'absolute', left: `${s * 78}%`, top: `${seededRandom(i + 140) * 65}%`, width: 400 + s * 240, height: 400 + s * 240, borderRadius: '50%', border: '3px solid #FFA3FD', opacity: 0.06, transform: `rotate(${frame * 0.09}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(8)].map((_, r) => [...Array(14)].map((_, c) => (
          <div key={`${r}-${c}`} style={{ position: 'absolute', left: `${(c / 14) * 100}%`, top: `${(r / 8) * 100}%`, width: 4, height: 4, borderRadius: '50%', background: '#000' }} />
        )))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 330);
        const y = (s * 1080 + frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${y}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        {/* check + bulb */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 60, transform: `scale(${sp(6)})` }}>
          <svg width="180" height="180" viewBox="0 0 120 120">
            <circle cx="60" cy="60" r="52" fill="#FFA3FD" stroke="#000" strokeWidth="7" />
            <path d="M35 62 L52 80 L88 40" fill="none" stroke="#000" strokeWidth="10" strokeLinecap="round" strokeLinejoin="round" strokeDasharray="100" strokeDashoffset={100 - checkLen} />
          </svg>
          {/* bulb = awareness */}
          <svg width="150" height="180" viewBox="0 0 100 120">
            <circle cx="50" cy="45" r="34" fill="#FFA3FD" opacity={bulbGlow} />
            <circle cx="50" cy="45" r="30" fill="none" stroke="#000" strokeWidth="6" />
            <rect x="38" y="74" width="24" height="18" rx="3" fill="#000" />
            <line x1="42" y1="98" x2="58" y2="98" stroke="#000" strokeWidth="5" strokeLinecap="round" />
          </svg>
        </div>

        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 100, lineHeight: 1.05, color: '#000', fontWeight: 700, marginTop: 40, textAlign: 'center', transform: `translateY(${interpolate(sp(30),[0,1],[40,0])}px)`, textShadow: '0 5px 0 #FFA3FD' }}>
          इसमें कुछ गलत नहीं
        </div>

        <div style={{ fontSize: 50, fontWeight: 700, color: '#1A1A2E', marginTop: 26, opacity: sp(60) }}>
          ये तो <span style={{ background: '#FFA3FD', padding: '0 14px', borderRadius: 10 }}>समझदारी और जागरूकता</span> की निशानी है
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.24)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_Samajhdari;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16_DonoTaraf',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_DonoTaraf: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.03, 1.0], { extrapolateRight: 'clamp' });

  const leftIn = sp(30);
  const rightIn = sp(50);
  // scale tilt: balance tips toward headache
  const tilt = interpolate(sp(70, { stiffness: 40 }), [0, 1], [0, -8]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #C9CEFB 0%, #B7BDF7 55%, #9098E8 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 35);
          return <div key={i} style={{ position: 'absolute', left: `${s * 80}%`, top: `${seededRandom(i + 150) * 65}%`, width: 380 + s * 240, height: 380 + s * 240, borderRadius: i % 2 ? '50%' : '24%', background: '#FFA3FD', opacity: 0.05, transform: `rotate(${frame * 0.09}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — vertical center split lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(14)].map((_, i) => <div key={i} style={{ position: 'absolute', left: `${(i / 14) * 100}%`, top: 0, bottom: 0, width: 1, background: '#000' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 350);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 74, color: '#000', fontWeight: 700, marginBottom: 30, opacity: sp(4) }}>
          दोनों तरफ देखो
        </div>

        {/* Balance scale */}
        <svg width="700" height="120" viewBox="0 0 700 120" style={{ marginBottom: 10 }}>
          <line x1="350" y1="10" x2="350" y2="40" stroke="#000" strokeWidth="6" />
          <g transform={`rotate(${tilt} 350 40)`}>
            <line x1="120" y1="40" x2="580" y2="40" stroke="#000" strokeWidth="7" strokeLinecap="round" />
            <line x1="120" y1="40" x2="120" y2="80" stroke="#000" strokeWidth="4" />
            <line x1="580" y1="40" x2="580" y2="80" stroke="#000" strokeWidth="4" />
            <path d="M80 80 h80 l-14 30 h-52 z" fill="#fff" stroke="#000" strokeWidth="4" />
            <path d="M540 80 h80 l-14 30 h-52 z" fill="#FFA3FD" stroke="#000" strokeWidth="4" />
          </g>
        </svg>

        {/* two cards */}
        <div style={{ display: 'flex', gap: 60 }}>
          <div style={{ opacity: leftIn, transform: `translateX(${interpolate(leftIn,[0,1],[-50,0])}px)`, background: '#fff', border: '5px solid #000', borderRadius: 18, padding: '30px 40px', width: 400, textAlign: 'center' }}>
            <div style={{ fontSize: 40, fontWeight: 700, color: '#7C5BE0' }}>Saver के लिए</div>
            <div style={{ fontSize: 48, fontWeight: 700, color: '#000', marginTop: 8 }}>अच्छी खबर</div>
          </div>
          <div style={{ opacity: rightIn, transform: `translateX(${interpolate(rightIn,[0,1],[50,0])}px)`, background: '#FFA3FD', border: '5px solid #000', borderRadius: 18, padding: '30px 40px', width: 400, textAlign: 'center' }}>
            <div style={{ fontSize: 40, fontWeight: 700, color: '#000' }}>बैंकों के लिए</div>
            <div style={{ fontSize: 48, fontWeight: 700, color: '#000', marginTop: 8 }}>सिरदर्द</div>
          </div>
        </div>

        <div style={{ fontSize: 40, fontWeight: 700, color: '#1A1A2E', marginTop: 40, opacity: sp(95), textAlign: 'center', maxWidth: 1200 }}>
          इतिहास गवाह — Saver ने दशकों नुकसान वाला रिटर्न चुपचाप स्वीकारा
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_DonoTaraf;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17_Mehngai',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_Mehngai: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.0], { extrapolateRight: 'clamp' });

  // two bars: inflation 6.6 tall, savings 3-5 short
  const barInf = interpolate(sp(30, { stiffness: 40 }), [0, 1], [0, 340]);
  const barSav = interpolate(sp(50, { stiffness: 40 }), [0, 1], [0, 200]);
  const numInf = (interpolate(sp(30), [0, 1], [0, 6.6])).toFixed(1);

  const rukoIn = sp(90, { stiffness: 160 });
  const rukoPulse = 1 + Math.sin(frame * 0.2) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 55% 40%, #C9CEFB 0%, #B7BDF7 50%, #8A92E4 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 37);
          return <div key={i} style={{ position: 'absolute', left: `${s * 80}%`, top: `${seededRandom(i + 160) * 65}%`, width: 380 + s * 240, height: 380 + s * 240, borderRadius: '26%', border: '3px solid #FFA3FD', opacity: 0.06, transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(8)].map((_, r) => [...Array(16)].map((_, c) => (
          <div key={`${r}-${c}`} style={{ position: 'absolute', left: `${(c / 16) * 100}%`, top: `${(r / 8) * 100}%`, width: 4, height: 4, borderRadius: '50%', background: '#000' }} />
        )))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 370);
        const y = (s * 1080 + frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${y}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 64, color: '#000', fontWeight: 700, opacity: sp(4), marginBottom: 30 }}>
          30 साल · महंगाई बनाम Savings
        </div>

        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 100, height: 360 }}>
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 70, fontWeight: 700, color: '#000', marginBottom: 8 }}>{numInf}%</div>
            <div style={{ width: 170, height: barInf, background: '#FFA3FD', border: '5px solid #000', borderRadius: '10px 10px 0 0' }} />
            <div style={{ fontSize: 34, fontWeight: 700, color: '#1A1A2E', marginTop: 14 }}>महंगाई</div>
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 70, fontWeight: 700, color: '#000', marginBottom: 8 }}>3-5%</div>
            <div style={{ width: 170, height: barSav, background: '#fff', border: '5px solid #000', borderRadius: '10px 10px 0 0' }} />
            <div style={{ fontSize: 34, fontWeight: 700, color: '#1A1A2E', marginTop: 14 }}>Savings</div>
          </div>
        </div>

        {/* Ruko */}
        <div style={{ marginTop: 44, transform: `scale(${rukoIn * rukoPulse})`, fontFamily: 'Yantra One, sans-serif', fontSize: 84, fontWeight: 700, color: '#000', background: '#FFA3FD', padding: '8px 40px', borderRadius: 14, border: '5px solid #000' }}>
          रुको — इसे फिर पढ़ो, धीरे से
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_Mehngai;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18_KhamoshTax',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_KhamoshTax: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.0], { extrapolateRight: 'clamp' });

  // passbook number goes up
  const pbNum = Math.floor(interpolate(sp(30, { stiffness: 25 }), [0, 1], [10000, 10850]));
  // real value line goes down
  const realProg = interpolate(sp(40, { stiffness: 30 }), [0, 1], [0, 1]);
  const W = 420, H = 200;
  const realPts = [...Array(7)].map((_, i) => {
    const x = (i / 6) * W;
    const y = (i / 6) * H * 0.7 * realProg;
    return `${x},${y}`;
  }).join(' ');

  const taxIn = sp(95, { stiffness: 150 });
  const taxRot = interpolate(sp(95), [0, 1], [-12, -6]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(150deg, #C9CEFB 0%, #B7BDF7 55%, #8E96E6 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 39);
          return <div key={i} style={{ position: 'absolute', left: `${s * 78}%`, top: `${seededRandom(i + 170) * 65}%`, width: 400 + s * 240, height: 400 + s * 240, borderRadius: i % 2 ? '50%' : '24%', background: '#FFA3FD', opacity: 0.05, transform: `rotate(${frame * 0.09}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — vertical lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(16)].map((_, i) => <div key={i} style={{ position: 'absolute', left: `${(i / 16) * 100}%`, top: 0, bottom: 0, width: 1, background: '#000' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 390);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontSize: 46, fontWeight: 700, color: '#1A1A2E', opacity: sp(4), marginBottom: 30, textAlign: 'center', maxWidth: 1300 }}>
          बैंक में पैसा रखकर तुम तकनीकी रूप से हर साल <span style={{ color: '#7C5BE0' }}>गरीब</span> होते रहे
        </div>

        {/* passbook up vs real value down */}
        <div style={{ display: 'flex', gap: 70, alignItems: 'center' }}>
          {/* passbook */}
          <div style={{ background: '#fff', border: '5px solid #000', borderRadius: 14, padding: '24px 34px', textAlign: 'center', opacity: sp(20) }}>
            <div style={{ fontSize: 28, fontWeight: 700, color: '#1A1A2E' }}>पासबुक नंबर ↑</div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 60, fontWeight: 700, color: '#000' }}>₹{pbNum.toLocaleString('en-IN')}</div>
          </div>
          {/* real value chart down */}
          <div style={{ background: '#FFA3FD', border: '5px solid #000', borderRadius: 14, padding: '20px 24px', opacity: sp(35) }}>
            <div style={{ fontSize: 28, fontWeight: 700, color: '#000', marginBottom: 8 }}>असली कीमत ↓</div>
            <svg width={W} height={H + 20}>
              <polyline points={realPts} fill="none" stroke="#000" strokeWidth="7" strokeLinecap="round" strokeLinejoin="round" />
              <circle cx={W} cy={H * 0.7 * realProg} r="8" fill="#fff" stroke="#000" strokeWidth="3" />
            </svg>
          </div>
        </div>

        {/* Silent tax stamp */}
        <div style={{ marginTop: 44, transform: `scale(${taxIn}) rotate(${taxRot}deg)`, background: '#000', color: '#fff', padding: '16px 44px', borderRadius: 14, fontFamily: 'Yantra One, sans-serif', fontSize: 72, fontWeight: 700 }}>
          खामोश <span style={{ color: '#FFA3FD' }}>टैक्स</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 340px rgba(0,0,0,0.3)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_KhamoshTax;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19_NoReceipt',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_NoReceipt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.03, 1.0], { extrapolateRight: 'clamp' });

  const noReceipt = sp(20);
  const noNotice = sp(35);
  const commentIn = sp(75, { stiffness: 140 });
  const cursorBlink = Math.sin(frame * 0.3) > 0 ? 1 : 0.2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 45%, #C9CEFB 0%, #B7BDF7 50%, #8A92E4 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 41);
          return <div key={i} style={{ position: 'absolute', left: `${s * 78}%`, top: `${seededRandom(i + 180) * 65}%`, width: 400 + s * 240, height: 400 + s * 240, borderRadius: '50%', border: '3px solid #FFA3FD', opacity: 0.06, transform: `rotate(${frame * 0.09}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(8)].map((_, r) => [...Array(14)].map((_, c) => (
          <div key={`${r}-${c}`} style={{ position: 'absolute', left: `${(c / 14) * 100}%`, top: `${(r / 8) * 100}%`, width: 4, height: 4, borderRadius: '50%', background: '#000' }} />
        )))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 410);
        const y = (s * 1080 + frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${y}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        {/* crossed out receipt + notice */}
        <div style={{ display: 'flex', gap: 60 }}>
          {[{ label: 'रसीद', op: noReceipt }, { label: 'नोटिस', op: noNotice }].map((item, i) => (
            <div key={i} style={{ position: 'relative', opacity: item.op, transform: `scale(${item.op})` }}>
              <svg width="150" height="190" viewBox="0 0 100 130">
                <path d="M20 10 h60 v100 l-10 -8 l-10 8 l-10 -8 l-10 8 l-10 -8 l-10 8 z" fill="#fff" stroke="#000" strokeWidth="5" strokeLinejoin="round" />
                <line x1="32" y1="35" x2="68" y2="35" stroke="#000" strokeWidth="4" />
                <line x1="32" y1="52" x2="68" y2="52" stroke="#000" strokeWidth="4" />
                <line x1="32" y1="69" x2="58" y2="69" stroke="#000" strokeWidth="4" />
              </svg>
              {/* red cross */}
              <svg width="150" height="190" style={{ position: 'absolute', top: 0, left: 0 }}>
                <line x1="30" y1="20" x2="120" y2="170" stroke="#E5484D" strokeWidth="10" strokeLinecap="round" />
                <line x1="120" y1="20" x2="30" y2="170" stroke="#E5484D" strokeWidth="10" strokeLinecap="round" />
              </svg>
              <div style={{ textAlign: 'center', fontSize: 34, fontWeight: 700, color: '#000', marginTop: 6 }}>कोई {item.label} नहीं</div>
            </div>
          ))}
        </div>

        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 76, color: '#000', fontWeight: 700, marginTop: 36, opacity: sp(50), textAlign: 'center' }}>
          कटौती चुपचाप — नींद में, बिना जानकारी
        </div>

        {/* comment box CTA */}
        <div style={{ marginTop: 40, transform: `scale(${commentIn})`, background: '#fff', border: '5px solid #000', borderRadius: 16, padding: '20px 34px', display: 'flex', alignItems: 'center', gap: 16, maxWidth: 1200 }}>
          <svg width="50" height="50" viewBox="0 0 40 40"><path d="M6 6 h28 v20 h-18 l-10 8 z" fill="#FFA3FD" stroke="#000" strokeWidth="4" strokeLinejoin="round" /></svg>
          <span style={{ fontSize: 40, fontWeight: 700, color: '#000' }}>बचत का कितना % आज भी Savings में?</span>
          <span style={{ fontSize: 40, color: '#000', opacity: cursorBlink }}>|</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_NoReceipt;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20_Sawal',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20_Sawal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08], { extrapolateRight: 'clamp' });

  // question marks orbiting a head
  const qOrbit = frame * 0.03;
  const sawalIn = sp(80, { stiffness: 140 });
  const impactShake = frame > 80 && frame < 92 ? Math.sin(frame * 3) * interpolate(frame, [80, 92], [8, 0]) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, #C9CEFB 0%, #B7BDF7 45%, #868EE2 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(4)].map((_, i) => {
          const s = seededRandom(i + 43);
          return <div key={i} style={{ position: 'absolute', left: `${s * 82}%`, top: `${seededRandom(i + 190) * 70}%`, width: 320 + s * 260, height: 320 + s * 260, borderRadius: i % 2 ? '50%' : '20%', border: '3px solid #FFA3FD', opacity: 0.06, transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — concentric */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', opacity: 0.05 }}>
        {[...Array(6)].map((_, i) => <div key={i} style={{ position: 'absolute', width: 320 + i * 220, height: 320 + i * 220, border: '2px solid #000', borderRadius: '50%' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 430);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${impactShake}px, 0)`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontSize: 48, fontWeight: 700, color: '#1A1A2E', opacity: sp(6) }}>
          बिना झिझक लिखना — पता चले <span style={{ color: '#7C5BE0' }}>हम कहाँ खड़े हैं</span>
        </div>

        {/* head with orbiting question marks */}
        <div style={{ position: 'relative', width: 300, height: 300, marginTop: 40 }}>
          <svg width="300" height="300" viewBox="0 0 300 300">
            <circle cx="150" cy="150" r="70" fill="#fff" stroke="#000" strokeWidth="7" />
            <circle cx="128" cy="140" r="8" fill="#000" /><circle cx="172" cy="140" r="8" fill="#000" />
            <path d="M125 178 Q150 195 175 178" fill="none" stroke="#000" strokeWidth="6" strokeLinecap="round" />
          </svg>
          {[0, 1, 2, 3].map((i) => {
            const ang = qOrbit + (i * Math.PI) / 2;
            const x = 150 + Math.cos(ang) * 120;
            const y = 150 + Math.sin(ang) * 120;
            return <div key={i} style={{ position: 'absolute', left: x - 20, top: y - 30, fontFamily: 'Yantra One, sans-serif', fontSize: 70, fontWeight: 700, color: '#FFA3FD', WebkitTextStroke: '3px #000' }}>?</div>;
          })}
        </div>

        {/* payoff */}
        <div style={{ marginTop: 40, transform: `scale(${sawalIn})`, fontFamily: 'Yantra One, sans-serif', fontSize: 88, fontWeight: 700, color: '#000', textAlign: 'center', textShadow: '0 6px 0 #FFA3FD' }}>
          अब वो सबसे बड़ा सवाल
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 340px rgba(0,0,0,0.3)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene20_Sawal;
```

