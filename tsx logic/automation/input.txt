```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'NotOvernight',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const NotOvernight: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headlineY = interpolate(sp(10), [0, 1], [60, 0]);
  const headlineOp = sp(10);
  const crossLine = interpolate(sp(70), [0, 1], [0, 1]);
  const truthOp = sp(110);
  const truthY = interpolate(sp(110), [0, 1], [40, 0]);
  const tvShake = Math.sin(frame * 0.3) * 1.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 20%, #FFFFFF 0%, #E8E8E8 60%, #D5D5D5 100%)' }} />

      {/* Grid texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid1" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid1)" />
      </svg>

      {/* Floating shapes */}
      <div style={{ position: 'absolute', top: 100, right: 150, width: 280, height: 280, border: '2px solid #1C1C1C', opacity: 0.05, borderRadius: '50%', transform: `rotate(${frame * 0.2}deg)` }} />
      <div style={{ position: 'absolute', bottom: 80, left: 100, width: 220, height: 220, border: '2px solid #1C1C1C', opacity: 0.06, transform: `rotate(${-frame * 0.15}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const x = seededRandom(i) * 1920;
        const y = (seededRandom(i + 50) * 1080 + frame * 0.3) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#1C1C1C', opacity: 0.15 }} />;
      })}

      {/* Top label */}
      <div style={{ position: 'absolute', top: 80, left: 0, width: '100%', textAlign: 'center', opacity: headlineOp, transform: `translateY(${headlineY}px)` }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#9E9E9E', letterSpacing: 8 }}>// MEDIA NARRATIVE</div>
      </div>

      {/* TV/News Icon with myth */}
      <div style={{ position: 'absolute', top: 200, left: '50%', transform: `translateX(-50%) translateX(${tvShake}px)`, opacity: headlineOp }}>
        <svg width="180" height="140" viewBox="0 0 180 140">
          <rect x="10" y="10" width="160" height="100" rx="8" fill="none" stroke="#1C1C1C" strokeWidth="4" />
          <rect x="25" y="25" width="130" height="70" fill="#1C1C1C" opacity="0.1" />
          <text x="90" y="68" textAnchor="middle" fontFamily="Inter" fontSize="20" fontWeight="800" fill="#1C1C1C">BREAKING</text>
          <line x1="60" y1="120" x2="120" y2="120" stroke="#1C1C1C" strokeWidth="4" />
          <line x1="90" y1="110" x2="90" y2="130" stroke="#1C1C1C" strokeWidth="4" />
        </svg>
      </div>

      {/* The myth statement (will be crossed) */}
      <div style={{ position: 'absolute', top: 380, left: 0, width: '100%', textAlign: 'center', opacity: headlineOp, transform: `translateY(${headlineY}px)` }}>
        <div style={{ fontFamily: 'Yantra One', fontSize: 110, color: '#1C1C1C', lineHeight: 1.1 }}>
          "तबाही एक रात में आती है"
        </div>
        {/* Cross line */}
        <div style={{ position: 'absolute', top: '50%', left: '15%', width: `${70 * crossLine}%`, height: 8, backgroundColor: '#ff0000', borderRadius: 4 }} />
      </div>

      {/* Truth reveal */}
      <div style={{ position: 'absolute', bottom: 220, left: 0, width: '100%', textAlign: 'center', opacity: truthOp, transform: `translateY(${truthY}px)` }}>
        <div style={{ fontFamily: 'Inter', fontSize: 36, color: '#9E9E9E', letterSpacing: 4, marginBottom: 20 }}>SACH YEH HAI →</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 130, color: '#1C1C1C', textShadow: '4px 4px 0 #e9d700' }}>
          YEH JHOOTH HAI
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.25) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default NotOvernight;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'BridgeTermites',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BridgeTermites: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(8);
  const bridgeOp = sp(40);
  const bridgeScale = sp(40);
  const zoomScale = interpolate(frame, [120, 240], [1, 1.15], { extrapolateRight: 'clamp' });

  // Termite reveal stages
  const termite1 = sp(150);
  const termite2 = sp(180);
  const termite3 = sp(210);
  const cautionOp = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #F5F5F5 0%, #E0E0E0 100%)' }} />

      {/* Subtle dot pattern */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots2" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="2" cy="2" r="1.5" fill="#1C1C1C" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots2)" />
      </svg>

      {/* Floating soft shapes */}
      <div style={{ position: 'absolute', top: 60, left: 80, width: 320, height: 320, borderRadius: '50%', background: '#1C1C1C', opacity: 0.04, transform: `translateY(${Math.sin(frame * 0.02) * 10}px)` }} />
      <div style={{ position: 'absolute', bottom: 60, right: 100, width: 260, height: 260, background: '#1C1C1C', opacity: 0.04, transform: `rotate(45deg) translateY(${Math.cos(frame * 0.025) * 8}px)` }} />

      {/* Particles drifting */}
      {Array.from({ length: 25 }).map((_, i) => {
        const x = (seededRandom(i) * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(i + 100) * 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 3, height: 3, borderRadius: '50%', backgroundColor: '#1C1C1C', opacity: 0.2 }} />;
      })}

      {/* Top heading */}
      <div style={{ position: 'absolute', top: 60, left: 0, width: '100%', textAlign: 'center', opacity: titleOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#9E9E9E', letterSpacing: 6, marginBottom: 10 }}>// REAL ANALOGY</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 72, color: '#1C1C1C' }}>आर्थिक तबाही = पुल</div>
      </div>

      {/* BRIDGE SVG */}
      <div style={{ position: 'absolute', top: '38%', left: '50%', transform: `translateX(-50%) scale(${bridgeScale * zoomScale})`, opacity: bridgeOp }}>
        <svg width="1100" height="500" viewBox="0 0 1100 500">
          {/* Road on top */}
          <rect x="40" y="120" width="1020" height="40" fill="#1C1C1C" />
          <line x1="60" y1="140" x2="120" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeDasharray="20 20" />
          <line x1="200" y1="140" x2="280" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeDasharray="20 20" />
          <line x1="380" y1="140" x2="460" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeDasharray="20 20" />
          <line x1="560" y1="140" x2="640" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeDasharray="20 20" />
          <line x1="760" y1="140" x2="840" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeDasharray="20 20" />
          <line x1="940" y1="140" x2="1020" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeDasharray="20 20" />

          {/* Pillars */}
          <rect x="120" y="160" width="60" height="280" fill="#1C1C1C" />
          <rect x="380" y="160" width="60" height="280" fill="#1C1C1C" />
          <rect x="640" y="160" width="60" height="280" fill="#1C1C1C" />
          <rect x="900" y="160" width="60" height="280" fill="#1C1C1C" />

          {/* Ground */}
          <rect x="0" y="440" width="1100" height="60" fill="#9E9E9E" opacity="0.4" />

          {/* Termite holes - revealed gradually */}
          <g opacity={termite1}>
            <circle cx="150" cy="240" r="8" fill="#F5F5F5" />
            <circle cx="135" cy="280" r="6" fill="#F5F5F5" />
            <circle cx="165" cy="320" r="10" fill="#F5F5F5" />
            <circle cx="145" cy="380" r="7" fill="#F5F5F5" />
          </g>
          <g opacity={termite2}>
            <circle cx="410" cy="220" r="9" fill="#F5F5F5" />
            <circle cx="395" cy="290" r="7" fill="#F5F5F5" />
            <circle cx="425" cy="350" r="8" fill="#F5F5F5" />
          </g>
          <g opacity={termite3}>
            <circle cx="670" cy="250" r="10" fill="#F5F5F5" />
            <circle cx="655" cy="320" r="6" fill="#F5F5F5" />
            <circle cx="685" cy="390" r="9" fill="#F5F5F5" />
            <circle cx="930" cy="270" r="8" fill="#F5F5F5" />
            <circle cx="945" cy="340" r="7" fill="#F5F5F5" />
          </g>
        </svg>
      </div>

      {/* Caution label */}
      <div style={{ position: 'absolute', bottom: 100, left: 0, width: '100%', textAlign: 'center', opacity: cautionOp }}>
        <div style={{ display: 'inline-block', padding: '20px 50px', backgroundColor: '#1C1C1C', borderRadius: 8 }}>
          <span style={{ fontFamily: 'Yantra One', fontSize: 64, color: '#e9d700' }}>पिलर्स में दीमक लगी है</span>
        </div>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default BridgeTermites;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'AllLooksFine',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const AllLooksFine: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split screen: top normal life, bottom hidden danger
  const topOp = sp(10);
  const splitLine = interpolate(sp(80), [0, 1], [0, 1]);
  const bottomOp = sp(140);
  const warningOp = sp(220);

  // Cars moving
  const car1X = (frame * 6) % 1920;
  const car2X = (frame * 4 + 600) % 1920;
  const car3X = (frame * 7 + 1200) % 1920;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #F8F8F8 0%, #E8E8E8 50%, #2D2D2D 50%, #1C1C1C 100%)' }} />

      {/* Top half label */}
      <div style={{ position: 'absolute', top: 50, left: 80, opacity: topOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#9E9E9E', letterSpacing: 4 }}>// SURFACE</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 72, color: '#1C1C1C', marginTop: 10 }}>उपर से सब ठीक</div>
      </div>

      {/* Top right indicator */}
      <div style={{ position: 'absolute', top: 80, right: 80, opacity: topOp, display: 'flex', alignItems: 'center', gap: 15 }}>
        <div style={{ width: 16, height: 16, borderRadius: '50%', backgroundColor: '#22c55e', transform: `scale(${1 + Math.sin(frame * 0.15) * 0.2})` }} />
        <div style={{ fontFamily: 'Inter', fontSize: 32, color: '#1C1C1C', fontWeight: 700 }}>ALL SYSTEMS NORMAL</div>
      </div>

      {/* Road with cars */}
      <div style={{ position: 'absolute', top: '38%', left: 0, width: '100%', height: 100, opacity: topOp }}>
        <div style={{ width: '100%', height: 80, backgroundColor: '#1C1C1C', position: 'relative' }}>
          {/* Lane markers */}
          {Array.from({ length: 12 }).map((_, i) => (
            <div key={i} style={{ position: 'absolute', top: 38, left: i * 160, width: 80, height: 4, backgroundColor: '#F5F5F5' }} />
          ))}
          {/* Cars */}
          <div style={{ position: 'absolute', top: 12, left: car1X, width: 60, height: 28, backgroundColor: '#e9d700', borderRadius: 4 }} />
          <div style={{ position: 'absolute', top: 48, left: car2X, width: 60, height: 28, backgroundColor: '#F5F5F5', borderRadius: 4 }} />
          <div style={{ position: 'absolute', top: 12, left: car3X, width: 60, height: 28, backgroundColor: '#9E9E9E', borderRadius: 4 }} />
        </div>
      </div>

      {/* Center divider line - the lie */}
      <div style={{ position: 'absolute', top: '50%', left: 0, width: `${splitLine * 100}%`, height: 6, background: 'repeating-linear-gradient(90deg, #ff0000 0px, #ff0000 30px, transparent 30px, transparent 60px)' }} />

      {/* SUS label on divider */}
      <div style={{ position: 'absolute', top: '49.5%', right: 80, opacity: bottomOp, transform: 'translateY(-50%)' }}>
        <div style={{ padding: '8px 20px', backgroundColor: '#ff0000', fontFamily: 'JetBrains Mono', fontSize: 22, color: '#F5F5F5', fontWeight: 700, letterSpacing: 3 }}>UNDER THE SURFACE ↓</div>
      </div>

      {/* Bottom half - hidden rot */}
      <div style={{ position: 'absolute', bottom: 50, left: 80, opacity: bottomOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#9E9E9E', letterSpacing: 4 }}>// BENEATH</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 80, color: '#e9d700', marginTop: 10 }}>नीव खोखली हो रही है</div>
      </div>

      {/* Cracks SVG */}
      <div style={{ position: 'absolute', bottom: 250, right: 100, opacity: bottomOp }}>
        <svg width="400" height="300" viewBox="0 0 400 300">
          <path d="M 200 0 L 180 60 L 220 90 L 170 140 L 230 180 L 160 240 L 240 290" stroke="#ff0000" strokeWidth="4" fill="none" strokeLinecap="round" />
          <path d="M 200 60 L 250 80 M 220 90 L 280 100 M 170 140 L 110 160 M 230 180 L 290 210 M 160 240 L 100 270" stroke="#ff0000" strokeWidth="3" fill="none" strokeLinecap="round" />
        </svg>
      </div>

      {/* Reports stack icon */}
      <div style={{ position: 'absolute', bottom: 280, left: '50%', transform: 'translateX(-50%)', opacity: warningOp }}>
        <svg width="220" height="180" viewBox="0 0 220 180">
          <rect x="20" y="40" width="140" height="120" fill="#F5F5F5" stroke="#9E9E9E" strokeWidth="3" />
          <rect x="40" y="20" width="140" height="120" fill="#F5F5F5" stroke="#9E9E9E" strokeWidth="3" />
          <rect x="60" y="0" width="140" height="120" fill="#F5F5F5" stroke="#1C1C1C" strokeWidth="3" />
          <line x1="80" y1="30" x2="180" y2="30" stroke="#1C1C1C" strokeWidth="3" />
          <line x1="80" y1="50" x2="180" y2="50" stroke="#1C1C1C" strokeWidth="3" />
          <line x1="80" y1="70" x2="160" y2="70" stroke="#1C1C1C" strokeWidth="3" />
          <text x="130" y="100" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="700" fill="#22c55e">✓ OK</text>
        </svg>
        <div style={{ textAlign: 'center', marginTop: 10, fontFamily: 'Inter', fontSize: 22, color: '#F5F5F5', opacity: 0.7 }}>"Reports say all good"</div>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 60%, rgba(0,0,0,0.3) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default AllLooksFine;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'BeforeTheCrisis',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BeforeTheCrisis: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 130, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Timeline animation - BEFORE / CRISIS / AFTER
  const lineGrow = interpolate(sp(20), [0, 1], [0, 1]);
  const beforeOp = sp(50);
  const crisisOp = sp(100);
  const afterOp = sp(140);
  const highlightOp = sp(200);
  const finalOp = sp(260);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, #FFFFFF 0%, #DCDCDC 100%)' }} />

      {/* Hash texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="hash4" width="20" height="20" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="20" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hash4)" />
      </svg>

      {/* Title */}
      <div style={{ position: 'absolute', top: 70, left: 0, width: '100%', textAlign: 'center' }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#9E9E9E', letterSpacing: 8 }}>// CRISIS TIMELINE</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 72, color: '#1C1C1C', marginTop: 16 }}>असली खतरा कब है?</div>
      </div>

      {/* Timeline line */}
      <div style={{ position: 'absolute', top: '52%', left: '8%', width: `${84 * lineGrow}%`, height: 6, backgroundColor: '#1C1C1C' }} />

      {/* Three nodes */}
      {/* BEFORE */}
      <div style={{ position: 'absolute', top: '52%', left: '20%', transform: 'translate(-50%, -50%)', opacity: beforeOp }}>
        <div style={{ width: 50, height: 50, borderRadius: '50%', backgroundColor: '#F5F5F5', border: '6px solid #1C1C1C' }} />
      </div>
      <div style={{ position: 'absolute', top: '36%', left: '20%', transform: 'translateX(-50%)', textAlign: 'center', opacity: beforeOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E', letterSpacing: 3 }}>STAGE 1</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 52, color: '#1C1C1C' }}>BEFORE</div>
      </div>
      <div style={{ position: 'absolute', top: '60%', left: '20%', transform: 'translateX(-50%)', textAlign: 'center', opacity: beforeOp, width: 320 }}>
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#1C1C1C', fontWeight: 600 }}>सब चमकता है</div>
        <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#9E9E9E', marginTop: 6 }}>Property ↑ Stocks ↑</div>
      </div>

      {/* CRISIS - center */}
      <div style={{ position: 'absolute', top: '52%', left: '50%', transform: 'translate(-50%, -50%)', opacity: crisisOp }}>
        <div style={{ width: 90, height: 90, borderRadius: '50%', backgroundColor: '#ff0000', border: '6px solid #1C1C1C', boxShadow: `0 0 ${20 + Math.sin(frame * 0.1) * 10}px #ff0000` }} />
        <div style={{ position: 'absolute', top: '50%', left: '50%', transform: 'translate(-50%, -50%)', fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#F5F5F5' }}>!</div>
      </div>
      <div style={{ position: 'absolute', top: '34%', left: '50%', transform: 'translateX(-50%)', textAlign: 'center', opacity: crisisOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E', letterSpacing: 3 }}>STAGE 2</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 52, color: '#ff0000' }}>CRISIS</div>
      </div>
      <div style={{ position: 'absolute', top: '64%', left: '50%', transform: 'translateX(-50%)', textAlign: 'center', opacity: crisisOp, width: 320 }}>
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#1C1C1C', fontWeight: 600 }}>सब गिरता है</div>
      </div>

      {/* AFTER */}
      <div style={{ position: 'absolute', top: '52%', left: '80%', transform: 'translate(-50%, -50%)', opacity: afterOp }}>
        <div style={{ width: 50, height: 50, borderRadius: '50%', backgroundColor: '#F5F5F5', border: '6px solid #1C1C1C' }} />
      </div>
      <div style={{ position: 'absolute', top: '36%', left: '80%', transform: 'translateX(-50%)', textAlign: 'center', opacity: afterOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E', letterSpacing: 3 }}>STAGE 3</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 52, color: '#1C1C1C' }}>AFTER</div>
      </div>
      <div style={{ position: 'absolute', top: '60%', left: '80%', transform: 'translateX(-50%)', textAlign: 'center', opacity: afterOp, width: 320 }}>
        <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#1C1C1C', fontWeight: 600 }}>सब रोता है</div>
      </div>

      {/* HIGHLIGHT — danger zone is BEFORE */}
      <div style={{ position: 'absolute', top: '50%', left: '10%', width: '23%', height: 200, transform: 'translateY(-50%)', border: '5px dashed #ff0000', borderRadius: 16, opacity: highlightOp }} />

      {/* Bottom message */}
      <div style={{ position: 'absolute', bottom: 60, left: 0, width: '100%', textAlign: 'center', opacity: finalOp }}>
        <div style={{ display: 'inline-block', padding: '24px 60px', backgroundColor: '#1C1C1C' }}>
          <span style={{ fontFamily: 'Yantra One', fontSize: 56, color: '#e9d700' }}>← असली खतरा यहाँ है</span>
        </div>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default BeforeTheCrisis;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'GlowBeforeFall',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const GlowBeforeFall: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const sunRise = interpolate(sp(15), [0, 1], [200, 0]);
  const sunOp = sp(15);
  const sunGlow = 0.5 + Math.sin(frame * 0.06) * 0.1;
  const labelOp = sp(80);
  const term1Op = sp(140);
  const term2Op = sp(180);
  const term3Op = sp(220);
  const finalLabelOp = sp(290);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Dark gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 50% 80%, #2D2D2D 0%, #1C1C1C 70%)' }} />

      {/* Glow effect */}
      <div style={{ position: 'absolute', top: '40%', left: '50%', transform: 'translate(-50%, -50%)', width: 1200, height: 1200, borderRadius: '50%', background: `radial-gradient(circle, rgba(233,215,0,${sunGlow * 0.4}) 0%, transparent 50%)`, opacity: sunOp }} />

      {/* Floating gold particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const x = seededRandom(i) * 1920;
        const y = (seededRandom(i + 30) * 1080 - frame * 0.4 + 1080) % 1080;
        const size = 2 + seededRandom(i + 60) * 4;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: size, height: size, borderRadius: '50%', backgroundColor: '#e9d700', opacity: 0.4 + seededRandom(i + 90) * 0.4, boxShadow: '0 0 8px #e9d700' }} />;
      })}

      {/* Top label */}
      <div style={{ position: 'absolute', top: 80, left: 0, width: '100%', textAlign: 'center', opacity: labelOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#9E9E9E', letterSpacing: 8 }}>// THE PHENOMENON</div>
      </div>

      {/* Main glowing circle / sun */}
      <div style={{ position: 'absolute', top: 280, left: '50%', transform: `translateX(-50%) translateY(${sunRise}px)`, opacity: sunOp }}>
        <svg width="280" height="280" viewBox="0 0 280 280">
          <defs>
            <radialGradient id="sunGrad" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#FFC107" />
              <stop offset="60%" stopColor="#e9d700" />
              <stop offset="100%" stopColor="#FFC107" stopOpacity="0" />
            </radialGradient>
          </defs>
          <circle cx="140" cy="140" r="100" fill="url(#sunGrad)" />
          <circle cx="140" cy="140" r="70" fill="#e9d700" />
          {/* Rays */}
          {Array.from({ length: 12 }).map((_, i) => {
            const angle = (i * 30 + frame * 0.3) * Math.PI / 180;
            const x1 = 140 + Math.cos(angle) * 110;
            const y1 = 140 + Math.sin(angle) * 110;
            const x2 = 140 + Math.cos(angle) * 135;
            const y2 = 140 + Math.sin(angle) * 135;
            return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />;
          })}
        </svg>
      </div>

      {/* Floating term cards */}
      <div style={{ position: 'absolute', top: 380, left: 200, opacity: term1Op, transform: `translateY(${Math.sin(frame * 0.04) * 5}px)` }}>
        <div style={{ padding: '14px 28px', border: '2px solid #9E9E9E', borderRadius: 6, backgroundColor: 'rgba(245,245,245,0.05)' }}>
          <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E' }}>📈 Property ↑↑</div>
        </div>
      </div>
      <div style={{ position: 'absolute', top: 460, left: 1450, opacity: term2Op, transform: `translateY(${Math.cos(frame * 0.05) * 5}px)` }}>
        <div style={{ padding: '14px 28px', border: '2px solid #9E9E9E', borderRadius: 6, backgroundColor: 'rgba(245,245,245,0.05)' }}>
          <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E' }}>Easy Loans ✓</div>
        </div>
      </div>
      <div style={{ position: 'absolute', top: 580, left: 250, opacity: term3Op, transform: `translateY(${Math.sin(frame * 0.05 + 1) * 5}px)` }}>
        <div style={{ padding: '14px 28px', border: '2px solid #9E9E9E', borderRadius: 6, backgroundColor: 'rgba(245,245,245,0.05)' }}>
          <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E' }}>Sensex Record ↑</div>
        </div>
      </div>
      <div style={{ position: 'absolute', top: 600, left: 1400, opacity: term3Op, transform: `translateY(${Math.cos(frame * 0.04 + 2) * 5}px)` }}>
        <div style={{ padding: '14px 28px', border: '2px solid #9E9E9E', borderRadius: 6, backgroundColor: 'rgba(245,245,245,0.05)' }}>
          <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E' }}>India Shining ✨</div>
        </div>
      </div>

      {/* Bottom term */}
      <div style={{ position: 'absolute', bottom: 100, left: 0, width: '100%', textAlign: 'center', opacity: finalLabelOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 30, color: '#9E9E9E', letterSpacing: 4, marginBottom: 10 }}>// इसका नाम है</div>
        <div style={{ fontFamily: 'Inter', fontSize: 140, color: '#e9d700', fontWeight: 900, letterSpacing: 2, textShadow: '0 0 40px rgba(233,215,0,0.5)' }}>
          GLOW BEFORE FALL
        </div>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default GlowBeforeFall;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'IndiaBoom20032008',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const IndiaBoom20032008: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const chartProgress = interpolate(sp(50), [0, 1], [0, 1]);

  // Sensex chart points
  const startVal = 6000;
  const endVal = 21000;
  const currentVal = Math.floor(startVal + (endVal - startVal) * chartProgress);

  const chartPath = `M 100 500 Q 300 480 450 420 T 800 320 Q 1000 250 1200 150 T 1500 80`;

  const lowLabelOp = sp(80);
  const highLabelOp = sp(180);
  const factOp = sp(250);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #FFFFFF 0%, #E8E8E8 100%)' }} />

      {/* Grid */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid6" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid6)" />
      </svg>

      {/* Big background number */}
      <div style={{ position: 'absolute', top: 100, right: -80, fontFamily: 'Inter', fontSize: 480, fontWeight: 900, color: '#1C1C1C', opacity: 0.04 }}>
        2008
      </div>

      {/* Title */}
      <div style={{ position: 'absolute', top: 60, left: 80, opacity: titleOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#9E9E9E', letterSpacing: 6 }}>// CASE STUDY: INDIA</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 84, color: '#1C1C1C', marginTop: 8 }}>2003 → 2008 का दौर</div>
      </div>

      {/* Sensex live counter (top right) */}
      <div style={{ position: 'absolute', top: 80, right: 100, textAlign: 'right' }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E', letterSpacing: 4 }}>SENSEX</div>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 96, fontWeight: 900, color: chartProgress > 0.05 ? '#1C1C1C' : '#9E9E9E' }}>
          {currentVal.toLocaleString('en-IN')}
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#22c55e', fontWeight: 700 }}>
          ▲ +{((chartProgress * 250)).toFixed(1)}%
        </div>
      </div>

      {/* Chart area */}
      <div style={{ position: 'absolute', top: 320, left: 80, width: 1500, height: 600 }}>
        <svg width="1500" height="600" viewBox="0 0 1600 600">
          {/* Y-axis */}
          <line x1="80" y1="50" x2="80" y2="550" stroke="#1C1C1C" strokeWidth="3" />
          <line x1="80" y1="550" x2="1550" y2="550" stroke="#1C1C1C" strokeWidth="3" />

          {/* Y-axis labels */}
          <text x="60" y="60" textAnchor="end" fontFamily="JetBrains Mono" fontSize="20" fill="#9E9E9E">21K</text>
          <text x="60" y="200" textAnchor="end" fontFamily="JetBrains Mono" fontSize="20" fill="#9E9E9E">15K</text>
          <text x="60" y="350" textAnchor="end" fontFamily="JetBrains Mono" fontSize="20" fill="#9E9E9E">10K</text>
          <text x="60" y="500" textAnchor="end" fontFamily="JetBrains Mono" fontSize="20" fill="#9E9E9E">6K</text>

          {/* X-axis labels */}
          <text x="180" y="585" fontFamily="JetBrains Mono" fontSize="22" fill="#1C1C1C" fontWeight="700">2003</text>
          <text x="500" y="585" fontFamily="JetBrains Mono" fontSize="22" fill="#9E9E9E">2005</text>
          <text x="900" y="585" fontFamily="JetBrains Mono" fontSize="22" fill="#9E9E9E">2007</text>
          <text x="1380" y="585" fontFamily="JetBrains Mono" fontSize="22" fill="#1C1C1C" fontWeight="700">2008</text>

          {/* Chart line */}
          <path d="M 180 500 Q 380 480 530 420 T 880 320 Q 1080 250 1280 150 T 1520 80" stroke="#e9d700" strokeWidth="6" fill="none" strokeDasharray="2500" strokeDashoffset={2500 - 2500 * chartProgress} />

          {/* Area fill */}
          <path d={`M 180 500 Q 380 480 530 420 T 880 320 Q 1080 250 1280 150 T 1520 80 L 1520 550 L 180 550 Z`} fill="#e9d700" opacity={0.2 * chartProgress} />

          {/* Start dot */}
          <g opacity={lowLabelOp}>
            <circle cx="180" cy="500" r="12" fill="#1C1C1C" />
            <text x="180" y="475" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="22" fill="#1C1C1C" fontWeight="700">6,000</text>
          </g>

          {/* End dot */}
          <g opacity={highLabelOp}>
            <circle cx="1520" cy="80" r="14" fill="#ff0000" />
            <circle cx="1520" cy="80" r={20 + Math.sin(frame * 0.1) * 5} fill="none" stroke="#ff0000" strokeWidth="3" opacity="0.5" />
            <text x="1520" y="55" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="26" fill="#1C1C1C" fontWeight="800">21,000</text>
          </g>
        </svg>
      </div>

      {/* Bottom fact strip */}
      <div style={{ position: 'absolute', bottom: 30, left: 80, opacity: factOp, padding: '16px 36px', backgroundColor: '#1C1C1C' }}>
        <span style={{ fontFamily: 'Yantra One', fontSize: 40, color: '#e9d700' }}>एकानॉमी रॉकेट के तरह भागी</span>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 60%, rgba(0,0,0,0.25) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default IndiaBoom20032008;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'RealEstateBoom',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const RealEstateBoom: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const tier1Grow = interpolate(sp(50), [0, 1], [0, 1]);
  const tier2Grow = interpolate(sp(110), [0, 1], [0, 1]);
  const tier3Grow = interpolate(sp(170), [0, 1], [0, 1]);
  const fireOp = sp(230);
  const stampOp = sp(290);
  const stampScale = sp(290, { damping: 8, stiffness: 200 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #FFFFFF 0%, #DCDCDC 100%)' }} />

      {/* Background */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="grid7" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid7)" />
      </svg>

      {/* Floating shapes */}
      <div style={{ position: 'absolute', top: 60, right: 60, width: 200, height: 200, border: '2px solid #1C1C1C', opacity: 0.04, transform: `rotate(${frame * 0.3}deg)` }} />

      {/* Title */}
      <div style={{ position: 'absolute', top: 60, left: 80, opacity: titleOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#9E9E9E', letterSpacing: 5 }}>// REAL ESTATE INFLATION</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 72, color: '#1C1C1C', marginTop: 8 }}>हर शहर में आग</div>
      </div>

      {/* Three city columns */}
      <div style={{ position: 'absolute', top: 280, left: 0, width: '100%', display: 'flex', justifyContent: 'space-around', alignItems: 'flex-end', height: 600 }}>
        {/* Tier 1 - Mumbai */}
        <div style={{ textAlign: 'center', width: 360 }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E', letterSpacing: 3, marginBottom: 12 }}>TIER 1</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 44, color: '#1C1C1C', marginBottom: 16 }}>मुंबई</div>
          {/* Building stack */}
          <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end', height: 380 }}>
            <svg width="200" height={380 * tier1Grow} viewBox={`0 0 200 ${380 * tier1Grow}`} preserveAspectRatio="none">
              <rect x="40" y="0" width="120" height={380 * tier1Grow} fill="#1C1C1C" />
              {Array.from({ length: 12 }).map((_, i) => (
                <g key={i}>
                  <rect x="55" y={20 + i * 30} width="20" height="15" fill="#e9d700" opacity={tier1Grow} />
                  <rect x="90" y={20 + i * 30} width="20" height="15" fill="#e9d700" opacity={tier1Grow} />
                  <rect x="125" y={20 + i * 30} width="20" height="15" fill="#e9d700" opacity={tier1Grow} />
                </g>
              ))}
            </svg>
          </div>
          <div style={{ marginTop: 12, fontFamily: 'JetBrains Mono', fontSize: 32, color: '#1C1C1C', fontWeight: 800 }}>₹{(50 * tier1Grow).toFixed(0)}K/sqft</div>
        </div>

        {/* Tier 2 */}
        <div style={{ textAlign: 'center', width: 360 }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E', letterSpacing: 3, marginBottom: 12 }}>TIER 2</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 44, color: '#1C1C1C', marginBottom: 16 }}>जयपुर</div>
          <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end', height: 380 }}>
            <svg width="200" height={340 * tier2Grow} viewBox={`0 0 200 ${340 * tier2Grow}`} preserveAspectRatio="none">
              <rect x="40" y="0" width="120" height={340 * tier2Grow} fill="#1C1C1C" />
              {Array.from({ length: 11 }).map((_, i) => (
                <g key={i}>
                  <rect x="55" y={20 + i * 30} width="20" height="15" fill="#e9d700" opacity={tier2Grow} />
                  <rect x="90" y={20 + i * 30} width="20" height="15" fill="#e9d700" opacity={tier2Grow} />
                  <rect x="125" y={20 + i * 30} width="20" height="15" fill="#e9d700" opacity={tier2Grow} />
                </g>
              ))}
            </svg>
          </div>
          <div style={{ marginTop: 12, fontFamily: 'JetBrains Mono', fontSize: 32, color: '#1C1C1C', fontWeight: 800 }}>₹{(45 * tier2Grow).toFixed(0)}K/sqft</div>
        </div>

        {/* Tier 3 */}
        <div style={{ textAlign: 'center', width: 360 }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E', letterSpacing: 3, marginBottom: 12 }}>TIER 3</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 44, color: '#1C1C1C', marginBottom: 16 }}>नागपुर</div>
          <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end', height: 380 }}>
            <svg width="200" height={320 * tier3Grow} viewBox={`0 0 200 ${320 * tier3Grow}`} preserveAspectRatio="none">
              <rect x="40" y="0" width="120" height={320 * tier3Grow} fill="#1C1C1C" />
              {Array.from({ length: 10 }).map((_, i) => (
                <g key={i}>
                  <rect x="55" y={20 + i * 30} width="20" height="15" fill="#e9d700" opacity={tier3Grow} />
                  <rect x="90" y={20 + i * 30} width="20" height="15" fill="#e9d700" opacity={tier3Grow} />
                  <rect x="125" y={20 + i * 30} width="20" height="15" fill="#e9d700" opacity={tier3Grow} />
                </g>
              ))}
            </svg>
          </div>
          <div style={{ marginTop: 12, fontFamily: 'JetBrains Mono', fontSize: 32, color: '#1C1C1C', fontWeight: 800 }}>₹{(42 * tier3Grow).toFixed(0)}K/sqft</div>
        </div>
      </div>

      {/* Fire icon at top */}
      <div style={{ position: 'absolute', top: 240, left: '50%', transform: `translateX(-50%) scale(${1 + Math.sin(frame * 0.15) * 0.1})`, opacity: fireOp }}>
        <svg width="80" height="100" viewBox="0 0 80 100">
          <path d="M 40 10 Q 25 35 30 55 Q 15 50 20 75 Q 25 90 40 95 Q 55 90 60 75 Q 65 50 50 55 Q 55 35 40 10 Z" fill="#ff0000" />
          <path d="M 40 30 Q 32 50 38 65 Q 45 60 42 70 Q 40 80 40 85 Q 50 75 48 65 Q 52 50 40 30 Z" fill="#e9d700" />
        </svg>
      </div>

      {/* Stamp */}
      <div style={{ position: 'absolute', bottom: 60, right: 100, transform: `scale(${stampScale}) rotate(-12deg)`, opacity: stampOp }}>
        <div style={{ padding: '20px 40px', border: '6px solid #ff0000', backgroundColor: 'rgba(255,255,255,0.9)' }}>
          <div style={{ fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#ff0000', letterSpacing: 4 }}>INDIA SHINING</div>
        </div>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 60%, rgba(0,0,0,0.25) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default RealEstateBoom;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'TheQuestion',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const TheQuestion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const qOp = sp(10);
  const qScale = sp(10, { damping: 8, stiffness: 150 });
  const bar1 = interpolate(sp(70), [0, 1], [0, 1]);
  const bar2 = interpolate(sp(120), [0, 1], [0, 1]);
  const bar3 = interpolate(sp(170), [0, 1], [0, 1]);
  const answerOp = sp(260);
  const answerScale = sp(260, { damping: 10, stiffness: 180 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 30% 50%, #FFFFFF 0%, #D8D8D8 100%)' }} />

      {/* Texture - lines */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="line8" width="10" height="10" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="10" y2="10" stroke="#1C1C1C" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#line8)" />
      </svg>

      {/* Floating shapes */}
      <div style={{ position: 'absolute', top: 80, right: 200, width: 240, height: 240, borderRadius: '50%', border: '2px solid #1C1C1C', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})` }} />

      {/* Big question mark background */}
      <div style={{ position: 'absolute', top: 100, left: 100, fontFamily: 'Yantra One', fontSize: 800, color: '#1C1C1C', opacity: 0.05, lineHeight: 0.8, transform: `scale(${qScale})` }}>?</div>

      {/* Title */}
      <div style={{ position: 'absolute', top: 70, left: 80, opacity: qOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#9E9E9E', letterSpacing: 6 }}>// CRITICAL QUESTION</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 76, color: '#1C1C1C', marginTop: 8, lineHeight: 1.1 }}>
          लेकिन क्या <span style={{ color: '#ff0000' }}>आमदनी</span> भी<br />उतनी रफ्तार से बढ़ी?
        </div>
      </div>

      {/* Comparison bars */}
      <div style={{ position: 'absolute', top: 380, right: 100, width: 800 }}>
        {/* LOANS bar */}
        <div style={{ marginBottom: 30 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
            <span style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#1C1C1C' }}>LOANS / उधार</span>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 800, color: '#ff0000' }}>+340%</span>
          </div>
          <div style={{ width: '100%', height: 60, backgroundColor: '#E0E0E0', borderRadius: 4 }}>
            <div style={{ width: `${100 * bar1}%`, height: '100%', backgroundColor: '#ff0000', borderRadius: 4 }} />
          </div>
        </div>

        {/* STOCK MARKET */}
        <div style={{ marginBottom: 30 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
            <span style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#1C1C1C' }}>STOCK MARKET</span>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 800, color: '#ff0000' }}>+250%</span>
          </div>
          <div style={{ width: '100%', height: 60, backgroundColor: '#E0E0E0', borderRadius: 4 }}>
            <div style={{ width: `${78 * bar2}%`, height: '100%', backgroundColor: '#ff0000', borderRadius: 4 }} />
          </div>
        </div>

        {/* REAL INCOME */}
        <div style={{ marginBottom: 30 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
            <span style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#1C1C1C' }}>REAL INCOME / आमदनी</span>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 800, color: '#1C1C1C' }}>+18%</span>
          </div>
          <div style={{ width: '100%', height: 60, backgroundColor: '#E0E0E0', borderRadius: 4 }}>
            <div style={{ width: `${8 * bar3}%`, height: '100%', backgroundColor: '#1C1C1C', borderRadius: 4 }} />
          </div>
        </div>
      </div>

      {/* Answer reveal */}
      <div style={{ position: 'absolute', bottom: 80, left: 0, width: '100%', textAlign: 'center', opacity: answerOp, transform: `scale(${answerScale})` }}>
        <div style={{ display: 'inline-block', padding: '24px 70px', backgroundColor: '#1C1C1C' }}>
          <span style={{ fontFamily: 'Yantra One', fontSize: 90, color: '#e9d700' }}>जवाब है — नहीं</span>
        </div>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default TheQuestion;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'WhereDidMoneyGo',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WhereDidMoneyGo: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const sourceOp = sp(50);
  const arrowsOp = sp(120);
  const dest1Op = sp(160);
  const dest2Op = sp(200);
  const dest3Op = sp(240);
  const dest3Cross = sp(310);
  const verdictOp = sp(360);

  // Money particles flowing
  const flowProgress = (frame % 90) / 90;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #FFFFFF 0%, #E0E0E0 100%)' }} />

      {/* Texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="circ9" width="60" height="60" patternUnits="userSpaceOnUse">
            <circle cx="30" cy="30" r="1.5" fill="#1C1C1C" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#circ9)" />
      </svg>

      {/* Title */}
      <div style={{ position: 'absolute', top: 60, left: 0, width: '100%', textAlign: 'center', opacity: titleOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#9E9E9E', letterSpacing: 6 }}>// FOLLOW THE MONEY</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 78, color: '#1C1C1C', marginTop: 8 }}>उधार का पैसा कहाँ गया?</div>
      </div>

      {/* SOURCE - Bank icon (left) */}
      <div style={{ position: 'absolute', top: 380, left: 100, opacity: sourceOp, textAlign: 'center' }}>
        <svg width="180" height="180" viewBox="0 0 180 180">
          <polygon points="90,15 165,55 15,55" fill="#1C1C1C" />
          <rect x="15" y="55" width="150" height="10" fill="#1C1C1C" />
          <rect x="30" y="65" width="15" height="80" fill="#1C1C1C" />
          <rect x="65" y="65" width="15" height="80" fill="#1C1C1C" />
          <rect x="100" y="65" width="15" height="80" fill="#1C1C1C" />
          <rect x="135" y="65" width="15" height="80" fill="#1C1C1C" />
          <rect x="15" y="145" width="150" height="15" fill="#1C1C1C" />
        </svg>
        <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#1C1C1C', marginTop: 12 }}>BANK / उधार</div>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#ff0000', marginTop: 4, fontWeight: 700 }}>₹₹₹ FLOW</div>
      </div>

      {/* Flowing money dots */}
      {arrowsOp > 0.5 && Array.from({ length: 8 }).map((_, i) => {
        const t = ((frame * 0.015) + i * 0.12) % 1;
        const startX = 280;
        const startY = 460;
        const endX = 700;
        const endY = 380 + (i % 3) * 120;
        const x = startX + (endX - startX) * t;
        const y = startY + (endY - startY) * t;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 14, height: 14, borderRadius: '50%', backgroundColor: '#e9d700', opacity: 1 - t, boxShadow: '0 0 8px #e9d700' }} />;
      })}

      {/* DESTINATIONS - 3 boxes on right */}
      {/* Property */}
      <div style={{ position: 'absolute', top: 320, right: 100, opacity: dest1Op, width: 600 }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 30, padding: 24, border: '3px solid #ff0000', backgroundColor: 'rgba(255,255,255,0.7)' }}>
          <svg width="80" height="80" viewBox="0 0 80 80">
            <polygon points="40,10 75,40 65,40 65,70 15,70 15,40 5,40" fill="#1C1C1C" />
            <rect x="32" y="50" width="16" height="20" fill="#F5F5F5" />
            <rect x="22" y="45" width="10" height="10" fill="#F5F5F5" />
            <rect x="48" y="45" width="10" height="10" fill="#F5F5F5" />
          </svg>
          <div style={{ flex: 1 }}>
            <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 800, color: '#1C1C1C' }}>PROPERTY सट्टा</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#ff0000', marginTop: 4 }}>// Speculation, not housing</div>
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 900, color: '#ff0000' }}>45%</div>
        </div>
      </div>

      {/* Stocks */}
      <div style={{ position: 'absolute', top: 470, right: 100, opacity: dest2Op, width: 600 }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 30, padding: 24, border: '3px solid #ff0000', backgroundColor: 'rgba(255,255,255,0.7)' }}>
          <svg width="80" height="80" viewBox="0 0 80 80">
            <polyline points="10,60 25,45 40,55 55,25 70,15" fill="none" stroke="#1C1C1C" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" />
            <polygon points="65,15 75,15 70,8" fill="#1C1C1C" />
          </svg>
          <div style={{ flex: 1 }}>
            <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 800, color: '#1C1C1C' }}>STOCK सट्टा</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#ff0000', marginTop: 4 }}>// Bubble, not investing</div>
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 900, color: '#ff0000' }}>40%</div>
        </div>
      </div>

      {/* Factories — crossed out */}
      <div style={{ position: 'absolute', top: 620, right: 100, opacity: dest3Op, width: 600 }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 30, padding: 24, border: '3px solid #9E9E9E', backgroundColor: 'rgba(245,245,245,0.5)', position: 'relative' }}>
          <svg width="80" height="80" viewBox="0 0 80 80">
            <rect x="10" y="40" width="60" height="30" fill="#9E9E9E" />
            <rect x="20" y="20" width="10" height="20" fill="#9E9E9E" />
            <rect x="40" y="15" width="10" height="25" fill="#9E9E9E" />
            <rect x="60" y="25" width="10" height="15" fill="#9E9E9E" />
          </svg>
          <div style={{ flex: 1 }}>
            <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 800, color: '#9E9E9E' }}>FACTORY / Production</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E', marginTop: 4 }}>// Real growth — IGNORED</div>
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 900, color: '#9E9E9E' }}>15%</div>
          {/* Cross line */}
          <div style={{ position: 'absolute', top: '50%', left: '5%', width: `${90 * dest3Cross}%`, height: 4, backgroundColor: '#ff0000', transform: 'rotate(-3deg)' }} />
        </div>
      </div>

      {/* Bottom verdict */}
      <div style={{ position: 'absolute', bottom: 30, left: 0, width: '100%', textAlign: 'center', opacity: verdictOp }}>
        <div style={{ display: 'inline-block', padding: '16px 40px', backgroundColor: '#1C1C1C' }}>
          <span style={{ fontFamily: 'Yantra One', fontSize: 44, color: '#e9d700' }}>उत्पादन नहीं — सिर्फ बुलबुला</span>
        </div>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 60%, rgba(0,0,0,0.25) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default WhereDidMoneyGo;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'TheGoldenRule',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const TheGoldenRule: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.5, stiffness: 115, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);

  // Two race lines animation
  const debtRaceProgress = interpolate(frame, [60, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const incomeRaceProgress = interpolate(frame, [60, 320], [0, 0.4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const ruleOp = sp(330);
  const ruleScale = sp(330, { damping: 8, stiffness: 150 });
  const finalOp = sp(400);

  const trackWidth = 1400;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #FFFFFF 0%, #DCDCDC 100%)' }} />

      {/* Bg texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="bg10" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="0" y1="20" x2="40" y2="20" stroke="#1C1C1C" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#bg10)" />
      </svg>

      {/* Background big text */}
      <div style={{ position: 'absolute', top: 200, right: -50, fontFamily: 'Inter', fontSize: 380, fontWeight: 900, color: '#1C1C1C', opacity: 0.04, lineHeight: 0.9 }}>RULE</div>

      {/* Title */}
      <div style={{ position: 'absolute', top: 60, left: 80, opacity: titleOp }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#9E9E9E', letterSpacing: 6 }}>// THE GOLDEN RULE</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 76, color: '#1C1C1C', marginTop: 8 }}>कर्ज़ vs कमाई — रेस</div>
      </div>

      {/* Race tracks */}
      <div style={{ position: 'absolute', top: 320, left: 80, width: trackWidth + 200 }}>
        {/* DEBT track */}
        <div style={{ marginBottom: 80 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 16 }}>
            <span style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 800, color: '#ff0000' }}>कर्ज़ / DEBT</span>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 700, color: '#ff0000' }}>+{(340 * debtRaceProgress).toFixed(0)}%</span>
          </div>
          <div style={{ position: 'relative', width: trackWidth, height: 80, backgroundColor: '#E0E0E0', borderRadius: 6 }}>
            {/* Track marks */}
            {Array.from({ length: 10 }).map((_, i) => (
              <div key={i} style={{ position: 'absolute', left: i * (trackWidth / 10), top: 0, width: 2, height: '100%', backgroundColor: '#9E9E9E', opacity: 0.4 }} />
            ))}
            {/* Runner — debt */}
            <div style={{ position: 'absolute', top: '50%', left: trackWidth * debtRaceProgress, transform: 'translate(-50%, -50%)' }}>
              <svg width="80" height="80" viewBox="0 0 80 80">
                <circle cx="40" cy="40" r="30" fill="#ff0000" />
                <text x="40" y="48" textAnchor="middle" fontFamily="Inter" fontSize="32" fontWeight="900" fill="#F5F5F5">↑</text>
              </svg>
            </div>
            {/* Trail */}
            <div style={{ position: 'absolute', top: 0, left: 0, width: trackWidth * debtRaceProgress, height: '100%', backgroundColor: '#ff0000', opacity: 0.25, borderRadius: 6 }} />
          </div>
        </div>

        {/* INCOME track */}
        <div>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 16 }}>
            <span style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 800, color: '#1C1C1C' }}>कमाई / INCOME</span>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 700, color: '#1C1C1C' }}>+{(45 * incomeRaceProgress / 0.4).toFixed(0)}%</span>
          </div>
          <div style={{ position: 'relative', width: trackWidth, height: 80, backgroundColor: '#E0E0E0', borderRadius: 6 }}>
            {Array.from({ length: 10 }).map((_, i) => (
              <div key={i} style={{ position: 'absolute', left: i * (trackWidth / 10), top: 0, width: 2, height: '100%', backgroundColor: '#9E9E9E', opacity: 0.4 }} />
            ))}
            <div style={{ position: 'absolute', top: '50%', left: trackWidth * incomeRaceProgress, transform: 'translate(-50%, -50%)' }}>
              <svg width="80" height="80" viewBox="0 0 80 80">
                <circle cx="40" cy="40" r="30" fill="#1C1C1C" />
                <text x="40" y="50" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="900" fill="#e9d700">₹</text>
              </svg>
            </div>
            <div style={{ position: 'absolute', top: 0, left: 0, width: trackWidth * incomeRaceProgress, height: '100%', backgroundColor: '#1C1C1C', opacity: 0.25, borderRadius: 6 }} />
          </div>
        </div>

        {/* Gap arrow */}
        {debtRaceProgress > 0.6 && (
          <svg style={{ position: 'absolute', top: 90, left: trackWidth * incomeRaceProgress + 50, opacity: ruleOp }} width={trackWidth * (debtRaceProgress - incomeRaceProgress) - 50} height="160">
            <line x1="0" y1="80" x2={trackWidth * (debtRaceProgress - incomeRaceProgress) - 80} y2="80" stroke="#ff0000" strokeWidth="4" strokeDasharray="12 8" />
            <text x={(trackWidth * (debtRaceProgress - incomeRaceProgress) - 80) / 2} y="60" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="28" fontWeight="800" fill="#ff0000">DANGER GAP</text>
          </svg>
        )}
      </div>

      {/* The Rule box */}
      <div style={{ position: 'absolute', bottom: 40, left: 0, width: '100%', textAlign: 'center', opacity: ruleOp, transform: `scale(${ruleScale})` }}>
        <div style={{ display: 'inline-block', padding: '28px 70px', backgroundColor: '#1C1C1C', borderTop: '6px solid #e9d700', borderBottom: '6px solid #e9d700' }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E', letterSpacing: 4, marginBottom: 8 }}>// REMEMBER THIS</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 64, color: '#F5F5F5' }}>
            कर्ज़ &gt; कमाई = <span style={{ color: '#e9d700' }}>तबाही</span>
          </div>
        </div>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default TheGoldenRule;
```

