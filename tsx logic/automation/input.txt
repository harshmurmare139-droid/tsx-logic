```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene1NotTemporary',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene1NotTemporary: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Card entrance
  const cardScale = sp(10);
  const strikeWidth = interpolate(sp(35), [0, 1], [0, 100]);
  const stampRotate = interpolate(sp(60), [0, 1], [-25, -8]);
  const stampScale = sp(60, { damping: 8, stiffness: 180 });

  // Micro: floating card breath
  const cardY = Math.sin(frame  0.04)  4;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      {/ Soft cloud gradient /}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 20%, #F4F4F4 0%, #E0E0E0 60%, #D4D4D4 100%)' }} />

      {/ Floating geometric shapes /}
      <div style={{ position: 'absolute', top: 100, left: 80, width: 380, height: 380, borderRadius: '50%', border: '2px solid #1A1208', opacity: 0.05, transform: rotate(${frame  0.2}deg) }} />
      <div style={{ position: 'absolute', bottom: 80, right: 120, width: 280, height: 280, borderRadius: 40, background: '#1A1208', opacity: 0.04, transform: rotate(${-frame  0.15}deg) }} />

      {/ Grid texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="g1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#1A1208" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#g1)" />
      </svg>

      {/ Floating particles /}
      {Array.from({ length: 18 }).map((, i) => {
        const px = (i  137) % 1920;
        const py = (Math.sin(frame  0.02 + i)  40 + i  60) % 1080;
        return <div key={i} style={{ position: 'absolute', left: px, top: py, width: 4, height: 4, borderRadius: '50%', background: '#1A1208', opacity: 0.15 }} />;
      })}

      {/ Main floating card /}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: translate(-50%, calc(-50% + ${cardY}px)) scale(${cardScale}),
        width: 1400, padding: '80px 90px',
        background: '#F8FAFC',
        borderRadius: 32,
        boxShadow: '0 40px 100px rgba(26,18,8,0.18), 0 0 0 1px rgba(26,18,8,0.06)',
      }}>
        {/ Tag /}
        <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 36, opacity: sp(0) }}>
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#FF0000" strokeWidth="2.5" />
            <path d="M8 8L16 16M16 8L8 16" stroke="#FF0000" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
          <span style={{ fontFamily: inter, fontSize: 28, fontWeight: 700, color: '#FF0000', letterSpacing: 2 }}>MYTH</span>
        </div>

        {/ Crossed line /}
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <div style={{ fontSize: 110, fontWeight: 600, color: '#6B6B6B', lineHeight: 1.1 }}>
            "ये अस्थाई है"
          </div>
          <div style={{ position: 'absolute', top: '50%', left: 0, height: 6, background: '#FF0000', width: ${strikeWidth}%, borderRadius: 3 }} />
        </div>

        {/ Divider /}
        <div style={{ height: 2, background: '#1A1208', opacity: 0.1, margin: '50px 0', transform: scaleX(${sp(45)}), transformOrigin: 'left' }} />

        {/ Truth tag /}
        <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 28, opacity: sp(50) }}>
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <path d="M5 12L10 17L19 7" stroke="#E9D700" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span style={{ fontFamily: inter, fontSize: 28, fontWeight: 700, color: '#1A1208', letterSpacing: 2 }}>SACH</span>
        </div>

        <div style={{ fontSize: 138, fontWeight: 700, color: '#1A1208', lineHeight: 1.05, opacity: sp(55) }}>
          ढाँचागत <span style={{ color: '#E9D700', textShadow: '0 0 30px rgba(233,215,0,0.4)' }}>बदलाव</span>
        </div>
      </div>

      {/ Stamp /}
      <div style={{
        position: 'absolute', top: 130, right: 180,
        transform: rotate(${stampRotate}deg) scale(${stampScale}),
        padding: '20px 40px', border: '5px solid #FF0000', color: '#FF0000',
        fontFamily: inter, fontSize: 42, fontWeight: 700, letterSpacing: 4,
        background: 'rgba(255,255,255,0.7)',
      }}>
        PERMANENT
      </div>

      {/ Stick figure /}
      <div style={{
        position: 'absolute', bottom: 60, left: 90,
        transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(70)}),
      }}>
        <svg width="120" height="180" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      {/ Vignette /}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 50%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      {/ Watermark /}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene1NotTemporary;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene2DecadesShocks',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene2DecadesShocks: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headerY = interpolate(sp(0), [0, 1], [-40, 0]);
  const card1 = sp(20);
  const card2 = sp(45);
  const plus = sp(70);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #F0F0F0 0%, #E2E2E2 100%)' }} />

      {/ Floating triangle /}
      <svg style={{ position: 'absolute', top: 60, right: 100, opacity: 0.06, transform: rotate(${frame  0.3}deg) }} width="300" height="300">
        <polygon points="150,20 280,260 20,260" fill="none" stroke="#1A1208" strokeWidth="3" />
      </svg>
      <div style={{ position: 'absolute', bottom: 100, left: 80, width: 240, height: 240, borderRadius: 30, background: '#FFC107', opacity: 0.08, transform: rotate(${15 + Math.sin(frame  0.03)  5}deg) }} />

      {/ Dot texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots2" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#1A1208" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots2)" />
      </svg>

      {/ Header /}
      <div style={{ position: 'absolute', top: 90, left: '50%', transform: translateX(-50%) translateY(${headerY}px), opacity: sp(0), textAlign: 'center' }}>
        <div style={{ fontFamily: inter, fontSize: 26, fontWeight: 700, color: '#9E9E9E', letterSpacing: 6, marginBottom: 18 }}>THE FORMULA</div>
        <div style={{ fontSize: 96, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>
          आज की महंगाई = ?
        </div>
      </div>

      {/ Two cards /}
      <div style={{ position: 'absolute', top: 360, left: 100, width: 760, padding: 60, background: '#F8FAFC', borderRadius: 28, boxShadow: '0 30px 80px rgba(26,18,8,0.15)', transform: scale(${card1}) translateY(${Math.sin(frame  0.04)  3}px), transformOrigin: 'center' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 18, marginBottom: 30 }}>
          <svg width="56" height="56" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#1A1208" strokeWidth="2" />
            <path d="M12 6V12L16 14" stroke="#1A1208" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
          <span style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#9E9E9E', letterSpacing: 3 }}>PART 1</span>
        </div>
        <div style={{ fontSize: 86, fontWeight: 700, color: '#1A1208', lineHeight: 1.05 }}>
          दशकों के<br /><span style={{ color: '#FFC107' }}>फैसले</span>
        </div>
        <div style={{ fontFamily: inter, fontSize: 28, color: '#6B6B6B', marginTop: 24, fontWeight: 500 }}>
          Decades of Policy Decisions
        </div>
      </div>

      {/ Plus sign /}
      <div style={{ position: 'absolute', top: 520, left: '50%', transform: translateX(-50%) scale(${plus}) rotate(${frame  0.5}deg), fontSize: 130, fontWeight: 700, color: '#E9D700', textShadow: '0 0 40px rgba(233,215,0,0.5)' }}>+</div>

      <div style={{ position: 'absolute', top: 360, right: 100, width: 760, padding: 60, background: '#1A1208', color: '#F8FAFC', borderRadius: 28, boxShadow: '0 30px 80px rgba(26,18,8,0.35)', transform: scale(${card2}) translateY(${Math.sin(frame  0.04 + 1)  3}px), transformOrigin: 'center' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 18, marginBottom: 30 }}>
          <svg width="56" height="56" viewBox="0 0 24 24" fill="none">
            <path d="M13 2L3 14H12L11 22L21 10H12L13 2Z" stroke="#FF0000" strokeWidth="2.5" strokeLinejoin="round" fill="#FF0000" />
          </svg>
          <span style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#9E9E9E', letterSpacing: 3 }}>PART 2</span>
        </div>
        <div style={{ fontSize: 86, fontWeight: 700, lineHeight: 1.05 }}>
          हाल के<br /><span style={{ color: '#FF0000' }}>झटके</span>
        </div>
        <div style={{ fontFamily: inter, fontSize: 28, color: '#9E9E9E', marginTop: 24, fontWeight: 500 }}>
          Recent Economic Shocks
        </div>
      </div>

      {/ Stick figure /}
      <div style={{ position: 'absolute', bottom: 50, right: 60, transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(85)}) }}>
        <svg width="120" height="180" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene2DecadesShocks;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene3NeverComingBack',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene3NeverComingBack: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Line draw
  const lineProgress = interpolate(sp(20), [0, 1], [0, 1]);
  const dotX = interpolate(lineProgress, [0, 1], [180, 1740]);
  const dotY = interpolate(lineProgress, [0, 1], [780, 280]);

  const xStamp = sp(75, { damping: 8, stiffness: 200 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, #F2F2F2 0%, #DCDCDC 100%)' }} />

      {/ Floating hexagon /}
      <svg style={{ position: 'absolute', top: 150, left: 1500, opacity: 0.07, transform: rotate(${frame  0.2}deg) }} width="280" height="280">
        <polygon points="140,20 250,80 250,200 140,260 30,200 30,80" fill="none" stroke="#1A1208" strokeWidth="3" />
      </svg>

      {/ Diagonal lines texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="diag" width="30" height="30" patternUnits="userSpaceOnUse">
            <line x1="0" y1="30" x2="30" y2="0" stroke="#1A1208" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag)" />
      </svg>

      {/ Header /}
      <div style={{ position: 'absolute', top: 70, left: 120, opacity: sp(0) }}>
        <div style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#9E9E9E', letterSpacing: 5 }}>PRICE TRAJECTORY · 2019 → 2026</div>
        <div style={{ fontSize: 92, fontWeight: 700, color: '#1A1208', marginTop: 14, lineHeight: 1 }}>
          वापसी का रास्ता <span style={{ color: '#FF0000' }}>बंद</span>
        </div>
      </div>

      {/ Chart card /}
      <div style={{ position: 'absolute', top: 280, left: 120, width: 1680, height: 580, background: '#F8FAFC', borderRadius: 28, boxShadow: '0 30px 80px rgba(26,18,8,0.12)', transform: scale(${sp(15)}), transformOrigin: 'center' }}>
        {/ Grid /}
        <svg width="1680" height="580" style={{ position: 'absolute' }}>
          {[0, 1, 2, 3, 4].map(i => (
            <line key={i} x1="60" y1={100 + i  90} x2="1620" y2={100 + i  90} stroke="#1A1208" strokeOpacity="0.06" strokeWidth="1" />
          ))}
          {/ Rising line /}
          <path
            d={M 60 460 Q 400 440 700 360 T 1300 180 L 1620 100}
            fill="none"
            stroke="#1A1208"
            strokeWidth="6"
            strokeLinecap="round"
            strokeDasharray="2000"
            strokeDashoffset={2000 - 2000  lineProgress}
          />
          {/ Moving dot /}
          <circle cx={60 + (1620 - 60)  lineProgress} cy={460 - (460 - 100)  lineProgress} r="14" fill="#E9D700" stroke="#1A1208" strokeWidth="4" />

          {/ Anchor: 2019 /}
          <circle cx="60" cy="460" r="10" fill="#1A1208" />
          <text x="60" y="510" fontFamily={inter} fontSize="26" fontWeight="700" fill="#1A1208">2019</text>
          <text x="60" y="540" fontFamily={inter} fontSize="22" fill="#9E9E9E">₹5,000</text>

          {/ Anchor: 2026 /}
          <circle cx="1620" cy="100" r="10" fill="#FF0000" />
          <text x="1500" y="80" fontFamily={inter} fontSize="26" fontWeight="700" fill="#FF0000">2026</text>
          <text x="1500" y="55" fontFamily={inter} fontSize="22" fill="#9E9E9E">₹9,000+</text>
        </svg>

        {/ Red barrier /}
        <div style={{ position: 'absolute', left: 50, top: 440, right: 30, height: 4, background: '#FF0000', opacity: sp(55), borderRadius: 2 }} />
        <div style={{ position: 'absolute', left: 70, top: 410, fontFamily: inter, fontSize: 22, fontWeight: 700, color: '#FF0000', opacity: sp(55), letterSpacing: 2 }}>
          ◄ NO RETURN ZONE ►
        </div>
      </div>

      {/ X overlay on 2019 /}
      <div style={{ position: 'absolute', top: 720, left: 170, transform: scale(${xStamp}) rotate(-10deg), fontSize: 90, fontWeight: 700, color: '#FF0000', textShadow: '0 0 20px rgba(255,0,0,0.4)' }}>✕</div>

      {/ Stick figure /}
      <div style={{ position: 'absolute', bottom: 50, right: 90, transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(80)}) }}>
        <svg width="110" height="170" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene3NeverComingBack;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene4RupeeWeakMoneySupply',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene4RupeeWeakMoneySupply: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Rupee shrinking
  const rupeeScale = interpolate(sp(15), [0, 1], [1.4, 0.7]);
  // Money bar growing
  const moneyBar = interpolate(sp(45), [0, 1], [0, 100]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #EFEFEF 0%, #D8D8D8 100%)' }} />

      {/ Floating square /}
      <div style={{ position: 'absolute', top: 80, right: 100, width: 320, height: 320, border: '3px solid #1A1208', opacity: 0.05, transform: rotate(${frame  0.25}deg) }} />
      <div style={{ position: 'absolute', bottom: 60, left: 60, width: 260, height: 260, borderRadius: '50%', background: '#E9D700', opacity: 0.08 }} />

      {/ Cross-hatch /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="hatch4" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 0 50 L 50 0 M -10 10 L 10 -10 M 40 60 L 60 40" stroke="#1A1208" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hatch4)" />
      </svg>

      {/ Header /}
      <div style={{ position: 'absolute', top: 70, left: '50%', transform: 'translateX(-50%)', opacity: sp(0), textAlign: 'center' }}>
        <div style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#9E9E9E', letterSpacing: 5, marginBottom: 12 }}>TWO FORCES · ONE OUTCOME</div>
        <div style={{ fontSize: 88, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>
          रुपया गिरा। पैसा बढ़ा।
        </div>
      </div>

      {/ Left card: Rupee shrinking /}
      <div style={{ position: 'absolute', top: 300, left: 130, width: 800, padding: 60, background: '#F8FAFC', borderRadius: 28, boxShadow: '0 30px 80px rgba(26,18,8,0.15)', transform: scale(${sp(10)}) translateY(${Math.sin(frame  0.04)  3}px), transformOrigin: 'center' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 30 }}>
          <svg width="48" height="48" viewBox="0 0 24 24" fill="none">
            <path d="M7 4H17M7 8H17M9 12H13C15 12 16 11 16 9C16 7 15 4 13 4H11M7 16L17 16M11 12L17 20" stroke="#FF0000" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
          <span style={{ fontFamily: inter, fontSize: 22, fontWeight: 700, color: '#FF0000', letterSpacing: 3 }}>RUPEE STRENGTH</span>
        </div>

        <div style={{ display: 'flex', alignItems: 'baseline', justifyContent: 'center', gap: 30, marginTop: 30 }}>
          <span style={{ fontSize: 140  1.4, fontWeight: 700, color: '#1A1208', opacity: 0.4 }}>₹</span>
          <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
            <path d="M5 12H19M19 12L13 6M19 12L13 18" stroke="#1A1208" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span style={{ fontSize: 140  rupeeScale, fontWeight: 700, color: '#FF0000', textShadow: '0 0 30px rgba(255,0,0,0.3)' }}>₹</span>
        </div>
        <div style={{ fontFamily: inter, fontSize: 24, color: '#9E9E9E', textAlign: 'center', marginTop: 20, fontWeight: 600 }}>
          70 → 95 per USD
        </div>
      </div>

      {/ Right card: Money supply growing /}
      <div style={{ position: 'absolute', top: 300, right: 130, width: 800, padding: 60, background: '#1A1208', color: '#F8FAFC', borderRadius: 28, boxShadow: '0 30px 80px rgba(26,18,8,0.3)', transform: scale(${sp(35)}) translateY(${Math.sin(frame  0.04 + 1)  3}px), transformOrigin: 'center' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 30 }}>
          <svg width="48" height="48" viewBox="0 0 24 24" fill="none">
            <path d="M3 17L9 11L13 15L21 7M21 7H15M21 7V13" stroke="#E9D700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span style={{ fontFamily: inter, fontSize: 22, fontWeight: 700, color: '#E9D700', letterSpacing: 3 }}>MONEY SUPPLY</span>
        </div>

        {/ Bar chart /}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 24, height: 220, marginTop: 40, justifyContent: 'center' }}>
          {[40, 55, 70, 88, 100].map((h, i) => {
            const grow = interpolate(sp(45 + i  6), [0, 1], [0, 1]);
            return (
              <div key={i} style={{ width: 80, height: (h  moneyBar  grow) / 100  2, background: i === 4 ? '#E9D700' : '#F8FAFC', opacity: 0.4 + i  0.15, borderRadius: '8px 8px 0 0' }} />
            );
          })}
        </div>
        <div style={{ fontFamily: inter, fontSize: 24, color: '#9E9E9E', textAlign: 'center', marginTop: 20, fontWeight: 600 }}>
          M3 Expansion · 2020-2026
        </div>
      </div>

      {/ Stick figure /}
      <div style={{ position: 'absolute', bottom: 50, left: '50%', transform: translateX(-50%) translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(75)}) }}>
        <svg width="110" height="170" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene4RupeeWeakMoneySupply;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene5EnergyPermanent',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene5EnergyPermanent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Flame pulse
  const flamePulse = 1 + Math.sin(frame  0.15)  0.05;
  const meterFill = interpolate(sp(50), [0, 1], [0, 85]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 80%, #F5F0E5 0%, #E0DCD2 60%, #C8C4B8 100%)' }} />

      {/ Floating shapes /}
      <div style={{ position: 'absolute', top: 100, left: 80, width: 350, height: 350, borderRadius: 50, border: '2px solid #1A1208', opacity: 0.05, transform: rotate(${frame  0.18}deg) }} />
      <div style={{ position: 'absolute', bottom: 100, right: 100, width: 200, height: 200, borderRadius: '50%', background: '#FF0000', opacity: 0.06 }} />

      {/ Circle texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="circ5" width="60" height="60" patternUnits="userSpaceOnUse">
            <circle cx="30" cy="30" r="18" fill="none" stroke="#1A1208" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#circ5)" />
      </svg>

      {/ Header /}
      <div style={{ position: 'absolute', top: 80, left: 130, opacity: sp(0) }}>
        <div style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#9E9E9E', letterSpacing: 5, marginBottom: 14 }}>ENERGY FLOOR · LOCKED IN</div>
        <div style={{ fontSize: 100, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>
          ऊर्जा <span style={{ color: '#FF0000' }}>स्थायी रूप से</span><br />महंगी
        </div>
      </div>

      {/ Big flame icon /}
      <div style={{ position: 'absolute', top: 360, right: 200, transform: scale(${sp(20)  flamePulse}) }}>
        <svg width="280" height="320" viewBox="0 0 100 120">
          <path d="M50 10 C40 30 25 40 25 65 C25 85 35 105 50 110 C65 105 75 85 75 65 C75 40 60 30 50 10 Z" fill="#FFC107" opacity="0.9" />
          <path d="M50 35 C44 48 36 55 36 72 C36 88 42 100 50 102 C58 100 64 88 64 72 C64 55 56 48 50 35 Z" fill="#FF0000" />
          <path d="M50 55 C46 64 42 68 42 78 C42 88 46 95 50 96 C54 95 58 88 58 78 C58 68 54 64 50 55 Z" fill="#E9D700" />
        </svg>
      </div>

      {/ Energy meter card /}
      <div style={{ position: 'absolute', bottom: 100, left: 130, width: 1080, padding: 50, background: '#F8FAFC', borderRadius: 28, boxShadow: '0 30px 80px rgba(26,18,8,0.15)', transform: scale(${sp(40)}), transformOrigin: 'left' }}>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 28 }}>
          <span style={{ fontFamily: inter, fontSize: 22, fontWeight: 700, color: '#1A1208', letterSpacing: 3 }}>OIL · GAS · LOGISTICS COST INDEX</span>
          <span style={{ fontFamily: inter, fontSize: 32, fontWeight: 700, color: '#FF0000' }}>+{Math.round(meterFill)}%</span>
        </div>

        {/ Meter bar /}
        <div style={{ width: '100%', height: 48, background: '#E5E5E5', borderRadius: 24, overflow: 'hidden', position: 'relative' }}>
          <div style={{ width: ${meterFill}%, height: '100%', background: 'linear-gradient(90deg, #FFC107 0%, #FF0000 100%)', borderRadius: 24, transition: 'width 0.3s' }} />
          {/ Floor line /}
          <div style={{ position: 'absolute', left: '40%', top: -10, bottom: -10, width: 3, background: '#1A1208', opacity: sp(70) }} />
          <div style={{ position: 'absolute', left: '40%', top: -34, fontFamily: inter, fontSize: 16, fontWeight: 700, color: '#1A1208', transform: 'translateX(-50%)', opacity: sp(70) }}>2019 BASE</div>
        </div>

        <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 30 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="6" fill="#1A1208" /></svg>
            <span style={{ fontFamily: inter, fontSize: 20, color: '#1A1208', fontWeight: 600 }}>NEW PERMANENT FLOOR</span>
          </div>
          <div style={{ fontFamily: inter, fontSize: 18, color: '#9E9E9E', fontWeight: 500 }}>No return below baseline</div>
        </div>
      </div>

      {/ Stick figure /}
      <div style={{ position: 'absolute', bottom: 50, right: 80, transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(80)}) }}>
        <svg width="110" height="170" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene5EnergyPermanent;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene6FoodSystemRebalance',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene6FoodSystemRebalance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #F0F0F0 0%, #DCDCDC 100%)' }} />

      {/ Floating shapes /}
      <svg style={{ position: 'absolute', top: 80, right: 150, opacity: 0.06, transform: rotate(${frame  0.2}deg) }} width="300" height="300">
        <polygon points="150,30 280,250 20,250" fill="#1A1208" />
      </svg>
      <div style={{ position: 'absolute', bottom: 120, left: 80, width: 240, height: 240, borderRadius: 30, background: '#E9D700', opacity: 0.1, transform: rotate(${frame  0.1}deg) }} />

      {/ Grid /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="g6" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#1A1208" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#g6)" />
      </svg>

      {/ Header /}
      <div style={{ position: 'absolute', top: 80, left: '50%', transform: 'translateX(-50%)', opacity: sp(0), textAlign: 'center' }}>
        <div style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#9E9E9E', letterSpacing: 5, marginBottom: 14 }}>FOOD DISTRIBUTION · NEW EQUILIBRIUM</div>
        <div style={{ fontSize: 92, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>
          सिस्टम <span style={{ color: '#E9D700' }}>रीसेट</span> हो चुका
        </div>
      </div>

      {/ 3-node system /}
      <div style={{ position: 'absolute', top: 380, left: 0, right: 0, display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 60 }}>
        {[
          { label: 'फार्म', sub: 'FARM', icon: 'M3 20H21M5 20V12L12 5L19 12V20M9 20V14H15V20' },
          { label: 'सप्लाई', sub: 'SUPPLY', icon: 'M3 17H6L7 8H17L18 17H21M6 17C6 18 7 19 8 19C9 19 10 18 10 17M14 17C14 18 15 19 16 19C17 18 18 17 18 17' },
          { label: 'थाली', sub: 'PLATE', icon: 'M12 2A10 10 0 1 0 22 12M12 6A6 6 0 1 0 18 12' },
        ].map((n, i) => {
          const s = sp(15 + i  15);
          const breathe = Math.sin(frame  0.04 + i)  4;
          return (
            <React.Fragment key={i}>
              <div style={{ transform: scale(${s}) translateY(${breathe}px), transformOrigin: 'center' }}>
                <div style={{ width: 320, padding: '50px 30px', background: i === 1 ? '#1A1208' : '#F8FAFC', color: i === 1 ? '#F8FAFC' : '#1A1208', borderRadius: 28, boxShadow: '0 30px 70px rgba(26,18,8,0.18)', textAlign: 'center' }}>
                  <svg width="80" height="80" viewBox="0 0 24 24" fill="none" style={{ margin: '0 auto 24px', display: 'block' }}>
                    <path d={n.icon} stroke={i === 1 ? '#E9D700' : '#1A1208'} strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
                  </svg>
                  <div style={{ fontSize: 64, fontWeight: 700, lineHeight: 1 }}>{n.label}</div>
                  <div style={{ fontFamily: inter, fontSize: 18, fontWeight: 600, marginTop: 12, opacity: 0.6, letterSpacing: 3 }}>{n.sub}</div>
                </div>
              </div>
              {i < 2 && (
                <svg width="80" height="40" viewBox="0 0 80 40" style={{ opacity: sp(30 + i  15) }}>
                  <path d={M 5 20 L ${5 + 70  sp(30 + i  15)} 20} stroke="#E9D700" strokeWidth="4" strokeLinecap="round" />
                  <path d="M 60 10 L 75 20 L 60 30" fill="none" stroke="#E9D700" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" opacity={sp(40 + i  15)} />
                </svg>
              )}
            </React.Fragment>
          );
        })}
      </div>

      {/ Bottom badge /}
      <div style={{ position: 'absolute', bottom: 150, left: '50%', transform: translateX(-50%) scale(${sp(75)}), padding: '22px 50px', background: '#1A1208', color: '#E9D700', borderRadius: 100, fontFamily: inter, fontSize: 28, fontWeight: 700, letterSpacing: 4, boxShadow: '0 20px 50px rgba(26,18,8,0.3)' }}>
        ⟲ PRICES REBALANCED · NEW BASELINE
      </div>

      {/ Stick figure /}
      <div style={{ position: 'absolute', bottom: 40, right: 80, transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(85)}) }}>
        <svg width="100" height="160" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene6FoodSystemRebalance;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene7ShockListAssumption',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene7ShockListAssumption: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const items = [
    { hi: 'तेल के दाम बढ़े', en: 'Oil prices spiked', icon: 'M12 2L4 9V20H20V9L12 2M9 14L11 16L15 12' },
    { hi: 'मज़दूरी महंगी हुई', en: 'Labour wages rose', icon: 'M16 12C18 12 20 14 20 16V20H4V16C4 14 6 12 8 12M12 4A4 4 0 1 1 12 12A4 4 0 1 1 12 4' },
    { hi: 'सप्लाई बाधित हुई', en: 'Supply disrupted', icon: 'M3 17H21M3 12H21M3 7H21' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      <AbsoluteFill style={{ background: 'radial-gradient(circle at 80% 20%, #F4F4F4 0%, #DDDDDD 70%)' }} />

      {/ Floating shapes /}
      <div style={{ position: 'absolute', top: 60, left: 100, width: 280, height: 280, borderRadius: 40, border: '3px solid #1A1208', opacity: 0.05, transform: rotate(${frame  0.15}deg) }} />
      <svg style={{ position: 'absolute', bottom: 60, right: 80, opacity: 0.06, transform: rotate(${-frame  0.2}deg) }} width="220" height="220">
        <polygon points="110,15 200,180 20,180" fill="#1A1208" />
      </svg>

      {/ Lines texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="ln7" width="20" height="20" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="20" stroke="#1A1208" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#ln7)" />
      </svg>

      {/ Header /}
      <div style={{ position: 'absolute', top: 70, left: 130, opacity: sp(0) }}>
        <div style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#9E9E9E', letterSpacing: 5, marginBottom: 14 }}>THE ASSUMPTION</div>
        <div style={{ fontSize: 92, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>
          "ये सिर्फ़ <span style={{ color: '#FFC107' }}>झटके</span> हैं"
        </div>
      </div>

      {/ List of shocks /}
      <div style={{ position: 'absolute', top: 320, left: 130, width: 1660, display: 'flex', flexDirection: 'column', gap: 24 }}>
        {items.map((item, i) => {
          const s = sp(15 + i  14);
          const x = interpolate(s, [0, 1], [-100, 0]);
          return (
            <div key={i} style={{
              transform: translateX(${x}px),
              opacity: s,
              display: 'flex', alignItems: 'center', gap: 32,
              padding: '32px 50px',
              background: '#F8FAFC',
              borderRadius: 22,
              boxShadow: '0 20px 50px rgba(26,18,8,0.10)',
              borderLeft: '8px solid #FFC107',
            }}>
              <div style={{ width: 78, height: 78, borderRadius: 18, background: '#1A1208', display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}>
                <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                  <path d={item.icon} stroke="#E9D700" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
              </div>
              <div style={{ flex: 1 }}>
                <div style={{ fontSize: 56, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>{item.hi}</div>
                <div style={{ fontFamily: inter, fontSize: 22, color: '#9E9E9E', marginTop: 8, fontWeight: 500, letterSpacing: 1 }}>{item.en}</div>
              </div>
              <div style={{ fontFamily: inter, fontSize: 22, fontWeight: 700, color: '#9E9E9E', letterSpacing: 3 }}>SHOCK</div>
            </div>
          );
        })}
      </div>

      {/ Bottom assumption tag /}
      <div style={{ position: 'absolute', bottom: 140, left: '50%', transform: translateX(-50%) scale(${sp(70)}), padding: '24px 56px', background: '#FFC107', color: '#1A1208', borderRadius: 100, fontSize: 44, fontWeight: 700, boxShadow: '0 20px 50px rgba(255,193,7,0.4)' }}>
        "जल्द ही ख़त्म हो जाएंगे..."
      </div>

      {/ Stick figure /}
      <div style={{ position: 'absolute', bottom: 40, right: 80, transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(85)}) }}>
        <svg width="100" height="160" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene7ShockListAssumption;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene8WrongAssumption',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene8WrongAssumption: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big X reveal
  const xScale = sp(20, { damping: 9, stiffness: 200 });
  const xRotate = interpolate(sp(20), [0, 1], [-40, 0]);

  // Shake on impact
  const shake = frame > 25 && frame < 40 ? Math.sin(frame  4)  4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand, transform: translate(${shake}px, 0) }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #ECECEC 0%, #D2D2D2 100%)' }} />

      {/ Floating shapes /}
      <div style={{ position: 'absolute', top: 120, right: 120, width: 300, height: 300, borderRadius: '50%', background: '#FF0000', opacity: 0.07 }} />
      <svg style={{ position: 'absolute', bottom: 100, left: 80, opacity: 0.05, transform: rotate(${frame  0.18}deg) }} width="280" height="280">
        <rect x="20" y="20" width="240" height="240" fill="none" stroke="#1A1208" strokeWidth="3" />
      </svg>

      {/ X-mark texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="x8" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 10 10 L 50 50 M 50 10 L 10 50" stroke="#1A1208" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#x8)" />
      </svg>

      {/ Top label /}
      <div style={{ position: 'absolute', top: 100, left: '50%', transform: 'translateX(-50%)', opacity: sp(0), textAlign: 'center' }}>
        <div style={{ display: 'inline-block', padding: '14px 36px', background: '#FF0000', color: '#F8FAFC', borderRadius: 100, fontFamily: inter, fontSize: 26, fontWeight: 700, letterSpacing: 4 }}>
          REALITY CHECK
        </div>
      </div>

      {/ Big X with assumption /}
      <div style={{ position: 'absolute', top: 220, left: '50%', transform: translateX(-50%) scale(${xScale}) rotate(${xRotate}deg), width: 1000, height: 220, position: 'absolute', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ position: 'absolute', fontSize: 96, fontWeight: 600, color: '#1A1208', opacity: 0.35, textDecoration: 'line-through' }}>
          ये सोच सही है
        </div>
      </div>

      {/ Main statement card /}
      <div style={{ position: 'absolute', bottom: 200, left: '50%', transform: translateX(-50%) scale(${sp(45)}) translateY(${Math.sin(frame  0.04)  3}px), transformOrigin: 'center', width: 1500, padding: '70px 80px', background: '#1A1208', color: '#F8FAFC', borderRadius: 32, boxShadow: '0 40px 100px rgba(26,18,8,0.4)' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 30 }}>
          <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
            <path d="M12 9V13M12 17H12.01M10.29 3.86L1.82 18A2 2 0 0 0 3.54 21H20.46A2 2 0 0 0 22.18 18L13.71 3.86A2 2 0 0 0 10.29 3.86Z" stroke="#E9D700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#E9D700', letterSpacing: 4 }}>STRUCTURAL TRUTH</span>
        </div>
        <div style={{ fontSize: 110, fontWeight: 700, lineHeight: 1.05 }}>
          कुछ बदलाव <span style={{ color: '#E9D700', textShadow: '0 0 40px rgba(233,215,0,0.5)' }}>कभी उल्टे</span><br />नहीं जाते
        </div>
        <div style={{ fontFamily: inter, fontSize: 28, color: '#9E9E9E', marginTop: 24, fontWeight: 500, letterSpacing: 1 }}>
          Some changes are one-way streets.
        </div>
      </div>

      {/ Stick figure /}
      <div style={{ position: 'absolute', bottom: 40, right: 80, transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(80)}) }}>
        <svg width="100" height="160" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene8WrongAssumption;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene9RBIExpansion',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene9RBIExpansion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Timeline progress
  const yearProgress = interpolate(sp(25), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, #F0F0F0 0%, #D8D8D8 100%)' }} />

      {/ Floating shapes /}
      <svg style={{ position: 'absolute', top: 60, left: 80, opacity: 0.05, transform: rotate(${frame  0.2}deg) }} width="300" height="300">
        <circle cx="150" cy="150" r="140" fill="none" stroke="#1A1208" strokeWidth="3" strokeDasharray="20 10" />
      </svg>
      <div style={{ position: 'absolute', bottom: 80, right: 100, width: 260, height: 260, borderRadius: 40, background: '#FFC107', opacity: 0.08, transform: rotate(${frame  0.1}deg) }} />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="g9" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 0 25 L 50 25" stroke="#1A1208" strokeWidth="0.7" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#g9)" />
      </svg>

      {/ Header /}
      <div style={{ position: 'absolute', top: 70, left: 130, opacity: sp(0) }}>
        <div style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#9E9E9E', letterSpacing: 5, marginBottom: 14 }}>RBI · MONETARY BASE EXPANSION</div>
        <div style={{ fontSize: 92, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>
          2020 — 2022 <span style={{ color: '#E9D700' }}>का दौर</span>
        </div>
      </div>

      {/ Timeline card /}
      <div style={{ position: 'absolute', top: 320, left: 130, right: 130, padding: 60, background: '#F8FAFC', borderRadius: 28, boxShadow: '0 30px 80px rgba(26,18,8,0.15)', transform: scale(${sp(15)}), transformOrigin: 'center' }}>
        {/ Timeline base /}
        <div style={{ position: 'relative', height: 200, marginTop: 20 }}>
          {/ Track /}
          <div style={{ position: 'absolute', top: 100, left: 40, right: 40, height: 8, background: '#E5E5E5', borderRadius: 4 }} />
          <div style={{ position: 'absolute', top: 100, left: 40, width: calc((100% - 80px)  ${yearProgress}), height: 8, background: 'linear-gradient(90deg, #FFC107, #E9D700, #FF0000)', borderRadius: 4 }} />

          {/ Year markers /}
          {[
            { year: '2020', label: 'Pandemic Start', delay: 20 },
            { year: '2021', label: 'Liquidity Surge', delay: 35 },
            { year: '2022', label: 'Peak Expansion', delay: 50 },
          ].map((m, i) => {
            const x = 40 + (i  (100 - 8) / 2);
            const s = sp(m.delay);
            return (
              <div key={i} style={{ position: 'absolute', left: calc(${x}% - 80px), top: 0, width: 160, textAlign: 'center', opacity: s, transform: translateY(${interpolate(s, [0, 1], [20, 0])}px) }}>
                <div style={{ fontSize: 48, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>{m.year}</div>
                <div style={{ width: 24, height: 24, borderRadius: '50%', background: i === 2 ? '#FF0000' : '#E9D700', margin: '24px auto 14px', boxShadow: 0 0 20px ${i === 2 ? 'rgba(255,0,0,0.5)' : 'rgba(233,215,0,0.5)'} }} />
                <div style={{ fontFamily: inter, fontSize: 18, fontWeight: 600, color: '#9E9E9E', letterSpacing: 1 }}>{m.label}</div>
              </div>
            );
          })}
        </div>

        {/ Stats row /}
        <div style={{ display: 'flex', justifyContent: 'space-around', marginTop: 60, opacity: sp(65) }}>
          {[
            { v: 'Digital', l: 'New Money Form', icon: 'M12 1V23M5 7H17C18 7 19 7 19 9C19 11 18 11 17 11H7C6 11 5 11 5 13C5 15 6 15 7 15H19' },
            { v: 'Asset Buy', l: 'Liquidity Channel', icon: 'M3 17L9 11L13 15L21 7' },
            { v: 'Relief Pkg', l: 'Direct Injection', icon: 'M21 12C21 17 17 21 12 21S3 17 3 12S7 3 12 3S21 7 21 12M12 8V16M8 12H16' },
          ].map((stat, i) => (
            <div key={i} style={{ textAlign: 'center' }}>
              <svg width="56" height="56" viewBox="0 0 24 24" fill="none" style={{ margin: '0 auto 14px', display: 'block' }}>
                <path d={stat.icon} stroke="#1A1208" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
              <div style={{ fontFamily: inter, fontSize: 32, fontWeight: 700, color: '#1A1208' }}>{stat.v}</div>
              <div style={{ fontFamily: inter, fontSize: 18, color: '#9E9E9E', marginTop: 6, fontWeight: 500 }}>{stat.l}</div>
            </div>
          ))}
        </div>
      </div>

      {/ Stick figure /}
      <div style={{ position: 'absolute', bottom: 40, right: 80, transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(80)}) }}>
        <svg width="100" height="160" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene9RBIExpansion;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';
import { loadFont as loadKhand } from '@remotion/google-fonts/Khand';
import { loadFont as loadInter } from '@remotion/google-fonts/Inter';

const { fontFamily: khand } = loadKhand('normal', { weights: ['600', '700'], subsets: ['devanagari'] });
const { fontFamily: inter } = loadInter('normal', { weights: ['500', '700'] });

export const compositionConfig = {
  id: 'Ch2Scene10MoneyCircularFlow',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Ch2Scene10MoneyCircularFlow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Rotating particle along circle
  const orbitAngle = (frame  2) % 360;
  const cx = 960, cy = 600, r = 260;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EAEAEA', overflow: 'hidden', opacity: exitOp, fontFamily: khand }}>
      {/ Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark /}

      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #F2F2F2 0%, #DEDEDE 100%)' }} />

      {/ Floating shapes /}
      <div style={{ position: 'absolute', top: 80, left: 80, width: 280, height: 280, borderRadius: '50%', background: '#E9D700', opacity: 0.08 }} />
      <div style={{ position: 'absolute', bottom: 80, right: 80, width: 240, height: 240, borderRadius: 40, border: '3px solid #1A1208', opacity: 0.05, transform: rotate(${frame  0.2}deg) }} />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="g10" width="80" height="80" patternUnits="userSpaceOnUse">
            <circle cx="40" cy="40" r="2" fill="#1A1208" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#g10)" />
      </svg>

      {/ Header /}
      <div style={{ position: 'absolute', top: 70, left: '50%', transform: 'translateX(-50%)', opacity: sp(0), textAlign: 'center' }}>
        <div style={{ fontFamily: inter, fontSize: 24, fontWeight: 700, color: '#9E9E9E', letterSpacing: 5, marginBottom: 14 }}>MONEY · CIRCULAR FLOW · NEW NORMAL</div>
        <div style={{ fontSize: 92, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>
          पैसा घूमा। कीमतें <span style={{ color: '#E9D700' }}>तय</span> हो गईं।
        </div>
      </div>

      {/ Central circular flow /}
      <svg width="1920" height="1080" style={{ position: 'absolute' }}>
        {/ Orbit ring /}
        <circle cx={cx} cy={cy} r={r} fill="none" stroke="#1A1208" strokeWidth="3" strokeDasharray="10 8" opacity={sp(15)  0.25} />

        {/ Center label /}
        <g opacity={sp(20)}>
          <circle cx={cx} cy={cy} r="100" fill="#1A1208" />
          <text x={cx} y={cy - 5} textAnchor="middle" fontFamily={inter} fontSize="22" fontWeight="700" fill="#E9D700" letterSpacing="3">MONEY</text>
          <text x={cx} y={cy + 22} textAnchor="middle" fontFamily={khand} fontSize="28" fontWeight="700" fill="#F8FAFC">घूमता रहा</text>
        </g>

        {/ Orbiting particle /}
        <circle
          cx={cx + Math.cos((orbitAngle  Math.PI) / 180)  r}
          cy={cy + Math.sin((orbitAngle  Math.PI) / 180)  r}
          r="18" fill="#E9D700" stroke="#1A1208" strokeWidth="3"
          opacity={sp(25)}
        />
      </svg>

      {/ 4 surrounding nodes /}
      {[
        { hi: 'मज़दूरी', en: 'WAGES', angle: -90, delay: 30 },
        { hi: 'ट्रांसपोर्ट', en: 'TRANSPORT', angle: 0, delay: 40 },
        { hi: 'किराना मार्जिन', en: 'MARGINS', angle: 90, delay: 50 },
        { hi: 'दाम', en: 'PRICES', angle: 180, delay: 60 },
      ].map((node, i) => {
        const s = sp(node.delay);
        const nodeR = r + 100;
        const x = cx + Math.cos((node.angle  Math.PI) / 180)  nodeR;
        const y = cy + Math.sin((node.angle  Math.PI) / 180)  nodeR;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - 120, top: y - 60,
            width: 240, padding: '20px 24px',
            background: '#F8FAFC',
            borderRadius: 18,
            boxShadow: '0 20px 50px rgba(26,18,8,0.18)',
            transform: scale(${s}) translateY(${Math.sin(frame  0.04 + i)  3}px),
            transformOrigin: 'center',
            textAlign: 'center',
            borderTop: '4px solid #E9D700',
          }}>
            <div style={{ fontSize: 38, fontWeight: 700, color: '#1A1208', lineHeight: 1 }}>{node.hi}</div>
            <div style={{ fontFamily: inter, fontSize: 16, fontWeight: 700, color: '#9E9E9E', marginTop: 8, letterSpacing: 3 }}>↑ {node.en}</div>
          </div>
        );
      })}

      {/ Bottom badge /}
      <div style={{ position: 'absolute', bottom: 60, left: '50%', transform: translateX(-50%) scale(${sp(80)}), padding: '20px 50px', background: '#1A1208', color: '#E9D700', borderRadius: 100, fontFamily: inter, fontSize: 26, fontWeight: 700, letterSpacing: 3, boxShadow: '0 20px 50px rgba(26,18,8,0.3)' }}>
        ⚙ NEW BASELINE LOCKED IN
      </div>

      {/ Stick figure /}
      <div style={{ position: 'absolute', bottom: 30, right: 70, transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(85)}) }}>
        <svg width="90" height="140" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="22" fill="none" stroke="#1A1208" strokeWidth="3.5" />
          <rect x="42" y="28" width="36" height="10" rx="2" fill="#FFC107" />
          <line x1="60" y1="57" x2="60" y2="120" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="35" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="75" x2="85" y2="100" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="42" y2="170" stroke="#1A1208" strokeWidth="3.5" />
          <line x1="60" y1="120" x2="78" y2="170" stroke="#1A1208" strokeWidth="3.5" />
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(26,18,8,0.15) 100%)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: inter, fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Ch2Scene10_MoneyCircularFlow;
`
```

