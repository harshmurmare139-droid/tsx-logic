```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11InflationAlarm',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11InflationAlarm: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow push-in + micro drift
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });
  const camX = Math.sin(frame * 0.008) * 8;

  // Impact shake when 4.38% slams
  const shakeAmp = frame > 95 && frame < 118 ? (118 - frame) * 0.6 : 0;
  const shakeX = Math.sin(frame * 2.7) * shakeAmp;
  const shakeY = Math.cos(frame * 3.1) * shakeAmp;

  // Inflation ticker 0 -> 4.38
  const counter = interpolate(frame, [40, 110], [0, 4.38], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  const months = ['NOV', 'DEC', 'JAN', 'FEB', 'MAR', 'APR', 'MAY', 'JUN'];
  const vals = [3.05, 3.2, 3.4, 3.6, 3.8, 4.0, 4.18, 4.38];
  const chartH = 380;
  const marqueeX = -((frame * 3.2) % 1400);

  const stamp = sp(430, { damping: 9, stiffness: 200 });
  const stampShake = frame > 430 && frame < 448 ? Math.sin(frame * 3) * (448 - frame) * 0.5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background (darker punk orange) */}
      <AbsoluteFill style={{ background: 'linear-gradient(140deg, #E8823A 0%, #D9762B 45%, #B85E1D 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at ${30 + Math.sin(frame * 0.01) * 10}% 20%, rgba(255,155,81,0.55), transparent 60%)` }} />

      {/* LAYER 2: Animated shape layers — drifting transparent geometry */}
      <div style={{ position: 'absolute', width: 640, height: 640, borderRadius: '50%', border: '80px solid rgba(37,52,63,0.06)', left: -180 + Math.sin(frame * 0.006) * 24, top: -220, transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.02) * 0.03})` }} />
      <div style={{ position: 'absolute', width: 420, height: 420, background: 'rgba(255,244,73,0.05)', right: -120, bottom: -140, transform: `rotate(${18 + Math.sin(frame * 0.012) * 6}deg)` }} />

      {/* LAYER 3: Motion texture — halftone dots */}
      <AbsoluteFill style={{ backgroundImage: 'radial-gradient(#25343F 1.4px, transparent 1.4px)', backgroundSize: '30px 30px', backgroundPosition: `${frame * 0.15}px 0px`, opacity: 0.07 }} />

      {/* LAYER 4: Floating collage particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const sx = seededRandom(i * 7 + 1) * 1920;
        const sy = seededRandom(i * 13 + 2) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 18;
        return (
          <div key={i} style={{ position: 'absolute', left: sx, top: sy + drift, width: 8 + seededRandom(i) * 10, height: 8 + seededRandom(i + 3) * 10, background: i % 3 === 0 ? '#FFF449' : '#25343F', opacity: 0.14, transform: `rotate(${frame * 0.4 + i * 30}deg)` }} />
        );
      })}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${camX + shakeX}px, ${shakeY}px)` }}>

        {/* Top marquee strip — punk ticker */}
        <div style={{ position: 'absolute', top: 0, left: 0, right: 0, height: 62, background: '#25343F', display: 'flex', alignItems: 'center', overflow: 'hidden', borderBottom: '5px solid #FFF449', opacity: sp(5) }}>
          <div style={{ whiteSpace: 'nowrap', transform: `translateX(${marqueeX}px)`, fontFamily: 'JetBrains Mono, monospace', fontSize: 30, fontWeight: 700, color: '#FFF449', letterSpacing: 2 }}>
            {'JUNE 2026 /// INFLATION RISING /// 8th MONTH STRAIGHT /// TARGET BREACHED /// '.repeat(4)}
          </div>
        </div>

        {/* Headline block */}
        <div style={{ position: 'absolute', top: 96, left: 90, transform: `translateY(${(1 - sp(15)) * -60}px)`, opacity: sp(15) }}>
          <div style={{ fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 92, color: '#25343F', lineHeight: 1, textShadow: '5px 5px 0 #FFF449' }}>INFLATION ALARM</div>
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 40, fontWeight: 700, color: '#FFF8EC', marginTop: 10 }}>जुलाई 2026 — मुश्किल और बढ़ गई</div>
        </div>

        {/* Giant counter card — brutalist */}
        <div style={{ position: 'absolute', left: 90, top: 320, width: 660, background: '#25343F', border: '6px solid #1A252D', boxShadow: '16px 16px 0 rgba(26,37,45,0.45)', padding: '36px 44px', transform: `rotate(-1.5deg) scale(${sp(35)})` }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 30, fontWeight: 800, color: '#FF9B51', letterSpacing: 4 }}>JUNE CPI INFLATION</div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 150, fontWeight: 800, color: '#FFF449', lineHeight: 1.05 }}>
            {counter.toFixed(2)}<span style={{ fontSize: 76 }}>%</span>
          </div>
          <div style={{ height: 5, background: '#FF9B51', margin: '14px 0' }} />
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 34, fontWeight: 700, color: '#FFF8EC' }}>
            RBI TARGET <span style={{ color: '#FFF449' }}>4.00%</span> से ऊपर
          </div>
        </div>

        {/* Bar chart — 8 months climbing */}
        <div style={{ position: 'absolute', right: 90, top: 300, width: 880, height: chartH + 130, background: 'rgba(255,248,236,0.94)', border: '6px solid #25343F', boxShadow: '16px 16px 0 rgba(37,52,63,0.5)', padding: '26px 34px', transform: `rotate(1deg) translateY(${(1 - sp(60)) * 80}px)`, opacity: sp(60) }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 26, fontWeight: 800, color: '#25343F', letterSpacing: 3 }}>लगातार 8वें महीने ऊपर</div>
          <div style={{ position: 'relative', height: chartH, marginTop: 12, display: 'flex', alignItems: 'flex-end', gap: 26 }}>
            {/* dashed 4% target line */}
            <div style={{ position: 'absolute', left: 0, right: 0, bottom: (4 / 5) * chartH, borderTop: '4px dashed #B8341B', zIndex: 2, opacity: sp(120) }}>
              <span style={{ position: 'absolute', right: 0, top: -34, fontFamily: 'JetBrains Mono, monospace', fontSize: 22, fontWeight: 700, color: '#B8341B' }}>TARGET 4%</span>
            </div>
            {vals.map((v, i) => {
              const h = (v / 5) * chartH * sp(150 + i * 14, { damping: 11 });
              const hot = i === vals.length - 1;
              return (
                <div key={i} style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'flex-end', height: '100%' }}>
                  <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 20, fontWeight: 700, color: '#25343F', opacity: sp(170 + i * 14) }}>{v.toFixed(2)}</div>
                  <div style={{ width: '100%', height: Math.max(0, h), background: hot ? '#B8341B' : '#25343F', border: '3px solid #1A252D', boxShadow: hot ? '6px 6px 0 rgba(184,52,27,0.35)' : 'none', transform: hot ? `scaleY(${1 + Math.sin(frame * 0.15) * 0.008})` : 'none', transformOrigin: 'bottom' }} />
                  <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 18, fontWeight: 700, color: '#25343F', marginTop: 8 }}>{months[i]}</div>
                </div>
              );
            })}
          </div>
        </div>

        {/* Cause chips — SVG iconography */}
        {[
          { t: 'US–IRAN CONFLICT', d: 300, icon: <path d="M12 2 L15 9 L22 9 L16.5 13.5 L18.5 21 L12 16.5 L5.5 21 L7.5 13.5 L2 9 L9 9 Z" fill="#B8341B" /> },
          { t: 'कमज़ोर MONSOON', d: 330, icon: <path d="M6 14 a6 6 0 1 1 2-11.6 A7 7 0 0 1 21 9 a4.5 4.5 0 0 1-1 8.9 H6 Z M8 20 l-2 3 M13 20 l-2 3 M18 20 l-2 3" fill="none" stroke="#25343F" strokeWidth="2.4" /> },
          { t: 'FOOD + FUEL PRICES ↑', d: 360, icon: <path d="M12 2 C7 8 6 11 6 14 a6 6 0 0 0 12 0 c0-3-1-6-6-12 Z" fill="#D9762B" stroke="#25343F" strokeWidth="2" /> },
        ].map((c, i) => (
          <div key={i} style={{ position: 'absolute', left: 90 + i * 400, bottom: 120, display: 'flex', alignItems: 'center', gap: 14, background: '#FFF449', border: '5px solid #25343F', boxShadow: '9px 9px 0 #25343F', padding: '14px 24px', transform: `rotate(${i % 2 === 0 ? -2 : 2}deg) scale(${sp(c.d, { damping: 9 })}) translateY(${Math.sin(frame * 0.05 + i) * 3}px)` }}>
            <svg width="34" height="34" viewBox="0 0 24 24">{c.icon}</svg>
            <span style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 27, fontWeight: 800, color: '#25343F' }}>{c.t}</span>
          </div>
        ))}

        {/* Rubber stamp payoff */}
        <div style={{ position: 'absolute', right: 150, top: 130, transform: `rotate(${-9 + stampShake}deg) scale(${interpolate(stamp, [0, 1], [2.6, 1])})`, opacity: Math.min(1, stamp * 1.4), border: '7px solid #B8341B', color: '#B8341B', padding: '10px 26px', fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 44, background: 'rgba(255,248,236,0.9)' }}>
          17 महीनों में पहली बार
        </div>
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(58,26,4,0.5) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene11InflationAlarm;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12RbiDilemma',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12RbiDilemma: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow lateral pan + gentle zoom-out settle
  const camScale = interpolate(frame, [0, 120, durationInFrames], [1.08, 1.02, 1.05], { extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) });
  const camY = Math.sin(frame * 0.006) * 10;

  // Seesaw physics: wobbles, then locks flat at STATUS QUO moment (frame ~470)
  const wobble = Math.sin(frame * 0.045) * 7;
  const settle = interpolate(frame, [430, 480], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const beamRot = wobble * settle;

  const stamp = sp(480, { damping: 8, stiffness: 210 });
  const slamShake = frame > 480 && frame < 500 ? Math.sin(frame * 3.2) * (500 - frame) * 0.55 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #E8823A 0%, #C96A22 60%, #A85717 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 50% ${28 + Math.sin(frame * 0.012) * 6}%, rgba(255,155,81,0.5), transparent 55%)` }} />

      {/* LAYER 2: Animated shape layers */}
      <div style={{ position: 'absolute', width: 900, height: 900, border: '3px solid rgba(37,52,63,0.1)', borderRadius: '50%', left: 510, top: 90, transform: `scale(${1 + Math.sin(frame * 0.015) * 0.04}) rotate(${frame * 0.04}deg)` }} />
      <div style={{ position: 'absolute', width: 0, height: 0, borderLeft: '260px solid transparent', borderRight: '260px solid transparent', borderBottom: '440px solid rgba(255,244,73,0.05)', left: -140, bottom: -160, transform: `rotate(${Math.sin(frame * 0.01) * 8}deg)` }} />

      {/* LAYER 3: Motion texture — diagonal caution stripes */}
      <AbsoluteFill style={{ backgroundImage: 'repeating-linear-gradient(45deg, rgba(37,52,63,0.05) 0 26px, transparent 26px 52px)', backgroundPosition: `${frame * 0.4}px 0`, opacity: 0.9 }} />

      {/* LAYER 4: Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const sx = seededRandom(i * 5 + 2) * 1920;
        const sy = seededRandom(i * 9 + 4) * 1080;
        return (
          <div key={i} style={{ position: 'absolute', left: sx + Math.sin(frame * 0.018 + i) * 22, top: sy + Math.cos(frame * 0.014 + i) * 16, width: 10, height: 10, borderRadius: i % 2 ? '50%' : 0, background: i % 3 === 0 ? '#FFF449' : '#25343F', opacity: 0.13, transform: `rotate(${frame + i * 24}deg)` }} />
        );
      })}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${slamShake}px, ${camY}px)` }}>

        {/* Headline */}
        <div style={{ position: 'absolute', top: 60, width: '100%', textAlign: 'center', transform: `translateY(${(1 - sp(10)) * -50}px)`, opacity: sp(10) }}>
          <span style={{ fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 96, color: '#FFF449', textShadow: '6px 6px 0 #25343F' }}>RBI का DILEMMA</span>
        </div>

        {/* SEESAW infographic */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 480, display: 'flex', justifyContent: 'center' }}>
          <div style={{ position: 'relative', width: 1240 }}>
            {/* beam */}
            <div style={{ position: 'absolute', left: 20, right: 20, top: 0, height: 22, background: '#25343F', border: '4px solid #1A252D', transform: `rotate(${beamRot}deg)`, transformOrigin: 'center', boxShadow: '0 10px 0 rgba(26,37,45,0.3)' }}>
              {/* left pan card: RATE CUT */}
              <div style={{ position: 'absolute', left: -30, top: -240, width: 400, background: '#FFF8EC', border: '6px solid #25343F', boxShadow: '12px 12px 0 rgba(37,52,63,0.45)', padding: '22px 26px', transform: `rotate(${-beamRot * 0.6 - 2}deg) scale(${sp(80)})` }}>
                <svg width="46" height="46" viewBox="0 0 24 24"><path d="M4 6 L12 18 L20 6" fill="none" stroke="#B8341B" strokeWidth="3.4" /></svg>
                <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 40, fontWeight: 900, color: '#B8341B' }}>RATE CUT?</div>
                <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 28, fontWeight: 700, color: '#25343F', lineHeight: 1.35 }}>Inflation और भड़केगी • Purchasing power पर चोट</div>
              </div>
              {/* right pan card: RATE HIKE */}
              <div style={{ position: 'absolute', right: -30, top: -240, width: 400, background: '#FFF8EC', border: '6px solid #25343F', boxShadow: '12px 12px 0 rgba(37,52,63,0.45)', padding: '22px 26px', transform: `rotate(${-beamRot * 0.6 + 2}deg) scale(${sp(140)})` }}>
                <svg width="46" height="46" viewBox="0 0 24 24"><path d="M4 18 L12 6 L20 18" fill="none" stroke="#25343F" strokeWidth="3.4" /></svg>
                <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 40, fontWeight: 900, color: '#25343F' }}>RATE HIKE?</div>
                <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 28, fontWeight: 700, color: '#25343F', lineHeight: 1.35 }}>Economy पर pressure • Growth slowdown</div>
              </div>
            </div>
            {/* pivot triangle */}
            <div style={{ position: 'absolute', left: '50%', top: 18, transform: `translateX(-50%) scale(${sp(40)})`, width: 0, height: 0, borderLeft: '70px solid transparent', borderRight: '70px solid transparent', borderBottom: '120px solid #25343F' }} />
            <div style={{ position: 'absolute', left: '50%', top: 62, transform: 'translateX(-50%)', fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 900, color: '#FFF449', zIndex: 3 }}>RBI</div>
          </div>
        </div>

        {/* Governor quote card — slides up */}
        <div style={{ position: 'absolute', left: '50%', top: 200, width: 780, transform: `translateX(-50%) translateY(${(1 - sp(260)) * 90}px) rotate(-1deg)`, opacity: sp(260), background: '#25343F', border: '6px solid #1A252D', boxShadow: '14px 14px 0 rgba(26,37,45,0.5)', padding: '26px 36px' }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 24, fontWeight: 700, color: '#FF9B51', letterSpacing: 3 }}>GOVERNOR SANJAY MALHOTRA</div>
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 36, fontWeight: 700, color: '#FFF8EC', marginTop: 8, lineHeight: 1.4 }}>
            &ldquo;Inflation pressures <span style={{ color: '#FFF449' }}>supply-side</span> हैं — tightening की बात <span style={{ color: '#FFF449' }}>premature</span>&rdquo;
          </div>
        </div>

        {/* STATUS QUO stamp slam */}
        <div style={{ position: 'absolute', left: '50%', top: 430, transform: `translateX(-50%) rotate(${-7 + slamShake}deg) scale(${interpolate(stamp, [0, 1], [3, 1])})`, opacity: Math.min(1, stamp * 1.5), border: '9px solid #B8341B', color: '#B8341B', padding: '14px 44px', fontFamily: 'Yantra One, sans-serif', fontSize: 90, letterSpacing: 6, background: 'rgba(255,248,236,0.92)', zIndex: 5 }}>
          STATUS QUO
        </div>

        {/* Bottom verdict strip */}
        <div style={{ position: 'absolute', bottom: 70, left: 0, right: 0, display: 'flex', justifyContent: 'center', opacity: sp(560) }}>
          <div style={{ background: '#FFF449', border: '6px solid #25343F', boxShadow: '10px 10px 0 #25343F', padding: '16px 44px', transform: `rotate(1deg) translateY(${Math.sin(frame * 0.06) * 3}px) scale(${sp(560, { damping: 9 })})`, fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 46, fontWeight: 900, color: '#25343F' }}>
            मतलब — आपकी EMI में कोई राहत नहीं
          </div>
        </div>
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(58,26,4,0.52) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene12RbiDilemma;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13GdpSlide',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13GdpSlide: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: tilt-drift orbit feel
  const camScale = interpolate(frame, [0, durationInFrames], [1.07, 1], { extrapolateRight: 'clamp' });
  const camRot = Math.sin(frame * 0.005) * 0.5;

  // GDP bars data
  const bars = [
    { label: 'FY26 ACTUAL', v: 7.7, color: '#25343F', d: 90 },
    { label: 'FY27 RBI EST.', v: 6.6, color: '#8A4A12', d: 170 },
    { label: 'FY27 IMF', v: 6.4, color: '#B8341B', d: 250 },
  ];
  const maxH = 420;

  // Zigzag decline arrow draw-on
  const dash = interpolate(frame, [300, 420], [900, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  const inflCounter = interpolate(frame, [430, 510], [0, 5.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(120deg, #DD7C31 0%, #C4661F 55%, #9E5013 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at ${70 + Math.sin(frame * 0.01) * 8}% 75%, rgba(255,155,81,0.4), transparent 55%)` }} />

      {/* LAYER 2: Animated shape layers */}
      <div style={{ position: 'absolute', width: 760, height: 760, background: 'rgba(37,52,63,0.05)', borderRadius: '50%', right: -260, top: -260, transform: `scale(${1 + Math.sin(frame * 0.018) * 0.035})` }} />
      <div style={{ position: 'absolute', width: 520, height: 520, border: '60px solid rgba(255,244,73,0.05)', left: -200, bottom: -200, transform: `rotate(${frame * 0.06}deg)` }} />

      {/* LAYER 3: Motion texture — blueprint grid */}
      <AbsoluteFill style={{ backgroundImage: 'linear-gradient(rgba(37,52,63,0.07) 1px, transparent 1px), linear-gradient(90deg, rgba(37,52,63,0.07) 1px, transparent 1px)', backgroundSize: '64px 64px', backgroundPosition: `0 ${frame * 0.2}px` }} />

      {/* LAYER 4: Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: seededRandom(i * 3 + 1) * 1920 + Math.sin(frame * 0.02 + i) * 20, top: seededRandom(i * 11 + 6) * 1080 + Math.cos(frame * 0.016 + i * 2) * 14, width: 7 + seededRandom(i) * 9, height: 7 + seededRandom(i) * 9, background: i % 4 === 0 ? '#FFF449' : '#25343F', opacity: 0.12, transform: `rotate(${45 + frame * 0.5}deg)` }} />
      ))}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>

        {/* Headline — typographic collide */}
        <div style={{ position: 'absolute', top: 64, left: 90 }}>
          <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 100, color: '#25343F', lineHeight: 0.95, transform: `translateX(${(1 - sp(10)) * -300}px)`, opacity: sp(10) }}>GROWTH <span style={{ color: '#B8341B' }}>↓</span></div>
          <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 100, color: '#FFF449', lineHeight: 0.95, textShadow: '5px 5px 0 #25343F', transform: `translateX(${(1 - sp(30)) * 300}px)`, opacity: sp(30) }}>INFLATION <span style={{ color: '#FFF8EC' }}>↑</span></div>
        </div>

        {/* GDP bar chart — brutalist card */}
        <div style={{ position: 'absolute', left: 90, bottom: 110, width: 900, background: 'rgba(255,248,236,0.95)', border: '6px solid #25343F', boxShadow: '16px 16px 0 rgba(37,52,63,0.5)', padding: '30px 40px', transform: `rotate(-1deg) translateY(${(1 - sp(60)) * 100}px)`, opacity: sp(60) }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 27, fontWeight: 900, color: '#25343F', letterSpacing: 3 }}>GDP GROWTH FORECAST</div>
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 60, height: maxH, marginTop: 16, position: 'relative' }}>
            {bars.map((b, i) => {
              const h = (b.v / 8) * maxH * sp(b.d, { damping: 10 });
              return (
                <div key={i} style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'flex-end', height: '100%' }}>
                  <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 46, fontWeight: 800, color: b.color, opacity: sp(b.d + 25) }}>{b.v}%</div>
                  <div style={{ width: '78%', height: Math.max(0, h), background: b.color, border: '4px solid #1A252D', transform: `scaleY(${1 + Math.sin(frame * 0.1 + i) * 0.006})`, transformOrigin: 'bottom' }} />
                  <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 21, fontWeight: 800, color: '#25343F', marginTop: 10, letterSpacing: 1 }}>{b.label}</div>
                </div>
              );
            })}
            {/* zigzag decline arrow across bars */}
            <svg width="800" height="420" viewBox="0 0 800 420" style={{ position: 'absolute', left: 0, top: 0, pointerEvents: 'none' }}>
              <path d="M 110 60 L 400 130 L 690 155 L 660 130 M 690 155 L 655 165" fill="none" stroke="#B8341B" strokeWidth="9" strokeDasharray="900" strokeDashoffset={dash} strokeLinecap="square" />
            </svg>
          </div>
        </div>

        {/* Inflation forecast card */}
        <div style={{ position: 'absolute', right: 90, bottom: 200, width: 660, background: '#25343F', border: '6px solid #1A252D', boxShadow: '16px 16px 0 rgba(26,37,45,0.5)', padding: '30px 40px', transform: `rotate(1.5deg) scale(${sp(400, { damping: 9 })}) translateY(${Math.sin(frame * 0.05) * 4}px)` }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 24, fontWeight: 700, color: '#FF9B51', letterSpacing: 3 }}>BMI / FITCH GROUP — FY27</div>
          <div style={{ display: 'flex', alignItems: 'baseline', gap: 20 }}>
            <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 128, fontWeight: 800, color: '#FFF449' }}>{inflCounter.toFixed(1)}%</span>
            <span style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 36, fontWeight: 700, color: '#FFF8EC' }}>तक INFLATION</span>
          </div>
          <div style={{ height: 4, background: '#FF9B51', margin: '12px 0' }} />
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 28, fontWeight: 700, color: '#FFF8EC', lineHeight: 1.4 }}>
            Global trade uncertainty + weaker external demand
          </div>
        </div>

        {/* Corner tape label */}
        <div style={{ position: 'absolute', right: 120, top: 110, background: '#FFF449', border: '5px solid #25343F', padding: '10px 26px', transform: `rotate(6deg) scale(${sp(200, { damping: 9 })})`, fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 32, fontWeight: 900, color: '#25343F', boxShadow: '8px 8px 0 #25343F' }}>
          Economy की रफ़्तार धीमी
        </div>
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(58,26,4,0.5) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene13GdpSlide;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14CpiTrajectory',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14CpiTrajectory: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow zoom toward chart payoff
  const camScale = interpolate(frame, [0, 500, durationInFrames], [1, 1.04, 1.09], { extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) });

  // CPI projection points (month, value)
  const pts = [
    { m: 'JUL', v: 4.55 }, { m: 'AUG', v: 4.8 }, { m: 'SEP', v: 5.05 },
    { m: 'OCT', v: 5.4 }, { m: 'NOV', v: 5.7 }, { m: 'DEC', v: 5.95 },
  ];
  const W = 1060, H = 470;
  const px = (i: number) => 80 + (i * (W - 160)) / (pts.length - 1);
  const py = (v: number) => H - ((v - 4) / 2.4) * (H - 90) - 40;
  const pathD = pts.map((p, i) => `${i === 0 ? 'M' : 'L'} ${px(i)} ${py(p.v)}`).join(' ');
  const draw = interpolate(frame, [120, 360], [1600, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  const stamp = sp(480, { damping: 8, stiffness: 210 });
  const shake = frame > 480 && frame < 498 ? Math.sin(frame * 3.1) * (498 - frame) * 0.5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #E27E33 0%, #C86921 50%, #A2540F 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at 20% ${80 + Math.sin(frame * 0.01) * 5}%, rgba(255,155,81,0.45), transparent 58%)` }} />

      {/* LAYER 2: Animated shape layers */}
      <div style={{ position: 'absolute', width: 700, height: 700, borderRadius: '50%', border: '90px solid rgba(255,244,73,0.045)', right: -240, top: -240, transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.02) * 0.03})` }} />
      <div style={{ position: 'absolute', width: 460, height: 460, background: 'rgba(37,52,63,0.06)', left: -160, top: 300, transform: `rotate(${24 + Math.sin(frame * 0.01) * 6}deg)` }} />

      {/* LAYER 3: Motion texture — vertical scanlines */}
      <AbsoluteFill style={{ backgroundImage: 'repeating-linear-gradient(90deg, rgba(37,52,63,0.05) 0 3px, transparent 3px 42px)', backgroundPosition: `${frame * 0.35}px 0` }} />

      {/* LAYER 4: Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: seededRandom(i * 7 + 3) * 1920 + Math.sin(frame * 0.02 + i * 1.4) * 24, top: seededRandom(i * 5 + 8) * 1080 - ((frame * (0.3 + seededRandom(i) * 0.4)) % 1140) + 60, width: 6 + seededRandom(i + 9) * 8, height: 6 + seededRandom(i + 9) * 8, background: i % 3 === 0 ? '#FFF449' : '#25343F', opacity: 0.13, transform: `rotate(${frame * 0.6 + i * 40}deg)` }} />
      ))}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)` }}>

        {/* Headline */}
        <div style={{ position: 'absolute', top: 58, left: 90, transform: `translateY(${(1 - sp(8)) * -60}px)`, opacity: sp(8) }}>
          <div style={{ fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 88, color: '#25343F', textShadow: '5px 5px 0 #FFF449', lineHeight: 1 }}>CPI की चढ़ती TRAJECTORY</div>
        </div>

        {/* Hormuz chip with ship SVG */}
        <div style={{ position: 'absolute', right: 100, top: 70, display: 'flex', alignItems: 'center', gap: 14, background: '#25343F', border: '5px solid #1A252D', boxShadow: '9px 9px 0 rgba(26,37,45,0.5)', padding: '12px 24px', transform: `rotate(2deg) scale(${sp(60, { damping: 9 })}) translateY(${Math.sin(frame * 0.05) * 3}px)` }}>
          <svg width="40" height="40" viewBox="0 0 24 24"><path d="M3 15 L21 15 L18 20 L6 20 Z M12 15 V4 M12 6 L17 9 L12 12" fill="none" stroke="#FFF449" strokeWidth="2.2" /></svg>
          <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 27, fontWeight: 800, color: '#FFF8EC', letterSpacing: 1 }}>STRAIT OF HORMUZ RISK</span>
        </div>

        {/* Line chart card */}
        <div style={{ position: 'absolute', left: 90, top: 210, width: W + 80, background: 'rgba(255,248,236,0.95)', border: '6px solid #25343F', boxShadow: '16px 16px 0 rgba(37,52,63,0.5)', padding: '24px 30px', transform: `rotate(-0.8deg) translateY(${(1 - sp(50)) * 90}px)`, opacity: sp(50) }}>
          <svg width={W} height={H} viewBox={`0 0 ${W} ${H}`}>
            {/* danger zone above 5% */}
            <rect x="0" y="0" width={W} height={py(5)} fill="rgba(184,52,27,0.1)" opacity={sp(300)} />
            <line x1="0" x2={W} y1={py(5)} y2={py(5)} stroke="#B8341B" strokeWidth="4" strokeDasharray="16 10" opacity={sp(280)} />
            <text x={W - 260} y={py(5) - 14} fontFamily="JetBrains Mono, monospace" fontSize="26" fontWeight="700" fill="#B8341B" opacity={sp(300)}>DANGER: 5% पार</text>
            {/* projection path draw-on */}
            <path d={pathD} fill="none" stroke="#25343F" strokeWidth="10" strokeDasharray="1600" strokeDashoffset={draw} strokeLinejoin="round" />
            {/* data nodes + labels */}
            {pts.map((p, i) => (
              <g key={i} opacity={sp(160 + i * 40)}>
                <circle cx={px(i)} cy={py(p.v)} r={p.v >= 5 ? 15 : 11} fill={p.v >= 5 ? '#B8341B' : '#FFF449'} stroke="#25343F" strokeWidth="5" />
                <text x={px(i)} y={py(p.v) - 26} textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="30" fontWeight="800" fill="#25343F">{p.v}%</text>
                <text x={px(i)} y={H - 6} textAnchor="middle" fontFamily="Inter, sans-serif" fontSize="24" fontWeight="800" fill="#25343F">{p.m}</text>
              </g>
            ))}
            {/* pulsing head marker */}
            <circle cx={px(5)} cy={py(5.95)} r={18 + Math.sin(frame * 0.25) * 5} fill="none" stroke="#B8341B" strokeWidth="4" opacity={sp(380)} />
          </svg>
        </div>

        {/* BMI hike card */}
        <div style={{ position: 'absolute', right: 90, top: 300, width: 540, background: '#25343F', border: '6px solid #1A252D', boxShadow: '14px 14px 0 rgba(26,37,45,0.5)', padding: '28px 34px', transform: `rotate(1.6deg) scale(${sp(400, { damping: 9 })})` }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 23, fontWeight: 700, color: '#FF9B51', letterSpacing: 3 }}>BMI FORECAST</div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 96, fontWeight: 800, color: '#FFF449', lineHeight: 1.05 }}>+50<span style={{ fontSize: 44 }}> BPS</span></div>
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 30, fontWeight: 700, color: '#FFF8EC', lineHeight: 1.4 }}>Cumulative RATE HIKE की संभावना</div>
        </div>

        {/* HIKE? stamp */}
        <div style={{ position: 'absolute', right: 160, bottom: 120, transform: `rotate(${-8 + shake}deg) scale(${interpolate(stamp, [0, 1], [2.8, 1])})`, opacity: Math.min(1, stamp * 1.5), border: '8px solid #B8341B', color: '#B8341B', padding: '12px 34px', fontFamily: 'Yantra One, sans-serif', fontSize: 74, letterSpacing: 4, background: 'rgba(255,248,236,0.92)' }}>
          RATE HIKE?
        </div>
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(58,26,4,0.52) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene14CpiTrajectory;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15WaitingLoop',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15WaitingLoop: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: hypnotic slow rotation + zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08], { extrapolateRight: 'clamp' });
  const camRot = Math.sin(frame * 0.004) * 0.8;

  // Clock hands
  const minRot = frame * 3.4;
  const hrRot = frame * 0.3;

  // Loop nodes of the waiting trap
  const nodes = ['WAIT करो', 'RATE CUT होगा?', 'EMI घटेगी?', 'POSTPONE करो'];
  const loopRot = frame * 0.25;

  const stamps = [
    { t: 'POSTPONED', d: 380, x: 1210, y: 250, r: -10 },
    { t: 'POSTPONED', d: 440, x: 1290, y: 420, r: 7 },
    { t: 'POSTPONED', d: 500, x: 1180, y: 590, r: -5 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 34% 46%, #E8823A 0%, #C4661F 55%, #94480C 100%)' }} />

      {/* LAYER 2: Animated shape layers — concentric hypnosis rings */}
      {[840, 640, 440].map((s, i) => (
        <div key={i} style={{ position: 'absolute', width: s, height: s, borderRadius: '50%', border: '3px dashed rgba(37,52,63,0.14)', left: 560 - s / 2 + 90, top: 560 - s / 2, transform: `rotate(${frame * (0.2 + i * 0.12) * (i % 2 ? -1 : 1)}deg)` }} />
      ))}
      <div style={{ position: 'absolute', width: 500, height: 500, background: 'rgba(255,244,73,0.045)', right: -170, top: -170, transform: `rotate(${Math.sin(frame * 0.012) * 10}deg)` }} />

      {/* LAYER 3: Motion texture — halftone */}
      <AbsoluteFill style={{ backgroundImage: 'radial-gradient(#25343F 1.3px, transparent 1.3px)', backgroundSize: '34px 34px', backgroundPosition: `0 ${frame * 0.18}px`, opacity: 0.06 }} />

      {/* LAYER 4: Floating particles — drifting "wait" dust */}
      {Array.from({ length: 15 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: seededRandom(i * 6 + 2) * 1920 + Math.sin(frame * 0.017 + i) * 26, top: seededRandom(i * 4 + 7) * 1080 + Math.cos(frame * 0.013 + i) * 20, width: 9, height: 9, borderRadius: '50%', background: i % 3 === 0 ? '#FFF449' : '#25343F', opacity: 0.12 }} />
      ))}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>

        {/* Headline */}
        <div style={{ position: 'absolute', top: 62, left: 0, right: 0, textAlign: 'center', transform: `translateY(${(1 - sp(8)) * -55}px)`, opacity: sp(8) }}>
          <span style={{ fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 94, color: '#FFF449', textShadow: '6px 6px 0 #25343F' }}>इंतज़ार का TRAP</span>
        </div>

        {/* Clock + orbiting loop nodes */}
        <div style={{ position: 'absolute', left: 200, top: 260, width: 700, height: 700 }}>
          {/* clock face */}
          <div style={{ position: 'absolute', left: 155, top: 155, width: 390, height: 390, borderRadius: '50%', background: '#FFF8EC', border: '10px solid #25343F', boxShadow: '14px 14px 0 rgba(37,52,63,0.45)', transform: `scale(${sp(40)})` }}>
            {Array.from({ length: 12 }).map((_, i) => (
              <div key={i} style={{ position: 'absolute', left: '50%', top: '50%', width: 6, height: 26, background: '#25343F', transform: `rotate(${i * 30}deg) translateY(-165px)`, transformOrigin: 'center' }} />
            ))}
            {/* hands */}
            <div style={{ position: 'absolute', left: '50%', top: '50%', width: 10, height: 130, background: '#25343F', transformOrigin: 'top center', transform: `rotate(${180 + minRot}deg)` }} />
            <div style={{ position: 'absolute', left: '50%', top: '50%', width: 14, height: 92, background: '#B8341B', transformOrigin: 'top center', transform: `rotate(${180 + hrRot}deg)` }} />
            <div style={{ position: 'absolute', left: '50%', top: '50%', width: 26, height: 26, borderRadius: '50%', background: '#FFF449', border: '5px solid #25343F', transform: 'translate(-50%, -50%)' }} />
          </div>
          {/* orbiting trap nodes */}
          {nodes.map((n, i) => {
            const ang = (loopRot + i * 90) * (Math.PI / 180);
            return (
              <div key={i} style={{ position: 'absolute', left: 350 + Math.cos(ang) * 330 - 110, top: 350 + Math.sin(ang) * 330 - 34, width: 220, textAlign: 'center', background: i === 3 ? '#B8341B' : '#25343F', border: '5px solid #1A252D', boxShadow: '7px 7px 0 rgba(26,37,45,0.4)', padding: '12px 8px', fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 26, fontWeight: 800, color: i === 3 ? '#FFF8EC' : '#FFF449', opacity: sp(120 + i * 30), transform: `rotate(${Math.sin(frame * 0.05 + i) * 3}deg) scale(${sp(120 + i * 30, { damping: 9 })})` }}>
                {n}
              </div>
            );
          })}
        </div>

        {/* Right narrative block */}
        <div style={{ position: 'absolute', right: 110, top: 250, width: 700 }}>
          <div style={{ background: 'rgba(255,248,236,0.95)', border: '6px solid #25343F', boxShadow: '14px 14px 0 rgba(37,52,63,0.5)', padding: '26px 34px', transform: `rotate(1deg) translateX(${(1 - sp(200)) * 200}px)`, opacity: sp(200) }}>
            <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 40, fontWeight: 900, color: '#25343F', lineHeight: 1.35 }}>
              &ldquo;EMI घटेगी <span style={{ background: '#FFF449', padding: '0 10px' }}>तब</span> invest करूँगा&rdquo;
            </div>
            <div style={{ height: 5, background: '#D9762B', margin: '16px 0' }} />
            <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 30, fontWeight: 700, color: '#8A4A12', lineHeight: 1.4 }}>
              यह narrative planning का center बन गया — और decisions रुकते गए
            </div>
          </div>
          {/* warning bar */}
          <div style={{ marginTop: 46, background: '#25343F', border: '6px solid #1A252D', boxShadow: '12px 12px 0 rgba(26,37,45,0.5)', padding: '20px 30px', transform: `rotate(-1.4deg) scale(${sp(560, { damping: 9 })})` }}>
            <span style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 36, fontWeight: 900, color: '#FFF449' }}>Rate बढ़ भी सकता है — और borrowers तैयार नहीं</span>
          </div>
        </div>

        {/* POSTPONED stamp pile */}
        {stamps.map((s, i) => {
          const g = sp(s.d, { damping: 8, stiffness: 220 });
          return (
            <div key={i} style={{ position: 'absolute', left: s.x, top: s.y, transform: `rotate(${s.r}deg) scale(${interpolate(g, [0, 1], [2.4, 1])})`, opacity: Math.min(0.9, g * 1.4), border: '6px solid #B8341B', color: '#B8341B', padding: '6px 20px', fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 40, letterSpacing: 5, background: 'rgba(255,248,236,0.6)' }}>
              {s.t}
            </div>
          );
        })}
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(58,26,4,0.55) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene15WaitingLoop;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16Flatline',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16Flatline: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow push + micro sway
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07], { extrapolateRight: 'clamp' });
  const camX = Math.sin(frame * 0.007) * 7;

  // ECG-style flatline draws across
  const lineDraw = interpolate(frame, [70, 300], [1500, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const headX = interpolate(frame, [70, 300], [80, 1380], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // Money-loss ticker ₹0 -> ₹6,00,000
  const loss = interpolate(frame, [430, 600], [0, 600000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const lossStr = Math.floor(loss).toLocaleString('en-IN');

  const slam = sp(360, { damping: 8, stiffness: 220 });
  const shake = frame > 360 && frame < 380 ? Math.sin(frame * 3.3) * (380 - frame) * 0.55 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(125deg, #E07B2F 0%, #C0631C 55%, #97490B 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at ${76 + Math.sin(frame * 0.011) * 6}% 24%, rgba(255,155,81,0.45), transparent 58%)` }} />

      {/* LAYER 2: Animated shape layers */}
      <div style={{ position: 'absolute', width: 620, height: 620, borderRadius: '50%', background: 'rgba(37,52,63,0.055)', left: -220, top: -200, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})` }} />
      <div style={{ position: 'absolute', width: 380, height: 380, border: '52px solid rgba(255,244,73,0.05)', right: -130, bottom: -150, transform: `rotate(${frame * 0.07}deg)` }} />

      {/* LAYER 3: Motion texture — monitor grid */}
      <AbsoluteFill style={{ backgroundImage: 'linear-gradient(rgba(37,52,63,0.08) 1px, transparent 1px), linear-gradient(90deg, rgba(37,52,63,0.08) 1px, transparent 1px)', backgroundSize: '56px 56px', backgroundPosition: `${-frame * 0.5}px 0` }} />

      {/* LAYER 4: Floating particles — falling coins (SVG circles) */}
      {Array.from({ length: 12 }).map((_, i) => {
        const fall = ((frame * (1.4 + seededRandom(i) * 2) + seededRandom(i * 8) * 1080) % 1220) - 70;
        return (
          <svg key={i} width="30" height="30" viewBox="0 0 24 24" style={{ position: 'absolute', left: seededRandom(i * 3 + 5) * 1880, top: fall, opacity: 0.16, transform: `rotate(${frame * 2 + i * 30}deg)` }}>
            <circle cx="12" cy="12" r="10" fill="none" stroke="#25343F" strokeWidth="2.5" />
            <text x="12" y="17" textAnchor="middle" fontSize="13" fontWeight="800" fill="#25343F">₹</text>
          </svg>
        );
      })}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX + shake}px)` }}>

        {/* ECG monitor card */}
        <div style={{ position: 'absolute', left: 90, top: 90, width: 1480, background: '#25343F', border: '6px solid #1A252D', boxShadow: '16px 16px 0 rgba(26,37,45,0.5)', padding: '26px 40px', transform: `translateY(${(1 - sp(20)) * -80}px)`, opacity: sp(20) }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline' }}>
            <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 26, fontWeight: 700, color: '#FF9B51', letterSpacing: 3 }}>REPO RATE MONITOR — 2026</span>
            <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 56, fontWeight: 800, color: '#FFF449', textShadow: `0 0 ${10 + Math.sin(frame * 0.2) * 6}px rgba(255,244,73,0.55)` }}>5.25%</span>
          </div>
          <svg width="1400" height="230" viewBox="0 0 1400 230">
            {/* flatline with tiny heartbeat blips at each meeting */}
            <path d="M 80 130 L 380 130 L 396 96 L 412 158 L 428 130 L 720 130 L 736 96 L 752 158 L 768 130 L 1060 130 L 1076 96 L 1092 158 L 1108 130 L 1380 130" fill="none" stroke="#FFF449" strokeWidth="7" strokeDasharray="1500" strokeDashoffset={lineDraw} strokeLinejoin="round" />
            <circle cx={headX} cy={130} r={9 + Math.sin(frame * 0.3) * 3} fill="#FFF449" />
            {['MEETING 1', 'MEETING 2', 'MEETING 3'].map((m, i) => (
              <text key={i} x={404 + i * 340} y={210} textAnchor="middle" fontFamily="Inter, sans-serif" fontSize="24" fontWeight="800" fill="#FF9B51" opacity={sp(150 + i * 60)}>{m} — UNCHANGED</text>
            ))}
          </svg>
        </div>

        {/* Typographic slam: TIME WASTE = MONEY WASTE */}
        <div style={{ position: 'absolute', left: 90, top: 560 }}>
          <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 92, color: '#25343F', transform: `translateX(${(1 - sp(330)) * -400}px)`, opacity: sp(330), lineHeight: 1 }}>TIME WASTE</div>
          <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 92, color: '#FFF449', textShadow: '6px 6px 0 #25343F', transform: `translateX(${(1 - slam) * 400}px) rotate(-1.2deg)`, opacity: slam, lineHeight: 1.05 }}>= MONEY WASTE</div>
        </div>

        {/* Loss counter card */}
        <div style={{ position: 'absolute', right: 90, top: 560, width: 640, background: 'rgba(255,248,236,0.95)', border: '6px solid #25343F', boxShadow: '14px 14px 0 rgba(37,52,63,0.5)', padding: '26px 36px', transform: `rotate(1.4deg) scale(${sp(420, { damping: 9 })}) translateY(${Math.sin(frame * 0.05) * 4}px)` }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 25, fontWeight: 900, color: '#B8341B', letterSpacing: 2 }}>सिर्फ 0.25% – 0.50% CHANGE से</div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 92, fontWeight: 800, color: '#25343F', lineHeight: 1.1 }}>₹{lossStr}</div>
          <div style={{ height: 5, background: '#B8341B', margin: '10px 0' }} />
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 30, fontWeight: 800, color: '#25343F' }}>तक EXTRA REPAYMENT (₹2–6 लाख)</div>
        </div>

        {/* poll chip */}
        <div style={{ position: 'absolute', left: 96, bottom: 84, background: '#FFF449', border: '5px solid #25343F', boxShadow: '8px 8px 0 #25343F', padding: '12px 26px', transform: `rotate(-2deg) scale(${sp(600, { damping: 9 })})`, fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 30, fontWeight: 900, color: '#25343F' }}>
          POLL: 2026 में बदलाव नहीं
        </div>
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(58,26,4,0.52) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene16Flatline;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17CommentCta',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17CommentCta: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: gentle dolly-in
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.1], { extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) });
  const camY = Math.sin(frame * 0.006) * 8;

  // Typewriter effects
  const q1 = 'आपकी EMI कितने साल से SAME है?';
  const q2 = 'ORIGINAL vs CURRENT tenure का फर्क?';
  const t1 = q1.slice(0, Math.floor(Math.max(0, frame - 90) / 2.2));
  const t2 = q2.slice(0, Math.floor(Math.max(0, frame - 260) / 2.2));
  const cursorOn = Math.floor(frame / 14) % 2 === 0;

  const stamp = sp(470, { damping: 8, stiffness: 210 });
  const shake = frame > 470 && frame < 488 ? Math.sin(frame * 3.2) * (488 - frame) * 0.5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(155deg, #E5803711 0%, #E58037 0%, #C66820 52%, #9C4E0E 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 50% ${20 + Math.sin(frame * 0.013) * 5}%, rgba(255,155,81,0.5), transparent 52%)` }} />

      {/* LAYER 2: Animated shape layers — giant floating question marks */}
      {Array.from({ length: 5 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: 120 + i * 380, top: seededRandom(i * 9 + 1) * 700 + Math.sin(frame * 0.015 + i * 2) * 30, fontFamily: 'Yantra One, sans-serif', fontSize: 300 + seededRandom(i) * 140, color: 'rgba(37,52,63,0.06)', transform: `rotate(${(i % 2 ? 1 : -1) * (8 + Math.sin(frame * 0.01 + i) * 4)}deg)`, lineHeight: 1 }}>?</div>
      ))}

      {/* LAYER 3: Motion texture — dotted rows */}
      <AbsoluteFill style={{ backgroundImage: 'radial-gradient(#FFF449 1.6px, transparent 1.6px)', backgroundSize: '46px 46px', backgroundPosition: `${frame * 0.25}px ${frame * 0.12}px`, opacity: 0.07 }} />

      {/* LAYER 4: Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: seededRandom(i * 5 + 4) * 1920 + Math.sin(frame * 0.02 + i) * 18, top: seededRandom(i * 7 + 2) * 1080 + Math.cos(frame * 0.015 + i) * 14, width: 8, height: 8, background: i % 3 === 0 ? '#FFF449' : '#25343F', opacity: 0.14, transform: `rotate(${frame * 0.7 + i * 20}deg)` }} />
      ))}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY + shake}px)` }}>

        {/* Headline */}
        <div style={{ position: 'absolute', top: 66, left: 0, right: 0, textAlign: 'center', transform: `translateY(${(1 - sp(8)) * -50}px)`, opacity: sp(8) }}>
          <span style={{ fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 88, color: '#25343F', textShadow: '5px 5px 0 #FFF449' }}>आपकी STORY क्या है?</span>
        </div>

        {/* Comment box UI mock — brutalist */}
        <div style={{ position: 'absolute', left: '50%', top: 220, width: 1060, transform: `translateX(-50%) rotate(-0.8deg) translateY(${(1 - sp(40)) * 90}px)`, opacity: sp(40) }}>
          <div style={{ background: '#25343F', border: '6px solid #1A252D', padding: '14px 24px', display: 'flex', gap: 12, alignItems: 'center' }}>
            {['#B8341B', '#FFF449', '#FF9B51'].map((c, i) => <div key={i} style={{ width: 18, height: 18, borderRadius: '50%', background: c }} />)}
            <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 24, fontWeight: 700, color: '#FFF8EC', marginLeft: 12, letterSpacing: 2 }}>COMMENT SECTION</span>
          </div>
          {/* comment 1 — typewriter */}
          <div style={{ background: 'rgba(255,248,236,0.96)', borderLeft: '6px solid #1A252D', borderRight: '6px solid #1A252D', padding: '26px 30px', display: 'flex', gap: 20, alignItems: 'flex-start' }}>
            <div style={{ width: 62, height: 62, background: '#FFF449', border: '4px solid #25343F', display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}>
              <svg width="34" height="34" viewBox="0 0 24 24"><circle cx="12" cy="8" r="4.5" fill="#25343F" /><path d="M3.5 21 a8.5 8.5 0 0 1 17 0 Z" fill="#25343F" /></svg>
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 40, fontWeight: 800, color: '#25343F', lineHeight: 1.35, minHeight: 60 }}>
              {t1}{frame > 90 && frame < 280 && cursorOn ? <span style={{ background: '#25343F', color: '#25343F' }}>|</span> : null}
            </div>
          </div>
          {/* comment 2 — typewriter */}
          <div style={{ background: 'rgba(255,248,236,0.96)', border: '6px solid #1A252D', borderTop: '4px dashed #25343F', padding: '26px 30px', display: 'flex', gap: 20, alignItems: 'flex-start', boxShadow: '14px 14px 0 rgba(37,52,63,0.5)' }}>
            <div style={{ width: 62, height: 62, background: '#FF9B51', border: '4px solid #25343F', display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}>
              <svg width="34" height="34" viewBox="0 0 24 24"><circle cx="12" cy="8" r="4.5" fill="#25343F" /><path d="M3.5 21 a8.5 8.5 0 0 1 17 0 Z" fill="#25343F" /></svg>
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 40, fontWeight: 800, color: '#25343F', lineHeight: 1.35, minHeight: 60 }}>
              {t2}{frame > 260 && cursorOn ? <span style={{ background: '#25343F', color: '#25343F' }}>|</span> : null}
            </div>
          </div>
        </div>

        {/* UNDER-REPORTED stamp */}
        <div style={{ position: 'absolute', right: 170, top: 170, transform: `rotate(${9 + shake}deg) scale(${interpolate(stamp, [0, 1], [2.7, 1])})`, opacity: Math.min(1, stamp * 1.5), border: '7px solid #B8341B', color: '#B8341B', padding: '10px 26px', fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 46, letterSpacing: 4, background: 'rgba(255,248,236,0.9)', zIndex: 6 }}>
          UNDER-REPORTED
        </div>

        {/* CTA arrow strip + next-topic teaser */}
        <div style={{ position: 'absolute', bottom: 86, left: 0, right: 0, display: 'flex', justifyContent: 'center', gap: 40, alignItems: 'center' }}>
          <div style={{ background: '#FFF449', border: '6px solid #25343F', boxShadow: '10px 10px 0 #25343F', padding: '16px 36px', transform: `rotate(-1.5deg) scale(${sp(540, { damping: 9 })}) translateY(${Math.sin(frame * 0.09) * 5}px)`, fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 40, fontWeight: 900, color: '#25343F', display: 'flex', alignItems: 'center', gap: 16 }}>
            <svg width="42" height="42" viewBox="0 0 24 24"><path d="M4 4 h16 v12 h-9 l-5 5 v-5 h-2 Z" fill="none" stroke="#25343F" strokeWidth="2.6" strokeLinejoin="round" /></svg>
            COMMENT में बताइए
          </div>
          <div style={{ background: '#25343F', border: '6px solid #1A252D', boxShadow: '10px 10px 0 rgba(26,37,45,0.5)', padding: '16px 36px', transform: `rotate(1.2deg) scale(${sp(600, { damping: 9 })})`, fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 34, fontWeight: 800, color: '#FFF449' }}>
            आगे: BANKS tenure कैसे बढ़ाते हैं →
          </div>
        </div>
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 53%, rgba(58,26,4,0.52) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene17CommentCta;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18BankFlowchart',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18BankFlowchart: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: top-down settle then drift right
  const camScale = interpolate(frame, [0, 90, durationInFrames], [1.12, 1.02, 1.06], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const camX = interpolate(frame, [200, durationInFrames], [0, -18], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Connector lines draw-on
  const drawL = interpolate(frame, [150, 240], [400, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const drawR = interpolate(frame, [190, 280], [400, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // Red X strike on option A
  const cross = sp(360, { damping: 9, stiffness: 190 });
  const glow = 0.5 + Math.sin(frame * 0.12) * 0.5;

  const shake = frame > 500 && frame < 518 ? Math.sin(frame * 3.2) * (518 - frame) * 0.5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(170deg, #E27F34 0%, #C2651E 55%, #98490C 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at ${28 + Math.sin(frame * 0.012) * 6}% 30%, rgba(255,155,81,0.45), transparent 55%)` }} />

      {/* LAYER 2: Animated shape layers */}
      <div style={{ position: 'absolute', width: 560, height: 560, background: 'rgba(255,244,73,0.05)', borderRadius: '50%', right: -190, top: 240, transform: `scale(${1 + Math.sin(frame * 0.017) * 0.04})` }} />
      <div style={{ position: 'absolute', width: 480, height: 480, border: '4px solid rgba(37,52,63,0.1)', left: -160, top: -160, transform: `rotate(${frame * 0.08}deg)` }} />

      {/* LAYER 3: Motion texture — circuit dashes */}
      <AbsoluteFill style={{ backgroundImage: 'repeating-linear-gradient(0deg, rgba(37,52,63,0.06) 0 2px, transparent 2px 40px), repeating-linear-gradient(90deg, rgba(37,52,63,0.04) 0 2px, transparent 2px 64px)', backgroundPosition: `0 ${frame * 0.3}px` }} />

      {/* LAYER 4: Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: seededRandom(i * 4 + 6) * 1920 + Math.sin(frame * 0.019 + i) * 20, top: seededRandom(i * 8 + 3) * 1080 + Math.cos(frame * 0.014 + i) * 16, width: 9, height: 9, background: i % 3 === 0 ? '#FFF449' : '#25343F', opacity: 0.13, transform: `rotate(${frame + i * 25}deg)` }} />
      ))}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX + shake}px)` }}>

        {/* Top trigger node: RATES UP */}
        <div style={{ position: 'absolute', left: '50%', top: 70, transform: `translateX(-50%) scale(${sp(15, { damping: 9 })})`, background: '#B8341B', border: '6px solid #1A252D', boxShadow: '12px 12px 0 rgba(26,37,45,0.5)', padding: '18px 46px', display: 'flex', alignItems: 'center', gap: 18 }}>
          <svg width="46" height="46" viewBox="0 0 24 24"><path d="M4 20 L10 12 L14 15 L20 5 M20 5 h-6 M20 5 v6" fill="none" stroke="#FFF449" strokeWidth="2.8" /></svg>
          <span style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 58, color: '#FFF8EC' }}>RATES बढ़े</span>
        </div>

        {/* EBLR / RLLR chips orbit under trigger */}
        {['EBLR-LINKED', 'RLLR-LINKED', 'REPO → सीधा असर'].map((c, i) => (
          <div key={i} style={{ position: 'absolute', left: 400 + i * 400, top: 218 + Math.sin(frame * 0.06 + i * 2) * 5, background: '#FFF449', border: '4px solid #25343F', boxShadow: '6px 6px 0 #25343F', padding: '8px 20px', fontFamily: 'JetBrains Mono, monospace', fontSize: 24, fontWeight: 800, color: '#25343F', transform: `rotate(${(i - 1) * 2}deg) scale(${sp(90 + i * 25, { damping: 9 })})` }}>{c}</div>
        ))}

        {/* Split connectors */}
        <svg width="1920" height="1080" viewBox="0 0 1920 1080" style={{ position: 'absolute', left: 0, top: 0, pointerEvents: 'none' }}>
          <path d="M 960 290 C 960 360, 560 360, 560 440" fill="none" stroke="#25343F" strokeWidth="9" strokeDasharray="400" strokeDashoffset={drawL} />
          <path d="M 960 290 C 960 360, 1360 360, 1360 440" fill="none" stroke="#25343F" strokeWidth="9" strokeDasharray="400" strokeDashoffset={drawR} />
        </svg>

        {/* OPTION A: EMI बढ़ाओ — rejected */}
        <div style={{ position: 'absolute', left: 200, top: 440, width: 640, background: 'rgba(255,248,236,0.95)', border: '6px solid #25343F', boxShadow: '14px 14px 0 rgba(37,52,63,0.5)', padding: '28px 34px', transform: `rotate(-1.6deg) translateY(${(1 - sp(240)) * 80}px)`, opacity: sp(240) }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 25, fontWeight: 900, color: '#8A4A12', letterSpacing: 3 }}>OPTION A</div>
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 58, fontWeight: 900, color: '#25343F', lineHeight: 1.1 }}>EMI बढ़ाओ</div>
          <div style={{ height: 4, background: '#25343F', margin: '14px 0' }} />
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 29, fontWeight: 700, color: '#25343F', lineHeight: 1.45 }}>
            Customer तुरंत react → COMPLAINT → LOAN TRANSFER
          </div>
          {/* red strike X */}
          <svg width="640" height="290" viewBox="0 0 640 290" style={{ position: 'absolute', left: 0, top: 0, opacity: cross }}>
            <line x1="40" y1="40" x2="600" y2="250" stroke="#B8341B" strokeWidth="16" strokeDasharray={600} strokeDashoffset={(1 - cross) * 600} />
            <line x1="600" y1="40" x2="40" y2="250" stroke="#B8341B" strokeWidth="16" strokeDasharray={600} strokeDashoffset={(1 - cross) * 600} />
          </svg>
        </div>

        {/* OPTION B: TENURE बढ़ाओ — bank's pick */}
        <div style={{ position: 'absolute', right: 200, top: 440, width: 640, background: '#25343F', border: '6px solid #1A252D', boxShadow: `0 0 ${24 * glow}px rgba(255,244,73,0.4), 14px 14px 0 rgba(26,37,45,0.55)`, padding: '28px 34px', transform: `rotate(1.6deg) translateY(${(1 - sp(300)) * 80}px)`, opacity: sp(300) }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 25, fontWeight: 900, color: '#FF9B51', letterSpacing: 3 }}>OPTION B — SILENT MOVE</div>
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 58, fontWeight: 900, color: '#FFF449', lineHeight: 1.1 }}>TENURE बढ़ाओ</div>
          <div style={{ height: 4, background: '#FF9B51', margin: '14px 0' }} />
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 29, fontWeight: 700, color: '#FFF8EC', lineHeight: 1.45 }}>
            EMI same → कोई शोर नहीं → किसी को पता नहीं चलता
          </div>
          {/* animated check */}
          <svg width="70" height="70" viewBox="0 0 24 24" style={{ position: 'absolute', right: 24, top: 24 }}>
            <path d="M4 13 l5 5 L20 6" fill="none" stroke="#FFF449" strokeWidth="3.4" strokeDasharray="40" strokeDashoffset={(1 - sp(380)) * 40} />
          </svg>
        </div>

        {/* Bottom stamp */}
        <div style={{ position: 'absolute', left: '50%', bottom: 84, transform: `translateX(-50%) rotate(${-4 + shake}deg) scale(${interpolate(sp(500, { damping: 8, stiffness: 210 }), [0, 1], [2.6, 1])})`, opacity: Math.min(1, sp(500) * 1.5), border: '8px solid #B8341B', color: '#B8341B', padding: '10px 34px', fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 58, background: 'rgba(255,248,236,0.92)' }}>
          BANK की SMART STRATEGY
        </div>
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 53%, rgba(58,26,4,0.52) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene18BankFlowchart;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19DoublePayback',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19DoublePayback: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow pan left->right across the bars
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1.01], { extrapolateRight: 'clamp' });
  const camX = interpolate(frame, [0, durationInFrames], [24, -24], { extrapolateRight: 'clamp' });

  // Horizontal payback bars
  const rows = [
    { label: 'आपने लिया', amt: '₹50 लाख', w: 420, color: '#25343F', d: 120 },
    { label: '20 साल में लौटाया', amt: '₹95L – ₹1 Cr', w: 850, color: '#8A4A12', d: 220 },
    { label: '25 साल में लौटाया', amt: '₹1.10 Cr+', w: 1010, color: '#B8341B', d: 330 },
  ];

  const burst = sp(470, { damping: 8, stiffness: 210 });
  const shake = frame > 470 && frame < 492 ? Math.sin(frame * 3.1) * (492 - frame) * 0.6 : 0;

  // WIN-WIN strike-through
  const strike = interpolate(frame, [60, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(115deg, #E5813674 0%, #E58136 0%, #C4661F 50%, #9A4A0B 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at 82% ${70 + Math.sin(frame * 0.012) * 6}%, rgba(255,155,81,0.42), transparent 56%)` }} />

      {/* LAYER 2: Animated shape layers — giant rupee ghost */}
      <div style={{ position: 'absolute', right: -60, top: -120, fontFamily: 'Yantra One, sans-serif', fontSize: 900, color: 'rgba(37,52,63,0.05)', transform: `rotate(${8 + Math.sin(frame * 0.008) * 3}deg)`, lineHeight: 1 }}>₹</div>
      <div style={{ position: 'absolute', width: 520, height: 520, borderRadius: '50%', border: '70px solid rgba(255,244,73,0.045)', left: -200, bottom: -220, transform: `rotate(${frame * 0.06}deg)` }} />

      {/* LAYER 3: Motion texture — horizontal speed lines */}
      <AbsoluteFill style={{ backgroundImage: 'repeating-linear-gradient(0deg, rgba(37,52,63,0.055) 0 2px, transparent 2px 54px)', backgroundPosition: `0 ${frame * 0.4}px` }} />

      {/* LAYER 4: Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: ((seededRandom(i * 6 + 1) * 1920 + frame * (0.5 + seededRandom(i))) % 1980) - 30, top: seededRandom(i * 9 + 5) * 1080, width: 8 + seededRandom(i) * 8, height: 8 + seededRandom(i) * 8, background: i % 3 === 0 ? '#FFF449' : '#25343F', opacity: 0.13, transform: `rotate(${frame * 0.8 + i * 15}deg)` }} />
      ))}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX + shake}px)` }}>

        {/* WIN-WIN? headline with strike + correction */}
        <div style={{ position: 'absolute', top: 62, left: 90, display: 'flex', alignItems: 'baseline', gap: 34 }}>
          <div style={{ position: 'relative', fontFamily: 'Yantra One, sans-serif', fontSize: 96, color: '#FFF8EC', opacity: sp(10), transform: `translateY(${(1 - sp(10)) * -50}px)` }}>
            WIN-WIN?
            <div style={{ position: 'absolute', left: 0, top: '52%', height: 12, background: '#B8341B', width: `${strike * 104}%` }} />
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 52, fontWeight: 900, color: '#FFF449', textShadow: '4px 4px 0 #25343F', opacity: sp(110), transform: `scale(${sp(110, { damping: 9 })}) rotate(-1.5deg)` }}>
            सिर्फ BANK के लिए
          </div>
        </div>

        {/* Payback bars — horizontal brutalist gauge */}
        <div style={{ position: 'absolute', left: 90, top: 260, width: 1500 }}>
          {rows.map((r, i) => {
            const g = sp(r.d, { damping: 11 });
            return (
              <div key={i} style={{ marginBottom: 52, opacity: Math.min(1, sp(r.d - 20) * 1.2) }}>
                <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 32, fontWeight: 800, color: '#25343F', marginBottom: 10 }}>{r.label}</div>
                <div style={{ position: 'relative', height: 86 }}>
                  <div style={{ position: 'absolute', left: 0, top: 0, height: '100%', width: r.w * g, background: r.color, border: '5px solid #1A252D', boxShadow: '10px 10px 0 rgba(26,37,45,0.4)' }}>
                    {/* moving hatch micro-animation inside bar */}
                    <div style={{ position: 'absolute', inset: 0, backgroundImage: 'repeating-linear-gradient(45deg, rgba(255,248,236,0.12) 0 14px, transparent 14px 28px)', backgroundPosition: `${frame * 0.8}px 0` }} />
                  </div>
                  <div style={{ position: 'absolute', left: r.w * g + 26, top: 12, fontFamily: 'JetBrains Mono, monospace', fontSize: 47, fontWeight: 800, color: r.color, whiteSpace: 'nowrap' }}>{r.amt}</div>
                </div>
              </div>
            );
          })}
          {/* extra-5-years bracket on last bar */}
          <svg width="1500" height="60" viewBox="0 0 1500 60" style={{ marginTop: -30, opacity: sp(420) }}>
            <path d="M 852 8 L 852 34 L 1008 34 L 1008 8" fill="none" stroke="#FFF449" strokeWidth="7" />
            <text x="930" y="58" textAnchor="middle" fontFamily="Inter, sans-serif" fontSize="26" fontWeight="900" fill="#FFF449">EXTRA 5 साल</text>
          </svg>
        </div>

        {/* Cost burst — starburst polygon */}
        <div style={{ position: 'absolute', right: 120, bottom: 130, transform: `rotate(${6 + shake}deg) scale(${interpolate(burst, [0, 1], [0.2, 1])})`, opacity: Math.min(1, burst * 1.4) }}>
          <svg width="470" height="470" viewBox="0 0 200 200">
            <polygon
              points={Array.from({ length: 24 }).map((_, i) => {
                const a = (i * Math.PI) / 12;
                const r = i % 2 === 0 ? 98 : 74;
                return `${100 + Math.cos(a) * r},${100 + Math.sin(a) * r}`;
              }).join(' ')}
              fill="#FFF449" stroke="#25343F" strokeWidth="4"
              transform={`rotate(${frame * 0.35} 100 100)`}
            />
          </svg>
          <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', textAlign: 'center' }}>
            <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 62, fontWeight: 800, color: '#B8341B', lineHeight: 1 }}>₹10–15</span>
            <span style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 40, fontWeight: 900, color: '#25343F' }}>लाख EXTRA</span>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 800, color: '#25343F', letterSpacing: 2 }}>COST OF 5 YRS</span>
          </div>
        </div>

        {/* micro chip */}
        <div style={{ position: 'absolute', left: 96, bottom: 90, background: '#25343F', border: '5px solid #1A252D', boxShadow: '8px 8px 0 rgba(26,37,45,0.5)', padding: '12px 26px', transform: `rotate(-1.6deg) scale(${sp(560, { damping: 9 })}) translateY(${Math.sin(frame * 0.06) * 3}px)`, fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 30, fontWeight: 800, color: '#FFF449' }}>
          DOUBLE PAYBACK — यही 20-year loan की सच्चाई
        </div>
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 53%, rgba(58,26,4,0.52) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene19DoublePayback;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20HideAndShow',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20HideAndShow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: creeping zoom toward the hidden panel reveal
  const camScale = interpolate(frame, [0, 380, durationInFrames], [1.02, 1.05, 1.1], { extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) });

  // HIDDEN cover slides off at ~frame 380 (the reveal)
  const reveal = spring({ frame: Math.max(0, frame - 380), fps, config: { damping: 14, mass: 0.6, stiffness: 90 } });
  const coverX = interpolate(reveal, [0, 1], [0, 560]);
  const revealShake = frame > 380 && frame < 402 ? Math.sin(frame * 3) * (402 - frame) * 0.55 : 0;

  // Spotlight flicker on EMI panel
  const spot = 0.65 + Math.sin(frame * 0.09) * 0.12;

  const stamp = sp(560, { damping: 8, stiffness: 210 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9762B', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* LAYER 1: Dynamic gradient background — darkest of the chapter, stage mood */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #D0701F 0%, #A85614 55%, #7E3D06 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 28% 40%, rgba(255,155,81,${spot * 0.5}), transparent 42%)` }} />

      {/* LAYER 2: Animated shape layers — torn collage strips */}
      <div style={{ position: 'absolute', width: 1200, height: 130, background: 'rgba(255,244,73,0.05)', left: -100, top: 200, transform: `rotate(-7deg) translateX(${Math.sin(frame * 0.01) * 20}px)` }} />
      <div style={{ position: 'absolute', width: 1100, height: 110, background: 'rgba(37,52,63,0.07)', right: -120, bottom: 240, transform: `rotate(5deg) translateX(${Math.cos(frame * 0.012) * 18}px)` }} />

      {/* LAYER 3: Motion texture — coarse halftone */}
      <AbsoluteFill style={{ backgroundImage: 'radial-gradient(#25343F 1.7px, transparent 1.7px)', backgroundSize: '24px 24px', backgroundPosition: `${frame * 0.2}px ${-frame * 0.15}px`, opacity: 0.08 }} />

      {/* LAYER 4: Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: seededRandom(i * 7 + 4) * 1920 + Math.sin(frame * 0.018 + i) * 22, top: seededRandom(i * 3 + 9) * 1080 + Math.cos(frame * 0.014 + i * 1.6) * 18, width: 7 + seededRandom(i + 2) * 9, height: 7 + seededRandom(i + 2) * 9, background: i % 4 === 0 ? '#FFF449' : '#25343F', opacity: 0.13, transform: `rotate(${frame * 0.5 + i * 33}deg)` }} />
      ))}

      {/* LAYER 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${revealShake}px)` }}>

        {/* Headline: दिखाओ vs छुपाओ */}
        <div style={{ position: 'absolute', top: 60, left: 0, right: 0, display: 'flex', justifyContent: 'center', gap: 30, alignItems: 'baseline' }}>
          <span style={{ fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 100, color: '#FFF449', textShadow: '6px 6px 0 #25343F', transform: `translateX(${(1 - sp(10)) * -250}px)`, opacity: sp(10), display: 'inline-block' }}>दिखाओ</span>
          <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 56, fontWeight: 900, color: '#FFF8EC', opacity: sp(40) }}>vs</span>
          <span style={{ fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 100, color: '#25343F', textShadow: '6px 6px 0 rgba(255,244,73,0.5)', transform: `translateX(${(1 - sp(30)) * 250}px)`, opacity: sp(30), display: 'inline-block' }}>छुपाओ</span>
        </div>

        {/* LEFT panel: SHOWN — Monthly EMI under spotlight */}
        <div style={{ position: 'absolute', left: 150, top: 280, width: 640, background: 'rgba(255,248,236,0.97)', border: '6px solid #25343F', boxShadow: `0 0 ${44 * spot}px rgba(255,244,73,0.5), 14px 14px 0 rgba(37,52,63,0.5)`, padding: '30px 38px', transform: `rotate(-1.6deg) translateY(${(1 - sp(90)) * 90}px)`, opacity: sp(90) }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
            <svg width="40" height="40" viewBox="0 0 24 24"><circle cx="12" cy="12" r="5" fill="#25343F" /><path d="M12 2 v3 M12 19 v3 M2 12 h3 M19 12 h3 M4.9 4.9 l2.1 2.1 M17 17 l2.1 2.1 M19.1 4.9 L17 7 M7 17 l-2.1 2.1" stroke="#25343F" strokeWidth="2.2" /></svg>
            <span style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 26, fontWeight: 900, color: '#25343F', letterSpacing: 3 }}>BANK दिखाता है</span>
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 38, fontWeight: 800, color: '#8A4A12', marginTop: 16 }}>MONTHLY EMI</div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 100, fontWeight: 800, color: '#25343F', lineHeight: 1.05, transform: `scale(${1 + Math.sin(frame * 0.08) * 0.01})`, transformOrigin: 'left center' }}>₹43,000</div>
          <div style={{ background: '#FFF449', border: '4px solid #25343F', display: 'inline-block', padding: '6px 18px', marginTop: 14, fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 26, fontWeight: 800, color: '#25343F', transform: 'rotate(-1deg)' }}>
            &ldquo;बस इतना ही तो है&rdquo;
          </div>
        </div>

        {/* RIGHT panel: HIDDEN — Total interest behind sliding cover */}
        <div style={{ position: 'absolute', right: 150, top: 280, width: 640, height: 372, transform: `rotate(1.4deg) translateY(${(1 - sp(150)) * 90}px)`, opacity: sp(150) }}>
          <div style={{ position: 'absolute', inset: 0, background: '#25343F', border: '6px solid #1A252D', boxShadow: '14px 14px 0 rgba(26,37,45,0.55)', padding: '30px 38px', overflow: 'hidden' }}>
            <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 26, fontWeight: 900, color: '#FF9B51', letterSpacing: 3 }}>BANK छुपाता है</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 38, fontWeight: 800, color: '#FFF8EC', marginTop: 16 }}>TOTAL INTEREST</div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 92, fontWeight: 800, color: '#FFF449', lineHeight: 1.05, textShadow: `0 0 ${12 + Math.sin(frame * 0.2) * 6}px rgba(255,244,73,0.5)` }}>₹45 लाख+</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 26, fontWeight: 700, color: '#FFF8EC', marginTop: 12 }}>Longer tenure = EMI कम, interest बहुत ज़्यादा</div>
            {/* sliding HIDDEN cover */}
            <div style={{ position: 'absolute', inset: -6, background: '#B8341B', border: '6px solid #1A252D', display: 'flex', alignItems: 'center', justifyContent: 'center', transform: `translateX(${coverX}px) rotate(${reveal * 4}deg)`, backgroundImage: 'repeating-linear-gradient(45deg, rgba(26,37,45,0.25) 0 30px, transparent 30px 60px)' }}>
              <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 72, fontWeight: 900, color: '#FFF8EC', letterSpacing: 10 }}>HIDDEN</span>
            </div>
          </div>
        </div>

        {/* Credit-card parallel strip */}
        <div style={{ position: 'absolute', left: '50%', bottom: 170, width: 1240, transform: `translateX(-50%) rotate(-0.6deg) translateY(${(1 - sp(460)) * 70}px)`, opacity: sp(460), background: 'rgba(255,248,236,0.95)', border: '6px solid #25343F', boxShadow: '12px 12px 0 rgba(37,52,63,0.5)', padding: '18px 34px', display: 'flex', alignItems: 'center', gap: 22 }}>
          <svg width="52" height="52" viewBox="0 0 24 24"><rect x="2" y="5" width="20" height="14" rx="2" fill="none" stroke="#25343F" strokeWidth="2.4" /><rect x="2" y="8.4" width="20" height="3.4" fill="#25343F" /><rect x="5" y="14.6" width="6" height="2.2" fill="#B8341B" /></svg>
          <span style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 33, fontWeight: 800, color: '#25343F', lineHeight: 1.35 }}>
            CREDIT CARD वाली trick — <span style={{ background: '#FFF449', padding: '0 8px' }}>MINIMUM PAYMENT दिखाओ</span>, OUTSTANDING छुपाओ
          </span>
        </div>

        {/* SAME GAME stamp */}
        <div style={{ position: 'absolute', right: 190, bottom: 70, transform: `rotate(${-8 + revealShake}deg) scale(${interpolate(stamp, [0, 1], [2.6, 1])})`, opacity: Math.min(1, stamp * 1.5), border: '8px solid #B8341B', color: '#B8341B', padding: '8px 30px', fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 56, letterSpacing: 6, background: 'rgba(255,248,236,0.92)' }}>
          SAME GAME
        </div>
      </AbsoluteFill>

      {/* LAYER 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(46,20,2,0.58) 100%)', pointerEvents: 'none' }} />

      {/* LAYER 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 22, fontWeight: 600, color: '#25343F', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene20HideAndShow;
```

