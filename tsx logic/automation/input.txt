```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11SilverLining',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11SilverLining: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1.12], { extrapolateRight: 'clamp' });
  const titleIn = sp(10);
  const q1 = sp(45);
  const q2 = sp(70);
  const warnIn = sp(110);
  const warnShake = frame > 110 && frame < 130 ? Math.sin(frame * 1.5) * 4 : 0;
  const gradShift = Math.sin(frame * 0.02) * 30;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at ${50 + gradShift}% 30%, rgba(147,181,198,0.35), rgba(244,206,20,0) 60%)` }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: -200 + Math.sin(frame * 0.03) * 40, left: -150, width: 700, height: 700, borderRadius: '50%', border: '3px solid rgba(0,0,0,0.06)', transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.04) * 0.05})` }} />
        <div style={{ position: 'absolute', bottom: -250, right: -100, width: 600, height: 600, background: 'rgba(147,181,198,0.07)', transform: `rotate(${-frame * 0.15}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture - dots grid */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 60 }).map((_, i) => {
          const r = seededRandom(i + 1);
          return <div key={i} style={{ position: 'absolute', top: `${r * 100}%`, left: `${seededRandom(i + 40) * 100}%`, width: 6, height: 6, borderRadius: '50%', background: '#000000', transform: `translateY(${Math.sin(frame * 0.05 + i) * 10}px)` }} />;
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => {
          const r = seededRandom(i + 100);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 + frame * 0.1) % 100}%`, left: `${seededRandom(i + 200) * 100}%`, width: 4 + r * 6, height: 4 + r * 6, borderRadius: '50%', background: 'rgba(0,0,0,0.12)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, justifyContent: 'center', alignItems: 'center', padding: 100 }}>
        <div style={{ opacity: titleIn, transform: `translateY(${(1 - titleIn) * 40}px)`, fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 64, color: '#000000', letterSpacing: 2, marginBottom: 20 }}>
          THE SILVER LINING
        </div>
        <div style={{ width: 200 * titleIn, height: 6, background: '#93B5C6', marginBottom: 50 }} />

        <div style={{ opacity: q1, transform: `translateX(${(1 - q1) * -60}px)`, fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 76, color: '#000000', textAlign: 'center', lineHeight: 1.2, marginBottom: 28 }}>
          When do you <span style={{ color: '#93B5C6' }}>claim?</span>
        </div>
        <div style={{ opacity: q2, transform: `translateX(${(1 - q2) * 60}px)`, fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 76, color: '#000000', textAlign: 'center', lineHeight: 1.2 }}>
          What about <span style={{ color: '#93B5C6' }}>your own?</span>
        </div>

        <div style={{ opacity: warnIn, transform: `translateY(${(1 - warnIn) * 30}px) translateX(${warnShake}px)`, marginTop: 60, display: 'flex', alignItems: 'center', gap: 18, background: 'rgba(0,0,0,0.06)', padding: '22px 40px', borderRadius: 14, border: '2px solid rgba(0,0,0,0.2)' }}>
          <svg width="50" height="50" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="2.2">
            <path d="M12 9v4M12 17h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z" />
          </svg>
          <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 600, fontSize: 44, color: '#000000' }}>Most people make a devastating mistake</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.35)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene11SilverLining;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12FirstStep',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12FirstStep: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-30, 30], { extrapolateRight: 'clamp' });
  const badgeIn = sp(8, { stiffness: 90 });
  const titleIn = sp(30);
  const age62In = sp(70);
  const age60In = sp(105);
  const arrowGrow = interpolate(frame, [105, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, rgba(147,181,198,0.3) 0%, rgba(244,206,20,0) 55%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: '15%', right: '8%', width: 0, height: 0, borderLeft: '180px solid transparent', borderRight: '180px solid transparent', borderBottom: '320px solid rgba(0,0,0,0.05)', transform: `rotate(${frame * 0.2}deg)` }} />
        <div style={{ position: 'absolute', bottom: '-15%', left: '-5%', width: 500, height: 500, borderRadius: '50%', background: 'rgba(147,181,198,0.08)', transform: `scale(${1 + Math.sin(frame * 0.05) * 0.06})` }} />
      </AbsoluteFill>

      {/* Motion texture - horizontal lines */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', top: `${i * 5}%`, left: 0, width: '100%', height: 2, background: '#000000', transform: `translateX(${Math.sin(frame * 0.03 + i) * 20}px)` }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 12 }).map((_, i) => {
          const r = seededRandom(i + 300);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 - frame * 0.08) % 100}%`, left: `${seededRandom(i + 400) * 100}%`, width: 5, height: 5, borderRadius: '50%', background: 'rgba(0,0,0,0.15)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)`, justifyContent: 'center', alignItems: 'center', padding: 90 }}>
        <div style={{ opacity: badgeIn, transform: `scale(${badgeIn})`, background: '#000000', color: '#F4CE14', fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 38, padding: '14px 36px', borderRadius: 50, letterSpacing: 3, marginBottom: 30 }}>
          STEP 01
        </div>
        <div style={{ opacity: titleIn, transform: `translateY(${(1 - titleIn) * 40}px)`, fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 96, color: '#000000', textAlign: 'center', lineHeight: 1.05, marginBottom: 70 }}>
          THE WIDOW'S<br /><span style={{ color: '#93B5C6' }}>AGE 60</span> LOOPHOLE
        </div>

        <div style={{ display: 'flex', alignItems: 'center', gap: 50 }}>
          <div style={{ opacity: age62In, transform: `scale(${age62In})`, textAlign: 'center', background: 'rgba(0,0,0,0.07)', padding: '30px 44px', borderRadius: 18, border: '2px dashed rgba(0,0,0,0.3)' }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 600, fontSize: 32, color: '#000000', opacity: 0.7 }}>EVERYONE ELSE</div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 110, color: '#000000' }}>62</div>
          </div>

          <svg width="120" height="60" viewBox="0 0 120 60" style={{ opacity: arrowGrow }}>
            <line x1="5" y1="30" x2={5 + 100 * arrowGrow} y2="30" stroke="#000000" strokeWidth="5" />
            <polygon points={`${105 * arrowGrow},20 ${115 * arrowGrow},30 ${105 * arrowGrow},40`} fill="#000000" />
          </svg>

          <div style={{ opacity: age60In, transform: `scale(${age60In})`, textAlign: 'center', background: '#93B5C6', padding: '30px 44px', borderRadius: 18, boxShadow: '0 12px 0 rgba(0,0,0,0.25)' }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 700, fontSize: 32, color: '#000000' }}>WIDOWS GET</div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 110, color: '#000000' }}>60</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.35)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene12FirstStep;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13TwoYears',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13TwoYears: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.1], { extrapolateRight: 'clamp' });
  const lockIn = sp(10, { stiffness: 100 });
  const textIn = sp(35);
  const bonusIn = sp(75);
  const numCount = Math.floor(interpolate(frame, [75, 110], [0, 2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at 70% 60%, rgba(147,181,198,0.4), rgba(244,206,20,0) 55%)` }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: '20%', left: '5%', width: 400, height: 400, border: '4px solid rgba(0,0,0,0.06)', borderRadius: 40, transform: `rotate(${frame * 0.25}deg)` }} />
        <div style={{ position: 'absolute', bottom: '5%', right: '10%', width: 300, height: 300, borderRadius: '50%', background: 'rgba(0,0,0,0.04)', transform: `scale(${1 + Math.sin(frame * 0.06) * 0.08})` }} />
      </AbsoluteFill>

      {/* Motion texture - concentric circles */}
      <AbsoluteFill style={{ opacity: 0.07, justifyContent: 'center', alignItems: 'center' }}>
        {Array.from({ length: 6 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', width: 200 + i * 180, height: 200 + i * 180, borderRadius: '50%', border: '2px solid #000000', transform: `scale(${1 + Math.sin(frame * 0.04 + i * 0.5) * 0.03})` }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 16 }).map((_, i) => {
          const r = seededRandom(i + 500);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 + frame * 0.12) % 100}%`, left: `${seededRandom(i + 600) * 100}%`, width: 4 + r * 5, height: 4 + r * 5, borderRadius: '50%', background: 'rgba(0,0,0,0.13)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, justifyContent: 'center', alignItems: 'center', padding: 100 }}>
        <div style={{ opacity: lockIn, transform: `scale(${lockIn}) rotate(${(1 - lockIn) * -20}deg)`, marginBottom: 30 }}>
          <svg width="110" height="110" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="1.8">
            <rect x="3" y="11" width="18" height="11" rx="2" fill="#93B5C6" />
            <path d="M7 11V7a5 5 0 0110 0v4" />
            <circle cx="12" cy="16" r="1.5" fill="#000000" />
          </svg>
        </div>

        <div style={{ opacity: textIn, transform: `translateY(${(1 - textIn) * 35}px)`, fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 70, color: '#000000', textAlign: 'center', lineHeight: 1.2, marginBottom: 50, maxWidth: 1400 }}>
          Legally allowed to claim a Survivor Benefit as early as <span style={{ color: '#93B5C6', fontWeight: 900 }}>age 60</span>
        </div>

        <div style={{ opacity: bonusIn, transform: `scale(${bonusIn})`, display: 'flex', alignItems: 'baseline', gap: 24, background: '#000000', padding: '26px 56px', borderRadius: 16 }}>
          <span style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 130, color: '#F4CE14', lineHeight: 1 }}>{numCount}</span>
          <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 48, color: '#F5F5F5' }}>YEARS EARLY<br />than everyone else</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.35)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene13TwoYears;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14ThePenalty',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14ThePenalty: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const catchIn = sp(8);
  const barFill = interpolate(frame, [45, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const pctCount = interpolate(frame, [45, 100], [0, 28.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const impactShake = frame > 95 && frame < 115 ? Math.sin(frame * 2) * 5 : 0;
  const labelIn = sp(110);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `translateX(${impactShake}px)` }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, rgba(0,0,0,0.08) 0%, rgba(244,206,20,0) 40%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: '-10%', right: '-5%', width: 550, height: 550, borderRadius: '50%', border: '5px solid rgba(0,0,0,0.05)', transform: `rotate(${-frame * 0.18}deg) scale(${1 + Math.sin(frame * 0.05) * 0.05})` }} />
        <div style={{ position: 'absolute', bottom: '-20%', left: '8%', width: 0, height: 0, borderLeft: '160px solid transparent', borderRight: '160px solid transparent', borderTop: '280px solid rgba(147,181,198,0.1)', transform: `rotate(${frame * 0.15}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture - diagonal grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', top: 0, left: `${i * 7}%`, width: 2, height: '100%', background: '#000000', transform: `skewX(-15deg) translateY(${Math.sin(frame * 0.03 + i) * 15}px)` }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 12 }).map((_, i) => {
          const r = seededRandom(i + 700);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 - frame * 0.1) % 100}%`, left: `${seededRandom(i + 800) * 100}%`, width: 5, height: 5, borderRadius: '50%', background: 'rgba(0,0,0,0.13)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', padding: 100 }}>
        <div style={{ opacity: catchIn, transform: `scale(${catchIn})`, display: 'flex', alignItems: 'center', gap: 16, marginBottom: 50 }}>
          <svg width="60" height="60" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="2.2">
            <circle cx="12" cy="12" r="10" fill="#93B5C6" />
            <path d="M12 8v4M12 16h.01" stroke="#000000" />
          </svg>
          <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 64, color: '#000000', letterSpacing: 2 }}>BUT THERE'S A CATCH</span>
        </div>

        {/* Penalty bar graphic */}
        <div style={{ width: 1100, height: 90, background: 'rgba(0,0,0,0.1)', borderRadius: 12, overflow: 'hidden', position: 'relative', border: '3px solid #000000' }}>
          <div style={{ width: `${barFill * 28.5}%`, height: '100%', background: '#000000', display: 'flex', alignItems: 'center', justifyContent: 'flex-end', paddingRight: 20 }}>
            <span style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 38, color: '#F4CE14' }}>CUT</span>
          </div>
        </div>

        <div style={{ marginTop: 50, display: 'flex', alignItems: 'baseline', gap: 16 }}>
          <span style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 200, color: '#000000', lineHeight: 1 }}>−{pctCount.toFixed(1)}%</span>
        </div>

        <div style={{ opacity: labelIn, transform: `translateY(${(1 - labelIn) * 25}px)`, marginTop: 30, fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 50, color: '#000000', background: '#93B5C6', padding: '14px 40px', borderRadius: 10 }}>
          PERMANENT penalty on your monthly check
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.4)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene14ThePenalty;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15Lifeline',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15Lifeline: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.08, 1.0], { extrapolateRight: 'clamp' });
  const lifelineIn = sp(10);
  const card1 = sp(50);
  const card2 = sp(85);
  const ringDash = interpolate(frame, [10, 80], [880, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at 30% 50%, rgba(147,181,198,0.4), rgba(244,206,20,0) 60%)` }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: '10%', right: '10%', width: 450, height: 450, borderRadius: '50%', background: 'rgba(0,0,0,0.04)', transform: `scale(${1 + Math.sin(frame * 0.05) * 0.07})` }} />
        <div style={{ position: 'absolute', bottom: '-15%', left: '-8%', width: 500, height: 500, border: '4px solid rgba(0,0,0,0.06)', borderRadius: 60, transform: `rotate(${frame * 0.2}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture - dot matrix */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 48 }).map((_, i) => {
          const r = seededRandom(i + 900);
          return <div key={i} style={{ position: 'absolute', top: `${r * 100}%`, left: `${seededRandom(i + 950) * 100}%`, width: 7, height: 7, background: '#000000', transform: `rotate(45deg) translateY(${Math.sin(frame * 0.04 + i) * 8}px)` }} />;
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => {
          const r = seededRandom(i + 1000);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 + frame * 0.09) % 100}%`, left: `${seededRandom(i + 1100) * 100}%`, width: 4 + r * 5, height: 4 + r * 5, borderRadius: '50%', background: 'rgba(0,0,0,0.12)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, justifyContent: 'center', alignItems: 'center', padding: 90 }}>
        <div style={{ opacity: lifelineIn, transform: `translateY(${(1 - lifelineIn) * 30}px)`, position: 'relative', marginBottom: 40 }}>
          <svg width="180" height="180" viewBox="0 0 180 180">
            <circle cx="90" cy="90" r="70" fill="none" stroke="rgba(0,0,0,0.12)" strokeWidth="14" />
            <circle cx="90" cy="90" r="70" fill="none" stroke="#000000" strokeWidth="14" strokeLinecap="round" strokeDasharray="440" strokeDashoffset={ringDash / 2} transform="rotate(-90 90 90)" />
            <g transform="translate(90 90)">
              <path d="M-30 0 L-15 0 L-8 -20 L5 25 L14 -10 L20 0 L30 0" fill="none" stroke="#000000" strokeWidth="6" strokeLinecap="round" strokeLinejoin="round" />
            </g>
          </svg>
        </div>

        <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 80, color: '#000000', textAlign: 'center', marginBottom: 50, opacity: lifelineIn }}>
          A CRITICAL <span style={{ color: '#93B5C6' }}>LIFELINE</span>
        </div>

        <div style={{ display: 'flex', gap: 40 }}>
          <div style={{ opacity: card1, transform: `translateY(${(1 - card1) * 40}px)`, width: 560, background: '#000000', padding: '36px 40px', borderRadius: 18 }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 40, color: '#F4CE14', marginBottom: 12 }}>IF IN CRISIS</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 600, fontSize: 42, color: '#F5F5F5', lineHeight: 1.25 }}>No income, facing eviction? Take it now to stay afloat.</div>
          </div>
          <div style={{ opacity: card2, transform: `translateY(${(1 - card2) * 40}px)`, width: 560, background: '#93B5C6', padding: '36px 40px', borderRadius: 18 }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 40, color: '#000000', marginBottom: 12 }}>IF YOU CAN WAIT</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 600, fontSize: 42, color: '#000000', lineHeight: 1.25 }}>Look at Step 2 — claiming at 60 locks the penalty FOREVER.</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.35)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene15Lifeline;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16StaggeredIntro',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16StaggeredIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [20, -20], { extrapolateRight: 'clamp' });
  const badgeIn = sp(8, { stiffness: 90 });
  const titleIn = sp(30);
  const powerIn = sp(75);
  const deemedIn = sp(115);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(225deg, rgba(147,181,198,0.35) 0%, rgba(244,206,20,0) 50%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: '12%', left: '6%', width: 380, height: 380, border: '4px solid rgba(0,0,0,0.05)', transform: `rotate(${frame * 0.3}deg)` }} />
        <div style={{ position: 'absolute', bottom: '-10%', right: '-5%', width: 480, height: 480, borderRadius: '50%', background: 'rgba(147,181,198,0.09)', transform: `scale(${1 + Math.sin(frame * 0.05) * 0.06})` }} />
      </AbsoluteFill>

      {/* Motion texture - vertical bars */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', bottom: 0, left: `${i * 4.2}%`, width: 3, height: `${40 + Math.sin(frame * 0.05 + i) * 30}%`, background: '#000000' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 13 }).map((_, i) => {
          const r = seededRandom(i + 1200);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 + frame * 0.1) % 100}%`, left: `${seededRandom(i + 1300) * 100}%`, width: 5, height: 5, borderRadius: '50%', background: 'rgba(0,0,0,0.14)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)`, justifyContent: 'center', alignItems: 'center', padding: 90 }}>
        <div style={{ opacity: badgeIn, transform: `scale(${badgeIn})`, background: '#000000', color: '#F4CE14', fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 38, padding: '14px 36px', borderRadius: 50, letterSpacing: 3, marginBottom: 30 }}>
          STEP 02 — THE BIG ONE
        </div>

        <div style={{ opacity: titleIn, transform: `translateY(${(1 - titleIn) * 40}px)`, fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 100, color: '#000000', textAlign: 'center', lineHeight: 1.05, marginBottom: 40 }}>
          THE <span style={{ color: '#93B5C6' }}>STAGGERED</span><br />CLAIMING LOOPHOLE
        </div>

        <div style={{ opacity: powerIn, transform: `scale(${powerIn})`, fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 50, color: '#000000', marginBottom: 50 }}>
          The single most powerful tool a widow has
        </div>

        <div style={{ opacity: deemedIn, transform: `translateY(${(1 - deemedIn) * 30}px)`, display: 'flex', alignItems: 'center', gap: 20, background: 'rgba(0,0,0,0.08)', padding: '24px 44px', borderRadius: 14, border: '2px solid rgba(0,0,0,0.25)' }}>
          <svg width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="2">
            <rect x="3" y="3" width="18" height="18" rx="2" fill="#93B5C6" />
            <path d="M9 12l2 2 4-4" stroke="#000000" />
          </svg>
          <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 600, fontSize: 46, color: '#000000' }}>When married: SSA forces "<span style={{ fontWeight: 800 }}>deemed filing</span>"</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.35)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene16StaggeredIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17SeparateBenefits',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17SeparateBenefits: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const lockedIn = sp(10);
  const splitProgress = interpolate(frame, [80, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const secretIn = sp(75);
  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.08], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at 50% 80%, rgba(147,181,198,0.35), rgba(244,206,20,0) 55%)` }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: '-12%', left: '12%', width: 420, height: 420, borderRadius: '50%', border: '4px solid rgba(0,0,0,0.06)', transform: `rotate(${-frame * 0.2}deg)` }} />
        <div style={{ position: 'absolute', bottom: '8%', right: '6%', width: 0, height: 0, borderLeft: '150px solid transparent', borderRight: '150px solid transparent', borderBottom: '260px solid rgba(147,181,198,0.09)', transform: `rotate(${frame * 0.15}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture - cross hatch */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', top: `${i * 7.5}%`, left: 0, width: '100%', height: 2, background: '#000000', transform: `translateX(${Math.cos(frame * 0.03 + i) * 25}px)` }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => {
          const r = seededRandom(i + 1400);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 - frame * 0.08) % 100}%`, left: `${seededRandom(i + 1500) * 100}%`, width: 4 + r * 5, height: 4 + r * 5, borderRadius: '50%', background: 'rgba(0,0,0,0.13)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, justifyContent: 'center', alignItems: 'center', padding: 90 }}>
        <div style={{ opacity: lockedIn, transform: `translateY(${(1 - lockedIn) * 30}px)`, fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 56, color: '#000000', marginBottom: 24 }}>
          MARRIED: forced to take <span style={{ color: '#93B5C6' }}>BOTH</span> together
        </div>

        {/* Two benefit blocks splitting apart */}
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: 50, position: 'relative', height: 200 }}>
          <div style={{ transform: `translateX(${-splitProgress * 220}px)`, background: '#000000', width: 360, height: 170, borderRadius: 16, display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 36, color: '#F4CE14' }}>YOUR OWN</div>
            <div style={{ fontFamily: 'Inter', fontWeight: 600, fontSize: 30, color: '#F5F5F5' }}>Personal Benefit</div>
          </div>
          <div style={{ width: 5, height: 120, background: '#000000', opacity: 1 - splitProgress }} />
          <div style={{ transform: `translateX(${splitProgress * 220}px)`, background: '#93B5C6', width: 360, height: 170, borderRadius: 16, display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 36, color: '#000000' }}>SURVIVOR</div>
            <div style={{ fontFamily: 'Inter', fontWeight: 600, fontSize: 30, color: '#000000' }}>Benefit</div>
          </div>
        </div>

        <div style={{ opacity: secretIn, transform: `scale(${secretIn})`, fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 64, color: '#000000', textAlign: 'center', background: 'rgba(0,0,0,0.07)', padding: '20px 50px', borderRadius: 14 }}>
          AFTER LOSS: you can <span style={{ color: '#93B5C6' }}>SEPARATE</span> &amp; stagger them
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.35)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene17SeparateBenefits;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18HowItWorks',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18HowItWorks: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-25, 25], { extrapolateRight: 'clamp' });
  const headerIn = sp(8);
  const ageIn = sp(35);
  const opt1 = sp(75);
  const opt2 = sp(110);
  const restrictIn = sp(150);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, rgba(147,181,198,0.3) 0%, rgba(244,206,20,0) 50%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: '8%', right: '8%', width: 350, height: 350, border: '4px solid rgba(0,0,0,0.05)', borderRadius: 50, transform: `rotate(${frame * 0.22}deg) scale(${1 + Math.sin(frame * 0.05) * 0.05})` }} />
        <div style={{ position: 'absolute', bottom: '-18%', left: '5%', width: 450, height: 450, borderRadius: '50%', background: 'rgba(0,0,0,0.04)' }} />
      </AbsoluteFill>

      {/* Motion texture - small squares */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 40 }).map((_, i) => {
          const r = seededRandom(i + 1600);
          return <div key={i} style={{ position: 'absolute', top: `${r * 100}%`, left: `${seededRandom(i + 1700) * 100}%`, width: 10, height: 10, background: '#000000', transform: `rotate(${frame * 0.5 + i * 10}deg)` }} />;
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 12 }).map((_, i) => {
          const r = seededRandom(i + 1800);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 + frame * 0.1) % 100}%`, left: `${seededRandom(i + 1900) * 100}%`, width: 5, height: 5, borderRadius: '50%', background: 'rgba(0,0,0,0.13)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)`, justifyContent: 'center', alignItems: 'center', padding: 80 }}>
        <div style={{ opacity: headerIn, transform: `translateY(${(1 - headerIn) * 30}px)`, fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 56, color: '#000000', marginBottom: 14 }}>
          HOW IT WORKS — at full retirement age
        </div>
        <div style={{ opacity: ageIn, transform: `scale(${ageIn})`, fontFamily: 'Inter', fontWeight: 900, fontSize: 130, color: '#93B5C6', lineHeight: 1, marginBottom: 40, textShadow: '0 6px 0 rgba(0,0,0,0.2)' }}>
          AGE 67
        </div>

        <div style={{ display: 'flex', gap: 36, marginBottom: 44 }}>
          <div style={{ opacity: opt1, transform: `translateY(${(1 - opt1) * 40}px)`, width: 540, background: '#000000', padding: '32px 38px', borderRadius: 18 }}>
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="#F4CE14" strokeWidth="2" style={{ marginBottom: 14 }}>
              <circle cx="12" cy="8" r="4" /><path d="M4 21v-2a6 6 0 0112 0v2" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 38, color: '#F4CE14' }}>YOUR OWN</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 600, fontSize: 36, color: '#F5F5F5' }}>Personal Retirement Benefit</div>
          </div>
          <div style={{ opacity: opt2, transform: `translateY(${(1 - opt2) * 40}px)`, width: 540, background: '#93B5C6', padding: '32px 38px', borderRadius: 18 }}>
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="2" style={{ marginBottom: 14 }}>
              <path d="M20 21v-2a4 4 0 00-3-3.87M9 21v-2a4 4 0 013-3.87M12 7a4 4 0 100-8 4 4 0 000 8z" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 38, color: '#000000' }}>SURVIVOR</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 600, fontSize: 36, color: '#000000' }}>Based on late spouse's record</div>
          </div>
        </div>

        <div style={{ opacity: restrictIn, transform: `scale(${restrictIn})`, display: 'flex', alignItems: 'center', gap: 16, background: 'rgba(0,0,0,0.08)', padding: '20px 44px', borderRadius: 12, border: '2px solid rgba(0,0,0,0.25)' }}>
          <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="2">
            <path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z" fill="#93B5C6" /><path d="M14 2v6h6M9 13l2 2 4-4" stroke="#000000" />
          </svg>
          <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 48, color: '#000000' }}>File a <span style={{ fontWeight: 900 }}>RESTRICTED application</span></span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.35)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene18HowItWorks;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19GrowthSwitch',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19GrowthSwitch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.04, 1.1], { extrapolateRight: 'clamp' });
  const collectIn = sp(10);
  const growthProgress = interpolate(frame, [60, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const switchIn = sp(200);

  // Growth bars 67 -> 70
  const bars = [67, 68, 69, 70];

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at 50% 100%, rgba(147,181,198,0.4), rgba(244,206,20,0) 60%)` }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: '5%', left: '4%', width: 400, height: 400, borderRadius: '50%', border: '4px solid rgba(0,0,0,0.05)', transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.05) * 0.05})` }} />
        <div style={{ position: 'absolute', top: '10%', right: '6%', width: 280, height: 280, background: 'rgba(147,181,198,0.08)', transform: `rotate(${-frame * 0.18}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture - rising lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 18 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', bottom: 0, left: `${i * 5.5}%`, width: 2, height: `${30 + Math.sin(frame * 0.04 + i) * 25}%`, background: '#000000' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => {
          const r = seededRandom(i + 2000);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 - frame * 0.12) % 100}%`, left: `${seededRandom(i + 2100) * 100}%`, width: 4 + r * 5, height: 4 + r * 5, borderRadius: '50%', background: 'rgba(0,0,0,0.13)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, justifyContent: 'center', alignItems: 'center', padding: 80 }}>
        <div style={{ opacity: collectIn, transform: `translateY(${(1 - collectIn) * 30}px)`, fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 52, color: '#000000', marginBottom: 30, textAlign: 'center' }}>
          Collect <span style={{ color: '#93B5C6' }}>SURVIVOR check now</span> — your own benefit GROWS 8%/yr
        </div>

        {/* Growth graph */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 60, height: 360, marginBottom: 40, padding: '0 20px', borderLeft: '4px solid #000000', borderBottom: '4px solid #000000' }}>
          {bars.map((age, i) => {
            const barAppear = interpolate(growthProgress, [i * 0.22, i * 0.22 + 0.25], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            const h = (150 + i * 65) * barAppear;
            return (
              <div key={age} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
                <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 30, color: '#000000', opacity: barAppear, marginBottom: 8 }}>+8%</div>
                <div style={{ width: 110, height: h, background: i === 3 ? '#000000' : '#93B5C6', borderRadius: '10px 10px 0 0' }} />
                <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 36, color: '#000000', marginTop: 12 }}>{age}</div>
              </div>
            );
          })}
        </div>

        <div style={{ opacity: switchIn, transform: `scale(${switchIn})`, display: 'flex', alignItems: 'center', gap: 18, background: '#000000', padding: '22px 50px', borderRadius: 14 }}>
          <svg width="50" height="50" viewBox="0 0 24 24" fill="none" stroke="#F4CE14" strokeWidth="2.2">
            <path d="M17 1l4 4-4 4M3 11V9a4 4 0 014-4h14M7 23l-4-4 4-4M21 13v2a4 4 0 01-4 4H3" />
          </svg>
          <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 48, color: '#F4CE14' }}>At 70: SWITCH to your maximized own benefit</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.35)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene19GrowthSwitch;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20ReverseStrategy',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20ReverseStrategy: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [25, -25], { extrapolateRight: 'clamp' });
  const reverseIn = sp(8, { stiffness: 90 });
  const flowIn = sp(45);
  const warnIn = sp(120);
  const warnShake = frame > 120 && frame < 145 ? Math.sin(frame * 1.6) * 4 : 0;
  const arrowFlow = (frame * 4) % 60;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(45deg, rgba(147,181,198,0.32) 0%, rgba(244,206,20,0) 50%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: '-15%', right: '5%', width: 480, height: 480, borderRadius: '50%', background: 'rgba(0,0,0,0.04)', transform: `scale(${1 + Math.sin(frame * 0.05) * 0.06})` }} />
        <div style={{ position: 'absolute', bottom: '5%', left: '-8%', width: 0, height: 0, borderLeft: '170px solid transparent', borderRight: '170px solid transparent', borderBottom: '300px solid rgba(147,181,198,0.08)', transform: `rotate(${-frame * 0.16}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture - dots */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 44 }).map((_, i) => {
          const r = seededRandom(i + 2200);
          return <div key={i} style={{ position: 'absolute', top: `${r * 100}%`, left: `${seededRandom(i + 2300) * 100}%`, width: 6, height: 6, borderRadius: '50%', background: '#000000', transform: `translateY(${Math.sin(frame * 0.05 + i) * 10}px)` }} />;
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 13 }).map((_, i) => {
          const r = seededRandom(i + 2400);
          return <div key={i} style={{ position: 'absolute', top: `${(r * 100 + frame * 0.1) % 100}%`, left: `${seededRandom(i + 2500) * 100}%`, width: 5, height: 5, borderRadius: '50%', background: 'rgba(0,0,0,0.13)' }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)`, justifyContent: 'center', alignItems: 'center', padding: 80 }}>
        <div style={{ opacity: reverseIn, transform: `scale(${reverseIn})`, background: '#000000', color: '#F4CE14', fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 44, padding: '14px 44px', borderRadius: 50, letterSpacing: 2, marginBottom: 40 }}>
          OR — THE EXACT REVERSE
        </div>

        {/* Flow diagram */}
        <div style={{ opacity: flowIn, display: 'flex', alignItems: 'center', gap: 30, marginBottom: 50 }}>
          <div style={{ background: '#93B5C6', padding: '28px 34px', borderRadius: 16, width: 380, textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 34, color: '#000000' }}>CLAIM SMALLER</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 600, fontSize: 34, color: '#000000' }}>Survivor Benefit early</div>
          </div>

          <svg width="120" height="40" viewBox="0 0 120 40">
            <line x1="0" y1="20" x2="100" y2="20" stroke="#000000" strokeWidth="4" strokeDasharray="12 8" strokeDashoffset={-arrowFlow} />
            <polygon points="100,10 120,20 100,30" fill="#000000" />
          </svg>

          <div style={{ background: '#000000', padding: '28px 34px', borderRadius: 16, width: 380, textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 34, color: '#F4CE14' }}>OWN BENEFIT</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 600, fontSize: 34, color: '#F5F5F5' }}>grows to age 70</div>
          </div>
        </div>

        <div style={{ opacity: warnIn, transform: `translateY(${(1 - warnIn) * 25}px) translateX(${warnShake}px)`, display: 'flex', alignItems: 'center', gap: 18, background: 'rgba(0,0,0,0.08)', padding: '22px 46px', borderRadius: 14, border: '2px solid rgba(0,0,0,0.3)' }}>
          <svg width="54" height="54" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="2.2">
            <path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z" fill="#93B5C6" />
            <path d="M12 9v4M12 17h.01" stroke="#000000" />
          </svg>
          <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 46, color: '#000000' }}>SSA won't do this math — <span style={{ fontWeight: 900 }}>YOU must tell them</span></span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.35)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene20ReverseStrategy;
```

