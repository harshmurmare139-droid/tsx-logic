```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_BunyadiSach',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_BunyadiSach: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  // Truth reveal animation
  const truthScale = sp(20, { damping: 10, stiffness: 90 });
  const lineWidth = interpolate(frame, [40, 75], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  
  // Money transfer animation
  const moneyX = interpolate(frame, [90, 150], [0, 600], { 
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.bezier(0.4, 0, 0.2, 1),
  });
  const moneyOp = interpolate(frame, [90, 110, 140, 160], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  
  const bankStamp = sp(170, { damping: 8, stiffness: 100 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(8,6,22,0.5) 0%, rgba(255,0,0,0.1) 50%, rgba(8,6,22,0.7) 100%)',
      }} />

      {/* Shape Layers - drifting geometry */}
      <div style={{
        position: 'absolute', top: '15%', left: '8%', width: 380, height: 380,
        border: '3px solid #F5F5F5', borderRadius: '50%', opacity: 0.06,
        transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', right: '5%', width: 500, height: 500,
        border: '4px solid #080616', opacity: 0.08,
        transform: `rotate(${-frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '50%', left: '50%', width: 250, height: 250,
        backgroundColor: '#F5F5F5', opacity: 0.03,
        transform: `translate(-50%, -50%) rotate(45deg) scale(${1 + Math.cos(frame * 0.03) * 0.1})`,
      }} />

      {/* Motion texture - grid lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#F5F5F5" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid1)" transform={`translate(${(frame * 0.5) % 80}, ${(frame * 0.3) % 80})`} />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = i + 1;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.4) % 1080;
        const size = 2 + seededRandom(seed * 3) * 4;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: size, height: size, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.4,
          }} />
        );
      })}

      {/* Main Content */}
      <div style={{
        position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        transform: `scale(${camZoom})`,
      }}>
        {/* Top label */}
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 700,
          color: '#080616', letterSpacing: 8, textTransform: 'uppercase',
          marginBottom: 30, opacity: sp(5), padding: '12px 32px',
          backgroundColor: '#F5F5F5', borderRadius: 6,
        }}>
          BUNYADI SACH
        </div>

        {/* Hero text */}
        <div style={{
          fontFamily: 'Yantra One', fontSize: 187,
          fontWeight: 900, color: '#F5F5F5',
          textAlign: 'center', lineHeight: 1,
          transform: `scale(${truthScale})`,
          textShadow: '6px 6px 0px #080616',
        }}>
          वो पैसा
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 187,
          fontWeight: 900, color: '#080616',
          textAlign: 'center', lineHeight: 1,
          transform: `scale(${sp(35, { damping: 9 })})`,
        }}>
          आपका नहीं
        </div>

        {/* Animated underline */}
        <div style={{
          width: `${lineWidth * 5}px`, height: 8,
          backgroundColor: '#F5F5F5', marginTop: 20,
        }} />

        {/* Money flow visualization */}
        <div style={{
          marginTop: 50, display: 'flex', alignItems: 'center', gap: 100,
          opacity: interpolate(frame, [85, 100], [0, 1], { extrapolateRight: 'clamp' }),
          position: 'relative',
        }}>
          {/* YOU box */}
          <div style={{
            width: 180, height: 180, backgroundColor: '#F5F5F5',
            border: '5px solid #080616', borderRadius: 12,
            display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
          }}>
            <svg width="60" height="60" viewBox="0 0 24 24" fill="#080616">
              <circle cx="12" cy="8" r="4"/>
              <path d="M12 14c-4 0-8 2-8 6v2h16v-2c0-4-4-6-8-6z"/>
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 800, color: '#080616', marginTop: 8 }}>
              आप
            </div>
          </div>

          {/* Money in transit */}
          <div style={{
            position: 'absolute', left: 180,
            transform: `translateX(${moneyX}px)`,
            opacity: moneyOp,
            display: 'flex', alignItems: 'center', gap: 8,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 60, fontWeight: 900, color: '#F5F5F5',
              backgroundColor: '#080616', padding: '8px 20px', borderRadius: 8,
            }}>
              ₹
            </div>
            <svg width="50" height="30" viewBox="0 0 50 30" fill="#F5F5F5">
              <path d="M0 15 L35 15 M25 5 L40 15 L25 25" stroke="#F5F5F5" strokeWidth="4" fill="none" strokeLinecap="round"/>
            </svg>
          </div>

          {/* BANK box */}
          <div style={{
            width: 220, height: 180, backgroundColor: '#080616',
            border: '5px solid #F5F5F5', borderRadius: 12,
            display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
            position: 'relative',
          }}>
            <svg width="80" height="60" viewBox="0 0 24 24" fill="#F5F5F5">
              <path d="M12 2L2 7v2h20V7L12 2zM4 11v8h2v-8H4zm5 0v8h2v-8H9zm5 0v8h2v-8h-2zm5 0v8h2v-8h-2zM2 21v2h20v-2H2z"/>
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 800, color: '#F5F5F5', marginTop: 4 }}>
              BANK
            </div>
            {/* Stamp */}
            <div style={{
              position: 'absolute', top: -25, right: -40,
              transform: `scale(${bankStamp}) rotate(-15deg)`,
              backgroundColor: '#F5F5F5', color: '#FF0000',
              padding: '6px 14px', fontFamily: 'Inter', fontSize: 20, fontWeight: 900,
              border: '3px solid #F5F5F5', letterSpacing: 2,
            }}>
              OWNER
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(8,6,22,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_BunyadiSach;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_IOUReceipt',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_IOUReceipt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camPan = interpolate(frame, [0, durationInFrames], [-30, 30], { extrapolateRight: 'clamp' });

  // Receipt drops in
  const receiptY = sp(15, { damping: 8, stiffness: 80 });
  const receiptRotate = interpolate(sp(15), [0, 1], [-8, -3]);

  // IOU stamp
  const iouStamp = sp(60, { damping: 6, stiffness: 90 });
  
  // Word reveals
  const word1 = sp(80);
  const word2 = sp(95);
  const word3 = sp(110);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,0,0,1) 0%, rgba(180,0,0,1) 50%, rgba(8,6,22,0.8) 100%)',
      }} />

      {/* Shape layers - paper documents drifting */}
      {Array.from({ length: 6 }).map((_, i) => {
        const seed = i + 1;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${seededRandom(seed) * 90}%`,
            top: `${seededRandom(seed * 2) * 90}%`,
            width: 180 + seededRandom(seed * 3) * 100,
            height: 240 + seededRandom(seed * 4) * 80,
            backgroundColor: '#F5F5F5', opacity: 0.05,
            transform: `rotate(${seededRandom(seed * 5) * 60 - 30 + Math.sin(frame * 0.01 + i) * 5}deg)`,
          }} />
        );
      })}

      {/* Diagonal stripes texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="stripes2" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform={`rotate(45) translate(${frame * 0.3}, 0)`}>
            <line x1="0" y1="0" x2="0" y2="40" stroke="#F5F5F5" strokeWidth="3"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#stripes2)"/>
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i + 10;
        const x = (seededRandom(seed) * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.3,
          }} />
        );
      })}

      {/* Main Layout */}
      <div style={{
        position: 'absolute', inset: 0,
        transform: `translateX(${camPan}px)`,
      }}>
        {/* Left side - Headline */}
        <div style={{
          position: 'absolute', left: 100, top: 200,
          width: 700,
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
            color: '#080616', letterSpacing: 6, marginBottom: 20,
            opacity: sp(5),
            backgroundColor: '#F5F5F5', display: 'inline-block',
            padding: '8px 20px',
          }}>
            BADLE MEIN MILTA HAI
          </div>
          
          <div style={{
            fontFamily: 'Yantra One', fontSize: 144, fontWeight: 900,
            color: '#F5F5F5', lineHeight: 1,
            marginTop: 20,
          }}>
            <span style={{ display: 'block', opacity: word1, transform: `translateX(${interpolate(word1, [0, 1], [-50, 0])}px)` }}>
              सिर्फ
            </span>
            <span style={{ display: 'block', opacity: word2, transform: `translateX(${interpolate(word2, [0, 1], [-50, 0])}px)`, color: '#080616', textShadow: '4px 4px 0 #F5F5F5' }}>
              एक
            </span>
            <span style={{ display: 'block', opacity: word3, transform: `translateX(${interpolate(word3, [0, 1], [-50, 0])}px)` }}>
              वादा
            </span>
          </div>
        </div>

        {/* Right side - Receipt visual */}
        <div style={{
          position: 'absolute', right: 150, top: 180,
          width: 480, height: 640,
          backgroundColor: '#F5F5F5',
          transform: `translateY(${interpolate(receiptY, [0, 1], [-800, 0])}px) rotate(${receiptRotate}deg)`,
          boxShadow: '20px 20px 0 rgba(8,6,22,0.6)',
          padding: 40,
          display: 'flex', flexDirection: 'column',
        }}>
          {/* Receipt header */}
          <div style={{
            borderBottom: '4px dashed #080616', paddingBottom: 16,
            fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 700,
            color: '#080616', textAlign: 'center',
          }}>
            BANK RECEIPT
          </div>

          {/* Line items */}
          <div style={{ marginTop: 30, fontFamily: 'JetBrains Mono', fontSize: 24, color: '#080616' }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 16 }}>
              <span>Deposit:</span>
              <span style={{ fontWeight: 900 }}>₹ 1,00,000</span>
            </div>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 16 }}>
              <span>Status:</span>
              <span style={{ fontWeight: 900, color: '#FF0000' }}>CLAIM</span>
            </div>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 16 }}>
              <span>Type:</span>
              <span style={{ fontWeight: 900 }}>UNSECURED</span>
            </div>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 16 }}>
              <span>Guarantee:</span>
              <span style={{ fontWeight: 900, color: '#FF0000' }}>NONE</span>
            </div>
          </div>

          {/* Promise line */}
          <div style={{
            marginTop: 'auto', borderTop: '4px dashed #080616', paddingTop: 16,
            fontFamily: 'Inter', fontSize: 18, color: '#080616', fontStyle: 'italic',
            textAlign: 'center',
          }}>
            "Bank promises to return on demand"
          </div>

          {/* IOU Stamp */}
          <div style={{
            position: 'absolute', top: 100, right: -60,
            transform: `scale(${iouStamp}) rotate(15deg)`,
            backgroundColor: 'transparent',
            border: '8px solid #FF0000',
            padding: '20px 40px',
            fontFamily: 'Inter', fontSize: 80, fontWeight: 900,
            color: '#FF0000', letterSpacing: 4,
          }}>
            I.O.U
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 35%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_IOUReceipt;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_UnsecuredCreditor',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_UnsecuredCreditor: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camTilt = interpolate(frame, [0, durationInFrames], [0, -2], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.1], { extrapolateRight: 'clamp' });

  // Title animation
  const titleScale = sp(10, { damping: 9 });

  // Status tag animations
  const tag1 = sp(50);
  const tag2 = sp(70);
  const tag3 = sp(90);
  const tag4 = sp(110);

  // Warning shake
  const shake = frame > 180 ? Math.sin(frame * 0.8) * 3 : 0;

  // Badge pop
  const badgePop = sp(150, { damping: 7, stiffness: 100 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 70% 30%, rgba(255,50,50,1) 0%, rgba(180,0,0,1) 40%, rgba(8,6,22,0.9) 100%)',
      }} />

      {/* Shape layers - hexagons */}
      {Array.from({ length: 5 }).map((_, i) => {
        const seed = i + 20;
        const size = 200 + seededRandom(seed) * 200;
        return (
          <svg key={i} style={{
            position: 'absolute',
            left: `${seededRandom(seed) * 80}%`,
            top: `${seededRandom(seed * 2) * 80}%`,
            opacity: 0.06,
            transform: `rotate(${frame * 0.2 + i * 30}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.1})`,
          }} width={size} height={size} viewBox="0 0 100 100">
            <polygon points="50,5 90,27 90,73 50,95 10,73 10,27" fill="none" stroke="#F5F5F5" strokeWidth="2"/>
          </svg>
        );
      })}

      {/* Dot grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="100%" height="100%">
        <defs>
          <pattern id="dots3" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#F5F5F5"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots3)"/>
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const seed = i + 30;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.6) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.4,
            boxShadow: '0 0 8px #F5F5F5',
          }} />
        );
      })}

      {/* Content */}
      <div style={{
        position: 'absolute', inset: 0,
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        transform: `scale(${camScale}) rotate(${camTilt}deg) translateX(${shake}px)`,
        padding: 80,
      }}>
        {/* Label */}
        <div style={{
          fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
          color: '#080616', backgroundColor: '#F5F5F5',
          padding: '10px 28px', letterSpacing: 6, marginBottom: 30,
          opacity: sp(0),
        }}>
          YOUR STATUS
        </div>

        {/* Big English title */}
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 195, fontWeight: 900,
          color: '#F5F5F5', lineHeight: 0.95, textAlign: 'center',
          transform: `scale(${titleScale})`,
          textShadow: '8px 8px 0 #080616',
          letterSpacing: -3,
        }}>
          UNSECURED
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 153, fontWeight: 900,
          color: '#080616', lineHeight: 0.95, textAlign: 'center',
          opacity: sp(25),
          letterSpacing: -2,
          marginTop: 10,
        }}>
          CREDITOR
        </div>

        {/* Status tags */}
        <div style={{
          display: 'flex', gap: 20, marginTop: 50, flexWrap: 'wrap', justifyContent: 'center',
        }}>
          {[
            { label: 'NO COLLATERAL', delay: tag1 },
            { label: 'NO GUARANTEE', delay: tag2 },
            { label: 'NO PRIORITY', delay: tag3 },
            { label: 'JUST A PROMISE', delay: tag4 },
          ].map((tag, i) => (
            <div key={i} style={{
              fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 800,
              color: '#FF0000', backgroundColor: '#F5F5F5',
              padding: '12px 24px', border: '4px solid #080616',
              transform: `scale(${tag.delay}) translateY(${interpolate(tag.delay, [0, 1], [40, 0])}px)`,
              opacity: tag.delay,
            }}>
              ✗ {tag.label}
            </div>
          ))}
        </div>

        {/* Warning badge */}
        <div style={{
          marginTop: 40,
          fontFamily: 'Yantra One', fontSize: 60, fontWeight: 900,
          color: '#F5F5F5', backgroundColor: '#080616',
          padding: '14px 40px', border: '5px solid #F5F5F5',
          transform: `scale(${badgePop})`,
        }}>
          लेनदार = कोई गारंटी नहीं
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 30%, rgba(8,6,22,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_UnsecuredCreditor;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_LoanSplit',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_LoanSplit: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom out
  const camZoom = interpolate(frame, [0, durationInFrames], [1.15, 1], { extrapolateRight: 'clamp' });

  // 100 rupee note appears
  const noteScale = sp(10, { damping: 8 });
  
  // Split animation
  const splitProgress = interpolate(frame, [60, 120], [0, 1], { 
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.bezier(0.4, 0, 0.2, 1),
  });

  // Bar fill
  const barFill = interpolate(frame, [80, 140], [0, 78], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  
  // Numbers count
  const countNum = Math.floor(interpolate(frame, [90, 150], [0, 78], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(255,0,0,1) 0%, rgba(150,0,0,1) 60%, rgba(8,6,22,0.95) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '10%', right: '5%', width: 400, height: 400,
        border: '4px solid #F5F5F5', opacity: 0.07,
        transform: `rotate(${frame * 0.25}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '15%', left: '3%', width: 300, height: 300,
        borderRadius: '50%', backgroundColor: '#080616', opacity: 0.1,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
      }} />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="cross4" width="100" height="100" patternUnits="userSpaceOnUse">
            <line x1="50" y1="0" x2="50" y2="100" stroke="#F5F5F5" strokeWidth="1"/>
            <line x1="0" y1="50" x2="100" y2="50" stroke="#F5F5F5" strokeWidth="1"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross4)"/>
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 40;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 - frame * 0.5 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.35,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        transform: `scale(${camZoom})`,
        padding: 60,
      }}>
        {/* Title */}
        <div style={{
          fontFamily: 'Yantra One', fontSize: 102, fontWeight: 900,
          color: '#F5F5F5', textAlign: 'center', lineHeight: 1,
          marginBottom: 50,
          opacity: sp(0),
        }}>
          हर ₹100 में से
        </div>

        {/* The 100 rupee note */}
        <div style={{
          width: 900, height: 100,
          backgroundColor: '#080616', border: '5px solid #F5F5F5',
          borderRadius: 8, position: 'relative', overflow: 'hidden',
          transform: `scale(${noteScale})`,
          display: 'flex', alignItems: 'center', justifyContent: 'center',
        }}>
          {/* Loan portion (red fill from left) */}
          <div style={{
            position: 'absolute', left: 0, top: 0, bottom: 0,
            width: `${barFill}%`, backgroundColor: '#FF0000',
            borderRight: '4px solid #F5F5F5',
            transition: 'width 0.1s',
          }} />
          
          <div style={{
            position: 'relative', zIndex: 2,
            fontFamily: 'JetBrains Mono', fontSize: 50, fontWeight: 900,
            color: '#F5F5F5',
          }}>
            ₹100 DEPOSIT
          </div>
        </div>

        {/* Labels below bar */}
        <div style={{
          width: 900, display: 'flex', justifyContent: 'space-between',
          marginTop: 30, opacity: interpolate(frame, [100, 130], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            backgroundColor: '#F5F5F5', padding: '14px 24px',
            border: '3px solid #080616',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#080616', fontWeight: 600 }}>
              LOANED OUT
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 76, fontWeight: 900, color: '#FF0000', lineHeight: 1 }}>
              ₹{countNum}
            </div>
          </div>

          <div style={{
            backgroundColor: '#080616', padding: '14px 24px',
            border: '3px solid #F5F5F5',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#F5F5F5', fontWeight: 600 }}>
              ACTUAL CASH
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 76, fontWeight: 900, color: '#F5F5F5', lineHeight: 1 }}>
              ₹{78 - countNum > 0 ? 100 - countNum : 22}
            </div>
          </div>
        </div>

        {/* Bottom punchline */}
        <div style={{
          marginTop: 50,
          fontFamily: 'Yantra One', fontSize: 68, fontWeight: 900,
          color: '#080616', backgroundColor: '#F5F5F5',
          padding: '12px 36px', textAlign: 'center',
          transform: `scale(${sp(160, { damping: 8 })})`,
        }}>
          बड़ा हिस्सा → लोन पर
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_LoanSplit;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_CRRSLR',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_CRRSLR: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // RBI building intro
  const rbiScale = sp(10, { damping: 9 });

  // Card 1 - CRR
  const card1 = sp(80, { damping: 10 });
  // Card 2 - SLR
  const card2 = sp(140, { damping: 10 });

  // Camera subtle orbit
  const camRot = Math.sin(frame * 0.01) * 1;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(255,30,30,1) 0%, rgba(160,0,0,1) 50%, rgba(8,6,22,0.95) 100%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${15 + i * 22}%`, top: `${10 + (i % 2) * 60}%`,
          width: 220, height: 220,
          border: '3px solid #F5F5F5', opacity: 0.06,
          transform: `rotate(${frame * 0.3 + i * 45}deg)`,
        }} />
      ))}

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="circles5" width="120" height="120" patternUnits="userSpaceOnUse">
            <circle cx="60" cy="60" r="40" fill="none" stroke="#F5F5F5" strokeWidth="1"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#circles5)"/>
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = i + 50;
        const x = (seededRandom(seed) * 1920 + frame * 0.4) % 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.4,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'flex-start',
        transform: `rotate(${camRot}deg)`,
        padding: 80, paddingTop: 80,
      }}>
        {/* RBI Header */}
        <div style={{
          display: 'flex', alignItems: 'center', gap: 30,
          transform: `scale(${rbiScale})`,
          backgroundColor: '#080616',
          padding: '20px 40px', border: '5px solid #F5F5F5',
          marginBottom: 30,
        }}>
          {/* RBI building icon */}
          <svg width="80" height="80" viewBox="0 0 100 100" fill="#F5F5F5">
            <rect x="10" y="40" width="80" height="50"/>
            <polygon points="5,40 50,15 95,40"/>
            <rect x="20" y="55" width="8" height="35" fill="#080616"/>
            <rect x="36" y="55" width="8" height="35" fill="#080616"/>
            <rect x="52" y="55" width="8" height="35" fill="#080616"/>
            <rect x="68" y="55" width="8" height="35" fill="#080616"/>
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#F5F5F5', letterSpacing: 4, fontWeight: 700 }}>
              REGULATED BY
            </div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 68, color: '#F5F5F5', fontWeight: 900, lineHeight: 1 }}>
              RBI
            </div>
          </div>
        </div>

        {/* Subtitle */}
        <div style={{
          fontFamily: 'Yantra One', fontSize: 60, fontWeight: 900,
          color: '#F5F5F5', textAlign: 'center', marginBottom: 50,
          opacity: sp(40),
        }}>
          दो ज़रूरी रिज़र्व नियम
        </div>

        {/* Two cards */}
        <div style={{ display: 'flex', gap: 60, justifyContent: 'center' }}>
          {/* CRR Card */}
          <div style={{
            width: 500, padding: 40,
            backgroundColor: '#F5F5F5',
            border: '6px solid #080616',
            transform: `scale(${card1}) translateY(${interpolate(card1, [0, 1], [60, 0])}px)`,
            opacity: card1,
            position: 'relative',
          }}>
            <div style={{
              position: 'absolute', top: -25, left: 30,
              backgroundColor: '#FF0000', color: '#F5F5F5',
              padding: '6px 18px', fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 800,
              border: '3px solid #080616',
            }}>
              RULE 01
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 119, fontWeight: 900,
              color: '#080616', lineHeight: 1,
            }}>
              CRR
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 28, fontWeight: 600,
              color: '#080616', marginTop: 8,
            }}>
              Cash Reserve Ratio
            </div>
            <div style={{
              marginTop: 24, fontFamily: 'JetBrains Mono', fontSize: 76, fontWeight: 900,
              color: '#FF0000',
            }}>
              ~ 4.5%
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 26, color: '#080616',
              marginTop: 10,
            }}>
              RBI के पास नकद रिज़र्व
            </div>
          </div>

          {/* SLR Card */}
          <div style={{
            width: 500, padding: 40,
            backgroundColor: '#080616',
            border: '6px solid #F5F5F5',
            transform: `scale(${card2}) translateY(${interpolate(card2, [0, 1], [60, 0])}px)`,
            opacity: card2,
            position: 'relative',
          }}>
            <div style={{
              position: 'absolute', top: -25, left: 30,
              backgroundColor: '#F5F5F5', color: '#080616',
              padding: '6px 18px', fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 800,
              border: '3px solid #FF0000',
            }}>
              RULE 02
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 119, fontWeight: 900,
              color: '#F5F5F5', lineHeight: 1,
            }}>
              SLR
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 28, fontWeight: 600,
              color: '#F5F5F5', marginTop: 8,
            }}>
              Statutory Liquidity Ratio
            </div>
            <div style={{
              marginTop: 24, fontFamily: 'JetBrains Mono', fontSize: 76, fontWeight: 900,
              color: '#FF0000',
            }}>
              ~ 18%
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 26, color: '#F5F5F5',
              marginTop: 10,
            }}>
              सरकारी सिक्योरिटीज़ में
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 35%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_CRRSLR;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_LoanCycle',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_LoanCycle: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pie chart fill
  const pieFill = interpolate(frame, [40, 130], [0, 78], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const pieAngle = (pieFill / 100) * 360;
  
  // Date stamp
  const dateStamp = sp(0);
  
  // Equation pieces
  const eq1 = sp(60);
  const eq2 = sp(90);
  const eq3 = sp(120);
  
  // Big number reveal
  const bigNum = sp(170, { damping: 7 });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1], { extrapolateRight: 'clamp' });

  // Helper for pie slice path
  const polarToCart = (cx: number, cy: number, r: number, deg: number) => {
    const rad = ((deg - 90) * Math.PI) / 180;
    return { x: cx + r * Math.cos(rad), y: cy + r * Math.sin(rad) };
  };
  const piePath = () => {
    const cx = 200, cy = 200, r = 180;
    const start = polarToCart(cx, cy, r, 0);
    const end = polarToCart(cx, cy, r, pieAngle);
    const largeArc = pieAngle > 180 ? 1 : 0;
    return `M ${cx} ${cy} L ${start.x} ${start.y} A ${r} ${r} 0 ${largeArc} 1 ${end.x} ${end.y} Z`;
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,0,0,1) 0%, rgba(120,0,0,1) 70%, rgba(8,6,22,1) 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', top: '5%', left: '50%', width: 600, height: 600,
        borderRadius: '50%', border: '4px solid #F5F5F5', opacity: 0.05,
        transform: `translateX(-50%) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-10%', right: '-5%', width: 500, height: 500,
        backgroundColor: '#080616', opacity: 0.15,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="diag6" width="30" height="30" patternUnits="userSpaceOnUse" patternTransform={`rotate(-45) translate(${frame * 0.3}, 0)`}>
            <line x1="0" y1="15" x2="30" y2="15" stroke="#F5F5F5" strokeWidth="1"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag6)"/>
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const seed = i + 60;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.35,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        display: 'flex', alignItems: 'center', justifyContent: 'space-around',
        transform: `scale(${camZoom})`,
        padding: 80,
      }}>
        {/* LEFT: Pie chart */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          {/* Date badge */}
          <div style={{
            backgroundColor: '#080616', color: '#F5F5F5',
            padding: '8px 24px', fontFamily: 'JetBrains Mono', fontSize: 26,
            fontWeight: 800, border: '3px solid #F5F5F5', marginBottom: 20,
            transform: `scale(${dateStamp})`,
          }}>
            MAY 2026
          </div>
          
          <svg width="400" height="400" viewBox="0 0 400 400">
            {/* Background circle */}
            <circle cx="200" cy="200" r="180" fill="#080616" stroke="#F5F5F5" strokeWidth="4"/>
            {/* Filled slice = loan portion */}
            <path d={piePath()} fill="#F5F5F5" />
            {/* Center label */}
            <circle cx="200" cy="200" r="70" fill="#FF0000" stroke="#080616" strokeWidth="4"/>
            <text x="200" y="195" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="42" fontWeight="900" fill="#F5F5F5">
              {Math.floor(pieFill)}%
            </text>
            <text x="200" y="225" textAnchor="middle" fontFamily="Inter" fontSize="16" fontWeight="700" fill="#F5F5F5">
              LOANED OUT
            </text>
          </svg>
        </div>

        {/* RIGHT: Equation */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 18, width: 700 }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 60, color: '#F5F5F5',
            opacity: sp(10),
            lineHeight: 1.1,
          }}>
            गणित सुनिए:
          </div>

          <div style={{
            display: 'flex', alignItems: 'center', gap: 18,
            opacity: eq1, transform: `translateX(${interpolate(eq1, [0, 1], [-40, 0])}px)`,
          }}>
            <div style={{
              backgroundColor: '#F5F5F5', color: '#080616',
              padding: '14px 24px', fontFamily: 'JetBrains Mono', fontSize: 51, fontWeight: 900,
            }}>₹100</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 51, color: '#F5F5F5', fontWeight: 900 }}>−</div>
            <div style={{
              backgroundColor: '#080616', color: '#F5F5F5',
              padding: '14px 20px', fontFamily: 'JetBrains Mono', fontSize: 38, fontWeight: 900,
              border: '3px solid #F5F5F5',
            }}>CRR 4.5%</div>
          </div>

          <div style={{
            display: 'flex', alignItems: 'center', gap: 18,
            opacity: eq2, transform: `translateX(${interpolate(eq2, [0, 1], [-40, 0])}px)`,
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 51, color: '#F5F5F5', fontWeight: 900, width: 120, textAlign: 'right' }}>−</div>
            <div style={{
              backgroundColor: '#080616', color: '#F5F5F5',
              padding: '14px 20px', fontFamily: 'JetBrains Mono', fontSize: 38, fontWeight: 900,
              border: '3px solid #F5F5F5',
            }}>SLR 18%</div>
          </div>

          <div style={{
            display: 'flex', alignItems: 'center', gap: 18,
            opacity: eq3, transform: `translateX(${interpolate(eq3, [0, 1], [-40, 0])}px)`,
            borderTop: '3px solid #F5F5F5', paddingTop: 18, marginTop: 8,
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 51, color: '#F5F5F5', fontWeight: 900 }}>=</div>
            <div style={{
              backgroundColor: '#F5F5F5', color: '#FF0000',
              padding: '16px 32px', fontFamily: 'JetBrains Mono', fontSize: 76, fontWeight: 900,
              transform: `scale(${bigNum})`,
              border: '5px solid #080616',
            }}>₹77-78</div>
          </div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 36, color: '#F5F5F5',
            opacity: sp(180), marginTop: 16, fontWeight: 700,
          }}>
            → आगे लोन में घूमा दिया जाता है
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_LoanCycle;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_DigitalEntry',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_DigitalEntry: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const screenScale = sp(10, { damping: 9 });
  const numFlash = Math.floor(frame / 4) % 2 === 0;
  
  // Glitch flicker
  const glitch = frame > 90 && frame < 120 ? (Math.random() > 0.7 ? 4 : 0) : 0;
  
  // Cash disappears
  const cashFade = interpolate(frame, [140, 180], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  
  // Truth text
  const truthIn = sp(190, { damping: 8 });
  
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 100%, rgba(255,50,50,1) 0%, rgba(140,0,0,1) 50%, rgba(8,6,22,1) 100%)',
      }} />

      {/* Binary code shapes - background */}
      {Array.from({ length: 12 }).map((_, i) => {
        const seed = i + 70;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${seededRandom(seed) * 100}%`,
            top: `${(seededRandom(seed * 2) * 100 + frame * 0.1) % 100}%`,
            fontFamily: 'JetBrains Mono', fontSize: 24,
            color: '#F5F5F5', opacity: 0.15,
            fontWeight: 700,
          }}>
            {Array.from({ length: 8 }).map(() => (Math.random() > 0.5 ? '1' : '0')).join('')}
          </div>
        );
      })}

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="100%" height="100%">
        <defs>
          <pattern id="techgrid7" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#F5F5F5" strokeWidth="0.5"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#techgrid7)"/>
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i + 80;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 - frame * 0.7 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 2, height: 6, backgroundColor: '#F5F5F5', opacity: 0.4,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        transform: `scale(${camZoom}) translateX(${glitch}px)`,
        padding: 60,
      }}>
        {/* Title */}
        <div style={{
          fontFamily: 'Yantra One', fontSize: 76, fontWeight: 900,
          color: '#F5F5F5', textAlign: 'center', marginBottom: 30,
          opacity: sp(0),
        }}>
          आपका बैंक बैलेंस =
        </div>

        {/* Phone/Screen Mockup */}
        <div style={{
          width: 800, padding: 30,
          backgroundColor: '#080616',
          border: '6px solid #F5F5F5',
          borderRadius: 24,
          transform: `scale(${screenScale})`,
          boxShadow: '12px 12px 0 rgba(0,0,0,0.5)',
          position: 'relative',
        }}>
          {/* Top status bar */}
          <div style={{
            display: 'flex', justifyContent: 'space-between',
            fontFamily: 'JetBrains Mono', fontSize: 18, color: '#F5F5F5',
            opacity: 0.6, marginBottom: 16,
          }}>
            <span>BANK APP</span>
            <span>● ONLINE</span>
          </div>

          {/* Account label */}
          <div style={{
            fontFamily: 'Inter', fontSize: 28, color: '#F5F5F5', opacity: 0.7,
            marginBottom: 8,
          }}>
            Available Balance
          </div>

          {/* Balance number */}
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 153, fontWeight: 900,
            color: '#F5F5F5', lineHeight: 1, letterSpacing: -3,
            opacity: numFlash ? 1 : 0.85,
          }}>
            ₹ 5,00,000
          </div>

          {/* Status line */}
          <div style={{
            marginTop: 20, display: 'flex', justifyContent: 'space-between',
            borderTop: '2px solid rgba(245,245,245,0.2)', paddingTop: 14,
            fontFamily: 'JetBrains Mono', fontSize: 22, color: '#F5F5F5', opacity: 0.6,
          }}>
            <span>TYPE: Digital Entry</span>
            <span>ACTUAL CASH: ???</span>
          </div>

          {/* Glitch overlay */}
          {frame > 100 && frame < 140 && (
            <div style={{
              position: 'absolute', inset: 0,
              background: 'linear-gradient(transparent 49%, rgba(245,245,245,0.4) 50%, transparent 51%)',
              backgroundSize: '100% 4px',
              pointerEvents: 'none', borderRadius: 24,
            }} />
          )}
        </div>

        {/* Vault visualization */}
        <div style={{
          marginTop: 40, display: 'flex', alignItems: 'center', gap: 30,
          opacity: interpolate(frame, [120, 150], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 51, color: '#F5F5F5', fontWeight: 900,
          }}>
            असली कैश यहाँ →
          </div>
          <div style={{
            width: 200, height: 140,
            backgroundColor: '#080616', border: '5px dashed #F5F5F5',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            position: 'relative',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 30, color: '#FF0000', fontWeight: 900,
              opacity: cashFade,
            }}>
              ₹₹₹₹
            </div>
            {cashFade < 0.5 && (
              <div style={{
                position: 'absolute', inset: 0,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Inter, Poppins', fontSize: 60, color: '#F5F5F5', fontWeight: 900,
              }}>
                ∅
              </div>
            )}
          </div>
        </div>

        {/* Final truth */}
        <div style={{
          marginTop: 30,
          fontFamily: 'Yantra One', fontSize: 60, fontWeight: 900,
          color: '#080616', backgroundColor: '#F5F5F5',
          padding: '14px 36px', textAlign: 'center',
          transform: `scale(${truthIn})`,
        }}>
          सिर्फ एक नंबर — कैश नहीं
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 30%, rgba(8,6,22,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_DigitalEntry;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_Vishwas',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_Vishwas: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pillar grow
  const pillarHeight = interpolate(sp(30), [0, 1], [0, 500]);
  
  // VISHWAS text reveals
  const word = "विश्वास";
  
  // Tower wobble (subtle)
  const wobble = Math.sin(frame * 0.05) * 1;

  // Stamp impact
  const stamp = sp(170, { damping: 6, stiffness: 110 });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.1, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 70%, rgba(255,30,30,1) 0%, rgba(140,0,0,1) 50%, rgba(8,6,22,1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '50%', width: 700, height: 700,
        border: '3px solid #F5F5F5', borderRadius: '50%', opacity: 0.05,
        transform: `translateX(-50%) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', top: '30%', left: '50%', width: 500, height: 500,
        border: '3px solid #F5F5F5', borderRadius: '50%', opacity: 0.06,
        transform: `translateX(-50%) rotate(${frame * 0.5}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '40%', left: '50%', width: 300, height: 300,
        border: '3px solid #F5F5F5', borderRadius: '50%', opacity: 0.07,
        transform: `translateX(-50%) rotate(${-frame * 0.7}deg)`,
      }} />

      {/* Texture - subtle lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="lines8" width="20" height="20" patternUnits="userSpaceOnUse">
            <line x1="0" y1="20" x2="20" y2="0" stroke="#F5F5F5" strokeWidth="1"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#lines8)"/>
      </svg>

      {/* Glow particles around center */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = i + 90;
        const angle = (i / 30) * Math.PI * 2 + frame * 0.005;
        const radius = 300 + seededRandom(seed) * 200;
        const x = 960 + Math.cos(angle) * radius;
        const y = 540 + Math.sin(angle) * radius;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.5,
            boxShadow: '0 0 10px #F5F5F5',
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        transform: `scale(${camZoom}) translateX(${wobble}px)`,
        padding: 80,
      }}>
        {/* Label */}
        <div style={{
          fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
          color: '#080616', backgroundColor: '#F5F5F5',
          padding: '10px 28px', letterSpacing: 6, marginBottom: 30,
          opacity: sp(0),
        }}>
          THE ONLY PILLAR
        </div>

        {/* The tower / pillar */}
        <div style={{
          position: 'relative',
          display: 'flex', flexDirection: 'column', alignItems: 'center',
        }}>
          {/* Hero word */}
          <div style={{
            fontFamily: 'Yantra One', fontSize: 246, fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1, letterSpacing: -4,
            textShadow: '10px 10px 0 #080616',
            transform: `scale(${sp(15, { damping: 8 })})`,
          }}>
            {word}
          </div>

          {/* Pillar bar growing */}
          <div style={{
            width: 80, height: pillarHeight,
            backgroundColor: '#F5F5F5',
            border: '4px solid #080616',
            marginTop: 10,
            position: 'relative',
          }}>
            <div style={{
              position: 'absolute', top: '50%', left: '50%',
              transform: 'translate(-50%, -50%) rotate(-90deg)',
              fontFamily: 'Inter', fontSize: 22, fontWeight: 800, color: '#080616',
              whiteSpace: 'nowrap', letterSpacing: 4,
            }}>
              TRUST
            </div>
          </div>

          {/* Floor base */}
          <div style={{
            width: 400, height: 20, backgroundColor: '#080616',
            border: '3px solid #F5F5F5',
          }} />
        </div>

        {/* English subtitle */}
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 51, fontWeight: 800,
          color: '#080616', backgroundColor: '#F5F5F5',
          padding: '10px 24px', marginTop: 20,
          opacity: sp(80),
          transform: `translateY(${interpolate(sp(80), [0, 1], [30, 0])}px)`,
        }}>
          BUILT ON ONE THING: TRUST
        </div>

        {/* Status stamp */}
        <div style={{
          marginTop: 30,
          fontFamily: 'JetBrains Mono', fontSize: 40, fontWeight: 900,
          color: '#FF0000', backgroundColor: '#F5F5F5',
          padding: '12px 32px',
          border: '5px solid #FF0000',
          transform: `scale(${stamp}) rotate(-3deg)`,
          letterSpacing: 4,
        }}>
          STATUS: STABLE — FOR NOW
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 35%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8_Vishwas;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_BankRun',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_BankRun: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Earthquake shake
  const shakeX = frame > 30 && frame < 90 ? Math.sin(frame * 1.2) * 6 : 0;
  const shakeY = frame > 30 && frame < 90 ? Math.cos(frame * 1.5) * 4 : 0;

  // Big % reveal
  const percentScale = sp(100, { damping: 7, stiffness: 100 });
  
  // Counter animation
  const counter = interpolate(frame, [100, 170], [0, 6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  
  // Stick figures running
  const figureCount = 12;
  
  // FAIL stamp
  const failStamp = sp(220, { damping: 6, stiffness: 110 });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(255,60,60,1) 0%, rgba(180,0,0,1) 50%, rgba(8,6,22,1) 100%)',
      }} />

      {/* Shape layers - cracking effect */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="100%" height="100%">
        <path d="M 100 200 L 400 350 L 350 600 L 700 750" stroke="#F5F5F5" strokeWidth="3" fill="none"/>
        <path d="M 1500 100 L 1300 400 L 1450 700 L 1200 1000" stroke="#F5F5F5" strokeWidth="3" fill="none"/>
      </svg>

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="crackgrid9" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 0 25 L 25 0 L 50 25 L 25 50 Z" fill="none" stroke="#F5F5F5" strokeWidth="0.5"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#crackgrid9)"/>
      </svg>

      {/* Particles - dust */}
      {Array.from({ length: 35 }).map((_, i) => {
        const seed = i + 100;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.8) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.4,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camZoom}) translate(${shakeX}px, ${shakeY}px)`,
      }}>
        {/* Top: Headline */}
        <div style={{
          position: 'absolute', top: 60, left: 0, right: 0,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, fontWeight: 800,
            color: '#080616', backgroundColor: '#F5F5F5',
            display: 'inline-block', padding: '10px 28px', letterSpacing: 6,
            opacity: sp(0),
          }}>
            BANK RUN SCENARIO
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 110, fontWeight: 900,
            color: '#F5F5F5', marginTop: 16, lineHeight: 1,
            opacity: sp(10),
          }}>
            विश्वास डगमगाया →
          </div>
        </div>

        {/* Middle: Bank building with people running */}
        <div style={{
          position: 'absolute', top: 320, left: '50%', transform: 'translateX(-50%)',
          width: 1400, height: 380,
          display: 'flex', alignItems: 'flex-end', justifyContent: 'center',
          gap: 60,
        }}>
          {/* Bank building */}
          <div style={{
            width: 380, height: 320,
            backgroundColor: '#080616', border: '6px solid #F5F5F5',
            position: 'relative', display: 'flex',
            flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
          }}>
            {/* Bank pillars */}
            <div style={{
              position: 'absolute', top: 40, left: 30, right: 30, height: 200,
              display: 'flex', justifyContent: 'space-between',
            }}>
              {[0, 1, 2, 3].map((i) => (
                <div key={i} style={{
                  width: 30, height: '100%', backgroundColor: '#F5F5F5',
                }} />
              ))}
            </div>
            {/* Bank top */}
            <div style={{
              position: 'absolute', top: -30, left: -10, right: -10, height: 40,
              backgroundColor: '#F5F5F5', clipPath: 'polygon(0 100%, 50% 0, 100% 100%)',
            }} />
            <div style={{
              position: 'absolute', bottom: 20,
              fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 900, color: '#FF0000',
              backgroundColor: '#F5F5F5', padding: '4px 16px',
            }}>
              BANK
            </div>
          </div>

          {/* Running figures */}
          <div style={{ display: 'flex', gap: 20, alignItems: 'flex-end', flexWrap: 'wrap', width: 800 }}>
            {Array.from({ length: figureCount }).map((_, i) => {
              const delay = 30 + i * 6;
              const figOp = sp(delay);
              const runX = Math.sin(frame * 0.3 + i) * 8;
              return (
                <svg key={i} width="55" height="100" viewBox="0 0 55 100" style={{
                  opacity: figOp,
                  transform: `translateY(${interpolate(figOp, [0, 1], [40, 0])}px) translateX(${runX}px)`,
                }}>
                  <circle cx="27" cy="18" r="12" fill="#F5F5F5"/>
                  <rect x="20" y="30" width="14" height="35" fill="#F5F5F5"/>
                  <line x1="27" y1="65" x2="15" y2="90" stroke="#F5F5F5" strokeWidth="6" strokeLinecap="round"/>
                  <line x1="27" y1="65" x2="40" y2="90" stroke="#F5F5F5" strokeWidth="6" strokeLinecap="round"/>
                  <line x1="27" y1="38" x2="10" y2="50" stroke="#F5F5F5" strokeWidth="5" strokeLinecap="round"/>
                  <line x1="27" y1="38" x2="45" y2="35" stroke="#F5F5F5" strokeWidth="5" strokeLinecap="round"/>
                </svg>
              );
            })}
          </div>
        </div>

        {/* Bottom: Big percentage */}
        <div style={{
          position: 'absolute', bottom: 100, left: 0, right: 0,
          display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 50,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 187, fontWeight: 900,
            color: '#F5F5F5',
            transform: `scale(${percentScale})`,
            textShadow: '8px 8px 0 #080616',
            lineHeight: 1,
          }}>
            {counter.toFixed(1)}%
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 38, fontWeight: 800,
              color: '#F5F5F5', maxWidth: 480,
            }}>
              जमाकर्ता एक साथ →
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 51, fontWeight: 900,
              color: '#080616', backgroundColor: '#F5F5F5',
              padding: '8px 20px', display: 'inline-block',
              transform: `scale(${failStamp})`,
              border: '4px solid #FF0000',
            }}>
              SYSTEM FAILS
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 30%, rgba(8,6,22,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9_BankRun;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_DesignFlaw',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_DesignFlaw: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // NOT scam stamp
  const notScam = sp(20, { damping: 7 });
  // Cross strike
  const crossLine = interpolate(frame, [50, 80], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  
  // Design flaw card
  const flawCard = sp(110, { damping: 10 });
  
  // Blueprint reveal
  const blueprintDraw = interpolate(frame, [130, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const camPan = interpolate(frame, [0, durationInFrames], [0, -15], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, rgba(255,0,0,1) 0%, rgba(180,0,0,1) 40%, rgba(8,6,22,1) 100%)',
      }} />

      {/* Blueprint shape layers */}
      <div style={{
        position: 'absolute', top: '10%', right: '10%', width: 350, height: 350,
        border: '3px dashed #F5F5F5', opacity: 0.08,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '15%', left: '8%', width: 280, height: 280,
        border: '3px dashed #F5F5F5', borderRadius: '50%', opacity: 0.07,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
      }} />

      {/* Blueprint grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="100%" height="100%">
        <defs>
          <pattern id="blueprintGrid10" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#F5F5F5" strokeWidth="0.5"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#blueprintGrid10)"/>
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = i + 110;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.35,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        transform: `translateX(${camPan}px)`,
        padding: 80, gap: 30,
      }}>
        {/* NOT a scam line */}
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 102, fontWeight: 900,
            color: '#F5F5F5',
            transform: `scale(${notScam})`,
            textShadow: '6px 6px 0 #080616',
          }}>
            ये घोटाला नहीं है
          </div>
          {/* Strike-through */}
          <div style={{
            position: 'absolute', top: '50%', left: 0,
            width: `${crossLine}%`, height: 8,
            backgroundColor: '#080616',
            transform: 'translateY(-50%) rotate(-3deg)',
          }} />
        </div>

        {/* It's a design flaw */}
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 162, fontWeight: 900,
          color: '#080616', backgroundColor: '#F5F5F5',
          padding: '20px 50px', lineHeight: 1,
          transform: `scale(${flawCard})`,
          letterSpacing: -2,
          border: '6px solid #080616',
        }}>
          DESIGN FLAW
        </div>

        {/* Hindi explanation */}
        <div style={{
          fontFamily: 'Yantra One', fontSize: 60, fontWeight: 900,
          color: '#F5F5F5', textAlign: 'center',
          opacity: sp(150), maxWidth: 1400,
          lineHeight: 1.2,
        }}>
          सिस्टम कभी इस स्थिति के लिए बनाया ही नहीं गया
        </div>

        {/* Blueprint-style label */}
        <div style={{
          marginTop: 20,
          display: 'flex', alignItems: 'center', gap: 30,
          opacity: sp(180),
        }}>
          {/* Blueprint icon */}
          <svg width="120" height="120" viewBox="0 0 120 120" style={{
            opacity: blueprintDraw,
          }}>
            <rect x="10" y="10" width="100" height="100" fill="none" stroke="#F5F5F5" strokeWidth="3" strokeDasharray="6 4"/>
            <line x1="10" y1="40" x2="110" y2="40" stroke="#F5F5F5" strokeWidth="2" strokeDasharray="4 4"/>
            <line x1="40" y1="10" x2="40" y2="110" stroke="#F5F5F5" strokeWidth="2" strokeDasharray="4 4"/>
            <text x="60" y="75" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="22" fill="#FF0000" fontWeight="900">
              ⚠
            </text>
          </svg>
          
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 38, fontWeight: 900,
            color: '#080616', backgroundColor: '#F5F5F5',
            padding: '12px 28px', letterSpacing: 3,
            border: '4px solid #080616',
          }}>
            BUILT ON TRUST, NOT CASH
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 35%, rgba(8,6,22,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_DesignFlaw;
```

