```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene1_StructuralShift',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene1_StructuralShift: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Top label slide
  const labelY = interpolate(sp(10), [0, 1], [-40, 0]);
  const labelOp = sp(10);

  // Main word reveal — letter by letter
  const word1 = "अस्थाई";
  const word2 = "नहीं";
  const word3 = "ढाँचागत";

  // Strike-through on "अस्थाई"
  const strikeWidth = interpolate(frame, [60, 90], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.65, 0, 0.35, 1),
  });

  // Big reveal of ढाँचागत
  const bigScale = sp(100, { damping: 12, stiffness: 90 });
  const bigOp = sp(100);

  // Underline grow
  const underlineW = interpolate(frame, [130, 165], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Subtle camera breathe
  const camScale = 1 + Math.sin(frame * 0.02) * 0.008;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EDEAE4', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient warm overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(255,255,255,0.6) 0%, transparent 55%), radial-gradient(circle at 80% 90%, rgba(0,0,0,0.04) 0%, transparent 60%)',
      }} />

      {/* Shape Layer — drifting transparent circles */}
      {[0, 1, 2, 3, 4].map((i) => {
        const r = seededRandom(i + 1);
        const size = 280 + r * 320;
        const x = (i * 380 + frame * 0.15) % 2200 - 200;
        const y = 100 + r * 800;
        return (
          <div key={`shp-${i}`} style={{
            position: 'absolute', left: x, top: y, width: size, height: size,
            borderRadius: '50%', border: '1.5px solid rgba(28,28,28,0.05)',
            transform: `rotate(${frame * 0.1 + i * 30}deg)`,
          }} />
        );
      })}

      {/* Motion texture — vertical thin lines */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <div key={`ln-${i}`} style={{
            position: 'absolute', left: i * 48, top: 0, width: 1, height: '100%',
            backgroundColor: '#1C1C1C',
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const px = (r * 1920 + frame * 0.3) % 1920;
        const py = (r * 1080 + frame * 0.5 + i * 60) % 1080;
        return (
          <div key={`pt-${i}`} style={{
            position: 'absolute', left: px, top: py, width: 4, height: 4,
            borderRadius: '50%', backgroundColor: '#1C1C1C', opacity: 0.15,
          }} />
        );
      })}

      {/* Camera wrapper */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Top eyebrow label */}
        <div style={{
          position: 'absolute', top: 90, left: 110,
          display: 'flex', alignItems: 'center', gap: 18,
          transform: `translateY(${labelY}px)`, opacity: labelOp,
        }}>
          {/* Icon: warning triangle SVG */}
          <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
            <path d="M12 2 L22 20 L2 20 Z" stroke="#1C1C1C" strokeWidth="2" fill="none" />
            <line x1="12" y1="9" x2="12" y2="14" stroke="#1C1C1C" strokeWidth="2" />
            <circle cx="12" cy="17" r="1.2" fill="#1C1C1C" />
          </svg>
          <span style={{
            fontFamily: 'Inter, Poppins, sans-serif', fontSize: 32, fontWeight: 600,
            color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
          }}>STRUCTURAL SHIFT — Chapter 02</span>
        </div>

        {/* Horizontal divider line growing */}
        <div style={{
          position: 'absolute', top: 160, left: 110,
          height: 2, width: `${interpolate(sp(20), [0, 1], [0, 1700])}px`,
          backgroundColor: '#1C1C1C', opacity: 0.4,
        }} />

        {/* MAIN STAGE — three-beat reveal */}
        <div style={{
          position: 'absolute', top: 260, left: 110, right: 110,
        }}>
          {/* Beat 1: "ये कोई" small */}
          <div style={{
            fontFamily: 'Yantra One, serif', fontSize: 72, color: '#2D2D2D',
            opacity: sp(30), transform: `translateX(${interpolate(sp(30),[0,1],[-30,0])}px)`,
            marginBottom: 12,
          }}>
            ये कोई
          </div>

          {/* Beat 2: word "अस्थाई" with strikethrough */}
          <div style={{
            position: 'relative', display: 'inline-block',
            opacity: sp(45), transform: `translateY(${interpolate(sp(45),[0,1],[40,0])}px)`,
            marginBottom: 30,
          }}>
            <span style={{
              fontFamily: 'Yantra One, serif', fontSize: 200, color: '#1C1C1C',
              fontWeight: 700, lineHeight: 1,
            }}>{word1}</span>
            {/* Strike line */}
            <div style={{
              position: 'absolute', top: '52%', left: 0,
              width: `${strikeWidth}%`, height: 10,
              backgroundColor: '#3B2A1E',
              transform: 'rotate(-3deg)',
            }} />
            <span style={{
              fontFamily: 'Yantra One, serif', fontSize: 200, color: '#1C1C1C',
              fontWeight: 700, marginLeft: 30,
            }}>{word2}</span>
          </div>

          {/* Beat 3: huge replacement word */}
          <div style={{
            display: 'flex', alignItems: 'center', gap: 40, marginTop: 30,
          }}>
            <div style={{
              fontFamily: 'Inter, sans-serif', fontSize: 90, fontWeight: 800,
              color: '#3B2A1E', opacity: bigOp,
              transform: `scale(${bigScale}) translateY(${interpolate(bigOp,[0,1],[60,0])}px)`,
              transformOrigin: 'left center',
            }}>→</div>

            <div style={{ position: 'relative' }}>
              <span style={{
                fontFamily: 'Yantra One, serif', fontSize: 240,
                color: '#1C1C1C', fontWeight: 700, lineHeight: 1,
                opacity: bigOp,
                transform: `scale(${bigScale})`, display: 'inline-block',
                transformOrigin: 'left bottom',
              }}>
                {word3}
              </span>
              {/* Bold underline */}
              <div style={{
                position: 'absolute', bottom: -18, left: 0,
                width: `${underlineW}%`, height: 14,
                backgroundColor: '#3B2A1E',
              }} />
            </div>
          </div>

          {/* Sub caption */}
          <div style={{
            marginTop: 60,
            fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 50,
            color: '#2D2D2D', fontWeight: 500, opacity: sp(170),
            transform: `translateY(${interpolate(sp(170),[0,1],[20,0])}px)`,
            maxWidth: 1400, lineHeight: 1.4,
          }}>
            खाने की कीमतों का <span style={{ color: '#3B2A1E', fontWeight: 800 }}>स्थाई बदलाव</span> — दशकों के फैसलों का नतीजा
          </div>
        </div>

        {/* Stick figure — Buffett right corner */}
        <g style={{
          position: 'absolute',
        }}>
          <svg width="180" height="280" viewBox="0 0 180 280" style={{
            position: 'absolute', right: 120, bottom: 120,
            transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(60)})`,
            transformOrigin: 'bottom center',
          }}>
            {/* Golden glow */}
            <ellipse cx="90" cy="50" rx="55" ry="55" fill="#FFC107" opacity="0.18" />
            {/* Head */}
            <circle cx="90" cy="50" r="32" fill="none" stroke="#1C1C1C" strokeWidth="3.5"
              transform={`rotate(${Math.sin(frame * 0.04) * 2} 90 50)`} />
            {/* Glasses */}
            <circle cx="78" cy="48" r="9" fill="none" stroke="#FFC107" strokeWidth="3" />
            <circle cx="102" cy="48" r="9" fill="none" stroke="#FFC107" strokeWidth="3" />
            <line x1="87" y1="48" x2="93" y2="48" stroke="#FFC107" strokeWidth="3" />
            {/* Body */}
            <line x1="90" y1="82" x2="90" y2="180" stroke="#1C1C1C" strokeWidth="3.5" />
            {/* Arms — one pointing at text */}
            <line x1="90" y1="110" x2="40" y2="130" stroke="#1C1C1C" strokeWidth="3.5" />
            <line x1="90" y1="110" x2="140" y2="95" stroke="#1C1C1C" strokeWidth="3.5" />
            {/* Legs */}
            <line x1="90" y1="180" x2="65" y2="250" stroke="#1C1C1C" strokeWidth="3.5" />
            <line x1="90" y1="180" x2="115" y2="250" stroke="#1C1C1C" strokeWidth="3.5" />
          </svg>
        </g>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene1_StructuralShift;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene2_DecadesOfDecisions',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene2_DecadesOfDecisions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Timeline progress
  const timelineW = interpolate(frame, [40, 140], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.65, 0, 0.35, 1),
  });

  const years = [
    { y: '1991', label: 'Liberalization', delay: 50 },
    { y: '2008', label: 'Global Crisis', delay: 75 },
    { y: '2016', label: 'Demonetization', delay: 100 },
    { y: '2020', label: 'Pandemic Print', delay: 125 },
    { y: '2026', label: 'You Are Here', delay: 150 },
  ];

  const camY = Math.sin(frame * 0.025) * 4;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EDEAE4', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(255,255,255,0.5) 0%, transparent 50%), radial-gradient(circle at 20% 80%, rgba(59,42,30,0.06) 0%, transparent 55%)',
      }} />

      {/* Shape layer — drifting squares */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 7);
        const size = 250 + r * 280;
        const x = 200 + r * 1400;
        const y = 100 + r * 700;
        return (
          <div key={`sq-${i}`} style={{
            position: 'absolute', left: x, top: y, width: size, height: size,
            border: '1.5px solid rgba(28,28,28,0.05)',
            transform: `rotate(${frame * 0.08 + i * 25}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.04})`,
          }} />
        );
      })}

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        {Array.from({ length: 24 }).map((_, row) =>
          Array.from({ length: 40 }).map((_, col) => (
            <div key={`dot-${row}-${col}`} style={{
              position: 'absolute', left: col * 50 + 20, top: row * 50 + 20,
              width: 3, height: 3, borderRadius: '50%', backgroundColor: '#1C1C1C',
            }} />
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const r = seededRandom(i + 100);
        const px = (r * 1920 + frame * 0.4) % 1920;
        const py = (r * 1080 + frame * 0.6) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py, width: 5, height: 5,
            backgroundColor: '#3B2A1E', opacity: 0.18, borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>

        {/* Top label */}
        <div style={{
          position: 'absolute', top: 80, left: 110,
          display: 'flex', alignItems: 'center', gap: 18,
          opacity: sp(8),
        }}>
          <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#1C1C1C" strokeWidth="2" />
            <polyline points="12,7 12,12 16,14" stroke="#1C1C1C" strokeWidth="2" fill="none" />
          </svg>
          <span style={{
            fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 600,
            color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
          }}>The Decisions Timeline</span>
        </div>

        {/* Main title */}
        <div style={{
          position: 'absolute', top: 180, left: 110, right: 110,
          fontFamily: 'Yantra One, serif', fontSize: 130, fontWeight: 700,
          color: '#1C1C1C', lineHeight: 1.05,
          opacity: sp(15), transform: `translateY(${interpolate(sp(15),[0,1],[40,0])}px)`,
        }}>
          दशकों के <span style={{ color: '#3B2A1E' }}>फैसले</span>,<br />
          आज का <span style={{ background: '#1C1C1C', color: '#EDEAE4', padding: '0 24px' }}>नतीजा</span>
        </div>

        {/* TIMELINE */}
        <div style={{
          position: 'absolute', top: 620, left: 140, right: 140,
        }}>
          {/* Base line */}
          <div style={{
            position: 'relative', height: 6, backgroundColor: 'rgba(28,28,28,0.15)',
            borderRadius: 3,
          }}>
            {/* Filled */}
            <div style={{
              position: 'absolute', left: 0, top: 0, height: 6, borderRadius: 3,
              width: `${timelineW}%`, backgroundColor: '#3B2A1E',
            }} />
          </div>

          {/* Year nodes */}
          {years.map((yr, i) => {
            const pos = (i / (years.length - 1)) * 100;
            const nodeOp = sp(yr.delay);
            const nodeScale = sp(yr.delay, { damping: 10, stiffness: 130 });
            const isLast = i === years.length - 1;
            return (
              <div key={yr.y} style={{
                position: 'absolute', left: `${pos}%`, top: -22,
                transform: `translateX(-50%) scale(${nodeScale})`,
                opacity: nodeOp,
              }}>
                {/* Dot */}
                <div style={{
                  width: 50, height: 50, borderRadius: '50%',
                  backgroundColor: isLast ? '#3B2A1E' : '#1C1C1C',
                  border: '4px solid #EDEAE4',
                  boxShadow: isLast ? '0 0 0 6px rgba(59,42,30,0.25)' : 'none',
                }} />
                {/* Year */}
                <div style={{
                  position: 'absolute', top: 70, left: '50%', transform: 'translateX(-50%)',
                  fontFamily: 'Inter, sans-serif', fontSize: 46, fontWeight: 800,
                  color: '#1C1C1C', whiteSpace: 'nowrap',
                }}>{yr.y}</div>
                {/* Label */}
                <div style={{
                  position: 'absolute', top: 130, left: '50%', transform: 'translateX(-50%)',
                  fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 500,
                  color: '#2D2D2D', whiteSpace: 'nowrap', letterSpacing: 1,
                  textTransform: 'uppercase',
                }}>{yr.label}</div>
              </div>
            );
          })}
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene2_DecadesOfDecisions;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene3_NeverComingBack',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene3_NeverComingBack: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Arrow path animation — goes up, never comes down
  const pathProgress = interpolate(frame, [30, 180], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.5, 0, 0.5, 1),
  });

  // Crossed-out arrow (down arrow with X)
  const crossOp = sp(140);
  const shake = frame > 150 && frame < 175 ? Math.sin(frame * 2) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EDEAE4', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #EDEAE4 0%, #E0DDD5 100%)',
      }} />

      {/* Shape layer — large triangles */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 12);
        const size = 400 + r * 300;
        const x = 100 + i * 600;
        const y = 100 + r * 500;
        return (
          <svg key={`tr-${i}`} width={size} height={size} style={{
            position: 'absolute', left: x, top: y,
            transform: `rotate(${frame * 0.05 + i * 60}deg)`, opacity: 0.05,
          }}>
            <polygon points={`${size/2},10 ${size-10},${size-10} 10,${size-10}`}
              fill="none" stroke="#1C1C1C" strokeWidth="2" />
          </svg>
        );
      })}

      {/* Texture — diagonal lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 60 }).map((_, i) => (
          <div key={`dl-${i}`} style={{
            position: 'absolute', left: -200, top: i * 50,
            width: 2400, height: 1, backgroundColor: '#1C1C1C',
            transform: 'rotate(-25deg)', transformOrigin: 'left',
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 200);
        const px = (r * 1920 + frame * 0.5) % 1920;
        const py = ((r * 1080) - frame * 0.8 + 1080) % 1080;
        return (
          <div key={`pt-${i}`} style={{
            position: 'absolute', left: px, top: py, width: 4, height: 4,
            backgroundColor: '#3B2A1E', opacity: 0.2, borderRadius: '50%',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 80, left: 110,
        display: 'flex', alignItems: 'center', gap: 18,
        opacity: sp(8), transform: `translateX(${shake}px)`,
      }}>
        <svg width="42" height="42" viewBox="0 0 24 24" fill="none">
          <polyline points="3,17 9,11 13,15 21,7" stroke="#1C1C1C" strokeWidth="2.5" fill="none" />
          <polyline points="15,7 21,7 21,13" stroke="#1C1C1C" strokeWidth="2.5" fill="none" />
        </svg>
        <span style={{
          fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
        }}>One-Way Street</span>
      </div>

      {/* Main statement */}
      <div style={{
        position: 'absolute', top: 200, left: 110,
        fontFamily: 'Yantra One, serif', fontSize: 110, fontWeight: 700,
        color: '#1C1C1C', lineHeight: 1.1,
        opacity: sp(15), transform: `translateY(${interpolate(sp(15),[0,1],[30,0])}px)`,
      }}>
        2019 के लेवल पर
      </div>

      <div style={{
        position: 'absolute', top: 330, left: 110,
        opacity: sp(45),
      }}>
        <span style={{
          fontFamily: 'Yantra One, serif', fontSize: 220, fontWeight: 700,
          color: '#3B2A1E', lineHeight: 1,
        }}>कभी नहीं</span>
        <span style={{
          fontFamily: 'Yantra One, serif', fontSize: 220, fontWeight: 700,
          color: '#1C1C1C', lineHeight: 1, marginLeft: 30,
        }}>आएगा</span>
      </div>

      {/* CHART — prices going up, never down */}
      <div style={{
        position: 'absolute', bottom: 120, left: 110, width: 900, height: 280,
        opacity: sp(30),
      }}>
        {/* Axes */}
        <div style={{ position: 'absolute', left: 0, bottom: 0, width: '100%', height: 2, backgroundColor: '#1C1C1C' }} />
        <div style={{ position: 'absolute', left: 0, bottom: 0, width: 2, height: '100%', backgroundColor: '#1C1C1C' }} />

        {/* SVG line chart */}
        <svg width="900" height="280" style={{ position: 'absolute', left: 0, top: 0 }}>
          <defs>
            <clipPath id="revealClip">
              <rect x="0" y="0" width={pathProgress * 900} height="280" />
            </clipPath>
          </defs>
          {/* Reference dashed line — 2019 level */}
          <line x1="0" y1="220" x2="900" y2="220" stroke="#1C1C1C" strokeWidth="2" strokeDasharray="8,8" opacity="0.4" />
          {/* Step-up price path */}
          <polyline
            points="20,220 150,220 200,180 350,180 400,130 550,130 600,80 750,80 800,40 880,40"
            fill="none" stroke="#3B2A1E" strokeWidth="6"
            clipPath="url(#revealClip)"
          />
          {/* Price dots */}
          {[[200,180],[400,130],[600,80],[800,40]].map(([x,y], i) => (
            <circle key={i} cx={x} cy={y} r="9" fill="#1C1C1C"
              opacity={pathProgress > (i+1)/5 ? 1 : 0} />
          ))}
        </svg>

        {/* Labels */}
        <div style={{
          position: 'absolute', left: -90, bottom: 215,
          fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 700, color: '#2D2D2D',
        }}>₹5K</div>
        <div style={{
          position: 'absolute', left: -90, top: 30,
          fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 700, color: '#3B2A1E',
        }}>₹9K+</div>
        <div style={{
          position: 'absolute', left: 0, top: -34,
          fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 600, color: '#1C1C1C',
          letterSpacing: 2, textTransform: 'uppercase',
        }}>Ratchet Effect — Prices only climb</div>
      </div>

      {/* Right side: crossed "down arrow" */}
      <div style={{
        position: 'absolute', right: 160, bottom: 220,
        opacity: crossOp, transform: `scale(${sp(140, { damping: 10, stiffness: 130 })})`,
      }}>
        <svg width="280" height="280" viewBox="0 0 100 100">
          {/* Down arrow */}
          <line x1="50" y1="15" x2="50" y2="80" stroke="#1C1C1C" strokeWidth="6" opacity="0.5" />
          <polyline points="30,65 50,85 70,65" stroke="#1C1C1C" strokeWidth="6" fill="none" opacity="0.5" />
          {/* Big X across it */}
          <line x1="15" y1="15" x2="85" y2="85" stroke="#3B2A1E" strokeWidth="8" />
          <line x1="85" y1="15" x2="15" y2="85" stroke="#3B2A1E" strokeWidth="8" />
        </svg>
        <div style={{
          textAlign: 'center', marginTop: 12,
          fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700,
          color: '#3B2A1E', letterSpacing: 2, textTransform: 'uppercase',
        }}>NO Return</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene3_NeverComingBack;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene4_RupeeWeakening',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene4_RupeeWeakening: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Rupee value counter 70 -> 95
  const rupeeVal = Math.round(interpolate(frame, [50, 150], [70, 95], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.65, 0, 0.35, 1),
  }));

  // Money supply bars growth
  const bar2019 = sp(60);
  const bar2026 = sp(110);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EDEAE4', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 20% 20%, rgba(255,255,255,0.55) 0%, transparent 50%)',
      }} />

      {/* Shape layer — overlapping circles */}
      {[0, 1, 2, 3, 4, 5].map((i) => {
        const r = seededRandom(i + 19);
        const size = 200 + r * 350;
        return (
          <div key={`c-${i}`} style={{
            position: 'absolute',
            left: 100 + r * 1600 + Math.sin(frame * 0.01 + i) * 20,
            top: 50 + r * 850 + Math.cos(frame * 0.012 + i) * 20,
            width: size, height: size, borderRadius: '50%',
            border: '1.5px solid rgba(59,42,30,0.06)',
          }} />
        );
      })}

      {/* Texture — horizontal lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={`hl-${i}`} style={{
            position: 'absolute', left: 0, top: i * 40,
            width: '100%', height: 1, backgroundColor: '#1C1C1C',
          }} />
        ))}
      </AbsoluteFill>

      {/* Falling rupee particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const r = seededRandom(i + 333);
        const px = r * 1920;
        const py = ((r * 1080) + frame * 1.2 + i * 80) % 1180 - 100;
        return (
          <div key={`rp-${i}`} style={{
            position: 'absolute', left: px, top: py,
            fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 700,
            color: '#3B2A1E', opacity: 0.12,
          }}>₹</div>
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 80, left: 110,
        display: 'flex', alignItems: 'center', gap: 18,
        opacity: sp(8),
      }}>
        <svg width="42" height="42" viewBox="0 0 24 24" fill="none">
          <path d="M6 3h10M6 8h10M9 13l6 8M6 13h7a4 4 0 0 0 0-8" stroke="#1C1C1C" strokeWidth="2" fill="none" />
        </svg>
        <span style={{
          fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
        }}>Currency Devaluation</span>
      </div>

      {/* Title */}
      <div style={{
        position: 'absolute', top: 175, left: 110,
        fontFamily: 'Yantra One, serif', fontSize: 110, fontWeight: 700,
        color: '#1C1C1C', lineHeight: 1.05,
        opacity: sp(15),
      }}>
        रुपया <span style={{ color: '#3B2A1E' }}>कमजोर</span>,<br />
        छपाई <span style={{ background: '#3B2A1E', color: '#EDEAE4', padding: '0 24px' }}>तेज़</span>
      </div>

      {/* LEFT: USD/INR counter */}
      <div style={{
        position: 'absolute', top: 480, left: 110, width: 800,
        opacity: sp(30),
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 3, textTransform: 'uppercase', marginBottom: 20,
        }}>USD / INR — Exchange Rate</div>

        <div style={{ display: 'flex', alignItems: 'baseline', gap: 30 }}>
          <span style={{
            fontFamily: 'Inter, sans-serif', fontSize: 50, fontWeight: 600, color: '#9E9E9E',
          }}>₹</span>
          <span style={{
            fontFamily: 'Inter, sans-serif', fontSize: 280, fontWeight: 900,
            color: '#1C1C1C', lineHeight: 1, fontVariantNumeric: 'tabular-nums',
          }}>{rupeeVal}</span>
          <span style={{
            fontFamily: 'Inter, sans-serif', fontSize: 60, fontWeight: 700,
            color: '#3B2A1E',
          }}>/$</span>
        </div>

        {/* From -> Now indicator */}
        <div style={{
          display: 'flex', alignItems: 'center', gap: 20, marginTop: 30,
          opacity: sp(130),
        }}>
          <div style={{
            padding: '10px 24px', backgroundColor: 'rgba(28,28,28,0.08)',
            borderRadius: 8,
            fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 700, color: '#2D2D2D',
          }}>2019 · ₹70</div>
          <span style={{ fontSize: 36, color: '#3B2A1E' }}>→</span>
          <div style={{
            padding: '10px 24px', backgroundColor: '#3B2A1E',
            borderRadius: 8,
            fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 700, color: '#EDEAE4',
          }}>2026 · ₹95</div>
        </div>
      </div>

      {/* RIGHT: Money supply bars */}
      <div style={{
        position: 'absolute', top: 480, right: 130, width: 600,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 3, textTransform: 'uppercase', marginBottom: 30,
          opacity: sp(40),
        }}>Money Supply (M3)</div>

        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 60, height: 380 }}>
          {/* 2019 bar */}
          <div style={{ textAlign: 'center' }}>
            <div style={{
              width: 160, height: 180 * bar2019,
              backgroundColor: '#1C1C1C', borderRadius: '8px 8px 0 0',
            }} />
            <div style={{
              fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 800,
              color: '#1C1C1C', marginTop: 14,
            }}>2019</div>
          </div>
          {/* 2026 bar */}
          <div style={{ textAlign: 'center' }}>
            <div style={{
              width: 160, height: 360 * bar2026,
              backgroundColor: '#3B2A1E', borderRadius: '8px 8px 0 0',
              position: 'relative',
            }}>
              <div style={{
                position: 'absolute', top: -50, left: '50%', transform: 'translateX(-50%)',
                fontFamily: 'Inter, sans-serif', fontSize: 38, fontWeight: 900,
                color: '#3B2A1E', opacity: bar2026,
              }}>+2x</div>
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 800,
              color: '#3B2A1E', marginTop: 14,
            }}>2026</div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene4_RupeeWeakening;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene5_EnergyPermanent',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene5_EnergyPermanent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Oil drop fill
  const oilFill = interpolate(frame, [40, 130], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.65, 0, 0.35, 1),
  });

  const items = [
    { icon: 'tractor', label: 'डीज़ल — Tractors', delay: 70 },
    { icon: 'bolt', label: 'बिजली — Cold Storage', delay: 95 },
    { icon: 'flask', label: 'गैस — Fertilizer', delay: 120 },
    { icon: 'truck', label: 'पेट्रोल — Transport', delay: 145 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#EDEAE4', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 80% 30%, rgba(255,255,255,0.5) 0%, transparent 50%), radial-gradient(circle at 10% 90%, rgba(59,42,30,0.07) 0%, transparent 55%)',
      }} />

      {/* Shape layer */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 27);
        const size = 350 + r * 250;
        return (
          <svg key={`hx-${i}`} width={size} height={size} style={{
            position: 'absolute',
            left: 100 + r * 1500, top: 50 + r * 800,
            transform: `rotate(${frame * 0.07 + i * 45}deg)`, opacity: 0.05,
          }}>
            <polygon
              points={`${size/2},10 ${size-30},${size/4} ${size-30},${size*3/4} ${size/2},${size-10} 30,${size*3/4} 30,${size/4}`}
              fill="none" stroke="#1C1C1C" strokeWidth="2"
            />
          </svg>
        );
      })}

      {/* Texture — vertical thick rule */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <div key={`vr-${i}`} style={{
            position: 'absolute', left: i * 80, top: 0, width: 2, height: '100%',
            backgroundColor: '#1C1C1C',
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 411);
        const px = (r * 1920 + frame * 0.4) % 1920;
        const py = (r * 1080 + frame * 0.6) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py, width: 4, height: 4,
            backgroundColor: '#1C1C1C', opacity: 0.15, borderRadius: '50%',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 80, left: 110,
        display: 'flex', alignItems: 'center', gap: 18,
        opacity: sp(8),
      }}>
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
          <polyline points="13,2 4,14 12,14 11,22 20,10 12,10 13,2" stroke="#1C1C1C" strokeWidth="2" fill="none" />
        </svg>
        <span style={{
          fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
        }}>Energy — Permanently Higher</span>
      </div>

      {/* Title */}
      <div style={{
        position: 'absolute', top: 180, left: 110, width: 1100,
        fontFamily: 'Yantra One, serif', fontSize: 130, fontWeight: 700,
        color: '#1C1C1C', lineHeight: 1.05,
        opacity: sp(15), transform: `translateY(${interpolate(sp(15),[0,1],[40,0])}px)`,
      }}>
        उर्जा का <br />
        <span style={{ color: '#3B2A1E' }}>नया फर्श</span>
      </div>

      {/* RIGHT: Filling oil drop */}
      <div style={{
        position: 'absolute', top: 200, right: 180, width: 360, height: 480,
        opacity: sp(25),
      }}>
        <svg viewBox="0 0 200 280" width="360" height="480">
          <defs>
            <clipPath id="dropClip">
              <path d="M100,10 C100,10 30,110 30,180 C30,235 60,270 100,270 C140,270 170,235 170,180 C170,110 100,10 100,10 Z" />
            </clipPath>
          </defs>
          {/* Outline */}
          <path d="M100,10 C100,10 30,110 30,180 C30,235 60,270 100,270 C140,270 170,235 170,180 C170,110 100,10 100,10 Z"
            fill="none" stroke="#1C1C1C" strokeWidth="4" />
          {/* Fill */}
          <rect x="0" y={280 - oilFill * 260} width="200" height="280"
            fill="#3B2A1E" clipPath="url(#dropClip)" />
          {/* Highlight */}
          <ellipse cx="75" cy="80" rx="12" ry="22" fill="#EDEAE4" opacity="0.3" />
        </svg>
        <div style={{
          textAlign: 'center', marginTop: 10,
          fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 700,
          color: '#3B2A1E', letterSpacing: 2,
        }}>हर कैलोरी में घुसा</div>
      </div>

      {/* BOTTOM: Four cost drivers */}
      <div style={{
        position: 'absolute', bottom: 110, left: 110, right: 110,
        display: 'flex', justifyContent: 'space-between', gap: 30,
      }}>
        {items.map((it, i) => {
          const op = sp(it.delay);
          const tx = interpolate(sp(it.delay), [0, 1], [40, 0]);
          return (
            <div key={i} style={{
              flex: 1, padding: '30px 24px',
              backgroundColor: 'rgba(28,28,28,0.06)',
              border: '2px solid rgba(28,28,28,0.15)',
              borderRadius: 12,
              opacity: op, transform: `translateY(${tx}px)`,
            }}>
              <div style={{ marginBottom: 16 }}>
                {it.icon === 'tractor' && (
                  <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                    <circle cx="7" cy="18" r="3" stroke="#1C1C1C" strokeWidth="2" />
                    <circle cx="17" cy="18" r="3" stroke="#1C1C1C" strokeWidth="2" />
                    <rect x="9" y="6" width="8" height="8" stroke="#1C1C1C" strokeWidth="2" fill="none" />
                    <line x1="3" y1="14" x2="9" y2="14" stroke="#1C1C1C" strokeWidth="2" />
                  </svg>
                )}
                {it.icon === 'bolt' && (
                  <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                    <polygon points="13,2 4,14 12,14 11,22 20,10 12,10 13,2" stroke="#1C1C1C" strokeWidth="2" fill="none" />
                  </svg>
                )}
                {it.icon === 'flask' && (
                  <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                    <path d="M9 2v6L4 20a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2L15 8V2" stroke="#1C1C1C" strokeWidth="2" fill="none" />
                    <line x1="8" y1="2" x2="16" y2="2" stroke="#1C1C1C" strokeWidth="2" />
                  </svg>
                )}
                {it.icon === 'truck' && (
                  <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                    <rect x="1" y="6" width="13" height="10" stroke="#1C1C1C" strokeWidth="2" fill="none" />
                    <polyline points="14,9 19,9 22,13 22,16 14,16" stroke="#1C1C1C" strokeWidth="2" fill="none" />
                    <circle cx="6" cy="18" r="2" stroke="#1C1C1C" strokeWidth="2" />
                    <circle cx="18" cy="18" r="2" stroke="#1C1C1C" strokeWidth="2" />
                  </svg>
                )}
              </div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 30,
                fontWeight: 700, color: '#1C1C1C', lineHeight: 1.3,
              }}>{it.label}</div>
            </div>
          );
        })}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene5_EnergyPermanent;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene6_NewEquilibrium',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene6_NewEquilibrium: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Scale tilt — left heavy in beginning, balances to new equilibrium (higher)
  const tilt = interpolate(frame, [30, 90, 150], [-15, 5, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.5, 0, 0.3, 1),
  });

  // Right side rises (new normal)
  const rightRise = interpolate(frame, [90, 160], [0, -80], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.5, 0, 0.3, 1),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EDEAE4', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #EDEAE4 0%, #DFDBD3 100%)',
      }} />

      {/* Shape layer — concentric rings center */}
      {[0, 1, 2].map((i) => (
        <div key={`ring-${i}`} style={{
          position: 'absolute', left: '50%', top: '50%',
          width: 800 + i * 200, height: 800 + i * 200,
          borderRadius: '50%',
          border: '1.5px solid rgba(59,42,30,0.05)',
          transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.015 + i) * 0.02})`,
        }} />
      ))}

      {/* Texture — crosshatch */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={`xh1-${i}`} style={{
            position: 'absolute', left: -300, top: i * 60,
            width: 2600, height: 1, backgroundColor: '#1C1C1C',
            transform: 'rotate(20deg)', transformOrigin: 'left',
          }} />
        ))}
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={`xh2-${i}`} style={{
            position: 'absolute', left: -300, top: i * 60,
            width: 2600, height: 1, backgroundColor: '#1C1C1C',
            transform: 'rotate(-20deg)', transformOrigin: 'left',
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 500);
        const px = (r * 1920 + frame * 0.3) % 1920;
        const py = (r * 1080 + frame * 0.5) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py, width: 5, height: 5,
            backgroundColor: '#3B2A1E', opacity: 0.15, borderRadius: '50%',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 80, left: 110,
        display: 'flex', alignItems: 'center', gap: 18,
        opacity: sp(8),
      }}>
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
          <line x1="12" y1="3" x2="12" y2="21" stroke="#1C1C1C" strokeWidth="2" />
          <polyline points="4,8 12,4 20,8" stroke="#1C1C1C" strokeWidth="2" fill="none" />
          <circle cx="6" cy="12" r="3" stroke="#1C1C1C" strokeWidth="2" />
          <circle cx="18" cy="12" r="3" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
        <span style={{
          fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
        }}>New Equilibrium</span>
      </div>

      {/* Title */}
      <div style={{
        position: 'absolute', top: 180, left: 110, right: 110,
        textAlign: 'center',
        fontFamily: 'Yantra One, serif', fontSize: 110, fontWeight: 700,
        color: '#1C1C1C', lineHeight: 1.1,
        opacity: sp(15),
      }}>
        खाद्य सिस्टम का{' '}
        <span style={{
          background: '#3B2A1E', color: '#EDEAE4', padding: '0 28px',
          display: 'inline-block',
        }}>नया संतुलन</span>
      </div>

      {/* CENTER: Balance scale animation */}
      <div style={{
        position: 'absolute', top: 460, left: '50%',
        transform: `translateX(-50%)`,
        width: 800, height: 400,
      }}>
        {/* Pole */}
        <div style={{
          position: 'absolute', left: '50%', top: 80, width: 6, height: 300,
          backgroundColor: '#1C1C1C', transform: 'translateX(-50%)',
        }} />
        {/* Base */}
        <div style={{
          position: 'absolute', left: '50%', bottom: 0, width: 200, height: 14,
          backgroundColor: '#1C1C1C', transform: 'translateX(-50%)', borderRadius: 4,
        }} />

        {/* Beam — rotates */}
        <div style={{
          position: 'absolute', left: '50%', top: 80, width: 700, height: 8,
          backgroundColor: '#1C1C1C', transform: `translateX(-50%) rotate(${tilt}deg)`,
          transformOrigin: 'center', borderRadius: 4,
        }}>
          {/* Left plate — "पुराना" */}
          <div style={{
            position: 'absolute', left: 20, top: 8,
            width: 200, height: 200,
            transform: `translateY(${tilt < 0 ? -tilt * 4 : 0}px)`,
          }}>
            <div style={{ width: 2, height: 60, backgroundColor: '#1C1C1C', margin: '0 auto' }} />
            <div style={{
              width: 200, height: 140, border: '4px solid #1C1C1C',
              borderRadius: '0 0 100px 100px', position: 'relative',
              backgroundColor: 'rgba(28,28,28,0.06)',
            }}>
              <div style={{
                position: 'absolute', top: '50%', left: '50%',
                transform: 'translate(-50%, -50%)',
                fontFamily: 'Yantra One, serif', fontSize: 36, fontWeight: 700,
                color: '#2D2D2D',
              }}>पुराना</div>
            </div>
          </div>

          {/* Right plate — "नया (HIGHER)" */}
          <div style={{
            position: 'absolute', right: 20, top: 8,
            width: 200, height: 200,
            transform: `translateY(${rightRise}px)`,
          }}>
            <div style={{ width: 2, height: 60, backgroundColor: '#3B2A1E', margin: '0 auto' }} />
            <div style={{
              width: 200, height: 140, border: '4px solid #3B2A1E',
              borderRadius: '0 0 100px 100px', position: 'relative',
              backgroundColor: '#3B2A1E',
            }}>
              <div style={{
                position: 'absolute', top: '50%', left: '50%',
                transform: 'translate(-50%, -50%)',
                fontFamily: 'Yantra One, serif', fontSize: 36, fontWeight: 700,
                color: '#EDEAE4',
              }}>नया</div>
            </div>
          </div>
        </div>

        {/* Up arrow on right indicating risen */}
        <div style={{
          position: 'absolute', right: -40, top: 0,
          opacity: sp(140),
        }}>
          <svg width="60" height="80" viewBox="0 0 24 32">
            <line x1="12" y1="4" x2="12" y2="28" stroke="#3B2A1E" strokeWidth="4" />
            <polyline points="4,12 12,4 20,12" stroke="#3B2A1E" strokeWidth="4" fill="none" />
          </svg>
        </div>
      </div>

      {/* Bottom caption */}
      <div style={{
        position: 'absolute', bottom: 90, left: 110, right: 110, textAlign: 'center',
        fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 44,
        fontWeight: 500, color: '#2D2D2D',
        opacity: sp(170),
      }}>
        Supply, Demand, Cost — सब reset हो चुका है <span style={{ color: '#3B2A1E', fontWeight: 700 }}>ऊंचे level पर</span>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene6_NewEquilibrium;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene7_TheShocks',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene7_TheShocks: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake at impact frames
  const shake1 = frame > 80 && frame < 95 ? Math.sin(frame * 2.5) * 5 : 0;
  const shake2 = frame > 130 && frame < 145 ? Math.sin(frame * 2.5) * 5 : 0;

  const shocks = [
    { num: '01', label: 'तेल के दाम', sub: 'Oil Prices ↑', delay: 50 },
    { num: '02', label: 'मज़दूरी', sub: 'Labor Cost ↑', delay: 90 },
    { num: '03', label: 'सप्लाई चेन', sub: 'Supply Chain ↑', delay: 130 },
  ];

  return (
    <AbsoluteFill style={{
      backgroundColor: '#EDEAE4', overflow: 'hidden',
      opacity: exitOp, transform: `translate(${shake1 + shake2}px, ${shake1}px)`,
    }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 0%, rgba(255,255,255,0.5) 0%, transparent 60%)',
      }} />

      {/* Shape layer */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 33);
        const size = 280 + r * 320;
        return (
          <div key={`bk-${i}`} style={{
            position: 'absolute',
            left: 100 + r * 1500, top: 50 + r * 750,
            width: size, height: size,
            border: '1.5px solid rgba(59,42,30,0.06)',
            transform: `rotate(${frame * 0.06 + i * 35}deg)`,
          }} />
        );
      })}

      {/* Texture — concentric circle ripple from center */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {[0, 1, 2, 3, 4].map((i) => {
          const ripple = (frame * 2 + i * 60) % 300;
          return (
            <div key={`rp-${i}`} style={{
              position: 'absolute', left: '50%', top: '50%',
              width: ripple * 4, height: ripple * 4,
              borderRadius: '50%',
              border: '2px solid #1C1C1C',
              transform: 'translate(-50%, -50%)',
              opacity: 1 - ripple / 300,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 600);
        const px = (r * 1920 + frame * 0.4) % 1920;
        const py = (r * 1080 + frame * 0.7) % 1080;
        return (
          <div key={`pt-${i}`} style={{
            position: 'absolute', left: px, top: py, width: 4, height: 4,
            backgroundColor: '#1C1C1C', opacity: 0.15, borderRadius: '50%',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 80, left: 110,
        display: 'flex', alignItems: 'center', gap: 18,
        opacity: sp(8),
      }}>
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
          <polygon points="12,2 15,9 22,10 17,15 18,22 12,18 6,22 7,15 2,10 9,9" stroke="#1C1C1C" strokeWidth="2" fill="none" />
        </svg>
        <span style={{
          fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
        }}>The Shocks Theory</span>
      </div>

      {/* Title */}
      <div style={{
        position: 'absolute', top: 180, left: 110,
        fontFamily: 'Yantra One, serif', fontSize: 120, fontWeight: 700,
        color: '#1C1C1C', lineHeight: 1.05,
        opacity: sp(15), transform: `translateY(${interpolate(sp(15),[0,1],[40,0])}px)`,
      }}>
        ये सब <span style={{ color: '#3B2A1E' }}>झटके</span> थे —<br />
        ऐसा <span style={{ textDecoration: 'line-through', textDecorationThickness: 8 }}>लगता</span> है
      </div>

      {/* THREE shock cards */}
      <div style={{
        position: 'absolute', top: 580, left: 110, right: 110,
        display: 'flex', gap: 40, justifyContent: 'space-between',
      }}>
        {shocks.map((s, i) => {
          const op = sp(s.delay);
          const ty = interpolate(sp(s.delay), [0, 1], [60, 0]);
          const cardShake = (i === 1 && shake1 !== 0) || (i === 2 && shake2 !== 0) ? Math.sin(frame * 3) * 3 : 0;
          return (
            <div key={i} style={{
              flex: 1, padding: '40px 30px',
              backgroundColor: '#1C1C1C',
              borderRadius: 16,
              opacity: op, transform: `translateY(${ty}px) translateX(${cardShake}px)`,
              position: 'relative',
            }}>
              {/* Big number */}
              <div style={{
                fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 700,
                color: '#3B2A1E', letterSpacing: 4, marginBottom: 20,
              }}>{s.num}</div>

              <div style={{
                fontFamily: 'Yantra One, serif', fontSize: 70, fontWeight: 700,
                color: '#EDEAE4', lineHeight: 1.1, marginBottom: 16,
              }}>{s.label}</div>

              <div style={{
                fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 500,
                color: '#9E9E9E', letterSpacing: 1,
              }}>{s.sub}</div>

              {/* Up arrow */}
              <div style={{ position: 'absolute', top: 30, right: 30 }}>
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                  <line x1="12" y1="20" x2="12" y2="4" stroke="#3B2A1E" strokeWidth="3" />
                  <polyline points="5,11 12,4 19,11" stroke="#3B2A1E" strokeWidth="3" fill="none" />
                </svg>
              </div>
            </div>
          );
        })}
      </div>

      {/* Bottom warning */}
      <div style={{
        position: 'absolute', bottom: 90, left: 110, right: 110, textAlign: 'center',
        fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 42,
        fontWeight: 500, color: '#2D2D2D',
        opacity: sp(180),
      }}>
        लेकिन <span style={{ color: '#3B2A1E', fontWeight: 800 }}>ये सोच गलत है</span> — कुछ बदलाव कभी उल्टे नहीं जाते
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene7_TheShocks;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene8_RBIExpansion',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene8_RBIExpansion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Money printer animation — bills shoot out
  const printerOp = sp(50);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EDEAE4', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 70%, rgba(255,255,255,0.5) 0%, transparent 55%)',
      }} />

      {/* Shape layer */}
      {[0, 1, 2, 3, 4].map((i) => {
        const r = seededRandom(i + 41);
        const size = 220 + r * 380;
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute',
            left: 80 + r * 1600, top: 60 + r * 800,
            width: size, height: size, borderRadius: '50%',
            border: '1.5px solid rgba(28,28,28,0.05)',
            transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
          }} />
        );
      })}

      {/* Texture — money grid pattern */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, row) =>
          Array.from({ length: 35 }).map((_, col) => (
            <div key={`g-${row}-${col}`} style={{
              position: 'absolute', left: col * 60 + 10, top: row * 60 + 10,
              width: 40, height: 24, border: '1px solid #1C1C1C',
            }} />
          ))
        )}
      </AbsoluteFill>

      {/* Flying currency particles (right to left) */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r = seededRandom(i + 700);
        const startY = 200 + r * 500;
        const px = (1500 - (frame * 3 + i * 80)) % 1800;
        const py = startY + Math.sin((frame + i * 30) * 0.05) * 30;
        return (
          <div key={`bl-${i}`} style={{
            position: 'absolute', left: px, top: py,
            width: 56, height: 28, backgroundColor: '#3B2A1E',
            opacity: 0.18, borderRadius: 2,
            transform: `rotate(${Math.sin(frame * 0.05 + i) * 10}deg)`,
          }}>
            <div style={{
              width: '100%', height: '100%',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              fontFamily: 'Inter, sans-serif', fontSize: 14, fontWeight: 800,
              color: '#EDEAE4',
            }}>₹</div>
          </div>
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 80, left: 110,
        display: 'flex', alignItems: 'center', gap: 18,
        opacity: sp(8),
      }}>
        <svg width="42" height="42" viewBox="0 0 24 24" fill="none">
          <rect x="2" y="6" width="20" height="12" stroke="#1C1C1C" strokeWidth="2" fill="none" rx="2" />
          <circle cx="12" cy="12" r="3" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="6" y1="12" x2="6" y2="12.5" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
        <span style={{
          fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
        }}>RBI · The Print Era</span>
      </div>

      {/* Title */}
      <div style={{
        position: 'absolute', top: 170, left: 110, width: 1050,
        fontFamily: 'Yantra One, serif', fontSize: 110, fontWeight: 700,
        color: '#1C1C1C', lineHeight: 1.05,
        opacity: sp(15),
      }}>
        2020 — 2022<br />
        <span style={{ color: '#3B2A1E' }}>नई मुद्रा</span> छपी
      </div>

      {/* LEFT: KEY STATS */}
      <div style={{
        position: 'absolute', top: 480, left: 110, width: 850,
      }}>
        {[
          { label: 'Liquidity Injected', value: '₹17+ Lakh Cr', delay: 60 },
          { label: 'New Digital Currency', value: 'CBDC Pilots', delay: 95 },
          { label: 'Relief Packages', value: 'Unprecedented', delay: 130 },
        ].map((s, i) => {
          const op = sp(s.delay);
          const tx = interpolate(sp(s.delay), [0, 1], [-40, 0]);
          return (
            <div key={i} style={{
              display: 'flex', alignItems: 'center',
              padding: '24px 30px', marginBottom: 18,
              backgroundColor: 'rgba(28,28,28,0.06)',
              borderLeft: '6px solid #3B2A1E',
              opacity: op, transform: `translateX(${tx}px)`,
            }}>
              <div style={{ flex: 1 }}>
                <div style={{
                  fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 600,
                  color: '#2D2D2D', letterSpacing: 2, textTransform: 'uppercase',
                  marginBottom: 8,
                }}>{s.label}</div>
                <div style={{
                  fontFamily: 'Inter, sans-serif', fontSize: 52, fontWeight: 800,
                  color: '#1C1C1C',
                }}>{s.value}</div>
              </div>
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                <line x1="5" y1="12" x2="19" y2="12" stroke="#3B2A1E" strokeWidth="3" />
                <polyline points="12,5 19,12 12,19" stroke="#3B2A1E" strokeWidth="3" fill="none" />
              </svg>
            </div>
          );
        })}
      </div>

      {/* RIGHT: Money printer SVG */}
      <div style={{
        position: 'absolute', top: 460, right: 130, width: 480, height: 480,
        opacity: printerOp,
        transform: `scale(${sp(50, { damping: 11, stiffness: 100 })})`,
      }}>
        <svg viewBox="0 0 200 200" width="480" height="480">
          {/* Printer body */}
          <rect x="40" y="80" width="120" height="80" stroke="#1C1C1C" strokeWidth="4" fill="rgba(28,28,28,0.08)" rx="6" />
          {/* Top tray */}
          <rect x="55" y="60" width="90" height="25" stroke="#1C1C1C" strokeWidth="4" fill="#1C1C1C" rx="3" />
          {/* Output slot */}
          <rect x="60" y="100" width="80" height="6" fill="#3B2A1E" />
          {/* Bills coming out (animated) */}
          <g transform={`translate(0, ${Math.sin(frame * 0.2) * 4})`}>
            <rect x="65" y="125" width="70" height="35" fill="#3B2A1E" rx="2" />
            <text x="100" y="148" textAnchor="middle"
              fill="#EDEAE4" fontFamily="Inter" fontSize="20" fontWeight="800">₹2000</text>
          </g>
          <g transform={`translate(0, ${Math.sin(frame * 0.2 + 1) * 4})`} opacity="0.7">
            <rect x="55" y="165" width="90" height="20" fill="#1C1C1C" rx="2" />
          </g>
          {/* Indicator light */}
          <circle cx="150" cy="95" r="5" fill="#3B2A1E">
            <animate attributeName="opacity" values="1;0.3;1" dur="1s" repeatCount="indefinite" />
          </circle>
        </svg>
        <div style={{
          textAlign: 'center', marginTop: -20,
          fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700,
          color: '#3B2A1E', letterSpacing: 3, textTransform: 'uppercase',
        }}>Brrr — Non Stop</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene8_RBIExpansion;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene9_NewLevelsLocked',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene9_NewLevelsLocked: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const rows = [
    { label: 'मज़दूरी', en: 'Wages', from: '₹350', to: '₹650', delay: 50 },
    { label: 'ट्रांसपोर्ट', en: 'Transport', from: '₹6/km', to: '₹11/km', delay: 95 },
    { label: 'किराना मार्जिन', en: 'Grocery Margin', from: '12%', to: '22%', delay: 140 },
  ];

  // Lock icon appears
  const lockOp = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EDEAE4', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(150deg, #EDEAE4 0%, #DFDCD3 100%)',
      }} />

      {/* Shape layer — overlapping squares */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 51);
        return (
          <div key={`sq-${i}`} style={{
            position: 'absolute',
            left: 50 + r * 1700, top: 50 + r * 850,
            width: 300 + r * 250, height: 300 + r * 250,
            border: '1.5px solid rgba(28,28,28,0.05)',
            transform: `rotate(${frame * 0.05 + i * 30}deg)`,
          }} />
        );
      })}

      {/* Texture — fine grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <div key={`gx-${i}`} style={{
            position: 'absolute', left: i * 48, top: 0, width: 1, height: '100%',
            backgroundColor: '#1C1C1C',
          }} />
        ))}
        {Array.from({ length: 24 }).map((_, i) => (
          <div key={`gy-${i}`} style={{
            position: 'absolute', left: 0, top: i * 48, width: '100%', height: 1,
            backgroundColor: '#1C1C1C',
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 800);
        const px = (r * 1920 + frame * 0.4) % 1920;
        const py = (r * 1080 + frame * 0.6) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py, width: 4, height: 4,
            backgroundColor: '#3B2A1E', opacity: 0.15, borderRadius: '50%',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 80, left: 110,
        display: 'flex', alignItems: 'center', gap: 18,
        opacity: sp(8),
      }}>
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
          <rect x="5" y="11" width="14" height="10" stroke="#1C1C1C" strokeWidth="2" fill="none" rx="2" />
          <path d="M8 11V7a4 4 0 0 1 8 0v4" stroke="#1C1C1C" strokeWidth="2" fill="none" />
        </svg>
        <span style={{
          fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
        }}>Locked At New Levels</span>
      </div>

      {/* Title */}
      <div style={{
        position: 'absolute', top: 170, left: 110, right: 110,
        fontFamily: 'Yantra One, serif', fontSize: 110, fontWeight: 700,
        color: '#1C1C1C', lineHeight: 1.05,
        opacity: sp(15),
      }}>
        जब लागत घटी, तब भी<br />
        <span style={{ background: '#3B2A1E', color: '#EDEAE4', padding: '0 24px' }}>दाम नहीं घटे</span>
      </div>

      {/* TABLE-LIKE rows */}
      <div style={{
        position: 'absolute', top: 470, left: 110, right: 110,
      }}>
        {/* Header */}
        <div style={{
          display: 'grid', gridTemplateColumns: '1.4fr 1fr 0.5fr 1fr',
          padding: '12px 30px', marginBottom: 12,
          fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 700,
          color: '#2D2D2D', letterSpacing: 3, textTransform: 'uppercase',
          opacity: sp(30),
        }}>
          <div>Cost Component</div>
          <div>Pre-Pandemic</div>
          <div></div>
          <div>Now (Locked)</div>
        </div>

        {rows.map((r, i) => {
          const op = sp(r.delay);
          const tx = interpolate(sp(r.delay), [0, 1], [-30, 0]);
          return (
            <div key={i} style={{
              display: 'grid', gridTemplateColumns: '1.4fr 1fr 0.5fr 1fr',
              alignItems: 'center',
              padding: '28px 30px', marginBottom: 14,
              backgroundColor: i % 2 === 0 ? 'rgba(28,28,28,0.05)' : 'rgba(28,28,28,0.09)',
              borderRadius: 10,
              opacity: op, transform: `translateX(${tx}px)`,
            }}>
              <div>
                <div style={{
                  fontFamily: 'Yantra One, serif', fontSize: 54, fontWeight: 700,
                  color: '#1C1C1C', lineHeight: 1,
                }}>{r.label}</div>
                <div style={{
                  fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 500,
                  color: '#9E9E9E', letterSpacing: 2, textTransform: 'uppercase',
                  marginTop: 4,
                }}>{r.en}</div>
              </div>
              <div style={{
                fontFamily: 'Inter, sans-serif', fontSize: 48, fontWeight: 700,
                color: '#9E9E9E', textDecoration: 'line-through',
              }}>{r.from}</div>
              <div style={{ fontSize: 40, color: '#3B2A1E', textAlign: 'center' }}>→</div>
              <div style={{
                fontFamily: 'Inter, sans-serif', fontSize: 60, fontWeight: 900,
                color: '#3B2A1E',
              }}>{r.to}</div>
            </div>
          );
        })}
      </div>

      {/* Lock icon stamp */}
      <div style={{
        position: 'absolute', bottom: 120, right: 150,
        opacity: lockOp, transform: `scale(${sp(180, { damping: 10, stiffness: 130 })}) rotate(-12deg)`,
      }}>
        <div style={{
          padding: '20px 36px', border: '4px solid #3B2A1E', borderRadius: 12,
          backgroundColor: 'rgba(59,42,30,0.08)',
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 42, fontWeight: 900,
            color: '#3B2A1E', letterSpacing: 6, textTransform: 'uppercase',
          }}>LOCKED IN</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene9_NewLevelsLocked;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Ch2Scene10_RatchetEffect',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Ch2Scene10_RatchetEffect: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Ratchet steps — climbs up over time
  const steps = 5;
  const stepProgress = (i: number) => sp(50 + i * 25);

  // Ratchet wheel rotation (one-way)
  const wheelRot = interpolate(frame, [40, 200], [0, 360], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.3, 0, 0.7, 1),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EDEAE4', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 80% 20%, rgba(255,255,255,0.55) 0%, transparent 50%), radial-gradient(circle at 20% 100%, rgba(59,42,30,0.07) 0%, transparent 55%)',
      }} />

      {/* Shape layer */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 61);
        const size = 300 + r * 280;
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute',
            left: 60 + r * 1600, top: 60 + r * 800,
            width: size, height: size, borderRadius: '50%',
            border: '1.5px solid rgba(28,28,28,0.05)',
            transform: `scale(${1 + Math.sin(frame * 0.018 + i) * 0.04})`,
          }} />
        );
      })}

      {/* Texture — diagonal hatch */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {Array.from({ length: 50 }).map((_, i) => (
          <div key={`dh-${i}`} style={{
            position: 'absolute', left: -200, top: i * 60,
            width: 2400, height: 1, backgroundColor: '#1C1C1C',
            transform: 'rotate(35deg)', transformOrigin: 'left',
          }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 900);
        const px = (r * 1920 + frame * 0.4) % 1920;
        const py = (r * 1080 - frame * 0.6 + 1080) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py, width: 4, height: 4,
            backgroundColor: '#1C1C1C', opacity: 0.15, borderRadius: '50%',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 80, left: 110,
        display: 'flex', alignItems: 'center', gap: 18,
        opacity: sp(8),
      }}>
        <svg width="42" height="42" viewBox="0 0 24 24" fill="none">
          <circle cx="12" cy="12" r="3" stroke="#1C1C1C" strokeWidth="2" />
          <path d="M12 2v4M12 18v4M2 12h4M18 12h4M5 5l3 3M16 16l3 3M5 19l3-3M16 8l3-3" stroke="#1C1C1C" strokeWidth="2" />
        </svg>
        <span style={{
          fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase',
        }}>The Ratchet Effect</span>
      </div>

      {/* MAIN TITLE — HUGE */}
      <div style={{
        position: 'absolute', top: 170, left: 110,
        opacity: sp(15), transform: `translateY(${interpolate(sp(15),[0,1],[40,0])}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 36, fontWeight: 600,
          color: '#2D2D2D', letterSpacing: 4, textTransform: 'uppercase', marginBottom: 10,
        }}>The Law of Prices</div>
        <div style={{
          fontFamily: 'Yantra One, serif', fontSize: 200, fontWeight: 700,
          color: '#1C1C1C', lineHeight: 0.95,
        }}>
          <span style={{ color: '#3B2A1E' }}>ऊपर</span> जल्दी,
        </div>
        <div style={{
          fontFamily: 'Yantra One, serif', fontSize: 200, fontWeight: 700,
          color: '#1C1C1C', lineHeight: 0.95, marginTop: 10,
        }}>
          <span style={{ textDecoration: 'line-through', textDecorationThickness: 12, textDecorationColor: '#3B2A1E' }}>नीचे</span> कभी नहीं
        </div>
      </div>

      {/* RIGHT: Ratchet staircase */}
      <div style={{
        position: 'absolute', bottom: 130, right: 130, width: 580, height: 460,
      }}>
        {/* Steps going up */}
        {Array.from({ length: steps }).map((_, i) => {
          const op = stepProgress(i);
          const w = 110;
          const h = 70;
          return (
            <div key={`st-${i}`} style={{
              position: 'absolute',
              left: i * w, bottom: i * h,
              width: w, height: h,
              backgroundColor: i === steps - 1 ? '#3B2A1E' : '#1C1C1C',
              opacity: op,
              transform: `scale(${op})`,
              transformOrigin: 'bottom left',
              border: '2px solid #EDEAE4',
            }}>
              {i === steps - 1 && (
                <div style={{
                  position: 'absolute', top: -50, left: '50%', transform: 'translateX(-50%)',
                  fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 900,
                  color: '#3B2A1E', whiteSpace: 'nowrap',
                }}>NOW</div>
              )}
            </div>
          );
        })}

        {/* Up arrow over steps */}
        <div style={{
          position: 'absolute', top: 20, right: 0,
          opacity: sp(180),
        }}>
          <svg width="80" height="120" viewBox="0 0 24 32">
            <line x1="12" y1="6" x2="12" y2="28" stroke="#3B2A1E" strokeWidth="4" />
            <polyline points="4,14 12,6 20,14" stroke="#3B2A1E" strokeWidth="4" fill="none" />
          </svg>
        </div>

        {/* Ratchet wheel (decorative — bottom left of staircase) */}
        <div style={{
          position: 'absolute', bottom: -50, left: -80,
          opacity: sp(40),
        }}>
          <svg width="140" height="140" viewBox="0 0 100 100"
            style={{ transform: `rotate(${wheelRot}deg)` }}>
            <circle cx="50" cy="50" r="40" stroke="#1C1C1C" strokeWidth="3" fill="none" />
            {Array.from({ length: 12 }).map((_, k) => {
              const angle = (k / 12) * Math.PI * 2;
              const x1 = 50 + Math.cos(angle) * 40;
              const y1 = 50 + Math.sin(angle) * 40;
              const x2 = 50 + Math.cos(angle) * 48;
              const y2 = 50 + Math.sin(angle) * 48;
              return <line key={k} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#1C1C1C" strokeWidth="3" />;
            })}
            <circle cx="50" cy="50" r="8" fill="#3B2A1E" />
          </svg>
        </div>
      </div>

      {/* Bottom caption */}
      <div style={{
        position: 'absolute', bottom: 70, left: 110, width: 900,
        fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 40,
        fontWeight: 500, color: '#2D2D2D',
        opacity: sp(190),
      }}>
        Ratchet घूमता है <span style={{ color: '#3B2A1E', fontWeight: 800 }}>एक तरफ़</span> — कीमतें वापस नहीं आतीं
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.4, letterSpacing: 1.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Ch2Scene10_RatchetEffect;
```

