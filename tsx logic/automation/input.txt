```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_MarketGossip',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_MarketGossip: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom-in
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.06]);

  // Word-by-word reveals
  const w1 = sp(8);   // सोचिए
  const w2 = sp(28);  // एक पल
  const w3 = sp(70);  // मम्मी
  const w4 = sp(110); // आंटी bubble
  const w5 = sp(165); // सोना ले लो
  const w6 = sp(230); // ₹1,00,000+
  const w7 = sp(310); // arrow up

  const priceCount = Math.floor(interpolate(frame, [230, 360], [99000, 100750], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(circle at 30% 20%, rgba(11,64,156,0.08), transparent 55%), radial-gradient(circle at 75% 85%, rgba(11,64,156,0.06), transparent 60%)',
      }} />

      {/* Shape layer — drifting circles + rect */}
      {[0,1,2,3,4].map(i => {
        const drift = Math.sin(frame * 0.012 + i) * 25;
        const rot = frame * 0.15 + i * 60;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${10 + i*22}%`, top: `${15 + (i%2)*55}%`,
            width: 280 + i*40, height: 280 + i*40,
            borderRadius: i%2 === 0 ? '50%' : '20%',
            border: '2px solid #0B409C',
            opacity: 0.05 + (i*0.008),
            transform: `translateY(${drift}px) rotate(${rot}deg) scale(${1 + Math.sin(frame*0.02+i)*0.05})`,
          }} />
        );
      })}

      {/* Motion texture — grid lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        {Array.from({length: 24}).map((_, i) => (
          <line key={`v${i}`} x1={i*80} y1={0} x2={i*80} y2={1080} stroke="#0B409C" strokeWidth="1" />
        ))}
        {Array.from({length: 14}).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i*80} x2={1920} y2={i*80} stroke="#0B409C" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({length: 30}).map((_, i) => {
        const s = seededRandom(i+1);
        const y = (s*1080 + frame*0.6) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 6, height: 6, borderRadius: '50%',
            background: '#0B409C', opacity: 0.25,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale})`,
        transformOrigin: '50% 50%',
      }}>

        {/* Word 1 — सोचिए */}
        <div style={{
          position: 'absolute', top: 120, left: '50%',
          transform: `translateX(-50%) translateY(${(1-w1)*40}px)`,
          opacity: w1,
          fontFamily: 'Yantra One',
          fontSize: 187, // 85% of 220
          color: '#0B409C',
          letterSpacing: '-2px',
        }}>
          सोचिए…
        </div>

        {/* Word 2 — एक पल के लिए */}
        <div style={{
          position: 'absolute', top: 320, left: '50%',
          transform: `translateX(-50%) translateY(${(1-w2)*30}px)`,
          opacity: w2,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 68, color: '#000000', fontWeight: 600,
        }}>
          एक पल के लिए
        </div>

        {/* Speech bubble — Aunty's gossip */}
        {w4 > 0.05 && (
          <div style={{
            position: 'absolute', top: 470, left: 180,
            opacity: w4,
            transform: `translateY(${(1-w4)*20}px) rotate(-2deg)`,
          }}>
            <div style={{
              background: '#FFFFFF',
              border: '4px solid #0B409C',
              borderRadius: 30,
              padding: '35px 50px',
              boxShadow: '0 20px 40px rgba(11,64,156,0.18)',
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 60, color: '#0B409C', fontWeight: 700,
              maxWidth: 720,
              lineHeight: 1.2,
            }}>
              "बेटा सोना ले लो"
            </div>
            {/* Tail */}
            <div style={{
              width: 0, height: 0,
              borderLeft: '25px solid transparent',
              borderRight: '25px solid transparent',
              borderTop: '30px solid #0B409C',
              marginLeft: 80,
            }} />
          </div>
        )}

        {/* Big price reveal — ₹1,00,000+ */}
        {w6 > 0.05 && (
          <div style={{
            position: 'absolute', top: 720, right: 120,
            opacity: w6,
            transform: `scale(${w6})`,
            transformOrigin: '100% 50%',
            textAlign: 'right',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 38,
              color: '#000000', opacity: 0.7, letterSpacing: 4,
            }}>
              GOLD / 10g
            </div>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 170,
              color: '#0B409C', lineHeight: 1,
              letterSpacing: '-4px',
            }}>
              ₹{priceCount.toLocaleString('en-IN')}
            </div>
            {/* Up arrow + badge */}
            {w7 > 0.05 && (
              <div style={{
                display: 'inline-flex', alignItems: 'center', gap: 12,
                background: '#0B409C', color: '#F2F7FF',
                padding: '12px 28px', borderRadius: 50,
                fontFamily: 'Inter', fontSize: 38, fontWeight: 700,
                opacity: w7, transform: `scale(${w7})`,
                marginTop: 10,
              }}>
                <svg width="34" height="34" viewBox="0 0 24 24" fill="none">
                  <path d="M12 4 L20 14 L14 14 L14 22 L10 22 L10 14 L4 14 Z" fill="#F2F7FF"/>
                </svg>
                आगे और भागेगा
              </div>
            )}
          </div>
        )}

        {/* Underline accent under सोचिए */}
        <div style={{
          position: 'absolute', top: 290, left: '50%',
          width: w1 * 280, height: 8,
          background: '#0B409C',
          transform: 'translateX(-50%)',
          borderRadius: 4,
        }} />
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_MarketGossip;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_TugOfWar',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_TugOfWar: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const leftCard = sp(15);
  const rightCard = sp(60);
  const vsBadge = sp(120, { stiffness: 200 });
  const youLabel = sp(220);
  const shake = frame > 280 ? Math.sin(frame * 1.5) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'linear-gradient(135deg, rgba(11,64,156,0.08) 0%, transparent 50%, rgba(11,64,156,0.06) 100%)',
      }} />

      {/* Shape layer */}
      {[0,1,2,3].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${i*28}%`, top: `${10 + (i%2)*60}%`,
          width: 320, height: 320,
          border: '2px solid #0B409C',
          opacity: 0.04,
          borderRadius: i%2 ? '30%' : '50%',
          transform: `rotate(${frame * 0.2 + i*45}deg) scale(${1 + Math.sin(frame*0.015+i)*0.08})`,
        }} />
      ))}

      {/* Motion texture — diagonal lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        {Array.from({length: 30}).map((_, i) => (
          <line key={i} x1={i*100 - 200} y1={0} x2={i*100} y2={1080} stroke="#0B409C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({length: 25}).map((_, i) => {
        const s = seededRandom(i+5);
        const y = (s*1080 + frame*0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 5, height: 5, borderRadius: '50%',
            background: '#0B409C', opacity: 0.2,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <div style={{ transform: `translateX(${shake}px)` }}>

        {/* Top title */}
        <div style={{
          position: 'absolute', top: 70, left: '50%',
          transform: `translateX(-50%) translateY(${(1-sp(5))*-30}px)`,
          opacity: sp(5),
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 51, color: '#000000', fontWeight: 600,
          letterSpacing: 1,
        }}>
          दो आवाज़ें, एक आप
        </div>

        {/* LEFT — मम्मी */}
        <div style={{
          position: 'absolute', top: 250, left: 100,
          opacity: leftCard,
          transform: `translateX(${(1-leftCard)*-80}px) rotate(-3deg)`,
          width: 600,
          background: '#FFFFFF',
          border: '5px solid #0B409C',
          borderRadius: 28,
          padding: '50px 45px',
          boxShadow: '0 25px 50px rgba(11,64,156,0.2)',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, color: '#0B409C', fontWeight: 700,
            opacity: 0.7, marginBottom: 18, letterSpacing: 3,
          }}>
            SIDE A
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 102, color: '#0B409C',
            lineHeight: 1, marginBottom: 20,
          }}>
            मम्मी
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 47,
            color: '#000000', lineHeight: 1.3, fontWeight: 600,
          }}>
            "सोना ले लो,<br/>भागेगा आगे"
          </div>
        </div>

        {/* RIGHT — Experts */}
        <div style={{
          position: 'absolute', top: 250, right: 100,
          opacity: rightCard,
          transform: `translateX(${(1-rightCard)*80}px) rotate(3deg)`,
          width: 600,
          background: '#0B409C',
          borderRadius: 28,
          padding: '50px 45px',
          boxShadow: '0 25px 50px rgba(11,64,156,0.3)',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, color: '#F2F7FF', fontWeight: 700,
            opacity: 0.7, marginBottom: 18, letterSpacing: 3,
          }}>
            SIDE B
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 89, color: '#F2F7FF',
            lineHeight: 1, marginBottom: 20,
          }}>
            एक्सपर्ट्स
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 47,
            color: '#F2F7FF', lineHeight: 1.3, fontWeight: 600,
          }}>
            "बबल है!<br/>क्रैश आएगा"
          </div>
        </div>

        {/* VS badge in middle */}
        <div style={{
          position: 'absolute', top: 410, left: '50%',
          transform: `translate(-50%, 0) scale(${vsBadge}) rotate(${vsBadge*360}deg)`,
          opacity: vsBadge,
          width: 160, height: 160, borderRadius: '50%',
          background: '#000000', color: '#F2F7FF',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontFamily: 'Yantra One', fontSize: 68,
          boxShadow: '0 15px 40px rgba(0,0,0,0.3)',
          border: '6px solid #F2F7FF',
        }}>
          VS
        </div>

        {/* Bottom — आप */}
        <div style={{
          position: 'absolute', bottom: 110, left: '50%',
          transform: `translateX(-50%) translateY(${(1-youLabel)*40}px)`,
          opacity: youLabel,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, color: '#000000',
            opacity: 0.6, letterSpacing: 4, marginBottom: 8,
          }}>
            BEECH MEIN
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 170, color: '#0B409C',
            lineHeight: 1, letterSpacing: '-4px',
          }}>
            आप
          </div>
          <div style={{
            width: youLabel * 320, height: 8, background: '#0B409C',
            margin: '5px auto 0', borderRadius: 4,
          }} />
        </div>
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_TugOfWar;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_HostIntro',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_HostIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const nameIn = sp(10);
  const channelIn = sp(50);
  const dataChip = sp(150);
  const truthChip = sp(190);
  const noFearChip = sp(230);
  const promiseIn = sp(300);

  // pan
  const panX = interpolate(frame, [0, durationInFrames], [-15, 15]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse at 20% 50%, rgba(11,64,156,0.12), transparent 50%)',
      }} />

      {/* Shape layer */}
      {[0,1,2].map(i => (
        <div key={i} style={{
          position: 'absolute',
          right: `${i*15}%`, top: `${20 + i*20}%`,
          width: 400, height: 400,
          border: '3px solid #0B409C',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame*0.02+i)*0.1})`,
        }} />
      ))}

      {/* Motion texture — dot grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="1920" height="1080">
        {Array.from({length: 20}).map((_, r) =>
          Array.from({length: 36}).map((__, c) => (
            <circle key={`${r}-${c}`} cx={c*54+20} cy={r*54+20} r="2" fill="#0B409C" />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({length: 22}).map((_, i) => {
        const s = seededRandom(i+10);
        const y = (s*1080 + frame*0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 5, height: 5, borderRadius: '50%',
            background: '#0B409C', opacity: 0.22,
          }} />
        );
      })}

      <div style={{ transform: `translateX(${panX}px)` }}>

        {/* LEFT BIG NAME BLOCK */}
        <div style={{
          position: 'absolute', top: 220, left: 130,
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 34, color: '#000000',
            opacity: nameIn * 0.6, letterSpacing: 5, marginBottom: 10,
          }}>
            HELLO, I AM
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 221, color: '#0B409C',
            lineHeight: 1, letterSpacing: '-6px',
            opacity: nameIn,
            transform: `translateY(${(1-nameIn)*30}px)`,
          }}>
            कुनाल
          </div>
          <div style={{
            opacity: channelIn,
            transform: `translateY(${(1-channelIn)*20}px)`,
            marginTop: 20,
            display: 'inline-block',
            background: '#0B409C',
            padding: '14px 36px',
            borderRadius: 12,
          }}>
            <span style={{
              fontFamily: 'Inter', fontSize: 42, fontWeight: 800,
              color: '#F2F7FF', letterSpacing: 1,
            }}>
              ProBuffet HQ
            </span>
          </div>
        </div>

        {/* RIGHT — three promise chips */}
        <div style={{
          position: 'absolute', top: 250, right: 130, width: 540,
          display: 'flex', flexDirection: 'column', gap: 22,
        }}>
          {[
            { label: 'सिर्फ डेटा', op: dataChip },
            { label: 'सिर्फ सच', op: truthChip },
            { label: 'कोई डर नहीं', op: noFearChip },
          ].map((chip, i) => (
            <div key={i} style={{
              opacity: chip.op,
              transform: `translateX(${(1-chip.op)*60}px) scale(${chip.op})`,
              background: '#FFFFFF',
              border: '4px solid #0B409C',
              borderRadius: 18,
              padding: '24px 36px',
              display: 'flex', alignItems: 'center', gap: 22,
              boxShadow: '0 12px 28px rgba(11,64,156,0.15)',
            }}>
              <div style={{
                width: 14, height: 60, background: '#0B409C', borderRadius: 3,
              }} />
              <div style={{
                fontFamily: 'Noto Sans Devanagari', fontSize: 60,
                color: '#000000', fontWeight: 700,
              }}>
                {chip.label}
              </div>
            </div>
          ))}
        </div>

        {/* Bottom strip — promise */}
        <div style={{
          position: 'absolute', bottom: 80, left: 0, right: 0,
          opacity: promiseIn,
          transform: `translateY(${(1-promiseIn)*30}px)`,
          textAlign: 'center',
        }}>
          <div style={{
            display: 'inline-block',
            background: '#000000',
            padding: '20px 60px',
            borderRadius: 50,
          }}>
            <span style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 47,
              color: '#F2F7FF', fontWeight: 700, letterSpacing: 1,
            }}>
              आज पूरा confusion खुलेगा
            </span>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_HostIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_ThreeReasons',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_ThreeReasons: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(10);
  const num1 = sp(80, { stiffness: 180 });
  const num2 = sp(120, { stiffness: 180 });
  const num3 = sp(160, { stiffness: 180 });
  const focusOn1 = sp(230);

  // tilt
  const tilt = Math.sin(frame*0.02)*1.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'linear-gradient(180deg, rgba(11,64,156,0.04) 0%, transparent 60%, rgba(11,64,156,0.08) 100%)',
      }} />

      {/* Shape layer — triangles */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        {[0,1,2,3].map(i => (
          <polygon key={i}
            points={`${300+i*350},150 ${500+i*350},150 ${400+i*350},350`}
            fill="none" stroke="#0B409C" strokeWidth="3"
            transform={`rotate(${frame*0.3+i*30} ${400+i*350} 250)`}
          />
        ))}
      </svg>

      {/* Motion texture — concentric circles */}
      <svg style={{ position: 'absolute', right: -200, bottom: -200, opacity: 0.08 }} width="800" height="800">
        {[1,2,3,4,5,6].map(i => (
          <circle key={i} cx="400" cy="400" r={i*60} fill="none" stroke="#0B409C" strokeWidth="2" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({length: 24}).map((_, i) => {
        const s = seededRandom(i+15);
        const y = (s*1080 + frame*0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 5, height: 5, borderRadius: '50%',
            background: '#0B409C', opacity: 0.22,
          }} />
        );
      })}

      <div style={{ transform: `rotate(${tilt*0.1}deg)` }}>

        {/* Title */}
        <div style={{
          position: 'absolute', top: 80, left: '50%',
          transform: `translateX(-50%) translateY(${(1-title)*-30}px)`,
          opacity: title,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, color: '#000000',
            opacity: 0.6, letterSpacing: 5,
          }}>
            WHY GOLD IS RISING
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 119, color: '#0B409C',
            lineHeight: 1, marginTop: 10,
          }}>
            तीन ठोस कारण
          </div>
        </div>

        {/* Three giant numbered cards */}
        <div style={{
          position: 'absolute', top: 380, left: 0, right: 0,
          display: 'flex', justifyContent: 'center', gap: 50,
        }}>
          {[
            { n: '01', label: 'रुपया', sub: 'RUPEE WEAK', op: num1, focus: true },
            { n: '02', label: 'RBI', sub: 'CENTRAL BANK', op: num2, focus: false },
            { n: '03', label: 'इंफ्लेशन', sub: 'GLOBAL RATES', op: num3, focus: false },
          ].map((card, i) => {
            const isFocus = card.focus;
            const focusScale = isFocus ? (1 + focusOn1 * 0.08) : 1;
            const focusOp = isFocus ? 1 : (1 - focusOn1*0.4);
            return (
              <div key={i} style={{
                opacity: card.op * focusOp,
                transform: `translateY(${(1-card.op)*60}px) scale(${focusScale})`,
                width: 440,
                background: isFocus && focusOn1 > 0.5 ? '#0B409C' : '#FFFFFF',
                border: '5px solid #0B409C',
                borderRadius: 24,
                padding: 50,
                textAlign: 'center',
                boxShadow: isFocus ? '0 25px 50px rgba(11,64,156,0.3)' : '0 15px 30px rgba(11,64,156,0.12)',
                transition: 'background 0.3s',
              }}>
                <div style={{
                  fontFamily: 'JetBrains Mono', fontSize: 51,
                  color: isFocus && focusOn1 > 0.5 ? '#F2F7FF' : '#0B409C',
                  opacity: 0.7, fontWeight: 700, letterSpacing: 3,
                }}>
                  {card.n}
                </div>
                <div style={{
                  height: 4, width: 80, background: isFocus && focusOn1 > 0.5 ? '#F2F7FF' : '#0B409C',
                  margin: '12px auto', borderRadius: 2,
                }} />
                <div style={{
                  fontFamily: 'Yantra One', fontSize: 102,
                  color: isFocus && focusOn1 > 0.5 ? '#F2F7FF' : '#0B409C',
                  lineHeight: 1, marginTop: 10,
                }}>
                  {card.label}
                </div>
                <div style={{
                  fontFamily: 'Inter', fontSize: 26, fontWeight: 700,
                  color: isFocus && focusOn1 > 0.5 ? '#F2F7FF' : '#000000',
                  opacity: 0.7, letterSpacing: 3, marginTop: 14,
                }}>
                  {card.sub}
                </div>
              </div>
            );
          })}
        </div>

        {/* Pointer to #1 */}
        {focusOn1 > 0.1 && (
          <div style={{
            position: 'absolute', bottom: 90, left: '50%',
            transform: `translateX(-50%) translateY(${(1-focusOn1)*-20}px)`,
            opacity: focusOn1,
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 55,
              color: '#000000', fontWeight: 700, textAlign: 'center',
            }}>
              पहले बात करते हैं <span style={{color: '#0B409C'}}>रुपये की</span>
            </div>
          </div>
        )}
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_ThreeReasons;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_RupeeGoldSeesaw',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_RupeeGoldSeesaw: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(10);
  const seesawIn = sp(50);
  const rupeeDown = interpolate(frame, [110, 250], [0, 60], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const goldUp = interpolate(frame, [110, 250], [0, -60], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const tilt = interpolate(frame, [110, 250], [0, -8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  const rupeeVal = interpolate(frame, [110, 250], [82, 89], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const goldVal = Math.floor(interpolate(frame, [110, 250], [55000, 102000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  const formulaIn = sp(300);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(circle at 50% 100%, rgba(11,64,156,0.1), transparent 60%)',
      }} />

      {/* Shape layer */}
      {[0,1,2].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${20+i*30}%`, top: `${10+(i%2)*70}%`,
          width: 350, height: 350,
          border: '3px solid #0B409C', borderRadius: i%2 ? '20%' : '50%',
          opacity: 0.04,
          transform: `rotate(${frame*0.2 + i*45}deg)`,
        }} />
      ))}

      {/* Texture — horizontal lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        {Array.from({length: 20}).map((_, i) => (
          <line key={i} x1={0} y1={i*60} x2={1920} y2={i*60} stroke="#0B409C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({length: 22}).map((_, i) => {
        const s = seededRandom(i+20);
        const y = (s*1080 + frame*0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 5, height: 5, borderRadius: '50%',
            background: '#0B409C', opacity: 0.2,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 80, left: '50%',
        transform: `translateX(-50%) translateY(${(1-title)*-30}px)`,
        opacity: title, textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 102, color: '#0B409C',
          lineHeight: 1,
        }}>
          रुपया ↓ &nbsp; सोना ↑
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 38,
          color: '#000000', opacity: 0.7, fontWeight: 600, marginTop: 8,
        }}>
          एक गिरे तो दूसरा उठे — सीधा रिश्ता
        </div>
      </div>

      {/* SEESAW */}
      <div style={{
        position: 'absolute', top: 480, left: '50%',
        transform: `translateX(-50%) rotate(${tilt}deg)`,
        transformOrigin: '50% 100%',
        opacity: seesawIn,
        width: 1100, height: 16,
        background: '#0B409C', borderRadius: 8,
      }}>
        {/* LEFT — Rupee */}
        <div style={{
          position: 'absolute', left: -50, top: -260 + rupeeDown,
          width: 360, textAlign: 'center',
        }}>
          <div style={{
            background: '#FFFFFF', border: '5px solid #0B409C',
            borderRadius: 20, padding: '25px 20px',
            boxShadow: '0 15px 30px rgba(11,64,156,0.2)',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 28, color: '#000000',
              opacity: 0.6, letterSpacing: 3,
            }}>
              USD / INR
            </div>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 102, color: '#0B409C',
              lineHeight: 1,
            }}>
              ₹{rupeeVal.toFixed(1)}
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 32,
              color: '#000000', fontWeight: 700, marginTop: 5,
            }}>
              रुपया कमजोर
            </div>
          </div>
        </div>

        {/* RIGHT — Gold */}
        <div style={{
          position: 'absolute', right: -50, top: -260 + goldUp,
          width: 360, textAlign: 'center',
        }}>
          <div style={{
            background: '#0B409C', borderRadius: 20,
            padding: '25px 20px',
            boxShadow: '0 15px 30px rgba(11,64,156,0.3)',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 28, color: '#F2F7FF',
              opacity: 0.7, letterSpacing: 3,
            }}>
              GOLD / 10g
            </div>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 80, color: '#F2F7FF',
              lineHeight: 1,
            }}>
              ₹{goldVal.toLocaleString('en-IN')}
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 32,
              color: '#F2F7FF', fontWeight: 700, marginTop: 5,
            }}>
              सोना मजबूत
            </div>
          </div>
        </div>

        {/* Pivot */}
        <div style={{
          position: 'absolute', left: '50%', top: -10,
          transform: 'translateX(-50%)',
          width: 0, height: 0,
          borderLeft: '60px solid transparent',
          borderRight: '60px solid transparent',
          borderTop: '90px solid #000000',
        }} />
      </div>

      {/* Bottom — Formula */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${(1-formulaIn)*30}px)`,
        opacity: formulaIn,
        background: '#000000', padding: '20px 50px',
        borderRadius: 50,
      }}>
        <span style={{
          fontFamily: 'JetBrains Mono', fontSize: 45,
          color: '#F2F7FF', fontWeight: 700, letterSpacing: 2,
        }}>
          Gold priced in $ → Rupee ↓ = Gold ↑
        </span>
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_RupeeGoldSeesaw;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_RBIReserves',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_RBIReserves: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(10);
  const labelIn = sp(50);

  // Bar chart years
  const years = [
    { y: '2019', val: 618, delay: 90 },
    { y: '2020', val: 676, delay: 115 },
    { y: '2021', val: 754, delay: 140 },
    { y: '2022', val: 787, delay: 165 },
    { y: '2023', val: 803, delay: 190 },
    { y: '2024', val: 854, delay: 215 },
  ];

  const signalIn = sp(300);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'linear-gradient(180deg, rgba(11,64,156,0.06) 0%, transparent 60%)',
      }} />

      {/* Shape layer */}
      {[0,1,2,3].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${i*25}%`, top: `${5+i*22}%`,
          width: 280, height: 280,
          border: '2px solid #0B409C', borderRadius: '20%',
          opacity: 0.04,
          transform: `rotate(${frame*0.25 + i*40}deg)`,
        }} />
      ))}

      {/* Texture — grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        {Array.from({length: 25}).map((_, i) => (
          <line key={`v${i}`} x1={i*77} y1={0} x2={i*77} y2={1080} stroke="#0B409C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({length: 20}).map((_, i) => {
        const s = seededRandom(i+25);
        const y = (s*1080 + frame*0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 5, height: 5, borderRadius: '50%',
            background: '#0B409C', opacity: 0.2,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: 100,
        transform: `translateY(${(1-title)*-30}px)`,
        opacity: title,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 32, color: '#000000',
          opacity: 0.6, letterSpacing: 5,
        }}>
          RESERVE BANK OF INDIA
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 119, color: '#0B409C',
          lineHeight: 1, marginTop: 5,
        }}>
          RBI खुद खरीद रहा है
        </div>
      </div>

      {/* Y-axis label */}
      <div style={{
        position: 'absolute', top: 320, left: 100,
        opacity: labelIn,
        fontFamily: 'JetBrains Mono', fontSize: 26,
        color: '#000000', opacity: 0.6, letterSpacing: 2,
      }}>
        GOLD RESERVES (TONNES)
      </div>

      {/* BAR CHART */}
      <div style={{
        position: 'absolute', bottom: 200, left: 130, right: 130,
        height: 480, display: 'flex', alignItems: 'flex-end',
        justifyContent: 'space-around', gap: 30,
        borderBottom: '4px solid #0B409C', borderLeft: '4px solid #0B409C',
        padding: '0 20px',
      }}>
        {years.map((yr, i) => {
          const grow = sp(yr.delay);
          const heightPx = (yr.val / 900) * 440 * grow;
          const isHighlight = i === years.length - 1;
          return (
            <div key={yr.y} style={{
              flex: 1, display: 'flex', flexDirection: 'column',
              alignItems: 'center', position: 'relative',
            }}>
              {/* value on top */}
              <div style={{
                opacity: grow,
                fontFamily: 'Yantra One', fontSize: 47,
                color: isHighlight ? '#0B409C' : '#000000',
                marginBottom: 10,
              }}>
                {yr.val}
              </div>
              {/* bar */}
              <div style={{
                width: '85%', height: heightPx,
                background: isHighlight ? '#0B409C' : '#000000',
                borderRadius: '12px 12px 0 0',
                boxShadow: isHighlight ? '0 0 30px rgba(11,64,156,0.4)' : 'none',
              }} />
              {/* year label */}
              <div style={{
                marginTop: 14,
                fontFamily: 'JetBrains Mono', fontSize: 32,
                color: '#000000', fontWeight: 700,
              }}>
                {yr.y}
              </div>
            </div>
          );
        })}
      </div>

      {/* Signal badge */}
      <div style={{
        position: 'absolute', bottom: 90, right: 130,
        opacity: signalIn,
        transform: `scale(${signalIn})`,
        background: '#0B409C', color: '#F2F7FF',
        padding: '20px 40px', borderRadius: 50,
        fontFamily: 'Noto Sans Devanagari', fontSize: 47,
        fontWeight: 700,
        boxShadow: '0 15px 30px rgba(11,64,156,0.3)',
      }}>
        इशारा साफ है ↑
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_RBIReserves;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_FedRates',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_FedRates: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(10);
  const ratesIn = sp(60);
  const arrow1 = sp(130);
  const dollarBlock = sp(180);
  const arrow2 = sp(240);
  const goldBlock = sp(290);

  const rateVal = interpolate(frame, [60, 200], [5.5, 4.25], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(circle at 80% 20%, rgba(11,64,156,0.1), transparent 50%)',
      }} />

      {/* Shape layer */}
      {[0,1,2].map(i => (
        <div key={i} style={{
          position: 'absolute',
          right: `${i*25}%`, bottom: `${i*20}%`,
          width: 320, height: 320,
          border: '2px solid #0B409C', borderRadius: '50%',
          opacity: 0.05,
          transform: `scale(${1+Math.sin(frame*0.02+i)*0.06})`,
        }} />
      ))}

      {/* Texture — diagonal */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        {Array.from({length: 25}).map((_, i) => (
          <line key={i} x1={-200+i*120} y1={1080} x2={i*120} y2={0} stroke="#0B409C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({length: 22}).map((_, i) => {
        const s = seededRandom(i+30);
        const y = (s*1080 + frame*0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 5, height: 5, borderRadius: '50%',
            background: '#0B409C', opacity: 0.22,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%) translateY(${(1-title)*-30}px)`,
        opacity: title, textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 32, color: '#000000',
          opacity: 0.6, letterSpacing: 5,
        }}>
          US FED INTEREST RATE CYCLE
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 102, color: '#0B409C',
          lineHeight: 1, marginTop: 8,
        }}>
          चेन रिएक्शन
        </div>
      </div>

      {/* CHAIN: FED ↓ → DOLLAR ↓ → GOLD ↑ */}
      <div style={{
        position: 'absolute', top: 320, left: 0, right: 0,
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        gap: 30,
      }}>
        {/* FED RATES */}
        <div style={{
          opacity: ratesIn,
          transform: `scale(${ratesIn})`,
          width: 380,
          background: '#FFFFFF', border: '5px solid #0B409C',
          borderRadius: 24, padding: 40, textAlign: 'center',
          boxShadow: '0 20px 40px rgba(11,64,156,0.15)',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
            color: '#000000', opacity: 0.7, letterSpacing: 3,
          }}>
            FED RATE
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 136, color: '#0B409C',
            lineHeight: 1, marginTop: 8,
          }}>
            {rateVal.toFixed(2)}%
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 36,
            color: '#000000', fontWeight: 700, marginTop: 6,
          }}>
            कम होगा ↓
          </div>
        </div>

        {/* Arrow 1 */}
        <div style={{
          opacity: arrow1, fontSize: 100, color: '#0B409C',
          transform: `scale(${arrow1})`,
        }}>
          →
        </div>

        {/* DOLLAR */}
        <div style={{
          opacity: dollarBlock,
          transform: `scale(${dollarBlock})`,
          width: 320,
          background: '#FFFFFF', border: '5px solid #0B409C',
          borderRadius: 24, padding: 40, textAlign: 'center',
          boxShadow: '0 20px 40px rgba(11,64,156,0.15)',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
            color: '#000000', opacity: 0.7, letterSpacing: 3,
          }}>
            US DOLLAR
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 119, color: '#0B409C',
            lineHeight: 1, marginTop: 8,
          }}>
            $ ↓
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 32,
            color: '#000000', fontWeight: 700, marginTop: 6,
          }}>
            कमजोर
          </div>
        </div>

        {/* Arrow 2 */}
        <div style={{
          opacity: arrow2, fontSize: 100, color: '#0B409C',
          transform: `scale(${arrow2})`,
        }}>
          →
        </div>

        {/* GOLD */}
        <div style={{
          opacity: goldBlock,
          transform: `scale(${goldBlock})`,
          width: 320,
          background: '#0B409C',
          borderRadius: 24, padding: 40, textAlign: 'center',
          boxShadow: '0 20px 40px rgba(11,64,156,0.3)',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
            color: '#F2F7FF', opacity: 0.8, letterSpacing: 3,
          }}>
            GOLD
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 119, color: '#F2F7FF',
            lineHeight: 1, marginTop: 8,
          }}>
            ↑
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 32,
            color: '#F2F7FF', fontWeight: 700, marginTop: 6,
          }}>
            मजबूत
          </div>
        </div>
      </div>

      {/* Bottom formula */}
      <div style={{
        position: 'absolute', bottom: 100, left: '50%',
        transform: `translateX(-50%) translateY(${(1-sp(330))*30}px)`,
        opacity: sp(330),
        background: '#000000', padding: '22px 60px',
        borderRadius: 50,
      }}>
        <span style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 47,
          color: '#F2F7FF', fontWeight: 700,
        }}>
          डॉलर कमजोर = सोना मजबूत
        </span>
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_FedRates;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_BubbleFear',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_BubbleFear: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const stamp = sp(10, { stiffness: 200, damping: 8 });
  const question = sp(80);
  const drop20 = sp(180);
  const drop30 = sp(230);
  const reality = sp(310);

  // line chart drawing 50yr
  const drawProg = interpolate(frame, [40, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse at 50% 30%, rgba(11,64,156,0.1), transparent 60%)',
      }} />

      {/* Shape layer */}
      {[0,1,2,3].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${5+i*23}%`, top: `${15+(i%2)*55}%`,
          width: 300, height: 300,
          border: '2px solid #0B409C', borderRadius: i%3 ? '20%' : '50%',
          opacity: 0.04,
          transform: `rotate(${frame*0.2 + i*40}deg)`,
        }} />
      ))}

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        {Array.from({length: 15}).map((_, i) => (
          <line key={i} x1={0} y1={i*80} x2={1920} y2={i*80} stroke="#0B409C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({length: 24}).map((_, i) => {
        const s = seededRandom(i+35);
        const y = (s*1080 + frame*0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 5, height: 5, borderRadius: '50%',
            background: '#0B409C', opacity: 0.22,
          }} />
        );
      })}

      {/* Big stamp — 50 YEARS */}
      <div style={{
        position: 'absolute', top: 80, left: 100,
        opacity: stamp,
        transform: `scale(${stamp}) rotate(-4deg)`,
        transformOrigin: '0 50%',
        border: '8px solid #0B409C',
        padding: '20px 40px',
        borderRadius: 18,
      }}>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 102, color: '#0B409C',
          lineHeight: 1,
        }}>
          50 YEARS PROVEN
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 32,
          color: '#000000', fontWeight: 700, marginTop: 4,
        }}>
          ये गणित बार-बार दोहराया गया
        </div>
      </div>

      {/* Line chart */}
      <svg style={{ position: 'absolute', top: 260, left: 100 }} width="850" height="380">
        {/* axes */}
        <line x1="40" y1="340" x2="820" y2="340" stroke="#0B409C" strokeWidth="3" />
        <line x1="40" y1="20" x2="40" y2="340" stroke="#0B409C" strokeWidth="3" />

        {/* Line path simulating upward zigzag */}
        <path
          d={`M40,300 L160,260 L240,290 L340,210 L440,240 L540,150 L640,180 L750,80 L820,60`}
          fill="none" stroke="#0B409C" strokeWidth="6"
          strokeDasharray="2000"
          strokeDashoffset={(1-drawProg)*2000}
          strokeLinecap="round"
        />

        {/* dots */}
        {[
          [40,300],[160,260],[240,290],[340,210],[440,240],[540,150],[640,180],[750,80],[820,60]
        ].map(([x,y], i) => drawProg > i/9 && (
          <circle key={i} cx={x} cy={y} r="9" fill="#000000" />
        ))}

        {/* labels */}
        <text x="40" y="370" fontFamily="JetBrains Mono" fontSize="22" fill="#000" opacity="0.7">1975</text>
        <text x="430" y="370" fontFamily="JetBrains Mono" fontSize="22" fill="#000" opacity="0.7">2000</text>
        <text x="780" y="370" fontFamily="JetBrains Mono" fontSize="22" fill="#000" opacity="0.7">2025</text>
      </svg>

      {/* RIGHT — Question + drops */}
      <div style={{
        position: 'absolute', top: 280, right: 80, width: 720,
      }}>
        <div style={{
          opacity: question,
          transform: `translateY(${(1-question)*30}px)`,
          fontFamily: 'Yantra One', fontSize: 102, color: '#0B409C',
          lineHeight: 1,
        }}>
          फिर बबल क्यों?
        </div>
        <div style={{
          opacity: question,
          fontFamily: 'Noto Sans Devanagari', fontSize: 36,
          color: '#000000', opacity: 0.75, fontWeight: 600, marginTop: 8,
        }}>
          हर उचाई पर एक डर पैदा होता है
        </div>

        {/* Two drop chips */}
        <div style={{ display: 'flex', gap: 24, marginTop: 50 }}>
          <div style={{
            opacity: drop20,
            transform: `scale(${drop20})`,
            background: '#000000', color: '#F2F7FF',
            padding: '30px 40px', borderRadius: 20,
            fontFamily: 'Yantra One', fontSize: 102,
            boxShadow: '0 15px 30px rgba(0,0,0,0.3)',
          }}>
            -20%
          </div>
          <div style={{
            opacity: drop30,
            transform: `scale(${drop30})`,
            background: '#0B409C', color: '#F2F7FF',
            padding: '30px 40px', borderRadius: 20,
            fontFamily: 'Yantra One', fontSize: 102,
            boxShadow: '0 15px 30px rgba(11,64,156,0.3)',
          }}>
            -30%
          </div>
        </div>
        <div style={{
          opacity: drop30,
          fontFamily: 'Noto Sans Devanagari', fontSize: 32,
          color: '#000000', fontWeight: 700, marginTop: 16,
        }}>
          एक झटके में भी आ सकता है
        </div>
      </div>

      {/* Bottom — reality strip */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${(1-reality)*30}px)`,
        opacity: reality,
        background: '#0B409C', padding: '20px 50px',
        borderRadius: 50,
      }}>
        <span style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 47,
          color: '#F2F7FF', fontWeight: 700,
        }}>
          हिस्ट्री गवाह है
        </span>
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_BubbleFear;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_2011Case',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_2011Case: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const year = sp(10);
  const peakIn = sp(60);
  const sideways = sp(150);
  const verdictX = sp(260);
  const verdictTick = sp(310);

  const draw = interpolate(frame, [50, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'linear-gradient(135deg, rgba(11,64,156,0.06), transparent, rgba(11,64,156,0.08))',
      }} />

      {/* Shape layer */}
      {[0,1,2].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${10+i*30}%`, top: `${20+(i%2)*55}%`,
          width: 350, height: 350,
          border: '2px solid #0B409C', borderRadius: '50%',
          opacity: 0.05,
          transform: `scale(${1+Math.sin(frame*0.018+i)*0.07})`,
        }} />
      ))}

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        {Array.from({length: 20}).map((_, i) => (
          <line key={i} x1={i*100} y1={0} x2={i*100} y2={1080} stroke="#0B409C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({length: 22}).map((_, i) => {
        const s = seededRandom(i+40);
        const y = (s*1080 + frame*0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 5, height: 5, borderRadius: '50%',
            background: '#0B409C', opacity: 0.22,
          }} />
        );
      })}

      {/* Year stamp */}
      <div style={{
        position: 'absolute', top: 70, left: 100,
        opacity: year,
        transform: `translateY(${(1-year)*-30}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 32, color: '#000000',
          opacity: 0.6, letterSpacing: 5,
        }}>
          CASE STUDY
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 187, color: '#0B409C',
          lineHeight: 1,
        }}>
          2011
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 42,
          color: '#000000', fontWeight: 700, marginTop: 5,
        }}>
          सोना पीक पर पहुंचा
        </div>
      </div>

      {/* Chart: peak then 4yr flat */}
      <svg style={{ position: 'absolute', top: 320, right: 60 }} width="1100" height="450">
        <line x1="40" y1="380" x2="1080" y2="380" stroke="#0B409C" strokeWidth="3" />
        <line x1="40" y1="20" x2="40" y2="380" stroke="#0B409C" strokeWidth="3" />

        {/* up then flat */}
        <path
          d={`M40,340 L150,310 L260,250 L380,150 L490,80 L600,90 L720,100 L840,95 L960,85 L1060,180`}
          fill="none" stroke="#0B409C" strokeWidth="6"
          strokeDasharray="2400"
          strokeDashoffset={(1-draw)*2400}
          strokeLinecap="round"
        />

        {/* PEAK marker */}
        {peakIn > 0.1 && (
          <g opacity={peakIn}>
            <circle cx="490" cy="80" r="14" fill="#000000" />
            <line x1="490" y1="80" x2="490" y2="40" stroke="#000000" strokeWidth="3" />
            <text x="500" y="35" fontFamily="Yantra One" fontSize="42" fill="#000">PEAK</text>
          </g>
        )}

        {/* 4-year sideways highlight */}
        {sideways > 0.1 && (
          <g opacity={sideways}>
            <rect x="490" y="60" width="470" height="80" fill="#0B409C" opacity="0.18" rx="10" />
            <text x="600" y="200" fontFamily="Yantra One" fontSize="51" fill="#0B409C">4 साल साइडवेज</text>
          </g>
        )}

        {/* labels */}
        <text x="40" y="420" fontFamily="JetBrains Mono" fontSize="22" fill="#000" opacity="0.7">2008</text>
        <text x="480" y="420" fontFamily="JetBrains Mono" fontSize="22" fill="#000" opacity="0.7">2011</text>
        <text x="950" y="420" fontFamily="JetBrains Mono" fontSize="22" fill="#000" opacity="0.7">2015</text>
      </svg>

      {/* Bottom verdict — split */}
      <div style={{
        position: 'absolute', bottom: 80, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 30,
      }}>
        {/* WRONG */}
        <div style={{
          opacity: verdictX,
          transform: `translateX(${(1-verdictX)*-60}px)`,
          background: '#FFFFFF', border: '5px solid #000000',
          borderRadius: 20, padding: '24px 40px',
          display: 'flex', alignItems: 'center', gap: 24,
        }}>
          <div style={{
            width: 70, height: 70, borderRadius: '50%',
            background: '#000000', color: '#F2F7FF',
            fontFamily: 'Yantra One', fontSize: 60,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
          }}>
            ✕
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 47,
            color: '#000000', fontWeight: 700,
          }}>
            "मत खरीदो"
          </div>
        </div>

        {/* RIGHT */}
        <div style={{
          opacity: verdictTick,
          transform: `translateX(${(1-verdictTick)*60}px) scale(${verdictTick})`,
          background: '#0B409C',
          borderRadius: 20, padding: '24px 40px',
          display: 'flex', alignItems: 'center', gap: 24,
          boxShadow: '0 15px 30px rgba(11,64,156,0.3)',
        }}>
          <div style={{
            width: 70, height: 70, borderRadius: '50%',
            background: '#F2F7FF', color: '#0B409C',
            fontFamily: 'Yantra One', fontSize: 60,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
          }}>
            ✓
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 47,
            color: '#F2F7FF', fontWeight: 700,
          }}>
            समझदारी से खरीदो
          </div>
        </div>
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_2011Case;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_BuffettQuote',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_BuffettQuote: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const attr = sp(10);
  const quoteMark = sp(40, { stiffness: 200 });
  const line1 = sp(80);
  const line2 = sp(140);

  // X stamp on "predict"
  const xStamp = sp(200, { stiffness: 220, damping: 9 });
  // Tick on "control"
  const tick = sp(260, { stiffness: 220, damping: 9 });

  const finale = sp(330);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F2F7FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(circle at 50% 50%, rgba(11,64,156,0.1), transparent 60%)',
      }} />

      {/* Shape layer */}
      {[0,1,2,3].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${i*25}%`, top: `${(i%2)*60}%`,
          width: 380, height: 380,
          border: '2px solid #0B409C', borderRadius: '50%',
          opacity: 0.05,
          transform: `scale(${1+Math.sin(frame*0.02+i)*0.08}) rotate(${frame*0.15}deg)`,
        }} />
      ))}

      {/* Texture — dots */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        {Array.from({length: 18}).map((_, r) =>
          Array.from({length: 32}).map((__, c) => (
            <circle key={`${r}-${c}`} cx={c*60+25} cy={r*60+25} r="2" fill="#0B409C" />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({length: 26}).map((_, i) => {
        const s = seededRandom(i+45);
        const y = (s*1080 + frame*0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s*100}%`, top: y,
            width: 5, height: 5, borderRadius: '50%',
            background: '#0B409C', opacity: 0.22,
          }} />
        );
      })}

      {/* Attribution top */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%) translateY(${(1-attr)*-30}px)`,
        opacity: attr, textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 30, color: '#000000',
          opacity: 0.6, letterSpacing: 5,
        }}>
          ORACLE OF OMAHA
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 85, color: '#0B409C',
          lineHeight: 1, marginTop: 8,
        }}>
          वारेन बफे साहब
        </div>
      </div>

      {/* Giant quote mark */}
      <div style={{
        position: 'absolute', top: 220, left: 100,
        opacity: quoteMark * 0.25,
        transform: `scale(${quoteMark})`,
        fontFamily: 'Yantra One', fontSize: 400,
        color: '#0B409C', lineHeight: 0.8,
      }}>
        "
      </div>

      {/* Main quote — line 1 with X */}
      <div style={{
        position: 'absolute', top: 320, left: 180, right: 180,
      }}>
        <div style={{
          opacity: line1,
          transform: `translateY(${(1-line1)*30}px)`,
          fontFamily: 'Noto Sans Devanagari', fontSize: 64,
          color: '#000000', fontWeight: 700, lineHeight: 1.3,
        }}>
          मार्केट की दिशा <span style={{
            position: 'relative', display: 'inline-block',
            color: '#0B409C',
          }}>
            प्रेडिक्ट
            {/* X stamp */}
            {xStamp > 0.1 && (
              <span style={{
                position: 'absolute', top: -20, right: -50,
                opacity: xStamp,
                transform: `scale(${xStamp}) rotate(15deg)`,
                fontFamily: 'Yantra One', fontSize: 95,
                color: '#000000',
                textShadow: '0 0 0 #000',
              }}>
                ✕
              </span>
            )}
          </span> करना <u style={{textDecorationColor: '#0B409C', textDecorationThickness: '4px'}}>नामुमकिन</u> है,
        </div>

        {/* Line 2 with tick */}
        <div style={{
          opacity: line2,
          transform: `translateY(${(1-line2)*30}px)`,
          fontFamily: 'Noto Sans Devanagari', fontSize: 64,
          color: '#000000', fontWeight: 700, lineHeight: 1.3,
          marginTop: 30,
        }}>
          पर अपने <span style={{
            position: 'relative', display: 'inline-block',
            color: '#0B409C',
          }}>
            बिहेवियर
            {tick > 0.1 && (
              <span style={{
                position: 'absolute', top: -25, right: -55,
                opacity: tick,
                transform: `scale(${tick})`,
                fontFamily: 'Yantra One', fontSize: 95,
                color: '#0B409C',
              }}>
                ✓
              </span>
            )}
          </span> को कंट्रोल करना <u style={{textDecorationColor: '#0B409C', textDecorationThickness: '4px'}}>पॉसिबल</u> है।
        </div>
      </div>

      {/* Finale strip */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${(1-finale)*30}px)`,
        opacity: finale,
        background: '#000000', padding: '22px 60px',
        borderRadius: 50,
      }}>
        <span style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 47,
          color: '#F2F7FF', fontWeight: 700, letterSpacing: 1,
        }}>
          सबसे बड़ा सबक यही है
        </span>
      </div>

      {/* Vignette */}
      <div style={{
        position: 'absolute', inset: 0,
        boxShadow: 'inset 0 0 250px rgba(11,64,156,0.18)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_BuffettQuote;
```

