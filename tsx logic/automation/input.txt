```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01TrillionSignal',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01TrillionSignal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counting number 0 -> 8
  const count = interpolate(frame, [50, 130], [0, 8], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Screen shake on impact (frame 130-145)
  const shake = frame >= 130 && frame <= 145 ? Math.sin(frame * 3) * (145 - frame) * 0.6 : 0;

  // Slow camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08]);

  const words = ['EIGHT.', 'TRILLION.', 'DOLLARS.'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${50 + Math.sin(frame * 0.01) * 10}% 40%, #FFE3F8 0%, #FFCEF3 55%, #F9B8E8 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        border: '3px solid #000000', opacity: 0.05,
        left: 200 + Math.sin(frame * 0.008) * 40, top: -180,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, backgroundColor: '#000000', opacity: 0.04,
        right: -120, bottom: -140, transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Motion texture: vertical ticker lines */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', top: 0, bottom: 0, width: 1,
          left: i * 170 + ((frame * 0.4) % 170), backgroundColor: '#000000', opacity: 0.05,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 1);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: ((seededRandom(i + 50) * 1080) + frame * (0.3 + s * 0.5)) % 1080,
            width: 4 + s * 5, height: 4 + s * 5, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.08,
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        justifyContent: 'center', alignItems: 'center',
        transform: `scale(${camScale}) translate(${shake}px, ${shake * 0.6}px)`,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}>
        {/* Top kicker */}
        <div style={{
          opacity: sp(10), transform: `translateY(${(1 - sp(10)) * -40}px)`,
          fontSize: 34, fontWeight: 700, letterSpacing: 12, color: '#000000',
          borderBottom: '3px solid #000000', paddingBottom: 14, marginBottom: 40,
        }}>
          EVERY SERIOUS CONVERSATION ON WALL STREET
        </div>

        {/* Hero counting number */}
        <div style={{
          display: 'flex', alignItems: 'baseline', gap: 24,
          transform: `scale(${sp(45)})`,
        }}>
          <span style={{ fontSize: 100, fontWeight: 900, color: '#000000' }}>$</span>
          <span style={{
            fontSize: 300, fontWeight: 900, color: '#000000', lineHeight: 1,
            fontFamily: 'JetBrains Mono, monospace',
          }}>
            {count.toFixed(1)}
          </span>
          <span style={{ fontSize: 90, fontWeight: 900, color: '#000000' }}>T</span>
        </div>

        {/* Three-beat word reveal */}
        <div style={{ display: 'flex', gap: 50, marginTop: 50 }}>
          {words.map((w, i) => (
            <div key={w} style={{
              fontSize: 74, fontWeight: 900, letterSpacing: 4,
              color: i === 1 ? '#EEEEEE' : '#000000',
              backgroundColor: i === 1 ? '#000000' : 'transparent',
              padding: i === 1 ? '6px 28px' : '6px 0',
              opacity: sp(150 + i * 20),
              transform: `translateY(${(1 - sp(150 + i * 20)) * 60}px)`,
            }}>
              {w}
            </div>
          ))}
        </div>

        {/* Bottom line */}
        <div style={{
          marginTop: 55, fontSize: 40, fontWeight: 600, color: '#000000',
          opacity: interpolate(frame, [560, 590], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          NOT A ROUNDING ERROR. — GOLDMAN SACHS BASELINE
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.14) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene01TrillionSignal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02QuietStreet',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02QuietStreet: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Waveform flattens over time: loud -> quiet
  const amplitude = interpolate(frame, [120, 420], [70, 4], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad),
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -50]);

  // Stamp impact shake
  const stampF = 640;
  const shake = frame >= stampF && frame <= stampF + 12 ? Math.sin(frame * 4) * (stampF + 12 - frame) * 0.8 : 0;

  const waveBars = 60;
  const quietWords = ['QUIETLY,', 'CAREFULLY,', 'DELIBERATELY', 'WORRIED.'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #FFDDF6 0%, #FFCEF3 50%, #F6ADE2 100%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900,
        border: '2px solid #000000', opacity: 0.04, borderRadius: '50%',
        left: -300, top: 300 + Math.sin(frame * 0.01) * 30,
      }} />
      <div style={{
        position: 'absolute', width: 0, height: 0, opacity: 0.05,
        borderLeft: '260px solid transparent', borderRight: '260px solid transparent',
        borderBottom: '440px solid #000000',
        right: -100, top: -150, transform: `rotate(${25 + frame * 0.06}deg)`,
      }} />

      {/* Motion texture: horizontal lines */}
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', left: 0, right: 0, height: 1,
          top: i * 150, backgroundColor: '#000000', opacity: 0.05,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 7);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (s * 1920 + frame * (0.2 + s * 0.4)) % 1920,
            top: seededRandom(i + 90) * 1080,
            width: 3 + s * 5, height: 3 + s * 5,
            backgroundColor: '#000000', opacity: 0.07, borderRadius: '50%',
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        justifyContent: 'center', alignItems: 'center',
        transform: `translate(${camX + shake}px, ${shake}px)`,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}>
        <div style={{
          opacity: sp(8), transform: `translateY(${(1 - sp(8)) * -30}px)`,
          fontSize: 46, fontWeight: 800, color: '#000000', letterSpacing: 3, marginBottom: 24,
        }}>
          THE SHARPEST MINDS ARE NOT CELEBRATING
        </div>

        {/* Market noise waveform that goes silent */}
        <div style={{
          display: 'flex', alignItems: 'center', gap: 8, height: 190, marginBottom: 40,
          opacity: sp(60),
        }}>
          {Array.from({ length: waveBars }).map((_, i) => {
            const h = 8 + Math.abs(Math.sin(i * 1.7 + frame * 0.25)) * amplitude * (0.5 + seededRandom(i) * 0.8);
            return (
              <div key={i} style={{
                width: 14, height: h, backgroundColor: '#000000',
                opacity: 0.85, borderRadius: 4,
              }} />
            );
          })}
        </div>

        {/* Staggered word reveal */}
        <div style={{ display: 'flex', gap: 34 }}>
          {quietWords.map((w, i) => (
            <span key={w} style={{
              fontSize: 64, fontWeight: 900, color: '#000000',
              opacity: sp(200 + i * 22),
              transform: `translateY(${(1 - sp(200 + i * 22)) * 50}px)`,
            }}>
              {w}
            </span>
          ))}
        </div>

        {/* Rubber stamp */}
        <div style={{
          marginTop: 70,
          transform: `scale(${interpolate(sp(stampF - 610 + 610 - 610 + 610), [0, 1], [1, 1])}) scale(${interpolate(
            spring({ frame: Math.max(0, frame - 610), fps, config: { damping: 9, stiffness: 200 } }),
            [0, 1], [2.4, 1]
          )}) rotate(-4deg)`,
          opacity: frame >= 610 ? 1 : 0,
          border: '6px solid #000000', color: '#000000',
          padding: '14px 46px', fontSize: 68, fontWeight: 900, letterSpacing: 6,
        }}>
          PAY ATTENTION
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene02QuietStreet;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03PatternHistory',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03PatternHistory: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1]);

  const crises = [
    { year: '1929', x: 260 },
    { year: '1973', x: 660 },
    { year: '2000', x: 1060 },
    { year: '2008', x: 1460 },
  ];

  const lineW = interpolate(frame, [300, 420], [0, 1560], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${135 + Math.sin(frame * 0.005) * 10}deg, #FFE6F9 0%, #FFCEF3 60%, #F2A6DD 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 620, height: 620, backgroundColor: '#000000', opacity: 0.035,
        borderRadius: '50%', left: -200, top: -220, transform: `scale(${1 + Math.sin(frame * 0.015) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 420, height: 420, border: '3px solid #000000', opacity: 0.06,
        right: -80, bottom: -100, transform: `rotate(${frame * 0.12}deg)`,
      }} />

      {/* Motion texture: dot grid */}
      {Array.from({ length: 60 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (i % 10) * 200 + 60,
          top: Math.floor(i / 10) * 190 + 40 + Math.sin(frame * 0.03 + i) * 4,
          width: 5, height: 5, borderRadius: '50%', backgroundColor: '#000000', opacity: 0.06,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 21);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: (seededRandom(i + 33) * 1080 + frame * 0.5) % 1080,
            width: 4, height: 4, backgroundColor: '#000000', opacity: 0.08, borderRadius: '50%',
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        justifyContent: 'center', alignItems: 'center',
        transform: `scale(${camScale})`, fontFamily: 'Inter, Poppins, sans-serif',
      }}>
        {/* Brand block */}
        <div style={{
          backgroundColor: '#000000', color: '#EEEEEE', padding: '20px 70px',
          fontSize: 110, fontWeight: 900, letterSpacing: 6,
          transform: `scale(${sp(15)}) rotate(${(1 - sp(15)) * -6}deg)`,
        }}>
          FAILED FINANCE
        </div>
        <div style={{
          marginTop: 26, fontSize: 42, fontWeight: 700, color: '#000000', letterSpacing: 2,
          opacity: sp(70), transform: `translateY(${(1 - sp(70)) * 30}px)`,
        }}>
          UNCOMFORTABLE REALITIES. STRAIGHT IN THE FACE.
        </div>

        {/* History pattern timeline */}
        <div style={{ position: 'relative', width: 1560, height: 240, marginTop: 90 }}>
          <div style={{
            position: 'absolute', top: 120, left: 0, height: 6,
            width: lineW, backgroundColor: '#000000',
          }} />
          {crises.map((c, i) => {
            const d = 330 + i * 25;
            return (
              <div key={c.year} style={{
                position: 'absolute', left: c.x - 70, top: 20, width: 140, textAlign: 'center',
                opacity: sp(d), transform: `translateY(${(1 - sp(d)) * 40}px)`,
              }}>
                <div style={{ fontSize: 50, fontWeight: 900, color: '#000000' }}>{c.year}</div>
                <div style={{
                  width: 26, height: 26, borderRadius: '50%', backgroundColor: '#000000',
                  margin: '16px auto',
                  boxShadow: `0 0 0 ${6 + Math.sin(frame * 0.15 + i) * 4}px rgba(0,0,0,0.12)`,
                }} />
                <div style={{ fontSize: 26, fontWeight: 700, color: '#000000', opacity: 0.7 }}>STEEP PRICE</div>
              </div>
            );
          })}
          {/* The next question mark */}
          <div style={{
            position: 'absolute', left: 1560 - 60, top: 46,
            fontSize: 90, fontWeight: 900, color: '#000000',
            opacity: sp(460), transform: `scale(${sp(460)})`,
          }}>
            ?
          </div>
        </div>

        <div style={{
          marginTop: 40, fontSize: 46, fontWeight: 800, color: '#000000',
          opacity: interpolate(frame, [520, 550], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          THE PATTERN HAS SHOWN UP BEFORE. MULTIPLE TIMES.
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 58%, rgba(0,0,0,0.14) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene03PatternHistory;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04BullRun',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04BullRun: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Rising S&P line drawn progressively
  const progress = interpolate(frame, [90, 330], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  const pts: Array<[number, number]> = [
    [0, 420], [120, 400], [240, 410], [360, 360], [480, 370], [600, 310],
    [720, 320], [840, 260], [960, 275], [1080, 210], [1200, 220], [1320, 150], [1440, 90],
  ];
  const visible = Math.max(2, Math.ceil(pts.length * progress));
  const lineStr = pts.slice(0, visible).map((p) => `${p[0]},${p[1]}`).join(' ');

  const tickers = ['NVIDIA', 'ALPHABET', 'BROADCOM', 'MICROSOFT'];
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #FFD9F6 0%, #FFCEF3 45%, #F8B3E7 100%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 560, height: 560, border: '3px solid #000000',
        opacity: 0.05, borderRadius: '50%', right: 120, top: -200,
        transform: `scale(${1 + Math.sin(frame * 0.018) * 0.05}) rotate(${frame * 0.05}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 380, height: 380, backgroundColor: '#000000', opacity: 0.04,
        left: -120, bottom: -120, transform: `rotate(${frame * -0.08}deg)`,
      }} />

      {/* Motion texture: chart grid */}
      {Array.from({ length: 7 }).map((_, i) => (
        <div key={`h${i}`} style={{
          position: 'absolute', left: 160, right: 160, height: 1, top: 220 + i * 110,
          backgroundColor: '#000000', opacity: 0.08,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 5);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: (seededRandom(i + 77) * 1080 - frame * (0.4 + s * 0.4) + 1080) % 1080,
            width: 4 + s * 4, height: 4 + s * 4, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.07,
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        justifyContent: 'center', alignItems: 'center',
        transform: `scale(${camScale})`, fontFamily: 'Inter, Poppins, sans-serif',
      }}>
        <div style={{
          fontSize: 88, fontWeight: 900, color: '#000000', letterSpacing: 3,
          opacity: sp(10), transform: `translateY(${(1 - sp(10)) * -40}px)`, marginBottom: 20,
        }}>
          S&P 500 — 2025
        </div>

        {/* Chart */}
        <div style={{ position: 'relative', width: 1440, height: 460 }}>
          <svg width={1440} height={460} viewBox="0 0 1440 460">
            <polyline
              points={lineStr}
              fill="none"
              stroke="#000000"
              strokeWidth={10}
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
          {/* +16% badge at line tip */}
          <div style={{
            position: 'absolute', left: 1440 - 200, top: 10,
            backgroundColor: '#000000', color: '#EEEEEE',
            fontSize: 64, fontWeight: 900, padding: '10px 30px', borderRadius: 12,
            transform: `scale(${sp(330)})`,
          }}>
            +16%
          </div>
        </div>

        {/* Ticker chips */}
        <div style={{ display: 'flex', gap: 28, marginTop: 44 }}>
          {tickers.map((t, i) => (
            <div key={t} style={{
              border: '4px solid #000000', color: '#000000',
              fontSize: 40, fontWeight: 800, padding: '10px 30px',
              opacity: sp(380 + i * 18),
              transform: `translateY(${(1 - sp(380 + i * 18)) * 46}px) rotate(${Math.sin(frame * 0.04 + i) * 1.2}deg)`,
              backgroundColor: '#FFE3F8',
            }}>
              {t}
            </div>
          ))}
        </div>

        <div style={{
          marginTop: 44, fontSize: 48, fontWeight: 900, color: '#000000', letterSpacing: 8,
          opacity: interpolate(frame, [560, 590], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `scale(${1 + Math.sin(frame * 0.1) * 0.01})`,
        }}>
          NEW HIGHS. RECORD CLOSES. BULL MARKET.
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.13) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene04BullRun;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05Divergence',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05Divergence: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Two lines share a path, then split
  const progress = interpolate(frame, [70, 320], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  const N = 40;
  const buildLine = (dir: 1 | -1) => {
    const pts: string[] = [];
    const count = Math.max(2, Math.ceil(N * progress));
    for (let i = 0; i < count; i++) {
      const x = (i / (N - 1)) * 1400;
      const base = 260 - i * 1.2 + Math.sin(i * 0.8) * 10;
      const splitStart = 0.45 * 1400;
      const split = x > splitStart ? ((x - splitStart) / (1400 - splitStart)) * 150 * dir : 0;
      pts.push(`${x},${base - split}`);
    }
    return pts.join(' ');
  };

  const shake = frame >= 470 && frame <= 482 ? Math.sin(frame * 4) * (482 - frame) * 0.7 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 30% ${60 + Math.sin(frame * 0.008) * 8}%, #FFE0F7 0%, #FFCEF3 55%, #F3A9DE 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 760, height: 760, border: '2px solid #000000', opacity: 0.045,
        transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.012) * 0.04})`,
        right: -260, top: 160,
      }} />
      <div style={{
        position: 'absolute', width: 340, height: 340, borderRadius: '50%',
        backgroundColor: '#000000', opacity: 0.035, left: 80, bottom: -80,
        transform: `translateY(${Math.sin(frame * 0.02) * 16}px)`,
      }} />

      {/* Motion texture: diagonal lines */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', width: 2600, height: 1, backgroundColor: '#000000', opacity: 0.05,
          left: -300, top: i * 90 + ((frame * 0.3) % 90), transform: 'rotate(-8deg)',
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 11);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (s * 1920 + frame * 0.35) % 1920,
            top: seededRandom(i + 44) * 1080,
            width: 4, height: 4, backgroundColor: '#000000', opacity: 0.08, borderRadius: '50%',
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        justifyContent: 'center', alignItems: 'center',
        transform: `translate(${shake}px, ${shake * 0.5}px)`,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}>
        <div style={{
          fontSize: 80, fontWeight: 900, color: '#000000', letterSpacing: 4, marginBottom: 26,
          opacity: sp(8), transform: `translateY(${(1 - sp(8)) * -36}px)`,
        }}>
          THE DIVERGENCE
        </div>

        <div style={{ position: 'relative', width: 1400, height: 420 }}>
          <svg width={1400} height={420} viewBox="0 0 1400 420">
            {/* Index line going up */}
            <polyline points={buildLine(1)} fill="none" stroke="#000000" strokeWidth={9} strokeLinecap="round" />
            {/* Tech line going down (rendered lighter) */}
            <polyline points={buildLine(-1)} fill="none" stroke="#EEEEEE" strokeWidth={9} strokeLinecap="round" />
          </svg>
          <div style={{
            position: 'absolute', right: -20, top: 0, fontSize: 38, fontWeight: 800,
            color: '#000000', opacity: sp(320),
          }}>
            INDEX — HIGHER
          </div>
          <div style={{
            position: 'absolute', right: -20, bottom: 20, fontSize: 38, fontWeight: 800,
            color: '#EEEEEE', backgroundColor: '#000000', padding: '6px 18px',
            opacity: sp(345),
          }}>
            TECH — 10% BELOW PEAK
          </div>
        </div>

        <div style={{
          marginTop: 46, display: 'flex', gap: 30, alignItems: 'center',
        }}>
          <div style={{
            fontSize: 66, fontWeight: 900, color: '#EEEEEE', backgroundColor: '#000000',
            padding: '10px 36px', transform: `scale(${spring({ frame: Math.max(0, frame - 470), fps, config: { damping: 9, stiffness: 190 } })})`,
          }}>
            THAT IS A SIGNAL
          </div>
        </div>

        <div style={{
          marginTop: 34, fontSize: 38, fontWeight: 700, color: '#000000', maxWidth: 1300, textAlign: 'center',
          opacity: interpolate(frame, [560, 592], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          "AS DIVERGENCE GROWS, SO DO THE RISKS OF SOMETHING GOING BADLY WRONG" — JPMORGAN AM
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene05Divergence;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06WrongDefinition',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06WrongDefinition: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // "WRONG" slams in with shake
  const slam = spring({ frame: Math.max(0, frame - 40), fps, config: { damping: 8, stiffness: 210 } });
  const shake = frame >= 40 && frame <= 54 ? Math.sin(frame * 4.2) * (54 - frame) * 1 : 0;

  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(200deg, #FFE3F8 0%, #FFCEF3 50%, #F5AEE1 100%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 820, height: 820, borderRadius: '50%',
        border: '2px dashed #000000', opacity: 0.06,
        left: 1150, top: -300, transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 300, height: 300, backgroundColor: '#000000', opacity: 0.04,
        left: -80, top: 620, transform: `rotate(${12 + Math.sin(frame * 0.02) * 5}deg)`,
      }} />

      {/* Motion texture: concentric circles */}
      {Array.from({ length: 5 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', left: 960 - (200 + i * 130), top: 540 - (200 + i * 130),
          width: (200 + i * 130) * 2, height: (200 + i * 130) * 2,
          borderRadius: '50%', border: '1px solid #000000',
          opacity: 0.05 - i * 0.006,
          transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.015})`,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const s = seededRandom(i + 3);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: ((seededRandom(i + 60) * 1080) + frame * (0.25 + s * 0.45)) % 1080,
            width: 3 + s * 5, height: 3 + s * 5, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.07,
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        justifyContent: 'center', alignItems: 'center',
        transform: `scale(${camScale}) translate(${shake}px, ${shake * 0.7}px)`,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}>
        {/* The word */}
        <div style={{
          fontSize: 190, fontWeight: 900, color: '#000000', letterSpacing: 10,
          transform: `scale(${interpolate(slam, [0, 1], [2.6, 1])})`,
          opacity: frame >= 40 ? 1 : 0,
          textDecoration: 'underline', textDecorationThickness: 12, textUnderlineOffset: 18,
        }}>
          "WRONG"
        </div>
        <div style={{
          fontSize: 44, fontWeight: 700, color: '#000000', marginTop: 24,
          opacity: sp(110), transform: `translateY(${(1 - sp(110)) * 30}px)`,
        }}>
          HE USED THAT WORD. THAT MATTERS.
        </div>

        {/* Dictionary card */}
        <div style={{
          marginTop: 60, width: 1240, backgroundColor: '#000000', color: '#EEEEEE',
          padding: '44px 60px', borderRadius: 8,
          opacity: sp(230), transform: `translateY(${(1 - sp(230)) * 80}px)`,
          boxShadow: '0 30px 60px rgba(0,0,0,0.25)',
        }}>
          <div style={{ display: 'flex', alignItems: 'baseline', gap: 22 }}>
            <span style={{ fontSize: 62, fontWeight: 900 }}>AI bubble</span>
            <span style={{ fontSize: 32, fontStyle: 'italic', opacity: 0.7 }}>/ noun / theorized — since 2025</span>
          </div>
          <div style={{ height: 3, backgroundColor: '#EEEEEE', opacity: 0.3, margin: '22px 0' }} />
          <div style={{ fontSize: 37, fontWeight: 500, lineHeight: 1.5 }}>
            A stock market bubble growing amid the AI boom — rapid investment in artificial
            intelligence now affecting the <span style={{ fontWeight: 900, color: '#FFCEF3' }}>broader economy</span>.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene06WrongDefinition;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07CircularFlow',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07CircularFlow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const nodes = ['CHIPS', 'CLOUD', 'STARTUPS'];
  const R = 300;
  const cx = 620;
  const cy = 560;
  const orbit = frame * 0.012;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 70% 30%, #FFE4F8 0%, #FFCEF3 50%, #F2A8DD 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 640, height: 640, border: '3px solid #000000', opacity: 0.05,
        borderRadius: '50%', right: -160, bottom: -160,
        transform: `scale(${1 + Math.sin(frame * 0.014) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 260, height: 260, backgroundColor: '#000000', opacity: 0.035,
        left: 1500, top: 90, borderRadius: 40, transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Motion texture: fine grid */}
      {Array.from({ length: 10 }).map((_, i) => (
        <div key={`v${i}`} style={{
          position: 'absolute', top: 0, bottom: 0, width: 1, left: 100 + i * 190,
          backgroundColor: '#000000', opacity: 0.045,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 17);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (s * 1920 - frame * (0.2 + s * 0.3) + 1920) % 1920,
            top: seededRandom(i + 29) * 1080,
            width: 4, height: 4, backgroundColor: '#000000', opacity: 0.08, borderRadius: '50%',
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ fontFamily: 'Inter, Poppins, sans-serif' }}>
        <div style={{
          position: 'absolute', top: 90, left: 120,
          fontSize: 76, fontWeight: 900, color: '#000000', letterSpacing: 3,
          opacity: sp(8), transform: `translateY(${(1 - sp(8)) * -30}px)`,
        }}>
          MONEY MOVING IN CIRCLES
        </div>

        {/* Circular flow diagram */}
        <div style={{ position: 'absolute', left: 0, top: 0 }}>
          <svg width={1920} height={1080}>
            <circle
              cx={cx} cy={cy} r={R}
              fill="none" stroke="#000000" strokeWidth={6}
              strokeDasharray="26 20"
              strokeDashoffset={-frame * 1.6}
              opacity={sp(60)}
            />
            {/* Orbiting money dot */}
            <circle
              cx={cx + Math.cos(orbit * 6) * R}
              cy={cy + Math.sin(orbit * 6) * R}
              r={16} fill="#000000" opacity={sp(90)}
            />
          </svg>
          {nodes.map((n, i) => {
            const a = (i / nodes.length) * Math.PI * 2 - Math.PI / 2;
            return (
              <div key={n} style={{
                position: 'absolute',
                left: cx + Math.cos(a) * R - 110,
                top: cy + Math.sin(a) * R - 46,
                width: 220, textAlign: 'center',
                backgroundColor: '#000000', color: '#EEEEEE',
                fontSize: 40, fontWeight: 900, padding: '20px 0', borderRadius: 10,
                transform: `scale(${sp(120 + i * 25)}) translateY(${Math.sin(frame * 0.05 + i * 2) * 5}px)`,
              }}>
                {n}
              </div>
            );
          })}
          <div style={{
            position: 'absolute', left: cx - 190, top: cy - 40, width: 380, textAlign: 'center',
            fontSize: 42, fontWeight: 800, color: '#000000', opacity: sp(220),
          }}>
            SAME DOLLARS, INFLATED STOCKS
          </div>
        </div>

        {/* Right-side statement stack */}
        <div style={{ position: 'absolute', right: 130, top: 330, width: 560 }}>
          {[
            { t: 'AI IS NOT FAKE.', d: 300 },
            { t: 'THE TECH IS EXTRAORDINARY.', d: 340 },
            { t: 'BUT THE MONEY IS RUNNING AHEAD OF REALITY.', d: 400 },
          ].map((row, i) => (
            <div key={i} style={{
              fontSize: i === 2 ? 52 : 44, fontWeight: 900,
              color: i === 2 ? '#EEEEEE' : '#000000',
              backgroundColor: i === 2 ? '#000000' : 'transparent',
              borderLeft: i === 2 ? 'none' : '8px solid #000000',
              padding: i === 2 ? '22px 26px' : '10px 0 10px 26px',
              marginBottom: 34, lineHeight: 1.2,
              opacity: sp(row.d), transform: `translateX(${(1 - sp(row.d)) * 90}px)`,
            }}>
              {row.t}
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene07CircularFlow;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08CapexTowers',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08CapexTowers: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const companies = [
    { name: 'AMAZON', h: 420 },
    { name: 'ALPHABET', h: 360 },
    { name: 'META', h: 300 },
    { name: 'MICROSOFT', h: 400 },
  ];

  // Counting total
  const total = interpolate(frame, [140, 320], [0, 300], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #FFE1F7 0%, #FFCEF3 55%, #F4ACE0 100%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 520, height: 520, borderRadius: '50%',
        backgroundColor: '#000000', opacity: 0.035, right: -140, top: -160,
        transform: `scale(${1 + Math.sin(frame * 0.016) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 700, height: 700, border: '2px solid #000000', opacity: 0.05,
        left: -260, bottom: -300, transform: `rotate(${frame * 0.07}deg)`,
      }} />

      {/* Motion texture: baseline grid */}
      {Array.from({ length: 6 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', left: 200, right: 200, height: 1,
          bottom: 240 + i * 90, backgroundColor: '#000000', opacity: 0.07,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 13);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: (seededRandom(i + 40) * 1080 - frame * (0.3 + s * 0.5) + 2160) % 1080,
            width: 3 + s * 5, height: 3 + s * 5, backgroundColor: '#000000',
            opacity: 0.08, borderRadius: '50%',
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        alignItems: 'center', transform: `scale(${camScale})`,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}>
        {/* Headline counter */}
        <div style={{ marginTop: 90, textAlign: 'center' }}>
          <div style={{
            fontSize: 44, fontWeight: 800, color: '#000000', letterSpacing: 6,
            opacity: sp(10),
          }}>
            CAPITAL EXPENDITURE — 2025
          </div>
          <div style={{
            fontSize: 170, fontWeight: 900, color: '#000000', lineHeight: 1.05,
            fontFamily: 'JetBrains Mono, monospace',
            transform: `scale(${sp(40)})`,
          }}>
            ${Math.round(total)}B
          </div>
        </div>

        {/* Company towers */}
        <div style={{
          position: 'absolute', bottom: 240, display: 'flex', gap: 70, alignItems: 'flex-end',
        }}>
          {companies.map((c, i) => {
            const grow = sp(160 + i * 22, { damping: 14 });
            return (
              <div key={c.name} style={{ textAlign: 'center' }}>
                <div style={{
                  width: 190, height: c.h * grow, backgroundColor: '#000000',
                  borderRadius: '8px 8px 0 0', position: 'relative', overflow: 'hidden',
                }}>
                  {/* Window texture inside tower */}
                  {Array.from({ length: 6 }).map((_, w) => (
                    <div key={w} style={{
                      position: 'absolute', left: 20, right: 20, height: 3,
                      top: 26 + w * 46, backgroundColor: '#FFCEF3', opacity: 0.35,
                    }} />
                  ))}
                </div>
                <div style={{
                  marginTop: 16, fontSize: 32, fontWeight: 900, color: '#000000',
                  opacity: grow,
                }}>
                  {c.name}
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom comparison strip */}
        <div style={{
          position: 'absolute', bottom: 110,
          backgroundColor: '#000000', color: '#EEEEEE',
          fontSize: 42, fontWeight: 800, padding: '14px 44px', borderRadius: 8,
          opacity: sp(470), transform: `translateY(${(1 - sp(470)) * 50}px)`,
        }}>
          FOUR COMPANIES. ONE YEAR. MORE THAN MOST COUNTRIES' GDP.
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.14) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene08CapexTowers;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09GdpShare',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09GdpShare: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 0 -> 1.3 -> 1.6 percentage journey
  const pct = interpolate(frame, [90, 260, 420, 560], [0, 1.3, 1.3, 1.6], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  // Progress bar fill (scaled: 2% = full width)
  const barW = interpolate(pct, [0, 2], [0, 1400]);

  const shake = frame >= 560 && frame <= 572 ? Math.sin(frame * 4) * (572 - frame) * 0.6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${30 + Math.sin(frame * 0.007) * 8}% 70%, #FFE5F9 0%, #FFCEF3 55%, #F1A5DC 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '2px solid #000000', opacity: 0.045, right: -350, top: -350,
        transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.011) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute', width: 0, height: 0, opacity: 0.045,
        borderLeft: '200px solid transparent', borderRight: '200px solid transparent',
        borderTop: '340px solid #000000', left: -60, bottom: -100,
        transform: `rotate(${-10 + Math.sin(frame * 0.015) * 4}deg)`,
      }} />

      {/* Motion texture: percentage rulers */}
      {Array.from({ length: 20 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', width: 2, height: i % 5 === 0 ? 34 : 16,
          left: 260 + i * 74, top: 700, backgroundColor: '#000000', opacity: 0.18,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 9);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (s * 1920 + frame * (0.25 + s * 0.35)) % 1920,
            top: seededRandom(i + 71) * 1080,
            width: 4 + s * 4, height: 4 + s * 4, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.07,
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        justifyContent: 'center', alignItems: 'center',
        transform: `translate(${shake}px, ${shake * 0.5}px)`,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}>
        <div style={{
          fontSize: 54, fontWeight: 800, color: '#000000', letterSpacing: 4,
          opacity: sp(8), transform: `translateY(${(1 - sp(8)) * -34}px)`,
        }}>
          HYPERSCALER CAPEX AS SHARE OF U.S. GDP
        </div>

        {/* Giant live percentage */}
        <div style={{
          fontSize: 280, fontWeight: 900, color: '#000000', lineHeight: 1,
          fontFamily: 'JetBrains Mono, monospace', marginTop: 20,
          transform: `scale(${sp(50)})`,
        }}>
          {pct.toFixed(1)}%
        </div>

        {/* Progress bar */}
        <div style={{
          width: 1400, height: 56, border: '5px solid #000000', borderRadius: 32,
          marginTop: 50, overflow: 'hidden', opacity: sp(80),
        }}>
          <div style={{
            width: barW, height: '100%', backgroundColor: '#000000',
            borderRadius: 26,
          }} />
        </div>

        {/* Year markers */}
        <div style={{ width: 1400, display: 'flex', justifyContent: 'space-between', marginTop: 24 }}>
          <div style={{
            fontSize: 40, fontWeight: 800, color: '#000000', opacity: sp(260),
          }}>
            2025 — 1.3%
          </div>
          <div style={{
            fontSize: 40, fontWeight: 900, color: '#EEEEEE', backgroundColor: '#000000',
            padding: '6px 22px', opacity: sp(540),
            transform: `scale(${spring({ frame: Math.max(0, frame - 540), fps, config: { damping: 9, stiffness: 200 } })})`,
          }}>
            2026 — PROJECTED 1.6%
          </div>
        </div>

        <div style={{
          marginTop: 46, fontSize: 44, fontWeight: 800, color: '#000000', letterSpacing: 3,
          opacity: interpolate(frame, [620, 650], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          STILL ACCELERATING.
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.14) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene09GdpShare;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10RevenueVsLoss',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10RevenueVsLoss: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Scale/see-saw tilt: loss side gets heavier
  const tilt = interpolate(frame, [260, 420], [0, -7], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad),
  });

  const revenueH = 300 * sp(140, { damping: 14 });
  const lossH = 200 + 160 * interpolate(frame, [260, 460], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFCEF3', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(150deg, #FFE2F8 0%, #FFCEF3 50%, #F3AADF 100%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 760, height: 760, borderRadius: '50%',
        border: '2px solid #000000', opacity: 0.045, left: -280, top: -280,
        transform: `rotate(${frame * 0.06}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 340, height: 340, backgroundColor: '#000000', opacity: 0.035,
        right: -80, bottom: 300, borderRadius: 60,
        transform: `rotate(${frame * -0.09}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />

      {/* Motion texture: cross grid */}
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={`g${i}`} style={{
          position: 'absolute', left: 0, right: 0, height: 1, top: 100 + i * 130,
          backgroundColor: '#000000', opacity: 0.045,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const s = seededRandom(i + 19);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: ((seededRandom(i + 55) * 1080) + frame * (0.3 + s * 0.4)) % 1080,
            width: 3 + s * 5, height: 3 + s * 5, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.08,
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        alignItems: 'center', transform: `scale(${camScale})`,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}>
        <div style={{
          marginTop: 100, fontSize: 74, fontWeight: 900, color: '#000000', letterSpacing: 3,
          opacity: sp(10), transform: `translateY(${(1 - sp(10)) * -36}px)`,
        }}>
          OPENAI — PROJECTED 2025
        </div>
        <div style={{
          fontSize: 38, fontWeight: 700, color: '#000000', marginTop: 12, opacity: sp(50),
        }}>
          THE CASH FLOW HASN'T APPEARED. YET.
        </div>

        {/* See-saw comparison */}
        <div style={{
          position: 'absolute', bottom: 200, width: 1500,
          transform: `rotate(${tilt}deg)`, transformOrigin: 'center bottom',
        }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end', padding: '0 120px' }}>
            {/* Revenue block */}
            <div style={{ textAlign: 'center' }}>
              <div style={{
                fontSize: 56, fontWeight: 900, color: '#000000', marginBottom: 16,
                opacity: sp(140),
              }}>
                $12B REVENUE
              </div>
              <div style={{
                width: 330, height: revenueH, border: '6px solid #000000',
                backgroundColor: '#FFE3F8', borderRadius: 10,
              }} />
            </div>
            {/* Loss block */}
            <div style={{ textAlign: 'center' }}>
              <div style={{
                fontSize: 56, fontWeight: 900, color: '#EEEEEE', backgroundColor: '#000000',
                padding: '4px 20px', marginBottom: 16, display: 'inline-block',
                opacity: frame >= 260 ? 1 : 0,
                transform: `scale(${spring({ frame: Math.max(0, frame - 260), fps, config: { damping: 9, stiffness: 190 } })})`,
              }}>
                -$8B OPERATING LOSS
              </div>
              <div style={{
                width: 330, height: lossH, backgroundColor: '#000000', borderRadius: 10,
                opacity: frame >= 260 ? 1 : 0,
              }} />
            </div>
          </div>
          {/* Beam */}
          <div style={{ height: 14, backgroundColor: '#000000', borderRadius: 7, marginTop: 26 }} />
        </div>

        {/* Fulcrum */}
        <div style={{
          position: 'absolute', bottom: 130, width: 0, height: 0,
          borderLeft: '46px solid transparent', borderRight: '46px solid transparent',
          borderBottom: '70px solid #000000', opacity: sp(100),
        }} />

        <div style={{
          position: 'absolute', bottom: 50, fontSize: 42, fontWeight: 800, color: '#000000',
          opacity: interpolate(frame, [560, 592], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          BETTING ENTIRE FUTURES ON AI
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene10RevenueVsLoss;
```

