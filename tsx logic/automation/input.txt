```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene1_GymAnalogy',
	durationInSeconds: 13,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene1_GymAnalogy: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Camera zoom
	const cameraZoom = interpolate(frame, [0, durationInFrames], [1, 1.06], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient background */}
			<AbsoluteFill
				style={{
					transform: `scale(${cameraZoom})`,
				}}
			>
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(ellipse at 30% 40%, #2a1a0a 0%, #1C1C1C 50%, #0a0a1a 100%)`,
					}}
				/>
				{/* Secondary gradient pulse */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at 70% 70%, rgba(255,69,0,0.08) 0%, transparent 50%)`,
						opacity: 0.5 + Math.sin(frame * 0.03) * 0.3,
					}}
				/>
			</AbsoluteFill>

			{/* Animated shape layers */}
			{[...Array(6)].map((_, i) => {
				const seed = seededRandom(i + 100);
				const size = 80 + seed * 300;
				const startX = seed * 1920;
				const startY = seededRandom(i + 200) * 1080;
				const drift = Math.sin(frame * 0.008 + i) * 40;
				const rotation = frame * 0.15 * (i % 2 === 0 ? 1 : -1);
				const breathe = 1 + Math.sin(frame * 0.02 + i * 2) * 0.05;
				const shapes = ['circle', 'rect', 'triangle'];
				const shape = shapes[i % 3];

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: startX + drift,
							top: startY + Math.cos(frame * 0.006 + i) * 30,
							transform: `rotate(${rotation}deg) scale(${breathe})`,
							opacity: 0.04 + seed * 0.04,
						}}
					>
						{shape === 'circle' && (
							<div
								style={{
									width: size,
									height: size,
									borderRadius: '50%',
									border: `2px solid rgba(255,140,0,0.15)`,
								}}
							/>
						)}
						{shape === 'rect' && (
							<div
								style={{
									width: size,
									height: size * 0.6,
									border: `2px solid rgba(255,69,0,0.12)`,
									borderRadius: 8,
								}}
							/>
						)}
						{shape === 'triangle' && (
							<svg width={size} height={size} viewBox="0 0 100 100">
								<polygon
									points="50,10 90,90 10,90"
									fill="none"
									stroke="rgba(255,165,0,0.1)"
									strokeWidth="2"
								/>
							</svg>
						)}
					</div>
				);
			})}

			{/* Motion texture - diagonal lines */}
			<svg
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					opacity: 0.04,
				}}
			>
				{[...Array(20)].map((_, i) => (
					<line
						key={i}
						x1={i * 100 + frame * 0.3}
						y1={0}
						x2={i * 100 - 200 + frame * 0.3}
						y2={1080}
						stroke="#FF6B00"
						strokeWidth="1"
					/>
				))}
			</svg>

			{/* Floating particles */}
			{[...Array(25)].map((_, i) => {
				const px = seededRandom(i + 50) * 1920;
				const py = seededRandom(i + 150) * 1080;
				const particleSize = 2 + seededRandom(i + 250) * 4;
				const speed = 0.3 + seededRandom(i + 350) * 0.8;
				const yPos = (py + frame * speed) % 1080;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px + Math.sin(frame * 0.02 + i) * 15,
							top: yPos,
							width: particleSize,
							height: particleSize,
							borderRadius: '50%',
							backgroundColor: i % 3 === 0 ? '#FF6B00' : '#FFC107',
							opacity: 0.15 + Math.sin(frame * 0.05 + i) * 0.1,
						}}
					/>
				);
			})}

			{/* Main content */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'column',
					alignItems: 'center',
					justifyContent: 'center',
					transform: `scale(${cameraZoom})`,
				}}
			>
				{/* "ठैरो!" text - dramatic entrance */}
				<div
					style={{
						position: 'absolute',
						top: 80,
						left: 100,
						fontSize: 200,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#ff0000',
						opacity: interpolate(
							sp(0, { damping: 8, stiffness: 80 }),
							[0, 1],
							[0, 0.08]
						),
						transform: `scale(${interpolate(sp(0, { damping: 8, stiffness: 80 }), [0, 1], [3, 1])})`,
						letterSpacing: '-5px',
					}}
				>
					ठैरो!
				</div>

				{/* Dumbbell SVG icon */}
				<div
					style={{
						transform: `translateY(${interpolate(sp(10), [0, 1], [80, 0])}px) rotate(${Math.sin(frame * 0.06) * 5}deg)`,
						opacity: sp(10),
						marginBottom: 30,
					}}
				>
					<svg width="180" height="100" viewBox="0 0 180 100">
						{/* Left weight */}
						<rect
							x="10"
							y="20"
							width="30"
							height="60"
							rx="5"
							fill="#FFC107"
							opacity="0.9"
						/>
						<rect
							x="25"
							y="10"
							width="20"
							height="80"
							rx="4"
							fill="#e9d700"
						/>
						{/* Bar */}
						<rect x="45" y="42" width="90" height="16" rx="8" fill="#9E9E9E" />
						{/* Right weight */}
						<rect
							x="135"
							y="20"
							width="30"
							height="60"
							rx="5"
							fill="#FFC107"
							opacity="0.9"
						/>
						<rect
							x="130"
							y="10"
							width="20"
							height="80"
							rx="4"
							fill="#e9d700"
						/>
					</svg>
				</div>

				{/* Main headline */}
				<div
					style={{
						fontSize: 110,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#F5F5F5',
						textAlign: 'center',
						lineHeight: 1.15,
						transform: `translateY(${interpolate(sp(20), [0, 1], [60, 0])}px)`,
						opacity: sp(20),
						textShadow: '0 4px 30px rgba(0,0,0,0.5)',
					}}
				>
					जिम में{' '}
					<span style={{ color: '#FFC107' }}>पहले हफ़्ते</span>
				</div>

				<div
					style={{
						fontSize: 95,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#ff0000',
						textAlign: 'center',
						lineHeight: 1.2,
						transform: `translateY(${interpolate(sp(35), [0, 1], [60, 0])}px) scale(${interpolate(sp(35, { damping: 8 }), [0, 1], [0.5, 1])})`,
						opacity: sp(35),
						marginTop: 10,
					}}
				>
					Six Pack नहीं आता! 💪
				</div>

				{/* Subtext */}
				<div
					style={{
						fontSize: 48,
						fontFamily: 'Noto Sans Devanagari',
						fontWeight: 600,
						color: '#9E9E9E',
						textAlign: 'center',
						marginTop: 40,
						transform: `translateY(${interpolate(sp(55), [0, 1], [40, 0])}px)`,
						opacity: sp(55),
					}}
				>
					पहले महीने में भी नहीं आता...
				</div>

				{/* Week counter animation */}
				<div
					style={{
						display: 'flex',
						gap: 25,
						marginTop: 50,
						opacity: sp(70),
						transform: `translateY(${interpolate(sp(70), [0, 1], [30, 0])}px)`,
					}}
				>
					{['Week 1', 'Week 2', 'Week 3', 'Week 4'].map((week, i) => {
						const weekSp = sp(75 + i * 12);
						const isActive = frame > 75 + i * 12 + 10;
						return (
							<div
								key={i}
								style={{
									padding: '14px 28px',
									borderRadius: 12,
									backgroundColor: isActive ? '#2D2D2D' : '#1C1C1C',
									border: `2px solid ${isActive ? '#ff0000' : '#333'}`,
									transform: `scale(${interpolate(weekSp, [0, 1], [0.5, 1])})`,
									opacity: weekSp,
								}}
							>
								<span
									style={{
										fontFamily: 'JetBrains Mono',
										fontSize: 28,
										color: isActive ? '#ff0000' : '#666',
										fontWeight: 700,
									}}
								>
									{week}
								</span>
								<div
									style={{
										fontSize: 20,
										color: '#9E9E9E',
										marginTop: 4,
										fontFamily: 'Noto Sans Devanagari',
									}}
								>
									❌ No result
								</div>
							</div>
						);
					})}
				</div>
			</AbsoluteFill>

			{/* Stick figure - gym character */}
			<div
				style={{
					position: 'absolute',
					bottom: 100,
					right: 120,
					transform: `translateY(${interpolate(sp(45), [0, 1], [100, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(45),
				}}
			>
				<svg width="120" height="200" viewBox="0 0 120 200">
					{/* Golden glow */}
					<circle cx="60" cy="45" r="40" fill="#FFC107" opacity="0.08" />
					{/* Head */}
					<circle
						cx="60"
						cy="45"
						r="22"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="3"
					/>
					{/* Sweat drops */}
					<circle cx="85" cy="35" r="3" fill="#4FC3F7" opacity="0.7">
						<animate
							attributeName="cy"
							values="35;55;35"
							dur="1.5s"
							repeatCount="indefinite"
						/>
					</circle>
					{/* Body */}
					<line
						x1="60"
						y1="67"
						x2="60"
						y2="130"
						stroke="#F5F5F5"
						strokeWidth="3"
					/>
					{/* Arms lifting */}
					<line
						x1="60"
						y1="90"
						x2={30 + Math.sin(frame * 0.08) * 5}
						y2={70 + Math.sin(frame * 0.08) * 8}
						stroke="#F5F5F5"
						strokeWidth="3"
					/>
					<line
						x1="60"
						y1="90"
						x2={90 - Math.sin(frame * 0.08) * 5}
						y2={70 + Math.sin(frame * 0.08) * 8}
						stroke="#F5F5F5"
						strokeWidth="3"
					/>
					{/* Small dumbbells */}
					<rect
						x={18 + Math.sin(frame * 0.08) * 5}
						y={64 + Math.sin(frame * 0.08) * 8}
						width="20"
						height="8"
						rx="3"
						fill="#FFC107"
					/>
					<rect
						x={80 - Math.sin(frame * 0.08) * 5}
						y={64 + Math.sin(frame * 0.08) * 8}
						width="20"
						height="8"
						rx="3"
						fill="#FFC107"
					/>
					{/* Legs */}
					<line
						x1="60"
						y1="130"
						x2="40"
						y2="180"
						stroke="#F5F5F5"
						strokeWidth="3"
					/>
					<line
						x1="60"
						y1="130"
						x2="80"
						y2="180"
						stroke="#F5F5F5"
						strokeWidth="3"
					/>
					{/* Tired expression - mouth */}
					<path
						d="M 52 52 Q 60 58 68 52"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2"
					/>
					{/* Eyes */}
					<circle cx="52" cy="42" r="2.5" fill="#F5F5F5" />
					<circle cx="68" cy="42" r="2.5" fill="#F5F5F5" />
				</svg>
			</div>

			{/* Vignette overlay */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene1_GymAnalogy;
```

```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene2_BodyTakesTime',
	durationInSeconds: 13,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene2_BodyTakesTime: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Camera pan
	const cameraPanX = interpolate(frame, [0, durationInFrames], [20, -20], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient background */}
			<div
				style={{
					position: 'absolute',
					width: '120%',
					height: '120%',
					left: '-10%',
					top: '-10%',
					background: `linear-gradient(135deg, #1a0a00 0%, #1C1C1C 40%, #0d1a0d 70%, #1C1C1C 100%)`,
					transform: `translateX(${cameraPanX}px)`,
				}}
			/>
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(circle at 60% 30%, rgba(233,215,0,0.06) 0%, transparent 50%)`,
				}}
			/>

			{/* Shape layers - hexagons and diamonds */}
			{[...Array(5)].map((_, i) => {
				const size = 100 + seededRandom(i + 300) * 250;
				const x = seededRandom(i + 400) * 1920;
				const y = seededRandom(i + 500) * 1080;
				const rot = frame * 0.1 * (i % 2 === 0 ? 1 : -1) + i * 45;
				const breathe = 1 + Math.sin(frame * 0.018 + i * 3) * 0.06;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: x + Math.sin(frame * 0.007 + i) * 30,
							top: y + Math.cos(frame * 0.009 + i) * 20,
							transform: `rotate(${rot}deg) scale(${breathe})`,
							opacity: 0.04 + seededRandom(i + 600) * 0.04,
						}}
					>
						<svg width={size} height={size} viewBox="0 0 100 100">
							<polygon
								points="50,5 95,27 95,73 50,95 5,73 5,27"
								fill="none"
								stroke={i % 2 === 0 ? 'rgba(233,215,0,0.2)' : 'rgba(255,193,7,0.15)'}
								strokeWidth="1.5"
							/>
						</svg>
					</div>
				);
			})}

			{/* Motion texture - concentric circles */}
			<svg
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					opacity: 0.03,
				}}
			>
				{[...Array(8)].map((_, i) => (
					<circle
						key={i}
						cx="960"
						cy="540"
						r={100 + i * 120 + Math.sin(frame * 0.01) * 20}
						fill="none"
						stroke="#e9d700"
						strokeWidth="0.8"
					/>
				))}
			</svg>

			{/* Floating particles */}
			{[...Array(20)].map((_, i) => {
				const px = seededRandom(i + 700) * 1920;
				const py = seededRandom(i + 800) * 1080;
				const size = 2 + seededRandom(i + 900) * 3;
				const yDrift = (py + frame * (0.2 + seededRandom(i + 1000) * 0.5)) % 1080;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px + Math.sin(frame * 0.015 + i * 2) * 20,
							top: yDrift,
							width: size,
							height: size,
							borderRadius: '50%',
							backgroundColor: i % 4 === 0 ? '#e9d700' : '#FFC107',
							opacity: 0.12 + Math.sin(frame * 0.04 + i) * 0.08,
						}}
					/>
				);
			})}

			{/* Main content - split screen comparison */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'row',
					alignItems: 'center',
					justifyContent: 'center',
					gap: 60,
					transform: `translateX(${cameraPanX * 0.5}px)`,
				}}
			>
				{/* Left side - "3 Months Gym" */}
				<div
					style={{
						display: 'flex',
						flexDirection: 'column',
						alignItems: 'center',
						transform: `translateX(${interpolate(sp(10), [0, 1], [-200, 0])}px)`,
						opacity: sp(10),
					}}
				>
					<div
						style={{
							width: 350,
							height: 420,
							borderRadius: 24,
							backgroundColor: '#2D2D2D',
							border: '3px solid #444',
							display: 'flex',
							flexDirection: 'column',
							alignItems: 'center',
							justifyContent: 'center',
							padding: 30,
							position: 'relative',
							overflow: 'hidden',
						}}
					>
						{/* Calendar icon */}
						<svg width="80" height="80" viewBox="0 0 80 80">
							<rect
								x="8"
								y="16"
								width="64"
								height="56"
								rx="8"
								fill="none"
								stroke="#9E9E9E"
								strokeWidth="3"
							/>
							<line
								x1="8"
								y1="32"
								x2="72"
								y2="32"
								stroke="#9E9E9E"
								strokeWidth="3"
							/>
							<line
								x1="24"
								y1="8"
								x2="24"
								y2="24"
								stroke="#9E9E9E"
								strokeWidth="3"
								strokeLinecap="round"
							/>
							<line
								x1="56"
								y1="8"
								x2="56"
								y2="24"
								stroke="#9E9E9E"
								strokeWidth="3"
								strokeLinecap="round"
							/>
							<text
								x="40"
								y="58"
								textAnchor="middle"
								fill="#ff0000"
								fontSize="22"
								fontWeight="bold"
								fontFamily="JetBrains Mono"
							>
								3M
							</text>
						</svg>

						<div
							style={{
								fontSize: 52,
								fontFamily: 'Noto Sans Devanagari',
								fontWeight: 800,
								color: '#F5F5F5',
								marginTop: 20,
								textAlign: 'center',
							}}
						>
							3 महीने
						</div>
						<div
							style={{
								fontSize: 36,
								fontFamily: 'Noto Sans Devanagari',
								fontWeight: 600,
								color: '#9E9E9E',
								marginTop: 8,
								textAlign: 'center',
							}}
						>
							बॉडी वैसी की वैसी
						</div>

						{/* X mark with shake */}
						<div
							style={{
								marginTop: 20,
								transform: `rotate(${frame > 80 ? Math.sin(frame * 0.3) * 3 : 0}deg) scale(${interpolate(sp(60, { damping: 6 }), [0, 1], [0, 1])})`,
							}}
						>
							<svg width="70" height="70" viewBox="0 0 70 70">
								<circle cx="35" cy="35" r="32" fill="#ff0000" opacity="0.15" />
								<line
									x1="20"
									y1="20"
									x2="50"
									y2="50"
									stroke="#ff0000"
									strokeWidth="5"
									strokeLinecap="round"
								/>
								<line
									x1="50"
									y1="20"
									x2="20"
									y2="50"
									stroke="#ff0000"
									strokeWidth="5"
									strokeLinecap="round"
								/>
							</svg>
						</div>
					</div>

					{/* "छोड़ दो?" label */}
					<div
						style={{
							fontSize: 42,
							fontFamily: 'Yantra One',
							fontWeight: 800,
							color: '#ff0000',
							marginTop: 20,
							opacity: sp(80),
							transform: `scale(${interpolate(sp(80, { damping: 8 }), [0, 1], [0.5, 1])})`,
						}}
					>
						"छोड़ दो?" ❌
					</div>
				</div>

				{/* VS Divider */}
				<div
					style={{
						display: 'flex',
						flexDirection: 'column',
						alignItems: 'center',
						opacity: sp(40),
						transform: `scale(${interpolate(sp(40, { damping: 8 }), [0, 1], [0, 1])})`,
					}}
				>
					<div
						style={{
							width: 4,
							height: 150,
							backgroundColor: '#444',
							borderRadius: 2,
						}}
					/>
					<div
						style={{
							fontSize: 60,
							fontFamily: 'Inter',
							fontWeight: 900,
							color: '#FFC107',
							margin: '15px 0',
						}}
					>
						VS
					</div>
					<div
						style={{
							width: 4,
							height: 150,
							backgroundColor: '#444',
							borderRadius: 2,
						}}
					/>
				</div>

				{/* Right side - "Reality: Patience!" */}
				<div
					style={{
						display: 'flex',
						flexDirection: 'column',
						alignItems: 'center',
						transform: `translateX(${interpolate(sp(25), [0, 1], [200, 0])}px)`,
						opacity: sp(25),
					}}
				>
					<div
						style={{
							width: 350,
							height: 420,
							borderRadius: 24,
							backgroundColor: '#1a2a1a',
							border: '3px solid #2a5a2a',
							display: 'flex',
							flexDirection: 'column',
							alignItems: 'center',
							justifyContent: 'center',
							padding: 30,
						}}
					>
						{/* Clock + growth icon */}
						<svg width="80" height="80" viewBox="0 0 80 80">
							<circle
								cx="40"
								cy="40"
								r="34"
								fill="none"
								stroke="#4CAF50"
								strokeWidth="3"
							/>
							<line
								x1="40"
								y1="40"
								x2="40"
								y2="18"
								stroke="#F5F5F5"
								strokeWidth="3"
								strokeLinecap="round"
								transform={`rotate(${frame * 0.5}, 40, 40)`}
							/>
							<line
								x1="40"
								y1="40"
								x2="55"
								y2="40"
								stroke="#FFC107"
								strokeWidth="2.5"
								strokeLinecap="round"
								transform={`rotate(${frame * 2}, 40, 40)`}
							/>
							<circle cx="40" cy="40" r="3" fill="#FFC107" />
						</svg>

						<div
							style={{
								fontSize: 52,
								fontFamily: 'Noto Sans Devanagari',
								fontWeight: 800,
								color: '#4CAF50',
								marginTop: 20,
								textAlign: 'center',
							}}
						>
							वक्त लगता है
						</div>
						<div
							style={{
								fontSize: 36,
								fontFamily: 'Noto Sans Devanagari',
								fontWeight: 600,
								color: '#9E9E9E',
								marginTop: 8,
								textAlign: 'center',
							}}
						>
							ये तुम जानते हो!
						</div>

						{/* Checkmark */}
						<div
							style={{
								marginTop: 20,
								transform: `scale(${interpolate(sp(90, { damping: 8 }), [0, 1], [0, 1])})`,
							}}
						>
							<svg width="70" height="70" viewBox="0 0 70 70">
								<circle cx="35" cy="35" r="32" fill="#4CAF50" opacity="0.15" />
								<path
									d="M 20 35 L 32 48 L 52 22"
									fill="none"
									stroke="#4CAF50"
									strokeWidth="5"
									strokeLinecap="round"
									strokeLinejoin="round"
								/>
							</svg>
						</div>
					</div>

					<div
						style={{
							fontSize: 42,
							fontFamily: 'Yantra One',
							fontWeight: 800,
							color: '#4CAF50',
							marginTop: 20,
							opacity: sp(100),
							transform: `scale(${interpolate(sp(100, { damping: 8 }), [0, 1], [0.5, 1])})`,
						}}
					>
						"PATIENCE!" ✅
					</div>
				</div>
			</AbsoluteFill>

			{/* Bottom punchline */}
			<div
				style={{
					position: 'absolute',
					bottom: 80,
					width: '100%',
					textAlign: 'center',
					fontSize: 50,
					fontFamily: 'Yantra One',
					fontWeight: 800,
					color: '#e9d700',
					opacity: sp(110),
					transform: `translateY(${interpolate(sp(110), [0, 1], [30, 0])}px)`,
					textShadow: '0 3px 20px rgba(233,215,0,0.3)',
				}}
			>
				तुम उसे पागल कहोगे! 🤪
			</div>

			{/* Stick figure character */}
			<div
				style={{
					position: 'absolute',
					bottom: 90,
					left: 80,
					transform: `translateY(${interpolate(sp(50), [0, 1], [80, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(50),
				}}
			>
				<svg width="90" height="160" viewBox="0 0 90 160">
					{/* Glow */}
					<circle cx="45" cy="35" r="35" fill="#FFC107" opacity="0.06" />
					{/* Head */}
					<circle
						cx="45"
						cy="35"
						r="18"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2.5"
						transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 35)`}
					/>
					{/* Golden glasses */}
					<rect x="32" y="29" width="12" height="9" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<rect x="48" y="29" width="12" height="9" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<line x1="44" y1="33" x2="48" y2="33" stroke="#FFC107" strokeWidth="1.5" />
					{/* Laughing mouth */}
					<path d="M 38 42 Q 45 50 52 42" fill="none" stroke="#F5F5F5" strokeWidth="2" />
					{/* Body */}
					<line x1="45" y1="53" x2="45" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Arms pointing right */}
					<line x1="45" y1="72" x2="20" y2="85" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="45" y1="72" x2="75" y2="60" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Legs */}
					<line x1="45" y1="105" x2="28" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="45" y1="105" x2="62" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
				</svg>
			</div>

			{/* Vignette */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.65) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene2_BodyTakesTime;
```

```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene3_PatienceGayab',
	durationInSeconds: 12,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene3_PatienceGayab: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Screen shake when "GAYAB" appears
	const shakeIntensity = frame > 45 && frame < 65 ? Math.sin(frame * 2) * 4 : 0;

	// Camera tilt
	const cameraTilt = interpolate(frame, [0, durationInFrames], [-0.5, 0.5], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient - deep purple mood */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(ellipse at 50% 50%, #1a0a2a 0%, #1C1C1C 60%)`,
					transform: `rotate(${cameraTilt}deg)`,
				}}
			/>
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(circle at 20% 80%, rgba(156,39,176,0.08) 0%, transparent 40%)`,
					opacity: 0.6 + Math.sin(frame * 0.04) * 0.3,
				}}
			/>

			{/* Shape layers - crosses and stars */}
			{[...Array(7)].map((_, i) => {
				const size = 60 + seededRandom(i + 1100) * 200;
				const x = seededRandom(i + 1200) * 1920;
				const y = seededRandom(i + 1300) * 1080;
				const rot = frame * 0.12 * (i % 2 === 0 ? 1 : -1);
				const breathe = 1 + Math.sin(frame * 0.022 + i) * 0.04;

				return (
					<svg
						key={i}
						style={{
							position: 'absolute',
							left: x + Math.sin(frame * 0.006 + i * 2) * 25,
							top: y + Math.cos(frame * 0.008 + i) * 20,
							transform: `rotate(${rot}deg) scale(${breathe})`,
							opacity: 0.03 + seededRandom(i + 1400) * 0.05,
						}}
						width={size}
						height={size}
						viewBox="0 0 100 100"
					>
						{i % 2 === 0 ? (
							<>
								<line
									x1="50"
									y1="10"
									x2="50"
									y2="90"
									stroke="rgba(156,39,176,0.3)"
									strokeWidth="2"
								/>
								<line
									x1="10"
									y1="50"
									x2="90"
									y2="50"
									stroke="rgba(156,39,176,0.3)"
									strokeWidth="2"
								/>
							</>
						) : (
							<polygon
								points="50,5 61,35 95,35 68,55 79,90 50,68 21,90 32,55 5,35 39,35"
								fill="none"
								stroke="rgba(233,215,0,0.12)"
								strokeWidth="1.5"
							/>
						)}
					</svg>
				);
			})}

			{/* Motion texture - grid dots */}
			{[...Array(12)].map((_, row) =>
				[...Array(20)].map((_, col) => (
					<div
						key={`${row}-${col}`}
						style={{
							position: 'absolute',
							left: col * 100 + 10,
							top: row * 100 + 10,
							width: 2,
							height: 2,
							borderRadius: '50%',
							backgroundColor: '#9E9E9E',
							opacity: 0.06 + Math.sin(frame * 0.03 + row + col) * 0.03,
						}}
					/>
				))
			)}

			{/* Floating particles */}
			{[...Array(22)].map((_, i) => {
				const px = seededRandom(i + 1500) * 1920;
				const py = seededRandom(i + 1600) * 1080;
				const pSize = 2 + seededRandom(i + 1700) * 4;
				const yD = (py + frame * (0.15 + seededRandom(i + 1800) * 0.4)) % 1080;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px + Math.sin(frame * 0.018 + i * 1.5) * 18,
							top: yD,
							width: pSize,
							height: pSize,
							borderRadius: '50%',
							backgroundColor: i % 3 === 0 ? '#9C27B0' : '#e9d700',
							opacity: 0.1 + Math.sin(frame * 0.04 + i) * 0.08,
						}}
					/>
				);
			})}

			{/* Main content */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'column',
					alignItems: 'center',
					justifyContent: 'center',
					transform: `translate(${shakeIntensity}px, ${shakeIntensity * 0.5}px)`,
				}}
			>
				{/* PATIENCE word breaking apart */}
				<div
					style={{
						position: 'relative',
						marginBottom: 30,
					}}
				>
					<div
						style={{
							fontSize: 140,
							fontFamily: 'Inter',
							fontWeight: 900,
							color: '#F5F5F5',
							letterSpacing: '15px',
							opacity: sp(5),
							transform: `translateY(${interpolate(sp(5), [0, 1], [50, 0])}px)`,
						}}
					>
						{'PATIENCE'.split('').map((letter, i) => {
							const disappearFrame = 50 + i * 5;
							const letterOp = frame > disappearFrame
								? interpolate(frame, [disappearFrame, disappearFrame + 15], [1, 0], {
										extrapolateRight: 'clamp',
									})
								: 1;
							const letterY = frame > disappearFrame
								? interpolate(frame, [disappearFrame, disappearFrame + 15], [0, 30 + i * 10], {
										extrapolateRight: 'clamp',
									})
								: 0;
							const letterRot = frame > disappearFrame
								? interpolate(frame, [disappearFrame, disappearFrame + 15], [0, (i % 2 === 0 ? 1 : -1) * 25], {
										extrapolateRight: 'clamp',
									})
								: 0;

							return (
								<span
									key={i}
									style={{
										display: 'inline-block',
										opacity: letterOp,
										transform: `translateY(${letterY}px) rotate(${letterRot}deg)`,
										color: i < 3 ? '#FFC107' : '#F5F5F5',
									}}
								>
									{letter}
								</span>
							);
						})}
					</div>

					{/* GAYAB stamp */}
					<div
						style={{
							position: 'absolute',
							top: '50%',
							left: '50%',
							transform: `translate(-50%, -50%) rotate(-12deg) scale(${interpolate(sp(60, { damping: 6, stiffness: 200 }), [0, 1], [3, 1])})`,
							opacity: sp(60),
						}}
					>
						<div
							style={{
								fontSize: 120,
								fontFamily: 'Yantra One',
								fontWeight: 900,
								color: '#ff0000',
								border: '6px solid #ff0000',
								padding: '10px 40px',
								borderRadius: 12,
								textShadow: '0 0 30px rgba(255,0,0,0.5)',
							}}
						>
							गायब!
						</div>
					</div>
				</div>

				{/* Divider line */}
				<div
					style={{
						width: interpolate(sp(80), [0, 1], [0, 800]),
						height: 3,
						backgroundColor: '#FFC107',
						borderRadius: 2,
						marginTop: 40,
						marginBottom: 40,
					}}
				/>

				{/* Bottom revelation */}
				<div
					style={{
						fontSize: 70,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#e9d700',
						textAlign: 'center',
						lineHeight: 1.3,
						opacity: sp(95),
						transform: `translateY(${interpolate(sp(95), [0, 1], [40, 0])}px)`,
						textShadow: '0 4px 30px rgba(233,215,0,0.25)',
					}}
				>
					Compounding का पूरा खेल
				</div>
				<div
					style={{
						fontSize: 90,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#FFC107',
						textAlign: 'center',
						lineHeight: 1.2,
						opacity: sp(110),
						transform: `scale(${interpolate(sp(110, { damping: 8 }), [0, 1], [0.6, 1])})`,
						marginTop: 10,
					}}
				>
					PATIENCE पर टिका है! ⏳
				</div>
			</AbsoluteFill>

			{/* Stick figure - confused/thinking */}
			<div
				style={{
					position: 'absolute',
					bottom: 80,
					right: 100,
					transform: `translateY(${interpolate(sp(40), [0, 1], [100, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(40),
				}}
			>
				<svg width="100" height="170" viewBox="0 0 100 170">
					<circle cx="50" cy="35" r="32" fill="#FFC107" opacity="0.06" />
					<circle
						cx="50"
						cy="35"
						r="18"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2.5"
						transform={`rotate(${Math.sin(frame * 0.04) * 3}, 50, 35)`}
					/>
					{/* Glasses */}
					<rect x="36" y="29" width="12" height="9" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<rect x="52" y="29" width="12" height="9" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<line x1="48" y1="33" x2="52" y2="33" stroke="#FFC107" strokeWidth="1.5" />
					{/* Thinking expression */}
					<line x1="43" y1="45" x2="57" y2="45" stroke="#F5F5F5" strokeWidth="2" />
					{/* Question mark above head */}
					<text x="55" y="12" fill="#e9d700" fontSize="22" fontWeight="bold" fontFamily="Inter"
						opacity={0.5 + Math.sin(frame * 0.08) * 0.5}
					>?</text>
					{/* Body */}
					<line x1="50" y1="53" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Arms - scratching head */}
					<line x1="50" y1="70" x2="25" y2="90" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="50" y1="70" x2="72" y2="55" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Legs */}
					<line x1="50" y1="105" x2="35" y2="150" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="50" y1="105" x2="65" y2="150" stroke="#F5F5F5" strokeWidth="2.5" />
				</svg>
			</div>

			{/* Vignette */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.7) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene3_PatienceGayab;
```

```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene4_YouAreTheEngine',
	durationInSeconds: 12,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene4_YouAreTheEngine: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Camera slow zoom in
	const cameraZoom = interpolate(frame, [0, durationInFrames], [1, 1.08], {
		extrapolateRight: 'clamp',
	});

	// Engine pulse
	const enginePulse = 1 + Math.sin(frame * 0.1) * 0.03;

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient - warm industrial */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(ellipse at 50% 60%, #2a1a0a 0%, #1C1C1C 55%, #0a0a0a 100%)`,
					transform: `scale(${cameraZoom})`,
				}}
			/>
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `conic-gradient(from ${frame * 0.5}deg at 50% 50%, rgba(255,193,7,0.03) 0%, transparent 15%, rgba(255,69,0,0.03) 30%, transparent 45%)`,
				}}
			/>

			{/* Shape layers - gears */}
			{[...Array(4)].map((_, i) => {
				const size = 150 + seededRandom(i + 2000) * 300;
				const x = [150, 1700, 200, 1600][i];
				const y = [100, 200, 800, 850][i];
				const rot = frame * (0.2 + i * 0.1) * (i % 2 === 0 ? 1 : -1);

				return (
					<svg
						key={i}
						style={{
							position: 'absolute',
							left: x,
							top: y,
							transform: `rotate(${rot}deg)`,
							opacity: 0.05,
						}}
						width={size}
						height={size}
						viewBox="0 0 100 100"
					>
						<circle
							cx="50"
							cy="50"
							r="35"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2"
						/>
						<circle
							cx="50"
							cy="50"
							r="12"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2"
						/>
						{[...Array(8)].map((_, j) => {
							const angle = (j * 45 * Math.PI) / 180;
							return (
								<rect
									key={j}
									x="46"
									y="10"
									width="8"
									height="15"
									rx="2"
									fill="#FFC107"
									opacity="0.3"
									transform={`rotate(${j * 45}, 50, 50)`}
								/>
							);
						})}
					</svg>
				);
			})}

			{/* Motion texture - horizontal speed lines */}
			{[...Array(15)].map((_, i) => {
				const y = 50 + seededRandom(i + 2100) * 980;
				const width = 100 + seededRandom(i + 2200) * 300;
				const xPos = (seededRandom(i + 2300) * 2200 + frame * 1.5) % 2200 - 200;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: xPos,
							top: y,
							width: width,
							height: 1.5,
							backgroundColor: '#FFC107',
							opacity: 0.04,
							borderRadius: 1,
						}}
					/>
				);
			})}

			{/* Floating particles */}
			{[...Array(18)].map((_, i) => {
				const px = seededRandom(i + 2400) * 1920;
				const py = seededRandom(i + 2500) * 1080;
				const pS = 2 + seededRandom(i + 2600) * 3;
				const yD = (py + frame * (0.2 + seededRandom(i + 2700) * 0.5)) % 1080;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px + Math.sin(frame * 0.02 + i) * 12,
							top: yD,
							width: pS,
							height: pS,
							borderRadius: '50%',
							backgroundColor: i % 2 === 0 ? '#FF6B00' : '#FFC107',
							opacity: 0.1 + Math.sin(frame * 0.05 + i) * 0.06,
						}}
					/>
				);
			})}

			{/* Main content */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'column',
					alignItems: 'center',
					justifyContent: 'center',
					transform: `scale(${cameraZoom})`,
				}}
			>
				{/* Big engine icon center */}
				<div
					style={{
						position: 'relative',
						marginBottom: 40,
						transform: `scale(${enginePulse * interpolate(sp(5), [0, 1], [0, 1])})`,
						opacity: sp(5),
					}}
				>
					<svg width="200" height="200" viewBox="0 0 200 200">
						{/* Glow */}
						<circle cx="100" cy="100" r="90" fill="#FFC107" opacity="0.06" />
						{/* Engine block */}
						<rect
							x="55"
							y="50"
							width="90"
							height="100"
							rx="12"
							fill="#2D2D2D"
							stroke="#FFC107"
							strokeWidth="3"
						/>
						{/* Pistons */}
						<rect
							x="70"
							y={60 + Math.sin(frame * 0.15) * 5}
							width="20"
							height="35"
							rx="5"
							fill="#FFC107"
							opacity="0.8"
						/>
						<rect
							x="110"
							y={65 - Math.sin(frame * 0.15) * 5}
							width="20"
							height="35"
							rx="5"
							fill="#FFC107"
							opacity="0.8"
						/>
						{/* Exhaust flames */}
						<ellipse
							cx="100"
							cy="160"
							rx={15 + Math.sin(frame * 0.2) * 5}
							ry={12 + Math.sin(frame * 0.25) * 4}
							fill="#FF6B00"
							opacity="0.6"
						/>
						<ellipse
							cx="100"
							cy="168"
							rx={10 + Math.sin(frame * 0.3) * 3}
							ry={8 + Math.sin(frame * 0.2) * 3}
							fill="#ff0000"
							opacity="0.4"
						/>
					</svg>
				</div>

				{/* "YOU are the ENGINE" headline */}
				<div
					style={{
						fontSize: 55,
						fontFamily: 'Noto Sans Devanagari',
						fontWeight: 600,
						color: '#9E9E9E',
						textAlign: 'center',
						opacity: sp(15),
						transform: `translateY(${interpolate(sp(15), [0, 1], [30, 0])}px)`,
					}}
				>
					शुरुवात में
				</div>

				<div
					style={{
						fontSize: 130,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#FFC107',
						textAlign: 'center',
						lineHeight: 1.1,
						opacity: sp(25),
						transform: `translateY(${interpolate(sp(25), [0, 1], [50, 0])}px) scale(${interpolate(sp(25, { damping: 8 }), [0, 1], [0.7, 1])})`,
						textShadow: '0 4px 40px rgba(255,193,7,0.3)',
					}}
				>
					तुम हो ENGINE!
				</div>

				{/* Three pillars */}
				<div
					style={{
						display: 'flex',
						gap: 50,
						marginTop: 50,
					}}
				>
					{[
						{ label: 'कमाई', icon: '💰', color: '#4CAF50' },
						{ label: 'बचत', icon: '🏦', color: '#2196F3' },
						{ label: 'Sacrifice', icon: '🔥', color: '#FF6B00' },
					].map((item, i) => (
						<div
							key={i}
							style={{
								display: 'flex',
								flexDirection: 'column',
								alignItems: 'center',
								opacity: sp(50 + i * 18),
								transform: `translateY(${interpolate(sp(50 + i * 18), [0, 1], [50, 0])}px) scale(${interpolate(sp(50 + i * 18, { damping: 8 }), [0, 1], [0.5, 1])})`,
							}}
						>
							<div
								style={{
									width: 110,
									height: 110,
									borderRadius: 20,
									backgroundColor: '#2D2D2D',
									border: `3px solid ${item.color}`,
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									fontSize: 50,
									boxShadow: `0 0 25px ${item.color}22`,
								}}
							>
								{item.icon}
							</div>
							<span
								style={{
									fontSize: 38,
									fontFamily: 'Noto Sans Devanagari',
									fontWeight: 700,
									color: item.color,
									marginTop: 12,
								}}
							>
								{item.label}
							</span>
						</div>
					))}
				</div>

				{/* Arrow pointing to engine */}
				<div
					style={{
						display: 'flex',
						alignItems: 'center',
						gap: 15,
						marginTop: 30,
						opacity: sp(100),
					}}
				>
					<div style={{ fontSize: 34, color: '#9E9E9E', fontFamily: 'Inter', fontWeight: 600 }}>
						ये सब = 
					</div>
					<div style={{ fontSize: 48, fontFamily: 'Yantra One', fontWeight: 900, color: '#e9d700' }}>
						⚙️ ENGINE
					</div>
				</div>
			</AbsoluteFill>

			{/* Stick figure pushing engine */}
			<div
				style={{
					position: 'absolute',
					bottom: 80,
					left: 80,
					transform: `translateY(${interpolate(sp(35), [0, 1], [80, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(35),
				}}
			>
				<svg width="110" height="170" viewBox="0 0 110 170">
					<circle cx="55" cy="35" r="30" fill="#FFC107" opacity="0.06" />
					<circle
						cx="55"
						cy="35"
						r="18"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2.5"
						transform={`rotate(${Math.sin(frame * 0.04) * 3}, 55, 35)`}
					/>
					{/* Glasses */}
					<rect x="42" y="29" width="11" height="8" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<rect x="57" y="29" width="11" height="8" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<line x1="53" y1="33" x2="57" y2="33" stroke="#FFC107" strokeWidth="1.5" />
					{/* Determined expression */}
					<line x1="48" y1="44" x2="62" y2="44" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Body */}
					<line x1="55" y1="53" x2="55" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Arms pushing forward */}
					<line x1="55" y1="75" x2="85" y2="70" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="55" y1="80" x2="85" y2="80" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Legs braced */}
					<line x1="55" y1="110" x2="35" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="55" y1="110" x2="55" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
				</svg>
			</div>

			{/* Vignette */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene4_YouAreTheEngine;
```

```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene5_MoneyBecomesEngine',
	durationInSeconds: 13,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene5_MoneyBecomesEngine: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Camera orbit effect
	const orbitX = Math.sin(frame * 0.008) * 15;
	const orbitY = Math.cos(frame * 0.006) * 10;

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient - golden energy */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(ellipse at 50% 50%, #1a1a00 0%, #1C1C1C 50%, #0a0a00 100%)`,
				}}
			/>
			{/* Pulsing energy ring */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(circle at 50% 45%, rgba(233,215,0,${0.06 + Math.sin(frame * 0.06) * 0.04}) 0%, transparent 35%)`,
				}}
			/>

			{/* Shape layers - energy rings */}
			{[...Array(5)].map((_, i) => {
				const radius = 200 + i * 80 + Math.sin(frame * 0.02 + i) * 20;
				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: '50%',
							top: '42%',
							width: radius * 2,
							height: radius * 2,
							marginLeft: -radius,
							marginTop: -radius,
							borderRadius: '50%',
							border: `1.5px solid rgba(233,215,0,${0.04 + i * 0.01})`,
							transform: `rotate(${frame * 0.3 * (i % 2 === 0 ? 1 : -1)}deg)`,
						}}
					/>
				);
			})}

			{/* Motion texture - radial beams */}
			<svg
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					opacity: 0.04,
				}}
			>
				{[...Array(12)].map((_, i) => {
					const angle = (i * 30 + frame * 0.2) * (Math.PI / 180);
					const x2 = 960 + Math.cos(angle) * 1000;
					const y2 = 470 + Math.sin(angle) * 1000;
					return (
						<line
							key={i}
							x1="960"
							y1="470"
							x2={x2}
							y2={y2}
							stroke="#e9d700"
							strokeWidth="1"
						/>
					);
				})}
			</svg>

			{/* Floating particles - more energetic */}
			{[...Array(30)].map((_, i) => {
				const angle = (i * 12 + frame * 0.5) * (Math.PI / 180);
				const dist = 100 + seededRandom(i + 3000) * 400 + Math.sin(frame * 0.02 + i) * 30;
				const px = 960 + Math.cos(angle) * dist;
				const py = 470 + Math.sin(angle) * dist;
				const pS = 2 + seededRandom(i + 3100) * 4;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px,
							top: py,
							width: pS,
							height: pS,
							borderRadius: '50%',
							backgroundColor: i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#FFC107' : '#FF6B00',
							opacity: 0.15 + Math.sin(frame * 0.06 + i) * 0.1,
						}}
					/>
				);
			})}

			{/* Main content */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'column',
					alignItems: 'center',
					justifyContent: 'center',
					transform: `translate(${orbitX}px, ${orbitY}px)`,
				}}
			>
				{/* Transformation visual - YOU → ₹ */}
				<div
					style={{
						display: 'flex',
						alignItems: 'center',
						gap: 60,
						marginBottom: 50,
					}}
				>
					{/* YOU box fading */}
					<div
						style={{
							display: 'flex',
							flexDirection: 'column',
							alignItems: 'center',
							opacity: interpolate(sp(10), [0, 1], [0, 1]) * interpolate(frame, [120, 180], [1, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
							transform: `scale(${interpolate(sp(10, { damping: 8 }), [0, 1], [0.3, 1])}) scale(${interpolate(frame, [120, 180], [1, 0.7], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})`,
						}}
					>
						<div
							style={{
								width: 180,
								height: 180,
								borderRadius: 24,
								backgroundColor: '#2D2D2D',
								border: '3px solid #9E9E9E',
								display: 'flex',
								alignItems: 'center',
								justifyContent: 'center',
							}}
						>
							{/* Person icon */}
							<svg width="80" height="100" viewBox="0 0 80 100">
								<circle cx="40" cy="25" r="18" fill="none" stroke="#9E9E9E" strokeWidth="3" />
								<path d="M 15 90 Q 15 55 40 55 Q 65 55 65 90" fill="none" stroke="#9E9E9E" strokeWidth="3" />
							</svg>
						</div>
						<span
							style={{
								fontSize: 36,
								fontFamily: 'Noto Sans Devanagari',
								fontWeight: 700,
								color: '#9E9E9E',
								marginTop: 12,
							}}
						>
							तुम = Engine
						</span>
					</div>

					{/* Arrow transforming */}
					<div
						style={{
							opacity: sp(30),
							transform: `translateX(${interpolate(sp(30), [0, 1], [30, 0])}px)`,
						}}
					>
						<svg width="120" height="60" viewBox="0 0 120 60">
							<defs>
								<linearGradient id="arrowGrad5" x1="0%" y1="0%" x2="100%" y2="0%">
									<stop offset="0%" stopColor="#9E9E9E" />
									<stop offset="100%" stopColor="#e9d700" />
								</linearGradient>
							</defs>
							<path
								d="M 10 30 L 80 30 L 80 15 L 110 30 L 80 45 L 80 30"
								fill="url(#arrowGrad5)"
								opacity="0.8"
							/>
						</svg>
						<div
							style={{
								textAlign: 'center',
								fontSize: 28,
								fontFamily: 'JetBrains Mono',
								color: '#e9d700',
								marginTop: 5,
							}}
						>
							SHIFT
						</div>
					</div>

					{/* MONEY box growing */}
					<div
						style={{
							display: 'flex',
							flexDirection: 'column',
							alignItems: 'center',
							opacity: sp(50),
							transform: `scale(${interpolate(sp(50, { damping: 8 }), [0, 1], [0.3, 1])}) scale(${interpolate(frame, [120, 180], [1, 1.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})`,
						}}
					>
						<div
							style={{
								width: 180,
								height: 180,
								borderRadius: 24,
								backgroundColor: '#1a1a00',
								border: '3px solid #e9d700',
								display: 'flex',
								alignItems: 'center',
								justifyContent: 'center',
								boxShadow: `0 0 ${30 + Math.sin(frame * 0.08) * 15}px rgba(233,215,0,0.2)`,
							}}
						>
							<span
								style={{
									fontSize: 100,
									fontFamily: 'Inter',
									fontWeight: 900,
									color: '#e9d700',
								}}
							>
								₹
							</span>
						</div>
						<span
							style={{
								fontSize: 36,
								fontFamily: 'Noto Sans Devanagari',
								fontWeight: 700,
								color: '#e9d700',
								marginTop: 12,
							}}
						>
							पैसा = Engine
						</span>
					</div>
				</div>

				{/* Headline */}
				<div
					style={{
						fontSize: 90,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#F5F5F5',
						textAlign: 'center',
						lineHeight: 1.2,
						opacity: sp(70),
						transform: `translateY(${interpolate(sp(70), [0, 1], [40, 0])}px)`,
					}}
				>
					पैसा खुद{' '}
					<span
						style={{
							color: '#e9d700',
							textShadow: '0 0 30px rgba(233,215,0,0.4)',
						}}
					>
						ENGINE
					</span>{' '}
					बन जाता है!
				</div>

				{/* Sub text */}
				<div
					style={{
						fontSize: 50,
						fontFamily: 'Noto Sans Devanagari',
						fontWeight: 600,
						color: '#FFC107',
						textAlign: 'center',
						marginTop: 25,
						opacity: sp(90),
						transform: `translateY(${interpolate(sp(90), [0, 1], [25, 0])}px)`,
					}}
				>
					तुम सिर्फ ⛽ FUEL डालते रहो
				</div>
			</AbsoluteFill>

			{/* Stick figure relaxed, pouring fuel */}
			<div
				style={{
					position: 'absolute',
					bottom: 80,
					right: 130,
					transform: `translateY(${interpolate(sp(60), [0, 1], [80, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(60),
				}}
			>
				<svg width="110" height="180" viewBox="0 0 110 180">
					<circle cx="55" cy="35" r="30" fill="#FFC107" opacity="0.07" />
					<circle
						cx="55"
						cy="35"
						r="18"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2.5"
						transform={`rotate(${Math.sin(frame * 0.04) * 3}, 55, 35)`}
					/>
					{/* Glasses */}
					<rect x="42" y="29" width="11" height="8" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<rect x="57" y="29" width="11" height="8" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<line x1="53" y1="33" x2="57" y2="33" stroke="#FFC107" strokeWidth="1.5" />
					{/* Smiling */}
					<path d="M 48 42 Q 55 48 62 42" fill="none" stroke="#F5F5F5" strokeWidth="2" />
					{/* Body */}
					<line x1="55" y1="53" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Arm holding fuel can */}
					<line x1="55" y1="75" x2="28" y2="65" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="55" y1="80" x2="80" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Fuel can */}
					<rect x="10" y="55" width="22" height="18" rx="3" fill="#FF6B00" opacity="0.8" />
					<rect x="15" y="48" width="5" height="10" rx="2" fill="#FF6B00" opacity="0.6" />
					{/* Fuel drops */}
					{[...Array(3)].map((_, i) => (
						<circle
							key={i}
							cx={22}
							cy={75 + ((frame * 2 + i * 15) % 30)}
							r={2}
							fill="#e9d700"
							opacity={0.6 - ((frame * 2 + i * 15) % 30) / 50}
						/>
					))}
					{/* Legs - casual stance */}
					<line x1="55" y1="115" x2="38" y2="160" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="55" y1="115" x2="72" y2="160" stroke="#F5F5F5" strokeWidth="2.5" />
				</svg>
			</div>

			{/* Vignette */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.65) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene5_MoneyBecomesEngine;
```

```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene6_Milestones',
	durationInSeconds: 13,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene6_Milestones: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Camera slow pan up
	const cameraPanY = interpolate(frame, [0, durationInFrames], [30, -30], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient - deep blue journey */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '120%',
					top: '-10%',
					background: `linear-gradient(180deg, #0a0a2a 0%, #1C1C1C 40%, #0a1a0a 80%, #1C1C1C 100%)`,
					transform: `translateY(${cameraPanY}px)`,
				}}
			/>
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(circle at 50% 80%, rgba(76,175,80,0.06) 0%, transparent 40%)`,
				}}
			/>

			{/* Shape layers - ascending triangles */}
			{[...Array(6)].map((_, i) => {
				const size = 80 + seededRandom(i + 4000) * 180;
				const x = seededRandom(i + 4100) * 1920;
				const y = seededRandom(i + 4200) * 1080;
				const rot = frame * 0.08 * (i % 2 === 0 ? 1 : -1) + i * 30;
				const breathe = 1 + Math.sin(frame * 0.02 + i * 2) * 0.05;

				return (
					<svg
						key={i}
						style={{
							position: 'absolute',
							left: x + Math.sin(frame * 0.005 + i) * 25,
							top: y + Math.cos(frame * 0.007 + i) * 20 + cameraPanY * 0.3,
							transform: `rotate(${rot}deg) scale(${breathe})`,
							opacity: 0.04,
						}}
						width={size}
						height={size}
						viewBox="0 0 100 100"
					>
						<polygon
							points="50,10 90,90 10,90"
							fill="none"
							stroke={i % 2 === 0 ? 'rgba(33,150,243,0.2)' : 'rgba(76,175,80,0.2)'}
							strokeWidth="1.5"
						/>
					</svg>
				);
			})}

			{/* Motion texture - ascending dots trail */}
			{[...Array(40)].map((_, i) => {
				const col = i % 8;
				const row = Math.floor(i / 8);
				const baseX = 200 + col * 200;
				const baseY = 900 - row * 200;
				const offset = Math.sin(frame * 0.03 + i * 0.5) * 8;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: baseX + offset,
							top: baseY + cameraPanY * 0.2,
							width: 3,
							height: 3,
							borderRadius: '50%',
							backgroundColor: '#2196F3',
							opacity: 0.06,
						}}
					/>
				);
			})}

			{/* Floating particles */}
			{[...Array(20)].map((_, i) => {
				const px = seededRandom(i + 4300) * 1920;
				const py = seededRandom(i + 4400) * 1080;
				const pS = 2 + seededRandom(i + 4500) * 3;
				const yD = (py - frame * (0.3 + seededRandom(i + 4600) * 0.4));
				const wrapped = ((yD % 1080) + 1080) % 1080;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px + Math.sin(frame * 0.02 + i) * 15,
							top: wrapped,
							width: pS,
							height: pS,
							borderRadius: '50%',
							backgroundColor: i % 3 === 0 ? '#4CAF50' : '#2196F3',
							opacity: 0.12 + Math.sin(frame * 0.04 + i) * 0.08,
						}}
					/>
				);
			})}

			{/* Main content - Milestone Road */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'column',
					alignItems: 'center',
					justifyContent: 'flex-start',
					paddingTop: 80,
					transform: `translateY(${cameraPanY}px)`,
				}}
			>
				{/* Title */}
				<div
					style={{
						fontSize: 90,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#F5F5F5',
						textAlign: 'center',
						opacity: sp(5),
						transform: `translateY(${interpolate(sp(5), [0, 1], [40, 0])}px)`,
						marginBottom: 60,
					}}
				>
					MILESTONES 🏔️
				</div>

				{/* Milestone path */}
				<div
					style={{
						position: 'relative',
						width: 1400,
						height: 600,
					}}
				>
					{/* Connecting path line */}
					<svg
						style={{
							position: 'absolute',
							width: '100%',
							height: '100%',
						}}
					>
						<path
							d="M 150 500 Q 400 400 500 350 Q 600 300 750 250 Q 900 200 1000 150 Q 1100 100 1250 50"
							fill="none"
							stroke="#333"
							strokeWidth="4"
							strokeDasharray="15,10"
							strokeDashoffset={-frame * 0.5}
						/>
						{/* Animated progress */}
						<path
							d="M 150 500 Q 400 400 500 350 Q 600 300 750 250 Q 900 200 1000 150 Q 1100 100 1250 50"
							fill="none"
							stroke="#e9d700"
							strokeWidth="4"
							strokeDasharray={`${interpolate(frame, [0, durationInFrames * 0.8], [0, 1600], { extrapolateRight: 'clamp' })},1600`}
							opacity="0.6"
						/>
					</svg>

					{/* Milestone markers */}
					{[
						{ label: '₹1 लाख', x: 100, y: 460, color: '#4CAF50', delay: 20, emoji: '🌱' },
						{ label: '₹5 लाख', x: 400, y: 320, color: '#2196F3', delay: 45, emoji: '📈' },
						{ label: '₹10 लाख', x: 680, y: 220, color: '#FF9800', delay: 70, emoji: '🔥' },
						{ label: '₹50 लाख', x: 950, y: 120, color: '#FFC107', delay: 95, emoji: '⚡' },
						{ label: '₹1 करोड़', x: 1200, y: 20, color: '#e9d700', delay: 120, emoji: '👑' },
					].map((ms, i) => (
						<div
							key={i}
							style={{
								position: 'absolute',
								left: ms.x,
								top: ms.y,
								display: 'flex',
								flexDirection: 'column',
								alignItems: 'center',
								opacity: sp(ms.delay),
								transform: `scale(${interpolate(sp(ms.delay, { damping: 8 }), [0, 1], [0, 1])})`,
							}}
						>
							{/* Flag */}
							<div
								style={{
									width: 80,
									height: 80,
									borderRadius: '50%',
									backgroundColor: '#2D2D2D',
									border: `3px solid ${ms.color}`,
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									fontSize: 36,
									boxShadow: `0 0 20px ${ms.color}33`,
								}}
							>
								{ms.emoji}
							</div>
							<div
								style={{
									fontSize: 32,
									fontFamily: 'JetBrains Mono',
									fontWeight: 800,
									color: ms.color,
									marginTop: 8,
									whiteSpace: 'nowrap',
								}}
							>
								{ms.label}
							</div>
						</div>
					))}

					{/* Moving figure on path */}
					<div
						style={{
							position: 'absolute',
							left: interpolate(frame, [0, durationInFrames * 0.8], [80, 1180], {
								extrapolateRight: 'clamp',
							}),
							top: interpolate(frame, [0, durationInFrames * 0.8], [460, 20], {
								extrapolateRight: 'clamp',
							}),
							transform: `translateY(${Math.sin(frame * 0.1) * 3}px)`,
						}}
					>
						<div
							style={{
								width: 30,
								height: 30,
								borderRadius: '50%',
								backgroundColor: '#e9d700',
								boxShadow: '0 0 15px rgba(233,215,0,0.5)',
							}}
						/>
					</div>
				</div>

				{/* Bottom text */}
				<div
					style={{
						fontSize: 50,
						fontFamily: 'Noto Sans Devanagari',
						fontWeight: 700,
						color: '#e9d700',
						textAlign: 'center',
						opacity: sp(140),
						transform: `translateY(${interpolate(sp(140), [0, 1], [20, 0])}px)`,
						marginTop: 30,
					}}
				>
					हर पड़ाव तुम्हें बदलता है! 🚀
				</div>
			</AbsoluteFill>

			{/* Stick figure - walking with determination */}
			<div
				style={{
					position: 'absolute',
					bottom: 60,
					left: 60,
					transform: `translateY(${interpolate(sp(30), [0, 1], [80, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(30),
				}}
			>
				<svg width="90" height="150" viewBox="0 0 90 150">
					<circle cx="45" cy="30" r="28" fill="#FFC107" opacity="0.06" />
					<circle
						cx="45"
						cy="30"
						r="16"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2.5"
						transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 30)`}
					/>
					{/* Glasses */}
					<rect x="34" y="25" width="10" height="7" rx="3" fill="none" stroke="#FFC107" strokeWidth="1.8" />
					<rect x="48" y="25" width="10" height="7" rx="3" fill="none" stroke="#FFC107" strokeWidth="1.8" />
					<line x1="44" y1="28" x2="48" y2="28" stroke="#FFC107" strokeWidth="1.5" />
					{/* Smile */}
					<path d="M 39 37 Q 45 42 51 37" fill="none" stroke="#F5F5F5" strokeWidth="1.8" />
					{/* Body */}
					<line x1="45" y1="46" x2="45" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Arms swinging */}
					<line x1="45" y1="62" x2={28 + Math.sin(frame * 0.12) * 8} y2={78 + Math.cos(frame * 0.12) * 5} stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="45" y1="62" x2={62 - Math.sin(frame * 0.12) * 8} y2={78 - Math.cos(frame * 0.12) * 5} stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Legs walking */}
					<line x1="45" y1="95" x2={32 + Math.sin(frame * 0.12) * 6} y2="135" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="45" y1="95" x2={58 - Math.sin(frame * 0.12) * 6} y2="135" stroke="#F5F5F5" strokeWidth="2.5" />
				</svg>
			</div>

			{/* Vignette */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.65) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene6_Milestones;
```

```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene7_FirstLakh',
	durationInSeconds: 14,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene7_FirstLakh: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Impact shake when number appears
	const impactShake = frame > 25 && frame < 45 ? Math.sin(frame * 3) * 5 : 0;

	// Camera zoom to number
	const cameraZoom = interpolate(frame, [0, 40, 80], [1.1, 1, 1.02], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient - green/gold celebration */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(ellipse at 50% 40%, #0a1a0a 0%, #1C1C1C 55%)`,
					transform: `scale(${cameraZoom})`,
				}}
			/>
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(circle at 50% 40%, rgba(76,175,80,${0.08 + Math.sin(frame * 0.05) * 0.04}) 0%, transparent 40%)`,
				}}
			/>

			{/* Shape layers - celebration confetti shapes */}
			{[...Array(8)].map((_, i) => {
				const size = 40 + seededRandom(i + 5000) * 100;
				const x = seededRandom(i + 5100) * 1920;
				const y = seededRandom(i + 5200) * 1080;
				const rot = frame * 0.3 * (i % 2 === 0 ? 1 : -1) + i * 45;
				const shapes = ['circle', 'rect', 'diamond'];
				const shape = shapes[i % 3];

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: x + Math.sin(frame * 0.01 + i) * 20,
							top: y + Math.cos(frame * 0.008 + i) * 15,
							transform: `rotate(${rot}deg)`,
							opacity: 0.05,
						}}
					>
						{shape === 'circle' && (
							<div
								style={{
									width: size,
									height: size,
									borderRadius: '50%',
									border: `2px solid rgba(76,175,80,0.2)`,
								}}
							/>
						)}
						{shape === 'rect' && (
							<div
								style={{
									width: size,
									height: size * 0.5,
									border: `2px solid rgba(233,215,0,0.15)`,
									borderRadius: 6,
								}}
							/>
						)}
						{shape === 'diamond' && (
							<div
								style={{
									width: size * 0.7,
									height: size * 0.7,
									border: `2px solid rgba(255,193,7,0.15)`,
									transform: 'rotate(45deg)',
								}}
							/>
						)}
					</div>
				);
			})}

			{/* Motion texture - sparkle grid */}
			{[...Array(30)].map((_, i) => {
				const sparkleX = seededRandom(i + 5300) * 1920;
				const sparkleY = seededRandom(i + 5400) * 1080;
				const sparklePhase = seededRandom(i + 5500) * Math.PI * 2;
				const sparkleOp = Math.max(0, Math.sin(frame * 0.08 + sparklePhase));

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: sparkleX,
							top: sparkleY,
							width: 4,
							height: 4,
							backgroundColor: '#FFC107',
							borderRadius: '50%',
							opacity: sparkleOp * 0.2,
							transform: `scale(${sparkleOp})`,
						}}
					/>
				);
			})}

			{/* Floating particles */}
			{[...Array(22)].map((_, i) => {
				const px = seededRandom(i + 5600) * 1920;
				const py = seededRandom(i + 5700) * 1080;
				const pS = 2 + seededRandom(i + 5800) * 4;
				const yD = (py - frame * (0.2 + seededRandom(i + 5900) * 0.6));
				const wrapped = ((yD % 1080) + 1080) % 1080;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px + Math.sin(frame * 0.015 + i) * 20,
							top: wrapped,
							width: pS,
							height: pS,
							borderRadius: '50%',
							backgroundColor: i % 3 === 0 ? '#4CAF50' : '#FFC107',
							opacity: 0.12 + Math.sin(frame * 0.05 + i) * 0.08,
						}}
					/>
				);
			})}

			{/* Main content */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'column',
					alignItems: 'center',
					justifyContent: 'center',
					transform: `translate(${impactShake}px, ${impactShake * 0.5}px) scale(${cameraZoom})`,
				}}
			>
				{/* "पहला पड़ाव" badge */}
				<div
					style={{
						backgroundColor: '#4CAF50',
						padding: '12px 50px',
						borderRadius: 50,
						opacity: sp(5),
						transform: `translateY(${interpolate(sp(5), [0, 1], [-30, 0])}px)`,
						marginBottom: 30,
					}}
				>
					<span
						style={{
							fontSize: 38,
							fontFamily: 'Noto Sans Devanagari',
							fontWeight: 800,
							color: '#F5F5F5',
						}}
					>
						🏁 पहला पड़ाव
					</span>
				</div>

				{/* Giant ₹1,00,000 */}
				<div
					style={{
						position: 'relative',
						opacity: sp(15, { damping: 6, stiffness: 200 }),
						transform: `scale(${interpolate(sp(15, { damping: 6, stiffness: 200 }), [0, 1], [2.5, 1])})`,
					}}
				>
					{/* Glow behind number */}
					<div
						style={{
							position: 'absolute',
							top: '50%',
							left: '50%',
							width: 600,
							height: 200,
							marginLeft: -300,
							marginTop: -100,
							borderRadius: '50%',
							background: `radial-gradient(circle, rgba(76,175,80,${0.15 + Math.sin(frame * 0.06) * 0.08}) 0%, transparent 70%)`,
						}}
					/>
					<div
						style={{
							fontSize: 220,
							fontFamily: 'JetBrains Mono',
							fontWeight: 900,
							color: '#4CAF50',
							textShadow: '0 0 60px rgba(76,175,80,0.4), 0 8px 40px rgba(0,0,0,0.5)',
							position: 'relative',
						}}
					>
						₹1,00,000
					</div>
				</div>

				{/* "कुछ नहीं है?" provocation */}
				<div
					style={{
						fontSize: 60,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#9E9E9E',
						marginTop: 30,
						opacity: sp(50),
						transform: `translateY(${interpolate(sp(50), [0, 1], [30, 0])}px)`,
					}}
				>
					आज के ज़माने में{' '}
					<span style={{ color: '#ff0000', textDecoration: 'line-through' }}>
						कुछ नहीं?
					</span>
				</div>

				{/* Examples in row */}
				<div
					style={{
						display: 'flex',
						gap: 40,
						marginTop: 50,
					}}
				>
					{[
						{ label: 'एक फोन', icon: '📱', delay: 70 },
						{ label: 'फैमिली ट्रिप', icon: '✈️', delay: 85 },
						{ label: '2-3 महीने किराया', icon: '🏠', delay: 100 },
					].map((item, i) => (
						<div
							key={i}
							style={{
								display: 'flex',
								flexDirection: 'column',
								alignItems: 'center',
								opacity: sp(item.delay),
								transform: `translateY(${interpolate(sp(item.delay), [0, 1], [40, 0])}px) scale(${interpolate(sp(item.delay, { damping: 8 }), [0, 1], [0.5, 1])})`,
							}}
						>
							<div
								style={{
									width: 100,
									height: 100,
									borderRadius: 20,
									backgroundColor: '#2D2D2D',
									border: '2px solid #444',
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									fontSize: 48,
								}}
							>
								{item.icon}
							</div>
							<span
								style={{
									fontSize: 30,
									fontFamily: 'Noto Sans Devanagari',
									fontWeight: 600,
									color: '#9E9E9E',
									marginTop: 10,
								}}
							>
								= {item.label}
							</span>
						</div>
					))}
				</div>

				{/* But... tease */}
				<div
					style={{
						fontSize: 55,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#e9d700',
						marginTop: 40,
						opacity: sp(130),
						transform: `scale(${interpolate(sp(130, { damping: 6 }), [0, 1], [0.5, 1])})`,
						textShadow: '0 0 25px rgba(233,215,0,0.3)',
					}}
				>
					लेकिन ये पैसा नहीं... ये <span style={{ color: '#FFC107' }}>सबूत</span> है! 🏆
				</div>
			</AbsoluteFill>

			{/* Stick figure - celebrating */}
			<div
				style={{
					position: 'absolute',
					bottom: 80,
					left: 100,
					transform: `translateY(${interpolate(sp(40), [0, 1], [80, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(40),
				}}
			>
				<svg width="100" height="170" viewBox="0 0 100 170">
					<circle cx="50" cy="35" r="30" fill="#FFC107" opacity="0.07" />
					<circle
						cx="50"
						cy="35"
						r="18"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2.5"
						transform={`rotate(${Math.sin(frame * 0.04) * 3}, 50, 35)`}
					/>
					{/* Glasses */}
					<rect x="37" y="29" width="11" height="8" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<rect x="52" y="29" width="11" height="8" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<line x1="48" y1="33" x2="52" y2="33" stroke="#FFC107" strokeWidth="1.5" />
					{/* Big smile */}
					<path d="M 42 42 Q 50 52 58 42" fill="none" stroke="#F5F5F5" strokeWidth="2" />
					{/* Body */}
					<line x1="50" y1="53" x2="50" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Arms up celebrating */}
					<line x1="50" y1="70" x2="25" y2={45 + Math.sin(frame * 0.1) * 5} stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="50" y1="70" x2="75" y2={45 - Math.sin(frame * 0.1) * 5} stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Legs */}
					<line x1="50" y1="110" x2="35" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="50" y1="110" x2="65" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
				</svg>
			</div>

			{/* Vignette */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene7_FirstLakh;
```

```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene8_ProofOfChange',
	durationInSeconds: 15,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene8_ProofOfChange: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Camera subtle zoom
	const cameraZ = interpolate(frame, [0, durationInFrames], [1, 1.04], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient - warm transformation */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(ellipse at 40% 50%, #1a0d00 0%, #1C1C1C 55%, #001a0d 100%)`,
					transform: `scale(${cameraZ})`,
				}}
			/>

			{/* Shape layers - transformation arrows */}
			{[...Array(5)].map((_, i) => {
				const size = 80 + seededRandom(i + 6000) * 200;
				const x = seededRandom(i + 6100) * 1920;
				const y = seededRandom(i + 6200) * 1080;
				const rot = frame * 0.1 * (i % 2 === 0 ? 1 : -1);

				return (
					<svg
						key={i}
						style={{
							position: 'absolute',
							left: x + Math.sin(frame * 0.006 + i) * 20,
							top: y + Math.cos(frame * 0.008 + i) * 15,
							transform: `rotate(${rot}deg)`,
							opacity: 0.04,
						}}
						width={size}
						height={size * 0.6}
						viewBox="0 0 100 60"
					>
						<path
							d="M 10 30 L 60 30 L 60 15 L 90 30 L 60 45 L 60 30"
							fill="none"
							stroke={i % 2 === 0 ? 'rgba(76,175,80,0.2)' : 'rgba(255,152,0,0.2)'}
							strokeWidth="2"
						/>
					</svg>
				);
			})}

			{/* Motion texture - crosshatch */}
			<svg
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					opacity: 0.025,
				}}
			>
				{[...Array(15)].map((_, i) => (
					<React.Fragment key={i}>
						<line
							x1={i * 140}
							y1={0}
							x2={i * 140 + 500}
							y2={1080}
							stroke="#4CAF50"
							strokeWidth="0.8"
						/>
						<line
							x1={i * 140 + 500}
							y1={0}
							x2={i * 140}
							y2={1080}
							stroke="#FF9800"
							strokeWidth="0.8"
						/>
					</React.Fragment>
				))}
			</svg>

			{/* Floating particles */}
			{[...Array(18)].map((_, i) => {
				const px = seededRandom(i + 6300) * 1920;
				const py = seededRandom(i + 6400) * 1080;
				const pS = 2 + seededRandom(i + 6500) * 3;
				const yD = (py + frame * (0.15 + seededRandom(i + 6600) * 0.4)) % 1080;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px + Math.sin(frame * 0.02 + i) * 12,
							top: yD,
							width: pS,
							height: pS,
							borderRadius: '50%',
							backgroundColor: i % 2 === 0 ? '#4CAF50' : '#FF9800',
							opacity: 0.1 + Math.sin(frame * 0.04 + i) * 0.07,
						}}
					/>
				);
			})}

			{/* Main content - Proof cards */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'column',
					alignItems: 'center',
					justifyContent: 'center',
					transform: `scale(${cameraZ})`,
				}}
			>
				{/* Title */}
				<div
					style={{
						fontSize: 80,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#FFC107',
						textAlign: 'center',
						opacity: sp(5),
						transform: `translateY(${interpolate(sp(5), [0, 1], [40, 0])}px)`,
						marginBottom: 50,
						textShadow: '0 3px 25px rgba(255,193,7,0.3)',
					}}
				>
					₹1 लाख = सबूत! 🏆
				</div>

				{/* Two comparison cards */}
				<div
					style={{
						display: 'flex',
						gap: 80,
						alignItems: 'stretch',
					}}
				>
					{/* OLD HABIT card */}
					<div
						style={{
							width: 500,
							borderRadius: 24,
							backgroundColor: 'rgba(255,0,0,0.05)',
							border: '3px solid rgba(255,0,0,0.3)',
							padding: '35px 40px',
							opacity: sp(15),
							transform: `translateX(${interpolate(sp(15), [0, 1], [-100, 0])}px)`,
						}}
					>
						<div
							style={{
								fontSize: 40,
								fontFamily: 'Inter',
								fontWeight: 800,
								color: '#ff0000',
								marginBottom: 30,
								textAlign: 'center',
							}}
						>
							❌ पुरानी आदत
						</div>

						{/* Swiggy example */}
						<div
							style={{
								display: 'flex',
								alignItems: 'center',
								gap: 20,
								marginBottom: 20,
								opacity: sp(35),
								transform: `translateX(${interpolate(sp(35), [0, 1], [-30, 0])}px)`,
							}}
						>
							<div
								style={{
									width: 60,
									height: 60,
									borderRadius: 14,
									backgroundColor: '#2D2D2D',
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									fontSize: 30,
								}}
							>
								🍔
							</div>
							<div>
								<div
									style={{
										fontSize: 32,
										fontFamily: 'Noto Sans Devanagari',
										fontWeight: 700,
										color: '#F5F5F5',
										textDecoration: 'line-through',
										textDecorationColor: '#ff0000',
									}}
								>
									Swiggy ₹400/शाम
								</div>
								<div
									style={{
										fontSize: 24,
										fontFamily: 'JetBrains Mono',
										color: '#ff0000',
									}}
								>
									₹12,000/महीना waste
								</div>
							</div>
						</div>

						{/* EMI phone */}
						<div
							style={{
								display: 'flex',
								alignItems: 'center',
								gap: 20,
								opacity: sp(55),
								transform: `translateX(${interpolate(sp(55), [0, 1], [-30, 0])}px)`,
							}}
						>
							<div
								style={{
									width: 60,
									height: 60,
									borderRadius: 14,
									backgroundColor: '#2D2D2D',
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									fontSize: 30,
								}}
							>
								📱
							</div>
							<div>
								<div
									style={{
										fontSize: 32,
										fontFamily: 'Noto Sans Devanagari',
										fontWeight: 700,
										color: '#F5F5F5',
										textDecoration: 'line-through',
										textDecorationColor: '#ff0000',
									}}
								>
									EMI वाला नया फोन
								</div>
								<div
									style={{
										fontSize: 24,
										fontFamily: 'JetBrains Mono',
										color: '#ff0000',
									}}
								>
									सब दोस्तों ने लिया
								</div>
							</div>
						</div>
					</div>

					{/* NEW HABIT card */}
					<div
						style={{
							width: 500,
							borderRadius: 24,
							backgroundColor: 'rgba(76,175,80,0.05)',
							border: '3px solid rgba(76,175,80,0.3)',
							padding: '35px 40px',
							opacity: sp(25),
							transform: `translateX(${interpolate(sp(25), [0, 1], [100, 0])}px)`,
						}}
					>
						<div
							style={{
								fontSize: 40,
								fontFamily: 'Inter',
								fontWeight: 800,
								color: '#4CAF50',
								marginBottom: 30,
								textAlign: 'center',
							}}
						>
							✅ नई आदत
						</div>

						{/* SIP */}
						<div
							style={{
								display: 'flex',
								alignItems: 'center',
								gap: 20,
								marginBottom: 20,
								opacity: sp(45),
								transform: `translateX(${interpolate(sp(45), [0, 1], [30, 0])}px)`,
							}}
						>
							<div
								style={{
									width: 60,
									height: 60,
									borderRadius: 14,
									backgroundColor: '#1a2a1a',
									border: '2px solid #4CAF50',
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									fontSize: 30,
								}}
							>
								💰
							</div>
							<div>
								<div
									style={{
										fontSize: 32,
										fontFamily: 'Noto Sans Devanagari',
										fontWeight: 700,
										color: '#4CAF50',
									}}
								>
									₹5,000 SIP/महीना
								</div>
								<div
									style={{
										fontSize: 24,
										fontFamily: 'JetBrains Mono',
										color: '#4CAF50',
									}}
								>
									₹60,000/साल invest
								</div>
							</div>
						</div>

						{/* Discipline */}
						<div
							style={{
								display: 'flex',
								alignItems: 'center',
								gap: 20,
								opacity: sp(65),
								transform: `translateX(${interpolate(sp(65), [0, 1], [30, 0])}px)`,
							}}
						>
							<div
								style={{
									width: 60,
									height: 60,
									borderRadius: 14,
									backgroundColor: '#1a2a1a',
									border: '2px solid #4CAF50',
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									fontSize: 30,
								}}
							>
								🧠
							</div>
							<div>
								<div
									style={{
										fontSize: 32,
										fontFamily: 'Noto Sans Devanagari',
										fontWeight: 700,
										color: '#4CAF50',
									}}
								>
									Impulsive नहीं हो
								</div>
								<div
									style={{
										fontSize: 24,
										fontFamily: 'JetBrains Mono',
										color: '#4CAF50',
									}}
								>
									आज ≠ कल समझते हो
								</div>
							</div>
						</div>
					</div>
				</div>

				{/* Bottom highlight */}
				<div
					style={{
						fontSize: 52,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#e9d700',
						textAlign: 'center',
						marginTop: 50,
						opacity: sp(120),
						transform: `scale(${interpolate(sp(120, { damping: 8 }), [0, 1], [0.6, 1])})`,
					}}
				>
					आदत बदली → ज़िन्दगी बदलेगी! ⚡
				</div>
			</AbsoluteFill>

			{/* Stick figure - proud stance */}
			<div
				style={{
					position: 'absolute',
					bottom: 70,
					right: 80,
					transform: `translateY(${interpolate(sp(50), [0, 1], [80, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(50),
				}}
			>
				<svg width="90" height="160" viewBox="0 0 90 160">
					<circle cx="45" cy="30" r="28" fill="#FFC107" opacity="0.06" />
					<circle
						cx="45"
						cy="30"
						r="16"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2.5"
						transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 30)`}
					/>
					<rect x="33" y="25" width="10" height="7" rx="3" fill="none" stroke="#FFC107" strokeWidth="1.8" />
					<rect x="47" y="25" width="10" height="7" rx="3" fill="none" stroke="#FFC107" strokeWidth="1.8" />
					<line x1="43" y1="28" x2="47" y2="28" stroke="#FFC107" strokeWidth="1.5" />
					<path d="M 39 37 Q 45 43 51 37" fill="none" stroke="#F5F5F5" strokeWidth="1.8" />
					<line x1="45" y1="46" x2="45" y2="100" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Arms on hips - proud */}
					<path d="M 45 65 L 25 75 L 30 90" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
					<path d="M 45 65 L 65 75 L 60 90" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="45" y1="100" x2="30" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="45" y1="100" x2="60" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
				</svg>
			</div>

			{/* Vignette */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene8_ProofOfChange;
```

```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene9_80PercentQuit',
	durationInSeconds: 15,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene9_80PercentQuit: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Screen shake on stat reveal
	const shakeX = frame > 40 && frame < 60 ? Math.sin(frame * 2.5) * 6 : 0;

	// Animated percentage counter
	const percentCount = Math.min(80, interpolate(frame, [40, 100], [0, 80], {
		extrapolateLeft: 'clamp',
		extrapolateRight: 'clamp',
	}));

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient - alarming red */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(ellipse at 50% 40%, #2a0a0a 0%, #1C1C1C 55%)`,
				}}
			/>
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(circle at 50% 40%, rgba(255,0,0,${0.06 + Math.sin(frame * 0.05) * 0.03}) 0%, transparent 45%)`,
				}}
			/>

			{/* Shape layers - warning symbols */}
			{[...Array(6)].map((_, i) => {
				const size = 100 + seededRandom(i + 7000) * 200;
				const x = seededRandom(i + 7100) * 1920;
				const y = seededRandom(i + 7200) * 1080;
				const rot = frame * 0.1 * (i % 2 === 0 ? 1 : -1);

				return (
					<svg
						key={i}
						style={{
							position: 'absolute',
							left: x + Math.sin(frame * 0.005 + i) * 20,
							top: y + Math.cos(frame * 0.007 + i) * 15,
							transform: `rotate(${rot}deg)`,
							opacity: 0.04,
						}}
						width={size}
						height={size}
						viewBox="0 0 100 100"
					>
						{i % 2 === 0 ? (
							<polygon
								points="50,10 90,90 10,90"
								fill="none"
								stroke="rgba(255,0,0,0.2)"
								strokeWidth="2"
							/>
						) : (
							<circle
								cx="50"
								cy="50"
								r="40"
								fill="none"
								stroke="rgba(255,0,0,0.12)"
								strokeWidth="2"
							/>
						)}
					</svg>
				);
			})}

			{/* Motion texture - warning stripes */}
			{[...Array(10)].map((_, i) => (
				<div
					key={i}
					style={{
						position: 'absolute',
						left: -100 + (i * 220 + frame * 0.5) % 2200,
						top: 0,
						width: 60,
						height: '100%',
						background: `repeating-linear-gradient(45deg, transparent, transparent 20px, rgba(255,0,0,0.02) 20px, rgba(255,0,0,0.02) 40px)`,
					}}
				/>
			))}

			{/* Floating particles */}
			{[...Array(20)].map((_, i) => {
				const px = seededRandom(i + 7300) * 1920;
				const py = seededRandom(i + 7400) * 1080;
				const pS = 2 + seededRandom(i + 7500) * 3;
				const yD = (py + frame * (0.3 + seededRandom(i + 7600) * 0.5)) % 1080;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px + Math.sin(frame * 0.02 + i) * 15,
							top: yD,
							width: pS,
							height: pS,
							borderRadius: '50%',
							backgroundColor: i % 3 === 0 ? '#ff0000' : '#FF5722',
							opacity: 0.1 + Math.sin(frame * 0.04 + i) * 0.06,
						}}
					/>
				);
			})}

			{/* Main content */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'column',
					alignItems: 'center',
					justifyContent: 'center',
					transform: `translateX(${shakeX}px)`,
				}}
			>
				{/* Survey badge */}
				<div
					style={{
						backgroundColor: '#2D2D2D',
						border: '2px solid #ff0000',
						padding: '10px 40px',
						borderRadius: 50,
						opacity: sp(5),
						transform: `translateY(${interpolate(sp(5), [0, 1], [-20, 0])}px)`,
						marginBottom: 40,
					}}
				>
					<span
						style={{
							fontSize: 32,
							fontFamily: 'Noto Sans Devanagari',
							fontWeight: 700,
							color: '#ff0000',
						}}
					>
						📊 INDIA SIP SURVEY
					</span>
				</div>

				{/* Giant 80% */}
				<div
					style={{
						position: 'relative',
						opacity: sp(15),
						transform: `scale(${interpolate(sp(15, { damping: 6, stiffness: 200 }), [0, 1], [3, 1])})`,
					}}
				>
					{/* Red glow */}
					<div
						style={{
							position: 'absolute',
							top: '50%',
							left: '50%',
							width: 500,
							height: 300,
							marginLeft: -250,
							marginTop: -150,
							borderRadius: '50%',
							background: `radial-gradient(circle, rgba(255,0,0,${0.12 + Math.sin(frame * 0.06) * 0.06}) 0%, transparent 70%)`,
						}}
					/>
					<div
						style={{
							fontSize: 260,
							fontFamily: 'JetBrains Mono',
							fontWeight: 900,
							color: '#ff0000',
							textShadow: '0 0 80px rgba(255,0,0,0.5), 0 8px 40px rgba(0,0,0,0.5)',
							position: 'relative',
						}}
					>
						{Math.round(percentCount)}%
					</div>
				</div>

				{/* Explanation */}
				<div
					style={{
						fontSize: 55,
						fontFamily: 'Noto Sans Devanagari',
						fontWeight: 700,
						color: '#F5F5F5',
						textAlign: 'center',
						marginTop: 20,
						opacity: sp(60),
						transform: `translateY(${interpolate(sp(60), [0, 1], [30, 0])}px)`,
						lineHeight: 1.4,
					}}
				>
					लोग SIP शुरू करके
				</div>

				<div
					style={{
						display: 'flex',
						alignItems: 'center',
						gap: 20,
						marginTop: 10,
						opacity: sp(75),
						transform: `translateY(${interpolate(sp(75), [0, 1], [25, 0])}px)`,
					}}
				>
					<div
						style={{
							fontSize: 80,
							fontFamily: 'Yantra One',
							fontWeight: 900,
							color: '#ff0000',
						}}
					>
						3 साल
					</div>
					<div
						style={{
							fontSize: 55,
							fontFamily: 'Noto Sans Devanagari',
							fontWeight: 700,
							color: '#F5F5F5',
						}}
					>
						में बंद कर देते हैं
					</div>
				</div>

				{/* Visual bar - 80% red, 20% green */}
				<div
					style={{
						width: 800,
						height: 50,
						borderRadius: 25,
						backgroundColor: '#2D2D2D',
						marginTop: 40,
						overflow: 'hidden',
						opacity: sp(90),
						border: '2px solid #444',
					}}
				>
					<div
						style={{
							width: `${interpolate(frame, [90, 150], [0, 80], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}%`,
							height: '100%',
							backgroundColor: '#ff0000',
							borderRadius: 25,
							transition: 'width 0.1s',
							position: 'relative',
						}}
					>
						<span
							style={{
								position: 'absolute',
								right: 15,
								top: '50%',
								transform: 'translateY(-50%)',
								fontSize: 22,
								fontFamily: 'JetBrains Mono',
								fontWeight: 800,
								color: '#F5F5F5',
							}}
						>
							छोड़ दिया
						</span>
					</div>
				</div>

				<div
					style={{
						display: 'flex',
						gap: 30,
						marginTop: 15,
						opacity: sp(110),
					}}
				>
					<span style={{ fontSize: 26, fontFamily: 'JetBrains Mono', color: '#ff0000', fontWeight: 700 }}>
						■ 80% बंद
					</span>
					<span style={{ fontSize: 26, fontFamily: 'JetBrains Mono', color: '#4CAF50', fontWeight: 700 }}>
						■ 20% जारी
					</span>
				</div>

				{/* Punchline */}
				<div
					style={{
						fontSize: 48,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#e9d700',
						marginTop: 35,
						opacity: sp(130),
						transform: `scale(${interpolate(sp(130, { damping: 8 }), [0, 1], [0.6, 1])})`,
					}}
				>
					सबसे critical phase में ही छोड़ देते हैं! 😤
				</div>
			</AbsoluteFill>

			{/* Stick figure - shocked */}
			<div
				style={{
					position: 'absolute',
					bottom: 70,
					left: 70,
					transform: `translateY(${interpolate(sp(40), [0, 1], [80, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(40),
				}}
			>
				<svg width="90" height="155" viewBox="0 0 90 155">
					<circle cx="45" cy="30" r="28" fill="#FFC107" opacity="0.06" />
					<circle
						cx="45"
						cy="30"
						r="16"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2.5"
						transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 30)`}
					/>
					{/* Glasses */}
					<rect x="33" y="25" width="10" height="7" rx="3" fill="none" stroke="#FFC107" strokeWidth="1.8" />
					<rect x="47" y="25" width="10" height="7" rx="3" fill="none" stroke="#FFC107" strokeWidth="1.8" />
					<line x1="43" y1="28" x2="47" y2="28" stroke="#FFC107" strokeWidth="1.5" />
					{/* Shocked mouth */}
					<circle cx="45" cy="40" r="5" fill="none" stroke="#F5F5F5" strokeWidth="2" />
					{/* Body */}
					<line x1="45" y1="46" x2="45" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Arms up in shock */}
					<line x1="45" y1="62" x2="20" y2="48" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="45" y1="62" x2="70" y2="48" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Legs */}
					<line x1="45" y1="95" x2="30" y2="140" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="45" y1="95" x2="60" y2="140" stroke="#F5F5F5" strokeWidth="2.5" />
				</svg>
			</div>

			{/* Vignette */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.7) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene9_80PercentQuit;
```

```tsx
import React from 'react';
import {
	AbsoluteFill,
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter2Scene10_GearShift',
	durationInSeconds: 14,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter2Scene10_GearShift: React.FC = () => {
	const frame = useCurrentFrame();
	const { fps, durationInFrames } = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{ extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
	);

	// Camera dramatic zoom out then in
	const cameraZoom = interpolate(frame, [0, 60, 180], [1.15, 1, 1.05], {
		extrapolateRight: 'clamp',
	});

	// Gear rotation
	const gearRot = frame * 0.8;

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Dynamic gradient - mechanical/dark */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(ellipse at 50% 55%, #0a0a1a 0%, #1C1C1C 50%, #1a0a0a 100%)`,
					transform: `scale(${cameraZoom})`,
				}}
			/>
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background: `radial-gradient(circle at 60% 40%, rgba(255,193,7,0.05) 0%, transparent 40%)`,
					opacity: 0.5 + Math.sin(frame * 0.04) * 0.3,
				}}
			/>

			{/* Shape layers - large gears background */}
			{[...Array(3)].map((_, i) => {
				const gearSize = 300 + i * 150;
				const positions = [
					{ x: -100, y: -100 },
					{ x: 1600, y: 800 },
					{ x: 800, y: -150 },
				];
				const speed = (i + 1) * 0.15;

				return (
					<svg
						key={i}
						style={{
							position: 'absolute',
							left: positions[i].x,
							top: positions[i].y,
							opacity: 0.04,
							transform: `rotate(${gearRot * speed * (i % 2 === 0 ? 1 : -1)}deg)`,
						}}
						width={gearSize}
						height={gearSize}
						viewBox="0 0 200 200"
					>
						<circle
							cx="100"
							cy="100"
							r="70"
							fill="none"
							stroke="#FFC107"
							strokeWidth="3"
						/>
						<circle
							cx="100"
							cy="100"
							r="25"
							fill="none"
							stroke="#FFC107"
							strokeWidth="3"
						/>
						{[...Array(12)].map((_, j) => (
							<rect
								key={j}
								x="92"
								y="20"
								width="16"
								height="25"
								rx="4"
								fill="#FFC107"
								opacity="0.3"
								transform={`rotate(${j * 30}, 100, 100)`}
							/>
						))}
					</svg>
				);
			})}

			{/* Motion texture - speed lines from center */}
			<svg
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					opacity: 0.04,
				}}
			>
				{[...Array(24)].map((_, i) => {
					const angle = (i * 15) * (Math.PI / 180);
					const innerR = 200;
					const outerR = 800;
					return (
						<line
							key={i}
							x1={960 + Math.cos(angle) * innerR}
							y1={540 + Math.sin(angle) * innerR}
							x2={960 + Math.cos(angle) * outerR}
							y2={540 + Math.sin(angle) * outerR}
							stroke="#9E9E9E"
							strokeWidth="1"
						/>
					);
				})}
			</svg>

			{/* Floating particles - mechanical sparks */}
			{[...Array(25)].map((_, i) => {
				const angle = (i * 14.4 + frame * 0.8) * (Math.PI / 180);
				const dist = 150 + seededRandom(i + 8000) * 350;
				const px = 960 + Math.cos(angle) * dist;
				const py = 540 + Math.sin(angle) * dist;
				const pS = 2 + seededRandom(i + 8100) * 3;

				return (
					<div
						key={i}
						style={{
							position: 'absolute',
							left: px,
							top: py,
							width: pS,
							height: pS,
							borderRadius: '50%',
							backgroundColor: i % 4 === 0 ? '#FF6B00' : i % 4 === 1 ? '#FFC107' : i % 4 === 2 ? '#e9d700' : '#FF9800',
							opacity: 0.15 + Math.sin(frame * 0.06 + i) * 0.1,
						}}
					/>
				);
			})}

			{/* Main content */}
			<AbsoluteFill
				style={{
					display: 'flex',
					flexDirection: 'column',
					alignItems: 'center',
					justifyContent: 'center',
					transform: `scale(${cameraZoom})`,
				}}
			>
				{/* Compounding growth curve */}
				<div
					style={{
						position: 'relative',
						width: 1200,
						height: 450,
						marginBottom: 30,
					}}
				>
					{/* Chart background */}
					<div
						style={{
							position: 'absolute',
							width: '100%',
							height: '100%',
							backgroundColor: 'rgba(45,45,45,0.3)',
							borderRadius: 20,
							border: '2px solid #333',
							opacity: sp(5),
						}}
					/>

					{/* Exponential curve */}
					<svg
						style={{
							position: 'absolute',
							width: '100%',
							height: '100%',
							opacity: sp(15),
						}}
						viewBox="0 0 1200 450"
					>
						{/* Grid lines */}
						{[...Array(5)].map((_, i) => (
							<line
								key={i}
								x1="80"
								y1={90 * (i + 0.5)}
								x2="1150"
								y2={90 * (i + 0.5)}
								stroke="#333"
								strokeWidth="1"
								strokeDasharray="5,5"
							/>
						))}

						{/* The curve - slow then explosive */}
						<path
							d={`M 100 400 Q 300 390 500 370 Q 650 355 750 320 Q 850 260 950 150 Q 1050 50 1100 20`}
							fill="none"
							stroke="#4CAF50"
							strokeWidth="4"
							strokeDasharray={`${interpolate(frame, [15, 200], [0, 1500], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })},1500`}
						/>

						{/* "QUIT HERE" zone */}
						<rect
							x="100"
							y="300"
							width={interpolate(sp(60), [0, 1], [0, 400])}
							height="120"
							fill="rgba(255,0,0,0.08)"
							rx="10"
						/>
						<text
							x="300"
							y="370"
							textAnchor="middle"
							fill="#ff0000"
							fontSize="28"
							fontWeight="bold"
							fontFamily="Inter"
							opacity={sp(70)}
						>
							80% यहाँ छोड़ देते हैं ❌
						</text>

						{/* "GEAR SHIFT" point */}
						<circle
							cx="750"
							cy="320"
							r={12 + Math.sin(frame * 0.1) * 3}
							fill="#FFC107"
							opacity={sp(100)}
						/>
						<text
							x="780"
							y="310"
							fill="#FFC107"
							fontSize="24"
							fontWeight="bold"
							fontFamily="Inter"
							opacity={sp(100)}
						>
							⚡ GEAR SHIFT
						</text>

						{/* Rocket zone */}
						<text
							x="1020"
							y="60"
							fill="#e9d700"
							fontSize="26"
							fontWeight="bold"
							fontFamily="Inter"
							opacity={sp(130)}
						>
							🚀 EXPLOSION
						</text>
					</svg>

					{/* Time axis labels */}
					<div
						style={{
							position: 'absolute',
							bottom: 10,
							left: 80,
							right: 50,
							display: 'flex',
							justifyContent: 'space-between',
							opacity: sp(25),
						}}
					>
						{['Year 1', 'Year 5', 'Year 10', 'Year 15', 'Year 20', 'Year 25'].map(
							(label, i) => (
								<span
									key={i}
									style={{
										fontSize: 20,
										fontFamily: 'JetBrains Mono',
										color: '#9E9E9E',
									}}
								>
									{label}
								</span>
							)
						)}
					</div>
				</div>

				{/* Bottom punchline */}
				<div
					style={{
						fontSize: 65,
						fontFamily: 'Yantra One',
						fontWeight: 900,
						color: '#F5F5F5',
						textAlign: 'center',
						lineHeight: 1.3,
						opacity: sp(110),
						transform: `translateY(${interpolate(sp(110), [0, 1], [30, 0])}px)`,
					}}
				>
					Compounding ने{' '}
					<span
						style={{
							color: '#FFC107',
							textShadow: '0 0 25px rgba(255,193,7,0.3)',
						}}
					>
						Gear Shift
					</span>{' '}
					भी नहीं किया था!
				</div>

				<div
					style={{
						fontSize: 48,
						fontFamily: 'Noto Sans Devanagari',
						fontWeight: 700,
						color: '#ff0000',
						textAlign: 'center',
						marginTop: 15,
						opacity: sp(140),
						transform: `scale(${interpolate(sp(140, { damping: 8 }), [0, 1], [0.7, 1])})`,
					}}
				>
					और लोगों ने छोड़ दिया! 💔
				</div>
			</AbsoluteFill>

			{/* Stick figure - pointing at chart */}
			<div
				style={{
					position: 'absolute',
					bottom: 120,
					left: 50,
					transform: `translateY(${interpolate(sp(45), [0, 1], [80, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					opacity: sp(45),
				}}
			>
				<svg width="100" height="170" viewBox="0 0 100 170">
					<circle cx="50" cy="35" r="30" fill="#FFC107" opacity="0.07" />
					<circle
						cx="50"
						cy="35"
						r="18"
						fill="none"
						stroke="#F5F5F5"
						strokeWidth="2.5"
						transform={`rotate(${Math.sin(frame * 0.04) * 3}, 50, 35)`}
					/>
					{/* Glasses */}
					<rect x="37" y="29" width="11" height="8" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<rect x="52" y="29" width="11" height="8" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
					<line x1="48" y1="33" x2="52" y2="33" stroke="#FFC107" strokeWidth="1.5" />
					{/* Serious expression */}
					<line x1="43" y1="44" x2="57" y2="44" stroke="#F5F5F5" strokeWidth="2" />
					{/* Body */}
					<line x1="50" y1="53" x2="50" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Arm pointing right/up */}
					<line x1="50" y1="70" x2="85" y2="45" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="50" y1="75" x2="25" y2="90" stroke="#F5F5F5" strokeWidth="2.5" />
					{/* Pointing finger */}
					<circle cx="88" cy="43" r="3" fill="#FFC107" />
					{/* Legs */}
					<line x1="50" y1="110" x2="35" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
					<line x1="50" y1="110" x2="65" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
				</svg>
			</div>

			{/* Vignette */}
			<div
				style={{
					position: 'absolute',
					width: '100%',
					height: '100%',
					background:
						'radial-gradient(ellipse at center, transparent 42%, rgba(0,0,0,0.65) 100%)',
					pointerEvents: 'none',
				}}
			/>

			{/* Watermark */}
			<div
				style={{
					position: 'absolute',
					bottom: 30,
					right: 45,
					fontFamily: 'Inter',
					fontSize: 22,
					fontWeight: 600,
					color: '#9E9E9E',
					opacity: interpolate(frame, [0, 30], [0, 0.3], {
						extrapolateRight: 'clamp',
					}),
				}}
			>
				ProBuffet HQ
			</div>
		</AbsoluteFill>
	);
};

export default Chapter2Scene10_GearShift;
```

