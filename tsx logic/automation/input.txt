```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11_WhiteHouseClaim',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_WhiteHouseClaim: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Label reveals
  const labelOp = sp(5);
  const titleOp = sp(15);
  const subOp = sp(35);
  const barrelOp = sp(55);

  // Barrel counter animation
  const barrelCount = Math.floor(interpolate(frame, [55, 180], [0, 4280000], { extrapolateRight: 'clamp' }));

  // Underline draw
  const underline = interpolate(frame, [40, 80], [0, 100], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Shape drift
  const shapeRot = frame * 0.15;
  const shapeY = Math.sin(frame * 0.02) * 12;

  // Stamp pulse
  const stampScale = sp(70, { damping: 6, stiffness: 200 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 20%, rgba(6,32,43,0.08) 0%, rgba(238,238,238,0) 60%), radial-gradient(ellipse at 80% 90%, rgba(6,32,43,0.06) 0%, rgba(238,238,238,0) 55%)',
      }} />

      {/* Shape Layers - large transparent forms */}
      <div style={{
        position: 'absolute',
        top: 100 + shapeY,
        left: 1300,
        width: 600,
        height: 600,
        border: '2px solid #06202B',
        borderRadius: '50%',
        opacity: 0.05,
        transform: `rotate(${shapeRot}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        top: 600,
        left: -150,
        width: 500,
        height: 500,
        backgroundColor: '#06202B',
        opacity: 0.04,
        transform: `rotate(${-shapeRot * 0.5}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        top: 800,
        left: 1500,
        width: 0,
        height: 0,
        borderLeft: '200px solid transparent',
        borderRight: '200px solid transparent',
        borderBottom: '350px solid #06202B',
        opacity: 0.05,
        transform: `rotate(${shapeRot * 0.7}deg)`,
      }} />

      {/* Motion texture - grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.08 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 100} y1={0} x2={i * 100} y2={1080} stroke="#06202B" strokeWidth="1" />
        ))}
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 100} x2={1920} y2={i * 100} stroke="#06202B" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 100) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 20;
        const r = seededRandom(i + 200) * 4 + 2;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + drift,
            top: py + Math.cos(frame * 0.02 + i) * 15,
            width: r,
            height: r,
            backgroundColor: '#06202B',
            borderRadius: '50%',
            opacity: 0.25,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>

        {/* Top tag label */}
        <div style={{
          position: 'absolute',
          top: 100,
          left: 120,
          opacity: labelOp,
          transform: `translateX(${(1 - labelOp) * -40}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <div style={{
            width: 18,
            height: 18,
            backgroundColor: '#FF0000',
            borderRadius: '50%',
            boxShadow: `0 0 ${20 + Math.sin(frame * 0.3) * 10}px #FF0000`,
          }} />
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 28,
            color: '#06202B',
            letterSpacing: 6,
            fontWeight: 700,
          }}>
            WHITE HOUSE / OFFICIAL CLAIM
          </div>
        </div>

        {/* Main title */}
        <div style={{
          position: 'absolute',
          top: 200,
          left: 120,
          right: 120,
          opacity: titleOp,
          transform: `translateY(${(1 - titleOp) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 119,
            lineHeight: 1.02,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -2,
          }}>
            "MILLIONS OF BARRELS,
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 119,
            lineHeight: 1.02,
            color: '#000000',
            fontWeight: 900,
            letterSpacing: -2,
            marginTop: 8,
          }}>
            QUIETLY PULLED."
          </div>
          {/* Underline */}
          <div style={{
            marginTop: 20,
            height: 8,
            width: `${underline * 8}px`,
            maxWidth: 900,
            backgroundColor: '#06202B',
          }} />
        </div>

        {/* Sub statement */}
        <div style={{
          position: 'absolute',
          top: 540,
          left: 120,
          opacity: subOp,
          transform: `translateY(${(1 - subOp) * 20}px)`,
          fontFamily: 'Inter, sans-serif',
          fontSize: 46,
          color: '#06202B',
          fontWeight: 500,
          maxWidth: 1100,
          lineHeight: 1.3,
        }}>
          The U.S. is reportedly pulling oil from the ground at
          <span style={{ fontWeight: 800, color: '#000000' }}> record levels</span> — straight from
          the Oval Office's own framing.
        </div>

        {/* Barrel counter card */}
        <div style={{
          position: 'absolute',
          bottom: 130,
          left: 120,
          opacity: barrelOp,
          transform: `translateY(${(1 - barrelOp) * 40}px)`,
          backgroundColor: '#06202B',
          padding: '34px 56px',
          borderRadius: 4,
          display: 'flex',
          alignItems: 'center',
          gap: 40,
          boxShadow: '0 20px 60px rgba(6,32,43,0.25)',
        }}>
          {/* Barrel icon */}
          <svg width="80" height="100" viewBox="0 0 80 100">
            <ellipse cx="40" cy="12" rx="32" ry="8" fill="#EEEEEE" stroke="#EEEEEE" strokeWidth="2"/>
            <path d="M 8 12 Q 4 50, 8 88 Q 40 96, 72 88 Q 76 50, 72 12" fill="#EEEEEE" stroke="#EEEEEE" strokeWidth="2"/>
            <line x1="10" y1="35" x2="70" y2="35" stroke="#06202B" strokeWidth="3"/>
            <line x1="10" y1="65" x2="70" y2="65" stroke="#06202B" strokeWidth="3"/>
          </svg>
          <div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#EEEEEE',
              letterSpacing: 4,
              opacity: 0.7,
              marginBottom: 6,
            }}>
              U.S. DAILY CRUDE OUTPUT
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 85,
              color: '#EEEEEE',
              fontWeight: 900,
              letterSpacing: -1,
              lineHeight: 1,
            }}>
              {barrelCount.toLocaleString()} <span style={{ fontSize: 38, opacity: 0.8 }}>BBL/DAY ~</span>
            </div>
          </div>
        </div>

        {/* Confidential stamp */}
        <div style={{
          position: 'absolute',
          top: 230,
          right: 140,
          transform: `scale(${stampScale}) rotate(-12deg)`,
          opacity: stampScale * 0.85,
          border: '6px solid #FF0000',
          padding: '14px 28px',
          fontFamily: 'Mont Heavy, sans-serif',
          fontSize: 44,
          color: '#FF0000',
          fontWeight: 900,
          letterSpacing: 4,
        }}>
          CLASSIFIED?
        </div>

      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_WhiteHouseClaim;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12_HiddenPlaces',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_HiddenPlaces: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -25]);

  const headOp = sp(5);
  const mapOp = sp(25);
  const pin1 = sp(55, { damping: 8 });
  const pin2 = sp(75, { damping: 8 });
  const pin3 = sp(95, { damping: 8 });
  const cushionOp = sp(140);

  const shapeRot = frame * 0.1;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #EEEEEE 0%, #E0E5E8 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: -200,
        left: -200,
        width: 700,
        height: 700,
        backgroundColor: '#06202B',
        opacity: 0.05,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        top: 700,
        right: -150,
        width: 500,
        height: 500,
        border: '3px solid #06202B',
        opacity: 0.06,
        transform: `rotate(${shapeRot}deg)`,
      }} />

      {/* Motion texture - concentric circles */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <circle key={i} cx="960" cy="540" r={150 + i * 100} fill="none" stroke="#06202B" strokeWidth="1" strokeDasharray="4 8" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 50) * 1080;
        const drift = Math.sin(frame * 0.025 + i) * 25;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + drift,
            top: py,
            width: 3,
            height: 3,
            backgroundColor: '#06202B',
            opacity: 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>

        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 120,
          opacity: headOp,
          transform: `translateY(${(1 - headOp) * -30}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#06202B',
            letterSpacing: 5,
            fontWeight: 700,
            marginBottom: 18,
            opacity: 0.7,
          }}>
            ▸ PLACES YOU'VE NEVER HEARD OF
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 102,
            lineHeight: 1.02,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -2,
            maxWidth: 1500,
          }}>
            A HIDDEN <span style={{ color: '#000000', backgroundColor: '#06202B', color: '#EEEEEE', padding: '0 18px' }}>PRODUCTION</span>
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 102,
            lineHeight: 1.02,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -2,
            marginTop: 6,
          }}>
            CUSHION.
          </div>
        </div>

        {/* Stylized map - rectangular US outline */}
        <div style={{
          position: 'absolute',
          top: 540,
          left: 120,
          opacity: mapOp,
          transform: `translateY(${(1 - mapOp) * 30}px)`,
        }}>
          <svg width="900" height="460" viewBox="0 0 900 460">
            {/* US abstract shape */}
            <path
              d="M 60 120 Q 100 70, 220 80 L 380 70 Q 500 65, 640 80 Q 780 90, 840 140 L 820 220 Q 800 280, 760 320 L 680 360 Q 580 400, 460 400 Q 340 405, 240 380 L 140 350 Q 80 310, 60 240 Z"
              fill="none"
              stroke="#06202B"
              strokeWidth="4"
              strokeDasharray="8 6"
            />
            <path
              d="M 60 120 Q 100 70, 220 80 L 380 70 Q 500 65, 640 80 Q 780 90, 840 140 L 820 220 Q 800 280, 760 320 L 680 360 Q 580 400, 460 400 Q 340 405, 240 380 L 140 350 Q 80 310, 60 240 Z"
              fill="#06202B"
              opacity="0.06"
            />

            {/* Pin 1 - Permian Basin */}
            <g transform="translate(380, 280)" opacity={pin1} style={{ transform: `translate(380px, 280px) scale(${pin1})` }}>
              <circle cx="0" cy="0" r="18" fill="#FF0000" />
              <circle cx="0" cy="0" r={18 + (frame % 60) * 0.8} fill="none" stroke="#FF0000" strokeWidth="2" opacity={1 - (frame % 60) / 60} />
              <text x="25" y="6" fontFamily="JetBrains Mono" fontSize="22" fontWeight="700" fill="#06202B">PERMIAN BASIN</text>
            </g>

            {/* Pin 2 - Bakken */}
            <g style={{ transform: `translate(450px, 130px) scale(${pin2})` }}>
              <circle cx="0" cy="0" r="14" fill="#06202B" />
              <circle cx="0" cy="0" r={14 + (frame % 50) * 0.6} fill="none" stroke="#06202B" strokeWidth="2" opacity={1 - (frame % 50) / 50} />
              <text x="22" y="6" fontFamily="JetBrains Mono" fontSize="20" fontWeight="700" fill="#06202B">BAKKEN</text>
            </g>

            {/* Pin 3 - Eagle Ford */}
            <g style={{ transform: `translate(420px, 350px) scale(${pin3})` }}>
              <circle cx="0" cy="0" r="14" fill="#06202B" />
              <circle cx="0" cy="0" r={14 + (frame % 55) * 0.7} fill="none" stroke="#06202B" strokeWidth="2" opacity={1 - (frame % 55) / 55} />
              <text x="22" y="6" fontFamily="JetBrains Mono" fontSize="20" fontWeight="700" fill="#06202B">EAGLE FORD</text>
            </g>
          </svg>
        </div>

        {/* Right side data card */}
        <div style={{
          position: 'absolute',
          top: 580,
          right: 140,
          width: 620,
          opacity: cushionOp,
          transform: `translateX(${(1 - cushionOp) * 40}px)`,
          backgroundColor: '#06202B',
          padding: 44,
          borderLeft: '10px solid #FF0000',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#EEEEEE',
            letterSpacing: 4,
            opacity: 0.6,
            marginBottom: 14,
          }}>
            THE CUSHION =
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 76,
            color: '#EEEEEE',
            fontWeight: 900,
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            POLITICAL
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 76,
            color: '#EEEEEE',
            fontWeight: 900,
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            COMFORT.
          </div>
          <div style={{
            marginTop: 22,
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            color: '#EEEEEE',
            opacity: 0.85,
            lineHeight: 1.35,
          }}>
            Enough barrels = enough room to shrug off a hot CPI print.
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_HiddenPlaces;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13_CPIShrug',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_CPIShrug: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on impact at frame 110
  const shakeFrame = frame - 110;
  const shake = shakeFrame > 0 && shakeFrame < 18
    ? Math.sin(shakeFrame * 4) * (18 - shakeFrame) * 0.6
    : 0;

  const tagOp = sp(5);
  const chartOp = sp(25);
  const lineProgress = interpolate(frame, [30, 130], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const stampOp = sp(110, { damping: 6, stiffness: 220 });
  const verdictOp = sp(140);

  const shapeRot = frame * 0.12;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(255,0,0,0.06) 0%, rgba(238,238,238,0) 50%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: 100,
        right: 80,
        width: 400,
        height: 400,
        border: '2px solid #06202B',
        opacity: 0.05,
        transform: `rotate(${shapeRot}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 100,
        width: 300,
        height: 300,
        backgroundColor: '#06202B',
        opacity: 0.04,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
      }} />

      {/* Motion texture - diagonal lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={i} x1={-200 + i * 100} y1={0} x2={i * 100} y2={1080} stroke="#06202B" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 10) * 1920;
        const py = seededRandom(i + 60) * 1080;
        const drift = Math.cos(frame * 0.02 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: py + drift,
            width: 3,
            height: 3,
            backgroundColor: '#06202B',
            opacity: 0.22,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${shake}px, ${shake * 0.5}px)` }}>

        {/* Top tag */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 120,
          opacity: tagOp,
          transform: `translateY(${(1 - tagOp) * -20}px)`,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 28,
          color: '#06202B',
          letterSpacing: 6,
          fontWeight: 700,
        }}>
          CPI PRINT // MAY 2026 — HOT.
        </div>

        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 160,
          left: 120,
          right: 120,
          opacity: tagOp,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 110,
            lineHeight: 1,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -2,
          }}>
            A NUMBER THAT WOULD'VE
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 110,
            lineHeight: 1,
            color: '#000000',
            fontWeight: 900,
            letterSpacing: -2,
            marginTop: 6,
          }}>
            ENDED A HONEYMOON.
          </div>
        </div>

        {/* CPI Line Chart */}
        <div style={{
          position: 'absolute',
          top: 470,
          left: 120,
          width: 1100,
          height: 380,
          opacity: chartOp,
        }}>
          {/* Axis */}
          <svg width="1100" height="380" viewBox="0 0 1100 380">
            {/* grid */}
            {[0, 1, 2, 3, 4].map(i => (
              <line key={i} x1="60" y1={50 + i * 70} x2="1080" y2={50 + i * 70} stroke="#06202B" strokeWidth="1" opacity="0.15" />
            ))}
            {/* Y axis labels */}
            {['9%', '7%', '5%', '3%', '1%'].map((label, i) => (
              <text key={i} x="20" y={56 + i * 70} fontFamily="JetBrains Mono" fontSize="20" fill="#06202B" opacity="0.7">{label}</text>
            ))}
            {/* X axis */}
            <line x1="60" y1="330" x2="1080" y2="330" stroke="#06202B" strokeWidth="2"/>
            {['JAN', 'FEB', 'MAR', 'APR', 'MAY'].map((m, i) => (
              <text key={i} x={130 + i * 200} y="360" fontFamily="JetBrains Mono" fontSize="22" fill="#06202B" fontWeight="700">{m}</text>
            ))}

            {/* Line path (rising) */}
            <path
              d={`M 130 250 L 330 220 L 530 200 L 730 160 L 930 90`}
              fill="none"
              stroke="#FF0000"
              strokeWidth="6"
              strokeLinecap="round"
              strokeDasharray="900"
              strokeDashoffset={900 - 900 * lineProgress}
            />
            {/* Dots */}
            {[[130, 250], [330, 220], [530, 200], [730, 160], [930, 90]].map(([x, y], i) => {
              const dotShow = lineProgress > i / 5 ? 1 : 0;
              return (
                <g key={i} opacity={dotShow}>
                  <circle cx={x} cy={y} r="10" fill="#06202B"/>
                  <circle cx={x} cy={y} r="5" fill="#EEEEEE"/>
                </g>
              );
            })}
            {/* Last point highlight */}
            <circle cx="930" cy="90" r={12 + Math.sin(frame * 0.2) * 4} fill="none" stroke="#FF0000" strokeWidth="3" opacity={lineProgress > 0.95 ? 0.8 : 0}/>
            <text x="950" y="80" fontFamily="Mont Heavy" fontSize="38" fontWeight="900" fill="#FF0000" opacity={lineProgress > 0.95 ? 1 : 0}>
              HOT
            </text>
          </svg>
        </div>

        {/* Shrug stamp */}
        <div style={{
          position: 'absolute',
          top: 460,
          right: 160,
          opacity: stampOp,
          transform: `scale(${stampOp}) rotate(8deg)`,
          backgroundColor: '#06202B',
          color: '#EEEEEE',
          padding: '32px 48px',
          fontFamily: 'Mont Heavy, sans-serif',
          fontSize: 76,
          fontWeight: 900,
          letterSpacing: -1,
          boxShadow: '0 16px 50px rgba(6,32,43,0.4)',
        }}>
          BUT HE
          <div style={{ color: '#FF0000', fontSize: 90 }}>SHRUGGED.</div>
        </div>

        {/* Bottom verdict */}
        <div style={{
          position: 'absolute',
          bottom: 90,
          left: 120,
          right: 120,
          opacity: verdictOp,
          transform: `translateY(${(1 - verdictOp) * 20}px)`,
          fontFamily: 'Inter, sans-serif',
          fontSize: 38,
          color: '#06202B',
          fontWeight: 600,
          borderTop: '3px solid #06202B',
          paddingTop: 20,
        }}>
          Any other administration's honeymoon would be over —
          <span style={{ fontWeight: 800 }}> this one barely flinched.</span>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_CPIShrug;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14_DropComment',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_DropComment: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const pauseOp = sp(5);
  const headOp = sp(25);
  const boxOp = sp(60);
  const oneOp = sp(110, { damping: 6 });

  const shapeRot = frame * 0.18;
  const cursorBlink = Math.floor(frame / 18) % 2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #EEEEEE 0%, #DDDDDD 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: 200,
        left: 100,
        width: 0,
        height: 0,
        borderLeft: '180px solid transparent',
        borderRight: '180px solid transparent',
        borderBottom: '300px solid #06202B',
        opacity: 0.05,
        transform: `rotate(${shapeRot}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        top: 600,
        right: 100,
        width: 500,
        height: 500,
        backgroundColor: '#06202B',
        opacity: 0.04,
        borderRadius: 60,
        transform: `rotate(${-shapeRot * 0.6}deg)`,
      }} />

      {/* Motion texture - dots grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {Array.from({ length: 30 }).map((_, i) =>
          Array.from({ length: 18 }).map((_, j) => (
            <circle key={`${i}-${j}`} cx={i * 70} cy={j * 70} r="2" fill="#06202B" />
          ))
        )}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 5) * 1920;
        const py = seededRandom(i + 70) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 22;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + drift,
            top: py,
            width: 4,
            height: 4,
            backgroundColor: '#06202B',
            opacity: 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill>

        {/* PAUSE tag */}
        <div style={{
          position: 'absolute',
          top: 100,
          left: 120,
          opacity: pauseOp,
          transform: `translateX(${(1 - pauseOp) * -30}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <div style={{
            display: 'flex',
            gap: 6,
          }}>
            <div style={{ width: 10, height: 38, backgroundColor: '#FF0000' }} />
            <div style={{ width: 10, height: 38, backgroundColor: '#FF0000' }} />
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 28,
            color: '#06202B',
            letterSpacing: 6,
            fontWeight: 700,
          }}>
            PAUSE — ONE QUICK ASK
          </div>
        </div>

        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 220,
          left: 120,
          right: 120,
          opacity: headOp,
          transform: `translateY(${(1 - headOp) * 25}px)`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 119,
            lineHeight: 1,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -3,
          }}>
            DROP A COMMENT.
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 119,
            lineHeight: 1,
            color: '#000000',
            fontWeight: 900,
            letterSpacing: -3,
            marginTop: 8,
          }}>
            ONE WORD.
          </div>
        </div>

        {/* Comment box mock */}
        <div style={{
          position: 'absolute',
          top: 560,
          left: 120,
          width: 1000,
          opacity: boxOp,
          transform: `translateY(${(1 - boxOp) * 30}px)`,
          backgroundColor: '#FFFFFF',
          border: '3px solid #06202B',
          borderRadius: 16,
          padding: 36,
          boxShadow: '0 20px 50px rgba(6,32,43,0.15)',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#06202B',
            opacity: 0.6,
            marginBottom: 18,
            letterSpacing: 3,
          }}>
            ▾ ADD A COMMENT…
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 60,
            color: '#06202B',
            fontWeight: 700,
            display: 'flex',
            alignItems: 'center',
          }}>
            <span style={{ color: '#06202B', opacity: 0.4 }}>$</span>
            <span style={{ marginLeft: 16 }}>{frame > 130 ? '4.29' : ''}</span>
            <span style={{
              display: 'inline-block',
              width: 4,
              height: 60,
              backgroundColor: '#FF0000',
              marginLeft: 6,
              opacity: cursorBlink,
            }} />
          </div>
        </div>

        {/* Big ONE on right */}
        <div style={{
          position: 'absolute',
          top: 520,
          right: 140,
          opacity: oneOp,
          transform: `scale(${oneOp})`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 280,
            color: '#FF0000',
            fontWeight: 900,
            lineHeight: 0.85,
            letterSpacing: -8,
            textShadow: `0 0 ${30 + Math.sin(frame * 0.2) * 10}px rgba(255,0,0,0.25)`,
          }}>
            1
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 30,
            color: '#06202B',
            letterSpacing: 6,
            fontWeight: 700,
            textAlign: 'center',
            marginTop: -10,
          }}>
            WORD
          </div>
        </div>

        {/* Bottom instruction */}
        <div style={{
          position: 'absolute',
          bottom: 90,
          left: 120,
          right: 120,
          opacity: boxOp,
          fontFamily: 'Inter, sans-serif',
          fontSize: 36,
          color: '#06202B',
          fontWeight: 500,
          borderLeft: '6px solid #FF0000',
          paddingLeft: 22,
        }}>
          The price you paid for a gallon of gas this week —
          <span style={{ fontWeight: 800 }}> in your zip code.</span>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_DropComment;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15_RealMap',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_RealMap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tagOp = sp(5);
  const headOp = sp(20);
  const mapOp = sp(50);
  const verdictOp = sp(160);

  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.04]);
  const shapeRot = frame * 0.1;

  // Zip code price tags appearing one by one
  const zips = [
    { city: 'PHOENIX 85001', price: '$3.89', x: 280, y: 100, delay: 60 },
    { city: 'AUSTIN 78701', price: '$2.97', x: 540, y: 220, delay: 78 },
    { city: 'COLUMBUS 43215', price: '$3.45', x: 780, y: 80, delay: 96 },
    { city: 'MIAMI 33101', price: '$3.62', x: 900, y: 280, delay: 114 },
    { city: 'PORTLAND 97201', price: '$4.42', x: 80, y: 60, delay: 132 },
    { city: 'DENVER 80202', price: '$3.21', x: 400, y: 320, delay: 150 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 60%, rgba(6,32,43,0.08) 0%, rgba(238,238,238,0) 65%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: 50,
        right: 100,
        width: 420,
        height: 420,
        border: '3px solid #06202B',
        borderRadius: '50%',
        opacity: 0.05,
        transform: `rotate(${shapeRot}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: 50,
        left: 200,
        width: 360,
        height: 360,
        backgroundColor: '#06202B',
        opacity: 0.04,
        transform: `rotate(${-shapeRot * 0.7}deg)`,
      }} />

      {/* Motion texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <line key={i} x1={0} y1={40 + i * 45} x2={1920} y2={40 + i * 45} stroke="#06202B" strokeWidth="1" strokeDasharray="6 12" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 30) * 1920;
        const py = seededRandom(i + 90) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + drift,
            top: py,
            width: 3,
            height: 3,
            backgroundColor: '#06202B',
            opacity: 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center' }}>

        {/* Top tag */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 120,
          opacity: tagOp,
          transform: `translateX(${(1 - tagOp) * -25}px)`,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 26,
          color: '#06202B',
          letterSpacing: 5,
          fontWeight: 700,
        }}>
          ▸ THE MAP NOBODY DRAWS ON TV
        </div>

        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 140,
          left: 120,
          right: 120,
          opacity: headOp,
          transform: `translateY(${(1 - headOp) * 20}px)`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 102,
            lineHeight: 1,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -2,
          }}>
            THE <span style={{ backgroundColor: '#06202B', color: '#EEEEEE', padding: '0 16px' }}>REAL</span> MAP
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 102,
            lineHeight: 1,
            color: '#000000',
            fontWeight: 900,
            letterSpacing: -2,
            marginTop: 6,
          }}>
            IS ON YOUR RECEIPTS.
          </div>
        </div>

        {/* Map area with zip tags */}
        <div style={{
          position: 'absolute',
          top: 430,
          left: 120,
          width: 1680,
          height: 500,
          opacity: mapOp,
        }}>
          {/* US outline */}
          <svg width="1680" height="500" viewBox="0 0 1680 500" style={{ position: 'absolute', top: 0, left: 0 }}>
            <path
              d="M 80 130 Q 140 60, 320 70 L 580 60 Q 800 55, 1080 70 Q 1340 85, 1500 140 L 1500 220 Q 1480 300, 1420 350 L 1280 410 Q 1100 460, 880 460 Q 660 465, 460 440 L 260 410 Q 130 360, 80 270 Z"
              fill="#06202B"
              opacity="0.08"
            />
            <path
              d="M 80 130 Q 140 60, 320 70 L 580 60 Q 800 55, 1080 70 Q 1340 85, 1500 140 L 1500 220 Q 1480 300, 1420 350 L 1280 410 Q 1100 460, 880 460 Q 660 465, 460 440 L 260 410 Q 130 360, 80 270 Z"
              fill="none"
              stroke="#06202B"
              strokeWidth="3"
              strokeDasharray="10 6"
            />
          </svg>

          {/* Zip code price cards */}
          {zips.map((z, i) => {
            const op = sp(z.delay);
            return (
              <div key={i} style={{
                position: 'absolute',
                left: z.x,
                top: z.y,
                opacity: op,
                transform: `translateY(${(1 - op) * -20}px) scale(${op})`,
                backgroundColor: '#06202B',
                color: '#EEEEEE',
                padding: '14px 22px',
                borderRadius: 8,
                boxShadow: '0 8px 20px rgba(6,32,43,0.3)',
                borderLeft: '4px solid #FF0000',
              }}>
                <div style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 18,
                  opacity: 0.7,
                  letterSpacing: 2,
                }}>
                  {z.city}
                </div>
                <div style={{
                  fontFamily: 'Mont Heavy, sans-serif',
                  fontSize: 51,
                  fontWeight: 900,
                  lineHeight: 1,
                  marginTop: 4,
                }}>
                  {z.price}
                </div>
              </div>
            );
          })}

          {/* Pulse dot connecting */}
          {zips.map((z, i) => (
            <div key={`d${i}`} style={{
              position: 'absolute',
              left: z.x + 10,
              top: z.y + 95,
              width: 14,
              height: 14,
              backgroundColor: '#FF0000',
              borderRadius: '50%',
              opacity: sp(z.delay - 5),
              boxShadow: `0 0 ${15 + Math.sin(frame * 0.25 + i) * 10}px #FF0000`,
            }} />
          ))}
        </div>

        {/* Bottom verdict */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: 120,
          right: 120,
          opacity: verdictOp,
          transform: `translateY(${(1 - verdictOp) * 15}px)`,
          fontFamily: 'Inter, sans-serif',
          fontSize: 36,
          color: '#06202B',
          fontWeight: 600,
          textAlign: 'center',
        }}>
          Not the one talking heads draw — <span style={{ fontWeight: 800, color: '#000000' }}>the one written on your receipts.</span>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_RealMap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16_ReceiptTruth',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_ReceiptTruth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split screen: TV (left fake) vs Receipt (right real)
  const tvOp = sp(5);
  const vsOp = sp(45);
  const receiptOp = sp(75);
  const tagOp = sp(150);

  const tvFlicker = 0.85 + Math.sin(frame * 0.5) * 0.15;
  const shapeRot = frame * 0.13;

  // Receipt items
  const items = [
    { name: 'EGGS DOZEN', price: '6.49', delay: 90 },
    { name: 'GROUND BEEF', price: '8.99', delay: 100 },
    { name: 'GAS 1 GAL', price: '3.89', delay: 110 },
    { name: 'CHILDCARE/DAY', price: '92.00', delay: 120 },
    { name: 'RENT (1/30)', price: '69.50', delay: 130 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(6,32,43,0.06) 0%, rgba(238,238,238,0) 50%, rgba(6,32,43,0.06) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: 100,
        left: 50,
        width: 300,
        height: 300,
        backgroundColor: '#06202B',
        opacity: 0.04,
        borderRadius: '50%',
        transform: `rotate(${shapeRot}deg) scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: 100,
        right: 80,
        width: 400,
        height: 400,
        border: '3px solid #06202B',
        opacity: 0.05,
        transform: `rotate(${-shapeRot}deg)`,
      }} />

      {/* Motion texture - vertical static lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <line key={i} x1={i * 50} y1={0} x2={i * 50} y2={1080} stroke="#06202B" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 20) * 1920;
        const py = seededRandom(i + 80) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + Math.sin(frame * 0.02 + i) * 20,
            top: py,
            width: 3,
            height: 3,
            backgroundColor: '#06202B',
            opacity: 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill>

        {/* TOP TITLE */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: tvOp,
          transform: `translateY(${(1 - tvOp) * -20}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#06202B',
            letterSpacing: 6,
            fontWeight: 700,
            opacity: 0.7,
          }}>
            ━━━━ TWO MAPS, ONE COUNTRY ━━━━
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 85,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -2,
            marginTop: 14,
          }}>
            TV STORY <span style={{ color: '#FF0000' }}>vs</span> RECEIPT TRUTH
          </div>
        </div>

        {/* LEFT - TV SCREEN (fake) */}
        <div style={{
          position: 'absolute',
          top: 290,
          left: 100,
          width: 760,
          height: 580,
          opacity: tvOp,
          transform: `translateX(${(1 - tvOp) * -30}px)`,
        }}>
          {/* TV frame */}
          <div style={{
            backgroundColor: '#06202B',
            padding: 18,
            borderRadius: 12,
            border: '6px solid #06202B',
            height: '100%',
            position: 'relative',
            boxShadow: '0 20px 50px rgba(6,32,43,0.3)',
          }}>
            {/* TV screen */}
            <div style={{
              backgroundColor: '#000000',
              height: '100%',
              borderRadius: 6,
              padding: 30,
              position: 'relative',
              overflow: 'hidden',
              opacity: tvFlicker,
            }}>
              {/* Scan lines */}
              <div style={{
                position: 'absolute',
                top: 0, left: 0, right: 0, bottom: 0,
                background: 'repeating-linear-gradient(0deg, transparent, transparent 3px, rgba(255,255,255,0.04) 3px, rgba(255,255,255,0.04) 4px)',
                pointerEvents: 'none',
              }} />
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 20,
                color: '#FF0000',
                letterSpacing: 3,
                marginBottom: 14,
              }}>
                ▸ LIVE • TALKING HEADS
              </div>
              <div style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 60,
                color: '#EEEEEE',
                fontWeight: 900,
                lineHeight: 1.05,
                marginBottom: 26,
              }}>
                "INFLATION IS PLUMMETING"
              </div>
              <div style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 30,
                color: '#EEEEEE',
                opacity: 0.7,
                lineHeight: 1.3,
              }}>
                "Everything is under control."
              </div>
              <div style={{
                position: 'absolute',
                bottom: 30,
                left: 30,
                right: 30,
                borderTop: '2px solid #FF0000',
                paddingTop: 14,
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                color: '#FF0000',
                opacity: 0.6 + Math.sin(frame * 0.3) * 0.3,
              }}>
                ◉ BREAKING — UNVERIFIED
              </div>
            </div>
          </div>
        </div>

        {/* VS divider */}
        <div style={{
          position: 'absolute',
          top: 530,
          left: 880,
          opacity: vsOp,
          transform: `scale(${vsOp})`,
          fontFamily: 'Mont Heavy, sans-serif',
          fontSize: 100,
          color: '#FF0000',
          fontWeight: 900,
          letterSpacing: -2,
          width: 160,
          textAlign: 'center',
        }}>
          VS
        </div>

        {/* RIGHT - RECEIPT (real) */}
        <div style={{
          position: 'absolute',
          top: 290,
          right: 120,
          width: 700,
          opacity: receiptOp,
          transform: `translateX(${(1 - receiptOp) * 30}px) rotate(-1.5deg)`,
          backgroundColor: '#FFFFFF',
          padding: '36px 44px',
          boxShadow: '0 30px 60px rgba(6,32,43,0.3)',
          fontFamily: 'JetBrains Mono, monospace',
          color: '#06202B',
        }}>
          {/* Top zigzag */}
          <div style={{ fontSize: 18, letterSpacing: 2, marginBottom: 16, opacity: 0.6 }}>
            ▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼▼
          </div>
          <div style={{ fontFamily: 'Mont Heavy, sans-serif', fontSize: 42, fontWeight: 900, marginBottom: 8 }}>
            REAL ECONOMY
          </div>
          <div style={{ fontSize: 20, opacity: 0.6, marginBottom: 24 }}>
            ZIP 00000 • 12 / JUN / 2026
          </div>

          {/* Items */}
          {items.map((it, i) => {
            const op = sp(it.delay);
            return (
              <div key={i} style={{
                opacity: op,
                transform: `translateX(${(1 - op) * 20}px)`,
                display: 'flex',
                justifyContent: 'space-between',
                fontSize: 30,
                fontWeight: 700,
                padding: '8px 0',
                borderBottom: '1px dashed #06202B',
              }}>
                <span>{it.name}</span>
                <span style={{ color: '#000000' }}>${it.price}</span>
              </div>
            );
          })}

          <div style={{
            marginTop: 20,
            display: 'flex',
            justifyContent: 'space-between',
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 51,
            fontWeight: 900,
          }}>
            <span>TOTAL</span>
            <span style={{ color: '#FF0000' }}>HURTS.</span>
          </div>

          <div style={{ fontSize: 18, letterSpacing: 2, marginTop: 16, opacity: 0.6 }}>
            ▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲
          </div>
        </div>

        {/* Bottom tag */}
        <div style={{
          position: 'absolute',
          bottom: 50,
          left: 120,
          right: 120,
          opacity: tagOp,
          fontFamily: 'Inter, sans-serif',
          fontSize: 32,
          color: '#06202B',
          fontWeight: 600,
          textAlign: 'center',
          borderTop: '2px solid #06202B',
          paddingTop: 16,
        }}>
          The real one is <span style={{ fontWeight: 800 }}>written on your receipts</span> — not on a teleprompter.
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_ReceiptTruth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17_UnpackThriller',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_UnpackThriller: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tagOp = sp(5);
  const titleOp = sp(20);
  const posterOp = sp(60);
  const elementsOp = sp(100);

  const camTilt = Math.sin(frame * 0.02) * 0.5;
  const shapeRot = frame * 0.15;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(6,32,43,0.1) 0%, rgba(238,238,238,0) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: 120,
        left: -100,
        width: 500,
        height: 500,
        backgroundColor: '#06202B',
        opacity: 0.05,
        borderRadius: 40,
        transform: `rotate(${shapeRot}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: 50,
        right: -120,
        width: 600,
        height: 600,
        border: '3px solid #06202B',
        borderRadius: '50%',
        opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
      }} />

      {/* Motion texture - radiating lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 36 }).map((_, i) => {
          const a = (i / 36) * Math.PI * 2;
          return (
            <line
              key={i}
              x1={960}
              y1={540}
              x2={960 + Math.cos(a) * 1100}
              y2={540 + Math.sin(a) * 1100}
              stroke="#06202B"
              strokeWidth="1"
              strokeDasharray="20 30"
            />
          );
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 40) * 1920;
        const py = seededRandom(i + 100) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: py + Math.sin(frame * 0.025 + i) * 20,
            width: 4,
            height: 4,
            backgroundColor: '#06202B',
            opacity: 0.22,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camTilt}deg)` }}>

        {/* Tag */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 120,
          opacity: tagOp,
          transform: `translateY(${(1 - tagOp) * -20}px)`,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 28,
          color: '#06202B',
          letterSpacing: 6,
          fontWeight: 700,
        }}>
          ▸ UNPACKING THE GEOPOLITICS
        </div>

        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 170,
          left: 120,
          right: 700,
          opacity: titleOp,
          transform: `translateY(${(1 - titleOp) * 25}px)`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 110,
            lineHeight: 1,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -3,
          }}>
            WILDER THAN
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 110,
            lineHeight: 1,
            color: '#000000',
            fontWeight: 900,
            letterSpacing: -3,
            marginTop: 6,
          }}>
            ANY THRILLER.
          </div>

          {/* Subline */}
          <div style={{
            marginTop: 32,
            fontFamily: 'Inter, sans-serif',
            fontSize: 38,
            color: '#06202B',
            fontWeight: 500,
            lineHeight: 1.3,
            maxWidth: 900,
          }}>
            What's <span style={{ fontWeight: 800 }}>actually happening</span> behind the soundbite — bigger than anything Netflix is releasing this year.
          </div>
        </div>

        {/* Right side - "poster" style geopolitics card */}
        <div style={{
          position: 'absolute',
          top: 180,
          right: 120,
          width: 540,
          height: 720,
          opacity: posterOp,
          transform: `translateY(${(1 - posterOp) * 30}px) rotate(2.5deg)`,
          backgroundColor: '#06202B',
          padding: 36,
          boxShadow: '0 30px 60px rgba(6,32,43,0.35)',
          display: 'flex',
          flexDirection: 'column',
        }}>
          {/* Top label */}
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 18,
            color: '#FF0000',
            letterSpacing: 4,
            marginBottom: 12,
          }}>
            ━━ FILE NO. 06-12 ━━
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 60,
            color: '#EEEEEE',
            fontWeight: 900,
            lineHeight: 0.95,
            letterSpacing: -2,
            marginBottom: 24,
          }}>
            THE OIL
            <br/>
            DOMINANCE
            <br/>
            <span style={{ color: '#FF0000' }}>PLAY.</span>
          </div>

          {/* Visual cluster */}
          <div style={{ flex: 1, position: 'relative' }}>
            <svg width="100%" height="100%" viewBox="0 0 460 380" style={{ opacity: elementsOp }}>
              {/* Globe outline */}
              <circle cx="230" cy="190" r="140" fill="none" stroke="#EEEEEE" strokeWidth="2" opacity="0.4"/>
              <ellipse cx="230" cy="190" rx="140" ry="50" fill="none" stroke="#EEEEEE" strokeWidth="1" opacity="0.3"/>
              <ellipse cx="230" cy="190" rx="50" ry="140" fill="none" stroke="#EEEEEE" strokeWidth="1" opacity="0.3"/>
              <ellipse cx="230" cy="190" rx="100" ry="140" fill="none" stroke="#EEEEEE" strokeWidth="1" opacity="0.2"/>

              {/* Arrows / connections */}
              <path d="M 80 80 Q 200 150, 230 190" stroke="#FF0000" strokeWidth="3" fill="none" strokeDasharray="6 4"/>
              <path d="M 400 100 Q 320 160, 240 200" stroke="#FF0000" strokeWidth="3" fill="none" strokeDasharray="6 4"/>
              <path d="M 100 320 Q 180 260, 230 200" stroke="#FF0000" strokeWidth="3" fill="none" strokeDasharray="6 4"/>

              {/* Dots */}
              <circle cx="80" cy="80" r="10" fill="#FF0000"/>
              <circle cx="400" cy="100" r="10" fill="#FF0000"/>
              <circle cx="100" cy="320" r="10" fill="#FF0000"/>
              <circle cx="230" cy="190" r="14" fill="#EEEEEE"/>

              {/* Labels */}
              <text x="20" y="70" fontFamily="JetBrains Mono" fontSize="16" fill="#EEEEEE">IRAN</text>
              <text x="370" y="92" fontFamily="JetBrains Mono" fontSize="16" fill="#EEEEEE">OPEC</text>
              <text x="40" y="345" fontFamily="JetBrains Mono" fontSize="16" fill="#EEEEEE">HORMUZ</text>
              <text x="195" y="225" fontFamily="JetBrains Mono" fontSize="18" fill="#EEEEEE" fontWeight="700">U.S.</text>
            </svg>
          </div>

          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 18,
            color: '#EEEEEE',
            opacity: 0.6,
            letterSpacing: 3,
            textAlign: 'center',
            marginTop: 12,
          }}>
            ▌ A 6-MOVE CHESS GAME ▌
          </div>
        </div>

        {/* Bottom label strip */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 120,
          right: 700,
          opacity: elementsOp,
          padding: '22px 28px',
          borderTop: '4px solid #06202B',
          borderBottom: '4px solid #06202B',
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 26,
          color: '#06202B',
          letterSpacing: 4,
          fontWeight: 700,
        }}>
          ◢ SOUNDBITE → STRATEGY → SETTLEMENT ◣
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_UnpackThriller;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18_TwoYeses',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_TwoYeses: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tagOp = sp(5);
  const titleOp = sp(20);
  const yes1 = sp(55, { damping: 8 });
  const yes2 = sp(110, { damping: 8 });
  const closerOp = sp(165);

  const shapeRot = frame * 0.11;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #EEEEEE 0%, #E2E5E7 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: -100,
        right: 200,
        width: 0,
        height: 0,
        borderLeft: '220px solid transparent',
        borderRight: '220px solid transparent',
        borderBottom: '380px solid #06202B',
        opacity: 0.05,
        transform: `rotate(${shapeRot}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: 100,
        left: 60,
        width: 400,
        height: 400,
        border: '3px solid #06202B',
        opacity: 0.06,
        transform: `rotate(${-shapeRot * 0.8}deg)`,
        borderRadius: 30,
      }} />

      {/* Motion texture - grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 100} y1={0} x2={i * 100} y2={1080} stroke="#06202B" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 33) * 1920;
        const py = seededRandom(i + 77) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + Math.sin(frame * 0.02 + i) * 20,
            top: py,
            width: 3,
            height: 3,
            backgroundColor: '#06202B',
            opacity: 0.22,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill>

        {/* Tag */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 120,
          opacity: tagOp,
          transform: `translateY(${(1 - tagOp) * -20}px)`,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 26,
          color: '#06202B',
          letterSpacing: 6,
          fontWeight: 700,
        }}>
          THE PRESIDENT'S ARGUMENT // BOILED DOWN
        </div>

        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 160,
          left: 120,
          right: 120,
          opacity: titleOp,
          transform: `translateY(${(1 - titleOp) * 20}px)`,
          fontFamily: 'Mont Heavy, sans-serif',
          fontSize: 102,
          color: '#06202B',
          fontWeight: 900,
          letterSpacing: -2,
          lineHeight: 1,
        }}>
          IT GOES LIKE THIS →
        </div>

        {/* Two Yes cards stacked */}
        <div style={{
          position: 'absolute',
          top: 350,
          left: 120,
          right: 120,
          display: 'flex',
          flexDirection: 'column',
          gap: 30,
        }}>

          {/* YES 1 */}
          <div style={{
            opacity: yes1,
            transform: `translateX(${(1 - yes1) * -50}px)`,
            backgroundColor: '#06202B',
            padding: '36px 50px',
            display: 'flex',
            alignItems: 'center',
            gap: 40,
            boxShadow: '0 16px 40px rgba(6,32,43,0.25)',
            borderRight: '12px solid #FF0000',
          }}>
            <div style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 136,
              color: '#FF0000',
              fontWeight: 900,
              lineHeight: 0.85,
              letterSpacing: -4,
              minWidth: 280,
            }}>
              YES,
            </div>
            <div>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                color: '#EEEEEE',
                opacity: 0.6,
                letterSpacing: 4,
                marginBottom: 8,
              }}>
                ADMISSION #1
              </div>
              <div style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 76,
                color: '#EEEEEE',
                fontWeight: 900,
                lineHeight: 1,
                letterSpacing: -1,
              }}>
                PRICES ARE RISING.
              </div>
            </div>
          </div>

          {/* YES 2 */}
          <div style={{
            opacity: yes2,
            transform: `translateX(${(1 - yes2) * 50}px)`,
            backgroundColor: '#000000',
            padding: '36px 50px',
            display: 'flex',
            alignItems: 'center',
            gap: 40,
            boxShadow: '0 16px 40px rgba(0,0,0,0.3)',
            borderLeft: '12px solid #FF0000',
          }}>
            <div style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 136,
              color: '#FF0000',
              fontWeight: 900,
              lineHeight: 0.85,
              letterSpacing: -4,
              minWidth: 280,
            }}>
              YES,
            </div>
            <div>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                color: '#EEEEEE',
                opacity: 0.6,
                letterSpacing: 4,
                marginBottom: 8,
              }}>
                ADMISSION #2
              </div>
              <div style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 64,
                color: '#EEEEEE',
                fontWeight: 900,
                lineHeight: 1.05,
                letterSpacing: -1,
              }}>
                HEADLINE INFLATION IS
                <span style={{ color: '#FF0000' }}> UNCOMFORTABLE.</span>
              </div>
            </div>
          </div>

        </div>

        {/* Closer */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 120,
          right: 120,
          opacity: closerOp,
          transform: `translateY(${(1 - closerOp) * 20}px)`,
          fontFamily: 'Inter, sans-serif',
          fontSize: 36,
          color: '#06202B',
          fontWeight: 600,
          textAlign: 'center',
        }}>
          He admits the obvious — then pivots to a much bigger claim.
          <span style={{ fontWeight: 800 }}>  Watch the pivot.</span>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_TwoYeses;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19_EnergyDominance',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_EnergyDominance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tagOp = sp(5);
  const titleOp = sp(25);
  const barsOp = sp(55);
  const verdictOp = sp(160);

  // Animated bars (US dominant)
  const usBarH = interpolate(frame, [60, 130], [0, 360], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const saBarH = interpolate(frame, [75, 135], [0, 220], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const ruBarH = interpolate(frame, [90, 140], [0, 195], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const shapeRot = frame * 0.1;
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, rgba(6,32,43,0.1) 0%, rgba(238,238,238,0) 70%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: 200,
        right: -100,
        width: 600,
        height: 600,
        borderRadius: '50%',
        backgroundColor: '#06202B',
        opacity: 0.04,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: -50,
        left: 100,
        width: 380,
        height: 380,
        border: '3px solid #06202B',
        borderRadius: 20,
        opacity: 0.06,
        transform: `rotate(${shapeRot}deg)`,
      }} />

      {/* Motion texture - horizontal grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 15 }).map((_, i) => (
          <line key={i} x1={0} y1={50 + i * 70} x2={1920} y2={50 + i * 70} stroke="#06202B" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 60) * 1920;
        const py = seededRandom(i + 120) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: py + Math.sin(frame * 0.02 + i) * 18,
            width: 3,
            height: 3,
            backgroundColor: '#06202B',
            opacity: 0.22,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center' }}>

        {/* Tag */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 120,
          opacity: tagOp,
          transform: `translateY(${(1 - tagOp) * -20}px)`,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 26,
          color: '#06202B',
          letterSpacing: 6,
          fontWeight: 700,
        }}>
          ▸ STRATEGIC ENERGY POSITION
        </div>

        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 160,
          left: 120,
          right: 120,
          opacity: titleOp,
          transform: `translateY(${(1 - titleOp) * 20}px)`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 110,
            lineHeight: 1,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -3,
          }}>
            SO DOMINANT —
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 110,
            lineHeight: 1,
            color: '#000000',
            fontWeight: 900,
            letterSpacing: -3,
            marginTop: 6,
          }}>
            WE CAN <span style={{ color: '#FF0000' }}>ABSORB</span> THE HEAT.
          </div>
        </div>

        {/* Bar chart - crude oil production comparison */}
        <div style={{
          position: 'absolute',
          top: 510,
          left: 120,
          width: 900,
          height: 460,
          opacity: barsOp,
        }}>
          {/* Title */}
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 20,
            color: '#06202B',
            letterSpacing: 3,
            marginBottom: 18,
            opacity: 0.7,
          }}>
            CRUDE PRODUCTION • MILLION BBL/DAY
          </div>

          {/* Bars container */}
          <div style={{
            display: 'flex',
            alignItems: 'flex-end',
            gap: 70,
            height: 360,
            borderBottom: '4px solid #06202B',
            paddingBottom: 0,
            position: 'relative',
          }}>
            {/* US Bar */}
            <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
              <div style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 47,
                color: '#FF0000',
                fontWeight: 900,
              }}>
                13.4
              </div>
              <div style={{
                width: 160,
                height: usBarH,
                backgroundColor: '#FF0000',
                position: 'relative',
                boxShadow: '0 0 30px rgba(255,0,0,0.3)',
              }}>
                <div style={{
                  position: 'absolute',
                  top: 12,
                  left: 0,
                  right: 0,
                  textAlign: 'center',
                  fontFamily: 'Mont Heavy, sans-serif',
                  fontSize: 38,
                  color: '#EEEEEE',
                  fontWeight: 900,
                }}>
                  USA
                </div>
              </div>
            </div>

            {/* Saudi Arabia Bar */}
            <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
              <div style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 38,
                color: '#06202B',
                fontWeight: 900,
              }}>
                9.1
              </div>
              <div style={{
                width: 160,
                height: saBarH,
                backgroundColor: '#06202B',
                position: 'relative',
              }}>
                <div style={{
                  position: 'absolute',
                  top: 12,
                  left: 0,
                  right: 0,
                  textAlign: 'center',
                  fontFamily: 'Mont Heavy, sans-serif',
                  fontSize: 26,
                  color: '#EEEEEE',
                  fontWeight: 900,
                }}>
                  SAUDI
                </div>
              </div>
            </div>

            {/* Russia Bar */}
            <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
              <div style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 38,
                color: '#06202B',
                fontWeight: 900,
              }}>
                8.7
              </div>
              <div style={{
                width: 160,
                height: ruBarH,
                backgroundColor: '#06202B',
                opacity: 0.8,
                position: 'relative',
              }}>
                <div style={{
                  position: 'absolute',
                  top: 12,
                  left: 0,
                  right: 0,
                  textAlign: 'center',
                  fontFamily: 'Mont Heavy, sans-serif',
                  fontSize: 24,
                  color: '#EEEEEE',
                  fontWeight: 900,
                }}>
                  RUSSIA
                </div>
              </div>
            </div>

            {/* Crown over US */}
            <div style={{
              position: 'absolute',
              top: -50,
              left: 50,
              opacity: barsOp > 0.9 ? 1 : 0,
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#FF0000',
              fontWeight: 800,
              letterSpacing: 3,
            }}>
              ▲ #1 IN THE WORLD
            </div>
          </div>
        </div>

        {/* Right side stat block */}
        <div style={{
          position: 'absolute',
          top: 520,
          right: 130,
          width: 600,
          opacity: barsOp,
        }}>
          <div style={{
            backgroundColor: '#06202B',
            padding: 40,
            borderLeft: '10px solid #FF0000',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#EEEEEE',
              opacity: 0.6,
              letterSpacing: 4,
              marginBottom: 16,
            }}>
              THE CUSHION =
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 119,
              color: '#EEEEEE',
              fontWeight: 900,
              lineHeight: 0.9,
              letterSpacing: -3,
            }}>
              13.4M
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 30,
              color: '#EEEEEE',
              opacity: 0.85,
              marginTop: 14,
              lineHeight: 1.3,
            }}>
              Barrels per day — enough room to absorb shocks rivals would crumble under.
            </div>
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_EnergyDominance;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20_GoodInflation',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20_GoodInflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tagOp = sp(5);
  const setupOp = sp(20);
  const goodOp = sp(80, { damping: 7, stiffness: 140 });
  const stampOp = sp(130, { damping: 6, stiffness: 220 });
  const subOp = sp(160);

  const shapeRot = frame * 0.13;

  // Highlighter sweep on "GOOD"
  const sweep = interpolate(frame, [85, 130], [0, 100], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Camera slow tilt
  const camTilt = Math.sin(frame * 0.015) * 0.4;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 50%, rgba(6,32,43,0.08) 0%, rgba(238,238,238,0) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: 80,
        right: 80,
        width: 500,
        height: 500,
        borderRadius: '50%',
        border: '3px solid #06202B',
        opacity: 0.05,
        transform: `rotate(${shapeRot}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: 50,
        left: -80,
        width: 0,
        height: 0,
        borderLeft: '200px solid transparent',
        borderRight: '200px solid transparent',
        borderBottom: '340px solid #06202B',
        opacity: 0.05,
        transform: `rotate(${-shapeRot}deg)`,
      }} />

      {/* Motion texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <circle key={i} cx={1920 / 2} cy={1080 / 2} r={50 + i * 50} fill="none" stroke="#06202B" strokeWidth="1" strokeDasharray="3 12" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 88) * 1920;
        const py = seededRandom(i + 144) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + Math.sin(frame * 0.025 + i) * 20,
            top: py,
            width: 4,
            height: 4,
            backgroundColor: '#06202B',
            opacity: 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camTilt}deg)` }}>

        {/* Top tag */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 120,
          opacity: tagOp,
          transform: `translateX(${(1 - tagOp) * -25}px)`,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 28,
          color: '#06202B',
          letterSpacing: 6,
          fontWeight: 700,
        }}>
          ▸ HIS REAL ARGUMENT
        </div>

        {/* Setup line */}
        <div style={{
          position: 'absolute',
          top: 200,
          left: 120,
          right: 120,
          opacity: setupOp,
          transform: `translateY(${(1 - setupOp) * 20}px)`,
          fontFamily: 'Inter, sans-serif',
          fontSize: 51,
          color: '#06202B',
          fontWeight: 600,
          lineHeight: 1.25,
          maxWidth: 1500,
        }}>
          He says he loves it — because the inflation we're seeing,
          <br/>he claims, is something he calls...
        </div>

        {/* GOOD INFLATION big reveal */}
        <div style={{
          position: 'absolute',
          top: 440,
          left: 120,
          right: 120,
          opacity: goodOp,
          transform: `scale(${goodOp})`,
          transformOrigin: 'left center',
        }}>
          <div style={{ display: 'flex', alignItems: 'baseline', gap: 26, flexWrap: 'wrap' }}>
            {/* GOOD with highlighter */}
            <div style={{ position: 'relative', display: 'inline-block' }}>
              <div style={{
                position: 'absolute',
                top: '32%',
                left: 0,
                height: '50%',
                width: `${sweep}%`,
                backgroundColor: '#FFC107',
                zIndex: 0,
              }} />
              <span style={{
                position: 'relative',
                zIndex: 1,
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 221,
                color: '#06202B',
                fontWeight: 900,
                letterSpacing: -6,
                lineHeight: 1,
              }}>
                GOOD
              </span>
            </div>
            <span style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 187,
              color: '#000000',
              fontWeight: 900,
              letterSpacing: -5,
              lineHeight: 1,
            }}>
              INFLATION.
            </span>
          </div>

          {/* Quote marks */}
          <div style={{
            position: 'absolute',
            top: -70,
            left: -20,
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 170,
            color: '#06202B',
            opacity: 0.25,
            lineHeight: 1,
          }}>
            "
          </div>
        </div>

        {/* Skeptical stamp */}
        <div style={{
          position: 'absolute',
          top: 460,
          right: 140,
          opacity: stampOp,
          transform: `scale(${stampOp}) rotate(10deg)`,
          border: '5px dashed #FF0000',
          padding: '20px 36px',
          fontFamily: 'Mont Heavy, sans-serif',
          fontSize: 51,
          color: '#FF0000',
          fontWeight: 900,
          letterSpacing: 2,
        }}>
          REALLY?
        </div>

        {/* Bottom translation */}
        <div style={{
          position: 'absolute',
          bottom: 90,
          left: 120,
          right: 120,
          opacity: subOp,
          transform: `translateY(${(1 - subOp) * 20}px)`,
          backgroundColor: '#06202B',
          padding: '24px 36px',
          borderLeft: '8px solid #FFC107',
          display: 'flex',
          alignItems: 'center',
          gap: 30,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#FFC107',
            letterSpacing: 4,
            fontWeight: 700,
            minWidth: 200,
          }}>
            TRANSLATION →
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 34,
            color: '#EEEEEE',
            fontWeight: 500,
            lineHeight: 1.3,
          }}>
            The kind that comes from <span style={{ fontWeight: 800, color: '#FFC107' }}>growth, tariffs, and reshoring</span> — not weakness. (Or so the pitch goes.)
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene20_GoodInflation;
```

