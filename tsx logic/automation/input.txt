```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11Squeeze',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const RECEIPTS = [
  { label: 'COFFEE', old: '$4.20', now: '$4.85' },
  { label: 'GAS', old: '$52', now: '$61' },
  { label: 'GROCERIES', old: '$118', now: '$139' },
  { label: 'INSURANCE', old: '$210', now: '$246' },
  { label: 'STREAMING', old: '$14', now: '$18' },
  { label: 'RENT', old: '$1,650', now: '$1,840' },
];

const Scene11Squeeze: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow camera push-in for cinematic depth
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.07], { easing: Easing.inOut(Easing.ease) });

  const payoffIn = sp(430);

  return (
    <AbsoluteFill style={{ backgroundColor: '#8F4712', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background (darker orange theme) */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${50 + Math.sin(frame / 60) * 8}% 38%, #C96A24 0%, #9E5116 48%, #6E3509 100%)`,
      }} />

      {/* Layer 2: Animated transparent shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '3px solid rgba(255,244,73,0.06)', left: -260, top: -300,
        transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame / 70) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 620, height: 620,
        background: 'rgba(37,52,63,0.06)', right: -180, bottom: -220,
        transform: `rotate(${12 + frame * 0.03}deg)`,
      }} />

      {/* Layer 3: Motion texture — drifting diagonal lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(115deg, rgba(255,255,255,0.035) 0px, rgba(255,255,255,0.035) 2px, transparent 2px, transparent 90px)',
        backgroundPosition: `${frame * 0.6}px 0px`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const x = seededRandom(i + 1) * 1920;
        const drift = (frame * (0.25 + seededRandom(i + 40) * 0.5)) % 1180;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1100 - drift,
            width: 5 + seededRandom(i + 9) * 6, height: 5 + seededRandom(i + 9) * 6,
            borderRadius: '50%', backgroundColor: 'rgba(255,244,73,0.16)',
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, alignItems: 'center', justifyContent: 'center' }}>

        {/* Headline block — typographic squeeze */}
        <div style={{ textAlign: 'center', marginTop: -270 }}>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 110, fontWeight: 900,
            color: '#25343F', letterSpacing: -3, lineHeight: 0.95,
            transform: `translateY(${(1 - sp(5)) * 80}px) scaleY(${interpolate(sp(5), [0, 1], [1.6, 1])})`,
            opacity: sp(5),
          }}>
            THE MIDDLE CLASS
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 150, fontWeight: 900,
            color: '#FFF449', letterSpacing: -4,
            transform: `scaleX(${interpolate(sp(25), [0, 1], [1.4, 1])})`,
            opacity: sp(25),
            textShadow: '0 12px 40px rgba(0,0,0,0.35)',
          }}>
            SQUEEZE
          </div>
        </div>

        {/* Receipt cards — hundreds of ordinary transactions */}
        <div style={{ display: 'flex', gap: 26, marginTop: 60 }}>
          {RECEIPTS.map((r, i) => {
            const s = sp(90 + i * 18);
            const wobble = Math.sin((frame + i * 30) / 22) * 2.2;
            return (
              <div key={i} style={{
                width: 220, padding: '24px 18px', backgroundColor: '#FFF6EA',
                borderRadius: 10, textAlign: 'center',
                transform: `translateY(${(1 - s) * 220}px) rotate(${wobble}deg)`,
                opacity: s, boxShadow: '0 18px 40px rgba(37,52,63,0.35)',
                borderBottom: '10px solid #25343F',
              }}>
                <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 800, color: '#25343F', letterSpacing: 2 }}>{r.label}</div>
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#9E9E9E', textDecoration: 'line-through', marginTop: 12 }}>{r.old}</div>
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 44, fontWeight: 800, color: '#B33A00', marginTop: 4 }}>{r.now}</div>
              </div>
            );
          })}
        </div>

        {/* Payoff strip — Oil demonstrates the danger */}
        <div style={{
          position: 'absolute', bottom: 120, padding: '26px 70px',
          backgroundColor: '#25343F', borderRadius: 14,
          transform: `scale(${payoffIn})`, opacity: payoffIn,
          boxShadow: '0 20px 60px rgba(0,0,0,0.4)',
        }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 62, fontWeight: 900, color: '#FFF449', letterSpacing: 1 }}>
            OIL DEMONSTRATES THE DANGER
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 52%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene11Squeeze;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12OilChart',
  durationInSeconds: 22,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12OilChart: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Line draw progress for the price path
  const pathLen = 2400;
  const drawn = interpolate(frame, [50, 300], [pathLen, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  // Camera pans slightly right as the chart draws
  const camPan = interpolate(frame, [0, durationInFrames], [0, -40]);

  const p100 = sp(105);
  const p70 = sp(210);
  const p80 = sp(290);
  const brent = sp(360);
  const payoff = sp(480);

  return (
    <AbsoluteFill style={{ backgroundColor: '#7A3B0C', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${160 + Math.sin(frame / 90) * 10}deg, #B85C1F 0%, #8F4712 55%, #5E2D07 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1100, height: 1100, borderRadius: '50%',
        background: 'rgba(255,155,81,0.07)', left: -420, bottom: -560,
        transform: `scale(${1 + Math.sin(frame / 80) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 0, height: 0, right: 90, top: -80,
        borderLeft: '260px solid transparent', borderRight: '260px solid transparent',
        borderBottom: '440px solid rgba(37,52,63,0.05)',
        transform: `rotate(${frame * 0.04}deg)`,
      }} />

      {/* Layer 3: Motion texture — chart grid */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(255,255,255,0.045) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.045) 1px, transparent 1px)',
        backgroundSize: '110px 110px',
        backgroundPosition: `${-frame * 0.35}px 0px`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = seededRandom(i + 3) * 1920;
        const drift = (frame * (0.3 + seededRandom(i + 21) * 0.4)) % 1160;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 4 + seededRandom(i + 7) * 5, height: 4 + seededRandom(i + 7) * 5,
            borderRadius: '50%', backgroundColor: 'rgba(255,244,73,0.14)',
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>

        {/* Title */}
        <div style={{
          position: 'absolute', top: 70, left: 120,
          transform: `translateX(${(1 - sp(0)) * -160}px)`, opacity: sp(0),
        }}>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 96, fontWeight: 900, color: '#FFF449', letterSpacing: -2 }}>
            WTI CRUDE — A WILD RIDE
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 600, color: '#25343F', marginTop: 6 }}>
            MAY → JULY 2026
          </div>
        </div>

        {/* Animated SVG price path */}
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {/* Baseline axis */}
          <line x1="180" y1="880" x2="1740" y2="880" stroke="rgba(37,52,63,0.5)" strokeWidth="4" />
          {/* Price line: above $100 -> near $70 -> bounce $80 */}
          <path
            d="M 220 500 C 380 420, 480 380, 600 400 C 760 430, 880 620, 1040 760 C 1140 840, 1220 830, 1330 780 C 1440 730, 1560 660, 1700 640"
            fill="none" stroke="#FFF449" strokeWidth="12" strokeLinecap="round"
            strokeDasharray={pathLen} strokeDashoffset={drawn}
            style={{ filter: 'drop-shadow(0 8px 20px rgba(0,0,0,0.4))' }}
          />
          {/* Marker dots */}
          <circle cx="600" cy="400" r={14 * p100} fill="#25343F" stroke="#FFF449" strokeWidth="5" />
          <circle cx="1040" cy="760" r={14 * p70} fill="#25343F" stroke="#FFF449" strokeWidth="5" />
          <circle cx="1700" cy="640" r={14 * p80} fill="#25343F" stroke="#FFF449" strokeWidth="5" />
        </svg>

        {/* Price labels */}
        <div style={{ position: 'absolute', left: 480, top: 260, transform: `scale(${p100})`, opacity: p100, backgroundColor: '#25343F', padding: '16px 34px', borderRadius: 12, textAlign: 'center' }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 64, fontWeight: 800, color: '#FFF449' }}>$100+</div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 700, color: '#FF9B51' }}>MAY</div>
        </div>
        <div style={{ position: 'absolute', left: 930, top: 830, transform: `scale(${p70})`, opacity: p70, backgroundColor: '#25343F', padding: '16px 34px', borderRadius: 12, textAlign: 'center' }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 64, fontWeight: 800, color: '#FFF449' }}>~$70</div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 700, color: '#FF9B51' }}>EARLY JULY</div>
        </div>
        <div style={{ position: 'absolute', left: 1560, top: 430, transform: `scale(${p80})`, opacity: p80, backgroundColor: '#25343F', padding: '16px 34px', borderRadius: 12, textAlign: 'center' }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 64, fontWeight: 800, color: '#FFF449' }}>$80</div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 700, color: '#FF9B51' }}>BOUNCE</div>
        </div>

        {/* Brent badge */}
        <div style={{
          position: 'absolute', left: 150, top: 620,
          transform: `translateX(${(1 - brent) * -300}px) rotate(-3deg)`, opacity: brent,
          backgroundColor: '#FFF449', padding: '22px 40px', borderRadius: 14,
          boxShadow: '0 16px 44px rgba(0,0,0,0.35)',
        }}>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 44, fontWeight: 900, color: '#25343F' }}>BRENT AVG — JUNE</div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 84, fontWeight: 800, color: '#25343F' }}>$85</div>
        </div>

        {/* Payoff message */}
        <div style={{
          position: 'absolute', bottom: 80, left: 0, right: 0, textAlign: 'center',
          transform: `translateY(${(1 - payoff) * 120}px)`, opacity: payoff,
        }}>
          <span style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 58, fontWeight: 900,
            color: '#FFF449', backgroundColor: 'rgba(37,52,63,0.92)', padding: '20px 56px', borderRadius: 12,
          }}>
            THE TRANSMISSION SYSTEM &gt; THE DAILY QUOTE
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 50%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene12OilChart;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13OilWeb',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const NODES = [
  { label: 'SHIPPING', angle: -150 },
  { label: 'AVIATION', angle: -90 },
  { label: 'AGRICULTURE', angle: -30 },
  { label: 'MANUFACTURING', angle: 30 },
  { label: 'PLASTICS', angle: 90 },
  { label: 'SHELF PRICE', angle: 150 },
];

const Scene13OilWeb: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Gentle orbital camera rotation
  const orbit = Math.sin(frame / 110) * 1.5;
  const cx = 960, cy = 470, radius = 360;

  const payoff = sp(400);

  return (
    <AbsoluteFill style={{ backgroundColor: '#6E3509', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% ${44 + Math.sin(frame / 70) * 5}%, #C4691F 0%, #93470F 50%, #5A2B06 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1200, height: 1200, borderRadius: '50%',
        border: '2px dashed rgba(255,244,73,0.08)', left: 360, top: -130,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        background: 'rgba(37,52,63,0.07)', left: -160, bottom: -160,
        transform: `rotate(${45 + frame * 0.05}deg)`,
      }} />

      {/* Layer 3: Motion texture — concentric circles */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-radial-gradient(circle at 50% 44%, transparent 0px, transparent 78px, rgba(255,255,255,0.03) 80px)',
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const a = seededRandom(i + 2) * Math.PI * 2 + frame * 0.004;
        const r = 300 + seededRandom(i + 11) * 500;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: cx + Math.cos(a) * r, top: cy + Math.sin(a) * r * 0.6,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: 'rgba(255,244,73,0.18)',
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `rotate(${orbit}deg)` }}>

        {/* Connector lines drawing outward */}
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {NODES.map((n, i) => {
            const rad = (n.angle * Math.PI) / 180;
            const x2 = cx + Math.cos(rad) * radius;
            const y2 = cy + Math.sin(rad) * radius * 0.72;
            const prog = interpolate(frame, [60 + i * 16, 100 + i * 16], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
            });
            return (
              <line key={i} x1={cx} y1={cy}
                x2={cx + (x2 - cx) * prog} y2={cy + (y2 - cy) * prog}
                stroke="#FFF449" strokeWidth="5" strokeDasharray="14 12" opacity={0.75}
              />
            );
          })}
          {/* Central oil drop icon */}
          <g transform={`translate(${cx}, ${cy}) scale(${sp(10)})`}>
            <circle r="95" fill="#25343F" />
            <path d="M0 -52 C 26 -14, 44 8, 44 30 A 44 44 0 1 1 -44 30 C -44 8, -26 -14, 0 -52 Z" fill="#FFF449" />
          </g>
        </svg>

        {/* Node chips */}
        {NODES.map((n, i) => {
          const rad = (n.angle * Math.PI) / 180;
          const x = cx + Math.cos(rad) * radius;
          const y = cy + Math.sin(rad) * radius * 0.72;
          const s = sp(95 + i * 16);
          const pulse = 1 + Math.sin((frame + i * 25) / 16) * 0.03;
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y,
              transform: `translate(-50%, -50%) scale(${s * pulse})`, opacity: s,
              backgroundColor: '#FFF449', padding: '18px 34px', borderRadius: 60,
              boxShadow: '0 14px 36px rgba(0,0,0,0.4)',
            }}>
              <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 34, fontWeight: 900, color: '#25343F', letterSpacing: 1 }}>
                {n.label}
              </span>
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Bottom double message */}
      <div style={{
        position: 'absolute', bottom: 150, left: 0, right: 0, textAlign: 'center',
        transform: `translateY(${(1 - sp(300)) * 90}px)`, opacity: sp(300),
      }}>
        <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 56, fontWeight: 900, color: '#25343F' }}>
          A LOWER HEADLINE ≠ A LOWER GROCERY BILL
        </span>
      </div>
      <div style={{
        position: 'absolute', bottom: 70, left: 0, right: 0, textAlign: 'center',
        transform: `scale(${payoff})`, opacity: payoff,
      }}>
        <span style={{
          fontFamily: 'Inter, sans-serif', fontSize: 42, fontWeight: 800, color: '#FFF449',
          backgroundColor: 'rgba(37,52,63,0.92)', padding: '14px 44px', borderRadius: 10,
        }}>
          NEXT SLOW SHOCK: HEALTHCARE
        </span>
      </div>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 52%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene13OilWeb;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14Healthcare',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14Healthcare: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 15 bars, last one (2026) is the tallest at 6.7%
  const barHeights = Array.from({ length: 15 }).map((_, i) =>
    i === 14 ? 340 : 90 + seededRandom(i + 5) * 140
  );

  // Screen shake on the impact of the 6.7% bar
  const impactFrame = 240;
  const shakeAmp = interpolate(frame, [impactFrame, impactFrame + 14], [9, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const shakeX = Math.sin(frame * 2.4) * shakeAmp;

  // Paycheck erosion bar
  const erode = interpolate(frame, [380, 500], [0, 0.38], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#83400E', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${25 + Math.sin(frame / 85) * 8}deg, #5F2D06 0%, #A5520F 52%, #C4691F 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 760, height: 760, borderRadius: 40,
        background: 'rgba(255,244,73,0.045)', right: -240, top: -260,
        transform: `rotate(${frame * 0.06}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 640, height: 640, borderRadius: '50%',
        border: '3px solid rgba(37,52,63,0.1)', left: -200, bottom: -240,
        transform: `scale(${1 + Math.sin(frame / 65) * 0.05})`,
      }} />

      {/* Layer 3: Motion texture — medical plus grid */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(0deg, rgba(255,255,255,0.03) 0px, rgba(255,255,255,0.03) 1px, transparent 1px, transparent 130px), repeating-linear-gradient(90deg, rgba(255,255,255,0.03) 0px, rgba(255,255,255,0.03) 1px, transparent 1px, transparent 130px)',
        backgroundPosition: `0px ${frame * 0.4}px`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const x = seededRandom(i + 6) * 1920;
        const drift = (frame * (0.3 + seededRandom(i + 30) * 0.45)) % 1150;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1110 - drift,
            width: 6, height: 6, borderRadius: 2,
            backgroundColor: 'rgba(255,244,73,0.15)',
            transform: `rotate(${frame + i * 20}deg)`,
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom}) translateX(${shakeX}px)` }}>

        {/* Headline */}
        <div style={{ position: 'absolute', top: 70, left: 0, right: 0, textAlign: 'center', opacity: sp(0), transform: `translateY(${(1 - sp(0)) * -60}px)` }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 92, fontWeight: 900, color: '#FFF449', letterSpacing: -2 }}>
            HEALTHCARE: THE SLOW SHOCK
          </span>
        </div>

        {/* 15-year bar chart */}
        <div style={{ position: 'absolute', bottom: 330, left: 200, right: 200, display: 'flex', alignItems: 'flex-end', gap: 22, height: 380 }}>
          {barHeights.map((h, i) => {
            const grow = interpolate(frame, [70 + i * 12, 110 + i * 12], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
            });
            const is2026 = i === 14;
            return (
              <div key={i} style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
                {is2026 && (
                  <div style={{
                    marginBottom: 14, transform: `scale(${sp(impactFrame)})`, opacity: sp(impactFrame),
                    backgroundColor: '#FFF449', padding: '10px 24px', borderRadius: 10,
                    boxShadow: '0 12px 30px rgba(0,0,0,0.4)',
                  }}>
                    <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 66, fontWeight: 800, color: '#25343F' }}>+6.7%</span>
                  </div>
                )}
                <div style={{
                  width: '100%', height: h * grow, borderRadius: '8px 8px 0 0',
                  backgroundColor: is2026 ? '#FFF449' : 'rgba(37,52,63,0.75)',
                  boxShadow: is2026 ? '0 0 44px rgba(255,244,73,0.45)' : 'none',
                }} />
              </div>
            );
          })}
        </div>
        <div style={{ position: 'absolute', bottom: 268, left: 0, right: 0, textAlign: 'center', opacity: sp(impactFrame + 20) }}>
          <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 800, color: '#25343F', letterSpacing: 3 }}>
            LARGEST INCREASE IN 15 YEARS — 2026
          </span>
        </div>

        {/* Paycheck erosion meter */}
        <div style={{
          position: 'absolute', bottom: 90, left: 360, right: 360,
          transform: `translateY(${(1 - sp(370)) * 140}px)`, opacity: sp(370),
        }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 34, fontWeight: 800, color: '#FFF449', marginBottom: 12, textAlign: 'center' }}>
            YOUR "RAISE" — AFTER HEALTHCARE DEDUCTIONS
          </div>
          <div style={{ height: 74, borderRadius: 40, backgroundColor: 'rgba(37,52,63,0.9)', overflow: 'hidden', display: 'flex' }}>
            <div style={{ width: `${(1 - erode) * 100}%`, backgroundColor: '#FFF449', transition: 'none' }} />
            <div style={{ flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, fontWeight: 800, color: '#FF9B51', opacity: erode > 0.05 ? 1 : 0 }}>
                DEDUCTED
              </span>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 52%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene14Healthcare;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15StagflationLite',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15StagflationLite: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Gauge needles
  const growthNeedle = interpolate(sp(180), [0, 1], [-90, -38]); // stuck low, below 2%
  const inflationNeedle = interpolate(sp(220), [0, 1], [-90, 42]); // uncomfortably high

  const camTilt = interpolate(frame, [0, durationInFrames], [-1, 1]);

  const Gauge: React.FC<{ x: number; needle: number; label: string; value: string; tone: string }> = ({ x, needle, label, value, tone }) => (
    <div style={{ position: 'absolute', left: x, top: 470, width: 460, textAlign: 'center' }}>
      <svg width="460" height="260" viewBox="0 0 460 260">
        <path d="M 40 240 A 190 190 0 0 1 420 240" fill="none" stroke="rgba(37,52,63,0.35)" strokeWidth="30" strokeLinecap="round" />
        <path d="M 40 240 A 190 190 0 0 1 420 240" fill="none" stroke={tone} strokeWidth="30" strokeLinecap="round"
          strokeDasharray="600" strokeDashoffset={interpolate(frame, [140, 260], [600, 240], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} />
        <g transform={`translate(230, 240) rotate(${needle})`}>
          <rect x="-7" y="-165" width="14" height="165" rx="7" fill="#25343F" />
          <circle r="24" fill="#25343F" />
        </g>
      </svg>
      <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 32, fontWeight: 800, color: '#25343F', letterSpacing: 2, marginTop: 10 }}>{label}</div>
      <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 56, fontWeight: 800, color: tone }}>{value}</div>
    </div>
  );

  return (
    <AbsoluteFill style={{ backgroundColor: '#75380A', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 50% ${30 + Math.sin(frame / 75) * 6}%, #BE6218 0%, #8C440D 55%, #542805 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1500, height: 1500, borderRadius: '50%',
        border: '2px solid rgba(255,244,73,0.05)', left: 210, top: -540,
        transform: `rotate(${frame * 0.03}deg) scale(${1 + Math.sin(frame / 90) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute', width: 420, height: 420,
        background: 'rgba(255,155,81,0.06)', left: -120, top: 160,
        transform: `rotate(${frame * 0.08}deg)`, borderRadius: 60,
      }} />

      {/* Layer 3: Motion texture — horizontal scanning lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(0deg, rgba(0,0,0,0.05) 0px, rgba(0,0,0,0.05) 2px, transparent 2px, transparent 64px)',
        backgroundPosition: `0px ${-frame * 0.5}px`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const x = seededRandom(i + 8) * 1920;
        const drift = (frame * (0.22 + seededRandom(i + 17) * 0.4)) % 1150;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1110 - drift,
            width: 4 + seededRandom(i + 4) * 6, height: 4 + seededRandom(i + 4) * 6,
            borderRadius: '50%', backgroundColor: 'rgba(255,244,73,0.14)',
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `rotate(${camTilt}deg)` }}>

        {/* Mega typographic title with staggered word reveal */}
        <div style={{ position: 'absolute', top: 110, left: 0, right: 0, textAlign: 'center' }}>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 172, fontWeight: 900, color: '#25343F',
            letterSpacing: -5, lineHeight: 0.9,
            transform: `translateY(${(1 - sp(5)) * -120}px)`, opacity: sp(5),
          }}>
            STAGFLATION
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 130, fontWeight: 900, color: '#FFF449',
            letterSpacing: 26, marginTop: 4,
            transform: `scaleX(${interpolate(sp(40), [0, 1], [2, 1])})`, opacity: sp(40),
            textShadow: '0 10px 40px rgba(0,0,0,0.4)',
          }}>
            LITE
          </div>
        </div>

        {/* Twin gauges: growth low + inflation high */}
        <Gauge x={330} needle={growthNeedle} label="GROWTH (RBC VIEW)" value="< 2%" tone="#25343F" />
        <Gauge x={1130} needle={inflationNeedle} label="INFLATION" value="STILL HIGH" tone="#FFF449" />

        {/* Bottom bar — no official siren */}
        <div style={{
          position: 'absolute', bottom: 70, left: 0, right: 0, display: 'flex', justifyContent: 'center', gap: 40,
          transform: `translateY(${(1 - sp(380)) * 110}px)`, opacity: sp(380),
        }}>
          <div style={{ backgroundColor: 'rgba(37,52,63,0.92)', padding: '20px 46px', borderRadius: 12 }}>
            <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 44, fontWeight: 900, color: '#FFF449' }}>
              NOT A DEPRESSION
            </span>
          </div>
          <div style={{ backgroundColor: 'rgba(37,52,63,0.92)', padding: '20px 46px', borderRadius: 12 }}>
            <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 44, fontWeight: 900, color: '#FF9B51' }}>
              NO OFFICIAL SIREN
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 50%, rgba(0,0,0,0.52) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene15StagflationLite;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16Seventies',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const PATTERNS = [
  { title: 'TRUST IN CASH', sub: 'FADING' },
  { title: 'CHASING ASSETS', sub: 'TOO LATE' },
  { title: 'BORROWING', sub: 'TO KEEP A LIFESTYLE' },
];

const Scene16Seventies: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Ghost year drifts slowly in the background
  const ghostX = interpolate(frame, [0, durationInFrames], [-60, 60]);
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#6B3308', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${200 + Math.sin(frame / 95) * 12}deg, #B25A15 0%, #7E3D0A 50%, #4C2404 100%)`,
      }} />

      {/* Ghost typography — 1970s watermark */}
      <div style={{
        position: 'absolute', top: 220, left: 0, right: 0, textAlign: 'center',
        transform: `translateX(${ghostX}px)`,
        fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 560, fontWeight: 900,
        color: 'rgba(37,52,63,0.1)', letterSpacing: -20, lineHeight: 1,
      }}>
        1970s
      </div>

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        border: '3px solid rgba(255,244,73,0.06)', right: -260, top: -280,
        transform: `rotate(${frame * 0.07}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 0, height: 0, left: -60, bottom: -80,
        borderLeft: '300px solid transparent', borderRight: '300px solid transparent',
        borderTop: '520px solid rgba(255,155,81,0.05)',
        transform: `rotate(${-8 + frame * 0.03}deg)`,
      }} />

      {/* Layer 3: Motion texture — retro vertical stripes */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(90deg, rgba(255,255,255,0.028) 0px, rgba(255,255,255,0.028) 3px, transparent 3px, transparent 120px)',
        backgroundPosition: `${frame * 0.3}px 0px`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = seededRandom(i + 12) * 1920;
        const drift = (frame * (0.28 + seededRandom(i + 33) * 0.42)) % 1150;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1110 - drift,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: 'rgba(255,244,73,0.15)',
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, alignItems: 'center' }}>

        {/* Stamp headline: WARNING / NOT A PREDICTION */}
        <div style={{ marginTop: 120, textAlign: 'center' }}>
          <div style={{
            display: 'inline-block', border: '10px solid #FFF449', padding: '18px 60px', borderRadius: 8,
            transform: `scale(${interpolate(sp(20), [0, 1], [2.4, 1])}) rotate(-4deg)`,
            opacity: sp(20),
          }}>
            <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 108, fontWeight: 900, color: '#FFF449', letterSpacing: 6 }}>
              A WARNING
            </span>
          </div>
          <div style={{
            marginTop: 28, opacity: sp(80),
            transform: `translateY(${(1 - sp(80)) * 50}px)`,
          }}>
            <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 60, fontWeight: 900, color: '#25343F', letterSpacing: 3 }}>
              NOT A PREDICTION
            </span>
          </div>
        </div>

        {/* Rhyming pattern cards */}
        <div style={{ display: 'flex', gap: 40, marginTop: 90 }}>
          {PATTERNS.map((p, i) => {
            const s = sp(180 + i * 25);
            const bob = Math.sin((frame + i * 40) / 24) * 5;
            return (
              <div key={i} style={{
                width: 420, padding: '36px 30px', backgroundColor: 'rgba(37,52,63,0.94)',
                borderRadius: 18, textAlign: 'center', borderTop: '8px solid #FFF449',
                transform: `translateY(${(1 - s) * 260 + bob}px) rotate(${(i - 1) * 1.6}deg)`,
                opacity: s, boxShadow: '0 22px 54px rgba(0,0,0,0.42)',
              }}>
                {/* Simple SVG glyph per card */}
                <svg width="84" height="84" viewBox="0 0 84 84" style={{ marginBottom: 16 }}>
                  {i === 0 && <>
                    <rect x="10" y="24" width="64" height="40" rx="6" fill="none" stroke="#FFF449" strokeWidth="5" />
                    <circle cx="42" cy="44" r="10" fill="#FFF449" opacity={interpolate(Math.sin(frame / 12), [-1, 1], [0.25, 1])} />
                  </>}
                  {i === 1 && <path d="M10 66 L34 42 L48 54 L74 20 M74 20 L74 40 M74 20 L56 20" fill="none" stroke="#FFF449" strokeWidth="6" strokeLinecap="round" strokeLinejoin="round" />}
                  {i === 2 && <>
                    <rect x="14" y="34" width="56" height="36" rx="6" fill="none" stroke="#FFF449" strokeWidth="5" />
                    <path d="M24 34 V24 a18 14 0 0 1 36 0 V34" fill="none" stroke="#FFF449" strokeWidth="5" />
                  </>}
                </svg>
                <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 40, fontWeight: 900, color: '#F5F5F5' }}>{p.title}</div>
                <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 700, color: '#FF9B51', marginTop: 8, letterSpacing: 2 }}>{p.sub}</div>
              </div>
            );
          })}
        </div>

        {/* Payoff line */}
        <div style={{ marginTop: 60, opacity: sp(420), transform: `scale(${sp(420)})` }}>
          <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 44, fontWeight: 800, color: '#FFF449' }}>
            THE EMOTIONAL PATTERN RHYMES
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 50%, rgba(0,0,0,0.52) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene16Seventies;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17RealWealth',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17RealWealth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Racing progress bars — costs overtake the 401(k)
  const race401 = interpolate(frame, [80, 300], [0, 0.45], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.ease) });
  const raceCosts = interpolate(frame, [110, 300], [0, 0.82], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.ease) });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.06, 1]);
  const payoff = sp(370);

  return (
    <AbsoluteFill style={{ backgroundColor: '#7D3C0B', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${30 + Math.sin(frame / 65) * 10}% 60%, #C4691F 0%, #8C440D 55%, #532705 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        background: 'rgba(255,244,73,0.04)', right: -330, bottom: -420,
        transform: `scale(${1 + Math.sin(frame / 72) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 520, height: 520, borderRadius: 30,
        border: '3px solid rgba(37,52,63,0.12)', left: -160, top: -160,
        transform: `rotate(${frame * 0.05}deg)`,
      }} />

      {/* Layer 3: Motion texture — dotted field */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(rgba(255,255,255,0.05) 2px, transparent 2px)',
        backgroundSize: '72px 72px',
        backgroundPosition: `${frame * 0.25}px ${frame * 0.18}px`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const x = seededRandom(i + 14) * 1920;
        const drift = (frame * (0.26 + seededRandom(i + 44) * 0.4)) % 1150;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1110 - drift,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: 'rgba(255,244,73,0.16)',
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>

        {/* Headline */}
        <div style={{ position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center', opacity: sp(0), transform: `translateY(${(1 - sp(0)) * -70}px)` }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 96, fontWeight: 900, color: '#FFF449', letterSpacing: -2 }}>
            THE NOMINAL ILLUSION
          </span>
        </div>

        {/* Race track: 401k vs Real Costs */}
        <div style={{ position: 'absolute', top: 280, left: 240, right: 240 }}>
          {/* 401k lane */}
          <div style={{ opacity: sp(60), transform: `translateX(${(1 - sp(60)) * -200}px)` }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 10 }}>
              <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 800, color: '#25343F' }}>YOUR 401(k)</span>
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 44, fontWeight: 800, color: '#25343F' }}>+5%</span>
            </div>
            <div style={{ height: 66, borderRadius: 36, backgroundColor: 'rgba(37,52,63,0.4)', overflow: 'hidden' }}>
              <div style={{ width: `${race401 * 100}%`, height: '100%', backgroundColor: '#25343F', borderRadius: 36 }} />
            </div>
          </div>

          {/* Costs lane — overtakes */}
          <div style={{ marginTop: 56, opacity: sp(90), transform: `translateX(${(1 - sp(90)) * -200}px)` }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 10 }}>
              <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 800, color: '#FFF449' }}>COSTS THAT MATTER</span>
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 44, fontWeight: 800, color: '#FFF449' }}>FASTER</span>
            </div>
            <div style={{ height: 66, borderRadius: 36, backgroundColor: 'rgba(37,52,63,0.4)', overflow: 'hidden' }}>
              <div style={{
                width: `${raceCosts * 100}%`, height: '100%', backgroundColor: '#FFF449', borderRadius: 36,
                boxShadow: '0 0 36px rgba(255,244,73,0.5)',
              }} />
            </div>
          </div>
        </div>

        {/* Definition card — real wealth */}
        <div style={{
          position: 'absolute', bottom: 110, left: 0, right: 0, display: 'flex', justifyContent: 'center',
          transform: `translateY(${(1 - payoff) * 160}px)`, opacity: payoff,
        }}>
          <div style={{
            backgroundColor: 'rgba(37,52,63,0.95)', padding: '44px 80px', borderRadius: 20,
            borderLeft: '14px solid #FFF449', maxWidth: 1300, textAlign: 'center',
            boxShadow: '0 24px 60px rgba(0,0,0,0.45)',
          }}>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 34, fontWeight: 700, color: '#FF9B51', letterSpacing: 4, marginBottom: 14 }}>
              REAL WEALTH
            </div>
            <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 62, fontWeight: 900, color: '#F5F5F5', lineHeight: 1.15 }}>
              NOT THE NUMBER ON THE STATEMENT —<br />
              <span style={{ color: '#FFF449' }}>WHAT THAT NUMBER CAN STILL BUY</span>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 52%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene17RealWealth;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18ExposedHousehold',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const RISKS = [
  'HIGH FIXED EXPENSES',
  'VARIABLE-RATE DEBT',
  'ONE INCOME SOURCE',
  'NO EMERGENCY LIQUIDITY',
];

const Scene18ExposedHousehold: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bonds vs yields mini chart draw
  const chartProg = interpolate(frame, [330, 470], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  const camPan = interpolate(frame, [0, durationInFrames], [30, -30]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#703509', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${140 + Math.sin(frame / 80) * 10}deg, #A6520F 0%, #7B3B09 50%, #4A2304 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 780, height: 780, borderRadius: '50%',
        border: '3px solid rgba(255,244,73,0.06)', left: -240, top: -240,
        transform: `rotate(${frame * 0.06}deg) scale(${1 + Math.sin(frame / 66) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 560, height: 560, borderRadius: 40,
        background: 'rgba(37,52,63,0.06)', right: -150, bottom: -190,
        transform: `rotate(${-10 + frame * 0.04}deg)`,
      }} />

      {/* Layer 3: Motion texture — cross-hatch */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(45deg, rgba(255,255,255,0.03) 0px, rgba(255,255,255,0.03) 1px, transparent 1px, transparent 80px), repeating-linear-gradient(-45deg, rgba(255,255,255,0.03) 0px, rgba(255,255,255,0.03) 1px, transparent 1px, transparent 80px)',
        backgroundPosition: `${frame * 0.3}px 0px`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = seededRandom(i + 22) * 1920;
        const drift = (frame * (0.24 + seededRandom(i + 55) * 0.42)) % 1150;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1110 - drift,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: 'rgba(255,244,73,0.15)',
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>

        {/* Left: headline + risk stack */}
        <div style={{ position: 'absolute', left: 140, top: 110, width: 860 }}>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 84, fontWeight: 900,
            color: '#FFF449', letterSpacing: -2, lineHeight: 1,
            opacity: sp(0), transform: `translateY(${(1 - sp(0)) * -70}px)`,
          }}>
            THE MOST EXPOSED HOUSEHOLD
          </div>

          {RISKS.map((r, i) => {
            const s = sp(80 + i * 22);
            return (
              <div key={i} style={{
                marginTop: i === 0 ? 50 : 24, display: 'flex', alignItems: 'center', gap: 24,
                backgroundColor: 'rgba(37,52,63,0.92)', padding: '24px 32px', borderRadius: 14,
                transform: `translateX(${(1 - s) * -360}px)`, opacity: s,
                boxShadow: '0 14px 36px rgba(0,0,0,0.35)',
              }}>
                {/* Warning triangle SVG */}
                <svg width="52" height="52" viewBox="0 0 52 52">
                  <path d="M26 6 L48 44 L4 44 Z" fill="none" stroke="#FF9B51" strokeWidth="5" strokeLinejoin="round" />
                  <rect x="24" y="20" width="4" height="12" rx="2" fill="#FF9B51" />
                  <circle cx="26" cy="38" r="3" fill="#FF9B51" />
                </svg>
                <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 800, color: '#F5F5F5' }}>{r}</span>
              </div>
            );
          })}
        </div>

        {/* Right: bonds vs yields mini chart */}
        <div style={{
          position: 'absolute', right: 130, top: 210, width: 620,
          backgroundColor: 'rgba(37,52,63,0.94)', borderRadius: 20, padding: '38px 40px',
          transform: `scale(${sp(300)})`, opacity: sp(300),
          boxShadow: '0 24px 60px rgba(0,0,0,0.45)',
        }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 32, fontWeight: 800, color: '#FF9B51', letterSpacing: 3, textAlign: 'center' }}>
            WHEN YIELDS RISE
          </div>
          <svg width="540" height="330" viewBox="0 0 540 330" style={{ marginTop: 20 }}>
            {/* Yields going up */}
            <path d="M30 250 C 150 230, 280 170, 500 70" fill="none" stroke="#FFF449" strokeWidth="9" strokeLinecap="round"
              strokeDasharray="700" strokeDashoffset={700 - 700 * chartProg} />
            {/* Long bonds going down */}
            <path d="M30 120 C 160 150, 300 220, 500 290" fill="none" stroke="#FF9B51" strokeWidth="9" strokeLinecap="round" strokeDasharray="14 12"
              opacity={chartProg} />
            <text x="360" y="52" fill="#FFF449" fontFamily="Inter, sans-serif" fontSize="28" fontWeight="800" opacity={chartProg}>YIELDS ↑</text>
            <text x="290" y="320" fill="#FF9B51" fontFamily="Inter, sans-serif" fontSize="28" fontWeight="800" opacity={chartProg}>LONG BONDS ↓</text>
          </svg>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 40, fontWeight: 900, color: '#F5F5F5', textAlign: 'center', marginTop: 14 }}>
            LONG-DURATION BONDS <span style={{ color: '#FFF449' }}>STRUGGLE</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 52%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene18ExposedHousehold;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19Overpay',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19Overpay: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Seesaw tilts: safe yields side goes down (heavy), speculative side rises and fades
  const tilt = interpolate(sp(120), [0, 1], [0, -9]);

  // Cash melt meter
  const melt = interpolate(frame, [400, 540], [1, 0.6], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#78390A', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${65 + Math.sin(frame / 70) * 8}% 35%, #BE6218 0%, #8A430D 52%, #4F2504 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1000, height: 1000, borderRadius: '50%',
        border: '2px solid rgba(255,244,73,0.055)', left: -320, bottom: -420,
        transform: `rotate(${frame * 0.05}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 460, height: 460,
        background: 'rgba(255,155,81,0.06)', right: -120, top: -140,
        transform: `rotate(${frame * 0.07}deg)`, borderRadius: 50,
      }} />

      {/* Layer 3: Motion texture — fine diagonal weave */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(60deg, rgba(0,0,0,0.045) 0px, rgba(0,0,0,0.045) 2px, transparent 2px, transparent 70px)',
        backgroundPosition: `${-frame * 0.45}px 0px`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const x = seededRandom(i + 31) * 1920;
        const drift = (frame * (0.25 + seededRandom(i + 61) * 0.4)) % 1150;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1110 - drift,
            width: 4 + seededRandom(i + 2) * 5, height: 4 + seededRandom(i + 2) * 5,
            borderRadius: '50%', backgroundColor: 'rgba(255,244,73,0.15)',
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>

        {/* Seesaw: speculation vs safe yields */}
        <div style={{ position: 'absolute', top: 130, left: 0, right: 0, display: 'flex', justifyContent: 'center' }}>
          <div style={{ position: 'relative', width: 1240, height: 460, opacity: sp(20) }}>
            {/* Beam */}
            <div style={{
              position: 'absolute', top: 250, left: 70, width: 1100, height: 22, borderRadius: 12,
              backgroundColor: '#25343F', transformOrigin: '50% 50%', transform: `rotate(${tilt}deg)`,
              boxShadow: '0 14px 34px rgba(0,0,0,0.35)',
            }}>
              {/* Speculative side (rises, fades) */}
              <div style={{
                position: 'absolute', left: 30, top: -150, width: 330, textAlign: 'center',
                transform: `rotate(${-tilt}deg)`, opacity: interpolate(sp(120), [0, 1], [1, 0.45]),
              }}>
                <div style={{ backgroundColor: 'rgba(37,52,63,0.9)', padding: '20px 26px', borderRadius: 14, border: '3px dashed #FF9B51' }}>
                  <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 38, fontWeight: 900, color: '#FF9B51' }}>SPECULATIVE</div>
                  <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700, color: '#F5F5F5', marginTop: 6 }}>PROFITS "SOMEDAY"</div>
                </div>
              </div>
              {/* Safe yields side (heavy) */}
              <div style={{
                position: 'absolute', right: 30, top: -170, width: 330, textAlign: 'center',
                transform: `rotate(${-tilt}deg)`,
              }}>
                <div style={{ backgroundColor: '#FFF449', padding: '22px 26px', borderRadius: 14, boxShadow: '0 16px 40px rgba(0,0,0,0.4)' }}>
                  <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 38, fontWeight: 900, color: '#25343F' }}>SAFE ASSETS</div>
                  <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 34, fontWeight: 800, color: '#25343F', marginTop: 6 }}>REAL YIELD TODAY</div>
                </div>
              </div>
            </div>
            {/* Fulcrum */}
            <div style={{
              position: 'absolute', top: 268, left: 570, width: 0, height: 0,
              borderLeft: '52px solid transparent', borderRight: '52px solid transparent',
              borderBottom: '120px solid #25343F',
            }} />
          </div>
        </div>

        {/* Middle warning line */}
        <div style={{
          position: 'absolute', top: 620, left: 0, right: 0, textAlign: 'center',
          opacity: sp(260), transform: `translateY(${(1 - sp(260)) * 70}px)`,
        }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 66, fontWeight: 900, color: '#FFF449' }}>
            GREAT COMPANY + WRONG PRICE = BAD INVESTMENT
          </span>
        </div>

        {/* Cash melt strip */}
        <div style={{
          position: 'absolute', bottom: 95, left: 420, right: 420,
          opacity: sp(390), transform: `translateY(${(1 - sp(390)) * 120}px)`,
        }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, fontWeight: 800, color: '#25343F', textAlign: 'center', marginBottom: 14 }}>
            100% CASH? TAXES + INFLATION STILL MELT IT
          </div>
          <div style={{ height: 60, borderRadius: 32, backgroundColor: 'rgba(37,52,63,0.85)', overflow: 'hidden' }}>
            <div style={{
              width: `${melt * 100}%`, height: '100%', backgroundColor: '#FFF449', borderRadius: 32,
            }} />
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 50%, rgba(0,0,0,0.52) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene19Overpay;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20BuffettBalance',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20BuffettBalance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Balance scale settles into equilibrium with a gentle wobble
  const settle = sp(80, { damping: 7, stiffness: 60 });
  const wobble = Math.sin(frame / 20) * 2 * (1 - Math.min(1, frame / 300));
  const beamRot = interpolate(settle, [0, 1], [-14, 0]) + wobble;

  // Quote typewriter-style reveal
  const quote = `"People will forever exchange what they produce for what others produce."`;
  const chars = Math.floor(interpolate(frame, [280, 470], [0, quote.length], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));

  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#6E3407', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${180 + Math.sin(frame / 100) * 14}deg, #B45B14 0%, #83400C 50%, #472103 100%)`,
      }} />

      {/* Layer 2: Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1300, height: 1300, borderRadius: '50%',
        border: '2px solid rgba(255,244,73,0.05)', left: 310, top: -420,
        transform: `rotate(${frame * 0.04}deg) scale(${1 + Math.sin(frame / 84) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute', width: 540, height: 540, borderRadius: 44,
        background: 'rgba(37,52,63,0.06)', left: -170, bottom: -180,
        transform: `rotate(${20 + frame * 0.04}deg)`,
      }} />

      {/* Layer 3: Motion texture — soft radial rings */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-radial-gradient(circle at 50% 40%, transparent 0px, transparent 96px, rgba(255,255,255,0.03) 98px)',
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const x = seededRandom(i + 42) * 1920;
        const drift = (frame * (0.22 + seededRandom(i + 72) * 0.38)) % 1150;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1110 - drift,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: 'rgba(255,244,73,0.16)',
          }} />
        );
      })}

      {/* Layer 5: Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, alignItems: 'center' }}>

        {/* Headline */}
        <div style={{ marginTop: 70, opacity: sp(0), transform: `translateY(${(1 - sp(0)) * -60}px)` }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 104, fontWeight: 900, color: '#FFF449', letterSpacing: -2 }}>
            THE ANSWER IS BALANCE
          </span>
        </div>

        {/* Balance scale — productive assets vs idle dollars */}
        <div style={{ position: 'relative', width: 1100, height: 400, marginTop: 40, opacity: sp(50) }}>
          {/* Pillar */}
          <div style={{ position: 'absolute', left: 536, top: 60, width: 28, height: 300, borderRadius: 14, backgroundColor: '#25343F' }} />
          <div style={{ position: 'absolute', left: 430, top: 352, width: 240, height: 26, borderRadius: 13, backgroundColor: '#25343F' }} />
          {/* Beam */}
          <div style={{
            position: 'absolute', left: 100, top: 62, width: 900, height: 18, borderRadius: 10,
            backgroundColor: '#25343F', transformOrigin: '50% 50%', transform: `rotate(${beamRot}deg)`,
          }}>
            {/* Left pan: productive assets */}
            <div style={{ position: 'absolute', left: 30, top: 14, width: 4, height: 90, backgroundColor: '#25343F', transform: `rotate(${-beamRot}deg)`, transformOrigin: 'top' }} />
            <div style={{
              position: 'absolute', left: -108, top: 100, width: 280, textAlign: 'center',
              transform: `rotate(${-beamRot}deg)`, transformOrigin: '50% -90px',
            }}>
              <div style={{ backgroundColor: '#FFF449', padding: '20px 20px', borderRadius: '0 0 130px 130px', boxShadow: '0 16px 40px rgba(0,0,0,0.4)' }}>
                <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 34, fontWeight: 900, color: '#25343F' }}>PRODUCTIVE ASSETS</div>
                <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 700, color: '#25343F', marginTop: 4 }}>OUTPUT KEEPS ITS VALUE</div>
              </div>
            </div>
            {/* Right pan: warehouse of dollars */}
            <div style={{ position: 'absolute', right: 30, top: 14, width: 4, height: 90, backgroundColor: '#25343F', transform: `rotate(${-beamRot}deg)`, transformOrigin: 'top' }} />
            <div style={{
              position: 'absolute', right: -108, top: 100, width: 280, textAlign: 'center',
              transform: `rotate(${-beamRot}deg)`, transformOrigin: '50% -90px',
            }}>
              <div style={{ backgroundColor: 'rgba(37,52,63,0.92)', padding: '20px 20px', borderRadius: '0 0 130px 130px', border: '3px dashed #FF9B51' }}>
                <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 34, fontWeight: 900, color: '#FF9B51' }}>A WAREHOUSE OF DOLLARS</div>
                <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 700, color: '#F5F5F5', marginTop: 4 }}>CANNOT RAISE ITS PRICE</div>
              </div>
            </div>
          </div>
        </div>

        {/* Buffett 2011 letter quote — typewriter reveal */}
        <div style={{
          position: 'absolute', bottom: 90, left: 260, right: 260, textAlign: 'center',
          backgroundColor: 'rgba(37,52,63,0.95)', padding: '38px 60px', borderRadius: 18,
          borderLeft: '12px solid #FFF449', borderRight: '12px solid #FFF449',
          opacity: sp(260), transform: `translateY(${(1 - sp(260)) * 130}px)`,
          boxShadow: '0 24px 60px rgba(0,0,0,0.45)',
        }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 700, color: '#FF9B51', letterSpacing: 4, marginBottom: 12 }}>
            WARREN BUFFETT — 2011 SHAREHOLDER LETTER
          </div>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 50, fontWeight: 900, color: '#FFF449', lineHeight: 1.25, minHeight: 130 }}>
            {quote.slice(0, chars)}
            <span style={{ opacity: Math.sin(frame / 4) > 0 ? 1 : 0 }}>|</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 50%, rgba(0,0,0,0.52) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene20BuffettBalance;
```

